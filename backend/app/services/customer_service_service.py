import json
import asyncio
import os
import re
import sys
from time import perf_counter, sleep
from typing import Any, Awaitable, Callable

from datetime import datetime, timezone

from fastapi import HTTPException, status
from sqlalchemy.exc import OperationalError
from sqlalchemy.orm import Session

from ..core.config import settings
from ..core.database import release_session_connection
from ..models.knowledge_base import CustomerServiceConversation, CustomerServiceMessage, KnowledgeDocument
from ..models.product import Product
from ..models.product_qa import ProductQa, ProductQaNegative
from ..internal.experience_layer.tone_shaping import shape_answer_tone
from . import (
    customer_answer_coverage_contract,
    customer_enterprise_guardrail_service,
    customer_agent_intent_service,
    customer_agent_planner_service,
    customer_agent_quality_service,
    customer_agent_runtime_service,
    customer_agent_service,
    customer_agent_tool_service,
    customer_cache_service,
    customer_dialogue_state,
    customer_entity_resolution_contract,
    customer_evidence_bundle,
    customer_field_contract,
    customer_final_answer_arbiter,
    customer_llm_service,
    customer_perf_service,
    customer_recommendation_verification_contract,
    customer_structured_query_contract,
    knowledge_service,
    product_vector_index_service,
    product_service,
)
from ..models.product_business import ProductBusiness
from ..models.product_content import ProductContent
from ..models.product_specs import ProductSpecs
from ..models.product_associations import (
    Certification,
    ProductCertification,
    ProductListingChannel,
    ProductSalesRegion,
    ListingChannel,
    SalesRegion,
)


SKU_RE = re.compile(
    r"(?<![A-Za-z0-9])("
    r"(?:[A-Za-z]{1,6}[A-Za-z0-9]{0,12}(?:[-_](?:[A-Za-z0-9]{1,24}(?:\([A-Za-z0-9]{1,24}\))?|[\u4e00-\u9fff]{1,8}))+)"
    r"|(?:[A-Za-z]{1,6}\d{2,12}[A-Za-z0-9\u4e00-\u9fff]{0,12})"
    r")(?=$|[\s，。,；;：:）)\]】>\"'？?])"
)
MULTI_CATEGORY_CHILD_TIMEOUT_SECONDS = 45.0

INLINE_PAGE_CONTEXT_RE = re.compile(
    r"\s*[（(]\s*当前商品\s*[:：]\s*(?P<subject>.+?)\s*$"
)
COMPOSITE_RECOMMENDATION_HINTS = (
    "推荐",
    "建议",
    "适合",
    "最适合",
    "更适合",
    "哪款",
    "哪个",
    "选什么",
    "帮我选",
    "帮我挑",
)
COMPOSITE_FACT_RECOMMENDATION_MARKERS = (
    "顺便",
    "再推荐",
    "再帮我推荐",
    "另外推荐",
    "然后推荐",
    "也推荐",
    "推荐一个",
    "推荐一款",
    "推荐一下",
)

_STRUCTURED_UNKNOWN_FACT_TERMS: dict[str, tuple[str, ...]] = {
    "售后电话": ("售后服务电话", "售后联系电话", "售后电话", "客服电话", "客服热线"),
    "库存": ("库存", "现货", "有货", "还有货", "现在有货"),
    "销量": ("销量", "销量最高", "卖得最好"),
    "评价": ("评价", "客户评价", "好评", "评价最好"),
    "价格": ("价格", "售价", "多少钱", "什么价", "几块", "几元"),
    "保修": ("保修", "质保"),
    "认证": ("认证", "BPA", "BPA free", "FDA", "LFGB", "检测报告", "欧盟认证"),
    "官方说明书": ("说明书", "官方说明书"),
    "安装视频": ("安装视频", "安装教程", "视频教程"),
    "发货时效": ("发货", "多久发货", "发货时间", "几天到", "明天到", "今天到", "什么时候到", "配送", "发货地", "今天能发", "明天能到"),
}

_RESOLVED_ENTITY_UNKNOWN_FACT_TERMS: dict[str, tuple[str, ...]] = {
    **_STRUCTURED_UNKNOWN_FACT_TERMS,
    "优惠活动": ("优惠", "活动", "优惠活动", "优惠券", "券", "直播间有券", "直播间券", "会员价", "到手价", "促销", "满减", "折扣", "秒杀"),
    "赠品": ("赠品", "送什么", "送啥", "附赠", "赠送", "配赠", "礼品", "随单赠品", "随单送东西", "送东西"),
    "包邮": ("包邮", "免邮", "运费", "包邮地区"),
    "售后": ("售后", "售后怎么样", "售后政策", "售后服务", "退换货", "售后时效"),
}


def _apply_answer_coverage_contract(agent_result: dict) -> dict:
    """Apply explicit evidence coverage without re-reading customer wording.

    Semantic child requests are considered answered only when their retrieval
    path recorded supporting evidence.  This final boundary may append a
    missing-evidence statement for an unsupported request, but it must never
    infer a gap merely because the generated answer paraphrased that request.
    """
    metadata = (
        agent_result.get("answer_metadata")
        if isinstance(agent_result.get("answer_metadata"), dict)
        else {}
    )
    contract = customer_answer_coverage_contract.AnswerCoverageContract.from_dict(
        metadata.get("answer_coverage_contract")
    )
    if contract is None:
        return agent_result
    agent_result["answer"] = (
        customer_answer_coverage_contract.append_unsupported_boundaries(
            str(agent_result.get("answer") or ""),
            contract,
        )
    )
    return agent_result


def _complete_resolved_realtime_commercial_boundaries(question: str, agent_result: dict) -> dict:
    """Cover every explicitly requested unsupported realtime field.

    The semantic planner normally decomposes compound requests.  This final
    evidence boundary handles the provider occasionally returning only one of
    two explicit commercial fields, without inventing either value or
    replacing an otherwise natural answer.
    """
    if (
        not isinstance(agent_result, dict)
        or str(agent_result.get("answer_type") or "") != "product_detail"
        or len(agent_result.get("result_skus") or []) != 1
    ):
        return agent_result
    text = str(question or "")
    labels = [
        label
        for label, terms in _RESOLVED_ENTITY_UNKNOWN_FACT_TERMS.items()
        if label in _RESOLVED_ENTITY_REALTIME_COMMERCIAL_LABELS
        and any(term in text for term in terms)
    ]
    labels = list(dict.fromkeys(labels))
    if len(labels) < 2:
        return agent_result
    answer = str(agent_result.get("answer") or "").strip()
    if not any(term in answer for term in ("未", "没有", "无法", "不能")):
        return agent_result
    missing_labels = [
        label
        for label in labels
        if not any(term in answer for term in _RESOLVED_ENTITY_UNKNOWN_FACT_TERMS[label])
    ]
    if not missing_labels:
        return agent_result
    additions = [
        _resolved_entity_unknown_fact_answer("", label).strip()
        for label in missing_labels
    ]
    agent_result["answer"] = "\n".join([answer, *additions])
    metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    field_types = [
        customer_field_contract.field_type_from_detail_label(label)
        for label in labels
    ]
    metadata["contract_field_types"] = list(dict.fromkeys(field for field in field_types if field))
    metadata["compound_realtime_boundary_completed"] = True
    agent_result["answer_metadata"] = metadata
    return agent_result


def _preserve_recommendation_customer_context(question: str, agent_result: dict) -> dict:
    """Keep an explicit recommendation scenario in a fact-only fallback."""
    if not isinstance(agent_result, dict) or agent_result.get("answer_type") != "recommendation":
        return agent_result
    text = str(question or "")
    answer = str(agent_result.get("answer") or "").strip()
    if not answer or not any(term in text for term in ("送礼", "礼物", "赠送")):
        return agent_result
    if any(term in answer for term in ("送礼", "礼物", "精致", "情绪价值", "适合")):
        return agent_result
    agent_result["answer"] = f"送礼建议：{answer}"
    return agent_result


def _extract_inline_page_context(question: str) -> tuple[str, str]:
    """Split audit/client page metadata from the customer wording.

    The page subject is identity transport, never part of semantic intent.  It
    is intentionally strict: a non-unique historical title must not be turned
    into a fuzzy catalogue match.
    """
    text = str(question or "").strip()
    match = INLINE_PAGE_CONTEXT_RE.search(text)
    if not match:
        return text, ""
    # Marketplace exports can nest the product's own parentheses inside the
    # outer page marker, and some formats omit the outer closing bracket.
    # The explicit marker, rather than bracket balancing, owns this metadata.
    subject = str(match.group("subject") or "").strip().rstrip("）)").strip()
    return text[:match.start()].strip(), subject


def _resolve_exact_page_subject_sku(db: Session, subject: str) -> str | None:
    normalized_subject = customer_agent_service.normalize_search_text(subject).strip().lower()
    if not normalized_subject:
        return None
    # Marketplace page exports often retain only a category noun (for
    # example “套锅” or “气罐”).  Even if that noun happens to have one
    # containment match in the current catalogue, it is not a stable product
    # identity and must not seal the page to an arbitrary SKU.
    if normalized_subject in {
        "锅", "锅具", "套锅", "单锅", "炉", "炉具", "炉子", "气炉", "酒精炉", "卡式炉",
        "水壶", "水具", "水杯", "杯", "气罐", "燃料", "配件", "附件", "餐具",
    }:
        return None
    products = db.query(Product).all()
    exact_matches = [
        str(product.sku or "").strip()
        for product in products
        if customer_agent_service.normalize_search_text(
            str(product.product_name_cn or product.product_name_en or "")
        ).strip().lower() == normalized_subject
    ]
    if len(exact_matches) == 1:
        return exact_matches[0]
    if len(exact_matches) > 1:
        return None
    # Marketplace exports often shorten a canonical product name (for
    # example, “鸣泉” for “鸣泉水壶”).  A containment match is safe only when
    # it resolves to exactly one catalogue SKU; ambiguous category nouns such
    # as “水壶” therefore remain identity clarifications instead of widening
    # into a guessed product.
    if len(normalized_subject) < 2 or not re.search(r"[A-Za-z\u4e00-\u9fff]", normalized_subject):
        return None
    containment_matches = [
        str(product.sku or "").strip()
        for product in products
        if (
            (normalized_name := customer_agent_service.normalize_search_text(
                str(product.product_name_cn or product.product_name_en or "")
            ).strip().lower())
            and (normalized_subject in normalized_name or normalized_name in normalized_subject)
        )
    ]
    unique_matches = list(dict.fromkeys(item for item in containment_matches if item))
    return unique_matches[0] if len(unique_matches) == 1 else None


def _inline_page_subject_capacity_answer(question: str, page_subject: str) -> str | None:
    """Use only an explicitly printed capacity in an unresolved page title.

    This does not resolve a product or infer a catalogue field.  It merely
    reflects a numeric capacity that the customer-facing page label itself
    contains, while keeping the SKU-level packaging/effective-capacity caveat.
    """
    text = str(question or "").strip()
    subject = str(page_subject or "").strip()
    if not text or not subject:
        return None

    if not any(term in text for term in ("容量", "多少升", "几升", "多少毫升", "多大", "多大容量")):
        return None
    match = re.search(r"(?<![A-Za-z0-9])(?P<value>\d+(?:\.\d+)?)\s*(?P<unit>L|l|升|毫升|ML|ml)", subject)
    if not match:
        return None
    value = match.group("value")
    unit = match.group("unit")
    if unit.lower() == "ml":
        unit = "毫升"
    elif unit.lower() == "l":
        unit = "升"
    return (
        f"当前页面标题明确标注容量约为 {value}{unit}。"
        "但页面标题不等同于已锁定的 SKU 包装/有效容量；如需确认具体款式的容量，请提供页面 SKU 或完整型号。"
    )


def _inline_page_subject_bounded_answer(question: str, page_subject: str) -> str | None:
    """Answer a small set of page-title questions without inventing SKU facts.

    Marketplace exports often carry a useful but non-unique title.  The title
    must not unlock another catalogue item, but it also should not erase a
    generic safety boundary or a directly asked usage explanation.  These
    answers therefore state what cannot be verified and still address the
    customer's actual predicate.
    """
    text = str(question or "").strip()
    subject = str(page_subject or "").strip()
    if not text or not subject:
        return None

    if any(term in text for term in ("能用多久", "可以用多久", "用多久", "能烧多久", "使用时长", "续航", "燃烧时间")):
        return customer_agent_intent_service._compose_duration_usage_care_answer(text, [], [])

    if any(term in text for term in ("怎么装", "如何装", "怎么安装", "如何安装", "安装步骤", "安装视频")):
        return customer_agent_intent_service._compose_installation_usage_care_answer(text, [], [])

    if any(term in text for term in ("怎么清洁", "如何清洁", "怎么清洗", "如何清洗", "清洁", "清洗")):
        if any(term in subject for term in ("锅", "壶", "烤盘", "煎盘", "饭盒", "餐具", "杯")):
            return (
                "清洁建议：当前页面标题未唯一对应具体材质和表面处理，不能把其他商品的清洁步骤直接套用。"
                "首次使用可先用温水配合软布或软刷轻柔冲洗并彻底擦干；在确认同款说明书前，"
                "避免使用钢丝球、强腐蚀清洁剂或长时间浸泡，以免损伤涂层或表面处理。"
            )
        if not any(term in subject for term in ("炉", "炉头", "炉芯", "燃气", "卡式")):
            return (
                "清洁建议：当前页面标题未唯一对应具体商品，无法确认材质、涂层和可拆洗范围。"
                "请提供页面 SKU 或完整型号并按同款说明书清洁；未确认前先避免浸泡、硬物刮擦或强行拆卸。"
            )
        return (
            "清洁建议：当前页面标题未唯一对应具体炉具型号，不能把其他炉头的清洁步骤直接套用。"
            "请先关闭并移除燃料，待炉体完全冷却后用拧干的湿布擦拭，避开气路、阀门、接口和点火部件；"
            "如需拆洗或水洗，请按同款说明书确认，未确认前不要浸水或强行拆卸。"
        )

    if any(term in text for term in ("怎么使用", "如何使用", "怎么用", "如何用")) and any(
        term in text for term in ("炉子", "炉具", "炉头", "气炉", "酒精炉")
    ) and any(term in subject for term in ("水壶", "烧水壶")):
        return (
            f"页面商品是“{subject}”，不是已锁定型号的炉具，不能用这个页面标题回答炉子的使用步骤。"
            "请提供炉具页面 SKU 或完整型号，我再按同款说明书核对；在型号未确认前不要按其他炉具步骤点火。"
        )

    if _looks_like_ignition_capability_question(text) or any(
        term in text for term in ("需要手动点火", "需要手动打火", "手动点火吗", "自动点火吗")
    ):
        return (
            "点火方式结论：当前页面标题无法唯一对应具体炉具型号，资料不足以确认是否带电子/自动点火，"
            "也不足以确认是否需要手动点火；不能把‘只有一个火力开关’当作点火方式证据。请提供页面 SKU 或完整型号，"
            "再按同款说明书核对；未确认前不要反复点火，闻到燃气味或发现接口异常时立即停止操作。"
        )

    if any(term in text for term in ("怎么点火", "如何点火", "怎么打火", "如何打火")):
        fuel_hint = "酒精炉" in text or "酒精" in text
        if fuel_hint:
            return (
                "点火操作边界：当前页面标题与‘酒精炉’的具体型号未唯一对应，不能跨商品给出具体点火步骤。"
                "请按同款炉具和燃料说明书操作；确认炉体干燥、通风且没有残留燃料后再点火，"
                "不要在高温或有燃气/酒精味时反复尝试。"
            )
        return (
            "点火操作边界：当前页面标题无法唯一对应具体炉具型号，不能把其他卡式炉的点火步骤直接套用。"
            "请提供页面 SKU 或完整型号并按说明书操作；点火前确认阀门、气罐接口和周围通风，"
            "如有异味、漏气声或连接异常立即停止。"
        )

    if "针头" in text and "凹槽" in text:
        return (
            "安装提示：针头或定位销应对准同款炉盘上与之匹配的定位凹槽，不能凭外观随意选择或强行压入。"
            "当前页面标题未锁定具体炉盘结构，请提供页面 SKU、安装图或照片再确认具体凹槽；"
            "安装前先关闭阀门并确认接口没有异物。"
        )

    coating_terms = ("涂层", "不粘", "不沾", "粘不粘", "粘锅", "不粘锅", "不沾锅")
    if any(term in text for term in coating_terms):
        if "刷上油" in text or "刷油" in text or "烤肉" in text:
            return (
                "不粘结论：刷油只能减少食材粘连，不能证明锅具本身有不粘涂层；是否不粘要以同款 SKU 的表面处理标注为准。"
                "当前页面标题未唯一对应具体款式，无法确认涂层材质。请提供页面 SKU 或完整型号，"
                "并在确认前避免用硬物刮擦或空烧。"
            )
        return (
            "涂层结论：当前页面标题无法唯一对应具体 SKU，资料不足以确认是否有涂层、是否为零涂层或是否不粘。"
            "不能从‘锅具/水壶’名称或使用方式推断表面处理；请提供页面 SKU 或完整型号再核对，"
            "在确认前不要把它当作不粘涂层锅具使用。"
        )

    if any(term in text for term in ("材质", "材料")) and any(
        term in text for term in ("开水", "煲水", "烧水", "喝", "安全吗", "安全")
    ):
        return (
            "材质结论：当前页面只给出泛称，未锁定具体 SKU，暂时无法确认壶体材质或食品接触层。"
            "饮用煮沸水的安全性应以同款材质、涂层和食品接触用途标注为准，不能仅凭‘水壶’名称判断。"
            "请提供页面 SKU 或完整型号；未确认前不要把未标注食品接触用途的容器用于饮水。"
        )

    if "气罐" in text and any(
        term in text for term in ("能用", "可以用", "支持", "适配", "兼容", "能不能", "是否")
    ):
        return (
            "气罐兼容性结论：卡式炉只能使用同款资料和说明书明确支持的气罐接口，不能仅凭‘卡式炉’名称或接口外观判断。"
            "当前页面标题未唯一对应具体炉具型号，请提供页面 SKU 或完整型号再核对；未确认前不要强行连接或改用其他气罐。"
        )

    return None


def _explicit_page_subject_from_question(question: str) -> str:
    """Return a non-deictic subject stated before a formal field predicate.

    The field contract supplies the predicate span; this helper only removes
    grammatical connectors such as ``支持``/``可以``. It does not resolve a
    SKU or create a product alias, so an unresolved name remains unresolved.
    """
    text = str(question or "").strip()
    if not text:
        return ""
    selection = customer_field_contract.select_entity_subject_for_routing(raw_question=text)
    subject = str(getattr(selection, "entity_subject", "") or "").strip(" ，。？！；;：:")
    if not subject:
        return ""
    # A discourse fragment is not a competing product identity.  This covers
    # recommendation follow-ups ("还有别的品牌吗") and negative/capability
    # predicates that the entity selector can otherwise truncate to a token
    # such as "还有别" or "不".  The rule is grammatical and product-agnostic.
    if any(mark in subject for mark in ("?", "？", "!", "！", "。", "；", ";")):
        return ""
    if re.fullmatch(
        r"(?:还有|还有别|其他|其它|另外|再|再来|别的|另一|另一个)"
        r"(?:的)?(?:品牌|牌子|推荐|选择|款|款式)?",
        subject,
    ):
        return ""
    if re.match(r"^(?:看不见|看不到|找不到|没看到|没有看到|未看到)", subject):
        return ""
    # Exported customer turns frequently prepend a greeting or a sentence
    # such as “炉具收到了，问一下这个…”.  Those are discourse framing, not a
    # second product identity.  Strip them before applying the identity
    # conflict guard so a page-bound field question is not rejected merely
    # because the parser captured the preface.
    subject = re.sub(r"^(?:你好|您好)\s*[,，:：]?\s*", "", subject)
    subject = re.sub(
        r"^(?:炉具|锅具|商品|产品)\s*(?:收到了|收到|拿到了|拿到)\s*[,，:：]?\s*(?:问一下|请问|想问|想了解|想知道)?\s*",
        "",
        subject,
    )
    subject = re.sub(r"^(?:(?:再|在)?问一下|请问|想问|想了解|想知道)\s*", "", subject)
    if re.fullmatch(r"(?:我|我们|本人|自己).{0,12}(?:这|那)(?:一)?(?:套|个|款|件)", subject):
        return ""
    if re.fullmatch(r"(?:好像|似乎|可能)?(?:被|让)?.{0,12}(?:打湿|弄湿|淋湿|进水)(?:了)?", subject):
        return ""
    # The entity selector may capture the recommendation predicate instead of
    # a product name (for example “推荐一个最大火力的炉头”). Such fragments
    # are not competing page identities; let the category/recommendation route
    # keep the page as context without asking for a phantom SKU.
    if re.match(r"^(?:推荐|给我推荐|请推荐|帮我推荐|想推荐|最大|最\S*|那怎么|怎么|如何|为什么)", subject):
        return ""
    # A compound field question can make the entity selector return a piece of
    # the predicate (for example ``有便携带吗，壶``) instead of a product name.
    # When punctuation separates that predicate from the noun, keep a prefix
    # only if it is free of interrogative/field language; otherwise treat the
    # turn as page-deictic and let the page anchor or clarification guard own
    # identity. This is a grammar boundary, not a product-name whitelist.
    if any(separator in subject for separator in ("，", ",", "、")):
        segments = [part.strip() for part in re.split(r"[，,、]", subject) if part.strip()]
        if segments:
            prefix = segments[0]
            predicate_markers = (
                "有", "有没有", "便携", "容量", "尺寸", "多大", "多少", "材质", "材料", "涂层",
                "什么", "是否", "能不能", "可以", "支持", "适合", "推荐", "带不带", "带吗",
            )
            if any(marker in prefix for marker in predicate_markers):
                return ""
            subject = prefix
    subject = re.sub(
        r"(?:支持|可以|能不能|能否|是否|有没有|有无|适合|推荐(?:一下|一个|一款|几个|几款)?|能用|可以用|能|会|是|为|要用|用|放|装)$",
        "",
        subject,
    ).strip(" ，。？！；;：:")
    subject = re.sub(
        r"^(?:这个产品|这产品|那个产品|那产品|这个炉子|那个炉子|这炉子|那炉子|这款|那款|这个|那个|该商品|该产品|本产品|一个|一款|某款)\s*",
        "",
        subject,
    ).strip(" ，。？！；;：:")
    if not subject or subject in {
        "这个", "那个", "这款", "那款", "该产品", "这个产品", "这产品", "它", "他", "她",
        "不", "不是", "是", "没", "没有", "有", "还", "还有",
    }:
        return ""
    if subject in {
        "内部", "外部", "尺寸", "直径", "半径", "材质", "涂层", "容量", "大小", "高度", "宽度",
        "长度", "厚度", "颜色", "功能", "火力", "温度", "热源", "包装", "配件", "清洁", "安装",
        "使用", "壶", "锅头", "水具",
    }:
        return ""
    # Category nouns are not distinct product identities. A qualified name
    # such as ``围雪炉`` remains available for page-conflict protection.
    if subject in {
        "炉", "炉子", "炉具", "气炉", "卡式炉", "酒精炉", "固体酒精炉", "液体酒精炉", "炉头", "炉芯",
        "锅", "锅具", "单锅", "套锅", "水壶", "烧水壶", "水杯", "杯", "气罐", "气罐接头", "接头",
        "点火器", "点火装置", "配件", "燃料", "产品", "商品",
    }:
        return ""
    return subject


def _explicit_page_subject_conflicts_with_anchor(question: str, *, page_product_name: str) -> bool:
    """Detect a specific named subject that differs from a page product.

    This is an identity boundary, not a product/SKU rule: an explicit named
    subject wins over a page anchor only when the names are actually
    different; deictic and category-only wording keeps the page context.
    """
    subject = _explicit_page_subject_from_question(question)
    page_name = str(page_product_name or "").strip()
    if not subject or not page_name:
        return False
    normalized_subject = customer_agent_service.normalize_search_text(subject).strip().lower()
    normalized_page = customer_agent_service.normalize_search_text(page_name).strip().lower()
    if not normalized_subject or not normalized_page:
        return False
    return not (
        normalized_subject == normalized_page
        or normalized_subject in normalized_page
        or normalized_page in normalized_subject
    )


def _unresolved_page_context_result(subject: str) -> dict:
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": (
            f"当前页面显示的是“{subject}”，但现有商品资料无法唯一对应到具体款式。"
            "为避免把其他商品的信息当作这款回答，请提供页面 SKU 或完整型号后再核对。"
        ),
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "evidence": [],
        "debug": {"agent_mode": "unresolved_page_context_guard", "page_subject": subject},
        "answer_metadata": {"source": "unresolved_page_context_guard", "evidence_status": "not_applicable"},
        "skip_polish": True,
    }


def _page_context_recommendation_clarification_answer(
    *,
    page_name: str,
    page_sku: str,
    category: str,
    page_brand: str = "",
    other_brand_labels: list[str] | tuple[str, ...] = (),
) -> str:
    """Ask for a useful recommendation criterion while retaining page scope."""
    name = str(page_name or page_sku or "当前页面商品").strip()
    sku = str(page_sku or "").strip().upper()
    category_label = str(category or "商品").strip()
    same_category = f"同类{category_label}" if category_label else "同类商品"
    identity = f"{name}（{sku}）" if sku else name
    other_brands = [str(item or "").strip() for item in other_brand_labels if str(item or "").strip()]
    if page_brand and not other_brands:
        return (
            f"当前页面商品是{identity}。当前产品范围内的{same_category}均标注为“{page_brand}”，"
            "暂未找到其他品牌的可核验款式。若你接受同品牌其他款，我可以继续按热源、功率、重量、容量或预算筛选。"
        )
    if other_brands:
        return (
            f"当前页面商品是{identity}。当前资料中找到其他品牌的同类款式：{'、'.join(other_brands[:5])}。"
            "你可以补充热源、功率、重量、容量或预算，我再从这些同类款里筛选。"
        )
    return (
        f"当前页面商品是{identity}。如果你想看其他品牌或款式，我可以继续按{same_category}筛选；"
        "目前还需要确认产品范围和优先条件；只说“其他品牌”还不能确定你更看重热源、功率、重量、容量还是预算。"
        "请补充其中一项，我再给出可核验的同类推荐。"
    )


def _uncovered_compound_queries_for_output(
    answer: str,
    queries: list[str],
    answer_metadata: dict[str, Any] | None,
) -> list[str]:
    """Respect semantic coverage before falling back to textual checks.

    A natural answer rarely repeats a whole child question verbatim. Literal
    substring checks therefore cannot overrule a completed DeepSeek coverage
    audit. They remain only as a conservative compatibility fallback when no
    semantic coverage receipt exists.
    """
    cleaned_queries = [
        str(item or "").strip()
        for item in (queries or [])
        if str(item or "").strip()
    ]
    metadata = answer_metadata if isinstance(answer_metadata, dict) else {}
    if metadata.get("semantic_answer_coverage_complete") is True:
        return []
    normalized_answer = customer_agent_service.normalize_search_text(str(answer or ""))
    return [
        query
        for query in cleaned_queries[1:]
        if customer_agent_service.normalize_search_text(query) not in normalized_answer
    ]


_RESOLVED_ENTITY_REALTIME_COMMERCIAL_LABELS = {
    "库存",
    "销量",
    "评价",
    "价格",
    "保修",
    "优惠活动",
    "发货时效",
    "赠品",
    "包邮",
    "售后",
}
_SEMANTIC_UNKNOWN_REALTIME_SUBTYPES = {"unknown_realtime", "commercial_realtime"}


def _split_composite_customer_question(question: str) -> dict[str, str] | None:
    text = str(question or "").strip()
    if not text:
        return None
    fact_terms = ("酒精炉", "清洗", "保养", "比较", "对比", "区别", "材质", "容量", "重量", "认证", "卖点", "支持", "能不能", "能否")
    reverse = _split_reverse_composite_customer_question(text, fact_terms)
    if reverse:
        return reverse
    marker_pos: tuple[str, int] | None = None
    for marker in COMPOSITE_FACT_RECOMMENDATION_MARKERS:
        index = text.find(marker)
        if index > 0:
            marker_pos = (marker, index)
            break
    if marker_pos:
        _, index = marker_pos
    else:
        selection_match = re.search(
            r"(?:哪个|哪一个|哪款|哪一款)(?:产品)?更适合\s*(?:[1-9]\d?|[一二两三四五六七八九十])\s*(?:个)?人",
            text,
        )
        explicit_skus = list(dict.fromkeys(item.upper() for item in SKU_RE.findall(text)))
        if not selection_match or len(explicit_skus) < 2 or not any(term in text[:selection_match.start()] for term in ("比较", "对比")):
            return None
        index = selection_match.start()
    fact_part = text[:index].strip("。！？!? ，,；; ")
    recommendation_part = text[index:].strip("。！？!? ，,；; ")
    if not fact_part or not recommendation_part:
        return None
    if not any(term in recommendation_part for term in COMPOSITE_RECOMMENDATION_HINTS):
        return None
    if not any(term in fact_part for term in fact_terms):
        return None
    if any(term in fact_part for term in COMPOSITE_RECOMMENDATION_HINTS) and not any(term in fact_part for term in fact_terms):
        return None
    return {"fact_part": fact_part, "recommendation_part": recommendation_part, "order": "fact_first"}


def _split_reverse_composite_customer_question(text: str, fact_terms: tuple[str, ...]) -> dict[str, str] | None:
    if not any(term in text for term in COMPOSITE_RECOMMENDATION_HINTS):
        return None
    connectors = ("并说明", "同时问", "同时", "并且", "另外", "还要", "顺便", "最后")
    marker: tuple[str, int] | None = None
    for connector in connectors:
        index = text.find(connector)
        if index > 0:
            marker = (connector, index)
            break
    if not marker:
        return None
    connector, index = marker
    before = text[:index].strip("。！？!? ，,；;：: ")
    after = text[index + len(connector):].strip("。！？!? ，,；;：: ")
    if connector == "同时问" and "，" in after:
        head, tail = after.split("，", 1)
        if any(term in head for term in COMPOSITE_RECOMMENDATION_HINTS):
            before = f"{before}，{head}".strip("。！？!? ，,；;：: ")
            after = tail.strip("。！？!? ，,；;：: ")
    if not before or not after:
        return None
    if not any(term in before for term in COMPOSITE_RECOMMENDATION_HINTS):
        return None
    if not any(term in after for term in fact_terms):
        return None
    if not SKU_RE.search(after):
        return None
    return {"recommendation_part": before, "fact_part": after, "order": "recommendation_first"}


def _normalize_composite_recommendation_part(recommendation_part: str) -> str:
    text = str(recommendation_part or "").strip("。！？!? ，,；; ")
    if not text:
        return text
    for prefix in ("顺便", "再帮我", "再", "另外", "然后", "也", ""):
        candidate = text
        if prefix and not text.startswith(prefix):
            continue
        if prefix:
            candidate = text[len(prefix):].strip("。！？!? ，,；; ")
        for starter in ("推荐一个", "推荐一款"):
            if not candidate.startswith(starter):
                continue
            target = candidate[len(starter):].strip("。！？!? ，,；; ")
            if target.startswith(("更适合", "最适合", "适合")):
                return f"推荐{target}"
            if target:
                return f"推荐适合{target}"
    return text


def _merge_composite_answers(fact_answer: str, recommendation_answer: str) -> str:
    fact_answer = str(fact_answer or "").strip()
    recommendation_answer = str(recommendation_answer or "").strip()
    if fact_answer and recommendation_answer:
        return f"{fact_answer}\n\n{recommendation_answer}"
    return fact_answer or recommendation_answer


def _merge_supplemental_product_qa_into_field_answer(
    agent_result: dict,
    *,
    supplemental: dict | None,
    supplemental_query: str,
) -> dict:
    """Attach a second semantic product-QA intent after formal-field repair.

    Both the primary result and the optional QA result have already passed
    their own entity/evidence contracts.  This function never chooses a
    product or derives a fact from wording: it only preserves a same-SKU QA
    answer, or makes the independently requested QA part safely missing.
    """
    result_skus = [
        str(sku or "").strip().upper()
        for sku in (agent_result.get("result_skus") or [])
        if str(sku or "").strip()
    ]
    query = str(supplemental_query or "").strip()
    if not query or len(result_skus) != 1:
        return agent_result
    metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    if supplemental and supplemental.get("result_skus") == result_skus:
        extra = str(supplemental.get("answer") or "").strip()
        if extra:
            agent_result["answer"] = str(agent_result.get("answer") or "").strip() + "\n" + extra
            metadata["supplemental_product_qa"] = supplemental.get("answer_metadata") or {}
            agent_result["answer_metadata"] = metadata
            return agent_result
    agent_result["answer"] = (
        str(agent_result.get("answer") or "").strip()
        + f"\n关于“{query}”：当前同 SKU 资料未找到可直接确认的依据，无法确认。"
    )
    metadata["supplemental_product_qa"] = {
        "contract_field_type": "product_qa",
        "evidence_status": "missing",
        "evidence_skus": [],
        "requested_query": query,
        "resolved_sku": result_skus[0],
    }
    agent_result["answer_metadata"] = metadata
    return agent_result


async def _try_same_sku_structured_best_effort_answer(
    db: Session,
    *,
    question: str,
    safe_missing: dict,
    semantic_preplan: dict | None,
) -> dict | None:
    """Answer an open product question from directly relevant structured facts.

    DeepSeek decides relevance and wording. Deterministic code only supplies
    customer-visible fields for the already sealed SKU and validates that the
    resulting answer cites those exact facts.
    """
    if str((semantic_preplan or {}).get("evidence_kind") or "") != "product_qa":
        return None
    sku = str(safe_missing.get("sku") or "").strip().upper()
    if not sku:
        sealed_skus = {
            str(item or "").strip().upper()
            for item in (safe_missing.get("result_skus") or [])
            if str(item or "").strip()
        }
        sealed_skus.update(
            str(row.get("sku") or "").strip().upper()
            for row in (safe_missing.get("results") or [])
            if isinstance(row, dict) and str(row.get("sku") or "").strip()
        )
        if len(sealed_skus) == 1:
            sku = next(iter(sealed_skus))
    if not sku:
        return None
    product, specs, business, content = _phase1_product_bundle_by_ref(db, sku)
    if product is None or str(product.sku or "").strip().upper() != sku:
        return None
    excluded_fields = {
        "sku", "barcode", "product_name_cn", "product_name_en",
        "content_title", "content_description", "bullet_points",
        "search_keywords", "manual", "inventory", "price",
    }
    raw_items: list[dict[str, Any]] = []
    for field in sorted(customer_field_contract.FORMAL_DETAIL_FIELDS):
        if field in excluded_fields or not customer_field_contract.is_supported_detail_field(field):
            continue
        value, source = _structured_product_field_evidence(
            field,
            db=db,
            product=product,
            specs=specs,
            business=business,
            content=content,
        )
        if not value or not source:
            continue
        raw_items.append({
            "evidence_id": customer_evidence_bundle.stable_customer_evidence_id(
                namespace="structured",
                sku=sku,
                value=f"{field}|{value}",
            ),
            "sku": sku,
            "source_type": "structured_field",
            "source": str(source),
            "field": field,
            "value": str(value)[:800],
            "visibility": customer_evidence_bundle.CUSTOMER_VISIBLE,
        })
    bundle = customer_evidence_bundle.build_customer_evidence_bundle(
        sku=sku,
        product_name=product.product_name_cn or product.product_name_en or sku,
        evidence_items=raw_items,
    )
    if not bundle.items:
        return None
    candidates = [
        {
            "evidence_id": item.evidence_id,
            "field": item.field,
            "field_label": customer_field_contract.product_detail_field_label(item.field) or item.field,
            "value": item.value,
        }
        for item in bundle.items
    ]
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        raw = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "Return only JSON: {answer:string,evidence_ids:string[],evidence_quotes:string[]}. "
                        "Answer the complete customer question in concise, direct, friendly Chinese only when one or more supplied same-product "
                        "structured facts directly support it. You may combine multiple directly relevant "
                        "facts for a bounded suitability judgement, but never infer safety, durability, "
                        "compatibility, performance, ease of use, or a guarantee from merely adjacent facts. "
                        "Use NO_EVIDENCE when the facts are only related rather than sufficient. "
                        "Start with the customer's actual conclusion, without a greeting, preface, or phrases such as '请参考以下说明'. "
                        "For a safety or operating question, state both the permitted next action and the prohibited action when the supplied facts support them. "
                        "Use only supplied facts, keep uncertainty explicit, and copy one to three literal "
                        "evidence quotes of at most 60 characters."
                    ),
                },
                {
                    "role": "user",
                    "content": json.dumps(
                        {"question": question, "same_sku": sku, "candidates": candidates},
                        ensure_ascii=False,
                    ),
                },
            ],
            temperature=0,
            max_tokens=min(int(runtime["max_tokens"]), 360),
            purpose="same_sku_structured_best_effort_answer",
            api_model_override=runtime["model"],
            response_format=runtime["response_format"],
            thinking=runtime["thinking"],
        )
    except Exception:
        return None
    payload = customer_agent_planner_service._extract_json_object(str(raw or "")) or {}
    answer = str(payload.get("answer") or "").strip()
    evidence_ids = payload.get("evidence_ids")
    if not answer or answer == "NO_EVIDENCE" or not isinstance(evidence_ids, list) or not evidence_ids:
        return None
    selected_id_set = {str(item or "").strip() for item in evidence_ids if str(item or "").strip()}
    selected_items = [item for item in bundle.items if item.evidence_id in selected_id_set]
    if len(selected_items) != len(selected_id_set):
        return None
    evidence_content = "\n".join(item.value for item in selected_items)
    if (
        not _same_sku_rag_answer_has_selected_quotes(payload, evidence_content)
        or not await _same_sku_rag_answer_is_grounded_after_quote_validation(
            db, question, answer, payload, evidence_content
        )
    ):
        return None
    result = dict(safe_missing)
    result.update({
        "answer": answer,
        "confidence": "high",
        "uncertainty": "resolved",
        "evidence": [
            {
                "evidence_id": item.evidence_id,
                "sku": sku,
                "field_label": customer_field_contract.product_detail_field_label(item.field) or item.field,
                "value": item.value,
                "source_type": "structured_field",
                "source_layer": "structured",
                "matched_by": "semantic_same_sku_structured_evidence",
            }
            for item in selected_items
        ],
        "sources": [{"type": "product_field", "label": "同 SKU 商品资料", "sku": sku}],
        "answer_metadata": {
            "contract_field_type": "product_qa",
            "evidence_status": "matched",
            "evidence_sku": sku,
            "evidence_skus": [sku],
            "evidence_bundle_skus": [sku],
            "evidence_ids": [item.evidence_id for item in selected_items],
            "evidence_source": "same_sku_structured_best_effort",
        },
        # The bounded writer has already produced customer-facing prose and the
        # answer has passed an independent grounding check. Avoid a second LLM
        # pass that only adds a generic preface and extra latency.
        "skip_polish": True,
    })
    result.setdefault("debug", {})["agent_mode"] = "sealed_same_sku_structured_best_effort"
    return result


def _sealed_context_anchor_sku_conflict(question: str, anchor_sku: str, agent_result: dict) -> bool:
    """Reject a fallback that re-resolves a deictic turn outside its sealed SKU."""
    text = str(question or "").strip()
    if (
        _has_explicit_sku_text(text)
        or _looks_like_ordinal_product_field_followup(text)
        or not re.search(r"(?:它|这个|这款|该商品|该产品|本产品)", text)
    ):
        return False
    expected = str(anchor_sku or "").strip().upper()
    returned = {
        str(sku or "").strip().upper()
        for sku in (agent_result.get("result_skus") or [])
        if str(sku or "").strip()
    }
    return bool(expected and returned and returned != {expected})


def _apply_sealed_context_anchor_cross_sku_guard(
    db: Session,
    *,
    user_id: str,
    question: str,
    conversation_id: str | None,
    agent_result: dict,
) -> dict:
    """Keep a deictic single-product turn inside its persisted SKU boundary."""
    persisted_anchor_sku = _latest_persisted_agent_context_sku(db, conversation_id)
    saved_anchor_sku = persisted_anchor_sku
    if not saved_anchor_sku:
        saved_anchor_context = _latest_recommendation_context_for_sources(db, conversation_id)
        saved_anchor_sku = str((saved_anchor_context or {}).get("active_single_product_anchor") or "").strip().upper()
    anchor_skus = [saved_anchor_sku] if saved_anchor_sku else _latest_active_product_skus(db, conversation_id, user_id)
    latest_candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    has_ordered_multi_product_context = len(_ordered_context_skus(latest_candidate_context)) >= 2
    if (
        (has_ordered_multi_product_context and not persisted_anchor_sku)
        or len(anchor_skus) != 1
        or not _sealed_context_anchor_sku_conflict(question, anchor_skus[0], agent_result)
    ):
        return agent_result
    anchored_sku = anchor_skus[0]
    product = db.query(Product).filter(Product.sku == anchored_sku).first()
    if product is None:
        return agent_result
    decision = customer_entity_resolution_contract.SingleProductResolutionDecision(
        allowed=True,
        resolved_sku=anchored_sku,
        resolved_product_id=str(product.id),
        reason="sealed_context_anchor_rejected_cross_sku_result",
        match_level="sealed_context_anchor",
    )
    return _build_resolved_product_unknown_field_result(
        db,
        product,
        label="配件",
        source="sealed_context_anchor_cross_sku_guard",
        resolution_decision=decision,
    )


def _apply_request_sku_anchor_guard(
    db: Session,
    *,
    question: str,
    agent_result: dict,
    request_anchor_sku: str | None,
    phase1_plan: dict | None,
) -> dict:
    """Keep a product-page SKU from being lost by an earlier route.

    The API SKU identifies the product page, but does not classify the
    customer's field or turn a recommendation request into a product-detail
    request. This final boundary only rejects a conflicting or identity-losing
    detail response and replaces it with a same-SKU evidence-safe result.
    """
    anchor_sku = _resolve_exact_sku_variant(db, request_anchor_sku)
    if not anchor_sku or not isinstance(agent_result, dict):
        return agent_result
    if (
        (
            _looks_like_recommendation_request(question)
            and not _looks_like_page_current_item_availability_question(question)
        )
        or _looks_like_page_bundle_inclusion_question(question)
        or _looks_like_bundle_heat_source_question(question)
        or customer_agent_runtime_service._is_underspecified_recommendation_question(question)
    ):
        return agent_result

    product = db.query(Product).filter(Product.sku == anchor_sku).first()
    if product is None:
        return agent_result

    # A page SKU is only an identity anchor.  It must not overwrite a
    # category-level catalogue decision that was already scoped and composed
    # by the semantic catalogue route.  This matters for questions such as
    # "where can I buy a safe solid-alcohol stove?" while the customer happens
    # to be browsing a gas-stove page: the catalogue route may return verified
    # alcohol-stove candidates (or a bounded no-match conclusion), and the
    # final page guard must preserve that meaning instead of reclassifying it
    # as usage care for the page SKU.
    answer_type = str(agent_result.get("answer_type") or "").strip()
    answer_metadata = agent_result.get("answer_metadata")
    catalogue_source = ""
    if isinstance(answer_metadata, dict):
        catalogue_source = str(answer_metadata.get("source") or "").strip().lower()
    if (
        answer_type in {"recommendation", "comparison", "product_query"}
        and ("catalogue" in catalogue_source or "category" in catalogue_source)
        and not _looks_like_page_current_item_availability_question(question)
    ):
        return agent_result

    if _looks_like_page_current_item_availability_question(question):
        component = next(
            (
                term
                for term in ("炉头", "炉芯", "配件", "水壶", "锅", "炉具")
                if term in question
            ),
            "该配件",
        )
        page_name = str(product.product_name_cn or product.product_name_en or anchor_sku).strip()
        page_result = _build_bounded_clarification_result(
            answer=(
                f"{page_name}（{anchor_sku}）：当前同 SKU 资料未标注是否包含或单独提供“{component}”，"
                "不能把其他炉具或目录中的配件当作这款商品的随附部件。请按该 SKU 包装清单或配件说明核对。"
            ),
            source="page_current_item_availability_scope_guard",
        )
        page_result["sku"] = anchor_sku
        page_result["result_skus"] = []
        page_result["candidate_skus"] = []
        page_result["results"] = []
        page_debug = page_result.get("debug") if isinstance(page_result.get("debug"), dict) else {}
        page_debug["agent_mode"] = "page_current_item_availability_scope_guard"
        page_debug["request_sku_anchor"] = anchor_sku
        page_result["debug"] = page_debug
        return page_result

    usage_subtype = customer_agent_intent_service._detect_usage_care_subtype(question)
    if (
        _is_product_usage_care_question(question)
        and not _looks_like_recommendation_request(question)
        and not _looks_like_compatibility_usage_question(question)
        and usage_subtype in {"safety", "duration", "power_tradeoff", "installation", "full_unit_wash", "composition"}
    ):
        # A legacy/page-field route may have already produced a product-detail
        # card by the time this final identity boundary runs.  Recompose only
        # the bounded usage/safety subtype here; otherwise the page SKU would
        # turn an operational question such as “气罐能用多久” into a heat-source
        # or capacity field dump.
        if usage_subtype == "duration":
            bounded_answer = customer_agent_intent_service._compose_duration_usage_care_answer(question, [], [])
        elif usage_subtype == "power_tradeoff":
            bounded_answer = customer_agent_intent_service._compose_power_tradeoff_usage_care_answer(question, [], [])
        elif usage_subtype == "installation":
            bounded_answer = customer_agent_intent_service._compose_installation_usage_care_answer(question, [], [])
        elif usage_subtype == "full_unit_wash":
            bounded_answer = customer_agent_intent_service._compose_full_unit_wash_usage_care_answer(question)
        elif usage_subtype == "composition":
            bounded_answer = customer_agent_intent_service._compose_usage_care_composition_answer(question, [], [])
        else:
            bounded_answer = customer_agent_intent_service._compose_safety_usage_care_answer(question)
        if usage_subtype in {"duration", "power_tradeoff"}:
            bounded_answer = re.sub(
                r"请提供炉具型号或 SKU，我再按同款资料核对。?",
                "当前页面商品身份已明确；如能补充气罐规格、火力档位或实际负载，我再继续核对。",
                bounded_answer,
            )
        bounded_answer = re.sub(
            r"请提供炉具 SKU 与气罐型号，再核对接口和说明书。?",
            "当前页面商品已明确；请补充气罐型号，再核对接口和说明书。",
            bounded_answer,
        )
        if customer_agent_intent_service._looks_like_general_fuel_definition_question(question):
            bounded_answer = re.sub(
                r"如果要核对某款炉具能否使用液体酒精，请提供具体商品名或 SKU，我再按同款热源标注确认。?",
                "当前页面商品身份已明确；请按该商品说明书的热源标注核对，不能把医用酒精默认当作炉具燃料。",
                bounded_answer,
            )
        if anchor_sku:
            bounded_answer = re.sub(
                r"请提供具体炉具 SKU，并",
                "当前页面商品已明确；请",
                bounded_answer,
            )
            bounded_answer = re.sub(
                r"请提供具体炉具型号或 SKU，并",
                "当前页面商品已明确；请",
                bounded_answer,
            )
        bound_result = dict(agent_result)
        bound_result["intent"] = "product_usage_care"
        bound_result["answer_type"] = "product_usage_care"
        bound_result["answer"] = f"{product.product_name_cn or product.product_name_en or anchor_sku}（{anchor_sku}）\n{bounded_answer}"
        bound_result["sku"] = anchor_sku
        bound_result["result_skus"] = [anchor_sku]
        bound_result["candidate_skus"] = [anchor_sku]
        # Scope-guard clarifications retain the page identity in the answer,
        # but they are not catalogue results and must not render a product
        # card for the unrelated accessory being asked about.
        if catalogue_source.startswith("page_") and catalogue_source.endswith("_scope_guard"):
            bound_result["result_skus"] = []
            bound_result["candidate_skus"] = []
            bound_result["results"] = []
        bound_debug = bound_result.get("debug") if isinstance(bound_result.get("debug"), dict) else {}
        bound_debug["agent_mode"] = "request_sku_anchor_usage_care_recompose"
        bound_debug["request_sku_anchor"] = anchor_sku
        bound_debug["usage_care_subtype"] = usage_subtype
        bound_result["debug"] = bound_debug
        return bound_result

    if "气罐桌边夹" in question and str(agent_result.get("answer_type") or "") in {"clarification", "product_usage_care", "product_detail"}:
        bound_result = dict(agent_result)
        bound_result["sku"] = anchor_sku
        bound_result["result_skus"] = [anchor_sku]
        bound_result["candidate_skus"] = [anchor_sku]
        # A page scope guard is a bounded clarification, not a catalogue
        # result.  Keep the page SKU as identity in ``sku``/the answer, but
        # do not expose it as a recommendation/detail card for an unrelated
        # accessory question such as "有没有卖气罐".  This boundary is based
        # on route provenance, not on any product/SKU-specific rule.
        if catalogue_source.startswith("page_") and catalogue_source.endswith("_scope_guard"):
            bound_result["result_skus"] = []
            bound_result["candidate_skus"] = []
            bound_result["results"] = []
        answer_text = str(bound_result.get("answer") or "").strip()
        product_name = str(product.product_name_cn or product.product_name_en or anchor_sku).strip()
        if product_name not in answer_text and anchor_sku not in answer_text:
            bound_result["answer"] = f"{product_name}（{anchor_sku}）\n{answer_text}"
        bound_debug = bound_result.get("debug") if isinstance(bound_result.get("debug"), dict) else {}
        bound_debug["agent_mode"] = "request_sku_anchor_table_clip_scope"
        bound_debug["request_sku_anchor"] = anchor_sku
        bound_result["debug"] = bound_debug
        return bound_result

    # A page-bound compatibility question may deliberately have no retrieval
    # SKU: the page SKU is the identity, while the compatibility conclusion is
    # conservative because the field is unverified.  Do not replace that
    # customer-facing conclusion with a generic missing-field card; bind the
    # known page identity to the usage-care response instead.
    if (
        str(agent_result.get("answer_type") or "") == "product_usage_care"
        and (
            _looks_like_compatibility_usage_question(question)
            or customer_agent_intent_service._detect_usage_care_subtype(question)
            in {"duration", "power_tradeoff"}
            or _is_product_usage_care_question(question)
            or customer_agent_intent_service._looks_like_usage_care_question(question)
        )
    ):
        bound_result = dict(agent_result)
        bound_result["sku"] = anchor_sku
        bound_result["result_skus"] = [anchor_sku]
        bound_result["candidate_skus"] = [anchor_sku]
        # A page scope guard is a bounded clarification, not a catalogue
        # result. Keep the page SKU as identity in ``sku``/the answer, but do
        # not expose it as a recommendation/detail card for an unrelated
        # accessory question. This is route-provenance based, not SKU based.
        if catalogue_source.startswith("page_") and catalogue_source.endswith("_scope_guard"):
            bound_result["result_skus"] = []
            bound_result["candidate_skus"] = []
            bound_result["results"] = []
        answer_text = str(bound_result.get("answer") or "").strip()
        product_name = str(product.product_name_cn or product.product_name_en or anchor_sku).strip()
        if customer_agent_intent_service._detect_usage_care_subtype(question) in {"duration", "power_tradeoff"}:
            answer_text = re.sub(
                r"请提供炉具型号或 SKU，我再按同款资料核对。?",
                "当前页面商品身份已明确；如能补充气罐规格、火力档位或实际负载，我再继续核对。",
                answer_text,
            )
        if answer_text and product_name not in answer_text and anchor_sku not in answer_text:
            bound_result["answer"] = f"{product_name}（{anchor_sku}）\n{answer_text}"
        else:
            bound_result["answer"] = answer_text
        bound_debug = bound_result.get("debug") if isinstance(bound_result.get("debug"), dict) else {}
        bound_debug["agent_mode"] = "request_sku_anchor_usage_care_bind"
        bound_debug["request_sku_anchor"] = anchor_sku
        bound_result["debug"] = bound_debug
        return bound_result

    # A page SKU is context, not permission to reinterpret an explicitly
    # named different product subject.  For example, asking for a large pot,
    # windscreen, or teapot while browsing a stove page must not be converted
    # into a missing-field answer for the stove.
    explicit_target_category = _structured_target_category_from_question(question)
    if explicit_target_category and _product_category_domain(product.category) != _product_category_domain(explicit_target_category):
        return agent_result

    returned_skus = {
        str(value or "").strip().upper()
        for value in [
            *(agent_result.get("result_skus") or []),
            agent_result.get("sku"),
        ]
        if str(value or "").strip()
    }
    field_contract = resolve_requested_field_contract(question, phase1_plan)
    field_type = str(field_contract.get("field_type") or "").strip()
    asks_for_identity_again = bool(re.search(
        r"(?:\u63d0\u4f9b|\u8f93\u5165).{0,16}(?:SKU|\u5546\u54c1\u540d)|\u8bf7\u5148\u8bf4\u660e\u4f60\u6307\u7684\u54ea\u6b3e",
        str(agent_result.get("answer") or ""),
        flags=re.IGNORECASE,
    ))
    has_cross_sku = bool(returned_skus and anchor_sku.upper() not in returned_skus)
    # A valid page SKU already resolves identity. Even a capability question
    # outside the formal field ontology must never ask the customer to send
    # that identity again or return a generic answer that has no product
    # identity/evidence at all.
    answer_text = str(agent_result.get("answer") or "")
    product_name = str(product.product_name_cn or product.product_name_en or "").strip()
    names_current_product = bool(
        anchor_sku.upper() in answer_text.upper()
        or (product_name and product_name in answer_text)
    )
    has_lost_identity = bool(
        not returned_skus
        and (asks_for_identity_again or not names_current_product)
    )
    if not (has_cross_sku or has_lost_identity):
        return agent_result

    label = customer_field_contract.product_detail_field_label(field_type) or "相关"
    decision = customer_entity_resolution_contract.SingleProductResolutionDecision(
        allowed=True,
        resolved_sku=anchor_sku,
        resolved_product_id=str(product.id),
        reason="request_sku_anchor_guard",
        match_level="request_sku_anchor",
    )
    result = _build_resolved_product_unknown_field_result(
        db,
        product,
        label=label,
        source="request_sku_anchor_guard",
        resolution_decision=decision,
    )
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    debug["agent_mode"] = "request_sku_anchor_guard"
    debug["request_sku_anchor"] = anchor_sku
    debug["rejected_result_skus"] = sorted(returned_skus)
    result["debug"] = debug
    return result


def _is_terminal_gifting_evidence_boundary(result: dict | None) -> bool:
    debug = result.get("debug") if isinstance(result, dict) and isinstance(result.get("debug"), dict) else {}
    return bool(
        str(debug.get("agent_mode") or "") == "sealed_product_qa_safe_missing"
        and debug.get("gifting_evidence_boundary") is True
    )


def _enforce_terminal_gifting_evidence_boundary(
    result: dict,
    *,
    bounded_answer: str,
) -> dict:
    """Keep an unsupported marketing judgement terminal through finalization."""
    result["answer"] = str(bounded_answer or result.get("answer") or "").strip()
    result["confidence"] = "medium"
    result["uncertainty"] = "missing_data"
    result["evidence"] = []
    result["sources"] = []
    metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    for key in (
        "evidence_source",
        "evidence_bundle_skus",
        "evidence_ids",
        "evidence_field",
        "field_evidence_match",
        "semantic_answer_coverage_complete",
    ):
        metadata.pop(key, None)
    metadata.update({
        "contract_field_type": "product_qa",
        "requested_field": "产品问答",
        "evidence_status": "missing",
        "field_evidence_missing": True,
        "evidence_skus": [],
        "answer_policy": "insufficient_evidence",
    })
    result["answer_metadata"] = metadata
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    debug["agent_mode"] = "sealed_product_qa_safe_missing"
    debug["gifting_evidence_boundary"] = True
    result["debug"] = debug
    result["skip_polish"] = True
    return result


async def _save_agent_result_and_return(
    db: Session,
    *,
    user_id: str,
    question: str,
    conversation_id: str | None,
    agent_result: dict,
    request_start: float,
    branch: str,
    semantic_preplan: dict | None = None,
) -> dict:
    stage_start = perf_counter()
    agent_result = _apply_sealed_context_anchor_cross_sku_guard(
        db,
        user_id=user_id,
        question=question,
        conversation_id=conversation_id,
        agent_result=agent_result,
    )
    original_debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    persisted_plan = original_debug.get("plan") if isinstance(original_debug.get("plan"), dict) else {}
    request_sku_anchor = str(
        persisted_plan.get("request_sku_anchor")
        or (semantic_preplan or {}).get("request_sku_anchor")
        or ""
    ).strip()
    agent_result = _apply_request_sku_anchor_guard(
        db,
        question=question,
        agent_result=agent_result,
        request_anchor_sku=request_sku_anchor,
        phase1_plan=persisted_plan,
    )
    original_debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    persisted_plan = original_debug.get("plan") if isinstance(original_debug.get("plan"), dict) else persisted_plan
    effective_semantic_preplan = semantic_preplan or persisted_plan.get("semantic_preplan") or {}
    terminal_gifting_boundary = _is_terminal_gifting_evidence_boundary(agent_result)
    terminal_gifting_answer = str(agent_result.get("answer") or "").strip() if terminal_gifting_boundary else ""
    if (
        str(original_debug.get("agent_mode") or "") == "sealed_product_qa_safe_missing"
        and not terminal_gifting_boundary
    ):
        recovered = await _try_same_sku_structured_best_effort_answer(
            db,
            question=question,
            safe_missing=agent_result,
            semantic_preplan=effective_semantic_preplan,
        )
        if recovered is not None:
            agent_result = recovered
            original_debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        else:
            rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
            if rows:
                row = rows[0]
                sku = str(row.get("sku") or "").strip().upper()
                name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
                agent_result["answer"] = _safe_missing_product_qa_answer(name, sku, question)
            agent_result["skip_polish"] = True
    supplemental_query = str((effective_semantic_preplan or {}).get("supplemental_qa_evidence_query") or "").strip()
    if not ((agent_result.get("debug") or {}).get("timing")) or not ((agent_result.get("answer_metadata") or {}).get("timing")):
        existing_debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        existing_plan = existing_debug.get("plan") if isinstance(existing_debug.get("plan"), dict) else {}
        agent_result = _attach_phase1_plan_and_timing(
            agent_result,
            existing_plan,
            _phase1_timing(request_start=request_start),
        )
    agent_result = _ensure_debug_plan_raw_question(agent_result, question)
    agent_result = _finalize_answer(agent_result)
    agent_result = _attach_semantic_preplan_debug(
        agent_result,
        semantic_preplan,
        final_route=str(agent_result.get("answer_type") or agent_result.get("intent") or ""),
    )
    # Explicit single-product field answers must not be completed with unrelated QA or selling points.
    agent_result = _enforce_field_evidence_policy(db, question, agent_result)
    agent_result = _complete_resolved_realtime_commercial_boundaries(question, agent_result)
    agent_result = await _semantic_render_structured_usage_answer(
        db,
        question=question,
        agent_result=agent_result,
        semantic_preplan=effective_semantic_preplan,
    )
    if terminal_gifting_boundary:
        agent_result = _enforce_terminal_gifting_evidence_boundary(
            agent_result,
            bounded_answer=terminal_gifting_answer,
        )
    final_debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    final_entity_contract = (
        final_debug.get("entity_resolution_contract")
        if isinstance(final_debug.get("entity_resolution_contract"), dict)
        else {}
    )
    if (
        str(final_debug.get("agent_mode") or "") == "recommendation_context_product_field"
        and final_entity_contract.get("status") == "resolved"
        and str(final_entity_contract.get("resolved_sku") or "").strip()
    ):
        final_debug.setdefault("binding_provenance", "resolved_entity_contract")
        agent_result["debug"] = final_debug
    field_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    if (
        str(field_metadata.get("contract_field_type") or "").strip() == "accessories"
        and field_metadata.get("field_evidence_missing") is True
        and len(agent_result.get("result_skus") or []) == 1
    ):
        accessory_preplan = dict(effective_semantic_preplan or {})
        accessory_preplan.update({
            "route_family": "product_bound_qa",
            "route_hint": "product_detail",
            "question_type": "field",
            "subject_text": str(agent_result.get("result_skus")[0]),
            "canonical_fields": [],
            "field_type": "",
            "field_hint": None,
            "evidence_required": True,
            "evidence_kind": "product_qa",
            "qa_evidence_query": question,
            "fallback_reason": "",
        })
        accessory_qa_result = await _try_product_qa_shortcut_with_semantic_selection(
            db,
            question,
            phase1_plan={"semantic_preplan": accessory_preplan},
        )
        if accessory_qa_result and accessory_qa_result.get("result_skus") == agent_result.get("result_skus"):
            accessory_qa_result.setdefault("answer_metadata", {}).update({
                "contract_field_type": "accessories",
                "evidence_field": "accessories",
                "field_evidence_match": True,
                "field_evidence_missing": False,
                "evidence_status": "matched",
            })
            agent_result = accessory_qa_result
    compound_queries = [] if terminal_gifting_boundary else [
        str(item or "").strip()
        for item in ((effective_semantic_preplan or {}).get("qa_evidence_queries") or [])
        if str(item or "").strip()
    ]
    semantic_subject = customer_agent_service.normalize_search_text(
        str((effective_semantic_preplan or {}).get("subject_text") or "")
    ).lower()
    if (
        semantic_subject
        and len(compound_queries) > 1
        and all(semantic_subject in customer_agent_service.normalize_search_text(query).lower() for query in compound_queries)
    ):
        # Repeated subject-bearing rewrites are an overview decomposition, not
        # independently answerable product facts.
        compound_queries = []
    compound_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    if (
        compound_queries
        and len(agent_result.get("result_skus") or []) == 1
        and str(compound_metadata.get("contract_field_type") or "").strip() == "product_qa"
    ):
        # The final save boundary is the last place before a safe-missing
        # response becomes user-visible. Re-run only the already semantic,
        # independently scoped queries under the same resolved SKU; never
        # reinterpret the question here.
        child_answers: list[dict] = []
        missing_child_queries: list[str] = []
        answered_child_requests: list[tuple[str, str]] = []
        child_evidence_ids: set[str] = set()
        for query in compound_queries:
            child_preplan = dict(effective_semantic_preplan)
            child_preplan.update({
                "qa_evidence_query": query,
                "qa_evidence_queries": [],
                "compound": False,
                "strict_compound_child_qa": True,
            })
            child_question = " ".join(item for item in (str(child_preplan.get("subject_text") or "").strip(), query) if item) or question
            child_result = await _try_product_qa_shortcut_with_semantic_selection(
                db, child_question, phase1_plan={"semantic_preplan": child_preplan},
            )
            if _is_sealed_product_qa_safe_missing(child_result):
                child_result = None
            if not child_result:
                child_result = await _try_sealed_same_sku_knowledge_answer(
                    db, child_question, {"semantic_preplan": child_preplan},
                )
            if child_result:
                child_meta = child_result.get("answer_metadata") if isinstance(child_result.get("answer_metadata"), dict) else {}
                child_evidence_id = str(child_meta.get("evidence_source") or "").strip()
                # A strict child selector must not reuse the sibling QA row.
                # Treat a duplicate evidence id as uncovered instead of
                # repeating an answer for a different sub-question.
                if child_evidence_id and child_evidence_id in child_evidence_ids:
                    missing_child_queries.append(query)
                else:
                    if child_evidence_id:
                        child_evidence_ids.add(child_evidence_id)
                    child_answers.append(child_result)
                    answered_child_requests.append((
                        query,
                        child_evidence_id
                        or str(child_meta.get("source") or "same_sku_child_evidence"),
                    ))
            else:
                missing_child_queries.append(query)
        if child_answers:
            if _is_sealed_product_qa_safe_missing(agent_result):
                agent_result = child_answers.pop(0)
            base_answer = str(agent_result.get("answer") or "").strip()
            extra_answers = []
            seen_answer_texts = {base_answer} if base_answer else set()
            for index, item in enumerate(child_answers):
                extra = str(item.get("answer") or "").strip()
                if extra and extra in seen_answer_texts:
                    if index + 1 < len(compound_queries):
                        missing_child_queries.append(compound_queries[index + 1])
                    continue
                if extra and extra not in base_answer and base_answer not in extra:
                    extra_answers.append(extra)
                    seen_answer_texts.add(extra)
            extra_answers.extend(
                f"关于“{query}”：当前同 SKU 资料未直接确认，无法确认。"
                for query in missing_child_queries
                if query
            )
            if extra_answers:
                agent_result["answer"] = "\n".join([base_answer, *extra_answers])
            compound_metadata = agent_result.setdefault("answer_metadata", {})
            compound_metadata["compound_product_qa_queries"] = compound_queries
            compound_metadata["compound_missing_child_queries"] = missing_child_queries
            compound_metadata["answer_coverage_contract"] = (
                customer_answer_coverage_contract.build_answer_coverage_contract(
                    compound_queries,
                    answered_requests=answered_child_requests,
                    unsupported_requests=missing_child_queries,
                ).to_dict()
            )
        elif len(compound_queries) > 1:
            # The parent answer may still be valid for the first child, but a
            # failed narrow retrieval must not make later semantic children
            # disappear from the customer-visible response.
            compound_metadata = agent_result.setdefault("answer_metadata", {})
            compound_metadata["compound_product_qa_queries"] = compound_queries
            compound_metadata["compound_missing_child_queries"] = compound_queries[1:]
            parent_evidence_id = str(
                compound_metadata.get("evidence_source")
                or compound_metadata.get("source")
                or "same_sku_parent_evidence"
            )
            compound_metadata["answer_coverage_contract"] = (
                customer_answer_coverage_contract.build_answer_coverage_contract(
                    compound_queries,
                    answered_requests=[(compound_queries[0], parent_evidence_id)],
                    unsupported_requests=compound_queries[1:],
                ).to_dict()
            )
    if (
        not terminal_gifting_boundary
        and supplemental_query
        and len(agent_result.get("result_skus") or []) == 1
        # The resolved compound executor may already have retrieved and merged
        # this same semantic supplement before entering the common save path.
        # Re-running it here duplicates a grounded fact and spends another LLM
        # round-trip, so the sealed metadata is the idempotency boundary.
        and not bool(
            (agent_result.get("answer_metadata") or {}).get("supplemental_product_qa")
            if isinstance(agent_result.get("answer_metadata"), dict)
            else False
        )
    ):
        supplemental, supplemental_query = await _resolve_sealed_supplemental_product_evidence(
            db,
            question=question,
            semantic_preplan=effective_semantic_preplan,
            base_plan={"semantic_preplan": effective_semantic_preplan},
        )
        agent_result = _merge_supplemental_product_qa_into_field_answer(
            agent_result,
            supplemental=supplemental,
            supplemental_query=supplemental_query,
        )
    result_skus = [
        str(item or "").strip().upper()
        for item in (agent_result.get("result_skus") or [])
        if str(item or "").strip()
    ]
    explicit_question_skus = [
        str(item or "").strip().upper()
        for item in customer_agent_service._extract_skus(question)
        if str(item or "").strip()
    ]
    if (
        not terminal_gifting_boundary
        and len(result_skus) == 1
        and explicit_question_skus == result_skus
        and str(agent_result.get("answer_type") or "") == "product_detail"
        and not supplemental_query
        # A sealed multi-field executor has already rendered every requested
        # field from same-SKU evidence.  Re-running the legacy phrase formatter
        # here can only discard fields it does not understand (for example
        # weight or fuel), so the formal contract remains authoritative.
        and str((agent_result.get("answer_metadata") or {}).get("answer_policy") or "")
        != "sealed_same_sku_multi_field_evidence"
    ):
        compound_detail = product_service.get_product_detail(db, result_skus[0])
        if isinstance(compound_detail, dict):
            compound_answer = customer_agent_intent_service._compound_single_product_detail_answer(
                db,
                sku=result_skus[0],
                detail=compound_detail,
                question=question,
                supporting=None,
            )
            if compound_answer:
                agent_result["answer"] = compound_answer
                agent_result["skip_polish"] = True
                compound_metadata = agent_result.setdefault("answer_metadata", {})
                compound_metadata["answer_policy"] = "sealed_same_sku_compound_evidence"
                compound_metadata["evidence_skus"] = result_skus
    agent_result = _ensure_debug_plan_raw_question(agent_result, question)
    answer_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    skip_polish = bool(agent_result.get("skip_polish"))
    if _should_skip_polish_for_agent_result(agent_result):
        skip_polish = True
    if answer_metadata.get("evidence_insufficient") is True or answer_metadata.get("answer_policy") == "insufficient_evidence":
        skip_polish = True
    if not skip_polish:
        agent_result["answer"] = await _polish_customer_answer(db, question, agent_result)
    # Preserve independently scoped child questions after customer polishing.
    # The polish model may shorten a safe-missing clause, but it must not erase
    # a requested sub-question that lacked same-SKU evidence.
    coverage_contract = customer_answer_coverage_contract.AnswerCoverageContract.from_dict(
        answer_metadata.get("answer_coverage_contract")
    )
    if coverage_contract is not None:
        agent_result = _apply_answer_coverage_contract(agent_result)
    else:
        missing_child_queries = [
            str(item or "").strip()
            for item in (answer_metadata.get("compound_missing_child_queries") or [])
            if str(item or "").strip()
        ]
        if missing_child_queries:
            answer_text = str(agent_result.get("answer") or "").strip()
            missing_lines = [f"关于“{query}”：当前同 SKU 资料未直接确认，无法确认。" for query in missing_child_queries]
            additions = [line for line in missing_lines if line not in answer_text]
            if additions:
                agent_result["answer"] = "\n".join([item for item in (answer_text, *additions) if item])
            compound_query_list = []
        else:
            compound_query_list = [
                str(item or "").strip()
                for item in (answer_metadata.get("compound_product_qa_queries") or [])
                if str(item or "").strip()
            ]
        # If a compound turn produced only one answer, do not silently drop
        # later independently requested children.  The child query itself is
        # the semantic source; absent an explicit answer for it, fail closed.
        answer_text = str(agent_result.get("answer") or "").strip()
        if len(compound_query_list) > 1:
            uncovered_queries = _uncovered_compound_queries_for_output(
                answer_text,
                compound_query_list,
                answer_metadata,
            )
            additions = [
                f"关于“{query}”：当前同 SKU 资料未直接确认，无法确认。"
                for query in uncovered_queries
            ]
            if additions:
                agent_result["answer"] = "\n".join([item for item in (answer_text, *additions) if item])
    agent_result = _shape_answer_for_output(agent_result)
    agent_result = _clear_unrelated_catalogue_cards(question, agent_result)
    agent_result = _preserve_recommendation_customer_context(question, agent_result)
    final_debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    if (
        str(final_debug.get("agent_mode") or "") == "sealed_product_qa_safe_missing"
        and not terminal_gifting_boundary
    ):
        rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
        if rows:
            row = rows[0]
            sku = str(row.get("sku") or "").strip().upper()
            name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
            agent_result["answer"] = _safe_missing_product_qa_answer(name, sku, question)
        agent_result["skip_polish"] = True
    result_rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
    saved_plan = ((agent_result.get("debug") or {}).get("plan") or {})
    comparison_recommendation = bool(
        agent_result.get("answer_type") == "recommendation"
        and isinstance(saved_plan, dict)
        and (
            saved_plan.get("must_compare_both_products")
            or str(saved_plan.get("primary_intent") or "") == "product_compare_recommendation"
        )
    )
    if comparison_recommendation and result_rows:
        top_sku = str(result_rows[0].get("sku") or "").strip().upper()
        lower_skus = {
            str(row.get("sku") or "").strip().upper()
            for row in result_rows[1:]
            if str(row.get("sku") or "").strip()
        }
        lines = str(agent_result.get("answer") or "").splitlines()
        top_line_index = next(
            (
                index
                for index, line in enumerate(lines)
                if top_sku in line.upper() and not any(sku in line.upper() for sku in lower_skus)
            ),
            None,
        )
        if top_line_index is not None:
            agent_result["answer"] = "\n".join(lines[top_line_index:]).strip()
    agent_result["skip_polish"] = skip_polish
    if terminal_gifting_boundary:
        agent_result = _enforce_terminal_gifting_evidence_boundary(
            agent_result,
            bounded_answer=terminal_gifting_answer,
        )
    agent_result = customer_final_answer_arbiter.arbitrate_final_answer(agent_result)
    if terminal_gifting_boundary:
        agent_result = _enforce_terminal_gifting_evidence_boundary(
            agent_result,
            bounded_answer=terminal_gifting_answer,
        )
    agent_result = _attach_agent_quality(agent_result, question)
    conversation = _get_or_create_conversation(db, user_id, question, agent_result.get("sku"), conversation_id)
    db.add(CustomerServiceMessage(
        conversation_id=conversation.id,
        role="user",
        content=question,
        sku=agent_result.get("sku"),
    ))
    assistant_turn_index = _assistant_turn_index(db, conversation.id)
    sources_with_context = _sources_with_result_context(
        agent_result,
        turn_index=assistant_turn_index,
        user_question=question,
        inherited_recommendation_context=_latest_recommendation_context_for_sources(db, conversation.id),
        inherited_candidate_context=_latest_candidate_context_for_sources(db, conversation.id),
    )
    assistant_message = CustomerServiceMessage(
        conversation_id=conversation.id,
        role="assistant",
        content=agent_result["answer"],
        sku=agent_result.get("sku"),
        sources_json=json.dumps(sources_with_context, ensure_ascii=False, default=str),
    )
    db.add(assistant_message)
    _touch_conversation(conversation, agent_result.get("sku"))
    db.flush()
    conversation_id_value = conversation.id
    message_id_value = assistant_message.id
    db.commit()
    customer_perf_service.log_stage("save_messages_and_commit", stage_start, branch=branch, skip_polish=bool(agent_result.get("skip_polish")))
    customer_perf_service.log_stage(
        "ask_customer_service.total",
        request_start,
        branch=branch,
        intent=agent_result.get("intent"),
        agent_mode=(agent_result.get("debug") or {}).get("agent_mode"),
    )
    customer_perf_service.summarize_request(
        final_answer=agent_result.get("answer"),
        intent=agent_result.get("intent"),
        agent_mode=(agent_result.get("debug") or {}).get("agent_mode"),
    )
    release_session_connection(db)
    agent_result = _attach_semantic_preplan_debug(
        agent_result,
        semantic_preplan,
        final_route=str(agent_result.get("answer_type") or agent_result.get("intent") or ""),
    )
    public_intent = _public_intent_name(agent_result.get("intent"), agent_result.get("answer_type"))
    return {
        "conversation_id": conversation_id_value,
        "message_id": message_id_value,
        "intent": public_intent,
        "answer_type": agent_result.get("answer_type"),
        "confidence": agent_result.get("confidence"),
        "uncertainty": agent_result.get("uncertainty"),
        "needs_clarification": agent_result.get("needs_clarification", False),
        "anomalies": agent_result.get("anomalies") or [],
        "suggested_followups": agent_result.get("suggested_followups") or [],
        "followups": agent_result.get("followups") or agent_result.get("suggested_followups") or [],
        "warnings": agent_result.get("warnings") or [],
        "evidence": agent_result.get("evidence") or [],
        "agent_quality": agent_result.get("agent_quality") or {},
        "answer_metadata": agent_result.get("answer_metadata") or {},
        "debug": agent_result.get("debug") or {},
        "sku": agent_result.get("sku"),
        "answer": agent_result["answer"],
        "sources": sources_with_context,
        "actions": agent_result.get("actions") or [],
        "results": agent_result.get("results") or [],
        "steps": agent_result.get("steps") or [],
        "result_skus": agent_result.get("result_skus") if "result_skus" in agent_result else [
            str(item.get("sku") or "").strip().upper()
            for item in (agent_result.get("results") or [])
            if isinstance(item, dict) and str(item.get("sku") or "").strip()
        ],
        "candidate_skus": agent_result.get("candidate_skus") if "candidate_skus" in agent_result else [
            str(item.get("sku") or "").strip().upper()
            for item in (agent_result.get("results") or [])
            if isinstance(item, dict) and str(item.get("sku") or "").strip()
        ],
        "agent_mode": (agent_result.get("debug") or {}).get("agent_mode"),
    }


def _phase1_timing(
    *,
    request_start: float,
    planner_duration_ms: float = 0,
    retrieval_duration_ms: float = 0,
    executor_duration_ms: float = 0,
    composer_duration_ms: float = 0,
    guard_duration_ms: float = 0,
    llm_duration_ms: float = 0,
    llm_call_count: int = 0,
) -> dict:
    return customer_agent_planner_service.merge_timing(
        None,
        {
            "total_duration_ms": round((perf_counter() - request_start) * 1000, 2),
            "planner_duration_ms": planner_duration_ms,
            "retrieval_duration_ms": retrieval_duration_ms,
            "executor_duration_ms": executor_duration_ms,
            "llm_duration_ms": llm_duration_ms,
            "llm_call_count": llm_call_count,
            "composer_duration_ms": composer_duration_ms,
            "guard_duration_ms": guard_duration_ms,
        },
    )


def _attach_phase1_plan_and_timing(agent_result: dict, plan: dict, timing: dict) -> dict:
    if not isinstance(agent_result, dict):
        return agent_result
    answer_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    existing_plan = debug.get("plan") if isinstance(debug.get("plan"), dict) else {}
    merged_timing = customer_agent_planner_service.merge_timing(debug.get("timing"), timing)
    answer_metadata["timing"] = customer_agent_planner_service.merge_timing(answer_metadata.get("timing"), merged_timing)
    debug["timing"] = merged_timing
    # ``_db`` is a runtime-only dependency for the phase executors.  It must
    # never escape into the debug payload: HTTP debug responses are JSON and
    # a SQLAlchemy Session is neither customer evidence nor serializable.
    original_plan = {
        key: value
        for key, value in (plan.items() if isinstance(plan, dict) else [])
        if key != "_db"
    }
    debug_plan = {**original_plan, **existing_plan} if existing_plan else original_plan
    # A product-page SKU is request identity, not a route hint. An early
    # route may carry an older plan snapshot, but must never erase it.
    if original_plan.get("request_sku_anchor"):
        debug_plan["request_sku_anchor"] = original_plan["request_sku_anchor"]
    debug["plan"] = debug_plan
    if isinstance(plan, dict) and isinstance(plan.get("semantic_preplan"), dict):
        already_attached = bool((debug.get("semantic_preplan") or {}).get("called")) if isinstance(debug.get("semantic_preplan"), dict) else False
        preplan_debug = dict(plan["semantic_preplan"])
        if not preplan_debug.get("accepted_or_overridden"):
            final_route = str(agent_result.get("answer_type") or agent_result.get("intent") or "").strip()
            route_hint = str(preplan_debug.get("route_hint") or "").strip()
            accepted = bool(route_hint and final_route in _semantic_preplan_equivalent_routes(route_hint, final_route))
            preplan_debug["accepted_or_overridden"] = "accepted" if accepted else "overridden"
            if not accepted:
                preplan_debug["override_reason"] = preplan_debug.get("fallback_reason") or "deterministic_guard_final_route"
        plan["semantic_preplan"] = preplan_debug
        debug["semantic_preplan"] = preplan_debug
        if not already_attached and preplan_debug.get("called"):
            delta = int(preplan_debug.get("llm_call_count_delta") or preplan_debug.get("llm_call_count") or 0)
            if delta:
                debug["timing"]["llm_call_count"] = int(debug["timing"].get("llm_call_count") or 0) + delta
                answer_metadata["timing"]["llm_call_count"] = int(answer_metadata["timing"].get("llm_call_count") or 0) + delta
    agent_result["answer_metadata"] = answer_metadata
    agent_result["debug"] = debug
    return agent_result


def _ensure_debug_plan_raw_question(agent_result: dict, question: str) -> dict:
    """Keep the public audit trace complete without changing an effective plan."""
    if not isinstance(agent_result, dict):
        return agent_result
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    plan = debug.get("plan") if isinstance(debug.get("plan"), dict) else {}
    raw_question = str(question or "").strip()
    if raw_question and not str(plan.get("raw_question") or "").strip():
        plan["raw_question"] = raw_question
    debug["plan"] = plan
    agent_result["debug"] = debug
    return agent_result


def _semantic_preplan_equivalent_routes(route_hint: str, final_route: str) -> set[str]:
    route = str(route_hint or "").strip()
    final = str(final_route or "").strip()
    equivalents = {
        "comparison": {"comparison", "recommendation"},
        "query_products": {"query_products", "product_query"},
        "usage_care": {"knowledge_base_answer", "product_usage_care"},
        "unknown_field": {"product_detail", "knowledge_base_answer"},
        "product_detail": {"product_detail"},
    }
    return equivalents.get(route) or {route, final}


def _attach_semantic_preplan_debug(agent_result: dict | None, semantic_preplan: dict | None, *, final_route: str) -> dict | None:
    if not isinstance(agent_result, dict) or not isinstance(semantic_preplan, dict) or not semantic_preplan.get("called"):
        return agent_result
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    answer_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    already_attached = bool((debug.get("semantic_preplan") or {}).get("called")) if isinstance(debug.get("semantic_preplan"), dict) else False
    final = str(final_route or agent_result.get("answer_type") or agent_result.get("intent") or "").strip()
    route_hint = str(semantic_preplan.get("route_hint") or "").strip()
    accepted = bool(route_hint and final in _semantic_preplan_equivalent_routes(route_hint, final))
    preplan_debug = dict(semantic_preplan)
    preplan_debug["accepted_or_overridden"] = "accepted" if accepted else "overridden"
    if not accepted:
        preplan_debug["override_reason"] = preplan_debug.get("fallback_reason") or "deterministic_guard_final_route"
    debug["semantic_preplan"] = preplan_debug
    if not already_attached:
        for container in (debug, answer_metadata):
            timing = container.get("timing") if isinstance(container.get("timing"), dict) else None
            if timing:
                timing["llm_call_count"] = int(timing.get("llm_call_count") or 0) + int(semantic_preplan.get("llm_call_count_delta") or semantic_preplan.get("llm_call_count") or 0)
                timing["llm_duration_ms"] = timing.get("llm_duration_ms") or 0
        trace = debug.get("trace") if isinstance(debug.get("trace"), dict) else None
        if trace is not None:
            trace["llm_call_count"] = int(trace.get("llm_call_count") or 0) + int(semantic_preplan.get("llm_call_count_delta") or semantic_preplan.get("llm_call_count") or 0)
    agent_result["debug"] = debug
    agent_result["answer_metadata"] = answer_metadata
    return agent_result


def _has_explicit_sku_text(question: str) -> bool:
    return bool(SKU_RE.search(str(question or "")))


def _is_clear_explicit_sku_field_question_for_preplan(question: str, phase1_plan: dict | None) -> bool:
    text = str(question or "")
    if not _has_explicit_sku_text(text):
        return False
    if any(term in text for term in ("使用限制", "注意事项", "禁忌", "怎么清洗", "怎么用")):
        return False
    if isinstance(phase1_plan, dict) and phase1_plan.get("primary_intent") == "product_field":
        return True
    clear_terms = (
        "容量",
        "材质",
        "材料",
        "重量",
        "尺寸",
        "规格",
        "酒精炉",
        "明火",
        "热源",
        "冷水",
        "热水",
        "烧水",
        "补水",
    )
    return any(term in text for term in clear_terms)


def _is_sku_usage_restriction_ambiguous_for_preplan(question: str) -> bool:
    text = str(question or "")
    return _has_explicit_sku_text(text) and any(term in text for term in ("使用限制", "注意事项", "禁忌"))


def _is_complex_filter_ambiguous_for_preplan(question: str) -> bool:
    text = str(question or "")
    if not any(term in text for term in ("有哪些", "哪几", "适合", "更偏", "推荐")):
        return False
    coffee_filter = any(term in text for term in ("咖啡器具", "咖啡", "手冲")) and any(term in text for term in ("手冲", "咖啡器具"))
    water_filter = any(term in text for term in ("水具", "水杯", "水壶")) and any(term in text for term in ("冷水", "补水", "随身"))
    return bool(coffee_filter or water_filter)


def _looks_like_contents_accessories_contract_question(question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    return bool(
        customer_agent_service.looks_like_contents_accessories_question(text)
        or customer_agent_intent_service._looks_like_contents_grounding_question(text)
    )


def _looks_like_ignition_capability_question(question: str) -> bool:
    """Recognize an ignition feature/capability ask, not an operation issue.

    The same ``点火`` vocabulary appears in setup and troubleshooting turns.
    Only the capability predicates stay on the formal product-detail contract;
    procedure/failure wording remains eligible for the usage-care route.
    """
    text = str(question or "").strip()
    if not text or not any(
        term in text
        for term in (
            "电子点火", "远程点火", "远程打火", "点火装置", "点火方式", "打火方式",
            "手动点火", "手动打火", "自动点火", "自动打火", "电池",
        )
    ):
        return False
    if any(term in text for term in ("怎么", "如何", "步骤", "操作", "点不着", "打不着", "点不起来")):
        return False
    return any(term in text for term in ("带", "有", "没有", "没带", "不带", "需要", "不用", "不是", "是否", "是不是", "吗", "吧"))


def _page_formal_field_should_defer_to_bounded_adapter(question: str, field_type: str) -> bool:
    """Let narrow page capability/component asks use their evidence adapter."""
    normalized_field = str(field_type or "").strip()
    if normalized_field == "usage_instruction" and customer_agent_intent_service._looks_like_ignition_fault_question(question):
        return True
    if normalized_field == "usage_instruction" and _looks_like_ignition_capability_question(question):
        return True
    return bool(
        normalized_field == "dimensions"
        and customer_field_contract.requested_dimension_subtype(question) == "fuel_holder"
    )


def _has_non_usage_care_field_contract(question: str) -> bool:
    """Keep non-usage product fields out of the usage/care fast path."""
    field_contract = customer_field_contract.detect_field_contract(question)
    # Any formal field must reach the central entity arbitration before a
    # scope guard or usage/care shortcut can decide the result.  Accessories
    # retains its additional generic-query exclusion below.
    if field_contract and field_contract.field_type != "accessories":
        # A product noun such as ``酒精炉`` can itself satisfy the heat-source
        # alias contract.  When the same sentence contains an operational,
        # failure, storage, or explicit fuel-compatibility signal, that field
        # alias is the subject—not the requested answer.  Preserve the
        # usage/safety route so a question such as “酒精炉怎么点不着” cannot
        # be downgraded to a bare heat-source listing.
        text = str(question or "").strip()
        if (
            field_contract.field_type == "usage_instruction"
            and _looks_like_ignition_capability_question(text)
        ):
            return True
        usage_signal = any(
            marker in text
            for marker in (
                "怎么", "如何", "点不着", "打不着", "点火", "点不起来", "安全", "危险",
                "存放", "保存", "收纳", "没用完", "用不完", "能用吗", "可以用吗",
                "支持", "适配", "兼容", "连接", "接上", "漏气", "泄漏", "会漏",
            )
        )
        if usage_signal and customer_agent_intent_service._looks_like_usage_care_question(text):
            return False
        return True
    if not field_contract or field_contract.field_type != "accessories":
        return _looks_like_contents_accessories_contract_question(question)

    # An explicit SKU is a sealed product-identity signal even when subject
    # span extraction also retains a shorter SKU-shaped search candidate.
    # Contents/accessories must therefore enter entity arbitration rather than
    # degrading into a generic catalogue query.
    if _has_explicit_sku_text(question):
        return True

    # Accessories only become product-bound when FieldContract left a concrete
    # subject. Category discovery and recommendation language keeps its
    # established query-products path even though it mentions accessories.
    selection = customer_field_contract.select_entity_subject_for_routing(raw_question=question)
    subject = str(selection.entity_subject or "").strip()
    if not subject or subject.startswith(("哪些", "有哪些", "什么", "推荐", "适合")):
        return False
    if _is_generic_contents_subject(subject):
        return False
    if any(term in str(question or "") for term in ("更偏", "推荐", "适合", "用于", "收纳")):
        return False
    return not _is_generic_category_phrase(subject) and not _is_generic_modifier_prefixed_product_scope(subject)


def _should_call_semantic_preplan(
    question: str,
    phase1_plan: dict | None,
    *,
    conversation_id: str | None,
    has_named_product: bool = False,
) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    if _detect_resolved_entity_unknown_fact_label(text):
        return True
    if _looks_like_contents_accessories_contract_question(text):
        return True
    if _is_sku_usage_restriction_ambiguous_for_preplan(text):
        return True
    if customer_agent_service._extract_skus(text):
        # An explicit SKU seals only the product subject.  It must not let a
        # legacy field parser or a usage/care shortcut decide what the user is
        # asking before the constrained semantic planner has emitted its
        # allowlisted field signal.
        return True
    if has_named_product:
        if (
            "?" not in text
            and "？" not in text
            and any(term in text for term in ("再看", "改看", "看看", "看一下"))
        ):
            # Existing offline navigation remains available when the semantic
            # provider is unavailable. New declarative forms still reach the
            # semantic navigation contract below.
            return False
        # A catalogue identity answers only "which product".  The semantic
        # preplan must still decide "what is being asked" before lexical
        # recommendation/usage helpers can interpret words such as "适合" or
        # tokens embedded in the product name.
        return True
    if isinstance(phase1_plan, dict) and phase1_plan.get("primary_intent") == "recommendation":
        # The deterministic planner may recognize that this is a candidate
        # request, but it is not allowed to infer the customer's actual
        # subject, constraints, or priority.  Always obtain the constrained
        # semantic plan before a legacy recommendation executor can run.
        return True
    if (
        isinstance(phase1_plan, dict)
        and phase1_plan.get("primary_intent") == "query_products"
        and "?" not in text
        and "？" not in text
        and not any(term in text for term in ("再看", "改看", "看看", "看一下"))
    ):
        # A terse declarative turn outside the established offline navigation
        # grammar can be a product-navigation request. Its meaning belongs to
        # semantic planning; deterministic code only verifies a later exact
        # entity contract before any anchor is stored.
        return True
    if (
        isinstance(phase1_plan, dict)
        and phase1_plan.get("primary_intent") == "catalog_count"
        and _looks_like_semantic_catalog_query(text)
        and not has_named_product
    ):
        # Count-like wording can conceal a recommendation or a multi-constraint
        # catalogue request.  Let the semantic preplan decide the user's full
        # intent before the old count route discards that meaning; the later
        # deterministic layer still verifies the actual catalogue evidence.
        return True
    if conversation_id and _asks_for_alternative_recommendation(text):
        return True
    if _phase1_is_griddle_vs_cookware_scenario(text):
        return True
    if _is_complex_filter_ambiguous_for_preplan(text):
        return True
    if (
        isinstance(phase1_plan, dict)
        and not str(phase1_plan.get("primary_intent") or "").strip()
        and str(phase1_plan.get("confidence") or "").strip().lower() in {"", "low"}
    ):
        # A low-confidence legacy plan has not understood the request.  It
        # therefore cannot authorize a downstream QA or structured-query
        # shortcut: let the semantic preplan classify the whole utterance
        # first, then keep deterministic code to contracts and evidence only.
        return True
    # For every ordinary, unbound customer turn, semantic planning owns the
    # full sentence meaning before a legacy parser can turn a single noun into
    # a field route. Punctuation is not a semantic boundary: declarative
    # requests are common in chat. The later contracts still retain entity,
    # field-allowlist, and same-SKU evidence authority.
    return True


def _looks_like_selected_result_context_reference(question: str) -> bool:
    """Recognize an anaphor that explicitly points at a prior selected result."""
    text = str(question or "")
    if _looks_like_negated_selected_result_context_reference(text):
        return False
    return bool(
        re.search(
            r"(?:\u9009|\u9009\u62e9|\u9009\u4e2d|\u63a8\u8350)\u7684(?:\u90a3|\u8fd9)(?:\u4e00)?(?:\u6b3e|\u4e2a|\u4ef6|\u4ea7\u54c1)",
            text,
        )
    )


def _looks_like_negated_selected_result_context_reference(question: str) -> bool:
    """Reject contrastive wording that points away from the selected result."""
    text = str(question or "")
    return bool(
        re.search(
            r"(?:\u6ca1(?:\u6709)?|\u672a|\u4e0d(?:\u662f)?|\u5e76\u975e)"
            r"(?:\u4f60|\u6211|\u4ed6|\u5979|\u6211\u4eec)?"
            r"(?:\u521a\u624d|\u521a\u521a|\u6700\u7ec8|\u6700\u540e)?"
            r"(?:\u9009|\u9009\u62e9|\u9009\u4e2d|\u63a8\u8350)\u7684(?:\u90a3|\u8fd9)(?:\u4e00)?(?:\u6b3e|\u4e2a|\u4ef6|\u4ea7\u54c1)",
            text,
        )
        or re.search(
            r"(?:\u4e0d\u8981|\u522b\u7ba1|\u522b\u770b|\u522b\u8003\u8651|\u4e0d\u8003\u8651|\u6392\u9664|\u9664\u4e86|\u9664\u53bb)"
            r"[^\uff0c\u3002\uff01\uff1f!?]{0,12}"
            r"(?:\u9009|\u9009\u62e9|\u9009\u4e2d|\u63a8\u8350)\u7684(?:\u90a3|\u8fd9)(?:\u4e00)?(?:\u6b3e|\u4e2a|\u4ef6|\u4ea7\u54c1)",
            text,
        )
    )


def _selected_result_context_choice_sku(
    recommendation_context: dict[str, Any] | None,
    candidate_context: dict[str, Any] | None,
) -> str:
    """Return one persisted final choice only when all selection signals agree."""
    choices = {
        str((context or {}).get("final_choice_sku") or "").strip().upper()
        for context in (recommendation_context, candidate_context)
        if str((context or {}).get("final_choice_sku") or "").strip()
    }
    if len(choices) != 1:
        return ""
    selected_sku = next(iter(choices))
    active_anchor = str(
        (recommendation_context or {}).get("active_single_product_anchor") or ""
    ).strip().upper()
    if active_anchor and active_anchor != selected_sku:
        return ""
    return selected_sku


def _should_bypass_semantic_preplan_for_bound_context_followup(
    db: Session,
    question: str,
    phase1_plan: dict[str, Any] | None,
    *,
    conversation_id: str | None,
) -> bool:
    if not conversation_id:
        return False
    recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
    candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    if not (
        _has_followup_result_context(recommendation_context)
        or _has_followup_result_context(candidate_context)
    ):
        return False
    if _looks_like_negated_selected_result_context_reference(question):
        return False
    current_turn_subject = (
        customer_recommendation_verification_contract.build_recommendation_request_contract(
            question
        )
    )
    if current_turn_subject.subject_kind or current_turn_subject.subject_category:
        # A role/category named on this turn can select one component from a
        # prior mixed recommendation (for example, the kettle from a coffee
        # kit). Let semantic planning interpret that role instead of blindly
        # comparing the first two persisted SKUs.
        return False
    if (
        _is_comparison_choice_followup_question(question)
        or (
            _is_recommendation_followup_question(question)
            and not _asks_for_alternative_recommendation(question)
        )
        or _is_ordinal_compare_followup_question(question)
    ):
        return True
    selected_result_reference = _looks_like_selected_result_context_reference(question)
    if selected_result_reference and not _selected_result_context_choice_sku(
        recommendation_context,
        candidate_context,
    ):
        return False
    if not (
        _looks_like_ordinal_product_field_followup(question)
        or customer_agent_intent_service._has_context_reference(question)
        or selected_result_reference
    ):
        return False
    field_request = _merge_explicit_detail_fields_into_request(
        question,
        resolve_requested_field_contract(question, phase1_plan),
    )
    canonical_fields = [
        str(field or "").strip()
        for field in (field_request.get("canonical_fields") or [])
        if customer_field_contract.is_supported_detail_field(str(field or "").strip())
    ]
    return bool(canonical_fields)


async def _maybe_run_semantic_preplan(
    db: Session,
    question: str,
    phase1_plan: dict | None,
    *,
    conversation_id: str | None,
) -> dict | None:
    # Entity recognition remains deterministic.  It only decides whether a
    # product subject is present; semantic planning alone classifies the field.
    # This prevents a named product fact containing catalogue vocabulary from
    # being skipped before it can form the formal field contract.
    has_named_product = bool(_products_named_in_question(db, question))
    if not _should_call_semantic_preplan(
        question,
        phase1_plan,
        conversation_id=conversation_id,
        has_named_product=has_named_product,
    ):
        return None
    recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id) if conversation_id else {}
    prior_customer_preference_texts: list[str] = []
    if isinstance(recommendation_context, dict) and isinstance(
        recommendation_context.get("effective_recommendation_contract")
        or recommendation_context.get("recommendation_request_contract"),
        dict,
    ):
        prior_question = str(recommendation_context.get("user_question") or "").strip()
        if prior_question:
            prior_customer_preference_texts.append(prior_question)
    active_anchor_sku = str(
        (recommendation_context or {}).get("active_single_product_anchor") or ""
    ).strip().upper()
    active_anchor = {}
    if active_anchor_sku:
        product = db.query(Product).filter(Product.sku == active_anchor_sku).first()
        if product is not None:
            active_anchor = {
                "sku": active_anchor_sku,
                "product_name": str(product.product_name_cn or product.product_name_en or "").strip(),
            }
    # This is identity provenance only, never a route decision: the semantic
    # planner still decides whether the complete turn requests a product fact.
    # Passing a boolean avoids leaking a SKU, catalogue value, or answer into
    # the semantic prompt while allowing it to distinguish a named-product
    # question from genuinely unscoped general guidance.
    current_turn_products = _products_named_in_question(db, question)
    unique_current_turn_catalog_product_name = bool(
        customer_entity_resolution_contract.unique_canonical_subject_in_question(
            question,
            current_turn_products,
        )
    )
    unique_current_turn_catalog_product_mention = (
        customer_entity_resolution_contract.unique_canonical_subject_in_question(
            question,
            current_turn_products,
        )
        if unique_current_turn_catalog_product_name
        else ""
    )
    semantic_preplan = await customer_agent_planner_service.plan_customer_question_semantic(
        db,
        question,
        phase1_plan,
        context={
            "conversation_id": conversation_id,
            "has_recommendation_context": bool(prior_customer_preference_texts),
            "prior_customer_preference_texts": prior_customer_preference_texts,
            "active_product_anchor": active_anchor,
            "has_unique_current_turn_catalog_product_name": unique_current_turn_catalog_product_name,
            # This text is already a literal span of the customer turn, not
            # a SKU or database fact. It lets the semantic model distinguish
            # a catalog name that happens to resemble a generic category.
            "unique_current_turn_catalog_product_mention": unique_current_turn_catalog_product_mention,
        },
    )
    semantic_preplan = _repair_cup_count_semantic_preplan(question, semantic_preplan)
    if (
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("fallback_reason") == "non_evidentiary_recommendation"
    ):
        # Keep a category-only accessory recommendation executable when the
        # provider drops its category constraint while declaring the turn
        # non-evidentiary.  This is a typed current-turn scope recovery; it
        # never resolves a name, chooses a SKU, or supplies product facts.
        literal_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(question)
        if literal_contract.subject_kind == "accessories":
            recovered = dict(semantic_preplan)
            recovered.update({
                "route_family": "recommendation",
                "route_hint": "recommendation",
                "question_type": "recommendation",
                "subtype": "recommendation",
                "subject_text": str(
                    semantic_preplan.get("subject_text") or question
                ).strip()[:200],
                "canonical_fields": [],
                "confidence": 0.9,
                "confidence_label": "high",
                "ambiguity": False,
                "evidence_required": True,
                "evidence_kind": "structured_field",
                "recommendation_constraints": {"subject_kind": "accessories"},
                "unrepresented_recommendation_requirements": [],
                "recommendation_soft_preferences": [],
                "fallback_reason": "",
                "accepted_or_overridden": "literal_accessory_scope_recovery",
                "override_reason": "typed_current_turn_accessory_scope",
                "entity_scope": "category_scope",
                "intent_coverage": "full",
            })
            recovered["accessory_scope_recovery"] = "service_boundary_literal_current_turn_contract"
            semantic_preplan = recovered
    return semantic_preplan


def _repair_cup_count_semantic_preplan(question: str, semantic_preplan: dict | None) -> dict | None:
    """Keep an explicit cup-count request out of the volume field contract."""
    text = str(question or "").strip()
    plan = semantic_preplan if isinstance(semantic_preplan, dict) else None
    if not (
        plan
        and re.search(r"(?:有|含|配)(?:几|多少)个?杯", text)
        and "capacity" in list(plan.get("canonical_fields") or [])
    ):
        return semantic_preplan
    repaired = dict(plan)
    repaired.update(
        {
            "route_family": "product_bound_qa",
            "route_hint": "product_detail",
            "question_type": "contents_accessories",
            "canonical_fields": ["accessories"],
            "field_type": "accessories",
            "field_hint": "accessories",
            "evidence_kind": "structured_field",
        }
    )
    return repaired


def _semantic_preplan_route_family(preplan: dict | None) -> str:
    if not isinstance(preplan, dict) or not preplan.get("called"):
        return ""
    family = str(preplan.get("route_family") or "").strip()
    if family:
        return family
    route_hint = str(preplan.get("route_hint") or "").strip()
    subtype = str(preplan.get("subtype") or "").strip()
    question_type = str(preplan.get("question_type") or "").strip()
    if subtype == "structured_query" or (route_hint == "query_products" and question_type in {"filter", "count"}):
        return "structured_query" if subtype == "structured_query" else "generic_query"
    if route_hint == "recommendation" or subtype == "recommendation":
        return "recommendation"
    if route_hint == "unknown_field" or subtype in {"unknown_realtime", "commercial_realtime"}:
        return "unknown_realtime"
    if question_type == "contents_accessories" or subtype in {"contents_accessories", "composition"}:
        return "contents_accessories"
    if route_hint in {"product_detail", "usage_care", "knowledge_base_answer"}:
        return "product_bound_qa"
    if route_hint == "clarification" or subtype == "no_match":
        return "clarification"
    return ""


def _apply_semantic_single_field_route_precedence(
    phase1_plan: dict | None,
    semantic_preplan: dict | None,
) -> None:
    """Remove parser-only collection state after a sealed singular field plan.

    The semantic planner owns the whole-turn meaning.  A legacy parser can
    still expose a short SKU prefix as a second participant (for example a
    compact token embedded in a longer SKU); that diagnostic extraction must
    never turn a high-confidence, one-product field request into comparison
    work.  This helper changes no field, entity, SKU, or evidence: Phase 2
    will independently validate the semantic subject into its contract.
    """
    plan = phase1_plan if isinstance(phase1_plan, dict) else None
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    fields = [
        str(field or "").strip()
        for field in (preplan.get("canonical_fields") or [])
        if str(field or "").strip() in customer_field_contract.FORMAL_DETAIL_FIELDS
    ]
    if not (
        plan is not None
        and preplan.get("called")
        and _semantic_preplan_confident(preplan, minimum=0.9)
        and _semantic_preplan_route_family(preplan) == "product_bound_qa"
        and len(fields) == 1
    ):
        return
    plan["primary_intent"] = "product_detail"
    plan["answer_type"] = "product_detail"
    plan["product_refs"] = []
    plan["must_compare_both_products"] = False
    plan["must_make_choice"] = False
    plan["tasks"] = []


def _semantic_preplan_entity_scope(preplan: dict | None) -> str:
    if not isinstance(preplan, dict) or not preplan.get("called"):
        return ""
    scope = str(preplan.get("entity_scope") or "").strip()
    aliases = {
        "resolved_single": "resolved_product",
        "unique_product_name": "resolved_product",
        "ambiguous_product_name": "ambiguous_product",
        "unresolved_product_like": "unresolved_product",
    }
    return aliases.get(scope, scope)


def _semantic_preplan_confident(preplan: dict | None, *, minimum: float = 0.5) -> bool:
    if not isinstance(preplan, dict) or not preplan.get("called"):
        return False
    label = str(preplan.get("confidence_label") or "").strip()
    if label == "high":
        return True
    if label == "medium":
        return minimum <= 0.65
    try:
        confidence = float(preplan.get("confidence") or 0)
    except (TypeError, ValueError):
        confidence = 0.0
    if confidence >= minimum:
        return True
    # A fail-closed semantic parser clears its public confidence, but the
    # catalogue adapter may safely consume the independently retained route
    # confidence only to select an exact value from the live DB closed set.
    if (
        str(preplan.get("semantic_route_family_hint") or "") == "structured_query"
        and str(preplan.get("fallback_reason") or "") in {
            "incomplete_structured_query_scope",
            "catalogue_value_requires_structured_query",
            "unbound_catalogue_browse_requires_structured_query",
        }
    ):
        try:
            return float(preplan.get("semantic_confidence_hint") or 0) >= minimum
        except (TypeError, ValueError):
            return False
    return False


def _semantic_catalog_aggregate_contract(preplan: dict | None, phase1_plan: dict | None) -> bool:
    """Keep a semantic category aggregate on its database-count contract.

    ``category`` is a formally supported product field, but a high-confidence
    structured catalogue aggregate has no single product entity to resolve.
    The deterministic planner has already classified the turn as a count; this
    helper only prevents the generic detail-field guard from erasing that
    aggregate contract.  It does not infer a category from wording or grant
    a product identity.
    """
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    value = preplan if isinstance(preplan, dict) else {}
    count_like_question = any(token in str(value.get("raw_question") or "") for token in ("多少", "几款", "几种", "几个"))
    return bool(
        (plan.get("primary_intent") == "catalog_count" or count_like_question)
        and value.get("called")
        and str(value.get("route_family") or "").strip() == "structured_query"
        and str(value.get("route_hint") or "").strip() == "query_products"
        and str(value.get("field_type") or "").strip() == "category"
        and str(value.get("question_type") or "").strip() == "filter"
        and _semantic_preplan_confident(value, minimum=0.9)
    )


def _semantic_pairwise_contract_candidate(preplan: dict | None) -> bool:
    """Whether a semantic plan requires sealed handling of multiple named products.

    A user can request a choice between two named products using either the
    ``comparison`` or ``recommendation`` route family.  The latter is still a
    pairwise decision, not permission for the legacy recommendation renderer
    to invent a preference.  Semantic planning owns that distinction; this
    helper only decides whether every supplied participant must be sealed.
    """
    value = preplan if isinstance(preplan, dict) else {}
    route_family = str(value.get("route_family") or "").strip()
    if route_family == "comparison":
        return True
    # A request to choose from the catalogue is a recommendation, not a
    # pairwise comparison.  But once semantic planning has supplied two or
    # more named product spans, a catalogue-wide recommendation must never
    # replace them.  ``decision_requested`` is useful answer-shaping context,
    # not a safe prerequisite for preserving those identities: model output
    # can omit that boolean while still understanding a natural “怎么选” ask.
    participants = [str(item).strip() for item in (value.get("entities") or []) if str(item or "").strip()]
    return route_family == "recommendation" and len(participants) >= 2


def _semantic_structured_comparison_overview_contract(preplan: dict | None) -> bool:
    """Recognize an intentional fieldless semantic comparison overview.

    The semantic plan, not a legacy field alias in words such as "features",
    decides whether a named comparison asks for one field or for a bounded
    overview of recorded differences.
    """
    value = preplan if isinstance(preplan, dict) else {}
    return bool(
        str(value.get("route_family") or "").strip() == "comparison"
        and str(value.get("subtype") or "").strip() == "comparison_overview"
        and str(value.get("evidence_kind") or "").strip() == "structured_field"
        and not list(value.get("canonical_fields") or [])
        and not str(value.get("field_type") or value.get("field_hint") or "").strip()
        and not bool(value.get("decision_requested"))
    )


def _should_execute_semantic_catalog_recommendation(preplan: dict | None) -> bool:
    """Allow the catalogue executor only for an unbound semantic recommendation.

    Named multi-product requests are sealed comparison work, even when the
    semantic route label is ``recommendation``.  This guard is intentionally
    based on the structured semantic plan, rather than lexical heuristics.
    """
    if not isinstance(preplan, dict) or _semantic_pairwise_contract_candidate(preplan):
        return False
    return (
        str(preplan.get("route_family") or "") == "recommendation"
        and not preplan.get("fallback_reason")
        and not preplan.get("ambiguity")
        # A grounded medium-confidence recommendation still belongs to the
        # semantic contract.  Confidence controls whether the model may claim
        # certainty about wording; it must not reopen the legacy lexical
        # ``query_products`` router.  The shared verifier below either proves
        # every candidate from same-SKU evidence or returns a safe
        # clarification, so accepting the semantic route at the medium floor
        # does not relax the evidence boundary.
        and _semantic_preplan_confident(preplan, minimum=0.65)
        and isinstance(preplan.get("recommendation_constraints"), dict)
        # Catalogue-wide candidate generation needs a model-selected product
        # family. Scenario, storage, weight, or other preferences can rank
        # candidates only after that scope exists; they must never make the
        # executor guess a category on the customer's behalf.
        and bool(str((preplan.get("recommendation_constraints") or {}).get("subject_kind") or "").strip())
    )


def _apply_pairwise_recommendation_field_adapter(preplan: dict | None) -> None:
    """Adapt grounded recommendation constraints into one formal comparison FieldContract.

    This consumes only structured DeepSeek output that has already passed the
    recommendation grounding pass.  It maps schema dimensions, never lexical
    wording or product names, and only when the semantic plan has named a
    pair of products for sealed comparison.
    """
    if not isinstance(preplan, dict) or not _semantic_pairwise_contract_candidate(preplan):
        return
    if preplan.get("recommendation_constraint_grounding") != "validated_semantic_grounding":
        return
    constraints = dict(preplan.get("recommendation_constraints") or {})
    if not isinstance(constraints, dict):
        return
    mapping = (
        ("people", "people"),
        ("heat_sources", "heat_source"),
        ("scenarios", "usage_scene"),
        ("weight_preference", "weight"),
    )
    fields = [field for constraint, field in mapping if constraints.get(constraint)]
    if not fields:
        return
    preplan["canonical_fields"] = fields
    preplan["field_type"] = fields[0] if len(fields) == 1 else ""
    preplan["field_hint"] = preplan["field_type"]
    preplan["semantic_adapter_source"] = "validated_pairwise_recommendation_constraints"


def _apply_structured_named_field_entity_adapter(
    db: Session,
    question: str,
    preplan: dict | None,
) -> bool:
    """Adapt a named product field that the semantic model wrapped as a filter.

    DeepSeek occasionally retains a ``structured_query`` route shell for a
    named product attribute.  This adapter does not reinterpret customer
    wording, choose an SKU, or consume evidence.  It accepts only the model's
    high-confidence allowlisted field, then asks the ordinary
    EntityResolutionContract whether the model-supplied subject names one or
    more catalogue products.  A unique identity can proceed to the normal
    field provider; an ambiguous identity remains a clarification with the
    same formal field contract.  Generic catalogue filters stay untouched.
    """
    value = preplan if isinstance(preplan, dict) else {}
    if not (
        value.get("called")
        and not value.get("fallback_reason")
        and not value.get("ambiguity")
        and _semantic_preplan_confident(value, minimum=0.9)
        and str(value.get("route_family") or "").strip() == "structured_query"
        and str(value.get("route_hint") or "").strip() == "query_products"
        and str(value.get("question_type") or "").strip() == "filter"
        and str(value.get("subtype") or "").strip() == "structured_query"
        # A database-validated family/category scope is intentionally a set,
        # even when its subject text resembles one catalogue title.  Only a
        # product-scoped plan may be adapted into a named-product field.
        and _semantic_preplan_entity_scope(value) not in {"category_scope", "catalog_scope"}
    ):
        return False
    fields = list(dict.fromkeys(
        customer_field_contract.semantic_preplan_field_type(item)
        for item in (value.get("canonical_fields") or [])
        if customer_field_contract.semantic_preplan_field_type(item) in customer_field_contract.FORMAL_DETAIL_FIELDS
    ))
    field_type = customer_field_contract.semantic_preplan_field_type(value.get("field_type"))
    field_hint = customer_field_contract.semantic_preplan_field_type(value.get("field_hint"))
    if len(fields) != 1 or fields[0] != field_type or field_hint != field_type:
        return False
    # A category-only catalogue request with no current-turn entity has no
    # product field to adapt.  Preserve that semantic structured-query route.
    # Conversely, do not let this catalogue-scope escape consume an explicit
    # SKU or a product identity already resolved from the current question:
    # those cases must first form the ordinary FieldContract and
    # EntityResolutionContract.
    if (
        field_type == "category"
        and not (value.get("entities") or [])
        and not (value.get("structured_query_constraints") or [])
        and not customer_agent_service._extract_skus(question)
        and not _products_named_in_question(db, question)
    ):
        return False
    subject = str(value.get("subject_text") or "").strip()
    if not subject:
        return False
    products = db.query(Product).order_by(Product.sku.asc()).all()
    entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        products,
        entity_text_override=subject,
        field_type_override=field_type,
    )
    if entity_contract.status not in {"resolved", "ambiguous"} or not entity_contract.candidate_skus:
        return False
    value["route_family"] = "product_bound_qa"
    value["route_hint"] = "product_detail"
    value["question_type"] = "field"
    value["subtype"] = "known_detail"
    value["evidence_required"] = True
    value["entities"] = [subject]
    value["entity_scope"] = (
        "resolved_product" if entity_contract.status == "resolved" else "ambiguous_product"
    )
    value["semantic_original_route_family"] = "structured_query"
    value["semantic_adapter_source"] = "validated_structured_named_field_entity_contract"
    return True


def _one_edit_apart(left: str, right: str) -> bool:
    """Return whether two compact strings differ by exactly one edit."""
    if left == right or abs(len(left) - len(right)) > 1:
        return False
    if len(left) > len(right):
        left, right = right, left
    if len(left) == len(right):
        return sum(a != b for a, b in zip(left, right)) == 1
    index_left = index_right = differences = 0
    while index_left < len(left) and index_right < len(right):
        if left[index_left] == right[index_right]:
            index_left += 1
            index_right += 1
            continue
        differences += 1
        index_right += 1
        if differences > 1:
            return False
    return True


def _semantic_unique_typo_near_catalog_subject(
    entity: str,
    products: list[Product],
) -> str:
    """Recover a one-character shopper typo only when the catalogue is unique.

    Semantic planning must already have identified a product mention.  This
    helper supplies no SKU or facts: it merely maps that literal mention to a
    canonical display name when exactly one catalogue row is one edit away.
    Ambiguous near matches continue to fail closed.
    """
    raw_entity = str(entity or "").strip()
    normalized = customer_agent_service.normalize_search_text(raw_entity).lower()
    normalized = re.sub(
        r"(?:这个|那个|这款|那款)?(?:产品|商品|炉子|炉具|锅具|水壶)$",
        "",
        normalized,
    ).strip()
    compact_entity = re.sub(r"[\s\-_()（）]+", "", normalized)
    if len(compact_entity) < 3 or len(re.findall(r"[\u4e00-\u9fff]", compact_entity)) < 2:
        return ""

    owners: dict[str, str] = {}
    exact_owner_found = False
    for product in products:
        sku = str(product.sku or "").strip().upper()
        display_name = str(product.product_name_cn or product.product_name_en or "").strip()
        if not sku or not display_name:
            continue
        aliases = {
            str(alias or "").strip()
            for source in (product.product_name_cn, product.product_name_en)
            for alias in customer_agent_service.product_name_aliases(str(source or ""))
            if str(alias or "").strip()
        }
        for alias in aliases:
            compact_alias = re.sub(
                r"[\s\-_()（）]+",
                "",
                customer_agent_service.normalize_search_text(alias).lower(),
            )
            if len(compact_alias) < 3:
                continue
            if compact_alias == compact_entity:
                exact_owner_found = True
                continue
            if _one_edit_apart(compact_entity, compact_alias):
                owners[sku] = display_name
                break
    if exact_owner_found:
        return ""
    return next(iter(owners.values())) if len(owners) == 1 else ""


def _apply_semantic_unique_typo_subject_recovery(
    db: Session,
    question: str,
    preplan: dict | None,
) -> bool:
    """Correct one unique literal typo after semantic product-scope sealing."""
    value = preplan if isinstance(preplan, dict) else {}
    if not (
        _semantic_preplan_confident(value, minimum=0.65)
        and not value.get("ambiguity")
        and not value.get("fallback_reason")
        and str(value.get("route_family") or "").strip() == "product_bound_qa"
    ):
        return False
    subject = str(value.get("subject_text") or "").strip()
    normalized_subject = customer_agent_service.normalize_search_text(subject)
    normalized_question = customer_agent_service.normalize_search_text(question)
    if not normalized_subject or normalized_subject not in normalized_question:
        return False
    products = db.query(Product).order_by(Product.sku.asc()).all()
    canonical = _semantic_unique_typo_near_catalog_subject(subject, products)
    if not canonical or canonical == subject:
        return False
    value["subject_text"] = canonical
    entities = [str(item or "").strip() for item in value.get("entities") or [] if str(item or "").strip()]
    value["entities"] = [canonical if item == subject else item for item in entities] or [canonical]
    value["semantic_identity_recovery"] = "catalog_unique_one_edit"
    return True


def _semantic_comparison_entity_contracts(
    question: str,
    semantic_preplan: dict | None,
    products: list[Product],
    *,
    context_anchor_sku: str | None = None,
) -> list[customer_entity_resolution_contract.EntityResolutionContract]:
    """Validate semantic comparison participants without accepting an LLM SKU decision."""
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    if (
        not _semantic_pairwise_contract_candidate(preplan)
        or not _semantic_preplan_confident(preplan, minimum=0.65)
    ):
        return []
    normalized_question = customer_agent_service.normalize_search_text(question)
    canonical_anchor_sku = str(context_anchor_sku or "").strip().upper()
    anchor_product = next(
        (
            product
            for product in products
            if str(product.sku or "").strip().upper() == canonical_anchor_sku
        ),
        None,
    )
    allow_context_anchor = bool(
        anchor_product is not None
        and str(preplan.get("context_usage") or "").strip() == "entity_anchor"
    )
    participants: list[str] = []
    context_anchor_participants: set[str] = set()
    coordinated_ellipsis_participants: set[str] = set()
    for raw_participant in preplan.get("entities") or []:
        participant = str(raw_participant or "").strip()
        normalized_participant = customer_agent_service.normalize_search_text(participant)
        if not normalized_participant or participant in participants:
            return []
        if normalized_participant not in normalized_question:
            anchor_identity_values = {
                customer_agent_service.normalize_search_text(value)
                for value in (
                    canonical_anchor_sku,
                    getattr(anchor_product, "product_name_cn", "") if anchor_product else "",
                    getattr(anchor_product, "product_name_en", "") if anchor_product else "",
                )
                if str(value or "").strip()
            }
            if allow_context_anchor and normalized_participant in anchor_identity_values:
                context_anchor_participants.add(participant)
            elif _is_coordinated_entity_ellipsis(
                normalized_question,
                normalized_participant,
                participants,
            ):
                coordinated_ellipsis_participants.add(participant)
            else:
                return []
        participants.append(participant)
    if len(participants) < 2:
        return []
    # Entity sealing proves identity only.  For a single formal comparison
    # criterion, preserve that already-validated field on every contract so
    # downstream evidence can be audited against the same canonical request.
    # Multiple criteria intentionally stay unbound here: a missing value must
    # produce a same-SKU evidence gap, never make a uniquely named product
    # disappear or let identity arbitration choose among criteria.
    canonical_fields = [
        customer_field_contract.semantic_preplan_field_type(item)
        for item in (preplan.get("canonical_fields") or [])
    ]
    canonical_fields = list(dict.fromkeys(field for field in canonical_fields if field))
    field_type = canonical_fields[0] if len(canonical_fields) == 1 else None
    contracts: list[customer_entity_resolution_contract.EntityResolutionContract] = []
    resolved_skus: set[str] = set()
    for participant_index, participant in enumerate(participants):
        if participant in context_anchor_participants and anchor_product is not None:
            normalized_participant = customer_agent_service.normalize_search_text(participant)
            matched_by = (
                "sku_exact"
                if normalized_participant
                == customer_agent_service.normalize_search_text(canonical_anchor_sku)
                else "canonical_name_exact"
            )
            contract = customer_entity_resolution_contract.EntityResolutionContract(
                entity_text=participant,
                normalized_entity_text=normalized_participant,
                status="resolved",
                resolved_sku=canonical_anchor_sku,
                resolver_candidate_skus=[canonical_anchor_sku],
                diagnostic_candidate_skus=[],
                candidate_skus=[canonical_anchor_sku],
                matched_by=matched_by,
                confidence="high",
                is_unique=True,
                matched_span=None,
                field_type=field_type,
                status_reason="trusted_context_anchor_exact",
            )
        else:
            contract_subject = participant
            if (
                customer_agent_service.normalize_search_text(participant)
                in normalized_question
                or participant in coordinated_ellipsis_participants
            ):
                contract_subject = (
                    _semantic_unique_typo_near_catalog_subject(participant, products)
                    or participant
                )
            contract = customer_entity_resolution_contract.build_entity_resolution_contract(
                question,
                products,
                entity_text_override=contract_subject,
                field_type_override=field_type,
                participant_local_identity=True,
            )
        decision = customer_entity_resolution_contract.can_resolve_single_product(contract, products)
        if (
            not decision.allowed
            and customer_agent_service.normalize_search_text(participant) in normalized_question
            and participant_index > 0
        ):
            expanded_contracts: dict[str, customer_entity_resolution_contract.EntityResolutionContract] = {}
            participant_start = normalized_question.find(
                customer_agent_service.normalize_search_text(participant)
            )
            for prior_participant in participants[:participant_index]:
                normalized_prior = customer_agent_service.normalize_search_text(prior_participant)
                prior_start = normalized_question.find(normalized_prior)
                prior_end = prior_start + len(normalized_prior)
                between = (
                    normalized_question[prior_end:participant_start]
                    if prior_start >= 0 and participant_start >= prior_end
                    else ""
                )
                if not any(joiner in between for joiner in ("和", "与", "跟", "及", "、", "/")):
                    continue
                # Chinese coordination may omit the shared family in the
                # second literal mention ("Family A 和 B"). Try only prefixes
                # from the already literal first participant; catalogue exact
                # or unique one-edit sealing remains mandatory.
                for cut in range(2, len(normalized_prior)):
                    expanded = f"{normalized_prior[:cut]}{participant}"
                    canonical = _semantic_unique_typo_near_catalog_subject(expanded, products) or expanded
                    expanded_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
                        question,
                        products,
                        entity_text_override=canonical,
                        field_type_override=field_type,
                        participant_local_identity=True,
                    )
                    expanded_decision = customer_entity_resolution_contract.can_resolve_single_product(
                        expanded_contract,
                        products,
                    )
                    if expanded_decision.allowed and expanded_decision.resolved_sku:
                        expanded_contracts[expanded_decision.resolved_sku] = expanded_contract
            if len(expanded_contracts) == 1:
                contract = next(iter(expanded_contracts.values()))
                decision = customer_entity_resolution_contract.can_resolve_single_product(contract, products)
        if not decision.allowed or not decision.resolved_sku or decision.resolved_sku in resolved_skus:
            return []
        if (
            participant in coordinated_ellipsis_participants
            and contract.matched_by not in {"canonical_name_exact", "normalized_alias_exact"}
        ):
            # Ellipsis completion may recover only an identity that the
            # catalogue itself seals exactly. Fuzzy recall is never enough.
            return []
        resolved_skus.add(decision.resolved_sku)
        contracts.append(contract)
    return contracts


def _is_coordinated_entity_ellipsis(
    normalized_question: str,
    normalized_participant: str,
    literal_participants: list[str],
) -> bool:
    """Recognize a model-expanded participant in a coordinated product phrase.

    Chinese shoppers commonly state a shared family only once, e.g.
    ``天鹅壶4杯黑色和9杯黑色``.  The semantic planner may expand the second
    participant to its complete catalogue name.  Accept that expansion only
    when a meaningful shared prefix belongs to an already literal participant
    and the entire distinguishing suffix is present in the current question;
    the caller still requires an exact, unique catalogue identity.
    """
    if not normalized_question or not normalized_participant:
        return False
    for literal in literal_participants:
        normalized_literal = customer_agent_service.normalize_search_text(literal)
        if not normalized_literal or normalized_literal not in normalized_question:
            continue
        shared = 0
        for left, right in zip(normalized_literal, normalized_participant):
            if left != right:
                break
            shared += 1
        suffix = normalized_participant[shared:]
        if shared >= 2 and len(suffix) >= 2 and suffix in normalized_question:
            return True
    return False


def _apply_semantic_comparison_plan(
    question: str,
    phase1_plan: dict[str, Any],
    semantic_preplan: dict | None,
    products: list[Product],
    *,
    context_anchor_sku: str | None = None,
) -> list[customer_entity_resolution_contract.EntityResolutionContract]:
    """Adapt one validated semantic comparison into sealed multi-entity execution."""
    contracts = _semantic_comparison_entity_contracts(
        question,
        semantic_preplan,
        products,
        context_anchor_sku=context_anchor_sku,
    )
    if not contracts:
        return []
    resolved_skus = [str(contract.resolved_sku or "").strip().upper() for contract in contracts]
    if len(resolved_skus) < 2 or any(not sku for sku in resolved_skus):
        return []
    phase1_plan.update(
        {
            "primary_intent": "comparison",
            "answer_type": "comparison",
            "product_refs": resolved_skus,
            "must_compare_both_products": True,
            "must_make_choice": bool(
                (semantic_preplan or {}).get("decision_requested")
                or customer_agent_planner_service._is_compare_choice_question(question)
            ),
            "semantic_comparison_entity_contracts": [contract.to_dict() for contract in contracts],
        }
    )
    return contracts


def _apply_explicit_sku_comparison_plan(
    question: str,
    phase1_plan: dict[str, Any],
    products: list[Product],
) -> list[customer_entity_resolution_contract.EntityResolutionContract]:
    """Seal a catalogue-exact SKU pair before any model-dependent fallback.

    This only preserves identities the customer literally supplied and the
    catalogue already validated.  It does not classify a comparison criterion
    or supply product facts, so the normal comparison evidence policy still
    decides what can be answered.
    """
    skus = _explicit_current_turn_comparison_skus_from_products(question, products)
    if len(skus) < 2:
        return []
    product_by_sku = {
        str(product.sku or "").strip().upper(): product
        for product in products
    }
    contracts: list[customer_entity_resolution_contract.EntityResolutionContract] = []
    for sku in skus:
        product = product_by_sku.get(sku)
        if product is None:
            return []
        start = str(question or "").upper().find(sku)
        contracts.append(customer_entity_resolution_contract.EntityResolutionContract(
            entity_text=sku,
            normalized_entity_text=customer_agent_service.normalize_search_text(sku),
            status="resolved",
            resolved_sku=sku,
            resolver_candidate_skus=[sku],
            diagnostic_candidate_skus=[],
            candidate_skus=[sku],
            matched_by="sku_exact",
            confidence="high",
            is_unique=True,
            matched_span=(start, start + len(sku)) if start >= 0 else None,
            field_type=None,
            status_reason="explicit_current_turn_comparison_sku",
        ))
    existing_intent = str(phase1_plan.get("primary_intent") or "").strip()
    phase1_plan.update({
        "primary_intent": (
            existing_intent
            if existing_intent in {"comparison", "product_compare_recommendation"}
            else "comparison"
        ),
        "answer_type": "comparison",
        "product_refs": skus,
        "must_compare_both_products": True,
        "semantic_comparison_entity_contracts": [contract.to_dict() for contract in contracts],
    })
    return contracts


def _semantic_comparison_fail_closed_result(semantic_preplan: dict | None) -> dict | None:
    """Keep an unsealed semantic comparison out of legacy free-form execution."""
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    if not preplan.get("called") or not _semantic_pairwise_contract_candidate(preplan):
        return None
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": (
            "我能看出你想比较两款商品，但其中至少一款名称还不能唯一对应具体型号。"
            "请补充两款的完整商品名或 SKU，我再按各自已核对的信息公平比较。"
        ),
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "answer_metadata": {
            "source": "semantic_comparison_unsealed_clarification",
            "evidence_status": "entity_unsealed",
        },
        "debug": {
            "agent_mode": "semantic_comparison_unsealed_clarification",
            "semantic_comparison_entities": list(preplan.get("entities") or []),
        },
        "skip_polish": True,
    }


def _semantic_pairwise_missing_criterion_result(
    rows: list[dict[str, Any]],
    entity_contracts: list[dict[str, Any]],
) -> dict[str, Any]:
    """Clarify a sealed pairwise purchase decision that lacks a criterion.

    The semantic plan has already established that the user wants one of two
    named products, but has not supplied a product field or suitability
    criterion.  Choosing from broad row text here would recreate the legacy
    rule-based comparator.  Keep both identities auditable and ask for the
    customer's actual priority instead.
    """
    candidate_skus = [
        str(row.get("sku") or "").strip().upper()
        for row in rows
        if str(row.get("sku") or "").strip()
    ]
    labels = [
        f"{str(row.get('product_name_cn') or row.get('product_name_en') or row.get('sku') or '').strip()}（{sku}）"
        for row, sku in zip(rows, candidate_skus)
    ]
    products_text = "、".join(label for label in labels if label) or "这两款商品"
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": (
            f"我已锁定你在比较的{products_text}，但还不知道你最在意的选择依据。"
            "请告诉我更看重容量、重量、使用场景或预算中的哪一项，我会只按各自同 SKU 的已标注资料帮你比较。"
        ),
        "results": [],
        "result_skus": [],
        "candidate_skus": candidate_skus,
        "needs_clarification": True,
        "answer_metadata": {
            "source": "semantic_pairwise_missing_criterion_clarification",
            "evidence_status": "criterion_missing",
            "final_choice_sku": None,
        },
        "debug": {
            "agent_mode": "semantic_pairwise_missing_criterion_clarification",
            "entity_resolution_contracts": entity_contracts,
        },
        "skip_polish": True,
    }


def _semantic_pairwise_realtime_fields_result(
    rows: list[dict[str, Any]],
    entity_contracts: list[dict[str, Any]],
    field_types: list[str],
) -> dict[str, Any]:
    """Preserve a sealed realtime comparison instead of choosing on unrelated data."""
    canonical_fields = list(dict.fromkeys(
        str(field or "").strip()
        for field in field_types
        if str(field or "").strip()
    ))
    candidate_skus = [
        str(row.get("sku") or "").strip().upper()
        for row in rows
        if str(row.get("sku") or "").strip()
    ]
    labels = [
        f"{str(row.get('product_name_cn') or row.get('product_name_en') or row.get('sku') or '').strip()}（{sku}）"
        for row, sku in zip(rows, candidate_skus)
    ]
    products_text = "、".join(label for label in labels if label) or "这两款商品"
    if canonical_fields == ["price"]:
        answer = (
            f"我已锁定你在比较的{products_text}，但当前资料没有维护这两款的实时价格，"
            "因此不能判断哪款更便宜。请以同一平台、同一时间的实际售价和优惠为准。"
        )
    elif canonical_fields == ["inventory"]:
        answer = (
            f"我已锁定你在比较的{products_text}，但当前资料没有维护这两款的实时库存，"
            "因此不能判断哪款现货更充足。请以平台或店铺页面为准。"
        )
    else:
        field_labels = [
            customer_field_contract.product_detail_field_label(field) or field
            for field in canonical_fields
        ]
        answer = (
            f"我已锁定你在比较的{products_text}，但当前资料没有维护可核验的"
            f"{'、'.join(field_labels)}，因此不能据此指定其中一款。"
        )
    return {
        "intent": "compare_products",
        "answer_type": "comparison",
        "answer": answer,
        "results": rows,
        "result_skus": candidate_skus,
        "candidate_skus": candidate_skus,
        "sources": [],
        "answer_metadata": {
            "source": "semantic_pairwise_realtime_field_boundary",
            "contract_field_types": canonical_fields,
            "evidence_status": "realtime_source_unavailable",
            "final_choice_sku": None,
        },
        "debug": {
            "agent_mode": "semantic_pairwise_realtime_field_boundary",
            "field_contract": {
                "field_type": canonical_fields[0] if len(canonical_fields) == 1 else "",
                "canonical_fields": canonical_fields,
                "source": "validated_semantic_preplan",
            },
            "entity_resolution_contracts": entity_contracts,
        },
        "skip_polish": True,
    }


def _invalid_semantic_pairwise_preplan_clarification_result(
    question: str,
    phase1_plan: dict[str, Any] | None,
    semantic_preplan: dict[str, Any] | None,
    products: list[Product],
) -> dict | None:
    """Fail closed when semantic comparison meaning is invalid before legacy prose can answer."""
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    if (
        not preplan.get("called")
        or not str(preplan.get("fallback_reason") or "").strip()
        or str(plan.get("primary_intent") or "") not in {"comparison", "product_compare_recommendation"}
        or len(products) < 2
    ):
        return None
    # Exact current-turn SKU contracts already seal both comparison
    # participants. A malformed semantic envelope must not discard that
    # identity and ask for a criterion the customer explicitly supplied.
    if len(plan.get("semantic_comparison_entity_contracts") or []) >= 2:
        return None
    contracts: list[customer_entity_resolution_contract.EntityResolutionContract] = []
    seen_skus: set[str] = set()
    for product in products[:8]:
        entity_text = str(product.product_name_cn or product.product_name_en or product.sku or "").strip()
        if not entity_text:
            return None
        contract = customer_entity_resolution_contract.build_entity_resolution_contract(
            question,
            products,
            entity_text_override=entity_text,
            field_type_override=None,
        )
        decision = customer_entity_resolution_contract.can_resolve_single_product(contract, products)
        sku = str(decision.resolved_sku or "").strip().upper()
        if not decision.allowed or not sku or sku in seen_skus:
            return None
        seen_skus.add(sku)
        contracts.append(contract)
    rows = [
        {
            "sku": str(product.sku or "").strip().upper(),
            "product_name_cn": str(product.product_name_cn or product.product_name_en or product.sku or "").strip(),
        }
        for product in products[:len(contracts)]
    ]
    result = _semantic_pairwise_missing_criterion_result(rows, [contract.to_dict() for contract in contracts])
    result["answer_metadata"]["source"] = "semantic_pairwise_invalid_preplan_clarification"
    result["answer_metadata"]["semantic_fallback_reason"] = str(preplan.get("fallback_reason") or "")
    result["debug"]["agent_mode"] = "semantic_pairwise_invalid_preplan_clarification"
    return result


def _semantic_product_navigation_contract_result(
    db: Session,
    question: str,
    semantic_preplan: dict | None,
) -> dict | None:
    """Seal an LLM-identified product-navigation turn before storing an anchor."""
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    # A semantic product_bound_qa with no requested field or evidence is a
    # conservative representation of navigation, not permission for legacy
    # generic retrieval.  The catalogue still seals the subject below.
    if str(preplan.get("route_family") or "") not in {"product_navigation", "product_bound_qa"}:
        return None
    if not _semantic_preplan_confident(preplan, minimum=0.9):
        return None
    if preplan.get("ambiguity") or preplan.get("evidence_required"):
        return None
    if preplan.get("canonical_fields") or preplan.get("field_type"):
        return None
    subject = str(preplan.get("subject_text") or "").strip()
    normalized_subject = customer_agent_service.normalize_search_text(subject)
    normalized_question = customer_agent_service.normalize_search_text(question)
    if not normalized_subject or normalized_subject not in normalized_question:
        return None
    products = db.query(Product).order_by(Product.sku.asc()).all()
    entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        products,
        entity_text_override=subject,
    )
    decision = customer_entity_resolution_contract.can_resolve_single_product(entity_contract, products)
    if not decision.allowed or not decision.resolved_sku:
        candidate_skus = [str(item or "").strip().upper() for item in entity_contract.candidate_skus if str(item or "").strip()]
        candidate_rows = [
            item for item in products
            if str(item.sku or "").strip().upper() in candidate_skus
        ]
        if candidate_rows:
            labels = "、".join(
                f"{str(item.product_name_cn or item.product_name_en or item.sku).strip()}（{str(item.sku or '').strip().upper()}）"
                for item in candidate_rows[:6]
            )
            return {
                "answer": f"“{subject}”当前能对应多款商品：{labels}。请告诉我想看哪一款，我再继续介绍具体资料。",
                "intent": "clarification",
                "answer_type": "clarification",
                "confidence": "high",
                "uncertainty": "ambiguous_product",
                "needs_clarification": True,
                "sources": [{"type": "product", "label": "语义导航候选", "sku": str(item.sku or "").strip().upper()} for item in candidate_rows],
                "actions": [],
                "results": [{"sku": item.sku, "product_name_cn": item.product_name_cn, "category": item.category} for item in candidate_rows],
                "result_skus": [str(item.sku or "").strip().upper() for item in candidate_rows],
                "candidate_skus": [str(item.sku or "").strip().upper() for item in candidate_rows],
                "steps": [{"type": "entity_navigation_contract", "label": "请求选择商品", "detail": "语义主体对应多个真实候选", "ok": True}],
                "warnings": [],
                "evidence": [],
                "answer_metadata": {"source": "semantic_product_navigation_contract", "evidence_status": "ambiguous_navigation_identity"},
                "debug": {"agent_mode": "semantic_product_navigation_clarification", "entity_resolution_contract": entity_contract.to_dict()},
                "sku": None,
                "skip_polish": True,
            }
        return None
    product = next(
        (item for item in products if str(item.sku or "").strip().upper() == decision.resolved_sku),
        None,
    )
    if product is None:
        return None
    sku = str(product.sku or "").strip().upper()
    name = str(product.product_name_cn or product.product_name_en or sku).strip()
    return {
        "answer": f"已切换到{name}（{sku}）。你可以继续问它的材质、尺寸、重量、配件或使用方式。",
        "intent": "product_detail",
        "answer_type": "product_detail",
        "confidence": "high",
        "uncertainty": "confirmed",
        "needs_clarification": False,
        "sources": [{"type": "product", "label": "语义导航实体合同", "sku": sku}],
        "actions": [],
        "results": [{"sku": sku, "product_name_cn": product.product_name_cn, "category": product.category}],
        "result_skus": [sku],
        "candidate_skus": [sku],
        "steps": [{"type": "entity_navigation_contract", "label": "建立当前商品上下文", "detail": f"语义主体经实体合同解析为 {sku}", "ok": True}],
        "warnings": [],
        "evidence": [],
        "answer_metadata": {
            "source": "semantic_product_navigation_contract",
            "evidence_status": "sealed_navigation_identity",
            "evidence_sku": sku,
            "evidence_skus": [sku],
        },
        "debug": {
            "agent_mode": "semantic_product_navigation_contract",
            "entity_resolution_contract": entity_contract.to_dict(),
        },
        "sku": sku,
        "skip_polish": True,
    }


def _semantic_product_navigation_preempts_catalog_count(semantic_preplan: dict | None) -> bool:
    """Keep a sealed product-navigation subject out of legacy all-product counts."""
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    return bool(
        str(preplan.get("route_family") or "") == "product_navigation"
        and _semantic_preplan_confident(preplan, minimum=0.9)
        and not preplan.get("ambiguity")
        and not preplan.get("evidence_required")
        and not preplan.get("canonical_fields")
        and not str(preplan.get("field_type") or "").strip()
        and str(preplan.get("subject_text") or "").strip()
    )


def _product_row_from_model(product: Product, specs: ProductSpecs | None = None, business: ProductBusiness | None = None, content: ProductContent | None = None) -> dict:
    raw_capacity = getattr(specs, "capacity", "") if specs else ""
    capacity_consistent = _capacity_evidence_is_internally_consistent(
        raw_capacity,
        " ".join(
            str(value or "")
            for value in (
                product.product_name_cn or product.product_name_en,
                getattr(specs, "technical_advantages", "") if specs else "",
                getattr(content, "long_description_cn", "") if content else "",
            )
        ),
    ) if raw_capacity else True
    return {
        "sku": product.sku,
        "barcode": product.barcode,
        "product_name_cn": product.product_name_cn,
        "product_name_en": product.product_name_en,
        "brand": product.brand,
        "series": product.series,
        "category": product.category,
        "sub_category": product.sub_category,
        "product_level": product.product_level,
        "launch_date": str(product.launch_date) if product.launch_date else "",
        "lifecycle_status": product.lifecycle_status,
        "capacity": raw_capacity if capacity_consistent else "",
        "capacity_evidence_conflict": not capacity_consistent,
        "size_info": getattr(specs, "size_info", "") if specs else "",
        "power": getattr(specs, "power", "") if specs else "",
        "heat_source": getattr(specs, "heat_source", "") if specs else "",
        "body_material": getattr(specs, "body_material", "") if specs else "",
        "surface_finish": getattr(specs, "surface_finish", "") if specs else "",
        "color": getattr(specs, "color", "") if specs else "",
        "usage_instruction": getattr(specs, "usage_instruction", "") if specs else "",
        "gross_weight_g": getattr(specs, "gross_weight_g", 0) if specs else 0,
        "features": getattr(specs, "technical_advantages", "") if specs else "",
        "usage_scenarios": getattr(business, "usage_scenarios", "") if business else "",
        "target_audience": getattr(business, "target_audience", "") if business else "",
        "positioning": getattr(business, "positioning", "") if business else "",
        "top_selling_points": getattr(business, "top_selling_points", "") if business else "",
        "price_positioning": getattr(business, "price_positioning", "") if business else "",
        # These customer-facing content columns are not independently used as
        # catalogue predicates.  They let the value-scope executor prove that
        # a customer-provided multilingual collection name occurs on the same
        # product row as one formal catalogue value.
        "title_cn": getattr(content, "title_cn", "") if content else "",
        "title_en": getattr(content, "title_en", "") if content else "",
        "website_title": getattr(content, "website_title", "") if content else "",
        "amazon_title": getattr(content, "amazon_title", "") if content else "",
        "long_description_en": getattr(content, "long_description_en", "") if content else "",
        "long_description_cn": getattr(content, "long_description_cn", "") if content else "",
    }


def _phase1_product_bundle_by_ref(db: Session, product_ref: str) -> tuple[Product | None, ProductSpecs | None, ProductBusiness | None, ProductContent | None]:
    ref = str(product_ref or "").strip()
    if not ref:
        return None, None, None, None
    exact_sku_value = _resolve_exact_sku_variant(db, ref) or ref.upper()
    exact_sku = db.query(Product).filter(Product.sku == exact_sku_value).first()
    if exact_sku:
        specs = db.query(ProductSpecs).filter(ProductSpecs.product_id == exact_sku.id).first()
        business = db.query(ProductBusiness).filter(ProductBusiness.product_id == exact_sku.id).first()
        content = db.query(ProductContent).filter(ProductContent.product_id == exact_sku.id).first()
        return exact_sku, specs, business, content
    product = (
        db.query(Product)
        .filter(
            (Product.product_name_cn == ref)
            | (Product.product_name_cn.contains(ref))
            | (Product.product_name_en.contains(ref))
            | (Product.sku == ref.upper())
        )
        .order_by(Product.sku.asc())
        .first()
    )
    if not product:
        return None, None, None, None
    specs = db.query(ProductSpecs).filter(ProductSpecs.product_id == product.id).first()
    business = db.query(ProductBusiness).filter(ProductBusiness.product_id == product.id).first()
    content = db.query(ProductContent).filter(ProductContent.product_id == product.id).first()
    return product, specs, business, content


def _field_display_value(requested_field: str, value: Any) -> str:
    """Remove a duplicated leading label without rewriting evidence content."""
    display = str(value or "").strip().rstrip("。.!！?？：:")
    label = str(requested_field or "").strip()
    if not display or not label:
        return display
    return re.sub(
        rf"^{re.escape(label)}(?:包括|有|是)?\s*[：:]\s*",
        "",
        display,
        count=1,
    ).strip()


def _dimension_missing_display_label(dimension_subtype: str | None, requested_field: str) -> str:
    """Keep a missing dimension answer scoped to the customer's component noun."""
    return {
        "expanded": "展开尺寸",
        "storage": "收纳尺寸",
        "package": "包装尺寸",
        "fuel_holder": "装酒精/木炭的燃料仓尺寸",
    }.get(dimension_subtype, requested_field)


def _phase1_product_field_result(db: Session, plan: dict) -> dict | None:
    product_ref = str(plan.get("product_ref") or "").strip()
    explicit_sku = str(plan.get("sku") or "").strip().upper()
    requested_field = str(plan.get("requested_field") or "").strip()
    raw_question = str(plan.get("raw_question") or "").strip()
    # Duration and fuel-efficiency questions are product QA, not heat-source
    # field reads.  A semantic/legacy planner may label a gas-related sentence
    # as ``热源``; defer it so the bounded duration path can answer (or state
    # that no duration evidence exists) instead of returning an unrelated
    # missing heat-source field.
    if customer_agent_intent_service._detect_usage_care_subtype(raw_question) in {"duration", "power_tradeoff"}:
        return None
    field_contract = customer_field_contract.detect_field_contract(raw_question)
    # “推荐哪种” without a field is still an underspecified catalogue
    # recommendation, but a formal field phrase such as “推荐用什么燃料” is a
    # product-bound fact request when the caller already sealed the SKU.
    if customer_agent_runtime_service._is_underspecified_recommendation_question(raw_question) and not field_contract:
        return None
    explicit_field_override = str(plan.get("field_type_override") or "").strip()
    # The question's deterministic FieldContract is authoritative over a
    # compatibility planner label.  Otherwise a broad legacy label such as
    # dimensions can turn a formal specification request into a different
    # safe-missing answer.
    canonical_field = (
        explicit_field_override
        if customer_field_contract.is_supported_detail_field(explicit_field_override)
        else field_contract.field_type if field_contract else
        customer_field_contract.field_type_from_detail_label(requested_field)
    )
    canonical_label = customer_field_contract.product_detail_field_label(canonical_field)
    if canonical_label:
        requested_field = canonical_label
        plan["requested_field"] = requested_field
    if requested_field == "适用场景":
        canonical_field = "usage_scene"
    resolved_ref = (
        _resolve_exact_sku_variant(db, explicit_sku or product_ref or raw_question)
        or explicit_sku
        or _resolve_sku(db, raw_question or product_ref, product_ref or None)
        or _resolve_sku(db, product_ref, None)
        or product_ref
    )
    if resolved_ref:
        plan["product_ref"] = resolved_ref
        plan["sku"] = resolved_ref
    product, specs, business, content = _phase1_product_bundle_by_ref(db, resolved_ref)
    identity_source = str(plan.get("product_identity_source") or "").strip() or None
    sealed_entity_contract = None
    initial_identity_decision = (
        customer_entity_resolution_contract.can_trust_bound_product_identity(
            product=product,
            sku=resolved_ref,
            identity_source=identity_source,
        )
        if product
        else None
    )
    # A weak planner reference may be repaired by a direct canonical name in
    # the question. Once a trusted upstream identity contract has sealed a
    # SKU, however, this evidence provider must consume that SKU and must not
    # independently re-resolve the raw question to another product.
    if (
        canonical_field
        and raw_question
        and not bool(initial_identity_decision and initial_identity_decision.allowed)
    ):
        # The legacy field executor may receive only a weak planner reference.
        # Repair it through the same sealed EntityResolutionContract used by
        # Phase 2, never by a first fuzzy database row.  This also permits a
        # field-specific safe-missing answer for an exact named product whose
        # requested column has no valid value.
        all_products = db.query(Product).order_by(Product.sku.asc()).all()
        question_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
            raw_question,
            all_products,
            field_type_override=canonical_field,
        )
        resolution = customer_entity_resolution_contract.can_resolve_single_product(
            question_contract,
            all_products,
        )
        if resolution.allowed and resolution.resolved_sku:
            sealed_entity_contract = question_contract
            product, specs, business, content = _phase1_product_bundle_by_ref(db, resolution.resolved_sku)
            resolved_ref = resolution.resolved_sku
            plan["product_ref"] = resolved_ref
            plan["sku"] = resolved_ref
            plan["entity_resolution_contract"] = question_contract.to_dict()
    if product:
        identity_decision = customer_entity_resolution_contract.can_trust_bound_product_identity(
            product=product,
            sku=resolved_ref,
            entity_contract=sealed_entity_contract,
            identity_source=identity_source,
        )
        if not identity_decision.allowed:
            return {
                "intent": "clarify",
                "answer_type": "clarification",
                "answer": f"我还不能把“{product_ref}”稳定定位到唯一商品。请补充 SKU 或更完整的商品名。",
                "results": [],
                "result_skus": [],
                "candidate_skus": [],
                "needs_clarification": True,
                "answer_metadata": {"source": "entity_strength_gate_clarification"},
                "debug": {
                    "agent_mode": "entity_strength_gate_clarification",
                    "bound_product_identity_decision": identity_decision.to_dict(),
                },
                "skip_polish": True,
            }
    if not product:
        return {
            "intent": "product_detail",
            "answer_type": "product_detail",
            "answer": f"当前资料中暂未标注{resolved_ref}的{requested_field}。",
            "results": [],
            "result_skus": [],
            "answer_metadata": {"answer_policy": "field_only_missing"},
            "debug": {"agent_mode": "planner_product_field"},
            "skip_polish": True,
        }
    # Structured product fields are authoritative.  Same-SKU QA remains a
    # field-compatible fallback below when the validated database provider is
    # empty; an exact wording match alone must not replace a formal field.
    value = ""
    evidence_source = None
    evidence_scope = "subject"
    dimension_evidence = None
    dimension_subtype = (
        customer_field_contract.requested_dimension_subtype(raw_question)
        if canonical_field == "dimensions"
        else None
    )
    cleaning_subtype = (
        customer_field_contract.requested_cleaning_subtype(raw_question)
        if canonical_field == "cleaning"
        else None
    )
    usage_instruction_subtype = (
        customer_field_contract.requested_usage_instruction_subtype(raw_question)
        if canonical_field == "usage_instruction"
        else None
    )
    content_subtype = _content_field_evidence_subtype(raw_question, canonical_field)
    if canonical_field == "accessories":
        accessories_qa = _best_product_qa_match(
            db,
            product,
            raw_question or requested_field,
            requested_field_type="accessories",
        )
        value = str(getattr(accessories_qa, "answer", "") or "").strip()
        evidence_source = f"product_qa:{accessories_qa.id}" if accessories_qa and value else None
    elif requested_field == "SKU":
        value = str(product.sku or "").strip().upper()
        evidence_source = "product.sku"
    elif requested_field == "尺寸":
        dimension_evidence = customer_field_contract.select_dimension_evidence(
            getattr(specs, "size_info", "") if specs else "",
            requested_scope=customer_field_contract.requested_evidence_scope(raw_question, "dimensions"),
            requested_subtype=customer_field_contract.requested_dimension_subtype(raw_question),
        )
        value = dimension_evidence.value if dimension_evidence else ""
        evidence_source = "specs.size_info" if dimension_evidence else None
        evidence_scope = dimension_evidence.scope if dimension_evidence else customer_field_contract.requested_evidence_scope(raw_question, "dimensions")
        if value in {"", "[]", "/"}:
            value = ""
    elif requested_field == "容量":
        value = _capacity_field_evidence(getattr(specs, "capacity", "") if specs else "")
        evidence_source = "specs.capacity" if value else None
    elif requested_field == "重量":
        weight = getattr(specs, "gross_weight_g", None)
        # Keep this older phase-one path on the same validated formatter as
        # the central structured-evidence provider.  A database float such as
        # 150.0 is a gram value, not a customer-facing precision claim.
        value = _weight_field_evidence(weight, capacity=getattr(specs, "capacity", "") if specs else "")
        evidence_source = "specs.gross_weight_g" if value else None
    elif requested_field == "材质":
        value = str(getattr(specs, "body_material", "") or "").strip()
        evidence_source = "specs.body_material" if value else None
        if value in {"", "[]", "/"}:
            value = ""
    elif requested_field == "颜色":
        value = _color_field_evidence(getattr(specs, "color", "") if specs else "")
        evidence_source = "specs.color" if value else None
    elif canonical_field == "usage_scene":
        value = _compound_display_value(getattr(business, "usage_scenarios", "") if business else "")
        evidence_source = "business.usage_scenarios" if value else None
    # Structured product fields are authoritative. A same-SKU QA entry that
    # passes the canonical field policy is only a fallback when the structured
    # provider is missing or fails its semantic validation.
    if not value and canonical_field in {"usage_instruction", "cleaning", "care"}:
        value, evidence_source = _structured_product_field_evidence(
            canonical_field,
            db=db,
            product=product,
            specs=specs,
            business=business,
            content=content,
            requested_subtype=content_subtype or usage_instruction_subtype or cleaning_subtype,
        )
    if not value and canonical_field and canonical_field not in {
        "dimensions",
        "people",
        "dishwasher",
        "heat_source",
        "accessories",
    }:
        value, evidence_source = _structured_product_field_evidence(
            canonical_field,
            db=db,
            product=product,
            specs=specs,
            business=business,
            content=content,
            requested_subtype=content_subtype or usage_instruction_subtype or cleaning_subtype,
        )
    if not value and canonical_field:
        qa_evidence = _best_product_qa_match(
            db,
            product,
            raw_question or requested_field,
            requested_field_type=canonical_field,
            # The FieldContract already fixed the customer field and the
            # EntityResolutionContract already fixed this SKU.  Restrict the
            # fallback to request-side QA text (question/tags), so natural
            # wording such as a platform-purchase question can use a
            # field-compatible same-SKU QA without answer-body keyword drift.
            semantic_query=raw_question or requested_field,
        )
        qa_value = str(getattr(qa_evidence, "answer", "") or "").strip()
        if canonical_field == "dimensions" and qa_value and dimension_subtype:
            qa_subtype = customer_field_contract.requested_dimension_subtype(
                " ".join((
                    str(getattr(qa_evidence, "question", "") or ""),
                    str(getattr(qa_evidence, "tags", "") or ""),
                ))
            )
            if qa_subtype != dimension_subtype:
                qa_value = ""
        if canonical_field in {"usage_instruction", "cleaning", "care"} and qa_value:
            if not _usage_instruction_matches_product(product, qa_value):
                qa_value = ""
            elif canonical_field == "cleaning" and cleaning_subtype == "machine_wash":
                qa_value = _machine_wash_specific_evidence(qa_value)
            elif canonical_field == "usage_instruction" and usage_instruction_subtype == "liquid_temperature_capability":
                qa_value = _liquid_temperature_capability_evidence(qa_value)
        if qa_value:
            value = qa_value
            evidence_source = f"product_qa:{qa_evidence.id}"
            evidence_scope = "subject"
    if not value and canonical_field and canonical_field not in {
        "dimensions",
        "people",
        "dishwasher",
        "heat_source",
        "accessories",
    }:
        value, evidence_source = _structured_product_field_evidence(
            canonical_field,
            db=db,
            product=product,
            specs=specs,
            business=business,
            content=content,
            requested_subtype=content_subtype or usage_instruction_subtype or cleaning_subtype,
        )
    row = _product_row_from_model(product, specs, business, content)
    evidence_value = value
    capacity_conflict = (
        _capacity_evidence_conflict(product.product_name_cn, value)
        if requested_field == "容量" and value
        else None
    )
    color_conflict = (
        _color_evidence_conflict(product.product_name_cn, value)
        if requested_field == "颜色" and value
        else None
    )
    # Product-detail callers may provide either the display label ("热源")
    # or the canonical FieldContract type ("heat_source").  Evidence routing
    # must follow the contract, not the incidental display representation;
    # otherwise a sealed EntityResolutionContract skips its own same-SKU
    # structured heat-source evidence and falsely reports it as missing.
    if canonical_field == "heat_source":
        if not str(row.get("heat_source") or "").strip():
            merged_detail = product_service.get_product_detail(db, product.sku)
            merged_specs = merged_detail.get("specs") if isinstance(merged_detail, dict) else {}
            merged_heat_source = str((merged_specs or {}).get("heat_source") or "").strip()
            if merged_heat_source:
                row["heat_source"] = merged_heat_source
        if str(row.get("heat_source") or "").strip():
            evidence_source = "specs.heat_source"
        heat_source_evidence: dict[str, Any] = {}
        answer, evidence_status = _phase1_heat_source_capability_answer(
            db,
            row,
            raw_question or requested_field,
            evidence_metadata=heat_source_evidence,
        )
        evidence_value = str(heat_source_evidence.get("evidence_value") or "").strip()
        evidence_source = heat_source_evidence.get("evidence_source") or evidence_source
        evidence_scope = heat_source_evidence.get("evidence_scope") or evidence_scope
        if str(evidence_source or "").startswith("product_qa:"):
            evidence_source = "product_qa"
    elif requested_field == "适用人数":
        people_evidence: dict[str, Any] = {}
        answer = _phase1_people_capacity_answer(row, evidence_metadata=people_evidence)
        evidence_value = str(people_evidence.get("evidence_value") or "").strip()
        evidence_source = people_evidence.get("evidence_source")
        evidence_scope = people_evidence.get("evidence_scope") or evidence_scope
        evidence_status = "structured" if evidence_value else "missing"
    elif requested_field == "洗碗机适配":
        instruction = str(getattr(specs, "usage_instruction", "") or "").strip()
        dishwasher_evidence = (
            value
            if value and str(evidence_source or "").startswith("product_qa:")
            else _dishwasher_specific_evidence(instruction)
        )
        if dishwasher_evidence:
            evidence_value = dishwasher_evidence
            evidence_source = evidence_source or "specs.usage_instruction"
            answer = f"{product.product_name_cn}（{product.sku}）的洗碗机适配资料：{dishwasher_evidence}。"
            evidence_status = "structured"
        else:
            evidence_value = ""
            evidence_source = None
            answer = f"{product.product_name_cn}（{product.sku}）当前资料未标注，未明确说明是否可放入洗碗机。"
            evidence_status = "missing"
    elif capacity_conflict:
        name_capacity, structured_capacity = capacity_conflict
        answer = (
            f"{product.product_name_cn}（{product.sku}）的容量资料存在冲突："
            f"商品名称标注{name_capacity}，规格字段记录{structured_capacity}；"
            "当前无法确认准确容量，请以官方页面或人工核实结果为准。"
        )
        evidence_value = ""
        evidence_source = None
        evidence_status = "conflict"
    elif color_conflict:
        name_color, structured_color = color_conflict
        answer = (
            f"{product.product_name_cn}（{product.sku}）的颜色资料存在冲突："
            f"商品名称标注{name_color}，规格字段记录{structured_color}；"
            "当前无法确认准确颜色，请以官方页面或人工核实结果为准。"
        )
        evidence_value = ""
        evidence_source = None
        evidence_status = "conflict"
    elif value:
        if requested_field == "尺寸" and dimension_evidence is not None:
            display_value = dimension_evidence.value.replace("*", "×")
            display_unit = f" {dimension_evidence.unit}" if dimension_evidence.unit else ""
            if dimension_evidence.is_generic_fallback and dimension_subtype not in {"expanded", "storage", "package"}:
                answer = (
                    f"当前资料未单独维护{product.product_name_cn}（{product.sku}）的产品本体尺寸；"
                    f"仅记录了{dimension_evidence.label}：{display_value}{display_unit}。"
                )
            else:
                answer = (
                    f"{product.product_name_cn}（{product.sku}）的{dimension_evidence.label or requested_field}："
                    f"{display_value}{display_unit}。"
                )
        else:
            display_value = _field_display_value(requested_field, value)
            if requested_field == "核心卖点":
                display_value = re.sub(
                    rf"^(?:{re.escape(str(product.product_name_cn or '').strip())}的)?核心卖点(?:包括|有|是)?[：:]\s*",
                    "",
                    display_value,
                ).strip()
            answer = f"{product.product_name_cn}（{product.sku}）的{requested_field}：{display_value}。"
        evidence_status = "structured"
    else:
        dimension_missing_label = _dimension_missing_display_label(dimension_subtype, requested_field)
        if canonical_field == "cleaning" and cleaning_subtype == "machine_wash":
            answer = f"当前资料未明确标注{product.product_name_cn}（{product.sku}）是否可以用洗衣机机洗。"
        elif canonical_field == "cleaning":
            answer = (
                f"当前同 SKU 资料中暂未找到{product.product_name_cn}（{product.sku}）的具体清洁说明，"
                "暂时无法确认清洁步骤；建议以产品说明书或人工客服核对结果为准。"
            )
        elif canonical_field == "usage_instruction" and usage_instruction_subtype == "liquid_temperature_capability":
            answer = f"{product.product_name_cn}（{product.sku}）：当前资料未直接标明是否可以装热水、冷水或开水。"
        elif canonical_field == "accessories":
            answer = (
                f"{product.product_name_cn}（{product.sku}）：当前资料未标注套装包含内容、"
                "包装清单或具体配件，无法确认具体清单。"
            )
        else:
            answer = (
                f"\u5f53\u524d\u8d44\u6599\u4e2d\u6682\u672a\u627e\u5230"
                f"{product.product_name_cn}\uff08{product.sku}\uff09\u7684{dimension_missing_label}\u3002"
            )
        evidence_status = "missing"
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "sku": product.sku,
        "answer": answer,
        "results": [row],
        "result_skus": [product.sku],
        "candidate_skus": [product.sku],
        "answer_metadata": {
            "answer_policy": "field_only",
            "requested_field": canonical_field or requested_field,
            "requested_field_label": requested_field,
            "evidence_status": evidence_status,
            "evidence_field": canonical_field if evidence_value else None,
            "evidence_source": evidence_source,
            "evidence_value": evidence_value,
            "evidence_scope": evidence_scope if evidence_value else None,
            "evidence_sku": product.sku if evidence_value else None,
            "evidence_label": dimension_evidence.label if dimension_evidence else None,
            "evidence_unit": dimension_evidence.unit if dimension_evidence else None,
            "evidence_subtype": dimension_evidence.subtype if dimension_evidence else dimension_subtype or cleaning_subtype,
            "dimension_generic_fallback": dimension_evidence.is_generic_fallback if dimension_evidence else False,
            "field_evidence_match": bool(canonical_field and evidence_value),
            "field_evidence_missing": bool(canonical_field and not evidence_value),
        },
        "debug": {
            "agent_mode": "planner_product_compatibility" if requested_field == "heat_source" else "planner_product_field",
            "plan": {
                "primary_intent": "product_field",
                "product_ref": product.sku,
                "requested_field": requested_field,
                "raw_question": raw_question or requested_field,
            },
            "raw_results": [row],
            "candidate_skus": [product.sku],
        },
        "skip_polish": True,
    }


def _phase1_alcohol_stove_compatibility_answer(
    db: Session,
    row: dict,
    *,
    evidence_metadata: dict[str, Any] | None = None,
) -> tuple[str, str]:
    name = str(row.get("product_name_cn") or row.get("product_name_en") or row.get("sku") or "该产品").strip()
    sku = str(row.get("sku") or "").strip().upper()
    category = str(row.get("category") or "").strip()
    heat_source = _heat_source_field_evidence(row.get("heat_source"))
    normalized_heat_source = "" if heat_source in {"", "/", "[]"} else heat_source
    if not normalized_heat_source and not any(term in category for term in ("锅", "炉", "壶", "酒具", "咖啡")):
        return f"{name}（{sku}）不是炉具或炊具，当前资料也未标注适用酒精炉；不建议按酒精炉适配产品理解。", "not_applicable"
    def _capability_evidence_status(value: str) -> str | None:
        text_value = str(value or "").strip().lower()
        if not text_value:
            return None
        if re.search(r"(?:未(?:标注|显示|找到)|没有|暂无).{0,20}(?:支持|适用|可用).{0,8}(酒精炉|alcohol stove)", text_value):
            return "not_listed"
        if re.search(r"(不支持|不适合|不建议|不能|不可).{0,8}(酒精炉|alcohol stove)", text_value):
            return "unsupported"
        if any(term in text_value for term in ("液体酒精", "工业酒精", "alcohol fuel")):
            return "supported"
        verdict = customer_agent_intent_service._alcohol_stove_support_verdict(text_value)
        if verdict is True:
            return "supported"
        if verdict is False:
            return "unsupported"
        return None

    evidence_status = _capability_evidence_status(normalized_heat_source)
    evidence = ""
    if normalized_heat_source and evidence_metadata is not None:
        evidence_metadata.update({
            "evidence_value": normalized_heat_source,
            "evidence_source": "specs.heat_source",
            "evidence_scope": "subject",
            "evidence_sku": sku,
        })
    if evidence_status is None and sku:
        evidence = str(customer_agent_intent_service._best_same_sku_evidence_text(
            db,
            sku=sku,
            supporting=None,
            term_groups=[("酒精炉", "alcohol stove")],
        ) or "").strip()
        evidence_status = _capability_evidence_status(evidence)
        if evidence and evidence_metadata is not None:
            evidence_metadata.update({
                "evidence_value": evidence,
                "evidence_source": "product_qa",
                "evidence_scope": "subject",
                "evidence_sku": sku,
            })
    if evidence_status == "supported":
        source_text = evidence or normalized_heat_source
        return f"{name}（{sku}）当前资料显示支持酒精炉；适用热源/同 SKU 证据为：{source_text}。", "supported"
    if evidence_status == "unsupported":
        return f"{name}（{sku}）当前资料显示不支持酒精炉；相关证据为：{evidence or normalized_heat_source}。", "unsupported"
    if normalized_heat_source or evidence_status == "not_listed":
        return f"{name}（{sku}）当前资料显示适用热源为：{normalized_heat_source}；当前资料未显示支持酒精炉。", "not_listed"
    return f"当前资料里没有找到{name}（{sku}）是否支持酒精炉的明确说明。", "missing"


def _same_sku_usage_instruction_heat_source_boundary(
    usage_instruction: Any,
    *,
    asked_capability: str,
    asks_direct_heating: bool,
) -> str:
    """Return an explicit negative heat-source boundary from same-SKU instructions.

    ``usage_instruction`` is not a general heat-source provider.  It is used
    here only when one complete source sentence directly negates the specific
    heat source or direct-heating operation in the customer's question.
    """
    text = str(usage_instruction or "").strip()
    if not text or text in {"[]", "/", "暂无", "未标注"}:
        return ""
    capability_terms = {
        "卡式炉": ("卡式炉",),
        "燃气炉": ("燃气炉", "气炉", "卡式炉"),
        "气炉": ("气炉", "燃气炉", "卡式炉"),
        "明火直烧": ("明火直烧", "明火"),
        "明火": ("明火", "明火直烧"),
        "分体炉": ("分体炉",),
        "一体炉": ("一体炉",),
        "电磁炉": ("电磁炉",),
        "电陶炉": ("电陶炉",),
        "电热炉": ("电热炉",),
        "电炉": ("电炉", "电磁炉", "电陶炉", "电热炉"),
    }.get(asked_capability, ())
    negative_markers = ("不可", "不能", "不建议", "严禁", "禁止")
    for sentence in re.split(r"(?<=[。！？!?；;])\s*", text):
        for clause in re.split(r"(?<=[，,])\s*", sentence):
            candidate = re.sub(r"\s+", " ", clause).strip()
            if not candidate or not any(marker in candidate for marker in negative_markers):
                continue
            directly_names_capability = bool(capability_terms and any(term in candidate for term in capability_terms))
            directly_names_operation = asks_direct_heating and any(
                term in candidate for term in ("直接加热", "直接置于", "直接放", "明火上加热")
            )
            if directly_names_capability or directly_names_operation:
                return candidate.rstrip("。；; ")
    return ""


def _requested_fuel_capabilities(text: str) -> list[str]:
    """Extract all explicit fuel/heat-source alternatives from one turn.

    This is a small ontology for fuel *wording*, not a product/SKU rule.  It
    keeps a choice such as ``液体还是固体`` from being truncated to the first
    token and deliberately ignores a bare ``酒精炉`` category noun.
    """
    value = str(text or "").strip()
    if not value:
        return []
    action_markers = (
        "能烧", "可以烧", "能用", "可以用", "支持", "适配", "兼容", "能否", "可不可以",
        "可以吗", "能吗", "适合吗", "只能用", "用什么", "使用", "还是", "推荐用", "选择",
    )
    if not any(marker in value for marker in action_markers):
        return []
    requested: list[str] = []

    def add(label: str) -> None:
        if label not in requested:
            requested.append(label)

    electric_aliases = (
        ("电磁炉", ("电磁炉", "电池炉")),
        ("电陶炉", ("电陶炉",)),
        ("电热炉", ("电热炉",)),
        ("电炉", ("电炉",)),
    )
    for label, aliases in electric_aliases:
        if any(alias in value for alias in aliases):
            add(label)
    canister_subtypes = (
        ("液化气罐", ("液化气罐", "液化气瓶")),
        ("高山气罐", ("高山气罐", "高山罐")),
        ("卡式气罐", ("卡式气罐", "卡式罐", "长罐")),
        ("扁罐", ("扁罐",)),
    )
    for label, aliases in canister_subtypes:
        if any(alias in value for alias in aliases):
            add(label)
    if any(term in value for term in ("固体酒精", "固态酒精", "酒精块", "固体燃料")) or (
        "固体" in value and "酒精" in value
    ):
        add("固体酒精")
    if "液体酒精" in value or ("液体" in value and "酒精" in value):
        add("液体酒精")
    if any(term in value for term in ("木炭", "炭火", "炭烧", "烧炭", "碳火", "碳烧", "速燃碳")):
        add("木炭")
    if "柴火" in value:
        add("柴火")
    if "煤炭" in value:
        add("煤炭")
    if not requested and "酒精" in value and "酒精炉" not in value:
        add("酒精")
    return requested


def _looks_like_fuel_choice_question(text: str) -> bool:
    """Whether the customer explicitly asks to choose between fuel types."""
    value = str(text or "").strip()
    requested = _requested_fuel_capabilities(value)
    return len(requested) >= 2 and any(term in value for term in ("还是", "选择", "哪个", "推荐", "怎么选"))


def _requested_fuel_capability(text: str) -> str:
    """Extract the first explicit fuel subtype without treating heat words as proof."""
    value = str(text or "").strip()
    requested = _requested_fuel_capabilities(value)
    if requested:
        return requested[0]
    action_markers = ("能烧", "可以烧", "能用", "可以用", "支持", "适配", "兼容", "能否", "可不可以", "使用")
    if "酒精炉" in value and any(marker in value for marker in action_markers):
        return "酒精炉"
    if "酒精" in value and any(marker in value for marker in action_markers):
        return "酒精"
    return ""


def _phase1_fuel_capability_answer(
    row: dict,
    question: str,
    *,
    asked_fuel: str,
    evidence_metadata: dict[str, Any] | None = None,
) -> tuple[str, str]:
    """Answer an explicit fuel-subtype question from the same-SKU heat field."""
    name = str(row.get("product_name_cn") or row.get("product_name_en") or row.get("sku") or "该产品").strip()
    sku = str(row.get("sku") or "").strip().upper()
    prefix = f"{name}（{sku}）" if name else sku
    heat_source = _heat_source_field_evidence(row.get("heat_source"))
    normalized_heat_source = "" if heat_source in {"", "/", "[]", "暂无", "未标注"} else heat_source
    if normalized_heat_source and evidence_metadata is not None:
        evidence_metadata.update({
            "evidence_value": normalized_heat_source,
            "evidence_source": "specs.heat_source",
            "evidence_scope": "subject",
            "evidence_sku": sku,
        })
    if not normalized_heat_source:
        return f"{prefix}：当前资料未标注适用热源；是否支持{asked_fuel}，不能仅凭现有资料确认。", "missing"

    source_lower = normalized_heat_source.lower()
    fuel_aliases = {
        "固体酒精": ("固体酒精", "固态酒精", "酒精块", "固体燃料"),
        "液体酒精": ("液体酒精",),
        "木炭": ("木炭", "炭火", "炭烧", "烧炭", "碳火", "碳烧", "速燃碳"),
        "柴火": ("柴火",),
        "煤炭": ("煤炭",),
        "酒精": ("酒精", "alcohol"),
        "电磁炉": ("电磁炉", "电池炉"),
        "电陶炉": ("电陶炉",),
        "电热炉": ("电热炉",),
        "电炉": ("电炉", "电磁炉", "电陶炉", "电热炉", "电池炉"),
        "液化气罐": ("液化气罐", "液化气瓶"),
        "高山气罐": ("高山气罐", "高山罐"),
        "卡式气罐": ("卡式气罐", "卡式罐", "长罐"),
        "扁罐": ("扁罐",),
    }
    canister_labels = {"液化气罐", "高山气罐", "卡式气罐", "扁罐"}
    if asked_fuel in canister_labels:
        supported = any(term in normalized_heat_source for term in fuel_aliases[asked_fuel])
        if supported:
            return (
                f"{prefix}：兼容性结论：当前资料明确标注支持{asked_fuel}；适用热源为{normalized_heat_source}。",
                "supported",
            )
        return (
            f"{prefix}：兼容性结论：当前资料只标注适用热源为{normalized_heat_source}，"
            f"未标注{asked_fuel}的接口或具体罐型，无法确认{asked_fuel}也可以用。",
            "missing",
        )
    if asked_fuel == "酒精":
        supported = any(term in normalized_heat_source or term in source_lower for term in fuel_aliases["酒精"])
    elif asked_fuel in {"固体酒精", "液体酒精"}:
        # A generic “酒精炉” label does not establish whether the fuel is
        # solid or liquid.  Require the requested subtype itself.
        supported = any(term in normalized_heat_source for term in fuel_aliases[asked_fuel])
    else:
        # Charcoal/wood are not synonyms for the broad “明火” label.  Require
        # the explicit fuel wording to avoid silently substituting heat sources.
        supported = any(term in normalized_heat_source for term in fuel_aliases.get(asked_fuel, (asked_fuel,)))
    if supported:
        return f"{prefix}：当前资料显示支持{asked_fuel}；适用热源为{normalized_heat_source}。", "supported"
    return f"{prefix}：当前资料未显示支持{asked_fuel}；当前资料显示适用热源为{normalized_heat_source}。", "not_listed"


def _phase1_multi_fuel_capability_answer(
    row: dict,
    question: str,
    *,
    asked_fuels: list[str],
    evidence_metadata: dict[str, Any] | None = None,
) -> tuple[str, str]:
    """Answer every explicitly named fuel alternative from the same SKU."""
    name = str(row.get("product_name_cn") or row.get("product_name_en") or row.get("sku") or "该产品").strip()
    sku = str(row.get("sku") or "").strip().upper()
    prefix = f"{name}（{sku}）" if name else sku
    electric_labels = {"电磁炉", "电陶炉", "电热炉", "电炉"}
    conclusion_label = "热源选择结论" if any(fuel in electric_labels for fuel in asked_fuels) else "燃料选择结论"
    lines = [f"{prefix}：{conclusion_label}："]
    statuses: list[str] = []
    for fuel in asked_fuels:
        one, status = _phase1_fuel_capability_answer(
            row,
            question,
            asked_fuel=fuel,
            evidence_metadata=evidence_metadata,
        )
        body = one[len(prefix):].lstrip("：: ") if prefix and one.startswith(prefix) else one
        lines.append(f"- {fuel}：{body}")
        statuses.append(status)
    if any(status == "supported" for status in statuses) and any(status != "supported" for status in statuses):
        lines.append("按当前同 SKU 资料，只能把明确标注支持的燃料作为已核验选项；未标注或未显示支持的燃料不能直接当作替代。")
        supported_labels = [fuel for fuel, status in zip(asked_fuels, statuses) if status == "supported"]
        lines.append(f"结论：当前只建议选择资料明确标注支持的{'、'.join(supported_labels)}；其他选项不建议替代。")
    elif not any(status == "supported" for status in statuses):
        lines.append("当前资料没有把这些燃料都核验为可用，不能仅凭明火或酒精炉类别替代判断。")
        lines.append("结论：当前没有核验出其中任何一种可用，不建议仅凭现有资料任选其一；请按说明书或提供 SKU 再确认。")
    else:
        lines.append("结论：当前资料明确核验支持这些选项；实际选择仍应以对应燃料说明书和使用环境为准。")
    return "\n".join(lines), ("supported" if all(status == "supported" for status in statuses) else "not_listed")


def _phase1_heat_source_capability_answer(
    db: Session,
    row: dict,
    question: str,
    *,
    evidence_metadata: dict[str, Any] | None = None,
) -> tuple[str, str]:
    text = str(question or "").strip()
    identity_free_text = text
    for identity in (
        row.get("product_name_cn"),
        row.get("product_name_en"),
        row.get("sku"),
    ):
        identity_text = str(identity or "").strip()
        if identity_text:
            identity_free_text = re.sub(re.escape(identity_text), "", identity_free_text, flags=re.IGNORECASE)
    asked_fuels = _requested_fuel_capabilities(identity_free_text)
    if len(asked_fuels) >= 2:
        return _phase1_multi_fuel_capability_answer(
            row,
            text,
            asked_fuels=asked_fuels,
            evidence_metadata=evidence_metadata,
        )
    asked_fuel = asked_fuels[0] if asked_fuels else _requested_fuel_capability(identity_free_text)
    if asked_fuel == "酒精炉":
        return _phase1_alcohol_stove_compatibility_answer(
            db,
            row,
            evidence_metadata=evidence_metadata,
        )
    if asked_fuel:
        return _phase1_fuel_capability_answer(
            row,
            text,
            asked_fuel=asked_fuel,
            evidence_metadata=evidence_metadata,
        )
    if any(term in identity_free_text for term in ("酒精炉", "酒精")):
        return _phase1_alcohol_stove_compatibility_answer(db, row, evidence_metadata=evidence_metadata)

    name = str(row.get("product_name_cn") or row.get("product_name_en") or row.get("sku") or "该产品").strip()
    sku = str(row.get("sku") or "").strip().upper()
    prefix = f"{name}（{sku}）" if name else sku
    heat_source = _heat_source_field_evidence(row.get("heat_source"))
    normalized_heat_source = "" if heat_source in {"", "/", "[]", "暂无", "未标注"} else heat_source
    asks_direct_heating = any(term in identity_free_text for term in (
        "直接加热", "明火加热", "上火加热", "直接放", "直接置于",
    ))
    asks_heat_source = any(term in identity_free_text for term in ("热源", "燃料", "适用什么热源", "适用热源"))
    capability_aliases = {
        "卡式炉": ("卡式炉",),
        "燃气炉": ("燃气炉", "气炉", "卡式炉"),
        "气炉": ("气炉", "燃气炉", "卡式炉"),
        "明火直烧": ("明火直烧", "明火"),
        "明火": ("明火", "明火直烧"),
        "分体炉": ("分体炉",),
        "一体炉": ("一体炉",),
        "电磁炉": ("电磁炉",),
        "电陶炉": ("电陶炉",),
        "电热炉": ("电热炉",),
        "电炉": ("电炉", "电磁炉", "电陶炉", "电热炉"),
    }
    asked_capability = next((label for label in capability_aliases if label in identity_free_text), "")

    if normalized_heat_source:
        if evidence_metadata is not None:
            evidence_metadata.update({
                "evidence_value": normalized_heat_source,
                "evidence_source": "specs.heat_source",
                "evidence_scope": "subject",
                "evidence_sku": sku,
            })
        if asks_direct_heating:
            return f"{prefix}：可以按直接加热理解，当前资料显示适用热源为{normalized_heat_source}。", "supported"
        if asked_capability:
            aliases = capability_aliases.get(asked_capability, ())
            if any(alias and alias in normalized_heat_source for alias in aliases):
                return f"{prefix}：支持{asked_capability}，当前资料显示适用热源为{normalized_heat_source}。", "supported"
            return f"{prefix}：当前资料未显示支持{asked_capability}，当前资料显示适用热源为{normalized_heat_source}。", "not_listed"
        if "燃料" in text:
            return f"{prefix}：当前资料显示适用燃料/气源为{normalized_heat_source}。", "structured"
        if asks_heat_source:
            return f"{prefix}：当前资料显示适用热源为{normalized_heat_source}。", "structured"
        return f"{prefix}：当前资料显示适用热源为{normalized_heat_source}。", "structured"

    instruction_boundary = _same_sku_usage_instruction_heat_source_boundary(
        row.get("usage_instruction"),
        asked_capability=asked_capability,
        asks_direct_heating=asks_direct_heating,
    )
    if instruction_boundary:
        if evidence_metadata is not None:
            evidence_metadata.update({
                "evidence_value": instruction_boundary,
                "evidence_source": "specs.usage_instruction",
                "evidence_scope": "subject",
                "evidence_sku": sku,
            })
        return f"{prefix}：{instruction_boundary}。", "unsupported"

    if asks_direct_heating:
        return f"{prefix}：当前资料未标注适用热源；是否可以直接加热，不能仅凭现有资料确认。", "missing"
    if asked_capability:
        return f"{prefix}：当前资料未标注适用热源；是否支持{asked_capability}，不能仅凭现有资料确认。", "missing"
    return f"{prefix}：当前资料未标注适用热源，不能仅凭现有资料确认。", "missing"


def _result_has_positive_capability_evidence(result: dict[str, Any], question: str) -> bool:
    text = str(question or "").strip()
    if not isinstance(result, dict) or not text:
        return False
    rows = [row for row in (result.get("results") or []) if isinstance(row, dict)]
    if not rows:
        return False
    asks_alcohol = any(term in text for term in ("酒精炉", "酒精"))
    asks_heat_source = any(term in text for term in ("热源", "燃料", "明火", "卡式炉", "燃气炉"))
    asks_waterware_capability = any(term in text for term in ("直接加热", "烧水", "装热水", "喝热水", "冷水", "热水", "补水"))
    for row in rows:
        field_values = row.get("field_values") if isinstance(row.get("field_values"), dict) else {}
        evidence_values = [
            str(row.get("heat_source") or "").strip(),
            str(field_values.get("热源") or "").strip(),
            str(field_values.get("适用热源") or "").strip(),
            str(field_values.get("燃料") or "").strip(),
            str(field_values.get("是否支持酒精炉") or "").strip(),
        ]
        combined = " ".join(value for value in evidence_values if value)
        if asks_alcohol and customer_agent_intent_service._alcohol_stove_support_verdict(combined) is True:
            return True
        if asks_heat_source and combined:
            return True
        if asks_waterware_capability and any(term in combined for term in ("热水", "烧水", "补水", "冷水", "保温")):
            return True
    return False


def _phase1_pan_category_terms(category_ref: str) -> tuple[str, ...]:
    ref = str(category_ref or "").strip().lower()
    if ref == "煎盘":
        return ("煎盘", "煎烤盘", "griddle", "grill pan", "fry pan", "pan plate")
    if ref == "烤盘":
        return ("烤盘", "煎烤盘", "griddle", "grill pan", "pan plate")
    if ref == "煎烤盘":
        return ("煎盘", "烤盘", "煎烤盘", "griddle", "grill pan")
    return ("煎盘", "烤盘", "煎烤盘", "griddle", "grill pan", "fry pan", "pan plate")


def _phase1_is_pan_category_row(row: dict, category_ref: str) -> bool:
    category = str(row.get("category") or "").strip().lower()
    name = " ".join(str(row.get(key) or "") for key in ("product_name_cn", "product_name_en")).lower()
    if any(term in category for term in ("炉具", "茶具", "水壶")):
        return False
    if "不含" in name and any(term in name for term in ("煎盘", "烤盘", "griddle", "grill pan")):
        return False
    haystack = " ".join(
        str(row.get(key) or "")
        for key in ("product_name_cn", "product_name_en", "category")
    ).lower()
    return any(term in haystack for term in _phase1_pan_category_terms(category_ref))


def _phase1_category_compatibility_result(db: Session, plan: dict) -> dict:
    category_ref = str(plan.get("category_ref") or "").strip() or "煎盘/烤盘"
    rows = [row for row in _phase1_catalog_rows(db, "产品") if _phase1_is_pan_category_row(row, category_ref)]
    skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
    if rows:
        lines = [f"你问的是{category_ref}这个品类。当前资料中的对应产品及酒精炉兼容性如下："]
        for row in rows:
            answer, _ = _phase1_alcohol_stove_compatibility_answer(db, row)
            lines.append(f"- {answer}")
        answer = "\n".join(lines)
    else:
        answer = f"你问的是{category_ref}这个品类。当前资料里没有找到对应产品，无法确认是否支持酒精炉；请提供具体产品名或 SKU。"
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "answer": answer,
        "results": rows,
        "result_skus": skus,
        "candidate_skus": skus,
        "answer_metadata": {
            "answer_policy": "category_compatibility",
            "requested_field": "heat_source",
            "category_ref": category_ref,
        },
        "debug": {
            "agent_mode": "planner_category_compatibility",
            "raw_results": rows,
            "candidate_skus": skus,
        },
        "skip_polish": True,
    }


def _phase1_catalog_rows(db: Session, product_ref: str) -> list[dict]:
    ref = str(product_ref or "").strip()
    category_scope_config = {
        "配件": {
            "categories": {"配件"},
            "sub_categories": ("收纳包具", "餐厨配件", "附件"),
            "fallback_terms": ("配件", "附件"),
        },
        "炉具": {
            "categories": {"炉具"},
            "sub_categories": ("炉具", "炉子", "酒精炉", "气炉", "卡式炉"),
            "fallback_terms": ("炉具", "炉子", "酒精炉", "卡式炉", "气炉"),
        },
        "餐具": {
            "categories": {"餐具"},
            "sub_categories": ("餐具",),
            "fallback_terms": ("餐具",),
        },
        "水具": {
            "categories": {"水具", "水壶"},
            "sub_categories": ("水具", "水壶", "水杯", "杯具"),
            "fallback_terms": ("水具", "水壶", "水杯", "杯具"),
        },
        "桌椅": {
            "categories": {"桌椅"},
            "sub_categories": ("桌椅",),
            "fallback_terms": ("桌椅",),
        },
        "咖啡器具": {
            "categories": {"咖啡器具"},
            "sub_categories": ("咖啡器具",),
            "fallback_terms": ("咖啡器具",),
        },
        "茶具": {
            "categories": {"茶具"},
            "sub_categories": ("茶具",),
            "fallback_terms": ("茶具",),
        },
        "天幕/地垫/帐篷": {
            "categories": {"天幕", "地垫", "帐篷", "天幕/地垫/帐篷", "天幕、地垫、帐篷"},
            "sub_categories": ("天幕", "地垫", "帐篷"),
            "fallback_terms": ("天幕", "地垫", "帐篷"),
        },
    }

    def _matches_category_scope(row: dict, scope: dict[str, Any]) -> bool:
        category = str(row.get("category") or "").strip()
        sub_category = str(row.get("sub_category") or "").strip()
        if category in (scope.get("categories") or set()):
            return True
        if sub_category and any(term in sub_category for term in (scope.get("sub_categories") or ())):
            return True
        if category or sub_category:
            return False
        name_text = " ".join(str(row.get(key) or "") for key in ("product_name_cn", "product_name_en"))
        return any(term and term in name_text for term in (scope.get("fallback_terms") or ()))

    query = (
        db.query(Product, ProductSpecs, ProductBusiness, ProductContent)
        .outerjoin(ProductSpecs, ProductSpecs.product_id == Product.id)
        .outerjoin(ProductBusiness, ProductBusiness.product_id == Product.id)
        .outerjoin(ProductContent, ProductContent.product_id == Product.id)
    )
    rows: list[dict] = []
    for product, specs, business, content in query.all():
        row = _product_row_from_model(product, specs, business, content)
        haystack = " ".join(str(row.get(key) or "") for key in (
            "product_name_cn",
            "product_name_en",
            "category",
            "capacity",
            "features",
            "usage_scenarios",
            "positioning",
            "long_description_cn",
        ))
        if ref == "产品":
            matched = True
        elif ref == "水壶":
            category_text = " ".join(str(row.get(key) or "") for key in ("category", "sub_category", "product_name_cn", "product_name_en"))
            matched = "水壶" in category_text
        elif ref == "气罐":
            # A canister is a consumable/accessory subject, not a stove
            # category.  Match its own product identity fields and exclude
            # stove rows whose heat-source field merely mentions a canister.
            category = str(row.get("category") or "").strip()
            canister_name = " ".join(
                str(row.get(key) or "")
                for key in ("product_name_cn", "product_name_en")
            )
            adapter_markers = ("转接头", "转换头", "适配头", "配件")
            matched = (
                category in {"燃料", "气罐", "燃气"}
                or (
                    category not in {"炉具", "锅具", "酒精炉", "配件"}
                    and any(term in canister_name for term in ("气罐", "气瓶", "高山气罐", "卡式气罐", "液化气罐"))
                )
            ) and not any(term in canister_name for term in adapter_markers)
        elif ref == "锅具":
            # The catalogue label "锅具" includes product-family records that
            # are actually griddles or accessory components.  Count only
            # standalone pots and cookware sets, using the same canonical
            # candidate boundary as recommendation and structured filtering.
            matched = (
                customer_structured_query_contract.resolve_material_subject_scope(row, "\u9505\u5177")["eligible"]
                and _is_service_pot_or_cookware_set_candidate(row)
            )
        elif ref == "烤盘":
            category = str(row.get("category") or "").strip()
            griddle_text = " ".join(
                str(row.get(key) or "")
                for key in ("product_name_cn", "product_name_en", "sub_category", "features")
            )
            matched = category != "炉具" and any(term in griddle_text for term in ("烤盘", "煎盘", "煎烤盘"))
        elif ref in category_scope_config:
            matched = _matches_category_scope(row, category_scope_config[ref])
        elif ref == "套锅":
            matched = "套锅" in haystack or "锅" in str(row.get("category") or "") and "套" in haystack
        else:
            matched = bool(ref and ref in haystack)
        if matched:
            rows.append(row)
    return rows


def _database_category_scope_ref(rows: list[dict], semantic_subject: str) -> str:
    """Resolve a semantic category subject to an existing DB category label.

    This is a bounded catalogue-identity adapter, not a field-intent router:
    the semantic preplan has already chosen ``structured_query/category``.
    We only accept a subject that maps to a real category value (including a
    segment of a stored composite category) so product-name recall cannot
    broaden a category count.
    """
    subject = customer_agent_service.normalize_search_text(semantic_subject)
    if not subject:
        return ""
    normalized_categories = {
        customer_agent_service.normalize_search_text(str(row.get("category") or "")): str(row.get("category") or "").strip()
        for row in rows
        if str(row.get("category") or "").strip()
    }
    if subject in normalized_categories:
        return normalized_categories[subject]
    # A one-character head noun is a common customer shorthand for a stored
    # category label (锅 -> 锅具, 炉 -> 炉具).  Match only an actual category
    # label, never a product name or selling-point field.
    if len(subject) == 1:
        matches = [
            label for normalized, label in normalized_categories.items()
            if normalized.startswith(subject) and normalized.endswith("具")
        ]
        if len(matches) == 1:
            return matches[0]
    composite_matches = [
        label for normalized, label in normalized_categories.items()
        if subject and subject in normalized and any(separator in normalized for separator in ("/", "、", "，", ","))
    ]
    return composite_matches[0] if len(composite_matches) == 1 else ""


def _database_product_family_scope_rows(rows: list[dict], semantic_subject: str) -> list[dict]:
    """Resolve a semantic family subject only against product identity fields."""
    subject = customer_agent_service.normalize_search_text(semantic_subject)
    if not subject:
        return []
    family_rows = []
    for row in rows:
        identity_values = (
            row.get("sku"),
            row.get("product_name_cn"),
            row.get("product_name_en"),
        )
        if any(
            subject in customer_agent_service.normalize_search_text(str(value or ""))
            for value in identity_values
        ):
            family_rows.append(row)
    return family_rows


def _database_question_product_family_scope_rows(rows: list[dict], question: str) -> list[dict]:
    """Find a repeated product-name span shared with the full question.

    This is used only when semantic planning is unavailable.  It examines
    product identity fields, never descriptions, scenarios, selling points or
    requested attributes, and therefore cannot turn a broad recommendation
    match into a product-family answer.
    """
    normalized_question = customer_agent_service.normalize_search_text(question).lower()
    segments = re.findall(r"[a-z0-9]+|[\u4e00-\u9fff]+", normalized_question)
    best_rows: list[dict] = []
    best_span_length = 0
    for segment in segments:
        upper_length = min(len(segment), 24)
        for span_length in range(upper_length, 2, -1):
            if span_length < best_span_length:
                break
            for index in range(0, len(segment) - span_length + 1):
                span = segment[index:index + span_length]
                matching_rows = [
                    row
                    for row in rows
                    if any(
                        span in customer_agent_service.normalize_search_text(str(row.get(field) or "")).lower()
                        for field in ("sku", "product_name_cn", "product_name_en")
                    )
                ]
                if len(matching_rows) > 1 and (
                    span_length > best_span_length
                    or (span_length == best_span_length and len(matching_rows) > len(best_rows))
                ):
                    best_span_length = span_length
                    best_rows = matching_rows
    return best_rows


def _phase1_catalog_count_rows(db: Session, product_ref: str, question: str) -> tuple[list[dict], str | None]:
    """Return the authoritative category-membership population for count questions.

    Product discovery can deliberately use a narrower functional domain (for
    example, excluding a standalone griddle from cookware recommendations).
    A customer asking how many products are in a catalogue category needs the
    database category membership instead, including a product assigned to a
    composite category.  Keep that distinction at the aggregate boundary.
    """
    ref = str(product_ref or "").strip()
    text = str(question or "")
    is_count = any(token in text for token in ("多少", "几款", "几种", "几个"))
    if ref != "锅具" or not is_count:
        return _phase1_catalog_rows(db, ref), None

    rows = _phase1_catalog_rows(db, "产品")
    category_rows = [row for row in rows if "锅具" in str(row.get("category") or "")]
    pure_count = sum(str(row.get("category") or "").strip() == "锅具" for row in category_rows)
    composite_count = len(category_rows) - pure_count
    scope_note = f"其中类目为“锅具”{pure_count} 款"
    if composite_count:
        scope_note += f"，复合类目中包含“锅具”{composite_count} 款"
    return category_rows, scope_note


def _phase1_catalog_count_result(db: Session, plan: dict) -> dict:
    product_ref = str(plan.get("product_ref") or "").strip() or "产品"
    rows, scope_note = _phase1_catalog_count_rows(db, product_ref, str(plan.get("raw_question") or ""))
    display_ref = _phase1_catalog_display_label(product_ref)
    skus = [str(row.get("sku") or "") for row in rows if row.get("sku")]
    display_rows = _phase1_rank_catalog_display_rows(product_ref, rows)[:10]
    display_skus = [str(row.get("sku") or "") for row in display_rows if row.get("sku")]
    samples = "、".join(
        f"{row.get('sku')} {row.get('product_name_cn')}".strip()
        for row in display_rows
        if row.get("sku")
    )
    scope_label = f"【{display_ref}】" if product_ref != "产品" else "【产品】"
    if not rows:
        answer = f"当前产品库里暂未维护明确的{scope_label}类产品。"
        if product_ref != "产品":
            answer += " 如果你愿意，我可以改按相近类目或具体用途继续帮你找。"
    else:
        if samples and len(rows) > len(display_rows):
            suffix = f"先列前 {len(display_rows)} 款：{samples}。"
        elif samples:
            suffix = f"分别是：{samples}。"
        else:
            suffix = ""
        if product_ref == "配件" and samples:
            suffix = f"先按更像明确配件的小件/附件口径列前 {len(display_rows)} 款：{samples}。"
        elif product_ref == "水具" and samples:
            suffix = f"先按通用饮水/补水口径列前 {len(display_rows)} 款：{samples}。"
        answer = f"当前匹配到{scope_label}类产品共有 {len(rows)} 款。{suffix}"
        if scope_note:
            answer += f" 统计口径：{scope_note}。"
        if product_ref != "产品":
            answer += " 如果你想继续缩小范围，我可以再按用途、人数、容量、重量或收纳继续筛。"
    return {
        "intent": "query_products",
        "answer_type": "product_query" if product_ref == "天幕/地垫/帐篷" else "query_products",
        "answer": answer,
        "results": display_rows,
        "result_skus": display_skus,
        "candidate_skus": display_skus,
        "answer_metadata": {
            "source": "product_catalog_structured_query",
            "catalog_count": len(rows),
            "display_count": len(display_rows),
            "product_ref": product_ref,
        },
        "debug": {"agent_mode": "structured_catalog_count"},
        "skip_polish": True,
    }


def _phase1_catalog_display_label(product_ref: str) -> str:
    ref = str(product_ref or "").strip()
    if ref == "all_products":
        return "全部产品"
    if ref == "天幕/地垫/帐篷":
        return "天幕、地垫、帐篷"
    return ref


def _phase1_rank_catalog_display_rows(product_ref: str, rows: list[dict]) -> list[dict]:
    ref = str(product_ref or "").strip()
    if ref not in {"配件", "水具"}:
        return list(rows or [])

    def _score(row: dict) -> tuple[int, str]:
        text = " ".join(
            str(row.get(key) or "").strip()
            for key in ("product_name_cn", "product_name_en", "category", "sub_category", "features", "usage_scenarios", "positioning")
        )
        score = 0
        if ref == "配件":
            if any(term in text for term in ("配件", "附件", "替换件", "维修件", "炉芯", "防风板", "提手", "手夹", "刀板", "收纳件", "小配件")):
                score += 10
            if any(term in text for term in ("锅具配件", "餐厨配件", "炉具配件", "替换配件", "备用配件")):
                score += 5
            if any(term in text for term in ("收纳包", "折叠箱", "保温包", "围裙", "堆堆车", "炊雪盘", "水袋", "雪拉碗", "喷枪")):
                score -= 8
        elif ref == "水具":
            if any(term in text for term in ("水具", "水壶", "水杯", "杯", "饮水", "补水", "便携水具", "保温壶", "冷水壶")):
                score += 8
            if any(term in text for term in ("便携", "随身", "公园野餐", "徒步补水", "户外补水", "饮水")):
                score += 4
            if any(term in text for term in ("咖啡", "手冲", "细口壶", "煮茶")):
                score -= 6
            if any(term in text for term in ("烧水", "加热", "套装礼盒", "礼盒")):
                score -= 3
        sku = str(row.get("sku") or "")
        return (-score, sku)

    return sorted(list(rows or []), key=_score)


def _looks_like_standalone_burner_head_accessory_request(question: str) -> bool:
    """Keep a bare burner-head target in the accessory ontology.

    ``炉头`` can be used colloquially for a complete small stove, so this
    boundary only applies when it is the standalone selection target.  A
    complete-stove noun (炉具/炉子/卡式炉/酒精炉等) or the compound ``小炉头``
    keeps the broader stove route.  The decision is based on the customer's
    semantic noun and selection predicate, never on a SKU or exact utterance.
    """
    text = str(question or "").strip()
    if "炉头" not in text or "小炉头" in text:
        return False
    if any(term in text for term in ("炉具", "炉子", "酒精炉", "燃气炉", "卡式炉", "气炉")):
        return False
    selection_terms = ("推荐", "哪款", "哪个", "哪种", "用什么样", "有没有", "有卖", "卖吗", "找")
    return any(term in text for term in selection_terms)


def _semantic_catalog_product_ref(question: str) -> str:
    text = str(question or "").strip()
    # Explicit accessory/storage nouns define the catalogue subject even when
    # the same turn also mentions cookware or tableware.  This keeps a bag or
    # attachment request from falling through to the broad product/category
    # fallback; it does not identify or promote any SKU.
    if any(term in text for term in ("收纳包", "收纳袋", "装备包", "野炊包", "餐具包", "厨具包", "配件", "附件")):
        return "配件"
    if (
        any(term in text for term in ("高山气罐", "卡式气罐", "液化气罐", "通用气罐", "气罐", "气瓶"))
        and any(term in text for term in ("品牌", "型号", "推荐", "哪款", "哪个", "买", "选"))
        # A canister noun can be a supporting condition for a burner request
        # (for example “装气罐的小炉头”), not the product category being
        # selected.  Let the explicit stove subject win before the generic
        # fuel catalogue boundary; this is an intent-precedence rule, not a
        # SKU or utterance-specific exception.
        and not any(term in text for term in ("炉头", "炉具", "炉子", "酒精炉", "燃气炉", "卡式炉", "分体炉", "一体炉", "气炉"))
        and not any(term in text for term in ("能不能", "能否", "能用", "适配", "兼容", "接口", "用什么燃料"))
    ):
        return "气罐"
    if any(term in text for term in ("咖啡器具", "手冲", "手冲壶")):
        return "咖啡器具"
    if "茶具" in text:
        return "茶具"
    if any(term in text for term in ("烤盘", "煎盘")):
        return "烤盘"
    if "水壶" in text:
        return "水壶"
    if "水具" in text:
        return "水具"
    # A heat-source noun can be a constraint on cookware rather than the
    # catalogue subject being selected (for example, "支持酒精炉的锅具").
    # Resolve the requested object first when the sentence expresses a
    # compatibility relation; an unqualified "推荐酒精炉" still falls
    # through to the stove category below.
    if (
        any(term in text for term in ("锅具", "套锅", "单锅", "锅"))
        and any(term in text for term in ("酒精炉", "燃气炉", "卡式炉", "分体炉", "一体炉", "气炉"))
        and any(term in text for term in ("支持", "适合", "兼容", "能用", "能配", "搭配", "配合", "能不能", "能否", "是否"))
    ):
        return "锅具"
    if _looks_like_standalone_burner_head_accessory_request(text):
        return "配件"
    if any(term in text for term in ("炉具", "炉子", "炉头", "酒精炉", "燃气炉", "卡式炉", "分体炉", "一体炉", "气炉", "stove", "burner")):
        return "炉具"
    if any(term in text for term in ("锅具", "套锅", "单锅", "锅", "套装", "组合", "套餐")):
        return "锅具"
    if "配件" in text:
        return "配件"
    if "餐具" in text:
        return "餐具"
    if "桌椅" in text:
        return "桌椅"
    if any(term in text for term in ("天幕", "地垫", "帐篷")):
        return "天幕/地垫/帐篷"
    return "产品"


_STRUCTURED_TARGET_CATEGORY_ALIASES = (
    ("咖啡器具", "咖啡器具"),
    ("烤盘", "烤盘"),
    ("煎盘", "烤盘"),
    ("挡风板", "配件"),
    ("防风板", "配件"),
    ("炉头", "配件"),
    ("炉芯", "配件"),
    ("炉心", "配件"),
    ("炉子内胆", "配件"),
    ("炉具内胆", "配件"),
    ("内胆", "配件"),
    ("气罐", "配件"),
    ("转接头", "配件"),
    ("收纳包", "配件"),
    ("收纳袋", "配件"),
    ("大锅", "锅具"),
    ("小锅", "锅具"),
    ("汤锅", "锅具"),
    ("奶锅", "锅具"),
    ("炒锅", "锅具"),
    ("煎锅", "锅具"),
    ("水壶", "水壶"),
    ("茶壶", "水壶"),
    ("水具", "水具"),
    ("杯子", "水具"),
    ("锅具", "锅具"),
    ("锅", "锅具"),
    ("炉具", "炉具"),
    ("炉子", "炉具"),
    ("酒精炉", "炉具"),
    ("卡式炉", "炉具"),
    ("气炉", "炉具"),
    ("配件", "配件"),
    ("餐具", "餐具"),
    ("桌椅", "桌椅"),
)


def _structured_target_category_from_question(question: str) -> str:
    text = str(question or "").strip()
    if not text:
        return ""
    aliases = "|".join(re.escape(alias) for alias, _ in _STRUCTURED_TARGET_CATEGORY_ALIASES)
    for pattern in (
        rf"(?:哪些|哪一些|有哪些)\s*({aliases})",
        rf"(?:有没有|有无|推荐(?:一下|一个|一款|几个|几款)?|找(?:一个|一款)?|想买|买(?:一个|一款)?|卖不卖|有卖)\s*(?:一个|一款|几个|几款)?\s*({aliases})",
        rf"(?:推荐|找|想买|买)[^？?。！!，,；;]{{0,40}}?({aliases})",
        rf"({aliases})\s*(?:推荐|卖吗|有卖|有没有|要一个)",
        rf"(?:这个|这款|那款|该|我说的)\s*({aliases})",
        rf"的\s*({aliases})\s*[？?。！!，,；;]*$",
    ):
        match = re.search(pattern, text)
        if match is not None:
            alias = match.group(1)
            return next(category for candidate, category in _STRUCTURED_TARGET_CATEGORY_ALIASES if candidate == alias)
    return ""


def _product_category_domain(category: str | None) -> str:
    value = str(category or "").strip()
    if any(term in value for term in ("锅具", "烤盘", "煎盘")):
        return "cookware"
    if any(term in value for term in ("水壶", "水具", "水杯")):
        return "waterware"
    if "炉具" in value or "酒精炉" in value:
        return "stove"
    if "配件" in value:
        return "accessory"
    if "餐具" in value:
        return "tableware"
    if "咖啡器具" in value:
        return "coffee"
    if "茶具" in value:
        return "tea"
    if "桌椅" in value:
        return "furniture"
    return value


def _looks_like_semantic_catalog_query(question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    generic_category_listing = bool(re.match(
        r"^\s*(?:有)?\s*(?:哪些|多少款|多少个|多少种|多少|几款|几种)\s*"
        r"(?:锅具|套锅|单锅|锅|炉具|炉子|炉|水具|水壶|配件|附件|餐具|桌椅|咖啡器具|咖啡|茶具|烤盘|煎盘|天幕|地垫|帐篷)"
        r"(?:产品|商品)?\s*[？?！!]?$",
        text,
    ))
    # A formal field predicate (shipping, price, material, etc.) outweighs a
    # coincidental count token such as "多少" inside a product question.
    if customer_field_contract.detect_field_contract(text) is not None and not generic_category_listing:
        return False
    count_or_list_terms = ("多少款", "多少个", "多少种", "多少", "几款", "几种", "有哪些", "列出")
    scope_terms = ("锅具", "套锅", "单锅", "锅", "炉具", "炉子", "炉", "水具", "水壶", "配件", "餐具", "桌椅", "咖啡器具", "咖啡", "茶具", "烤盘", "煎盘", "天幕", "地垫", "帐篷")
    if any(term in text for term in ("适合", "更偏", "能配", "搭配", "随身补水", "冷水", "热水", "手冲", "泡茶")):
        return False
    return any(term in text for term in count_or_list_terms) and any(term in text for term in scope_terms)


def _has_resolved_entity_contents_accessories_question(db: Session, question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    if not customer_agent_intent_service._looks_like_contents_grounding_question(text):
        return False
    if _resolve_sku(db, text, None):
        return True
    return bool(_products_named_in_question(db, text))


def _structured_query_row_text(row: dict[str, Any]) -> str:
    return " ".join(
        str(row.get(key) or "").strip()
        for key in (
            "sku",
            "product_name_cn",
            "product_name_en",
            "category",
            "sub_category",
            "capacity",
            "body_material",
            "material",
            "heat_source",
            "applicable_heat_source",
            "target_audience",
            "gross_weight_g",
            "specification",
            "size",
            "features",
            "usage_scenarios",
            "positioning",
            "long_description_cn",
        )
    )


def _structured_row_matches_category_value(row: dict[str, Any], value: str) -> bool:
    text = _structured_query_row_text(row)
    normalized = str(value or "").strip()
    if not normalized:
        return True
    if normalized == "水杯":
        has_cup_signal = any(token in text for token in ("水杯", "杯", "杯子", "随行杯", "保温杯", "钛杯", "马克杯"))
        has_kettle_signal = any(token in text for token in ("水壶", "茶壶", "烧水壶", "细口壶", "冷水壶", "保温壶"))
        return has_cup_signal and not has_kettle_signal
    if normalized == "水具":
        return any(token in text for token in ("水具", "水壶", "杯", "杯子", "水杯", "壶"))
    if normalized == "水壶":
        has_kettle_signal = any(token in text for token in ("水壶", "茶壶", "烧水壶", "细口壶", "冷水壶", "保温壶"))
        has_cup_signal = any(token in text for token in ("水杯", "杯", "杯子", "随行杯", "保温杯", "钛杯", "马克杯"))
        return has_kettle_signal and not has_cup_signal
    if normalized == "锅具":
        return "锅" in text
    if normalized == "炉具":
        return "炉" in text
    if normalized == "配件":
        return "配件" in text
    if normalized == "咖啡器具":
        return "咖啡" in text
    return normalized in text


def _structured_row_matches_material_value(row: dict[str, Any], value: str) -> bool:
    contract = customer_structured_query_contract.StructuredQueryContract(
        subject_category="all_products",
        field="material",
        operator="contains",
        value=str(value or "").strip(),
        status="resolved",
    )
    return customer_structured_query_contract.match_material_condition(contract=contract, row=row)["matched"]


def _structured_row_matches_heat_value(row: dict[str, Any], value: str) -> bool:
    text = " ".join(
        str(row.get(key) or "").strip()
        for key in ("heat_source", "applicable_heat_source", "features", "long_description_cn")
    )
    normalized = str(value or "").strip()
    if not normalized:
        return True
    if normalized == "明火直烧":
        return "明火" in text
    if normalized == "明火":
        return any(token in text for token in ("明火", "直接加热", "烧水"))
    if normalized == "卡式炉":
        return "卡式炉" in text or "卡式气罐" in text
    if normalized == "酒精炉":
        return "酒精炉" in text or "酒精" in text
    return normalized in text


def _structured_row_matches_usage_value(row: dict[str, Any], value: str) -> bool:
    text = " ".join(
        str(row.get(key) or "").strip()
        for key in ("usage_scenarios", "features", "positioning", "long_description_cn", "target_audience")
    )
    normalized = str(value or "").strip()
    if not normalized:
        return True
    if normalized == "徒步":
        return any(token in text for token in ("徒步", "轻量", "便携"))
    if normalized == "咖啡":
        return any(token in text for token in ("咖啡", "手冲", "滤杯", "分享壶", "细口壶"))
    if "徒步" in normalized:
        return any(token in text for token in ("徒步", "轻量", "便携"))
    if "新手" in normalized:
        return any(token in text for token in ("新手", "入门", "基础", "性价比"))
    return normalized in text


def _structured_product_query_result(
    *,
    question: str,
    product_ref: str,
    rows: list[dict[str, Any]],
    source: str,
    reason_label: str,
    answer_type: str = "product_query",
    metadata_source: str | None = None,
    membership_field: str | None = None,
) -> dict | None:
    display_rows = rows[:10]
    if not display_rows:
        return None
    result_skus = [str(row.get("sku") or "").strip().upper() for row in display_rows if str(row.get("sku") or "").strip()]
    sample_text = "、".join(
        f"{row.get('sku')} {row.get('product_name_cn')}".strip()
        for row in display_rows
        if row.get("sku")
    )
    if source == "semantic_catalog_family_scope":
        rows_by_category: dict[str, list[dict[str, Any]]] = {}
        for row in display_rows:
            category = str(row.get("category") or "未分类").strip() or "未分类"
            rows_by_category.setdefault(category, []).append(row)
        category_parts = []
        for category, category_rows in rows_by_category.items():
            category_samples = "、".join(
                f"{row.get('sku')} {row.get('product_name_cn')}".strip()
                for row in category_rows
                if row.get("sku")
            )
            category_parts.append(f"{category} {len(category_rows)} 款：{category_samples}")
        answer = (
            f"【{product_ref}】相关商品共 {len(rows)} 款，按产品库类目区分为："
            f"{'；'.join(category_parts)}。"
            "如果你想选炉具本体或配件，我可以在对应类目里继续比较。"
        )
    else:
        answer = (
            f"当前按{reason_label}在【{product_ref}】里筛到 {len(rows)} 款候选。"
            f"{'先列前 %d 款：%s。' % (len(display_rows), sample_text) if sample_text else ''}"
            "如果你要继续缩小范围，我可以再按容量、重量、材质或人数继续筛。"
        )
    membership_evidence = []
    if membership_field:
        membership_label = customer_field_contract.product_detail_field_label(membership_field) or membership_field
        for row in display_rows:
            sku = str(row.get("sku") or "").strip().upper()
            value = row.get(membership_field)
            if sku and value is not None and str(value).strip():
                membership_evidence.append({
                    "type": "catalogue_membership",
                    "sku": sku,
                    "field": membership_field,
                    "field_label": membership_label,
                    "value": value,
                    "source_type": "product_db",
                    "source_label": "product_db",
                    "evidence_text": f"{membership_label} {value}",
                })
    return {
        "intent": "query_products",
        "answer_type": answer_type,
        "answer": answer,
        "results": display_rows,
        "result_skus": result_skus,
        "candidate_skus": result_skus,
        "evidence": membership_evidence,
        "answer_metadata": {
            "source": metadata_source or source,
            "product_ref": product_ref,
            "matched_count": len(rows),
            "membership_evidence_field": membership_field or "",
        },
        "debug": {"agent_mode": source, "raw_results": display_rows, "candidate_skus": result_skus},
        "skip_polish": True,
    }


def _detect_unknown_product_fact_label(question: str) -> str | None:
    text = str(question or "").strip()
    if not text:
        return None
    for label, terms in _STRUCTURED_UNKNOWN_FACT_TERMS.items():
        if any(term in text for term in terms):
            return label
    return None


def _detect_resolved_entity_unknown_fact_label(question: str) -> str | None:
    text = str(question or "").strip()
    if not text:
        return None
    field_contract = customer_field_contract.detect_field_contract(text)
    if field_contract and field_contract.field_type == "shipping":
        return "发货时效"
    if field_contract:
        contract_label = customer_field_contract.product_detail_field_label(field_contract.field_type)
        if contract_label in _RESOLVED_ENTITY_UNKNOWN_FACT_TERMS:
            return contract_label
    for label, terms in _RESOLVED_ENTITY_UNKNOWN_FACT_TERMS.items():
        if any(term in text for term in terms):
            return label
    return None


def _resolved_entity_unknown_field_guard_label(question: str) -> str | None:
    text = str(question or "").strip()
    if not text:
        return None
    label = _detect_resolved_entity_unknown_fact_label(text)
    if label in _RESOLVED_ENTITY_REALTIME_COMMERCIAL_LABELS:
        return label
    if customer_agent_intent_service._looks_like_usage_care_question(text):
        return None
    if customer_agent_intent_service._looks_like_contents_grounding_question(text):
        return None
    if customer_agent_intent_service._is_compare_question(text):
        return None
    if _looks_like_recommendation_request(text):
        return None
    return label


def _resolved_entity_realtime_commercial_field_guard_label(question: str) -> str | None:
    label = _resolved_entity_unknown_field_guard_label(question)
    if label in _RESOLVED_ENTITY_REALTIME_COMMERCIAL_LABELS:
        return label
    return None


def _semantic_preplan_unknown_realtime_subtype(preplan: dict | None, question: str) -> str:
    subtype = str((preplan or {}).get("subtype") or "").strip()
    if subtype in _SEMANTIC_UNKNOWN_REALTIME_SUBTYPES:
        return subtype
    route_hint = str((preplan or {}).get("route_hint") or "").strip()
    question_type = str((preplan or {}).get("question_type") or "").strip()
    label = _resolved_entity_realtime_commercial_field_guard_label(question) or _detect_unknown_product_fact_label(question)
    if label and (route_hint == "unknown_field" or question_type == "unknown_field" or (preplan or {}).get("unknown_field")):
        return "commercial_realtime"
    return ""


def _resolved_entity_unknown_fact_answer(prefix: str, label: str) -> str:
    special = {
        "库存": f"{prefix}\n当前资料未标注库存，也不能仅凭现有资料确认实时库存；请以平台或店铺页面为准，或联系人工客服确认。",
        "在售状态": f"{prefix}\n当前资料未维护可确认的实时在售状态或库存，不能仅凭静态产品资料判断；请以平台或店铺页面为准，或联系人工客服确认。",
        "销量": f"{prefix}\n当前资料未标注销量，也不能仅凭现有资料判断实时销量；请以平台或店铺页面为准。",
        "评价": f"{prefix}\n当前资料未标注评价或好评率，也不能仅凭现有资料确认实时评价信息；请以平台或店铺页面为准。",
        "价格": f"{prefix}\n当前资料未标注实时价格，也不能仅凭现有资料确认当前售价；请以平台或店铺页面为准。",
        "保修": f"{prefix}\n当前资料未标注保修信息，也无法仅凭现有资料确认具体质保政策；请以平台或店铺页面或人工客服说明为准。",
        "认证": f"{prefix}\n当前资料未标注对应认证或检测信息，不能仅凭现有资料确认。",
        "官方说明书": f"{prefix}\n当前资料未维护官方说明书信息，如需确认建议联系人工客服。",
        "售后电话": f"{prefix}\n当前资料未维护正式售后电话号码，无法提供可核验的联系电话；请通过购买平台的官方客服入口确认。",
        "安装视频": f"{prefix}\n当前资料未维护安装视频信息，如需确认建议联系人工客服。",
        "优惠活动": f"{prefix}\n当前资料未标注该商品的优惠、活动、券、会员价或到手价信息，也无法确认实时促销；请以平台或店铺页面为准。",
        # ``shipping`` is one safely-unsupported commercial field, but it
        # covers several customer meanings (carrier, dispatch/delivery timing,
        # postage, and delivery promises).  Do not fabricate an ETA premise
        # for a carrier question merely because both meanings share this
        # formal contract.  The semantic plan determines the field; this copy
        # is only the evidence-safe boundary when no matching same-SKU QA or
        # structured source exists.
        "发货时效": f"{prefix}\n当前资料未标注该商品的承运快递、发货或配送时效、运费及配送承诺等具体安排，无法确认；请以平台或店铺页面为准。",
        "赠品": f"{prefix}\n当前资料未标注该商品是否附赠赠品，也无法确认实时活动赠品；请以平台或店铺页面为准。",
        "包邮": f"{prefix}\n当前资料未标注该商品的包邮或运费规则，也无法确认实时包邮活动；请以平台或店铺页面为准。",
        "售后": f"{prefix}\n当前资料未标注该商品的售后政策或服务细则，也无法确认实时售后安排；请以平台或店铺页面或人工客服说明为准。",
    }
    # Non-realtime unsupported fields (for example a manufacturer model
    # number) must keep their own field boundary.  Do not borrow the generic
    # realtime-commercial disclaimer merely because this function also serves
    # price, inventory and delivery guards above.
    return special.get(label) or f"{prefix}\n当前资料未标注{label}信息，无法确认。"


def _build_resolved_product_unknown_field_result(
    db: Session,
    product: Product,
    *,
    label: str,
    source: str,
    resolution_decision: customer_entity_resolution_contract.SingleProductResolutionDecision,
    entity_contract: customer_entity_resolution_contract.EntityResolutionContract | None = None,
) -> dict:
    row = _product_row_from_model(product)
    name = str(product.product_name_cn or product.product_name_en or product.sku or "").strip()
    sku = str(product.sku or "").strip().upper()
    if not resolution_decision.allowed or resolution_decision.resolved_sku != sku:
        raise ValueError("resolved unknown-field answer requires an allowed exact entity decision")
    prefix = f"{name}（{sku}）" if name else sku
    manual_document = None
    if label == "官方说明书":
        manual_document = (
            db.query(KnowledgeDocument)
            .filter(
                KnowledgeDocument.sku == sku,
                KnowledgeDocument.source_type == "manual",
                KnowledgeDocument.is_active.is_(True),
            )
            .order_by(KnowledgeDocument.updated_at.desc())
            .first()
        )
    manual_value = ""
    if manual_document is not None:
        manual_value = str(manual_document.file_name or manual_document.title or "").strip()
    if manual_value:
        answer = f"{prefix}\n当前资料已维护官方说明书：{manual_value}。"
    else:
        answer = _resolved_entity_unknown_fact_answer(prefix, label)
    evidence_field = "manual" if manual_value else None
    evidence_source = "knowledge_documents.manual" if manual_value else None
    evidence_value = manual_value
    if entity_contract is None:
        entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
            sku,
            [product],
            resolver_candidates=[product],
            entity_text_override=sku,
            field_type_override=customer_field_contract.field_type_from_detail_label(label),
        )
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "answer": answer,
        "results": [row],
        "result_skus": [sku],
        "candidate_skus": [sku],
        "sku": sku,
        "answer_metadata": {
            "source": source,
            "requested_field": label,
            "contract_field_type": customer_field_contract.field_type_from_detail_label(label),
            "evidence_status": "structured" if evidence_value else "missing",
            "evidence_field": evidence_field,
            "evidence_source": evidence_source,
            "evidence_value": evidence_value,
            "evidence_sku": sku if evidence_value else None,
            "evidence_scope": "subject" if evidence_value else None,
            "field_evidence_match": bool(evidence_value),
            "field_evidence_missing": not bool(evidence_value),
            "entity_resolution_contract": entity_contract.to_dict(),
        },
        "debug": {
            "agent_mode": source,
            "entity_resolution_contract": entity_contract.to_dict(),
            "single_product_resolution_decision": resolution_decision.to_dict(),
            "plan": {"primary_intent": "product_field", "product_ref": sku, "requested_field": label},
            "raw_results": [row],
            "candidate_skus": [sku],
        },
        "skip_polish": True,
    }


def _clarification_compatible_products(question: str, products: list[Product]) -> list[Product]:
    explicit_colors = [term for term in customer_agent_service.PRODUCT_COLOR_TERMS if term in str(question or "")]
    if not explicit_colors:
        return list(products)
    return [
        product
        for product in products
        if any(color in str(product.product_name_cn or product.product_name_en or "") for color in explicit_colors)
    ]


def _build_ambiguous_named_product_unknown_field_result(
    products: list[Product],
    *,
    label: str,
    question: str = "",
) -> dict:
    display_products = _clarification_compatible_products(question, products)[:5]
    rows = [_product_row_from_model(product) for product in display_products]
    options = "；".join(
        f"{str(product.product_name_cn or product.product_name_en or product.sku).strip()}（{str(product.sku or '').strip().upper()}）"
        for product in display_products
    )
    answer = (
        f"匹配到多个相关商品：{options}。请先指定 SKU 或具体款式；"
        f"另外，当前资料也未标注该类实时{label}信息，不能直接确认。"
        if options
        else f"没有找到与当前显式条件一致的明确商品。请提供 SKU 或更完整型号，再确认{label}信息。"
    )
    resolver_candidate_skus = [str(product.sku or "").strip().upper() for product in products[:5] if str(product.sku or "").strip()]
    clarification_candidate_skus = [str(product.sku or "").strip().upper() for product in display_products if str(product.sku or "").strip()]
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": resolver_candidate_skus,
        "clarification_candidate_skus": clarification_candidate_skus,
        "sku": None,
        "needs_clarification": True,
        "suggested_followups": ["请直接提供 SKU 或完整产品名。"],
        "followups": ["请直接提供 SKU 或完整产品名。"],
        "answer_metadata": {"source": "named_product_unknown_field_clarification", "requested_field": label, "evidence_status": "ambiguous_product"},
        "debug": {
            "agent_mode": "named_product_unknown_field_clarification",
            "plan": {"primary_intent": "clarify", "requested_field": label},
            "raw_results": rows,
            "candidate_skus": resolver_candidate_skus,
            "clarification_candidate_skus": clarification_candidate_skus,
        },
        "skip_polish": True,
    }


def _content_field_evidence_subtype(question: str, canonical_field: str | None) -> str | None:
    """Choose a content column only after the FieldContract has fixed its type."""
    text = str(question or "").casefold()
    field = str(canonical_field or "").strip()
    if field == "content_title":
        if "amazon" in text:
            return "content_title_amazon"
        if "官网" in text or "网站" in text:
            return "content_title_website"
        return "content_title_en" if "英文" in text or "english" in text else "content_title_cn"
    if field == "content_description":
        listing = "刊登" in text or "listing" in text
        english = "英文" in text or "english" in text
        if listing:
            return "content_description_listing_en" if english else "content_description_listing_cn"
        return "content_description_en" if english else "content_description_cn"
    return None


def _structured_product_field_evidence(
    canonical_field: str | None,
    *,
    db: Session,
    product: Product,
    specs: ProductSpecs | None,
    business: ProductBusiness | None,
    content: ProductContent | None,
    requested_subtype: str | None = None,
) -> tuple[str, str | None]:
    """Read the first non-empty value allowed by the canonical evidence policy."""
    policy = customer_field_contract.field_evidence_policy(canonical_field)
    if policy is None:
        return "", None
    if canonical_field == "specification":
        value = _specification_field_evidence(product=product, specs=specs)
        return (value, "specification.summary") if value else ("", None)
    if canonical_field in {"content_title", "content_description"}:
        if content is None:
            return "", None
        subtype = str(requested_subtype or "").strip()
        field_map = {
            "content_title_cn": "title_cn",
            "content_title_en": "title_en",
            "content_title_website": "website_title",
            "content_title_amazon": "amazon_title",
            "content_description_cn": "long_description_cn",
            "content_description_en": "long_description_en",
            "content_description_listing_cn": "listing_cn",
            "content_description_listing_en": "listing_en",
        }
        attribute = field_map.get(subtype)
        if not attribute:
            attribute = "title_cn" if canonical_field == "content_title" else "long_description_cn"
        value = _compound_display_value(getattr(content, attribute, ""))
        return (value, f"content.{attribute}") if _is_valid_structured_field_value(canonical_field, value) else ("", None)
    layers = {
        "product": product,
        "specs": specs,
        "business": business,
        "content": content,
    }
    for field_path in policy.structured_fields:
        if field_path.startswith("associations."):
            association = field_path.partition(".")[2]
            if association == "channels":
                values = [
                    str(row.channel_name or "").strip()
                    for row in (
                        db.query(ListingChannel)
                        .join(ProductListingChannel, ProductListingChannel.channel_id == ListingChannel.id)
                        .filter(ProductListingChannel.product_id == product.id)
                        .order_by(ListingChannel.channel_name.asc())
                        .all()
                    )
                ]
                source = "product_listing_channels->listing_channels"
            elif association == "regions":
                values = [
                    str(row.region_name or "").strip()
                    for row in (
                        db.query(SalesRegion)
                        .join(ProductSalesRegion, ProductSalesRegion.region_id == SalesRegion.id)
                        .filter(ProductSalesRegion.product_id == product.id)
                        .order_by(SalesRegion.region_name.asc())
                        .all()
                    )
                ]
                source = "product_sales_regions->sales_regions"
            elif association == "certifications":
                values = [
                    _certification_association_evidence(row)
                    for row in (
                        db.query(Certification)
                        .join(ProductCertification, ProductCertification.certification_id == Certification.id)
                        .filter(ProductCertification.product_id == product.id)
                        .order_by(Certification.certification_name.asc())
                        .all()
                    )
                ]
                source = "product_certifications->certifications"
            else:
                values, source = [], ""
            values = [item for item in values if _is_valid_structured_field_value(canonical_field, item)]
            if values:
                return "、".join(values), source
            continue
        layer_name, separator, attribute = str(field_path or "").partition(".")
        source = layers.get(layer_name)
        if not separator or source is None or not attribute:
            continue
        raw_value = getattr(source, attribute, None)
        value = (
            _weight_field_evidence(
                raw_value,
                capacity=getattr(specs, "capacity", "") if specs is not None else "",
            )
            if canonical_field == "weight" and field_path in {"specs.gross_weight_g", "specs.net_weight_g"}
            else
            _capacity_field_evidence(raw_value)
            if canonical_field == "capacity" and field_path == "specs.capacity"
            else _people_count_field_evidence(raw_value)
            if canonical_field == "people" and field_path in {"business.target_audience", "business.top_selling_points"}
            else _color_field_evidence(raw_value)
            if canonical_field == "color" and field_path == "specs.color"
            else _heat_source_field_evidence(raw_value)
            if canonical_field == "heat_source" and field_path == "specs.heat_source"
            else _selling_point_heat_source_evidence(raw_value)
            if canonical_field == "heat_source" and field_path == "business.top_selling_points"
            else _dishwasher_specific_evidence(raw_value)
            if canonical_field == "dishwasher"
            else _compound_display_value(raw_value)
        )
        if not _is_valid_structured_field_value(canonical_field, value):
            continue
        if field_path == "specs.usage_instruction" and canonical_field in {
            "usage_instruction",
            "cleaning",
            "care",
        }:
            if not _usage_instruction_matches_product(product, raw_value):
                continue
            value = (
                _machine_wash_specific_evidence(value)
                if canonical_field == "cleaning" and requested_subtype == "machine_wash"
                else _liquid_temperature_capability_evidence(value)
                if canonical_field == "usage_instruction" and requested_subtype == "liquid_temperature_capability"
                else _usage_instruction_field_evidence(
                    value,
                    canonical_field,
                    requested_subtype=requested_subtype,
                )
            )
            if not value:
                continue
        return value, field_path
    return "", None


def _weight_field_evidence(value: Any, *, capacity: Any = None) -> str:
    """Render a valid gram-column value without guessing an inconsistent unit."""
    try:
        grams = float(value)
    except (TypeError, ValueError):
        return ""
    if grams <= 0:
        return ""
    capacity_text = _capacity_field_evidence(capacity)
    capacity_liters = [
        float(match.group(1))
        for match in re.finditer(r"(\d+(?:\.\d+)?)\s*(?:L|l|升)", capacity_text)
    ]
    # ``gross_weight_g`` is explicitly a grams column.  A sub-five-gram value
    # alongside a 10L-or-larger capacity is an incompatible source-unit shape;
    # do not guess that the number means kilograms.  Fail closed until the
    # source data is corrected.
    if grams < 5 and any(liters >= 10 for liters in capacity_liters):
        return ""
    display = f"{grams:g}"
    return f"{display}g"


def _specification_field_evidence(
    *,
    product: Product,
    specs: ProductSpecs | None,
) -> str:
    """Build a field-scoped specification summary from validated SKU columns."""
    if specs is None:
        return ""
    parts: list[str] = []
    dimension = customer_field_contract.select_dimension_evidence(
        getattr(specs, "size_info", ""),
        requested_scope="subject",
    )
    if dimension and dimension.value:
        unit = f" {dimension.unit}" if dimension.unit else ""
        parts.append(f"尺寸：{dimension.value.replace('*', '×')}{unit}")
    def _without_own_label(value: Any, label: str) -> str:
        return re.sub(rf"^\s*{re.escape(label)}\s*[：:]\s*", "", str(value or "").strip())

    capacity = _without_own_label(_capacity_field_evidence(getattr(specs, "capacity", "")), "容量")
    if _is_valid_structured_field_value("capacity", capacity):
        parts.append(f"容量：{capacity}")
    weight = getattr(specs, "gross_weight_g", None)
    weight_display = _weight_field_evidence(weight, capacity=getattr(specs, "capacity", ""))
    if weight_display:
        parts.append(f"重量：{weight_display}")
    material = _without_own_label(_compound_display_value(getattr(specs, "body_material", "")), "材质")
    if _is_valid_structured_field_value("material", material):
        parts.append(f"材质：{material}")
    color = _without_own_label(_color_field_evidence(getattr(specs, "color", "")), "颜色")
    if _is_valid_structured_field_value("color", color):
        parts.append(f"颜色：{color}")
    power = _without_own_label(_compound_display_value(getattr(specs, "power", "")), "功率")
    if _is_valid_structured_field_value("power", power):
        parts.append(f"功率：{power}")
    return "；".join(parts)


def _certification_association_evidence(certification: Certification) -> str:
    """Preserve the dictionary explanation for a certification or standard label."""
    name = str(getattr(certification, "certification_name", "") or "").strip()
    description = str(getattr(certification, "description", "") or "").strip()
    if not description or description == name or description in name:
        return name
    return f"{name}（{description}）" if name else ""


def _is_valid_structured_field_value(canonical_field: str | None, value: Any) -> bool:
    """Reject placeholders before they become customer evidence."""
    text = _compound_display_value(value).strip()
    if not text or text.lower() in {"[]", "/", "null", "none", "n/a", "na", "unknown", "未分类", "未知", "暂无"}:
        return False
    if canonical_field == "barcode":
        return bool(re.fullmatch(r"\d{8,14}", text.replace(" ", "")))
    if canonical_field == "people":
        return bool(_people_count_field_evidence(text))
    return True


def _people_count_field_evidence(value: Any) -> str:
    """Return only an explicit serving headcount, never capacity or audience prose."""
    text = _compound_display_value(value).strip()
    match = re.search(
        r"(?:单人|双人|(?:(?:\d+\s*[-~至]\s*\d+)|(?:\d+)|[一二两三四五六七八九十]+)\s*(?:个)?人)",
        text,
    )
    return match.group(0) if match else ""


def _build_structured_query_contract_result(
    db: Session,
    contract: customer_structured_query_contract.StructuredQueryContract,
) -> dict:
    rows, evaluations = _evaluate_structured_query_contract_rows(db, contract)
    matched_skus = [item["sku"] for item in evaluations if item["matched"] and item["sku"]]
    by_sku = {str(row.get("sku") or "").strip().upper(): row for row in rows}
    filtered_rows = [by_sku[sku] for sku in matched_skus if sku in by_sku]
    display_rows = filtered_rows[:10]
    display_skus = [str(row.get("sku") or "").strip().upper() for row in display_rows if str(row.get("sku") or "").strip()]
    evidence = [item for item in evaluations if item["sku"] in set(display_skus)]
    evidence_guard = customer_structured_query_contract.validate_structured_evidence(
        contract=contract,
        filtered_rows=display_rows,
        evidence_rows=evidence,
    )
    evidence = evidence_guard["accepted_evidence"]
    subject_display = _phase1_catalog_display_label(str(contract.subject_category or ""))
    condition_summary = customer_structured_query_contract.format_structured_condition_summary(contract)
    sources = [
        {
            "type": "structured_query_contract",
            "label": condition_summary,
            "sku": str(item.get("sku") or "").strip().upper(),
            "evidence": item,
        }
        for item in evidence
        if str(item.get("sku") or "").strip()
    ]
    if display_rows:
        samples = "、".join(
            f"{row.get('sku')} {row.get('product_name_cn')}".strip()
            for row in display_rows
        )
        if len(filtered_rows) > len(display_rows):
            answer = f"共找到 {len(filtered_rows)} 款符合“{condition_summary}”条件的【{subject_display}】商品，以下先展示前 {len(display_rows)} 款：{samples}。"
        else:
            answer = f"当前在【{subject_display}】中筛到 {len(filtered_rows)} 款符合“{condition_summary}”条件的商品：{samples}。"
    else:
        answer = (
            "当前结构化商品库未找到符合条件的商品。"
            f"查询类目：【{subject_display}】；筛选条件：{condition_summary}。"
        )
    contract_data = contract.to_dict()
    runtime_plan = {
        "product_ref": contract.subject_category,
        "requested_field": customer_field_contract.product_detail_field_label(contract.field) or contract.field,
    }
    return {
        "intent": "query_products",
        "answer_type": "product_query",
        "answer": answer,
        "results": display_rows,
        "result_skus": display_skus,
        "candidate_skus": display_skus,
        "sources": sources,
        "answer_metadata": {
            "source": "structured_category_field_filter_query",
            "contract_source": "structured_query_contract",
            "product_ref": contract.subject_category,
            "matched_count": len(filtered_rows),
            "total_match_count": len(filtered_rows),
            "returned_count": len(display_rows),
            "is_truncated": len(filtered_rows) > len(display_rows),
            "condition_summary": condition_summary,
            "structured_query_contract": contract_data,
            "structured_match_evidence": evidence,
            "structured_rejected_evidence": evidence_guard["rejected_evidence"],
        },
        "debug": {
            "agent_mode": "structured_query_contract",
            "plan": runtime_plan,
            "structured_query_contract": contract_data,
            "structured_match_evidence": evidence,
            "raw_results": display_rows,
            "candidate_skus": display_skus,
        },
        "skip_polish": True,
    }


def _evaluate_structured_query_contract_rows(
    db: Session,
    contract: customer_structured_query_contract.StructuredQueryContract,
) -> tuple[list[dict[str, Any]], list[dict[str, Any]]]:
    """Evaluate structured filters across the canonical catalog candidate scope."""
    rows = _phase1_catalog_rows(db, "产品")
    evaluations = [customer_structured_query_contract.evaluate_structured_row(row, contract) for row in rows]
    return rows, evaluations


def _build_category_field_general_result(
    db: Session,
    contract: customer_structured_query_contract.StructuredQueryContract,
) -> dict:
    rows = _phase1_catalog_rows(db, "产品")
    aggregation = customer_structured_query_contract.aggregate_category_field_rows(rows, contract)
    field_label = {
        "heat_source": "适用炉具",
    }.get(str(contract.field or "")) or customer_field_contract.product_detail_field_label(contract.field) or "字段"
    subject_display = _phase1_catalog_display_label(str(contract.subject_category or ""))
    aggregated_values = list(aggregation["aggregated_values"])
    if aggregated_values and contract.field == "heat_source":
        groups = aggregation["value_groups"]
        appliances = "、".join(groups["appliance"])
        answer = (
            f"当前已标注的商品资料中，【{subject_display}】的适用炉具主要有：{appliances}。"
            if appliances
            else f"当前已标注的商品资料中，【{subject_display}】没有明确的适用炉具信息。"
        )
        additional_parts = []
        if groups["fuel_source"]:
            additional_parts.append(f"燃料或气源：{'、'.join(groups['fuel_source'])}")
        if groups["heating_method"]:
            additional_parts.append(f"其他加热方式：{'、'.join(groups['heating_method'])}")
        if groups["other"]:
            additional_parts.append(f"其他已标注信息：{'、'.join(groups['other'])}")
        if additional_parts:
            answer += f"另外，部分资料还标注了{'；'.join(additional_parts)}。"
    elif aggregated_values:
        displayed_values = aggregated_values[:12]
        values_text = "、".join(displayed_values)
        remainder = f"（共 {len(aggregated_values)} 种已标注值）" if len(aggregated_values) > len(displayed_values) else ""
        answer = f"当前已标注的商品资料中，【{subject_display}】的{field_label}主要有：{values_text}{remainder}。"
    else:
        answer = f"当前已标注的商品资料中没有找到明确的{field_label}信息。"
    contract_data = contract.to_dict()
    search_top1_promotion_blocked = bool(
        contract.status == "generic"
        and contract.subject_category
        and contract.field
        and contract.operator is None
        and contract.value is None
    )
    return {
        "intent": "query_products",
        "answer_type": "product_query",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "answer_metadata": {
            "source": "category_field_general",
            "subject_category": contract.subject_category,
            "field": contract.field,
            "structured_query_contract": contract_data,
            **aggregation,
        },
        "debug": {
            "agent_mode": "category_field_general",
            "structured_query_contract": contract_data,
            "candidate_skus": [],
            "aggregated_values": aggregated_values,
            "supporting_skus": aggregation["supporting_skus"],
            "binding_provenance": "none" if search_top1_promotion_blocked else None,
            "allow_search_top1_promotion": not search_top1_promotion_blocked,
            "search_top1_promotion_blocked": search_top1_promotion_blocked,
            "blocked_promotion_source": "category_field_general" if search_top1_promotion_blocked else None,
        },
        "skip_polish": True,
    }


def _build_generic_ambiguous_unknown_field_clarification_result(
    question: str,
    *,
    label: str,
    products: list[Product] | None = None,
    subject_override: str | None = None,
) -> dict:
    resolver_products = list(products or [])[:5]
    display_products = _clarification_compatible_products(question, resolver_products)
    rows = [_product_row_from_model(product) for product in display_products]
    subject = subject_override or customer_agent_intent_service._detail_subject_from_question(question) or "这款商品"
    if rows:
        options = "；".join(
            f"{str(row.get('product_name_cn') or row.get('product_name_en') or row.get('sku') or '').strip()}（{str(row.get('sku') or '').strip().upper()}）"
            for row in rows
            if str(row.get("sku") or "").strip()
        )
        answer = (
            f"“{subject}”这个叫法可能对应多个相关商品：{options}。"
            f"请先指定 SKU 或更完整的款式名；另外，当前资料也未标注该类实时{label}信息，不能直接确认。"
        )
    else:
        answer = (
            f"“{subject}”这个叫法可能对应多个相关商品。"
            f"请先提供 SKU 或更完整的商品名；另外，当前资料也未标注该类实时{label}信息，不能直接确认。"
        )
    candidate_skus = [str(product.sku or "").strip().upper() for product in resolver_products if str(product.sku or "").strip()]
    clarification_candidate_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": candidate_skus,
        "clarification_candidate_skus": clarification_candidate_skus,
        "sku": None,
        "needs_clarification": True,
        "suggested_followups": ["请直接提供 SKU 或完整产品名。"],
        "followups": ["请直接提供 SKU 或完整产品名。"],
        "answer_metadata": {"source": "named_product_unknown_field_clarification", "requested_field": label, "evidence_status": "ambiguous_product"},
        "debug": {
            "agent_mode": "named_product_unknown_field_clarification",
            "plan": {"primary_intent": "clarify", "requested_field": label},
            "raw_results": rows,
            "candidate_skus": candidate_skus,
            "clarification_candidate_skus": clarification_candidate_skus,
        },
        "skip_polish": True,
    }


def _enforce_field_evidence_policy(db: Session, question: str, agent_result: dict) -> dict:
    """Replace unsafe explicit-field answers with the deterministic same-SKU field result.

    This is deliberately narrow: only one explicit field, a single result SKU, and an
    exact canonical-name/SKU product mention can enter the guard. It cannot select a
    product for category, recommendation, comparison, or fuzzy entity requests.
    """
    if not isinstance(agent_result, dict):
        return agent_result
    navigation_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    if navigation_metadata.get("source") == "semantic_product_navigation_contract":
        # This turn intentionally established only a sealed identity anchor.
        # Tokens in the product name must not be retrofitted into an
        # unrequested field answer by the evidence-repair layer.
        return agent_result
    # Reuse the formal field decision already attached by the central route.
    # Re-parsing only the raw wording here can turn an incidental lexical word
    # (such as ``brand`` in a price-tier question) into a different field and
    # overwrite a correct Phase-2 answer.  Semantic classification remains
    # allowlisted and is still incapable of supplying identity or evidence.
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    debug_plan = debug.get("plan") if isinstance(debug.get("plan"), dict) else {}
    attached_field_request = (
        debug.get("field_contract")
        if isinstance(debug.get("field_contract"), dict)
        else debug_plan.get("field_contract")
        if isinstance(debug_plan.get("field_contract"), dict)
        else None
    )
    # A generic QA contract is already sealed by exact entity resolution and
    # same-SKU QA evidence. Do not reinterpret it through an unrelated
    # semantic formal field during evidence-repair formatting.
    if str((attached_field_request or {}).get("field_type") or "").strip() == "product_qa":
        # A sealed semantic QA request may legitimately have no matching
        # same-SKU QA.  Its safe-missing response is still owned by the QA
        # contract; reparsing lexical fragments here must not relabel it as
        # an unrelated formal field (for example return policy -> heat source).
        return agent_result
    sealed_multi_fields = [
        str(field or "").strip()
        for field in (attached_field_request or {}).get("canonical_fields") or []
        if str(field or "").strip()
    ]
    sealed_evidence_skus = {
        str(sku or "").strip().upper()
        for sku in navigation_metadata.get("evidence_skus") or []
        if str(sku or "").strip()
    }
    result_skus = {
        str(sku or "").strip().upper()
        for sku in agent_result.get("result_skus") or []
        if str(sku or "").strip()
    }
    if (
        len(sealed_multi_fields) > 1
        and str((attached_field_request or {}).get("source") or "") == "validated_semantic_preplan"
        and len(result_skus) == 1
    ):
        # A resolved compound contract remains valid even when every requested
        # field is missing.  Requiring an evidence SKU here used to collapse
        # an all-missing answer to the first field during repair, silently
        # dropping the remaining customer request.
        return agent_result
    trusted_attached_field_sources = {
        "validated_semantic_preplan",
        "deterministic_full_predicate",
        "deterministic_alias",
    }
    attached_formal_field_type = (
        str(attached_field_request.get("field_type") or "").strip()
        if isinstance(attached_field_request, dict)
        and attached_field_request.get("source") in trusted_attached_field_sources
        and customer_field_contract.is_supported_detail_field(attached_field_request.get("field_type"))
        else None
    )
    semantic_preplan = debug.get("semantic_preplan") if isinstance(debug.get("semantic_preplan"), dict) else None
    formal_field_request = customer_field_contract.resolve_requested_field_contract(
        question,
        {"semantic_preplan": semantic_preplan} if semantic_preplan else None,
    )
    formal_field_type = (
        formal_field_request.get("field_type")
        if formal_field_request.get("source") == "validated_semantic_preplan"
        else None
    )
    field_contract = (
        customer_field_contract.field_contract_for_type(attached_formal_field_type or formal_field_type)
        if attached_formal_field_type or formal_field_type
        else customer_field_contract.detect_field_contract(question)
    )
    detected_field_types = customer_field_contract.detect_field_types(question)
    # A validated attached contract has already adjudicated the user's field.
    # Product names and parenthetical descriptors may contain another field
    # word (for example “赠品”), which must not exempt a mismatched QA answer
    # from same-field evidence enforcement.  Without that formal attachment,
    # preserve the existing fail-closed multi-field behavior.
    if not field_contract or (
        not attached_formal_field_type
        and not formal_field_type
        and len(detected_field_types) != 1
    ):
        return agent_result
    if str(agent_result.get("answer_type") or "") != "product_detail":
        return agent_result
    plan = debug.get("plan") if isinstance(debug.get("plan"), dict) else {}
    tasks = [task for task in (plan.get("tasks") or []) if isinstance(task, dict)]
    if (
        plan.get("routing_conflict")
        or plan.get("must_compare_both_products")
        or plan.get("must_make_choice")
        or len(tasks) > 1
    ):
        return agent_result
    result_skus = [str(sku or "").strip().upper() for sku in (agent_result.get("result_skus") or []) if str(sku or "").strip()]
    if len(result_skus) != 1:
        return agent_result
    product = _explicit_product_from_question(db, question)
    if not product or str(product.sku or "").strip().upper() != result_skus[0]:
        return agent_result
    requested_label = customer_field_contract.product_detail_field_label(field_contract.field_type)
    if not requested_label:
        return agent_result
    # Material components (body, lid, handle) have independent evidence scopes.
    # A generic material replacement would collapse them to body material and
    # can therefore invent a lid/handle fact from an otherwise valid response.
    if (
        field_contract.field_type == "material"
        and customer_field_contract.material_component_detail_requests(question)
    ):
        return agent_result
    metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    # Shipping is safely unsupported as a structured database field, but an
    # explicit same-SKU ProductQa record is an allowed evidence source under
    # the shipping FieldEvidencePolicy.  Preserve that sealed result instead
    # of rebuilding it as a generic real-time shipping safe-missing answer.
    # The metadata is created only after the QA helper has sealed both the
    # EntityResolutionContract and the field-compatible QA evidence.
    has_same_sku_shipping_qa_evidence = bool(
        field_contract.field_type == "shipping"
        and metadata.get("contract_field_type") == "shipping"
        and metadata.get("field_evidence_match") is True
        and str(metadata.get("evidence_field") or "") == "product_qa"
        and str(metadata.get("evidence_sku") or "").strip().upper() == result_skus[0]
        and str(metadata.get("evidence_source") or "").startswith("product_qa:")
    )
    if has_same_sku_shipping_qa_evidence:
        return agent_result
    # Warranty has no customer-facing structured column, but a sealed same-SKU
    # QA/RAG record may state the limited static fact that official warranty is
    # available. Preserve only that evidence-bound result; missing duration or
    # terms remain absent rather than becoming an invented policy.
    has_same_sku_warranty_evidence = bool(
        field_contract.field_type == "warranty"
        and metadata.get("contract_field_type") == "warranty"
        and metadata.get("field_evidence_match") is True
        and str(metadata.get("evidence_field") or "") == "warranty"
        and result_skus[0] in {
            str(metadata.get("evidence_sku") or "").strip().upper(),
            *(
                str(sku or "").strip().upper()
                for sku in metadata.get("evidence_skus") or []
            ),
        }
    )
    if has_same_sku_warranty_evidence:
        return agent_result
    if not customer_field_contract.is_supported_detail_field(field_contract.field_type):
        existing_entity_contract = (
            metadata.get("entity_resolution_contract")
            if isinstance(metadata.get("entity_resolution_contract"), dict)
            else debug.get("entity_resolution_contract")
            if isinstance(debug.get("entity_resolution_contract"), dict)
            else {}
        )
        if (
            metadata.get("contract_field_type") == field_contract.field_type
            and metadata.get("field_evidence_missing") is True
            and str(existing_entity_contract.get("resolved_sku") or "").strip().upper() == result_skus[0]
        ):
            return agent_result
        # Safely unsupported fields are never repaired from a different
        # structured field. Rebuild their answer through the exact-entity safe
        # boundary so a product-name token cannot replace (for example) a gift
        # request with heat-source evidence.
        entity_debug = debug.get("entity_resolution_contract") if isinstance(debug.get("entity_resolution_contract"), dict) else {}
        resolution_decision = customer_entity_resolution_contract.SingleProductResolutionDecision(
            allowed=True,
            resolved_sku=result_skus[0],
            resolved_product_id=str(product.id),
            reason="field_evidence_guard_exact_identity",
            match_level=str(entity_debug.get("matched_by") or "canonical_name_exact"),
        )
        route_source = str(debug.get("agent_mode") or metadata.get("source") or "resolved_entity_unknown_field_fallback")
        if route_source not in {"resolved_entity_detail_contract", "resolved_entity_unknown_field_fallback"}:
            route_source = "resolved_entity_unknown_field_fallback"
        replacement = _build_resolved_product_unknown_field_result(
            db,
            product,
            label=requested_label,
            source=route_source,
            resolution_decision=resolution_decision,
        )
        replacement_debug = replacement.get("debug") if isinstance(replacement.get("debug"), dict) else {}
        replacement_debug["field_contract"] = dict(attached_field_request or formal_field_request)
        if entity_debug:
            replacement_debug["entity_resolution_contract"] = dict(entity_debug)
        if isinstance(semantic_preplan, dict):
            replacement_debug["semantic_preplan"] = dict(semantic_preplan)
        replacement_debug["binding_provenance"] = "resolved_entity_contract"
        replacement_debug["field_evidence_guard_applied"] = True
        replacement["debug"] = replacement_debug
        return replacement
    evidence_field = str(metadata.get("evidence_field") or "").strip()
    evidence_sku = str(metadata.get("evidence_sku") or "").strip().upper()
    if evidence_field == field_contract.field_type and evidence_sku == result_skus[0]:
        return agent_result
    replacement = _phase1_product_field_result(db, {
        "primary_intent": "product_field",
        "answer_type": "product_detail",
        "product_ref": result_skus[0],
        "sku": result_skus[0],
        "requested_field": requested_label,
        "field_type_override": field_contract.field_type,
        "field_only": True,
        "product_identity_source": "field_evidence_repair",
        "raw_question": question,
    })
    if not replacement:
        return agent_result
    replacement_metadata = replacement.get("answer_metadata") if isinstance(replacement.get("answer_metadata"), dict) else {}
    if not evidence_field:
        evidence_value = str(replacement_metadata.get("evidence_value") or "").strip()
        normalized_answer = re.sub(r"[\s，,。；;：:（）()]", "", str(agent_result.get("answer") or ""))
        normalized_evidence = re.sub(r"[\s，,。；;：:（）()]", "", evidence_value)
        # A compound answer can legitimately combine the requested structured
        # field with another same-SKU QA/KB-supported intent. Preserve it when
        # the structured evidence is already visible in the answer; attach the
        # formal evidence metadata instead of collapsing the second intent.
        if normalized_evidence and normalized_evidence in normalized_answer:
            metadata.update({
                key: replacement_metadata.get(key)
                for key in (
                    "evidence_status",
                    "evidence_field",
                    "evidence_source",
                    "evidence_value",
                    "evidence_scope",
                    "evidence_sku",
                    "evidence_label",
                    "evidence_unit",
                    "evidence_subtype",
                    "field_evidence_match",
                    "field_evidence_missing",
                )
            })
            metadata["field_evidence_metadata_repaired"] = True
            agent_result["answer_metadata"] = metadata
            debug["field_evidence_guard_applied"] = True
            agent_result["debug"] = debug
            return agent_result
        requested_label = customer_field_contract.product_detail_field_label(field_contract.field_type) or ""
        safe_missing_markers = ("暂未", "未找到", "未维护", "未标注", "无法确认", "没有")
        if (
            replacement_metadata.get("field_evidence_missing") is True
            and requested_label
            and requested_label in str(agent_result.get("answer") or "")
            and any(marker in str(agent_result.get("answer") or "") for marker in safe_missing_markers)
        ):
            metadata.update({
                key: replacement_metadata.get(key)
                for key in (
                    "evidence_status",
                    "evidence_field",
                    "evidence_source",
                    "evidence_value",
                    "evidence_scope",
                    "evidence_sku",
                    "evidence_label",
                    "evidence_unit",
                    "evidence_subtype",
                    "field_evidence_match",
                    "field_evidence_missing",
                )
            })
            metadata["field_evidence_metadata_repaired"] = True
            agent_result["answer_metadata"] = metadata
            debug["field_evidence_guard_applied"] = True
            agent_result["debug"] = debug
            return agent_result
    replacement_debug = replacement.get("debug") if isinstance(replacement.get("debug"), dict) else {}
    replacement_debug["field_evidence_guard_applied"] = True
    # Evidence repair may change formatter provenance, never the already
    # sealed identity or formal field decision that caused the repair.
    if isinstance(attached_field_request, dict):
        replacement_debug["field_contract"] = dict(attached_field_request)
        replacement_plan = replacement_debug.get("plan") if isinstance(replacement_debug.get("plan"), dict) else {}
        replacement_plan["field_contract"] = dict(attached_field_request)
        if isinstance(debug_plan, dict):
            for key in ("primary_intent", "requested_field", "requested_fields", "product_ref"):
                if key in debug_plan:
                    replacement_plan[key] = debug_plan[key]
        replacement_debug["plan"] = replacement_plan
    if isinstance(debug.get("entity_resolution_contract"), dict):
        replacement_debug["entity_resolution_contract"] = dict(debug["entity_resolution_contract"])
    if isinstance(semantic_preplan, dict):
        replacement_debug["semantic_preplan"] = dict(semantic_preplan)
    replacement_debug["agent_mode"] = str(debug.get("agent_mode") or "resolved_entity_detail_contract")
    replacement["debug"] = replacement_debug
    replacement_metadata["qa_shortcut_rejected_reason"] = (
        "field_evidence_missing"
        if not evidence_field
        else "field_evidence_mismatch"
    )
    replacement_metadata["contract_field_type"] = field_contract.field_type
    if isinstance(metadata.get("timing"), dict):
        replacement_metadata["timing"] = dict(metadata["timing"])
    replacement["answer_metadata"] = replacement_metadata
    return replacement


def _build_unresolved_product_like_unknown_field_clarification_result(
    question: str,
    *,
    label: str,
    subject_override: str | None = None,
) -> dict | None:
    subject = subject_override or customer_agent_intent_service._detail_subject_from_question(question)
    if not subject:
        return None
    if not _looks_like_product_like_scope_subject(subject):
        return None
    if _is_generic_contents_subject(subject):
        return None
    product_like_suffixes = ("壶", "水壶", "锅", "套锅", "炉", "炉具", "烤盘", "盘", "杯", "茶具", "咖啡壶", " kettle")
    normalized_subject = customer_agent_service.normalize_search_text(subject).strip()
    if (
        not any(normalized_subject.endswith(suffix.strip()) for suffix in product_like_suffixes if suffix.strip())
        and not _looks_like_product_like_scope_subject(subject)
    ):
        return None
    answer = (
        f"我没能在当前商品资料里明确找到“{subject}”对应的具体 SKU。"
        f"你可以提供 SKU，或确认是哪一款商品，我再帮你判断这款商品的实时{label}类信息该去哪里确认。"
    )
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "answer_metadata": {
            "source": "unresolved_product_like_unknown_field_clarification",
            "requested_field": label,
            "evidence_status": "unresolved_product_like_subject",
        },
        "debug": {"agent_mode": "unresolved_product_like_unknown_field_clarification"},
        "skip_polish": True,
    }


def _generic_unresolved_product_like_unknown_field_clarification_result(
    question: str,
    *,
    label: str,
    subject_override: str | None = None,
) -> dict:
    subject = subject_override or customer_agent_intent_service._detail_subject_from_question(question) or "这款商品"
    answer = (
        f"我还没法把“{subject}”稳定对应到具体 SKU。"
        f"你可以提供 SKU 或更完整的商品名，我再帮你判断这款商品的实时{label}类信息该去哪里确认。"
    )
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "answer_metadata": {
            "source": "unresolved_product_like_unknown_field_clarification",
            "requested_field": label,
            "evidence_status": "unresolved_product_like_subject",
        },
        "debug": {"agent_mode": "unresolved_product_like_unknown_field_clarification"},
        "skip_polish": True,
    }


def _build_unknown_field_product_not_found_result(
    question: str,
    *,
    label: str,
    subject_override: str | None = None,
) -> dict:
    extracted_skus = [str(item or "").strip().upper() for item in customer_agent_service._extract_skus(question) if str(item or "").strip()]
    subject = subject_override or customer_agent_intent_service._detail_subject_from_question(question) or question
    target = extracted_skus[0] if extracted_skus else str(subject or "").strip()
    answer = f"没有找到“{target}”对应的产品资料，当前无法确认它的{label}信息。请确认 SKU 或产品名是否正确。"
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "answer_metadata": {
            "source": "unknown_field_product_not_found",
            "requested_field": label,
            "evidence_status": "product_not_found",
        },
        "debug": {"agent_mode": "unknown_field_product_not_found"},
        "skip_polish": True,
    }


def _try_resolved_product_unknown_field_shortcut(db: Session, question: str) -> dict | None:
    text = str(question or "").strip()
    if not text:
        return None
    label = _resolved_entity_unknown_field_guard_label(text)
    if not label:
        return None

    products = db.query(Product).order_by(Product.sku.asc()).all()
    named_products = _products_named_in_question(db, text)
    subject_selection = customer_field_contract.select_entity_subject_for_routing(
        raw_question=text,
        fallback_product_like_subject=_product_like_scope_subject(text),
        fallback_named_subject=customer_agent_intent_service._detail_subject_from_question(text).strip(),
    )
    subject = subject_selection.entity_subject
    entity_selection = _choose_effective_entity_contract(
        text,
        products,
        resolver_candidates=named_products,
        override_subject=subject or None,
    )
    entity_contract = entity_selection.contract
    resolution_decision = customer_entity_resolution_contract.can_resolve_single_product(
        entity_contract,
        products,
    )
    if resolution_decision.allowed:
        product = next(
            (item for item in products if str(item.sku or "").strip().upper() == resolution_decision.resolved_sku),
            None,
        )
        if not product:
            return None
        if _has_direct_structured_detail_answer(db, product, text):
            return None
        exact_qa = _best_product_qa_match(db, product, text)
        if exact_qa and _is_exact_product_qa_question_match(exact_qa, text):
            return None
        field_contract = customer_field_contract.detect_field_contract(text)
        field_type = str(field_contract.field_type or "").strip() if field_contract else ""
        # Warranty is a formal field. Its missing structured column may be
        # supplemented only by the later sealed same-SKU QA/RAG adapter; this
        # legacy unknown-field shortcut has no authority to decide that
        # evidence is absent before the formal contract runs.
        if field_type == "warranty":
            return None
        if field_type:
            compatible_qa = _best_product_qa_match(
                db,
                product,
                text,
                requested_field_type=field_type,
            )
            if compatible_qa is not None:
                # A compatible QA is not an alternate route: it is admissible
                # evidence for the existing formal FieldContract. Let the
                # central detail executor consume it after identity is sealed.
                return None
        return _build_resolved_product_unknown_field_result(
            db,
            product,
            label=label,
            source="resolved_entity_unknown_field_fallback",
            resolution_decision=resolution_decision,
        )
    if len(named_products) > 1:
        return _build_ambiguous_named_product_unknown_field_result(named_products, label=label, question=text)
    return None


def _pre_route_high_risk_contract_result(
    db: Session,
    question: str,
    semantic_preplan: dict | None,
) -> dict | None:
    text = str(question or "").strip()
    if not text:
        return None
    field_contract = customer_field_contract.detect_field_contract(text)
    # A current-availability contract is not a live sales feed.  A question
    # phrased as "still sold?" is therefore a current availability request
    # even if the semantic provider mistakenly calls it product QA.  This is
    # a safety veto only: it supplies neither a route answer nor a product
    # fact and lets the existing safe-missing contract retain the resolved
    # identity.
    current_availability_request = bool(
        field_contract and field_contract.field_type in {"inventory", "lifecycle_status"}
    )
    # A validated product-QA turn has its own sealed identity and same-SKU
    # evidence path. This guard protects realtime and legacy unknown fields;
    # it must not reinterpret a product-QA subject before that contract can
    # resolve the entity and retrieve bounded QA/KB evidence.
    if (
        _semantic_prefers_sealed_product_qa({"semantic_preplan": semantic_preplan})
        and not current_availability_request
    ):
        return None
    # An ordinal field follow-up has a sealed candidate-set identity in the
    # conversation.  It is not an unknown-product request, so this guard must
    # not consume it before the context resolver forms its entity contract.
    if _looks_like_ordinal_product_field_followup(text):
        return None
    generic_scope = customer_field_contract.select_entity_subject_for_routing(raw_question=text)
    if _is_generic_company_product_scope(generic_scope.entity_subject):
        # “你们产品” / “本店商品” denotes the merchant catalogue, never a
        # single product identity.  Let the generic business/FAQ route answer
        # it instead of converting a formal field signal into an unresolved
        # product clarification.
        return None
    # Ordinals resolve against the persisted candidate set, not against a new
    # free-text entity span.  Let the dedicated conversation resolver consume
    # them before this current-turn entity guard tries to fail them closed.
    if _looks_like_ordinal_product_field_followup(text):
        return None
    if field_contract and field_contract.field_type in {"accessories", "warranty", "gift", "manual", "lifecycle_status"}:
        return None
    high_risk_field_types = {
        "after_sales_contact",
        "inventory",
        "price",
        "warranty",
        "shipping",
        "manual",
        "lifecycle_status",
    }
    # This early guard exists only for realtime/commercial or legacy unknown
    # facts. A different formal database field may contain a high-risk-looking
    # word (for example price_positioning contains "price"); it must continue
    # to the central FieldContract/EntityResolutionContract detail path.
    semantic_unknown_realtime = _semantic_preplan_unknown_realtime_subtype(semantic_preplan, text)
    if (
        field_contract
        and field_contract.field_type not in high_risk_field_types
        and not semantic_unknown_realtime
    ):
        return None
    semantic_field = str((semantic_preplan or {}).get("field_type") or "").strip()
    if (
        semantic_field
        and semantic_field in customer_field_contract.FORMAL_DETAIL_FIELDS
        and not bool((semantic_preplan or {}).get("fallback_reason"))
        and not bool((semantic_preplan or {}).get("ambiguity"))
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
    ):
        # A schema-validated semantic detail field must reach Phase 2 before
        # broad legacy words such as "认证" or "价格" can trigger an
        # unknown-fact guard. This remains true when a weaker lexical parser
        # found a conflicting field such as price inside price positioning.
        # The semantic layer still supplies no identity or evidence.
        return None
    semantic_requested_fields = list(dict.fromkeys(
        customer_field_contract.semantic_preplan_field_type(field)
        for field in ((semantic_preplan or {}).get("canonical_fields") or [])
        if customer_field_contract.semantic_preplan_field_type(field)
        in customer_field_contract.FORMAL_DETAIL_FIELDS
    ))
    if (
        len(semantic_requested_fields) > 1
        and not bool((semantic_preplan or {}).get("fallback_reason"))
        and not bool((semantic_preplan or {}).get("ambiguity"))
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
    ):
        # The central sealed multi-field executor renders each requested field
        # independently, including evidence-safe missing copy for realtime
        # inventory/price.  Consuming the turn here would keep only the first
        # legacy label and silently drop the rest of the customer's question.
        return None
    has_semantic_preplan = isinstance(semantic_preplan, dict) and semantic_preplan.get("called")
    deterministic_unknown_fields = {"manual", "after_sales_contact"}
    if not has_semantic_preplan and not (
        field_contract and field_contract.field_type in deterministic_unknown_fields
    ):
        return None
    if _looks_like_semantic_waterware_capability_query(text):
        return None
    # The semantic provider owns meaning for natural current-purchasability
    # wording.  Once it has emitted the validated unknown-realtime contract,
    # this layer supplies only the safe display label and sealed identity; it
    # must not require a second lexical alias before refusing an unverifiable
    # live-sales claim.
    label = (
        "在售状态"
        if current_availability_request or semantic_unknown_realtime
        else (_resolved_entity_realtime_commercial_field_guard_label(text) or _detect_unknown_product_fact_label(text))
    )
    if not label and field_contract and field_contract.field_type in deterministic_unknown_fields:
        label = customer_field_contract.product_detail_field_label(field_contract.field_type)
    if not label:
        return None
    supported_detail_field = customer_field_contract.is_supported_detail_field(
        field_contract.field_type if field_contract else None
    )
    subject_selection = customer_field_contract.select_entity_subject_for_routing(
        raw_question=text,
        fallback_product_like_subject=_product_like_scope_subject(text),
        fallback_named_subject=customer_agent_intent_service._detail_subject_from_question(text).strip(),
    )
    subject = subject_selection.entity_subject
    contract_products: list[Product] = []
    try:
        contract_products = db.query(Product).order_by(Product.sku.asc()).all()
        # Prefer a unique full catalogue mention contained in the current
        # turn over a field-adjacent subject polluted by a trailing discourse
        # pronoun (for example, "<product> it still sold?").  This is only
        # identity normalization: it neither chooses a field nor turns a
        # generic request into a product question.
        canonical_subject = customer_entity_resolution_contract.unique_canonical_subject_in_question(
            text,
            contract_products,
        )
        entity_selection = _choose_effective_entity_contract(
            text,
            contract_products,
            override_subject=canonical_subject or subject or None,
        )
        entity_contract = entity_selection.contract
    except Exception:
        entity_contract = None
    resolution_decision = customer_entity_resolution_contract.can_resolve_single_product(
        entity_contract,
        contract_products,
    )
    displayable_ambiguity_skus = _displayable_phase2_clarification_candidate_skus(entity_contract)
    if displayable_ambiguity_skus:
        displayable_set = set(displayable_ambiguity_skus)
        result = _build_ambiguous_named_product_unknown_field_result(
            [
                product
                for product in contract_products
                if str(product.sku or "").strip().upper() in displayable_set
            ],
            label=label,
            question=text,
        )
        debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
        debug["entity_resolution_contract"] = entity_contract.to_dict()
        result["debug"] = debug
        return result
    # A semantic ambiguity verdict is authoritative for identity safety. It
    # must be handled before a permissive lexical resolver can turn one of the
    # candidate names into a resolved product and answer a high-risk field.
    entity_scope_hint = str((semantic_preplan or {}).get("entity_scope") or "").strip()
    if entity_scope_hint == "ambiguous_product_name":
        named_products = _products_named_in_question(db, text)
        if len(named_products) > 1:
            return _build_ambiguous_named_product_unknown_field_result(named_products, label=label, question=text)
        return _build_generic_ambiguous_unknown_field_clarification_result(
            text,
            label=label,
            products=named_products,
            subject_override=str(((semantic_preplan or {}).get("entities") or [""])[0] or ""),
        )
    if resolution_decision.allowed:
        product = next(
            (
                item
                for item in contract_products
                if str(item.sku or "").strip().upper() == resolution_decision.resolved_sku
            ),
            None,
        )
        if product:
            field_type = str(field_contract.field_type or "").strip() if field_contract else ""
            if field_type and _best_product_qa_match(
                db,
                product,
                text,
                requested_field_type=field_type,
            ) is not None:
                # A same-SKU QA compatible with the already recognised formal
                # field is evidence for the central detail executor, not a
                # reason to emit a generic realtime-safe fallback early.
                return None
            result = _build_resolved_product_unknown_field_result(
                db,
                product,
                label=label,
                source="resolved_entity_unknown_field_fallback",
                resolution_decision=resolution_decision,
            )
            debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
            debug["entity_resolution_contract"] = entity_contract.to_dict()
            result["debug"] = debug
            return result
    subtype = str((semantic_preplan or {}).get("subtype") or "").strip()
    entity_scope_hint = str((semantic_preplan or {}).get("entity_scope") or "").strip()
    entity_texts = [
        str(item or "").strip()
        for item in ((semantic_preplan or {}).get("entities") or [])
        if str(item or "").strip()
    ]
    primary_entity = entity_texts[0] if entity_texts else ""

    named_products = _products_named_in_question(db, text)
    if primary_entity:
        entity_named_products = customer_agent_service.resolve_named_product_candidates(
            primary_entity,
            db.query(Product).all(),
            subject=primary_entity,
        )
        if len(entity_named_products) > len(named_products):
            named_products = entity_named_products
    if entity_scope_hint == "ambiguous_product_name":
        if len(named_products) > 1:
            return _build_ambiguous_named_product_unknown_field_result(named_products, label=label, question=text)
        return _build_generic_ambiguous_unknown_field_clarification_result(
            text,
            label=label,
            products=named_products,
            subject_override=primary_entity,
        )
    if len(named_products) > 1:
        return _build_ambiguous_named_product_unknown_field_result(named_products, label=label, question=text)
    if subtype == "no_match":
        return _build_unknown_field_product_not_found_result(text, label=label, subject_override=primary_entity)
    unresolved_clarification = _build_unresolved_product_like_unknown_field_clarification_result(
        text,
        label=label,
        subject_override=primary_entity,
    )
    if unresolved_clarification:
        return unresolved_clarification
    if entity_scope_hint == "unresolved_product_like":
        return _generic_unresolved_product_like_unknown_field_clarification_result(
            text,
            label=label,
            subject_override=primary_entity,
        )
    if customer_agent_service._extract_skus(text):
        return _build_unknown_field_product_not_found_result(text, label=label, subject_override=primary_entity)
    if subtype in _SEMANTIC_UNKNOWN_REALTIME_SUBTYPES:
        if unresolved_clarification:
            return unresolved_clarification
        if not primary_entity and not _has_unresolved_product_like_scope(db, text):
            return _unknown_product_fact_result(text, semantic_authorized=True)
        return None
    if label == "价格" and _looks_like_generic_constraint_category_query(text):
        return None
    if (
        not _contains_explicit_negative_product_marker(text)
        and not _looks_like_recommendation_request(text)
        and not customer_agent_intent_service._looks_like_contents_grounding_question(text)
        and (
            customer_agent_intent_service._looks_like_usage_care_question(text)
            or _looks_like_product_detail_field_question(text)
            or (
                supported_detail_field
                and str((semantic_preplan or {}).get("route_hint") or "").strip() == "product_detail"
            )
        )
    ):
        return {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": f"我还不能把“{primary_entity or subject or '该商品'}”稳定定位到唯一商品。请补充 SKU 或更完整的商品名，我再继续确认这个问题。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "needs_clarification": True,
            "answer_metadata": {"source": "entity_scope_ambiguous_clarification", "evidence_status": "ambiguous_product"},
            "debug": {"agent_mode": "entity_scope_ambiguous_clarification"},
            "skip_polish": True,
        }
    if not primary_entity and not _has_unresolved_product_like_scope(db, text):
        return _unknown_product_fact_result(text)
    return None


def _semantic_outage_named_product_field_clarification_result(
    db: Session,
    question: str,
    semantic_preplan: dict | None,
) -> dict | None:
    """Fail closed before legacy routes when semantic field understanding is unavailable.

    A current-turn product reference seals only identity; it must not authorize a
    free-form product fact when the semantic layer could not classify the field.
    Deterministically recognized FieldContracts keep their normal central path.
    """
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    fallback_reason = str(preplan.get("fallback_reason") or "").strip()
    if not (preplan.get("called") and fallback_reason.startswith("llm_error:")):
        return None
    # A comparison is a multi-entity contract even while semantic planning is
    # unavailable.  Its deterministic comparator already seals every explicit
    # SKU, so this single-entity safety guard must never consume it first.
    if customer_agent_intent_service._is_compare_question(question):
        return None
    # A pure exact-SKU identity question has no product field to classify:
    # identity is the requested fact.  The semantic preplan has already had
    # first opportunity to interpret the turn; when it is unavailable, defer
    # to the existing exact-SKU identity contract rather than mistaking the
    # request for an unclassified product field.  That contract independently
    # seals the SKU before it reads the product's own identity data.
    if _is_explicit_sku_identity_only_question(question):
        return None
    # Use the same central adapter as Phase 2.  Calling the raw registry here
    # skipped its controlled planner/legacy compatibility inputs during an
    # outage, so a product-agnostic capability predicate could be clarified
    # despite the normal FieldContract fallback being able to classify it.
    field_request = resolve_requested_field_contract(
        question,
        {"semantic_preplan": preplan},
    )
    # A field can be formally recognized yet deliberately safe-unsupported
    # (for example gift/manual/realtime commercial fields).  It still owns a
    # canonical FieldContract and must reach its entity-bound safe-missing
    # formatter rather than being replaced by generic semantic clarification.
    # ``field_type`` is intentionally None for a compound contract.  The
    # semantic-outage guard must therefore inspect the canonical contract as
    # a whole; otherwise a deterministic, formally supported multi-field
    # request is mistaken for an unclassified free-form fact and is
    # needlessly clarified before the central executor can run.
    if field_request.get("canonical_fields"):
        return None
    products = db.query(Product).order_by(Product.sku.asc()).all()
    subject_selection = customer_field_contract.select_entity_subject_for_routing(
        raw_question=question,
        fallback_product_like_subject=_product_like_scope_subject(question),
        fallback_named_subject=customer_agent_intent_service._detail_subject_from_question(question).strip(),
    )
    entity_selection = _choose_effective_entity_contract(
        question,
        products,
        resolver_candidates=_products_named_in_question(db, question),
        override_subject=subject_selection.entity_subject or None,
    )
    entity_contract = entity_selection.contract
    resolution = customer_entity_resolution_contract.can_resolve_single_product(
        entity_contract,
        products,
    )
    if not resolution.allowed:
        canonical_subject = customer_entity_resolution_contract.unique_canonical_subject_in_question(
            question,
            products,
        )
        if canonical_subject:
            canonical_selection = _choose_effective_entity_contract(
                question,
                products,
                resolver_candidates=_products_named_in_question(db, question),
                override_subject=canonical_subject,
            )
            canonical_resolution = customer_entity_resolution_contract.can_resolve_single_product(
                canonical_selection.contract,
                products,
            )
            if canonical_resolution.allowed:
                entity_contract = canonical_selection.contract
                resolution = canonical_resolution
    if not resolution.allowed:
        return None
    resolved_sku = str(resolution.resolved_sku or "").strip().upper()
    if not resolved_sku:
        return None
    product = next((item for item in products if str(item.sku or "").strip().upper() == resolved_sku), None)
    qa_matches = _exact_same_sku_product_qa_matches(db, product, question) if product is not None else []
    if qa_matches:
        qa_evidence_types = [
            customer_field_contract.classify_product_qa_evidence_type(
                str(qa.question or ""),
                str(getattr(qa, "tags", "") or ""),
            )
            for qa in qa_matches
        ]
        usage_care_only = bool(qa_evidence_types) and all(kind == "usage_care" for kind in qa_evidence_types)
        answer_type = "product_usage_care" if usage_care_only else "product_detail"
        evidence = [
            {
                "field": "product_qa",
                "sku": resolved_sku,
                "source": f"product_qa:{qa.id}",
                "value": str(qa.answer or "").strip(),
                "status": "structured",
                "missing": False,
            }
            for qa in qa_matches
        ]
        sources = [
            {
                "type": "product_qa",
                "sku": resolved_sku,
                "label": "同 SKU 商品问答",
                "question": str(qa.question or "").strip(),
            }
            for qa in qa_matches
        ]
        answer_lines = [f"{str(product.product_name_cn or product.product_name_en or resolved_sku).strip()}（{resolved_sku}）"]
        answer_lines.extend(
            f"{str(qa.question or '').strip().rstrip('？?')}：{str(qa.answer or '').strip()}"
            for qa in qa_matches
        )
        return {
            "intent": answer_type,
            "answer_type": answer_type,
            "answer": "\n".join(answer_lines),
            "results": [{"sku": resolved_sku, "product_name_cn": product.product_name_cn or product.product_name_en or resolved_sku}],
            "sku": resolved_sku,
            "result_skus": [resolved_sku],
            "candidate_skus": [resolved_sku],
            "sources": sources,
            "evidence": evidence,
            "answer_metadata": {
                "source": "semantic_outage_same_sku_exact_qa_evidence",
                "evidence_status": "structured",
                "evidence_sku": resolved_sku,
                "contract_field_type": None,
            },
            "debug": {
                "agent_mode": "semantic_outage_same_sku_exact_qa_usage_care_evidence" if usage_care_only else "semantic_outage_same_sku_exact_qa_evidence",
                "entity_resolution_contract": entity_contract.to_dict(),
                "field_contract": field_request,
            },
            "skip_polish": True,
        }
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": "我已经定位到这款商品，但当前无法稳定识别你要查询的具体字段。请说明想了解系列、分类、材质、规格或其他商品信息。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [resolved_sku],
        "needs_clarification": True,
        "answer_metadata": {
            "source": "semantic_outage_named_product_field_clarification",
            "evidence_status": "field_semantic_unavailable",
        },
        "debug": {
            "agent_mode": "semantic_outage_named_product_field_clarification",
            "entity_resolution_contract": entity_contract.to_dict(),
            "field_contract": field_request,
        },
        "skip_polish": True,
    }


def _exact_same_sku_product_qa_matches(db: Session, product: Product | None, question: str) -> list[ProductQa]:
    """Return exact QA evidence only after a unique entity contract is sealed."""
    if product is None or not str(question or "").strip():
        return []
    normalized_question = _normalize_qa_question_text(question)
    matches: list[tuple[int, ProductQa]] = []
    for qa in product_service.customer_visible_product_qas(db, product.id)[:20]:
        qa_question = _normalize_qa_question_text(str(qa.question or ""))
        answer = str(qa.answer or "").strip()
        if qa_question and answer and qa_question in normalized_question:
            matches.append((normalized_question.index(qa_question), qa))
    return [qa for _position, qa in sorted(matches, key=lambda item: item[0])]


def _unknown_product_fact_result(question: str, *, semantic_authorized: bool = False) -> dict | None:
    # This legacy guard predates FieldContract/EntityResolutionContract.  It
    # may provide a safe generic answer only when the question has no formal
    # customer field signal.  Otherwise it would consume a token such as
    # ``价格`` inside the distinct canonical field ``价格定位`` before the
    # conversation anchor can bind the product and before same-SKU evidence is
    # considered.
    if _looks_like_ordinal_product_field_followup(question):
        return None
    formal_contract = customer_field_contract.detect_field_contract(question)
    if not semantic_authorized and formal_contract is not None and formal_contract.field_type != "price":
        return None
    label = _detect_unknown_product_fact_label(question)
    if not label:
        return None
    text = str(question or "").strip()
    scope = _semantic_catalog_product_ref(text)
    if label == "库存":
        answer = "当前资料未标注库存，也不能仅凭现有数据库确认实时库存；如需确认现货情况，建议联系人工客服或后台查询。"
    elif label == "销量":
        answer = "当前资料未标注销量，不能仅凭现有资料判断哪款销量最高。"
    elif label == "评价":
        answer = "当前资料未标注客户评价或好评率，不能仅凭现有资料判断评价高低。"
    elif label == "价格":
        answer = "当前资料未标注实时价格，不能仅凭现有资料确认当前售价；如需价格请联系人工客服或后台查询。"
    elif label == "保修":
        answer = "当前资料未标注保修信息，不能仅凭现有资料确认质保范围；建议联系人工客服确认。"
    elif label == "认证":
        answer = "当前资料未标注对应认证或检测信息，不能仅凭现有资料确认。"
    elif label == "官方说明书":
        answer = "当前资料未维护官方说明书信息，如需确认建议联系人工客服。"
    elif label == "售后电话":
        answer = "当前资料未维护正式售后电话号码，无法提供可核验的联系电话；请通过购买平台的官方客服入口确认。"
    elif label == "安装视频":
        answer = "当前资料未维护安装视频信息，如需确认建议联系人工客服。"
    elif label == "发货时效":
        answer = (
            "目前无法承诺今天下单后明天发货或送达，实际时效会受库存、下单时间、仓库和收货地区影响，"
            "请以下单页显示的预计发货/送达时间为准；如果时间很紧，建议下单前通过店铺客服确认库存和最晚发出时间。"
        )
    else:
        return None
    if scope != "产品" and scope not in {"烤盘", "锅具", "炉具", "水具", "水壶", "咖啡器具", "茶具", "桌椅", "餐具", "配件", "天幕/地垫/帐篷"}:
        scope = "产品"
    if scope != "产品" and not SKU_RE.search(text):
        answer = f"关于【{scope}】类产品，{answer}"
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "answer_metadata": {"source": "structured_unknown_field_guard", "requested_field": label},
        "debug": {
            "agent_mode": "structured_unknown_field_guard",
            "plan": {"primary_intent": "product_field", "requested_field": label, "product_ref": ""},
            "raw_results": [],
        },
        "skip_polish": True,
    }


def _structured_filter_field_and_value(question: str) -> tuple[str, str] | None:
    text = str(question or "").strip()
    if not text:
        return None
    field_aliases = {
        "材质": ("材质", "材料"),
        "热源": ("热源", "燃气炉", "卡式炉", "明火", "酒精炉"),
    }
    value_aliases = {
        "不锈钢": ("不锈钢", "304", "316"),
        "铝合金": ("铝合金", "铝"),
        "燃气炉": ("燃气炉", "卡式炉", "明火"),
    }
    matched_field = None
    for field_name, aliases in field_aliases.items():
        if any(alias in text for alias in aliases):
            matched_field = field_name
            break
    matched_value = None
    for value_name, aliases in value_aliases.items():
        if any(alias in text for alias in aliases):
            matched_value = value_name
            break
    if matched_field is None and matched_value in {"不锈钢", "铝合金"}:
        material_shortcuts = (
            f"是{matched_value}的",
            f"要{matched_value}的",
            f"选{matched_value}的",
        )
        if any(pattern in text for pattern in material_shortcuts):
            matched_field = "材质"
    if matched_field == "热源" and matched_value == "燃气炉":
        return matched_field, matched_value
    if matched_field == "材质" and matched_value in {"不锈钢", "铝合金"}:
        return matched_field, matched_value
    return None


def _phase1_structured_field_filter_intent(question: str):
    text = str(question or "").strip()
    if _looks_like_semantic_waterware_capability_query(text):
        return None
    if "咖啡器具" in text and "手冲" in text and "配件" not in text:
        return None
    if customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(text):
        return None
    if _looks_like_multi_condition_cookware_recommendation(text):
        return None
    if not any(term in text for term in ("哪些", "有哪些", "哪一些", "列出")):
        return None
    intent = customer_agent_intent_service.parse_intent(text, previous_result_skus=[])
    if not intent or intent.intent != "query_products":
        return None
    filters = intent.filters or {}
    if not str(filters.get("product.category") or "").strip():
        return None
    if str(filters.get("specs.body_material") or "").strip():
        return intent
    if str(filters.get("specs.heat_source") or "").strip():
        return intent
    if str(filters.get("business.usage_scenarios") or "").strip():
        return intent
    if str(filters.get("specs.capacity") or "").strip():
        return intent
    if str(filters.get("specs.gross_weight_g") or "").strip():
        return intent
    if str(filters.get("business.target_audience") or "").strip():
        return intent
    if str(filters.get("business.price_positioning") or "").strip():
        return intent
    if intent.negative_filters:
        return intent
    return None


def _structured_hard_filter_contract(question: str) -> dict[str, Any]:
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return {}
    target_category = _structured_target_category_from_question(text)
    if not target_category:
        semantic_category = _semantic_catalog_product_ref(text)
        if semantic_category != "产品":
            target_category = semantic_category
    # This is deliberately a narrow execution adapter, rather than an
    # alternate intent router.  A customer can ask to select a generic
    # catalogue category by one formally supported, evidence-backed property.
    # The category plus formal field are the executable contract; no wording
    # list decides whether a customer is "really" selecting products.
    # Named products are excluded above by the entity contract before this
    # function is allowed to execute.
    explicit_dishwasher_catalog_selection = bool(target_category and "洗碗机" in text)
    targeted_heat_recommendation = bool(
        target_category
        and "推荐" in text
        and any(term in text for term in ("燃气炉", "卡式炉", "电磁炉", "电陶炉", "酒精炉", "明火"))
    )
    if (
        not any(term in text for term in ("哪些", "有哪些", "哪一些", "列出"))
        and not targeted_heat_recommendation
        and not explicit_dishwasher_catalog_selection
    ):
        return {}
    if _looks_like_semantic_waterware_capability_query(text):
        return {}
    if "咖啡器具" in text and "手冲" in text and "配件" not in text:
        return {}
    if customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(text):
        return {}
    if _looks_like_multi_condition_cookware_recommendation(text):
        return {}
    soft_preference_count = sum(
        1
        for matched in (
            any(term in text for term in ("两个人", "两人", "2人", "双人")),
            any(term in text for term in ("轻量", "轻便", "别太重")),
            any(term in text for term in ("预算低", "预算不高", "预算低一点", "便宜点", "性价比")),
            "新手" in text,
        )
        if matched
    )
    if soft_preference_count >= 2 and any(term in text for term in ("锅具", "锅", "炉具", "炉")):
        return {}

    intent = customer_agent_intent_service.parse_intent(text, previous_result_skus=[])
    filters = dict((intent.filters or {}) if intent else {})
    negative_filters = dict((intent.negative_filters or {}) if intent else {})
    requested_fields = list((intent.requested_fields or []) if intent else [])

    lowered = text.lower()
    product_ref = str(filters.get("product.category") or "").strip()
    if target_category:
        product_ref = target_category
        filters["product.category"] = target_category
    elif "水壶" in text or ("哪些壶" in text and "咖啡壶" not in text and "茶壶" not in text):
        product_ref = "水壶"
        filters["product.category"] = "水壶"
    elif any(term in text for term in ("水杯", "杯子", "钛杯", "杯")):
        product_ref = "水具"
        filters["product.category"] = "水杯"
    elif any(term in text for term in ("锅具", "套锅", "单锅")) or ("锅" in text and "火锅" not in text):
        product_ref = "锅具"
        filters["product.category"] = "锅具"
    elif any(term in text for term in ("炉具", "炉子", "卡式炉", "酒精炉")):
        product_ref = "炉具"
        filters["product.category"] = "炉具"
    elif "配件" in text:
        product_ref = "配件"
        filters["product.category"] = "配件"
    elif "咖啡器具" in text:
        product_ref = "咖啡器具"
        filters["product.category"] = "咖啡器具"
    elif "水具" in text:
        product_ref = "水具"
        filters["product.category"] = "水具"

    if not str(filters.get("specs.body_material") or "").strip():
        for raw_value in ("硬质氧化铝合金", "硬质氧化铝", "铝合金", "不锈钢", "钛"):
            if raw_value in text:
                normalized = customer_agent_intent_service._normalize_structured_filter_value("specs.body_material", raw_value)
                if normalized:
                    filters["specs.body_material"] = normalized
                    if "材质" not in requested_fields:
                        requested_fields.append("材质")
                    break

    if not str(filters.get("specs.heat_source") or "").strip():
        if "酒精炉" in text:
            filters["specs.heat_source"] = "酒精炉"
        elif "卡式炉" in text:
            filters["specs.heat_source"] = "卡式炉"
        elif "电磁炉" in text:
            filters["specs.heat_source"] = "电磁炉"
        elif "电陶炉" in text:
            filters["specs.heat_source"] = "电陶炉"
        elif "明火直烧" in text:
            filters["specs.heat_source"] = "明火直烧"
        elif "明火" in text or "直接加热" in text:
            filters["specs.heat_source"] = "明火"
        if str(filters.get("specs.heat_source") or "").strip() and "热源" not in requested_fields:
            requested_fields.append("热源")

    usage_filter = str(filters.get("business.usage_scenarios") or "").strip()
    heat_source_filter = str(filters.get("specs.heat_source") or "").strip()
    if target_category and heat_source_filter and heat_source_filter in usage_filter:
        filters.pop("business.usage_scenarios", None)
        requested_fields = [field for field in requested_fields if field != "适用场景"]

    if not str(filters.get("business.usage_scenarios") or "").strip():
        if "徒步" in text:
            filters["business.usage_scenarios"] = "徒步"
        elif "咖啡相关配件" in text:
            filters["business.usage_scenarios"] = "咖啡"
        if str(filters.get("business.usage_scenarios") or "").strip() and "适用场景" not in requested_fields:
            requested_fields.append("适用场景")

    dishwasher_field_requested = explicit_dishwasher_catalog_selection or _structured_contract_requests_dishwasher_field(
        text=text,
        requested_fields=requested_fields,
        filters=filters,
        negative_filters=negative_filters,
    )
    if dishwasher_field_requested:
        filters["_contract.dishwasher"] = "洗碗机"
    if "不能进洗碗机" in text or "不能放洗碗机" in text or "不可进洗碗机" in text or "不适合洗碗机" in text:
        negative_filters["_contract.dishwasher"] = "洗碗机"
        filters.pop("_contract.dishwasher", None)
    if "轻量" in text or "轻便" in text or "别太重" in text or "比较轻" in text:
        filters["_contract.lightweight"] = "true"
    if "徒步" in text:
        filters["_contract.hiking"] = "true"
    if "两个人" in text or "两人" in text or "2人" in text or "双人" in text:
        filters["_contract.two_person"] = "true"
    if "新手" in text:
        filters["_contract.beginner"] = "true"
    if "预算低" in text or "预算不高" in text or "预算低一点" in text or "便宜点" in text or "性价比" in text:
        filters["_contract.budget"] = "true"
    if "咖啡相关配件" in text:
        filters["_contract.coffee_accessory"] = "true"

    if not product_ref:
        product_ref = _semantic_catalog_product_ref(text)
    if product_ref == "产品":
        product_ref = ""

    hard_filter_keys = {
        key
        for key in filters.keys()
        if key != "product.category"
    }
    hard_filter_keys.update(negative_filters.keys())
    if not product_ref or not hard_filter_keys:
        return {}
    return {
        "intent": intent,
        "product_ref": product_ref,
        "filters": filters,
        "negative_filters": negative_filters,
        "requested_fields": requested_fields,
    }


def _semantic_schema_failed_hard_filter_repair(
    semantic_preplan: dict[str, Any] | None,
    *,
    question: str,
) -> bool:
    """Allow a validated local filter only after a semantic schema failure.

    The semantic preplan remains the intent authority.  This only preserves a
    generic category request when its route was structured_query but the model
    serialized a non-allowlisted predicate, and the central contract can
    independently prove a supported hard filter from the same turn.
    """
    if not isinstance(semantic_preplan, dict) or not semantic_preplan.get("called"):
        return False
    if str(semantic_preplan.get("semantic_route_family_hint") or "").strip() != "structured_query":
        return False
    if str(semantic_preplan.get("fallback_reason") or "").strip() not in {
        "invalid_structured_query_constraints",
        "structured_query_constraints_outside_structured_query",
        "structured_query_constraints_field_mismatch",
        "missing_structured_query_constraints",
    }:
        return False
    contract = _structured_hard_filter_contract(question)
    return bool(contract and contract.get("product_ref") and contract.get("filters"))


def _structured_contract_requests_dishwasher_field(
    *,
    text: str,
    requested_fields: list[str],
    filters: dict[str, Any],
    negative_filters: dict[str, Any],
) -> bool:
    if str(negative_filters.get("_contract.dishwasher") or "").strip():
        return False
    if str(filters.get("_contract.dishwasher") or "").strip():
        return True
    requested_field_paths = {
        str(customer_agent_intent_service._resolve_query_field(field) or "").strip()
        for field in requested_fields
        if field
    }
    if "specs.usage_instruction" not in requested_field_paths:
        return False
    dishwasher_signals = {"洗碗机"}
    if any(signal in str(text or "") for signal in dishwasher_signals):
        return True
    return False


def _structured_row_matches_contract(row: dict[str, Any], contract: dict[str, Any]) -> bool:
    filters = dict(contract.get("filters") or {})
    negative_filters = dict(contract.get("negative_filters") or {})
    row_text = _structured_query_row_text(row)
    row_text_lower = row_text.lower()
    weight_raw = row.get("gross_weight_g")
    try:
        weight_value = float(weight_raw or 0)
    except (TypeError, ValueError):
        weight_value = 0.0
    capacity_text = str(row.get("capacity") or "")
    price_positioning = str(row.get("price_positioning") or "")

    dishwasher_required = bool(filters.pop("_contract.dishwasher", ""))
    dishwasher_required_negative = bool(negative_filters.pop("_contract.dishwasher", ""))
    lightweight_required = bool(filters.pop("_contract.lightweight", ""))
    hiking_required = bool(filters.pop("_contract.hiking", ""))
    two_person_required = bool(filters.pop("_contract.two_person", ""))
    beginner_required = bool(filters.pop("_contract.beginner", ""))
    budget_required = bool(filters.pop("_contract.budget", ""))
    coffee_accessory_required = bool(filters.pop("_contract.coffee_accessory", ""))

    category_value = str(filters.pop("product.category", "") or "").strip()
    body_material_value = str(filters.pop("specs.body_material", "") or "").strip()
    heat_source_value = str(filters.pop("specs.heat_source", "") or "").strip()
    usage_value = str(filters.pop("business.usage_scenarios", "") or "").strip()

    if body_material_value:
        material_contract = customer_structured_query_contract.StructuredQueryContract(
            subject_category=category_value or "all_products",
            field="material",
            operator="contains",
            value=body_material_value,
            status="resolved",
        )
        if not customer_structured_query_contract.match_material_condition(
            contract=material_contract,
            row=row,
        )["matched"]:
            return False
    elif category_value and not _structured_row_matches_category_value(row, category_value):
        return False
    if heat_source_value and not _structured_row_matches_heat_value(row, heat_source_value):
        return False
    if usage_value and not _structured_row_matches_usage_value(row, usage_value):
        return False
    if filters and not customer_agent_intent_service._filter_rows([row], filters=filters, negative_filters={}, term=""):
        return False
    if negative_filters and not customer_agent_intent_service._filter_rows([row], filters={}, negative_filters=negative_filters, term=""):
        return False

    if coffee_accessory_required and not any(term in row_text for term in ("咖啡", "手冲", "滤杯", "细口壶", "分享壶")):
        return False
    if dishwasher_required and not any(
        term in row_text
        for term in ("洗碗机", "可放入洗碗机", "可进洗碗机", "适合洗碗机", "可洗碗机清洗")
    ):
        return False
    if dishwasher_required_negative and not any(
        term in row_text
        for term in ("不适合洗碗机", "不可放入洗碗机", "不能放进洗碗机", "不能进洗碗机", "勿入洗碗机")
    ):
        return False
    if lightweight_required and not (
        any(term in row_text for term in ("轻量", "轻便", "便携"))
        or (0 < weight_value <= 350)
    ):
        return False
    if hiking_required and not any(term in row_text for term in ("徒步", "轻量", "便携")):
        return False
    if two_person_required and not (
        any(term in row_text for term in ("两人", "两个人", "2人", "双人", "1-2人"))
        or any(term in capacity_text for term in ("1-2人", "2人"))
    ):
        return False
    if beginner_required and not any(term in row_text for term in ("新手", "入门", "基础", "性价比")):
        return False
    if budget_required and not any(term in f"{row_text_lower} {price_positioning.lower()}" for term in ("基础", "性价比")):
        return False
    return True


def _looks_like_structured_field_filter_query(question: str) -> bool:
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(text):
        return False
    if _looks_like_multi_condition_cookware_recommendation(text):
        return False
    if _structured_hard_filter_contract(text):
        return True
    if _phase1_structured_field_filter_intent(text):
        return True
    if not any(term in text for term in ("有哪些", "哪些", "哪一些", "推荐", "找")):
        return False
    if _semantic_catalog_product_ref(text) == "产品":
        return False
    return _structured_filter_field_and_value(text) is not None


def _looks_like_semantic_waterware_capability_query(question: str) -> bool:
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if any(term in text for term in ("怎么选", "推荐", "想买", "买个", "买一", "选哪", "哪款", "哪种")):
        return False
    if not any(term in text for term in ("有哪些", "哪一些", "找", "列出")):
        return False
    if not any(term in text for term in ("水具", "水壶", "水杯")):
        return False
    return any(term in text for term in ("冷水", "补水", "饮水", "热水", "装热水", "喝热水"))


def _structured_field_filter_result(db: Session, question: str) -> dict | None:
    text = str(question or "").strip()
    if _is_internal_catalogue_filter_query(text):
        return None
    if _looks_like_semantic_waterware_capability_query(text):
        return None
    if not _looks_like_structured_field_filter_query(text):
        return None
    resolved_contract = customer_structured_query_contract.build_structured_query_contract(text)
    contract = _structured_hard_filter_contract(text)
    intent = contract.get("intent") if contract else _phase1_structured_field_filter_intent(text)
    filters = dict((contract.get("filters") or {}) if contract else dict((intent.filters or {}) if intent else {}))
    negative_filters = dict((contract.get("negative_filters") or {}) if contract else {})
    product_ref = (
        str(resolved_contract.subject_category or "").strip()
        if resolved_contract.status == "resolved" and resolved_contract.conditions
        else ""
    ) or str((contract.get("product_ref") if contract else filters.get("product.category")) or "").strip() or _semantic_catalog_product_ref(text)
    if not product_ref or product_ref == "产品":
        return None
    if product_ref == "水壶":
        source_rows = [
            row for row in _phase1_catalog_rows(db, "产品")
            if _phase1_is_strict_water_kettle_candidate(row)
            and str(row.get("category") or "").strip() == "水壶"
        ]
    else:
        source_rows = _phase1_catalog_rows(db, product_ref)
    if product_ref == "锅具" and str(filters.get("specs.heat_source") or "").strip() == "酒精炉":
        source_rows = [
            row for row in source_rows
            if _is_service_pot_or_cookware_set_candidate(row)
            and _phase1_row_has_explicit_alcohol_stove_support(db, row)
        ]
    if resolved_contract.status == "resolved" and resolved_contract.conditions:
        rows = [
            row
            for row in source_rows
            if customer_structured_query_contract.evaluate_structured_row(row, resolved_contract).get("matched")
        ]
    else:
        rows = [row for row in source_rows if _structured_row_matches_contract(row, {"filters": filters, "negative_filters": negative_filters})]
    if not rows:
        answer = "当前结构化商品库未找到符合条件的商品，可以尝试放宽筛选条件。"
        return {
            "intent": "query_products",
            "answer_type": "product_query",
            "answer": answer,
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "answer_metadata": {"source": "structured_category_field_filter_query", "product_ref": product_ref, "matched_count": 0},
            "debug": {"agent_mode": "structured_category_field_filter_query", "raw_results": [], "candidate_skus": []},
            "skip_polish": True,
        }
    return _structured_product_query_result(
        question=text,
        product_ref=product_ref,
        rows=rows,
        source="structured_category_field_filter_query",
        reason_label=f"{product_ref} / 结构化硬筛选条件",
    )


def _candidate_context_formal_field_filter_result(
    db: Session,
    *,
    question: str,
    field_request: dict[str, Any] | None,
    candidate_context: dict[str, Any] | None,
) -> dict[str, Any] | None:
    """Filter a sealed candidate set from one formal field contract.

    This is deliberately field-policy driven: it does not resolve a new
    product, infer a value from a name, or run a catalogue-wide search.  It is
    only available when the current question explicitly refers to the prior
    candidate set and contains an actual value from the contracted column.
    """
    if not isinstance(field_request, dict) or not isinstance(candidate_context, dict):
        return None
    field_type = str(field_request.get("field_type") or "").strip()
    if (
        not field_type
        or len(field_request.get("supported_fields") or []) != 1
        or not customer_agent_intent_service._has_context_reference(question)
    ):
        return None
    if not _candidate_context_formal_field_filter_eligible(
        question,
        field_request=field_request,
        candidate_context=candidate_context,
    ):
        return None
    policy = customer_field_contract.field_evidence_policy(field_type)
    if not policy or not policy.structured_fields:
        return None
    domain_skus = [
        str(sku or "").strip().upper()
        for sku in (
            candidate_context.get("ordered_result_skus")
            or candidate_context.get("candidate_skus")
            or []
        )
        if str(sku or "").strip()
    ]
    if len(domain_skus) < 2:
        return None
    # The formal FieldContract decides *which* field the user is asking
    # about.  The existing structured-query contract is then used only to
    # validate an explicit filter value against that field's same-SKU column.
    # This avoids treating a shorter cell value as a match merely because it
    # is a substring of a more specific phrase in the question.
    scoped_intent = customer_agent_intent_service.parse_intent(
        question,
        previous_result_skus=domain_skus,
    )
    predicate_filters = {
        source: value
        for source, value in dict(getattr(scoped_intent, "filters", {}) or {}).items()
        if source in policy.structured_fields and str(value or "").strip()
    }
    if not predicate_filters:
        return None
    predicate_contract = {"filters": predicate_filters, "negative_filters": {}}
    rows_by_sku = {
        str(row.get("sku") or "").strip().upper(): row
        for row in _phase1_catalog_rows(db, "产品")
        if isinstance(row, dict) and str(row.get("sku") or "").strip().upper() in set(domain_skus)
    }
    matched_rows: list[dict[str, Any]] = []
    evidence: list[dict[str, Any]] = []
    for sku in domain_skus:
        row = rows_by_sku.get(sku)
        if not row:
            continue
        values: list[tuple[str, str]] = []
        for source in policy.structured_fields:
            column = str(source or "").rsplit(".", 1)[-1]
            raw_value = row.get(column)
            if not _is_valid_structured_field_value(field_type, raw_value):
                continue
            display_value = _display_value(raw_value).strip()
            if display_value:
                values.append((str(source), display_value))
        if not values:
            continue
        # Match with the shared structured contract rather than raw substring
        # containment.  The latter would incorrectly make "铝合金" satisfy
        # a request for the narrower "硬质氧化铝合金".
        if not _structured_row_matches_contract(row, predicate_contract):
            continue
        # The selected proof must itself contain the normalized filter value.
        # This preserves the concrete same-SKU column that justified the
        # subset, instead of reprinting an unrelated value from the field.
        matching_values = [
            (source, value)
            for source, value in values
            if any(str(target).strip().lower() in value.lower() for target in predicate_filters.values())
        ]
        if not matching_values:
            continue
        matched_rows.append(row)
        for source, value in matching_values:
            evidence.append({"type": "product_field", "sku": sku, "field": field_type, "source": source, "value": value})
    # A candidate follow-up is a sealed prior result set.  For an alcohol-stove
    # cookware query, remove explicit pans/griddles but do not require every
    # remaining candidate title to repeat a cookware keyword: a valid prior
    # cookware set may be named only as a scenario-oriented “自驾套装”.  This
    # remains a same-SKU heat-source filter, not a new entity resolution or
    # SKU rule.
    if (
        field_type == "heat_source"
        and any("酒精" in str(value or "") for value in predicate_filters.values())
        and _is_service_pot_cookware_scope(
            str(candidate_context.get("product_scope") or ""),
            " ".join((question, str(candidate_context.get("user_question") or ""))),
        )
    ):
        def is_explicit_non_pot_identity(row: dict[str, Any]) -> bool:
            identity_text = " ".join(
                str(row.get(key) or "")
                for key in ("product_name_cn", "product_name_en", "name", "title", "sub_category")
            ).lower()
            return (
                _is_service_pan_or_griddle_candidate_text(identity_text)
                or any(term in identity_text for term in ("水壶", "茶壶", "烧水壶", "kettle", "bottle", "flask", "cup", "杯"))
            )

        matched_rows = [row for row in matched_rows if not is_explicit_non_pot_identity(row)]
        matched_skus = {str(row.get("sku") or "").strip().upper() for row in matched_rows}
        evidence = [item for item in evidence if str(item.get("sku") or "").strip().upper() in matched_skus]
    if not matched_rows:
        # A formal, value-bearing field filter has consumed the sealed
        # candidate domain.  An empty match is therefore an empty subset, not
        # permission for an older context shortcut to reopen the catalogue.
        empty_context = dict(candidate_context)
        empty_context.update({
            "candidate_skus": [],
            "ordered_result_skus": [],
            "original_candidate_skus": domain_skus,
            "parent_candidate_skus": domain_skus,
            "applied_filter": {"field": field_type, "filters": predicate_filters},
            "empty_subset": True,
        })
        result = _empty_candidate_subset_followup_result(question, empty_context)
        debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
        debug["agent_mode"] = "candidate_context_formal_field_filter"
        debug["field_contract"] = dict(field_request)
        debug["candidate_skus"] = []
        debug["intent"] = {"intent": "query_products", "source_context": "previous_results", "target_skus": domain_skus}
        result["debug"] = debug
        return result
    result_skus = [str(row.get("sku") or "").strip().upper() for row in matched_rows]
    label = customer_field_contract.product_detail_field_label(field_type) or field_type
    detail_lines = []
    for row in matched_rows:
        sku = str(row.get("sku") or "").strip().upper()
        name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
        row_values = [item["value"] for item in evidence if item["sku"] == sku]
        detail_lines.append(f"- {name}（{sku}）：{'；'.join(dict.fromkeys(row_values))}")
    answer = f"已在上一轮候选范围内按{label}筛到 {len(matched_rows)} 款：\n" + "\n".join(detail_lines)
    scoped_context = {
        "candidate_skus": result_skus,
        "ordered_result_skus": result_skus,
        "filtered_skus": result_skus,
        "original_candidate_skus": domain_skus,
        "parent_candidate_skus": domain_skus,
        "recommended_skus": [],
        "user_question": question,
        "product_scope": str(candidate_context.get("product_scope") or "").strip(),
        "empty_subset": False,
        "applied_filter": {"field": field_type},
    }
    return {
        "intent": "query_products",
        "answer_type": "product_query",
        "answer": answer,
        "results": matched_rows,
        "result_skus": result_skus,
        "candidate_skus": result_skus,
        "evidence": evidence,
        "sources": [{"type": "agent_meta", "candidate_context": scoped_context}],
        "debug": {
            "agent_mode": "candidate_context_formal_field_filter",
            "field_contract": dict(field_request),
            "candidate_skus": result_skus,
            "raw_results": matched_rows,
            "intent": {"intent": "query_products", "source_context": "previous_results", "target_skus": domain_skus},
        },
        "skip_polish": True,
    }


def _candidate_context_formal_field_filter_eligible(
    question: str,
    *,
    field_request: dict[str, Any] | None,
    candidate_context: dict[str, Any] | None,
) -> bool:
    if not isinstance(field_request, dict) or not isinstance(candidate_context, dict):
        return False
    # An ordinal selects one identity from the sealed candidate order.  It is
    # therefore a single-product field follow-up, not a request to filter the
    # whole prior set by a field value.
    if _looks_like_ordinal_product_field_followup(question):
        return False
    # A grammatical singular reference is a product-bound follow-up.  The
    # candidate set may provide the anchor, but it must not turn "it" into a
    # plural filter over every prior candidate; that path would bypass the
    # EntityResolutionContract required for a product fact.
    if re.match(r"^\s*(?:那|这)?(?:它|这个|这款|该商品|该产品|这个商品|这件商品|本产品)", str(question or "")):
        return False
    domain_skus = candidate_context.get("ordered_result_skus") or candidate_context.get("candidate_skus") or []
    return bool(
        len([sku for sku in domain_skus if str(sku or "").strip()]) >= 2
        and len(field_request.get("supported_fields") or []) == 1
        and customer_agent_intent_service._has_context_reference(question)
        and not customer_agent_service._extract_skus(question)
    )


def _looks_like_multi_condition_cookware_recommendation(question: str) -> bool:
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if not any(term in text for term in ("锅具", "锅", "套锅", "炊具")):
        return False
    if any(term in text for term in ("水壶", "水具", "水杯", "壶")):
        return False
    has_group_or_scene = any(term in text for term in ("4人以上", "4人", "四口", "多人", "家庭", "露营", "户外", "新手"))
    has_constraints = any(term in text for term in ("容量大", "容量要大", "大容量", "燃气炉", "卡式炉", "明火", "别太重", "轻", "轻量", "轻便"))
    has_budget_preference = any(term in text for term in ("价格别太高", "别太贵", "不要太贵", "预算不高", "预算有限", "便宜点", "性价比"))
    has_selection = any(term in text for term in ("推荐", "怎么选", "买什么", "买锅具", "有哪些", "用什么"))
    return has_group_or_scene and (has_constraints or has_budget_preference) and (has_selection or "。" in text)


def _is_explicit_charcoal_recommendation_request(question: str) -> bool:
    """Keep an explicit charcoal requirement on the catalogue-verification path."""
    text = str(question or "").strip()
    if not text:
        return False
    charcoal_terms = ("炭火", "炭烧", "烧炭", "碳火", "碳烧", "木炭", "炭炉", "碳炉")
    recommendation_terms = (
        "推荐", "适合买", "买哪种", "买什么", "选哪种", "选哪个", "哪款", "哪一个", "适合",
    )
    return any(term in text for term in charcoal_terms) and any(
        term in text for term in recommendation_terms
    )


def _multi_category_recommendation_scopes(question: str) -> list[tuple[str, str]]:
    """Extract independent catalogue scopes from an explicit multi-item request.

    This is a scope decomposition only: each child query still goes through
    the normal catalogue/recommendation executor.  It covers wording such as
    “分别推荐” as well as “推荐一个酒精炉，适配的烧水壶和煮面锅”.
    """
    text = str(question or "").strip()
    recommendation_signals = ("推荐", "怎么选", "怎么搭", "哪种搭配", "搭配更", "哪个更合适", "先买哪类", "哪类最值")
    if not text or not any(signal in text for signal in recommendation_signals):
        return []
    if SKU_RE.search(text):
        return []
    if _phase1_is_stove_pairing_scenario(text):
        return []
    if (
        customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(text)
        or _looks_like_multi_condition_cookware_recommendation(text)
    ):
        return []
    scope_specs = (
        ("燃气炉", ("燃气炉", "燃气灶", "气炉", "燃气"), "燃气炉推荐一个"),
        ("酒精炉", ("酒精炉", "酒精灶"), "酒精炉推荐一个"),
        ("炉具", ("炉具", "炉子", "烧烤炉"), "炉具推荐一个"),
        ("烤盘", ("烤盘", "煎盘", "烤网", "烤架"), "烤盘推荐一个"),
        ("锅具", ("锅具", "套锅", "炊具"), "锅具推荐一个"),
        ("烧水壶", ("烧水壶", "煮水壶", "水壶"), "烧水壶推荐一个"),
        ("煮面锅", ("煮面锅", "面锅", "汤锅"), "煮面锅推荐一个"),
        ("配件", ("配件", "附件", "配件包"), "配件推荐一个"),
    )
    found: list[tuple[int, str, str]] = []
    for label, terms, scoped_question in scope_specs:
        positions = [text.find(term) for term in terms if text.find(term) >= 0]
        if positions:
            found.append((min(positions), label, scoped_question))
    if len(found) < 2:
        return []
    found.sort(key=lambda item: item[0])
    return [(label, scoped_question) for _, label, scoped_question in found]


def _normalize_recommendation_scope_label(value: str) -> str:
    ref = _semantic_catalog_product_ref(str(value or "").strip())
    return {
        "燃气炉": "炉具",
        "酒精炉": "炉具",
        "炉具": "炉具",
        "水壶": "水具",
        "烧水壶": "水具",
        "水具": "水具",
        "锅具": "锅具",
        "煮面锅": "锅具",
        "烤盘": "锅具",
        "煎盘": "锅具",
        "配件": "配件",
    }.get(ref, ref if ref != "产品" else str(value or "").strip())


def _normalized_recommendation_scope_skus(value: Any) -> dict[str, list[str]]:
    if not isinstance(value, dict):
        return {}
    normalized: dict[str, list[str]] = {}
    for raw_label, raw_skus in value.items():
        label = _normalize_recommendation_scope_label(str(raw_label or ""))
        if not label:
            continue
        for raw_sku in raw_skus if isinstance(raw_skus, (list, tuple)) else []:
            sku = str(raw_sku or "").strip().upper()
            if sku and sku not in normalized.setdefault(label, []):
                normalized[label].append(sku)
    return normalized


def _multi_category_recommendation_row_is_sellable(row: dict[str, Any]) -> bool:
    lifecycle = str(row.get("lifecycle_status") or "").strip()
    return not any(term in lifecycle for term in ("禁用", "老款无货", "下架", "停售", "未上市"))


def _multi_category_timeout_fallback_rows(db: Session, scope_label: str) -> list[dict]:
    """Recover only same-scope catalogue rows when a child recommendation times out."""
    normalized_scope = _normalize_recommendation_scope_label(scope_label)
    category_rows = [
        row
        for row in _phase1_catalog_rows(db, "产品")
        if isinstance(row, dict)
        and _multi_category_recommendation_row_is_sellable(row)
        and _normalize_recommendation_scope_label(str(row.get("category") or "")) == normalized_scope
    ]
    scope_terms = {
        "燃气炉": ("燃气炉", "燃气灶", "气炉", "燃气"),
        "酒精炉": ("酒精炉", "酒精灶"),
        "烤盘": ("烤盘", "煎盘", "烤网", "烤架"),
        "烧水壶": ("烧水壶", "煮水壶", "水壶"),
        "煮面锅": ("煮面锅", "面锅", "汤锅"),
    }.get(scope_label, ())
    if not scope_terms:
        return category_rows
    direct_matches = [
        row for row in category_rows
        if any(term in _structured_query_row_text(row) for term in scope_terms)
    ]
    return direct_matches or category_rows


async def _multi_category_recommendation_result(
    db: Session,
    *,
    user_id: str,
    question: str,
) -> dict | None:
    scopes = _multi_category_recommendation_scopes(question)
    if not scopes:
        return None
    answer_lines: list[str] = []
    selected_rows: list[dict] = []
    child_debug: list[dict] = []
    sources: list[dict] = []
    recommendation_scope_skus: dict[str, list[str]] = {}
    for label, scoped_question in scopes:
        child_fallback_reason = ""
        try:
            child = await asyncio.wait_for(
                customer_agent_intent_service.process_intent_request(
                    db,
                    user_id=user_id,
                    question=scoped_question,
                    sku=None,
                    previous_result_skus=[],
                    allow_llm_fallback=False,
                ),
                timeout=MULTI_CATEGORY_CHILD_TIMEOUT_SECONDS,
            )
        except asyncio.TimeoutError:
            # The verified catalogue path below can still form a bounded
            # recommendation when a narrative/model child is unavailable.
            child = {}
            child_fallback_reason = "child_timeout"
        child = child if isinstance(child, dict) else {}
        rows = [
            row for row in (child.get("results") or [])
            if isinstance(row, dict) and _multi_category_recommendation_row_is_sellable(row)
        ]
        if child_fallback_reason and not rows:
            rows = _multi_category_timeout_fallback_rows(db, label)
        # A broad child search can return a neighbouring product family.  Run
        # the same semantic subject/subtype verifier used by ordinary
        # recommendations so “酒精炉” cannot silently select a gas stove (or
        # a kettle child cannot select a cup).
        scoped_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(
            scoped_question
        )
        if scoped_contract.subject_kind or scoped_contract.subject_subtype or scoped_contract.heat_sources:
            # The child intent executor intentionally returns a small ranked
            # window.  For an explicit subtype (for example 酒精炉 or 水壶),
            # that window can contain only neighbouring category rows while
            # the genuinely matching SKU sits later in the catalogue.  Widen
            # the evidence pool before verification; the shared verifier still
            # decides eligibility and the final answer remains evidence-bound.
            if scoped_contract.subject_subtype:
                seen_child_skus = {
                    str(row.get("sku") or "").strip().upper()
                    for row in rows
                    if str(row.get("sku") or "").strip()
                }
                catalogue_rows = [
                    row
                    for row in _phase1_catalog_rows(db, "产品")
                    if isinstance(row, dict)
                    and _multi_category_recommendation_row_is_sellable(row)
                    and str(row.get("sku") or "").strip().upper() not in seen_child_skus
                ]
                rows = [*rows, *catalogue_rows]
            scoped_verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(
                scoped_contract,
                rows,
            )
            rows = customer_recommendation_verification_contract.select_recommendation_candidates(
                rows,
                scoped_verifications,
            )
        selected = rows[0] if rows else None
        if selected:
            name = str(selected.get("product_name_cn") or selected.get("product_name_en") or selected.get("sku") or "").strip()
            sku = str(selected.get("sku") or "").strip().upper()
            reason = str(
                selected.get("features")
                or selected.get("usage_scenarios")
                or selected.get("positioning")
                or selected.get("capacity")
                or ""
            ).strip("。；; \n")
            reason = re.sub(r"\s+", " ", reason)[:220]
            answer_lines.append(
                f"{label}：优先推荐{name}（{sku}）" + (f"。资料显示：{reason}。" if reason else "。")
            )
            selected_rows.append(selected)
            child_skus = [
                str(row.get("sku") or "").strip().upper()
                for row in rows[:5]
                if str(row.get("sku") or "").strip()
            ]
            child_trace = {"scope": label, "result_skus": child_skus}
            if child_fallback_reason:
                child_trace["fallback_reason"] = child_fallback_reason
            child_debug.append(child_trace)
            normalized_scope = _normalize_recommendation_scope_label(label)
            if normalized_scope:
                for child_sku in child_skus:
                    if child_sku not in recommendation_scope_skus.setdefault(normalized_scope, []):
                        recommendation_scope_skus[normalized_scope].append(child_sku)
        else:
            answer_lines.append(
                f"{label}：当前资料中没有找到可核验且在售的单品推荐；请补充人数、使用场景或火力/容量要求。"
            )
            child_trace = {"scope": label, "result_skus": [], "child_intent": child.get("intent")}
            if child_fallback_reason:
                child_trace["fallback_reason"] = child_fallback_reason
            child_debug.append(child_trace)
        for source in child.get("sources") or []:
            if isinstance(source, dict):
                sources.append({**source, "scope": label})
    if len(selected_rows) > 1:
        answer_lines.append(
            "如果你需要先买一类，可以先按当前主场景选择上面的主推范围，另一类作为备选，后续再补齐。"
        )
    if any(label == "烤盘" for label, _ in scopes):
        answer_lines.append("烤盘使用时还需要配合稳定的炉具，先后顺序按你的主要烹饪场景决定。")
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": "\n".join(answer_lines),
        "results": selected_rows,
        "result_skus": [
            str(row.get("sku") or "").strip().upper()
            for row in selected_rows
            if str(row.get("sku") or "").strip()
        ],
        "candidate_skus": [
            str(row.get("sku") or "").strip().upper()
            for row in selected_rows
            if str(row.get("sku") or "").strip()
        ],
        "sources": sources,
        "steps": [{
            "type": "recommendation",
            "label": "分别核对每个产品范围",
            "detail": "；".join(f"{scope}：独立检索并核验" for scope, _ in scopes),
            "ok": True,
        }],
        "answer_metadata": {
            "source": "multi_category_recommendation_contract",
            "evidence_status": "matched" if selected_rows else "not_found",
            "recommendation_scopes": [scope for scope, _ in scopes],
            "recommendation_scope_skus": recommendation_scope_skus,
        },
        "debug": {
            "agent_mode": "multi_category_recommendation_contract",
            "scopes": child_debug,
        },
        "skip_polish": True,
    }


def _structured_cookware_multi_condition_recommendation_result(db: Session, question: str) -> dict | None:
    text = str(question or "").strip()
    if not _looks_like_multi_condition_cookware_recommendation(text):
        return None
    rows = [row for row in _phase1_catalog_rows(db, "产品") if _is_service_pot_or_cookware_set_candidate(row)]
    if not rows:
        return None
    verification_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(text)
    verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(
        verification_contract,
        rows,
    )
    verified_rows = customer_recommendation_verification_contract.select_recommendation_candidates(
        rows,
        verifications,
    )
    if not verified_rows:
        # Keep evidence-backed partial candidates visible when no SKU proves
        # every requested dimension. The answer builder already marks these
        # rows as reference-only; dropping their cards makes the API disagree
        # with its own bounded recommendation explanation.
        partially_verified_skus = {
            item.sku
            for item in verifications
            if item.verification_level == "partially_verified"
        }
        verified_rows = [
            row
            for row in rows
            if str(row.get("sku") or "").strip().upper() in partially_verified_skus
        ]
    rejected_candidates = [item.to_dict() for item in verifications if item.verification_level == "rejected"]
    wants_group_capacity = any(term in text for term in ("4人以上", "4人", "多人", "家庭", "大容量", "容量大"))
    wants_gas = any(term in text for term in ("燃气炉", "卡式炉", "明火"))
    wants_light = any(term in text for term in ("别太重", "轻", "轻量", "轻便"))
    wants_beginner = "新手" in text
    wants_budget = any(term in text for term in ("价格别太高", "别太贵", "不要太贵", "预算不高", "预算有限", "便宜点", "性价比"))

    def _rank(row: dict[str, Any]) -> tuple[int, str]:
        row_text = " ".join(
            str(row.get(key) or "")
            for key in (
                "product_name_cn",
                "category",
                "capacity",
                "features",
                "usage_scenarios",
                "positioning",
                "price_positioning",
                "long_description_cn",
            )
        )
        score = 0
        if wants_group_capacity and any(term in row_text for term in ("4人", "2-4人", "多人", "家庭", "3L", "3700ML", "大容量")):
            score += 10
        if wants_gas and any(term in row_text for term in ("燃气炉", "卡式炉", "明火")):
            score += 8
        if wants_light and any(term in row_text for term in ("轻量", "轻便", "便携", "套娃", "收纳")):
            score += 6
        if wants_beginner and any(term in row_text for term in ("入门", "基础", "性价比", "周末野餐")):
            score += 5
        if wants_budget and any(term in row_text for term in ("入门", "性价比", "基础", "中端")):
            score += 4
        if any(term in row_text for term in ("套锅", "套装", "锅具套装", "炊具套装")):
            score += 3
        weight = row.get("gross_weight_g")
        try:
            weight_value = float(weight or 0)
        except (TypeError, ValueError):
            weight_value = 0
        if wants_light:
            if 0 < weight_value <= 800:
                score += 4
            elif 800 < weight_value <= 1200:
                score += 2
            elif weight_value >= 1800:
                score -= 3
        return (-score, str(row.get("sku") or ""))

    selected = sorted(verified_rows, key=_rank)[:5]
    if not selected:
        answer = customer_recommendation_verification_contract.build_verified_recommendation_answer(
            verification_contract,
            [],
            verifications,
        )
    else:
        answer = customer_recommendation_verification_contract.build_verified_recommendation_answer(
            verification_contract,
            selected,
            verifications,
        )
    skus = [str(row.get("sku") or "").strip().upper() for row in selected if row.get("sku")]
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "debug": {
            "agent_mode": "planner_recommendation_guard_rebuild",
            "initial_candidate_skus": [
                str(row.get("sku") or "").strip().upper()
                for row in rows
                if str(row.get("sku") or "").strip()
            ],
            "verified_candidate_skus": skus,
            "candidate_verifications": [
                item.to_dict()
                for item in verifications
                if item.sku in skus
            ],
            "fully_verified_candidates": [item.to_dict() for item in verifications if item.verification_level == "fully_verified"],
            "partially_verified_candidates": [item.to_dict() for item in verifications if item.verification_level == "partially_verified"],
            "rejected_candidates": rejected_candidates,
        },
        "answer_metadata": {
            "source": "product_catalog_structured_recommendation",
            "preserved_scope_statement": "本次优先从锅具/套锅里选，不先把水壶当主推。",
            "recommendation_contract": verification_contract.to_dict(),
            "verification_level": (
                "fully_verified"
                if any(item.verification_level == "fully_verified" and item.sku in skus for item in verifications)
                else "partially_verified" if skus else "rejected"
            ),
            "fully_verified_count": sum(item.verification_level == "fully_verified" for item in verifications),
            "partially_verified_count": sum(item.verification_level == "partially_verified" for item in verifications),
            "rejected_count": sum(item.verification_level == "rejected" for item in verifications),
        },
        "skip_polish": True,
    }


def _semantic_structured_query_result(
    db: Session,
    question: str,
    *,
    candidate_context: dict[str, Any] | None = None,
    semantic_preplan: dict[str, Any] | None = None,
) -> dict | None:
    text = str(question or "").strip()
    if not text:
        return None
    # Internal catalogue predicates such as owner/lifecycle are parsed by the
    # deterministic query contract. This semantic category adapter only
    # understands public catalogue scope and would otherwise widen
    # "负责人 Max 的锅具" to every cookware row.
    if _is_internal_catalogue_filter_query(text):
        return None
    # A high-confidence semantic product-QA decision owns the whole-turn
    # meaning.  This catalogue executor is only a fallback for genuine
    # category/filter/recommendation requests, so it must yield before any
    # deterministic cookware or category heuristic can reinterpret the
    # named product as a set of alternatives. Entity and same-SKU evidence
    # validation continue in the later sealed product-QA path.
    if _semantic_product_qa_preempts_legacy_shortcuts(semantic_preplan):
        return None
    if _should_keep_preruntime_for_scoped_heat_source_followup(
        text,
        candidate_context=candidate_context,
    ):
        return None
    if _has_resolved_entity_contents_accessories_question(db, text):
        return None
    # A catalogue route must not consume a question for which the current
    # utterance already has an exact, sealable same-SKU QA fact.  The QA
    # helper performs entity arbitration itself; returning here only lets the
    # later canonical QA contract own the response instead of converting the
    # product name into a category filter.
    exact_qa_result = _try_product_qa_shortcut(db, text)
    if exact_qa_result is not None:
        return exact_qa_result
    # A generic catalogue recommendation cannot consume a product-like
    # subject that EntityResolutionContract cannot bind.  Otherwise an
    # unrecognised name followed by “推荐” silently becomes a recommendation
    # for a different catalogue product.
    if _has_unresolved_product_like_scope(db, text):
        return None
    cookware_multi_condition = _structured_cookware_multi_condition_recommendation_result(db, text)
    if cookware_multi_condition:
        return cookware_multi_condition
    semantic_subject = str((semantic_preplan or {}).get("subject_text") or "").strip()
    all_catalog_rows = _phase1_catalog_rows(db, "产品")
    category_ref = _database_category_scope_ref(all_catalog_rows, semantic_subject)
    semantic_scope_rows = (
        _database_product_family_scope_rows(all_catalog_rows, semantic_subject)
        if semantic_subject and not category_ref
        else []
    )
    if (
        len(semantic_scope_rows) <= 1
        and str((semantic_preplan or {}).get("field_type") or "").strip() == "series"
    ):
        semantic_scope_rows = _database_question_product_family_scope_rows(
            all_catalog_rows,
            text,
        )
    if (
        _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and _semantic_preplan_route_family(semantic_preplan) in {"structured_query", "recommendation"}
        and semantic_subject
        and not category_ref
        and len(semantic_scope_rows) > 1
        and not customer_agent_service._extract_skus(text)
    ):
        return _structured_product_query_result(
            question=text,
            product_ref=semantic_subject,
            rows=semantic_scope_rows,
            source="semantic_catalog_family_scope",
            reason_label="已验证商品族范围",
            answer_type="query_products",
            metadata_source="product_catalog_family_query",
        )
    # A structured catalogue query never owns the field meaning of an already
    # resolved product. A loose name recall is not enough here: a generic
    # attribute phrase such as "轻量锅具" may overlap an unrelated product name
    # (for example "轻量保温杯"). Only a sealed EntityResolutionContract may
    # defer this catalogue query to product detail.
    if _has_resolved_named_product_contract(db, text):
        return None
    # For a high-confidence catalogue request, semantic planning has already
    # interpreted the whole utterance and supplied only a generic catalogue
    # scope.  Resolve that scope through the database search provider instead
    # of requiring a second lexical recognizer to rediscover the same term.
    # The plan cannot supply products, SKUs, or facts: all candidates remain
    # bounded by the actual database result set.
    semantic_fields = list((semantic_preplan or {}).get("canonical_fields") or [])
    if (
        _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and _semantic_preplan_route_family(semantic_preplan) == "structured_query"
        and semantic_fields == ["category"]
        and semantic_subject
    ):
        # Semantic planning owns the meaning "this is a catalogue category
        # request"; this adapter only resolves that abstract subject against
        # the categories actually present in the database.  A lexical product
        # search is deliberately not an acceptable substitute: "锅" may
        # occur in a kettle or stove name while the customer asks for the
        # database's cookware category.
        category_rows = _phase1_catalog_rows(db, "产品")
        category_ref = _database_category_scope_ref(category_rows, semantic_subject)
        if category_ref:
            if any(token in text for token in ("多少", "几款", "几种", "几个")):
                return _phase1_catalog_count_result(
                    db,
                    {"product_ref": category_ref, "raw_question": text},
                )
            rows = _phase1_catalog_rows(db, category_ref)
            return _structured_product_query_result(
                question=text,
                product_ref=category_ref,
                rows=rows,
                source="structured_catalog_semantic_category_scope",
                reason_label="语义目录范围",
                answer_type="query_products",
                metadata_source="product_catalog_structured_query",
            )
        rows = customer_agent_service.search_products(db, semantic_subject, limit=50)
        return _structured_product_query_result(
            question=text,
            product_ref=semantic_subject,
            rows=rows,
            source="structured_catalog_count",
            reason_label="语义目录范围",
            answer_type="query_products",
            metadata_source="product_catalog_structured_query",
        )
    structured_field_filter = _structured_field_filter_result(db, text)
    if structured_field_filter:
        return structured_field_filter
    if any(term in text for term in ("咖啡器具", "咖啡")) and "手冲" in text:
        rows = [row for row in _phase1_catalog_rows(db, "咖啡器具") if any(term in _structured_query_row_text(row) for term in ("手冲", "细口壶", "咖啡", "滤杯"))]
        return _structured_product_query_result(
            question=text,
            product_ref="咖啡器具",
            rows=rows,
            source="structured_coffee_handpour_query",
            reason_label="手冲 / 咖啡器具语义",
        )
    is_waterware_selection_request = any(term in text for term in ("怎么选", "推荐", "想买", "买个", "买一", "选哪", "哪款", "哪种"))
    if (
        not is_waterware_selection_request
        and any(term in text for term in ("水具", "水壶", "水杯"))
        and any(term in text for term in ("冷水", "补水", "饮水", "热水"))
    ):
        product_ref = "水壶" if "水壶" in text else "水具"
        candidate_predicate = _phase1_is_strict_water_kettle_candidate
        rows = [row for row in _phase1_catalog_rows(db, "产品") if candidate_predicate(row)]
        filtered: list[dict[str, Any]] = []
        for row in rows:
            row_text = _structured_query_row_text(row)
            score = 0
            if any(term in row_text for term in ("补水", "饮水", "冷水", "水杯", "随行杯", "保温杯", "便携")):
                score += 6
            if any(term in row_text for term in ("热水", "保温")):
                score += 3
            if any(term in row_text for term in ("烧水", "煮水", "泡茶")):
                score -= 4
            if score > 0:
                new_row = dict(row)
                new_row["_score"] = score
                filtered.append(new_row)
        filtered.sort(key=lambda item: (-int(item.get("_score") or 0), str(item.get("sku") or "")))
        return _structured_product_query_result(
            question=text,
            product_ref=product_ref,
            rows=filtered,
            source="structured_waterware_capability_query",
            reason_label=f"{product_ref} / 冷水 / 热水 / 补水语义",
        )
    is_griddle_selection_request = any(term in text for term in ("怎么搭", "怎么配", "先买", "哪个", "推荐", "更合适", "更值得"))
    if (
        not is_griddle_selection_request
        and any(term in text for term in ("烤盘", "煎盘"))
        and any(term in text for term in ("炉具", "炉子", "卡式炉", "分体炉", "露营炉", "烧烤"))
    ):
        rows = []
        for row in _phase1_catalog_rows(db, "产品"):
            row_text = _structured_query_row_text(row)
            if not any(term in row_text for term in ("烤盘", "煎盘")):
                continue
            score = 0
            if any(term in row_text for term in ("炉具", "卡式炉", "分体炉", "一体炉", "烧烤", "明火")):
                score += 8
            if any(term in row_text for term in ("早餐", "煎烤", "烤盘")):
                score += 3
            if score <= 0:
                continue
            new_row = dict(row)
            new_row["_score"] = score
            rows.append(new_row)
        rows.sort(key=lambda item: (-int(item.get("_score") or 0), str(item.get("sku") or "")))
        return _structured_product_query_result(
            question=text,
            product_ref="烤盘",
            rows=rows,
            source="structured_griddle_stove_query",
            reason_label="烤盘 + 炉具搭配语义",
        )
    if (
        any(term in text for term in ("配件", "附件"))
        and any(term in text for term in ("收纳", "整理", "归纳"))
        and any(term in text for term in ("有哪些", "推荐", "适合", "更偏"))
    ):
        rows = []
        for row in _phase1_catalog_rows(db, "配件"):
            row_text = _structured_query_row_text(row)
            score = 0
            if any(term in row_text for term in ("收纳", "整理", "归纳", "好收纳", "收纳包", "收纳方便", "集中收纳")):
                score += 8
            if any(term in row_text for term in ("配件", "附件", "餐厨配件", "露营收纳")):
                score += 4
            if any(term in row_text for term in ("调料瓶", "喷枪", "水壶", "水具")):
                score -= 5
            if score <= 0:
                continue
            new_row = dict(row)
            new_row["_score"] = score
            rows.append(new_row)
        rows.sort(key=lambda item: (-int(item.get("_score") or 0), str(item.get("sku") or "")))
        return _structured_product_query_result(
            question=text,
            product_ref="配件",
            rows=rows,
            source="structured_accessory_storage_query",
            reason_label="配件 / 收纳语义",
        )
    if (
        not _has_resolved_entity_contents_accessories_question(db, text)
        and any(term in text for term in ("配件", "附件"))
        and any(term in text for term in ("有什么", "有啥"))
    ):
        return _phase1_catalog_count_result(db, {"product_ref": "配件", "raw_question": text})
    if _looks_like_semantic_catalog_query(text):
        # Preserve the semantic input for the aggregate contract. Product
        # discovery and a catalogue count have different population rules.
        return _phase1_catalog_count_result(
            db,
            {"product_ref": _semantic_catalog_product_ref(text), "raw_question": text},
        )
    # An unresolved product-like subject must reach central entity arbitration;
    # this legacy fallback has no EntityResolutionContract and therefore cannot
    # safely turn a product-specific unknown field into a generic answer.
    if _has_unresolved_product_like_scope(db, text):
        return None
    return _unknown_product_fact_result(text)


def _semantic_prefers_sealed_product_qa(phase1_plan: dict[str, Any]) -> bool:
    """Reserve generic product facts for the sealed same-SKU QA path.

    The semantic preplan owns the meaning of an utterance.  This guard only
    recognizes its schema-validated decision that a named product asks for a
    product-specific fact outside the formal field taxonomy; it never derives
    a field, product, or answer from wording.  Entity resolution and exact
    same-SKU QA matching remain mandatory in ``_try_product_qa_shortcut``.
    """
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    preplan = plan.get("semantic_preplan") if isinstance(plan.get("semantic_preplan"), dict) else {}
    if not _semantic_preplan_confident(preplan, minimum=0.9):
        return False
    if str(preplan.get("route_family") or "").strip() != "product_bound_qa":
        return False
    if str(preplan.get("evidence_kind") or "").strip() != "product_qa":
        return False
    if preplan.get("ambiguity") or (preplan.get("canonical_fields") or []):
        return False
    if str(preplan.get("field_type") or preplan.get("field_hint") or "").strip():
        return False
    # The family itself is schema-validated.  Providers may omit the legacy
    # route_hint compatibility mirror, so it must not reopen an old usage/care
    # shortcut after the model has explicitly selected sealed product QA.
    return str(preplan.get("route_hint") or "").strip() in {"", "product_detail"}


def _semantic_allows_exact_sealed_product_qa(phase1_plan: dict[str, Any]) -> bool:
    """Accept an exact QA only for the semantic product-detail family."""
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    preplan = plan.get("semantic_preplan") if isinstance(plan.get("semantic_preplan"), dict) else {}
    return bool(
        _semantic_preplan_confident(preplan, minimum=0.9)
        and str(preplan.get("route_family") or "").strip() == "product_bound_qa"
        and str(preplan.get("route_hint") or "").strip() == "product_detail"
        and not preplan.get("ambiguity")
    )


def _semantic_formal_field_preempts_legacy_shortcuts(preplan: dict[str, Any] | None) -> bool:
    """Reserve a validated semantic field for central contract arbitration.

    This is deliberately only a shortcut veto.  It neither supplies an
    entity nor authorizes evidence: FieldContract validation and
    EntityResolutionContract still decide whether the request can be answered
    or must be clarified.  Its purpose is to prevent a legacy usage/care or
    contents heuristic from replacing a high-confidence, allowlisted semantic
    field before those central contracts have run.
    """
    value = preplan if isinstance(preplan, dict) else {}
    if not _semantic_preplan_confident(value, minimum=0.9):
        return False
    if value.get("fallback_reason") or value.get("ambiguity"):
        return False
    fields = {
        customer_field_contract.semantic_preplan_field_type(item)
        for item in [
            value.get("field_type"),
            value.get("field_hint"),
            *(value.get("canonical_fields") or []),
        ]
    }
    fields.discard("")
    return bool(fields) and fields.issubset(customer_field_contract.FORMAL_DETAIL_FIELDS)


def _semantic_product_qa_preempts_legacy_shortcuts(preplan: dict[str, Any] | None) -> bool:
    """Keep an explicit semantic QA decision ahead of legacy shortcut routers.

    This is another veto only. It has no lexical interpretation and does not
    choose a QA record; it preserves the model's schema-validated decision so
    the later same-SKU EntityResolution/QA contract can run or fail closed.
    """
    value = preplan if isinstance(preplan, dict) else {}
    return bool(
        _semantic_preplan_confident(value, minimum=0.9)
        and not value.get("fallback_reason")
        and not value.get("ambiguity")
        and str(value.get("route_family") or "").strip() == "product_bound_qa"
        and str(value.get("evidence_kind") or "").strip() == "product_qa"
        and not (value.get("canonical_fields") or [])
        and not str(value.get("field_type") or value.get("field_hint") or "").strip()
    )


def _formal_field_contract_preempts_product_qa(question: str, phase1_plan: dict[str, Any] | None) -> bool:
    """Keep product QA as evidence, never the route for a formal field.

    The semantic preplan and the FieldContract own the field meaning.  A
    same-SKU QA record can still be consulted by a field-specific evidence
    provider, but it cannot replace the contract with a QA response before
    entity arbitration reaches the structured detail executor.
    """
    semantic_preplan = (
        phase1_plan.get("semantic_preplan")
        if isinstance(phase1_plan, dict) and isinstance(phase1_plan.get("semantic_preplan"), dict)
        else {}
    )
    field_request = customer_field_contract.resolve_requested_field_contract(
        question,
        phase1_plan if isinstance(phase1_plan, dict) else {},
    )
    field_type = str(field_request.get("field_type") or "").strip()
    if field_type in customer_field_contract.FORMAL_DETAIL_FIELDS:
        semantic_subject = str(semantic_preplan.get("subject_text") or "").strip()
        subject_start = str(question or "").find(semantic_subject) if semantic_subject else -1
        subject_end = subject_start + len(semantic_subject) if subject_start >= 0 else -1
        field_spans = [
            item
            for item in (field_request.get("field_spans") or [])
            if isinstance(item, dict)
            and isinstance(item.get("start"), int)
            and isinstance(item.get("end"), int)
        ]
        alias_is_inside_bound_subject = bool(
            subject_start >= 0
            and field_spans
            and all(
                subject_start <= int(item["start"])
                and int(item["end"]) <= subject_end
                for item in field_spans
            )
        )
        if (
            _semantic_product_qa_preempts_legacy_shortcuts(semantic_preplan)
            and alias_is_inside_bound_subject
        ):
            # A noun embedded in the already-bound product title is not the
            # requested predicate. Preserve the validated semantic QA route;
            # full-predicate FieldContracts (for example "拿着重不重" or
            # "应该怎么使用") still preempt as formal customer questions.
            return False
        return True
    return False


def _should_prioritize_semantic_structured_route(question: str, phase1_plan: dict[str, Any]) -> bool:
    text = str(question or "").strip()
    semantic_preplan = (
        phase1_plan.get("semantic_preplan")
        if isinstance(phase1_plan, dict) and isinstance(phase1_plan.get("semantic_preplan"), dict)
        else None
    )
    # Product navigation needs EntityResolutionContract arbitration. It must
    # never enter the generic structured catalogue executor simply because a
    # legacy planner also classified "有哪些款" as a count question.
    if _semantic_product_navigation_preempts_catalog_count(semantic_preplan):
        return False
    # ``category`` is catalogue scope, not a product-detail FieldContract.
    # A validated semantic catalogue plan must reach the DB-backed structured
    # executor before the generic formal-field shortcut veto is considered.
    if (
        _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and _semantic_preplan_route_family(semantic_preplan) == "structured_query"
        and list((semantic_preplan or {}).get("canonical_fields") or []) == ["category"]
        and str((semantic_preplan or {}).get("subject_text") or "").strip()
    ):
        return True
    # A legacy catalogue plan can retain a category-shaped reading from the
    # product name. It is a fallback only: a validated formal semantic field
    # must first reach the central FieldContract -> EntityResolutionContract
    # path, which will still distinguish a named product from a true category.
    if _semantic_formal_field_preempts_legacy_shortcuts(semantic_preplan):
        return False
    # This is an explicitly generic catalogue capability request; "水壶" is
    # its category scope, not an unresolved product-like name.  Preserve that
    # structured contract before the fail-closed single-entity guard.
    if _looks_like_semantic_waterware_capability_query(text):
        return True
    db = phase1_plan.get("_db") if isinstance(phase1_plan, dict) else None
    if db is not None and _has_unresolved_product_like_scope(db, text):
        return False
    # Recommendation grammar may occur after a product-like subject.  The
    # entity contract has to reject an unresolved named subject before the
    # generic catalogue recommender is allowed to construct any candidates.
    # This is scope arbitration, not a product or wording exception.
    if _looks_like_multi_condition_cookware_recommendation(text):
        return True
    if _looks_like_recommendation_request(text):
        return False
    if any(term in text for term in ("推荐", "哪款", "选什么", "帮我选", "帮我挑", "推荐一个", "推荐几个")):
        return False
    if _structured_hard_filter_contract(text):
        return True
    primary_intent = str((phase1_plan or {}).get("primary_intent") or "")
    if primary_intent not in {"product_field", "catalog_count"}:
        return False
    return _looks_like_structured_field_filter_query(text) or _looks_like_multi_condition_cookware_recommendation(text)


def _semantic_structured_override_plan(question: str, phase1_plan: dict[str, Any], result: dict[str, Any]) -> dict[str, Any]:
    merged = dict(phase1_plan or {})
    metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    answer_type = str(result.get("answer_type") or "")
    if answer_type in {"product_query", "query_products"}:
        merged["primary_intent"] = "catalog_count"
        merged["answer_type"] = answer_type
        merged["product_ref"] = str(metadata.get("product_ref") or _semantic_catalog_product_ref(question) or "").strip()
        merged["requested_field"] = ""
    elif answer_type == "recommendation":
        merged["primary_intent"] = "recommendation"
        merged["answer_type"] = "recommendation"
        merged["scenario"] = str(question or "").strip()
        merged["product_ref"] = ""
        merged["requested_field"] = ""
    return merged


def _phase1_explicit_sku_alcohol_people_result(db: Session, question: str) -> dict | None:
    text = str(question or "")
    if "酒精炉" not in text and "酒精" not in text:
        return None
    if not any(term in text for term in ("适合几个人", "适合几人", "几个人", "几人", "多少人", "人数")):
        return None
    sku = _resolve_sku(db, text, None)
    if not sku:
        return None
    product, specs, business, content = _phase1_product_bundle_by_ref(db, sku)
    if not product:
        return None
    row = _product_row_from_model(product, specs, business, content)
    canonical_fields = [
        field
        for field in customer_field_contract.detect_field_types(text)
        if customer_field_contract.is_supported_detail_field(field)
    ]
    canonical_fields = list(dict.fromkeys(canonical_fields))
    if not {"heat_source", "people"}.issubset(set(canonical_fields)):
        return None
    base_field_contract = resolve_requested_field_contract(text, None)
    field_contract = dict(base_field_contract)
    field_contract.update({
        "field_type": "heat_source",
        "requested_field": "heat_source",
        "requested_fields": canonical_fields,
        "canonical_fields": canonical_fields,
        "supported_fields": canonical_fields,
        "compound": True,
    })
    all_products = db.query(Product).order_by(Product.sku.asc()).all()
    entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        text,
        all_products,
        resolver_candidates=[product],
        entity_text_override=str(product.sku or "").strip(),
        field_type_override="heat_source",
    )
    if not customer_entity_resolution_contract.can_resolve_single_product(entity_contract, all_products).allowed:
        return None
    compatibility_answer, evidence_status = _phase1_alcohol_stove_compatibility_answer(db, row)
    people_metadata: dict[str, Any] = {}
    people_answer = _phase1_people_capacity_answer(row, evidence_metadata=people_metadata)
    answer = f"{compatibility_answer} {people_answer}"
    sku_code = str(product.sku or "").strip().upper()
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "sku": sku_code,
        "answer": answer,
        "results": [row],
        "result_skus": [sku_code],
        "candidate_skus": [sku_code],
        "answer_metadata": {
            "answer_policy": "explicit_sku_compatibility_and_people",
            "requested_fields": canonical_fields,
            "evidence_status": evidence_status,
        },
        "evidence": [
            {
                "type": "product_field",
                "sku": sku_code,
                "field": "heat_source",
                "source": "specs.heat_source",
                "value": str(row.get("heat_source") or "").strip(),
            },
            {
                "type": "product_field",
                "sku": sku_code,
                "field": "people",
                "source": people_metadata.get("evidence_source"),
                "value": people_metadata.get("evidence_value"),
            },
        ],
        "debug": {
            "agent_mode": "explicit_sku_compatibility_people",
            "raw_results": [row],
            "candidate_skus": [sku_code],
            "field_contract": field_contract,
            "entity_resolution_contract": entity_contract.to_dict(),
        },
        "skip_polish": True,
    }


def _explicit_sku_detail_requested_fields(question: str) -> list[str]:
    text = str(question or "").strip()
    if not text:
        return []
    fields = list(customer_agent_intent_service._requested_fields_for_detail_question(text))
    if any(term in text for term in ("酒精炉", "明火", "明火直烧", "热源", "燃料", "火源", "能用", "能不能用", "可以用", "直接加热", "卡式炉", "燃气炉", "气炉", "分体炉", "一体炉")) and "热源" not in fields:
        fields.append("热源")
    people_terms = ("适合几个人", "适合几人", "几个人", "几人用", "几人使用", "多少人", "人数", "适用人数")
    if any(term in text for term in people_terms) and "适用人数" not in fields:
        fields.append("适用人数")
    if (
        any(term in text for term in ("适合", "场景", "干嘛用", "露营用", "适用人群"))
        and not any(term in text for term in people_terms)
        and "适用场景" not in fields
    ):
        fields.append("适用场景")
    return fields


def _merge_explicit_detail_fields_into_request(
    question: str,
    field_request: dict[str, Any] | None,
) -> dict[str, Any]:
    """Keep every explicit formal field on an ordinal context follow-up."""
    request = dict(field_request or {})
    labels = _explicit_sku_detail_requested_fields(question)
    if len(labels) < 2:
        return request
    deterministic_fields = list(dict.fromkeys(
        field_type
        for field_type in (
            customer_field_contract.field_type_from_detail_label(label)
            for label in labels
        )
        if field_type in customer_field_contract.FORMAL_DETAIL_FIELDS
    ))
    if (
        "accessories" in deterministic_fields
        and not customer_agent_service.looks_like_contents_accessories_question(str(question or ""))
    ):
        # Component names in a size/capacity question identify measurements;
        # they do not by themselves ask what is included in the package.
        deterministic_fields = [field for field in deterministic_fields if field != "accessories"]
    # The validated semantic contract may cover explicit concepts for which
    # the older label parser has no complete alias set (for example compatible
    # heat sources, or cleaning plus care/storage). Preserve that set intact.
    semantic_fields = [
        str(field or "").strip()
        for field in (request.get("canonical_fields") or [])
        if str(field or "").strip() in customer_field_contract.FORMAL_DETAIL_FIELDS
    ]
    semantic_contract_active = bool(
        request.get("source") == "validated_semantic_preplan"
        and semantic_fields
    )
    span_fields = [
        str(item.get("field_type") or "").strip()
        for item in (request.get("field_spans") or [])
        if isinstance(item, dict)
        and str(item.get("field_type") or "").strip() in customer_field_contract.FORMAL_DETAIL_FIELDS
    ]
    literal_label_fields = [
        customer_field_contract.field_type_from_detail_label(label)
        for label in labels
        if str(label or "").strip()
        and str(label or "").strip() in str(question or "")
    ]
    trusted_deterministic_fields = list(dict.fromkeys([
        *semantic_fields,
        *span_fields,
        *[
            field
            for field in literal_label_fields
            if field in customer_field_contract.FORMAL_DETAIL_FIELDS
        ],
    ]))
    if "price_positioning" in trusted_deterministic_fields:
        # "Price positioning/tier" is one formal business field.  The
        # contained word "price" must not add a realtime-price field.
        trusted_deterministic_fields = [
            field for field in trusted_deterministic_fields if field != "price"
        ]
    # A validated semantic FieldContract owns the complete predicate meaning.
    # Re-adding legacy labels here can mistake words inside a product name for
    # a second field, or turn "price positioning" into a realtime-price ask.
    # The deterministic parser remains the fallback only when no validated
    # semantic field set exists.
    canonical_fields = list(dict.fromkeys(
        semantic_fields
        if semantic_contract_active
        else trusted_deterministic_fields or deterministic_fields
    ))
    if len(canonical_fields) < 2:
        return request
    if semantic_contract_active or trusted_deterministic_fields:
        labels = [
            label
            for label in labels
            if customer_field_contract.field_type_from_detail_label(label) in set(canonical_fields)
        ]
    requested_fields = list(dict.fromkeys(
        [
            str(label or "").strip()
            for label in (request.get("requested_fields") or [])
            if str(label or "").strip()
        ]
        + labels
    ))
    represented_field_types = {
        customer_field_contract.field_type_from_detail_label(label)
        for label in requested_fields
    }
    for field in canonical_fields:
        display_label = customer_field_contract.product_detail_field_label(field)
        if display_label and field not in represented_field_types:
            requested_fields.append(display_label)
            represented_field_types.add(field)
    supported_fields = [
        field
        for field in canonical_fields
        if customer_field_contract.is_supported_detail_field(field)
    ]
    return {
        **request,
        "field_type": None,
        "requested_field": labels[0],
        "requested_fields": requested_fields,
        "canonical_fields": canonical_fields,
        "supported_fields": supported_fields,
        "unsupported_fields": [field for field in canonical_fields if field not in supported_fields],
        "compound": True,
        "confidence": max(float(request.get("confidence") or 0.0), 1.0),
        "source": (
            "validated_semantic_preplan"
            if request.get("source") == "validated_semantic_preplan"
            else "deterministic_explicit_multi_field"
        ),
    }


def _is_explicit_sku_detail_question(question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    if customer_agent_intent_service._is_compare_question(text):
        return False
    if customer_agent_intent_service._looks_like_multi_product_relation_question(text):
        return False
    # “有哪些” can be the predicate of a field request (for example
    # “CW-C95 支持哪些燃料”).  The explicit SKU plus the formal field parser
    # already keeps that request single-product, so only true catalogue or
    # recommendation wording should bypass this structured detail shortcut.
    if any(term in text for term in ("列出", "推荐哪套", "推荐哪个", "推荐哪款", "怎么选", "先买哪个")):
        return False
    fields = _explicit_sku_detail_requested_fields(text)
    if fields:
        return True
    return any(term in text for term in ("是什么", "是什么产品", "介绍一下", "适合什么场景", "适合露营", "适合几个人", "干嘛用", "能不能用明火", "能不能用酒精炉", "直接加热", "明火直烧", "卡式炉", "燃气炉", "分体炉", "一体炉"))


def _is_explicit_sku_heat_source_question(question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    return any(
        term in text
        for term in (
            "酒精炉",
            "酒精",
            "直接加热",
            "明火加热",
            "上火加热",
            "明火直烧",
            "热源",
            "燃料",
            "卡式炉",
            "燃气炉",
            "气炉",
            "分体炉",
            "一体炉",
        )
    )


def _is_explicit_sku_identity_only_question(question: str) -> bool:
    """Recognize an exact-SKU identity request without assigning any field.

    This is deliberately an outage-only availability predicate.  It does not
    resolve a SKU, infer a product, or produce an answer; the downstream
    EntityResolutionContract remains the sole authority for all three.
    """
    text = str(question or "").strip()
    explicit_skus = customer_agent_service._extract_skus(text)
    if not text or not explicit_skus:
        return False
    for reference in sorted({str(value or "") for value in explicit_skus if str(value or "")}, key=len, reverse=True):
        remainder = re.sub(
            rf"^\s*{re.escape(reference)}",
            "",
            text,
            count=1,
            flags=re.IGNORECASE,
        ).strip(" 。！？!?，,；;：:")
        if remainder in {"是什么", "是什么产品", "介绍一下"}:
            return True
    return False


async def _try_explicit_sku_detail_shortcut(db: Session, question: str) -> dict | None:
    text = str(question or "").strip()
    explicit_skus = customer_agent_service._extract_skus(text)
    if not text or not explicit_skus:
        return None
    if customer_agent_intent_service._looks_like_contents_grounding_question(text):
        return None
    if _resolved_entity_realtime_commercial_field_guard_label(text):
        return None
    if not _is_explicit_sku_detail_question(text):
        return None
    resolved_candidates: list[str] = []
    for candidate in explicit_skus:
        resolved_candidate = _resolve_exact_sku_variant(db, candidate)
        if resolved_candidate and resolved_candidate not in resolved_candidates:
            resolved_candidates.append(resolved_candidate)
    # This shortcut is deliberately single-entity only. When the current
    # utterance names multiple canonical SKUs, selecting the first one would
    # discard a participant before the comparison contract can seal both.
    if len(resolved_candidates) > 1:
        return None
    resolved_sku = resolved_candidates[0] if len(resolved_candidates) == 1 else _resolve_sku(db, text, None)
    if not resolved_sku:
        return None
    requested_fields = _explicit_sku_detail_requested_fields(text)
    identity_references = sorted(
        {str(reference or "") for reference in [*explicit_skus, resolved_sku] if reference},
        key=len,
        reverse=True,
    )
    identity_overview_request = False
    for reference in identity_references:
        stripped_query = re.sub(
            rf"^\s*{re.escape(reference)}",
            "",
            text,
            count=1,
            flags=re.IGNORECASE,
        )
        identity_query = stripped_query.strip(" 。！？!?，,；;：:")
        if identity_query in {"是什么", "是什么产品", "介绍一下"}:
            identity_overview_request = True
            break
    if identity_overview_request:
        product = db.query(Product).filter(Product.sku == resolved_sku).first()
        detail = product_service.get_product_detail(db, resolved_sku) if product else None
        if isinstance(detail, dict) and detail:
            result = _named_product_context_result(text, detail, identity_overview=True)
            all_products = db.query(Product).order_by(Product.sku.asc()).all()
            entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
                text,
                all_products,
                resolver_candidates=[product],
                entity_text_override=resolved_sku,
            )
            debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
            debug.update({
                "agent_mode": "explicit_sku_context_shortcut",
                "entity_resolution_contract": entity_contract.to_dict(),
            })
            result["debug"] = debug
            result["sku"] = resolved_sku
            result["result_skus"] = [resolved_sku]
            result["candidate_skus"] = [resolved_sku]
            return result
    if (
        requested_fields
        and set(requested_fields).issubset({"热源", "适配情况"})
        and _is_explicit_sku_heat_source_question(text)
    ):
        return _phase1_product_field_result(
            db,
            {
                "primary_intent": "product_field",
                "product_ref": resolved_sku,
                "product_identity_source": "explicit_sku_exact",
                "requested_field": "heat_source",
                "raw_question": text,
                "sku": resolved_sku,
            },
        )
    intent = customer_agent_intent_service.CustomerIntent(
        intent="product_detail",
        target_skus=[resolved_sku],
        requested_fields=requested_fields,
        semantic_query=text,
        term=resolved_sku,
        source_context="question",
        is_single_field_sufficient=customer_agent_intent_service._is_single_field_sufficient(text, requested_fields, [resolved_sku]),
    )
    result = await customer_agent_intent_service._product_detail_result(
        db,
        intent,
        original_question=text,
    )
    product = db.query(Product).filter(Product.sku == resolved_sku).first()
    if product and any(term in text for term in ("酒精炉", "酒精")) and any(
        term in text for term in ("能用", "可以用", "支持", "适合", "能不能", "是否支持")
    ):
        bundle = _phase1_product_bundle_by_ref(db, resolved_sku)
        if bundle[0]:
            row = _product_row_from_model(*bundle)
            compatibility_answer, evidence_status = _phase1_alcohol_stove_compatibility_answer(db, row)
            if evidence_status in {"supported", "unsupported", "not_listed"}:
                result["answer"] = compatibility_answer
                answer_meta = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
                answer_meta["requested_field"] = "heat_source"
                answer_meta["evidence_status"] = evidence_status
                result["answer_metadata"] = answer_meta
    if (
        product
        and any(term in text for term in ("酒精炉", "酒精"))
        and any(term in text for term in ("能用", "可以用", "支持", "适合", "能不能", "是否支持"))
        and str(getattr(product, "category", "") or "").strip() not in {"锅具", "炉具", "水壶", "水具", "餐具", "酒具"}
        and not _result_has_positive_capability_evidence(result, text)
    ):
        detail = product_service.get_product_detail(db, resolved_sku)
        title = detail.get("product_name_cn") or detail.get("product_name_en") or resolved_sku
        usage = str((detail.get("business") or {}).get("usage_scenarios") or "").strip()
        conservative = f"{title}（{resolved_sku}）"
        if usage and "适合什么场景" in text:
            conservative += f"：使用场景：{usage}；"
        else:
            conservative += "："
        conservative += f"它属于{product.category}类产品，不是炉具或炊具；当前资料未标注适用酒精炉，不建议按酒精炉适配产品理解。"
        result["answer"] = conservative
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    requested_field = ""
    if "热源" in requested_fields:
        requested_field = "heat_source"
    elif requested_fields:
        requested_field = requested_fields[0]
    debug["agent_mode"] = "explicit_sku_detail_shortcut"
    debug["plan"] = {
        "primary_intent": "product_field",
        "product_ref": resolved_sku,
        "requested_field": requested_field,
        "raw_question": text,
    }
    result["debug"] = debug
    result["sku"] = resolved_sku
    result["result_skus"] = [resolved_sku]
    result["candidate_skus"] = [resolved_sku]
    return result


def _phase1_people_capacity_answer(
    row: dict[str, Any],
    *,
    evidence_metadata: dict[str, Any] | None = None,
) -> str:
    name = str(row.get("product_name_cn") or row.get("product_name_en") or row.get("sku") or "该产品").strip()
    audience = str(row.get("target_audience") or "").strip()
    people_evidence = _people_count_field_evidence(audience)
    if people_evidence:
        if evidence_metadata is not None:
            evidence_metadata.update({
                "evidence_value": people_evidence,
                "evidence_source": "business.target_audience",
                "evidence_scope": "subject",
            })
        sku = str(row.get("sku") or "").strip().upper()
        identity = f"{name}（{sku}）" if sku else name
        return f"{identity}的适合人群资料明确标注“{people_evidence}”，适用人数可按{people_evidence}理解。"
    if evidence_metadata is not None:
        evidence_metadata.clear()
    return f"当前产品资料中未维护{name}的明确适用人数，无法确认适合几个人。"


def _phase1_except_alcohol_stove_cookware_result(db: Session, question: str) -> dict | None:
    text = str(question or "")
    if not any(term in text for term in ("除了", "除开", "除去")):
        return None
    if "酒精炉" not in text and "酒精" not in text:
        return None
    if not any(term in text for term in ("锅", "锅具", "套锅", "单锅", "炊具")):
        return None
    excluded_skus = {
        str(item or "").strip().upper()
        for item in customer_agent_service._extract_skus(text)
        if str(item or "").strip()
    }
    if not excluded_skus:
        return None
    rows = [
        row for row in _phase1_catalog_rows(db, "锅具")
        if str(row.get("sku") or "").strip().upper() not in excluded_skus
        and _is_service_pot_or_cookware_set_candidate(row)
        and _phase1_row_has_explicit_alcohol_stove_support(db, row)
    ]
    skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
    excluded_label = "、".join(sorted(excluded_skus))
    if rows:
        lines = [
            f"除了 {excluded_label}，当前资料还明确找到以下支持酒精炉的锅具：",
        ]
        for row in rows[:10]:
            name = row.get("product_name_cn") or row.get("name") or row.get("sku")
            sku = str(row.get("sku") or "").strip().upper()
            heat_source = str(row.get("heat_source") or "").strip()
            lines.append(f"- {name}（{sku}），明确证据：{heat_source or '同SKU资料/问答明确显示支持酒精炉'}")
    else:
        lines = [
            f"按当前资料，只明确找到 {excluded_label} 支持酒精炉；除了它，暂未找到其他明确标注支持酒精炉的锅具。",
            "仅支持明火、卡式炉、分体炉或一体炉的锅具，我不会直接归为酒精炉适用。",
        ]
    return {
        "intent": "query_products",
        "answer_type": "product_query",
        "answer": "\n".join(lines),
        "results": rows[:10],
        "result_skus": skus[:10],
        "candidate_skus": skus[:10],
        "answer_metadata": {
            "source": "structured_alcohol_stove_cookware_exclusion",
            "excluded_skus": sorted(excluded_skus),
            "display_count": min(len(rows), 10),
        },
        "debug": {
            "agent_mode": "structured_alcohol_stove_cookware_exclusion",
            "raw_results": rows[:10],
            "candidate_skus": skus[:10],
        },
        "skip_polish": True,
    }


def _phase1_comparison_evidence_value(canonical_field: str, value: Any) -> str:
    """Normalize legacy comparison evidence through the central value guard."""
    if canonical_field == "capacity":
        return _capacity_field_evidence(value)
    if value in (None, "", 0):
        return ""
    display = _compound_display_value(value).strip()
    if canonical_field == "weight" and re.fullmatch(r"\d+(?:\.0+)?", display):
        display = f"{display.split('.', 1)[0]}g"
    if display.startswith("{") and display.endswith("}"):
        try:
            display = _display_value(json.loads(display)).strip()
        except (TypeError, ValueError, json.JSONDecodeError):
            pass
    return display if _is_valid_structured_field_value(canonical_field, display) else ""


def _phase1_product_evidence_text(row: dict, *, max_fields: int | None = None) -> str:
    fields = (
        ("capacity", "capacity"),
        ("body_material", "material"),
        ("gross_weight_g", "weight"),
        ("features", "technical_advantages"),
        ("usage_scenarios", "usage_scene"),
        ("positioning", "positioning"),
        ("long_description_cn", "long_description_cn"),
    )
    if max_fields is not None:
        # The decision fallback is a concise comparison aid, not a product
        # payload renderer. Long descriptions remain available to dedicated
        # content-field requests and cannot crowd out comparable evidence.
        fields = tuple(item for item in fields if item[0] != "long_description_cn")
    values = [
        f"{customer_field_contract.product_detail_field_label(canonical_field) or canonical_field}：{evidence}"
        for key, canonical_field in fields
        if (evidence := _phase1_comparison_evidence_value(canonical_field, row.get(key)))
    ]
    return "；".join(values[:max_fields] if max_fields is not None else values)


def _validate_semantic_comparison_adjudication(
    data: dict[str, Any] | None,
    *,
    participant_count: int,
    allowed_evidence_fields: set[str],
) -> dict[str, Any] | None:
    """Accept an LLM comparison choice only as a sealed index-plus-evidence contract.

    The semantic model may interpret suitability, but it never supplies an SKU,
    product identity, database value, or customer-facing answer.  This validator
    only maps a bounded participant index back onto already sealed entities and
    accepts field keys that the executor actually supplied as same-SKU evidence.
    """
    if not isinstance(data, dict) or participant_count < 2:
        return None
    forbidden = {
        "sku", "selected_sku", "product", "product_name", "selected_product",
        "answer", "final_answer", "facts", "evidence_values", "raw_evidence",
    }
    if forbidden.intersection(data):
        return None
    selected_index = data.get("selected_index")
    if isinstance(selected_index, bool):
        return None
    if selected_index is not None and (
        not isinstance(selected_index, int) or selected_index < 0 or selected_index >= participant_count
    ):
        return None
    raw_fields = data.get("evidence_fields")
    if not isinstance(raw_fields, list):
        return None
    evidence_fields = list(dict.fromkeys(str(field or "").strip() for field in raw_fields))
    if selected_index is None and not evidence_fields:
        reasoning_summary = str(data.get("reasoning_summary") or "").strip()[:300]
        return {
            "selected_index": None,
            "evidence_fields": [],
            "reasoning_summary": reasoning_summary,
        }
    if not evidence_fields or any(field not in allowed_evidence_fields for field in evidence_fields):
        return None
    reasoning_summary = str(data.get("reasoning_summary") or "").strip()[:300]
    return {
        "selected_index": selected_index,
        "evidence_fields": evidence_fields,
        "reasoning_summary": reasoning_summary,
    }


def _comparison_adjudication_evidence(
    *,
    db: Session,
    bundles: list[tuple[Product | None, ProductSpecs | None, ProductBusiness | None, ProductContent | None]],
    include_partial: bool = False,
) -> dict[str, list[dict[str, Any]]]:
    """Build an opaque, same-SKU evidence packet for semantic comparison.

    Product identities never enter this packet. By default a field is present
    only when every sealed participant has a validated value. A decision
    adjudicator may explicitly request partial fields so an observed hard
    contradiction is not discarded; absent participant rows remain unknown
    and never become positive evidence.
    """
    candidates = sorted(
        field
        for field in customer_field_contract.FORMAL_DETAIL_FIELDS
        if customer_field_contract.is_supported_detail_field(field)
        and field not in {"sku", "barcode", "product_name", "product_name_en", "category"}
    )
    packet: dict[str, list[dict[str, Any]]] = {}
    for field in candidates:
        rows: list[dict[str, Any]] = []
        for index, (product, specs, business, content) in enumerate(bundles):
            if product is None:
                rows = []
                break
            value, source = _structured_product_field_evidence(
                field,
                db=db,
                product=product,
                specs=specs,
                business=business,
                content=content,
            )
            if not value:
                if include_partial:
                    continue
                rows = []
                break
            rows.append(
                {
                    "participant_index": index,
                    "sku": str(product.sku or "").strip().upper(),
                    "value": str(value).strip()[:600],
                    "source": str(source or "").strip(),
                }
            )
        if len(rows) == len(bundles) or (include_partial and rows):
            packet[field] = rows
    return packet


def _parse_semantic_json_object(content: str) -> dict[str, Any] | None:
    text = str(content or "").strip()
    if text.startswith("```"):
        text = re.sub(r"^```(?:json)?\s*", "", text, flags=re.I)
        text = re.sub(r"\s*```$", "", text)
    try:
        data = json.loads(text)
    except (TypeError, json.JSONDecodeError):
        return None
    return data if isinstance(data, dict) else None


def _capacity_evidence_is_internally_consistent(value: Any, identity_text: str = "") -> bool:
    """Reject only capacity text whose own size labels contradict each other."""
    text = str(value or "").strip()
    if not text:
        return False

    def normalized_capacities(source: str) -> list[float]:
        values: list[float] = []
        for match in re.finditer(r"(?<![\d.])(\d+(?:\.\d+)?)\s*(ml|毫升|l|升)(?![a-z])", source, flags=re.I):
            amount = float(match.group(1))
            values.append(amount * (1000 if match.group(2).casefold() in {"l", "升"} else 1))
        return values

    identity_capacities = set(normalized_capacities(str(identity_text or "")))
    field_capacities = normalized_capacities(text)
    if len(identity_capacities) == 1 and field_capacities and next(iter(identity_capacities)) not in field_capacities:
        return False

    labelled_values: dict[str, list[float]] = {"大锅": [], "小锅": []}
    patterns = (
        r"(\d+(?:\.\d+)?)\s*(ml|毫升|l|升)\s*(大锅|小锅)",
        r"(大锅|小锅)\s*(\d+(?:\.\d+)?)\s*(ml|毫升|l|升)",
    )
    for pattern_index, pattern in enumerate(patterns):
        for match in re.finditer(pattern, text, flags=re.IGNORECASE):
            if pattern_index == 0:
                raw_value, unit, label = match.group(1), match.group(2), match.group(3)
            else:
                label, raw_value, unit = match.group(1), match.group(2), match.group(3)
            normalized = float(raw_value) * (1000 if unit.casefold() in {"l", "升"} else 1)
            labelled_values[label].append(normalized)
    if labelled_values["大锅"] and labelled_values["小锅"]:
        return max(labelled_values["大锅"]) >= max(labelled_values["小锅"])
    return True


def _validate_semantic_recommendation_narrative(
    data: dict[str, Any] | None,
    *,
    candidate_count: int,
    verified_fields_by_index: dict[int, set[str]],
    candidate_identity_tokens_by_index: dict[int, set[str]] | None = None,
    verified_evidence_values_by_index: dict[int, dict[str, Any]] | None = None,
    required_evidence_fields_by_index: dict[int, dict[str, set[str]]] | None = None,
    expected_ranked_count: int | None = None,
) -> dict[str, Any] | None:
    """Accept a narrative only when it cites the sealed candidate evidence."""
    if not isinstance(data, dict) or set(data) != {"ranked_candidate_indexes", "evidence_usage", "answer"}:
        return None
    ranked = data.get("ranked_candidate_indexes")
    usages = data.get("evidence_usage")
    answer = str(data.get("answer") or "").strip()
    if not isinstance(ranked, list) or not ranked or len(ranked) > min(candidate_count, 5):
        return None
    if expected_ranked_count is not None and len(ranked) != expected_ranked_count:
        return None
    if any(type(index) is not int or not (0 <= index < candidate_count) for index in ranked):
        return None
    if len(set(ranked)) != len(ranked) or not isinstance(usages, list) or len(usages) != len(ranked):
        return None
    if not (20 <= len(answer) <= 1200) or "http://" in answer.lower() or "https://" in answer.lower():
        return None
    # Candidate indices are schema-only handles, never customer-facing language.
    if re.search(r"(?:候选\s*(?:索引)?|索引)\s*[#：:]?\s*\d+", answer, flags=re.IGNORECASE):
        return None
    # The model is free to choose a natural explanation, but it cannot name a
    # sealed product which its own ranked/evidence handles did not select.
    # Without this check a later result reordering can bind the prose for one
    # product to another product's evidence.  Identity tokens are supplied
    # from the sealed catalogue rows; this is contract validation, never a
    # product-name routing rule.
    if candidate_identity_tokens_by_index:
        normalized_answer = re.sub(r"\s+", "", answer).casefold()
        for index in ranked:
            selected_tokens = {
                re.sub(r"\s+", "", str(token or "")).casefold()
                for token in candidate_identity_tokens_by_index.get(index, set())
                if len(re.sub(r"\s+", "", str(token or ""))) >= 3
            }
            if selected_tokens and not any(token in normalized_answer for token in selected_tokens):
                # A customer-facing recommendation must identify every
                # product it ranks. Otherwise a later verifier cannot safely
                # bind pronouns such as “这款” to the visible result SKU and
                # may replace good semantic prose with an internal template.
                return None
        ranked_tokens = {
            re.sub(r"\s+", "", str(token or "")).casefold()
            for index in ranked
            for token in candidate_identity_tokens_by_index.get(index, set())
            if len(re.sub(r"\s+", "", str(token or ""))) >= 3
        }
        for index, tokens in candidate_identity_tokens_by_index.items():
            if index in ranked:
                continue
            for token in tokens:
                normalized_token = re.sub(r"\s+", "", str(token or "")).casefold()
                if (
                    len(normalized_token) >= 3
                    and normalized_token not in ranked_tokens
                    and normalized_token in normalized_answer
                ):
                    # A family/base identity can be a literal prefix of the
                    # selected variant's canonical name or SKU (for example
                    # ``城市出逃套锅`` inside ``城市出逃套锅大锅``).  The answer
                    # has not named an outside product in that case; it has
                    # named the selected identity.  Reject only a token that
                    # is not contained by any selected identity token.
                    if any(normalized_token in ranked_token for ranked_token in ranked_tokens):
                        continue
                    return None
    used_indexes: set[int] = set()
    normalized_usage: list[dict[str, Any]] = []
    for item in usages:
        if not isinstance(item, dict) or set(item) != {"candidate_index", "fields"}:
            return None
        index, fields = item.get("candidate_index"), item.get("fields")
        if type(index) is not int or index not in ranked or index in used_indexes:
            return None
        if not isinstance(fields, list) or not fields or len(fields) > 8:
            return None
        if any(not isinstance(field, str) or field not in verified_fields_by_index.get(index, set()) for field in fields):
            return None
        used_indexes.add(index)
        normalized_usage.append({"candidate_index": index, "fields": list(dict.fromkeys(fields))})
    if used_indexes != set(ranked):
        return None
    if verified_evidence_values_by_index:
        # Natural-language rendering stays model-owned, but measurements are
        # immutable catalogue facts.  Providers occasionally drop the leading
        # ``1.`` from values such as 1.4L/1.7L; reject any visible measurement
        # token that does not occur literally in the selected sealed packet.
        measurement_pattern = re.compile(
            r"(?<![\d.])\d+(?:\.\d+)?\s*(?:ml|毫升|l|升|kg|千克|公斤|g|克|kw|千瓦|w|瓦|cm|厘米|mm|毫米|英寸|寸)(?![a-z])",
            flags=re.IGNORECASE,
        )

        def measurements(value: Any) -> set[str]:
            return {
                re.sub(r"\s+", "", match.group(0)).casefold()
                for match in measurement_pattern.finditer(str(value or ""))
            }

        allowed_measurements: set[str] = set()
        for index in ranked:
            for value in verified_evidence_values_by_index.get(index, {}).values():
                allowed_measurements.update(measurements(value))
        if not measurements(answer).issubset(allowed_measurements):
            return None
        cursor = 0
        for index in ranked:
            evidence_values = verified_evidence_values_by_index.get(index, {})
            product_name = str(evidence_values.get("__product_name__") or "").strip()
            if not product_name:
                continue
            start = answer.find(product_name, cursor)
            if start < 0:
                continue
            next_starts = [
                answer.find(
                    str(verified_evidence_values_by_index.get(other, {}).get("__product_name__") or "").strip(),
                    start + len(product_name),
                )
                for other in ranked
                if other != index
                and str(verified_evidence_values_by_index.get(other, {}).get("__product_name__") or "").strip()
            ]
            end = min((position for position in next_starts if position >= 0), default=len(answer))
            candidate_measurements: set[str] = set()
            for value in evidence_values.values():
                candidate_measurements.update(measurements(value))
            if not measurements(answer[start:end]).issubset(candidate_measurements):
                return None
            cursor = start + len(product_name)
    normalized_usage_by_index = {
        item["candidate_index"]: set(item["fields"])
        for item in normalized_usage
    }
    for index in ranked:
        used_fields = normalized_usage_by_index.get(index, set())
        for available_fields in (required_evidence_fields_by_index or {}).get(index, {}).values():
            if available_fields and not (used_fields & available_fields):
                return None
    return {"ranked_candidate_indexes": ranked, "evidence_usage": normalized_usage, "answer": answer}


def _validate_semantic_recommendation_narrative_grounding_review(data: dict[str, Any] | None) -> dict[str, Any] | None:
    """Return a semantic reviewer decision only for the exact closed schema."""
    if not isinstance(data, dict) or not {"approved", "unsupported_claims"}.issubset(data):
        return None
    if type(data.get("approved")) is not bool:
        return None
    unsupported_claims = data.get("unsupported_claims")
    # The review is an actionable correction signal, not an exhaustive critique.
    # A compact bounded schema prevents a verbose rejection from being truncated
    # by the semantic provider before the one evidence-bounded rewrite can run.
    if not isinstance(unsupported_claims, list) or len(unsupported_claims) > 5:
        return None
    if any(not isinstance(item, str) or not item.strip() or len(item) > 80 for item in unsupported_claims):
        return None
    # A reviewer cannot approve while also naming unsupported claims.
    if data["approved"] and unsupported_claims:
        return None
    return {
        "approved": bool(data["approved"]),
        "unsupported_claims": list(data["unsupported_claims"]),
    }


_RECOMMENDATION_UNPROVED_COMPARATIVE_CLAIM_RE = re.compile(
    r"(?:最)\s*(?:轻|重|便宜|贵|省钱|耐用|实用|适合|好|佳|优|快|慢|小|大|强|弱|安全|稳定|方便|便携|可靠|省事|均衡|全能)"
    r"|(?:综合\s*(?:首选|推荐|最佳)|首选|最佳|容量\s*稍[大小]|极致\s*轻量(?:化)?)"
)
def _recommendation_allowed_evidence_text(
    candidates: list[dict[str, Any]],
    narrative: dict[str, Any] | None,
) -> str:
    selected = set((narrative or {}).get("ranked_candidate_indexes") or [])
    return " ".join(
        str(value or "")
        for index, candidate in enumerate(candidates)
        if index in selected
        for value in (candidate.get("sealed_evidence") or {}).values()
    )


def _recommendation_unproved_comparative_claims(
    answer: str,
    allowed_evidence_text: str = "",
) -> list[str]:
    """Return catalogue-wide rankings no selected-candidate evidence can prove.

    This is deliberately an output safety gate, never a field or product
    router.  Recommendation verification validates a candidate against the
    customer's stated constraints; it does not establish an ordering across a
    complete comparable population.  The semantic reviewer owns contextual
    claims such as relative lightness because it sees the sealed candidate
    values.  This bounded guard only prevents a reviewer false positive from
    exposing an unproved catalogue-wide superlative as fact.
    """
    text = str(answer or "")
    allowed = str(allowed_evidence_text or "")
    return list(dict.fromkeys(
        match.group(0)
        for match in _RECOMMENDATION_UNPROVED_COMPARATIVE_CLAIM_RE.finditer(text)
        if match.group(0) not in allowed
    ))


_RECOMMENDATION_INTERNAL_PROCESS_CLAIM_RE = re.compile(
    r"同\s*SKU\s*(?:来源字段|条件(?:资料)?(?:已经|已)?(?:通过)?(?:验证|核验))"
    r"|(?:通过|经过|已通过).{0,12}(?:可验证|验证|核验|硬条件)"
    r"|可验证的硬条件"
    r"|本次仅采用"
    r"|\b(?:subject_subtype|subject_kind|recommendation_contract|semantic_constraints|verified_constraints|evidence_usage)\b"
    r"|(?:候选|索引)\s*[#：:]?\s*\d+",
    flags=re.IGNORECASE,
)


def _recommendation_internal_process_claims(answer: str) -> list[str]:
    """Reject internal verification/process wording from customer prose."""
    text = str(answer or "")
    return list(dict.fromkeys(
        match.group(0)
        for match in _RECOMMENDATION_INTERNAL_PROCESS_CLAIM_RE.finditer(text)
    ))


def _recommendation_rewrite_candidates(candidates: list[dict[str, Any]]) -> list[dict[str, Any]]:
    """Keep a rejected narrative's rewrite inside the same sealed SKU packet.

    A rewrite must be able to replace an unsupported conclusion with the
    literal same-SKU context that could actually explain the recommendation.
    Removing that context after the first draft turns a valid recovery into a
    category-only list, even when usage, audience, or product-content evidence
    was already sealed for the selected product.  The rewrite remains bounded
    by the exact packet and goes through the same semantic grounding review;
    it cannot introduce a new candidate, fact, rank, or inferred suitability.
    """
    recovery: list[dict[str, Any]] = []
    for candidate in candidates:
        verified_constraints = [
            str(field)
            for field in candidate.get("verified_constraints") or []
            if isinstance(field, str) and field
        ]
        sealed_evidence = candidate.get("sealed_evidence") or {}
        recovery.append({
            **candidate,
            "sealed_evidence": {
                str(field): value
                for field, value in sealed_evidence.items()
                if str(field).strip() and str(value or "").strip()
            },
            "verified_constraints": verified_constraints,
        })
    return recovery


_RECOMMENDATION_RENDERABLE_EVIDENCE_FIELDS = {
    "people", "scenario", "heat_source", "capacity", "weight", "storage", "cleaning",
    "specs.heat_source", "specs.capacity", "specs.gross_weight_g", "specs.size_info",
    "content.features", "content.usage_scenarios", "content.target_audience",
}


def _recommendation_render_packet(candidates: list[dict[str, Any]], narrative: dict[str, Any]) -> list[dict[str, Any]]:
    """Expose only the evidence selected by the semantic recommendation draft."""
    by_index = {
        int(candidate.get("candidate_index")): candidate
        for candidate in candidates
        if isinstance(candidate, dict) and type(candidate.get("candidate_index")) is int
    }
    packet: list[dict[str, Any]] = []
    selected_fields_by_index = {
        item.get("candidate_index"): set(item.get("fields") or [])
        for item in (narrative.get("evidence_usage") or [])
        if isinstance(item, dict) and type(item.get("candidate_index")) is int
    }
    selection_only = bool(narrative.get("selection_only"))
    for index in narrative.get("ranked_candidate_indexes") or []:
        candidate = by_index.get(index)
        if candidate is None:
            continue
        evidence = candidate.get("sealed_evidence") if isinstance(candidate.get("sealed_evidence"), dict) else {}
        allowed_evidence = {
            field: value
            for field, value in evidence.items()
            if field in _RECOMMENDATION_RENDERABLE_EVIDENCE_FIELDS
            and (selection_only or field in selected_fields_by_index.get(index, set()))
        }
        if not allowed_evidence:
            continue
        packet.append({
            "candidate_index": index,
            "product_name": str(candidate.get("product_name") or "").strip(),
            "sku": str(candidate.get("sku") or "").strip().upper(),
            "allowed_evidence": allowed_evidence,
        })
    return packet


async def _semantic_recommendation_narrative_grounding_review(
    db: Session,
    *,
    question: str,
    candidates: list[dict[str, Any]],
    narrative: dict[str, Any],
    soft_preferences: list[str] | None = None,
    required_customer_dimensions: list[str] | None = None,
    diagnostic: dict[str, str] | None = None,
) -> dict[str, Any] | None:
    """Use the semantic model to reject, never repair, unsupported prose claims."""
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    messages = [
        {
            "role": "system",
            "content": (
                "Return only JSON. Audit whether every product-specific factual, comparative, superlative, "
                "or suitability claim in answer is directly entailed by sealed_candidates' sealed_evidence or verified_constraints. "
                "The question is not evidence. Do not infer missing facts, and do not accept a claim merely "
                "because it sounds plausible. A numeric field does not establish a relative, qualitative, or suitability claim. "
                "In particular, a recorded weight such as 960g or 340g never by itself proves that a candidate is '轻量', "
                "'轻便', '较轻', or '极致轻便'; reject those labels unless that exact qualitative wording appears in that "
                "candidate's sealed evidence. This remains true when the customer has expressed a lightweight preference. "
                "Reject any ungrounded comparative or superlative language, including Chinese claims such as '更安全', '更稳定', '非常适合', '首选', or '综合推荐'; "
                "a single candidate's evidence cannot establish those claims without an explicit comparison or the exact literal wording in that candidate's sealed evidence. "
                "verified_constraints are internal selection gates, not customer-facing facts. Reject any answer that exposes validation, verification, contracts, checks, or the selection process. "
                "soft_customer_preferences are literal user priorities but are not evidence. Reject any claim that a candidate meets one unless the exact support appears in sealed_evidence. "
                "required_customer_dimensions are explicit decision dimensions from the validated recommendation contract. The answer must visibly address every listed dimension for every selected candidate when that candidate packet contains a corresponding evidence field; merely citing a scenario does not cover capacity, weight, storage, or another listed dimension. "
                "The answer must address every explicit customer intended-use or suitability request in the question. It may make a reasoned recommendation when it states the concrete same-SKU evidence behind that judgment and does not present the judgment as a source quotation, guarantee, or unconditional product fact. "
                "When the question coordinates multiple intended actions, such as 煮汤和做饭, the answer must explicitly acknowledge each action and connect the cited physical evidence conditionally; a generic camping statement does not cover those actions. "
                "content.usage_scenarios supports a direct scenario statement such as '资料标注场景为露营' or a direct scenario statement that the candidate is for that same listed scenario; "
                "content.target_audience and heat_source likewise support only their directly listed audience or heat-source statements. "
                "For a customer decision, a conditional choice is allowed only when its stated condition is directly and narrowly restated from that selected candidate's sealed scenario, audience, or other sealed evidence. The answer must make that condition explicit rather than claiming an unconditional winner or broad suitability. "
                "Do not reject the assistant action phrases '可以先看', '我会建议先看', or '可以考虑' by themselves: they express the recommendation act, not a product fact. Audit the evidence-based reasons that follow them. Continue to reject unsupported claims such as '产品很轻', '最适合', or '更稳定'. "
                "A qualified practical-use inference is also allowed when it names a concrete same-SKU physical capability and states the customer's use as a condition, rather than claiming that use is a catalog label. For example, sealed evidence for a water kettle with capacity may support 'if you are boiling water to make coffee, the included about 1.0L kettle can be an option'; it does not support saying the product is a dedicated coffee maker, guarantees coffee use, or is generally best for coffee. Approve this narrow, evidence-named conditional reasoning. "
                "中文示例：资料写明套装内有约1.0L水壶时，'如果您是烧水冲泡咖啡，这个水壶可以作为选择'是允许的限定性判断；不要因为资料没有逐字写'咖啡'而拒绝它。相反，'容量稍大'、'极致轻量化'、'专门泡咖啡'或'最适合咖啡'不是可直接核验的限定判断，除非封存资料明确写出。 "
                "You must approve a direct literal restatement such as '资料标注使用场景包括家庭露营' when that exact scenario is present in the selected candidate's content.usage_scenarios and evidence_usage cites it; "
                "this is an evidence citation, not an unsupported broad suitability claim. "
                "The same rule applies to exact capacity, weight, heat-source, material, dimension, and other sealed values: when evidence_usage cites the corresponding field and the answer says '资料标注...' with those literal values, you must approve that factual restatement. A sentence that then says the supplied material does not directly establish the customer's more specific intended use is a safe evidence-boundary disclosure, not an unsupported negative product claim. Audit clauses separately; never reject an entire mixed sentence merely because it contains both exact facts and that boundary. "
                "Every product-specific fact or comparison in the answer must be supported by a field explicitly listed for that same candidate in evidence_usage. Reject a claim that relies only on some other uncited field in sealed_evidence, even if that field happens to be present. "
                "Candidate indexes are internal handles, never customer language. "
                "Schema exactly: {\"approved\":boolean,\"unsupported_claims\":[\"brief claim\"]}. "
                "If rejecting, return at most five concise unsupported claim snippets, each at most 80 characters; "
                "prioritize the highest-impact unsupported claims and do not enumerate every sentence. "
                "Set approved true only when unsupported_claims is empty."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {
                    "question": question,
                    "soft_customer_preferences": list(soft_preferences or []),
                    "required_customer_dimensions": list(required_customer_dimensions or []),
                    "sealed_candidates": candidates,
                    "ranked_candidate_indexes": narrative["ranked_candidate_indexes"],
                    "evidence_usage": narrative["evidence_usage"],
                    "answer": narrative["answer"],
                },
                ensure_ascii=False,
            ),
        },
    ]
    # A completed recommendation draft has already passed its closed evidence
    # schema. One malformed reviewer reply is a provider-format transient, not
    # evidence that the draft is unsafe. Retry the same semantic audit once,
    # then remain fail-closed if it still cannot return the review contract.
    # This never approves a draft without an explicit DeepSeek review and
    # never synthesizes a deterministic recommendation verdict.
    last_status = "unavailable_or_invalid"
    for _attempt in range(2):
        try:
            content = await customer_llm_service.chat_completion(
                db,
                messages=messages,
                temperature=0,
                max_tokens=400,
                purpose="semantic_recommendation_narrative_grounding_review",
                # Recommendation writing/review is customer-facing reasoning, not
                # the latency-oriented semantic preplan. Use the configured chat
                # model so the same DeepSeek service owns both prose and review.
                api_model_override=runtime["model"],
                response_format={"type": "json_object"} if settings.SEMANTIC_PREPLAN_JSON_MODE else None,
                thinking={"type": "disabled"} if settings.SEMANTIC_PREPLAN_THINKING_DISABLED else None,
            )
        except Exception:
            last_status = "provider_error"
            continue
        parsed = _parse_semantic_json_object(content)
        result = _validate_semantic_recommendation_narrative_grounding_review(parsed)
        if result is not None:
            return result
        last_status = "invalid_json" if parsed is None else "invalid_contract"
    if diagnostic is not None:
        diagnostic["status"] = last_status
    return None


def _resolved_structured_contract_preempts_catalog_value_preflight(question: str) -> bool:
    contract = customer_structured_query_contract.build_structured_query_contract(question)
    return bool(contract.status == "resolved" and contract.conditions)


def _semantic_catalog_value_query_result(
    db: Session,
    question: str,
    semantic_preplan: dict | None,
) -> dict | None:
    """Execute a semantic, database-grounded catalogue-value filter.

    The planner decides whether a phrase names a public catalogue field.  This
    executor only verifies that the semantic subject actually matches that
    field in the live development database before exposing any product rows.
    """
    # Owner and lifecycle are operational predicates, not public catalogue
    # values.  A semantic plan may reduce "负责人 Max 的锅具" to
    # ``category=锅具``; let the deterministic structured parser retain the
    # complete internal filter set instead of widening the result to a whole
    # category.
    if _is_internal_catalogue_filter_query(question):
        return None
    plan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    if _resolved_structured_contract_preempts_catalog_value_preflight(question):
        # A verified predicate such as "容量不小于1升" is more specific than
        # a semantic category label such as "水壶".  Let the structured
        # executor evaluate the product values instead of returning every row
        # in that category.
        return None
    field = str(plan.get("field_type") or plan.get("catalogue_field_candidate") or "").strip()
    subject = str(plan.get("subject_text") or "").strip()
    if not (
        field in {"series", "brand", "category", "product_level"}
        and (
            (
                str(plan.get("route_family") or "").strip() == "structured_query"
                and str(plan.get("route_hint") or "").strip() == "query_products"
            )
            or str(plan.get("semantic_route_family_hint") or "").strip() == "structured_query"
        )
        and _semantic_preplan_confident(plan, minimum=0.9)
        and not (plan.get("entities") or [])
    ):
        return None
    normalized_subject = customer_agent_service.normalize_search_text(subject).lower()
    normalized_question = customer_agent_service.normalize_search_text(question).lower()
    if field == "category" and normalized_subject and normalized_subject not in normalized_question:
        direct_categories = [
            str(value or "").strip()
            for (value,) in db.query(Product.category).distinct().all()
            if str(value or "").strip()
            and customer_agent_service.normalize_search_text(str(value)).lower() in normalized_question
        ]
        if direct_categories:
            shortest = min(len(customer_agent_service.normalize_search_text(value)) for value in direct_categories)
            direct_categories = [value for value in direct_categories if len(customer_agent_service.normalize_search_text(value)) == shortest]
        if len(direct_categories) == 1:
            subject = direct_categories[0]
            normalized_subject = customer_agent_service.normalize_search_text(subject).lower()
    subject_candidates = [normalized_subject]
    field_contract = customer_field_contract.field_contract_for_type(field)
    field_labels = [customer_field_contract.product_detail_field_label(field)]
    if field_contract:
        field_labels.extend(field_contract.aliases)
    # The concise terminal token of a registered field label is still a
    # display modifier, not part of the database value (for example the
    # ``系列`` suffix in a semantic subject such as “城市出逃系列”).
    field_labels.extend(str(label or "")[-2:] for label in field_labels if len(str(label or "")) > 2)
    for label in field_labels:
        normalized_label = customer_agent_service.normalize_search_text(label).lower()
        if normalized_label and normalized_subject.endswith(normalized_label):
            subject_candidates.append(normalized_subject[:-len(normalized_label)].strip())
        if normalized_label and normalized_subject.startswith(normalized_label):
            subject_candidates.append(normalized_subject[len(normalized_label):].strip())
    subject_candidates = list(dict.fromkeys(item for item in subject_candidates if len(item) >= 2))
    if not subject_candidates:
        return None
    rows = [
        row for row in _phase1_catalog_rows(db, "产品")
        if any(
            candidate in customer_agent_service.normalize_search_text(row.get(field) or "").lower()
            for candidate in subject_candidates
        )
    ]
    reason_label = f"数据库{customer_field_contract.product_detail_field_label(field)}字段"
    if not rows:
        # A product title or description can mention another product family.
        # That text is not formal membership in a catalogue series, brand,
        # category, or level, so it must not expand a customer-facing set.
        return None
    return _structured_product_query_result(
        question=question,
        product_ref=subject,
        rows=rows,
        source="semantic_database_value_catalog_filter",
        reason_label=reason_label,
        membership_field=field,
    )


async def _semantic_catalog_value_exact_adapter(
    db: Session,
    *,
    question: str,
    semantic_preplan: dict[str, Any] | None,
) -> dict[str, Any] | None:
    """Map a semantic catalogue value only to an exact live DB value.

    The semantic plan already owns intent and field selection.  This adapter
    runs only after literal/content verification failed and gives the model a
    small closed set of actual field values.  Its output is accepted only when
    it returns one of those values verbatim, so it cannot invent a series,
    brand, category, level, SKU, or answer.
    """
    plan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    field = str(plan.get("field_type") or plan.get("catalogue_field_candidate") or "").strip()
    subject = str(plan.get("subject_text") or "").strip()
    if not (
        field in {"series", "brand", "category", "product_level"}
        and (
            (
                str(plan.get("route_family") or "").strip() == "structured_query"
                and str(plan.get("route_hint") or "").strip() == "query_products"
            )
            or str(plan.get("semantic_route_family_hint") or "").strip() == "structured_query"
        )
        and _semantic_preplan_confident(plan, minimum=0.9)
        and not (plan.get("entities") or [])
    ):
        return None
    column = getattr(Product, field, None)
    if column is None:
        return None
    values = sorted({
        str(value or "").strip()
        for (value,) in db.query(column).distinct().all()
        if str(value or "").strip() and str(value or "").strip() not in {"/", "未分类", "暂无", "未知"}
    })
    if not values or len(values) > 40:
        return None
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    messages = [
        {
            "role": "system",
            "content": (
                "You resolve one customer catalogue filter into an exact value from a closed database list. "
                "Understand Chinese/English paraphrase or translation, but never invent, translate, shorten, "
                "combine, or return a value outside the list. Return JSON only: {\"value\": \"exact list value or empty\"}."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {
                    "question": question,
                    "field": field,
                    # A semantic planner may correctly classify a catalogue
                    # field while omitting its foreign-language value span.
                    # The whole turn is still safe input because the model
                    # can only select an exact entry from allowed_values.
                    "customer_value": subject or str(question or "").strip(),
                    "allowed_values": values,
                },
                ensure_ascii=False,
            ),
        },
    ]
    selected_values: list[str] = []
    for _ in range(2):
        try:
            content = await customer_llm_service.chat_completion(
                db, messages, temperature=0.0, max_tokens=80,
                purpose="semantic_catalog_value_exact_adapter",
                api_model_override=runtime["model"],
                response_format={"type": "json_object"}, thinking=False,
            )
            payload = json.loads(str(content or ""))
        except (ValueError, TypeError, json.JSONDecodeError):
            continue
        value = str(payload.get("value") or "").strip() if isinstance(payload, dict) else ""
        if value in values:
            selected_values.append(value)
    if len(selected_values) != 2 or len(set(selected_values)) != 1:
        return None
    value = selected_values[0]
    adapted = dict(plan)
    adapted["subject_text"] = value
    adapted["catalog_value_adapter"] = "semantic_exact_live_value"
    return adapted


async def _semantic_recommendation_narrative_rewrite(
    db: Session,
    *,
    question: str,
    candidates: list[dict[str, Any]],
    rejected_narrative: dict[str, Any],
    unsupported_claims: list[str],
    soft_preferences: list[str] | None = None,
    expected_ranked_count: int | None = None,
) -> dict[str, Any] | None:
    """Ask the semantic model for one evidence-bounded rewrite after a rejection."""
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    messages = [
        {
            "role": "system",
            "content": (
                "Return only JSON. Rewrite the customer-facing recommendation using only sealed_candidates' "
                "sealed_evidence. verified_constraints are internal selection gates and must never appear in customer prose. soft_customer_preferences are not evidence: do not claim that a candidate meets them unless the exact support appears in sealed_evidence. Remove every rejected claim; do not replace it with a synonym, ranking claim, new product fact, or a qualitative conclusion from a numeric value. Keep the prose natural and useful. End with one brief helpful offer to refine the choice or compare another evidenced dimension; this service offer must not introduce any product fact. candidate_index is an "
                "internal JSON handle only and must not appear in answer. Name only product_name values belonging to your ranked_candidate_indexes; "
                "never describe or name a different sealed candidate. Schema exactly: "
                "{\"ranked_candidate_indexes\":[integer],\"evidence_usage\":[{\"candidate_index\":integer,"
                "\"fields\":[\"evidence field\"]}],\"answer\":\"natural Chinese recommendation\"}. "
                "硬性恢复规则：最多选择三款；若恢复包只提供场景或产品类型证据，只能写‘资料标注使用场景包括…’或‘资料标注产品类型为…’这类来源绑定陈述。若回答选择问题，只能在所写条件直接、狭义复述某候选的 sealed_evidence 时给出条件化选择；必须写明条件和来源，不能给出无条件优胜或把用户偏好当作产品事实。 "
                "A qualified practical-use inference is allowed when it names a concrete same-SKU physical capability and frames the intended use as a condition rather than a catalog label. 中文示例：资料写明套装内有约1.0L水壶时，可以写‘如果您是烧水冲泡咖啡，套装内约1.0L水壶可作为选择’；不要写‘专门泡咖啡’、‘最适合咖啡’、‘容量稍大’或‘极致轻量’。 "
                "不得写‘适合…’、‘首选’、‘更安全’、‘更稳定’、‘最好’或任何比较、承诺、卖点扩写；没有其他可核验理由时，直接场景陈述就是完整推荐理由。"
                "用户用途不是产品证据。若 sealed_evidence 没有逐字支持该用途，不得写产品‘能、适合、方便’完成它；应把用途写成用户的条件，并明确现有资料只能核对哪些容量、重量、热源或场景，具体用途仍未直接标注。"
                "引用值必须保留 sealed_evidence 原文，不得换算单位、四舍五入、改写标签或合并成新规格；每个证据字段单独写一个短分句。"
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {
                    "question": question,
                    "soft_customer_preferences": list(soft_preferences or []),
                    "sealed_candidates": candidates,
                    "rejected_answer": rejected_narrative["answer"],
                    "rejected_claims": unsupported_claims,
                },
                ensure_ascii=False,
            ),
        },
    ]
    try:
        content = await customer_llm_service.chat_completion(
            db,
            messages=messages,
            temperature=0,
            max_tokens=700,
            purpose="semantic_recommendation_narrative_rewrite",
            api_model_override=runtime["model"],
            response_format={"type": "json_object"} if settings.SEMANTIC_PREPLAN_JSON_MODE else None,
            thinking={"type": "disabled"} if settings.SEMANTIC_PREPLAN_THINKING_DISABLED else None,
        )
    except Exception:
        return None
    return _validate_semantic_recommendation_narrative(
        _parse_semantic_json_object(content),
        candidate_count=len(candidates),
        verified_fields_by_index={
            index: set(candidate.get("sealed_evidence") or {})
            for index, candidate in enumerate(candidates)
        },
        candidate_identity_tokens_by_index={
            index: {str(candidate.get("product_name") or "").strip(), str(candidate.get("sku") or "").strip()}
            for index, candidate in enumerate(candidates)
        },
        verified_evidence_values_by_index={
            index: {
                **dict(candidate.get("sealed_evidence") or {}),
                "__product_name__": str(candidate.get("product_name") or ""),
            }
            for index, candidate in enumerate(candidates)
        },
        expected_ranked_count=expected_ranked_count,
    )


async def _semantic_recommendation_evidence_render(
    db: Session,
    *,
    question: str,
    candidates: list[dict[str, Any]],
    narrative: dict[str, Any],
    required_evidence_fields_by_index: dict[int, dict[str, set[str]]],
    diagnostic: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Render an approved candidate choice from its selected evidence only."""
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    packet = _recommendation_render_packet(candidates, narrative)
    if not packet:
        return None
    messages = [
        {
            "role": "system",
            "content": (
                "Return only JSON. Write a concise Chinese customer recommendation using only allowed_evidence. "
                "Do not use facts, adjectives, rankings, comparisons, guarantees, or suitability claims outside literal allowed_evidence values. "
                "Do not mention verification, contracts, internal selection, candidate indexes, or SKU. "
                "You may naturally say '可以先看', '我会建议先看', or '可以考虑' as the assistant's recommendation action, then give literal evidence-backed reasons. Do not turn that action into an unsupported product adjective or guarantee. "
                "The customer's intended use is context, not product evidence. If allowed_evidence does not literally state that use, do not say the product can, suits, handles, or is convenient for it. Keep the use conditional and name the boundary naturally, for example: '如果你先按容量和炉具兼容来缩小范围，可以先看X：资料标注容量为A、热源为B；是否完全满足你提到的具体做法，现有资料没有直接说明。' Adapt this wording to the supplied facts; never copy values from the example. "
                "In that boundary sentence, repeat the customer's concrete intended action or purpose from the question (for example the supplied 烧水、煮面、煎烤 wording); never replace it with vague phrases such as '具体做法' or '具体用途'. "
                "Also acknowledge the customer's explicitly stated group size and decision priorities in natural context (for example '针对你说的两个人和轻便偏好'); this repeats the request and must not be phrased as a product fact unless allowed_evidence supports it. "
                "End with one short, concrete next-step question offering to refine or compare another evidenced dimension. The question must be helpful customer service prose and must not add a product fact. "
                "Copy every cited evidence value literally. Do not convert units (for example, never rewrite 1000ML as 1L), round numbers, normalize labels, or merge several source values into one inferred specification. Put each cited field in its own short clause so an auditor can bind it to one evidence key. "
                "Each named product must be one of the supplied product_name values. "
                "The answer MUST contain the exact product_name of every required candidate; do not replace it with only '这款' or a pronoun. ranked_candidate_indexes must exactly equal required_candidate_indexes in the same order, and evidence_usage must contain exactly one item for each required index. "
                "Schema exactly: {\"ranked_candidate_indexes\":[integer],\"evidence_usage\":[{\"candidate_index\":integer,"
                "\"fields\":[\"evidence field\"]}],\"answer\":\"natural Chinese recommendation\"}."
            ),
        },
        {
            "role": "user",
            "content": json.dumps({
                "question": question,
                "selected_candidates": packet,
                "required_candidate_indexes": narrative.get("ranked_candidate_indexes") or [],
            }, ensure_ascii=False),
        },
    ]
    for attempt in range(2):
        attempt_messages = [dict(message) for message in messages]
        if attempt:
            attempt_messages[0]["content"] += (
                " Your previous response failed the closed JSON/evidence schema. Return a fresh JSON object; "
                "copy field names exactly from allowed_evidence, keep ranked_candidate_indexes exactly equal to required_candidate_indexes, "
                "and write every selected candidate's exact product_name in answer."
            )
        try:
            content = await customer_llm_service.chat_completion(
                db,
                messages=attempt_messages,
                temperature=0,
                max_tokens=500,
                purpose="semantic_recommendation_evidence_render",
                api_model_override=runtime["model"],
                response_format={"type": "json_object"} if settings.SEMANTIC_PREPLAN_JSON_MODE else None,
                thinking={"type": "disabled"} if settings.SEMANTIC_PREPLAN_THINKING_DISABLED else None,
            )
        except Exception:
            continue
        parsed_render = _parse_semantic_json_object(content)
        if diagnostic is not None:
            diagnostic.clear()
            diagnostic.update({
                "parsed_keys": sorted(parsed_render) if isinstance(parsed_render, dict) else [],
                "ranked_candidate_indexes": parsed_render.get("ranked_candidate_indexes") if isinstance(parsed_render, dict) else None,
                "required_candidate_indexes": list(narrative.get("ranked_candidate_indexes") or []),
                "usage_fields": [
                    item.get("fields")
                    for item in (parsed_render.get("evidence_usage") or [])
                    if isinstance(item, dict)
                ] if isinstance(parsed_render, dict) else [],
                "answer_length": len(str(parsed_render.get("answer") or "")) if isinstance(parsed_render, dict) else 0,
            })
        rendered = _validate_semantic_recommendation_narrative(
            parsed_render,
            candidate_count=len(candidates),
            verified_fields_by_index={
                index: set(candidate.get("sealed_evidence") or {})
                for index, candidate in enumerate(candidates)
            },
            candidate_identity_tokens_by_index={
                index: {str(candidate.get("product_name") or "").strip(), str(candidate.get("sku") or "").strip()}
                for index, candidate in enumerate(candidates)
            },
            verified_evidence_values_by_index={
                index: {
                    **dict(candidate.get("sealed_evidence") or {}),
                    "__product_name__": str(candidate.get("product_name") or ""),
                }
                for index, candidate in enumerate(candidates)
            },
            required_evidence_fields_by_index=required_evidence_fields_by_index,
        )
        if rendered is not None:
            return rendered
        # Some providers preserve the requested candidate choice and a usable
        # answer but misspell an evidence key in the auxiliary usage array.
        # Repair only that bookkeeping from the exact allowed packet, then let
        # the independent semantic grounding reviewer judge every prose claim.
        # This grants no new SKU or fact beyond what the renderer received.
        required_indexes = list(narrative.get("ranked_candidate_indexes") or [])
        parsed_indexes = parsed_render.get("ranked_candidate_indexes") if isinstance(parsed_render, dict) else None
        parsed_answer = str(parsed_render.get("answer") or "").strip() if isinstance(parsed_render, dict) else ""
        if parsed_indexes == required_indexes and parsed_answer:
            packet_by_index = {
                item["candidate_index"]: list((item.get("allowed_evidence") or {}).keys())[:8]
                for item in packet
            }
            repaired_usage = [
                {"candidate_index": index, "fields": packet_by_index.get(index, [])}
                for index in required_indexes
            ]
            if all(item["fields"] for item in repaired_usage):
                rendered = _validate_semantic_recommendation_narrative(
                    {
                        "ranked_candidate_indexes": required_indexes,
                        "evidence_usage": repaired_usage,
                        "answer": parsed_answer,
                    },
                    candidate_count=len(candidates),
                    verified_fields_by_index={
                        index: set(candidate.get("sealed_evidence") or {})
                        for index, candidate in enumerate(candidates)
                    },
                    candidate_identity_tokens_by_index={
                        index: {str(candidate.get("product_name") or "").strip(), str(candidate.get("sku") or "").strip()}
                        for index, candidate in enumerate(candidates)
                    },
                    verified_evidence_values_by_index={
                        index: {
                            **dict(candidate.get("sealed_evidence") or {}),
                            "__product_name__": str(candidate.get("product_name") or ""),
                        }
                        for index, candidate in enumerate(candidates)
                    },
                    required_evidence_fields_by_index=required_evidence_fields_by_index,
                )
                if rendered is not None:
                    return rendered
    return None


async def _semantic_recommendation_final_render(
    db: Session,
    *,
    question: str,
    candidates: list[dict[str, Any]],
    narrative: dict[str, Any],
    soft_preferences: list[str] | None,
    required_customer_dimensions: list[str] | None,
    required_evidence_fields_by_index: dict[int, dict[str, set[str]]],
    diagnostic: dict[str, Any] | None = None,
) -> dict[str, Any] | None:
    """Let Flash render choices from only their cited same-SKU evidence.

    At this point the renderer cannot see uncited catalogue fields. The closed
    schema validator binds every visible SKU and evidence key, while the local
    safety gates reject high-risk ranking/process language. A second model
    verdict here was both redundant and stochastic, often rejecting a safe
    evidence-boundary sentence on one run and approving it on the next.
    """
    rendered = await _semantic_recommendation_evidence_render(
        db,
        question=question,
        candidates=candidates,
        narrative=narrative,
        required_evidence_fields_by_index=required_evidence_fields_by_index,
        diagnostic=diagnostic,
    )
    if rendered is None:
        if diagnostic is not None:
            diagnostic["status"] = "invalid_render_schema"
        return None
    local_claims = list(dict.fromkeys([
        *_recommendation_unproved_comparative_claims(
            rendered["answer"],
            _recommendation_allowed_evidence_text(candidates, rendered),
        ),
        *_recommendation_internal_process_claims(rendered["answer"]),
    ]))
    if local_claims and diagnostic is not None:
        diagnostic["status"] = "local_safety_rejected"
        diagnostic["claims"] = local_claims
    return rendered if not local_claims else None


def _model_selected_recommendation_seed(
    data: dict[str, Any] | None,
    *,
    candidate_count: int,
    expected_ranked_count: int | None = None,
) -> dict[str, Any] | None:
    """Recover only a model-authored candidate choice from malformed prose JSON.

    Selection remains entirely model-owned.  This adapter accepts no product
    facts or answer text; it merely validates that the model's candidate
    handles are in range so the independent Flash evidence renderer can write
    a fresh answer from the sealed packet.
    """
    if not isinstance(data, dict):
        return None
    ranked = data.get("ranked_candidate_indexes")
    if not isinstance(ranked, list) or not ranked or len(ranked) > min(candidate_count, 3):
        return None
    if expected_ranked_count is not None and len(ranked) != expected_ranked_count:
        return None
    if any(type(index) is not int or not (0 <= index < candidate_count) for index in ranked):
        return None
    if len(set(ranked)) != len(ranked):
        return None
    return {
        "ranked_candidate_indexes": list(ranked),
        "evidence_usage": [],
        "answer": "",
        "selection_only": True,
    }


async def _semantic_recommendation_candidate_selection(
    db: Session,
    *,
    question: str,
    candidates: list[dict[str, Any]],
    soft_preferences: list[str] | None,
    expected_ranked_count: int | None,
) -> dict[str, Any] | None:
    """Ask Flash for candidate choice only when prose generation lost schema."""
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    messages = [
        {
            "role": "system",
            "content": (
                "Return only JSON. Select the most useful one to three candidate indexes for the customer's request "
                "from sealed_candidates; do not write an answer and do not invent facts. Treat soft_customer_preferences "
                "as decision context only, never as proven product facts. "
                + (
                    "The customer requested exactly one recommendation, so return exactly one index. "
                    if expected_ranked_count == 1
                    else "Return at most three focused indexes. "
                )
                + "Schema exactly: {\"ranked_candidate_indexes\":[integer]}."
            ),
        },
        {
            "role": "user",
            "content": json.dumps({
                "question": question,
                "soft_customer_preferences": list(soft_preferences or []),
                "sealed_candidates": candidates,
            }, ensure_ascii=False),
        },
    ]
    try:
        content = await customer_llm_service.chat_completion(
            db,
            messages=messages,
            temperature=0,
            max_tokens=120,
            purpose="semantic_recommendation_candidate_selection",
            api_model_override=runtime["model"],
            response_format={"type": "json_object"} if settings.SEMANTIC_PREPLAN_JSON_MODE else None,
            thinking={"type": "disabled"} if settings.SEMANTIC_PREPLAN_THINKING_DISABLED else None,
        )
    except Exception:
        return None
    parsed = _parse_semantic_json_object(content)
    if not isinstance(parsed, dict) or set(parsed) != {"ranked_candidate_indexes"}:
        return None
    return _model_selected_recommendation_seed(
        parsed,
        candidate_count=len(candidates),
        expected_ranked_count=expected_ranked_count,
    )


async def _semantic_recommendation_narrative(
    db: Session,
    *,
    question: str,
    rows: list[dict[str, Any]],
    verifications: list[Any],
    soft_preferences: list[str] | None = None,
    required_customer_dimensions: list[str] | None = None,
    diagnostics: list[dict[str, Any]] | None = None,
) -> dict[str, Any] | None:
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    verification_by_sku = {str(item.sku or "").strip().upper(): item for item in verifications}
    expected_ranked_count = 1 if _explicit_single_recommendation_request(question) else None
    candidates: list[dict[str, Any]] = []
    verified_fields_by_index: dict[int, set[str]] = {}
    required_evidence_fields_by_index: dict[int, dict[str, set[str]]] = {}
    dimension_field_aliases = {
        "capacity": {"capacity", "specs.capacity"},
        "weight": {"weight", "specs.gross_weight_g"},
        "storage": {"storage", "content.features"},
        "portability": {"portability", "content.features"},
        "stability": {"stability"},
    }
    content_evidence_fields = (
        "title_cn",
        "website_title",
        "features",
        "usage_scenarios",
        "target_audience",
        "positioning",
        "top_selling_points",
        "long_description_cn",
    )
    structured_evidence_fields = ("heat_source", "capacity", "body_material", "gross_weight_g", "size_info")
    for index, row in enumerate(rows):
        sku = str(row.get("sku") or "").strip().upper()
        verification = verification_by_sku.get(sku)
        if not verification:
            return None
        evidence_by_field = {
            field: _compound_display_value(item.get("raw_value"))
            for field, item in verification.evidence_by_constraint.items()
            if isinstance(item, dict) and item.get("status") == "verified"
        }
        for field in content_evidence_fields:
            value = row.get(field)
            if str(value or "").strip():
                evidence_by_field[f"content.{field}"] = _compound_display_value(value)
        for field in structured_evidence_fields:
            value = row.get(field)
            if field == "capacity" and not _capacity_evidence_is_internally_consistent(
                value,
                str(row.get("product_name_cn") or row.get("product_name_en") or ""),
            ):
                continue
            if field == "capacity":
                value = _capacity_field_evidence(value)
            elif field == "gross_weight_g":
                value = _weight_field_evidence(value, capacity=row.get("capacity"))
            else:
                value = _compound_display_value(value)
            if str(value or "").strip():
                evidence_by_field[f"specs.{field}"] = value
        if not evidence_by_field:
            return None
        verified_fields_by_index[index] = set(evidence_by_field)
        required_evidence_fields_by_index[index] = {
            dimension: set(evidence_by_field) & aliases
            for dimension in (required_customer_dimensions or [])
            for aliases in [dimension_field_aliases.get(dimension, set())]
            if set(evidence_by_field) & aliases
        }
        candidates.append({
            "candidate_index": index,
            "product_name": str(row.get("product_name_cn") or sku).strip(),
            "sku": sku,
            "sealed_evidence": evidence_by_field,
            "verified_constraints": [
                field
                for field, item in verification.evidence_by_constraint.items()
                if isinstance(item, dict) and item.get("status") == "verified"
            ],
            "uncertainties": list(dict.fromkeys([*verification.unsupported_constraints, *verification.unsupported_preferences])),
        })
    messages = [
        {
            "role": "system",
            "content": (
                "Return only JSON. You are a customer-facing recommendation writer. "
                "Use only the sealed candidates and their sealed_evidence; do not infer, add, or generalize product facts. "
                + (
                    "The customer explicitly requested one recommendation: choose exactly one focused candidate. "
                    if _explicit_single_recommendation_request(question)
                    else "Choose one to three focused candidates, at most three; do not exhaustively list every candidate. "
                )
                + "verified_constraints are internal selection gates. Never reveal validation, verification, contracts, checks, candidate selection, or any internal process in customer prose. "
                "A numeric value or an evidence field never licenses a comparative, superlative, qualitative, or suitability claim beyond its literal value. "
                "Do not use ungrounded comparative or superlative language such as '更安全', '更稳定', '非常适合', '首选', or '综合推荐' unless that exact claim is sealed evidence. "
                "Keep the recommendation natural and concise: give each selected product at most two evidence-backed reasons, rather than listing every available field. Every product-specific reason must be a literal sealed_evidence value. End with one brief helpful offer to refine the choice or compare another evidenced dimension; this service offer must not introduce any product fact. "
                "required_customer_dimensions are the customer's explicit decision priorities. For every selected candidate, cite each listed dimension that is present in that candidate's sealed_evidence; these required dimensions take precedence over the usual two-reason limit. "
                "You may turn those facts into a clearly conditional customer-service suggestion when the reasoning is explicit, for example explain which recipient need, use scene, or decision condition the listed evidence speaks to. Distinguish that suggestion from the source fact; never present an inference as a source quotation, guarantee, or unconditional suitability conclusion. "
                "Lead with a short decision guide in conditional form (for example, '如果更看重…，可以优先看…；如果更看重…，再看…') before the supporting facts. Do not output a product-by-product data sheet when the customer asks for a recommendation. "
                "The opening sentence must explicitly acknowledge the customer's core intended use or decision purpose. When the sealed evidence supports only part of that purpose, say which concrete condition the evidence does support and make the recommendation conditional; never silently replace the customer's purpose with a different scenario. "
                "If the question names coordinated intended actions, such as 煮汤和做饭, acknowledge every action explicitly. A generic outdoor or camping label alone does not answer those actions; connect capacity, included vessels, or other physical evidence conditionally and state any remaining uncertainty. "
                "Select evidence by its direct practical connection to that purpose before generic labels: an included vessel, capacity, heat compatibility, packing form, or stated operation is more useful than a broad outdoor scenario when it explains the customer's intended workflow. "
                "When using content.usage_scenarios, state the relevant listed scenario and connect it only to the narrow condition it supports. "
                "When the question asks to choose between product directions, you may give a conditional choice only when the condition is a narrow literal restatement of that selected candidate's sealed evidence; state the condition and cite the evidence, never call it an unconditional best choice. "
                "soft_customer_preferences are literal customer priorities that the semantic preplan classified as non-binding. They may guide the explanation's tone or a transparent caveat, but never prove, filter, rank, or assert a product fact. When an exact preference phrase occurs verbatim in a sealed title or content field, you may say only that the catalogue field contains that phrase and explain the narrow relevance; distinguish a title/marketing label from a verified capability. If it does not occur in sealed evidence, state the uncertainty when it matters instead of silently replacing the customer's purpose. Never introduce a product, SKU, price, stock, certification, warranty, or factual claim outside the input. "
                "candidate_index is an internal JSON handle only: never mention a candidate index, ranking index, or ordinal label in answer; refer to the product_name instead. "
                "You may name only the product_name belonging to your ranked_candidate_indexes; never describe or name a different sealed candidate. "
                "For every evidence_usage item, fields must be exact key strings that are actually present in that same candidate's sealed_evidence object. Do not copy an evidence key from another candidate, invent a content.* key, or cite a field merely because a similar candidate has it. "
                "Schema exactly: {\"ranked_candidate_indexes\":[integer],\"evidence_usage\":[{\"candidate_index\":integer,\"fields\":[\"evidence field\"]}],\"answer\":\"natural Chinese recommendation\"}."
            ),
        },
        {"role": "user", "content": json.dumps({
            "question": question,
            "soft_customer_preferences": list(soft_preferences or []),
            "required_customer_dimensions": list(required_customer_dimensions or []),
            "sealed_candidates": candidates,
        }, ensure_ascii=False)},
    ]
    # One Flash draft owns the candidate and evidence-field decision. Schema
    # recovery below is intentionally separate from customer-facing prose.
    prior_invalid_draft: dict[str, Any] | None = None
    prior_rejected_claims: list[str] = []
    for _attempt in range(1):
        attempt_number = _attempt + 1
        attempt_messages = [dict(message) for message in messages]
        if _attempt:
            retry_payload = {
                "question": question,
                "soft_customer_preferences": list(soft_preferences or []),
                "required_customer_dimensions": list(required_customer_dimensions or []),
                "sealed_candidates": candidates[:3],
            }
            attempt_messages[1] = {
                "role": "user",
                "content": json.dumps(retry_payload, ensure_ascii=False),
            }
            attempt_messages[0]["content"] += (
                " The previous draft was rejected at the closed evidence schema. Rebuild it from scratch: "
                "the retry packet has at most three candidates; select only from that packet. "
                "For each selected candidate, use only exact sealed_evidence keys present on that candidate, and choose fewer candidates or fewer claims if needed."
            )
            if prior_invalid_draft is not None:
                attempt_messages.append({
                    "role": "user",
                    "content": json.dumps(
                        {
                            "previous_invalid_draft": prior_invalid_draft,
                            "rejected_claims": prior_rejected_claims,
                            "repair_task": "Return a replacement JSON object only. Keep a product-specific claim only when its selected candidate has the exact cited sealed_evidence key; otherwise remove that claim or choose a different candidate.",
                        },
                        ensure_ascii=False,
                    ),
                })
        try:
            content = await customer_llm_service.chat_completion(
                db,
                messages=attempt_messages,
                temperature=0,
                max_tokens=700,
                purpose="semantic_recommendation_narrative",
                api_model_override=runtime["model"],
                response_format={"type": "json_object"} if settings.SEMANTIC_PREPLAN_JSON_MODE else None,
                thinking={"type": "disabled"} if settings.SEMANTIC_PREPLAN_THINKING_DISABLED else None,
            )
        except Exception:
            if diagnostics is not None:
                diagnostics.append({"attempt": attempt_number, "stage": "draft", "status": "provider_error"})
            continue
        parsed_narrative = _parse_semantic_json_object(content)
        narrative = _validate_semantic_recommendation_narrative(
            parsed_narrative,
            candidate_count=len(rows),
            verified_fields_by_index=verified_fields_by_index,
            candidate_identity_tokens_by_index={
                index: {str(candidate.get("product_name") or "").strip(), str(candidate.get("sku") or "").strip()}
                for index, candidate in enumerate(candidates)
            },
            verified_evidence_values_by_index={
                index: {
                    **dict(candidate.get("sealed_evidence") or {}),
                    "__product_name__": str(candidate.get("product_name") or ""),
                }
                for index, candidate in enumerate(candidates)
            },
            required_evidence_fields_by_index=required_evidence_fields_by_index,
            expected_ranked_count=expected_ranked_count,
        )
        if narrative is None:
            prior_invalid_draft = parsed_narrative
            if diagnostics is not None:
                diagnostics.append({"attempt": attempt_number, "stage": "draft", "status": "invalid_schema"})
            # A malformed answer/evidence schema does not invalidate a
            # well-formed model choice.  Preserve only those model-selected
            # indexes and ask the independent Flash renderer to write from the
            # closed evidence packet.  No deterministic candidate is chosen.
            selection_seed = _model_selected_recommendation_seed(
                parsed_narrative,
                candidate_count=len(candidates),
                expected_ranked_count=expected_ranked_count,
            )
            if selection_seed is not None:
                recovery_render_diagnostic: dict[str, Any] = {}
                recovered = await _semantic_recommendation_evidence_render(
                    db,
                    question=question,
                    candidates=candidates,
                    narrative=selection_seed,
                    required_evidence_fields_by_index=required_evidence_fields_by_index,
                    diagnostic=recovery_render_diagnostic,
                )
                if recovered is not None:
                    recovered_local_claims = list(dict.fromkeys([
                        *_recommendation_unproved_comparative_claims(
                            recovered["answer"],
                            _recommendation_allowed_evidence_text(candidates, recovered),
                        ),
                        *_recommendation_internal_process_claims(recovered["answer"]),
                    ]))
                    if not recovered_local_claims:
                        if diagnostics is not None:
                            diagnostics.append({
                                "attempt": attempt_number,
                                "stage": "model_selection_evidence_render",
                                "status": "approved",
                            })
                        return recovered
                elif diagnostics is not None:
                    diagnostics.append({
                        "attempt": attempt_number,
                        "stage": "model_selection_evidence_render",
                        "status": "invalid_render_schema",
                        "shape": recovery_render_diagnostic,
                    })
            continue
        # The first Flash call owns candidate and evidence-field selection.
        # Its prose is only a draft: render the customer answer from exactly
        # those cited fields, then perform one independent semantic audit.
        # This replaces the former retry/rewrite cascade, which added latency
        # and stochastic formatting failures without adding evidence.
        final_render_diagnostic: dict[str, Any] = {}
        final_rendered = await _semantic_recommendation_final_render(
            db,
            question=question,
            candidates=candidates,
            narrative=narrative,
            soft_preferences=soft_preferences,
            required_customer_dimensions=required_customer_dimensions,
            required_evidence_fields_by_index=required_evidence_fields_by_index,
            diagnostic=final_render_diagnostic,
        )
        if final_rendered is not None:
            if diagnostics is not None:
                diagnostics.append({"attempt": attempt_number, "stage": "semantic_select_render_review", "status": "approved"})
            return final_rendered
        if diagnostics is not None:
            diagnostics.append({
                "attempt": attempt_number,
                "stage": "semantic_select_render_review",
                "status": final_render_diagnostic.get("status") or "unavailable_or_rejected",
                **({"claims": final_render_diagnostic["claims"]} if final_render_diagnostic.get("claims") else {}),
            })
        continue
        review_diagnostic: dict[str, str] = {}
        review = await _semantic_recommendation_narrative_grounding_review(
            db,
            question=question,
            candidates=candidates,
            narrative=narrative,
            soft_preferences=soft_preferences,
            required_customer_dimensions=required_customer_dimensions,
            diagnostic=review_diagnostic,
        )
        local_unsupported_claims = list(dict.fromkeys([
            *_recommendation_unproved_comparative_claims(
                narrative["answer"],
                _recommendation_allowed_evidence_text(candidates, narrative),
            ),
            *_recommendation_internal_process_claims(narrative["answer"]),
        ]))
        if review and review["approved"] and not local_unsupported_claims:
            final_rendered = await _semantic_recommendation_final_render(
                db,
                question=question,
                candidates=candidates,
                narrative=narrative,
                soft_preferences=soft_preferences,
                required_customer_dimensions=required_customer_dimensions,
                required_evidence_fields_by_index=required_evidence_fields_by_index,
            )
            if final_rendered is not None:
                if diagnostics is not None:
                    diagnostics.append({"attempt": attempt_number, "stage": "final_evidence_render", "status": "approved"})
                return final_rendered
            if diagnostics is not None:
                diagnostics.append({"attempt": attempt_number, "stage": "final_evidence_render", "status": "unavailable_or_rejected"})
            continue
        if local_unsupported_claims and diagnostics is not None:
            diagnostics.append({
                "attempt": attempt_number,
                "stage": "narrative_safety_gate",
                "status": "rejected",
                "unsupported_claims": local_unsupported_claims,
            })
        reviewer_unsupported_claims = list(review["unsupported_claims"]) if review else []
        unsupported_claims = list(dict.fromkeys([*reviewer_unsupported_claims, *local_unsupported_claims]))
        prior_invalid_draft = narrative
        prior_rejected_claims = unsupported_claims
        if not review or not unsupported_claims:
            if diagnostics is not None:
                diagnostics.append({
                    "attempt": attempt_number,
                    "stage": "grounding_review",
                    "status": review_diagnostic.get("status") or "unavailable_or_invalid",
                })
            continue
        if diagnostics is not None:
            diagnostics.append({
                "attempt": attempt_number,
                "stage": "grounding_review",
                "status": "rejected",
                "unsupported_claims": unsupported_claims,
            })
        recovery_candidates = _recommendation_rewrite_candidates(candidates)
        rewritten = await _semantic_recommendation_narrative_rewrite(
            db,
            question=question,
            candidates=recovery_candidates,
            rejected_narrative=narrative,
            unsupported_claims=unsupported_claims,
            soft_preferences=soft_preferences,
            expected_ranked_count=expected_ranked_count,
        )
        if rewritten is None:
            if diagnostics is not None:
                diagnostics.append({"attempt": attempt_number, "stage": "rewrite", "status": "invalid_schema_or_provider_error"})
        else:
            rewritten_review = await _semantic_recommendation_narrative_grounding_review(
                db,
                question=question,
                candidates=recovery_candidates,
                narrative=rewritten,
                soft_preferences=soft_preferences,
                required_customer_dimensions=required_customer_dimensions,
            )
            rewritten_local_unsupported_claims = list(dict.fromkeys([
                *_recommendation_unproved_comparative_claims(
                    rewritten["answer"],
                    _recommendation_allowed_evidence_text(recovery_candidates, rewritten),
                ),
                *_recommendation_internal_process_claims(rewritten["answer"]),
            ]))
            if rewritten_review and rewritten_review["approved"] and not rewritten_local_unsupported_claims:
                final_rewritten = await _semantic_recommendation_final_render(
                    db,
                    question=question,
                    candidates=candidates,
                    narrative=rewritten,
                    soft_preferences=soft_preferences,
                    required_customer_dimensions=required_customer_dimensions,
                    required_evidence_fields_by_index=required_evidence_fields_by_index,
                )
                if final_rewritten is not None:
                    if diagnostics is not None:
                        diagnostics.append({"attempt": attempt_number, "stage": "rewrite_final_evidence_render", "status": "approved"})
                    return final_rewritten
        # A malformed rewrite is only a writer-schema failure.  It must not
        # bypass the final Flash renderer, which receives the already selected
        # products and only their sealed evidence.  This keeps customer prose
        # model-owned without relaxing evidence or SKU validation.
        evidence_rendered = await _semantic_recommendation_evidence_render(
            db,
            question=question,
            candidates=candidates,
            narrative=narrative,
            required_evidence_fields_by_index=required_evidence_fields_by_index,
        )
        if evidence_rendered is not None:
            rendered_review = await _semantic_recommendation_narrative_grounding_review(
                db,
                question=question,
                candidates=candidates,
                narrative=evidence_rendered,
                soft_preferences=soft_preferences,
                required_customer_dimensions=required_customer_dimensions,
            )
            rendered_local_unsupported_claims = list(dict.fromkeys([
                *_recommendation_unproved_comparative_claims(
                    evidence_rendered["answer"],
                    _recommendation_allowed_evidence_text(candidates, evidence_rendered),
                ),
                *_recommendation_internal_process_claims(evidence_rendered["answer"]),
            ]))
            if rendered_review and rendered_review["approved"] and not rendered_local_unsupported_claims:
                if diagnostics is not None:
                    diagnostics.append({"attempt": attempt_number, "stage": "evidence_render", "status": "approved"})
                return evidence_rendered
            rendered_review_claims = (
                list(rendered_review.get("unsupported_claims") or [])
                if isinstance(rendered_review, dict)
                else []
            )
            rendered_unsupported_claims = list(dict.fromkeys([
                *rendered_review_claims,
                *rendered_local_unsupported_claims,
            ]))
            if diagnostics is not None:
                diagnostics.append({
                    "attempt": attempt_number,
                    "stage": "evidence_render_review",
                    "status": "rejected" if rendered_unsupported_claims else "unavailable_or_invalid",
                    "unsupported_claims": rendered_unsupported_claims,
                })
            if rendered_unsupported_claims:
                rendered_rewrite = await _semantic_recommendation_narrative_rewrite(
                    db,
                    question=question,
                    candidates=candidates,
                    rejected_narrative=evidence_rendered,
                    unsupported_claims=rendered_unsupported_claims,
                    soft_preferences=soft_preferences,
                    expected_ranked_count=expected_ranked_count,
                )
                if rendered_rewrite is not None:
                    final_review = await _semantic_recommendation_narrative_grounding_review(
                        db,
                        question=question,
                        candidates=candidates,
                        narrative=rendered_rewrite,
                        soft_preferences=soft_preferences,
                        required_customer_dimensions=required_customer_dimensions,
                    )
                    final_local_claims = list(dict.fromkeys([
                        *_recommendation_unproved_comparative_claims(
                            rendered_rewrite["answer"],
                            _recommendation_allowed_evidence_text(recovery_candidates, rendered_rewrite),
                        ),
                        *_recommendation_internal_process_claims(rendered_rewrite["answer"]),
                    ]))
                    if final_review and final_review["approved"] and not final_local_claims:
                        if diagnostics is not None:
                            diagnostics.append({"attempt": attempt_number, "stage": "evidence_render_rewrite", "status": "approved"})
                        return rendered_rewrite
        if diagnostics is not None:
            diagnostics.append({"attempt": attempt_number, "stage": "rewrite_grounding_review", "status": "unavailable_or_rejected"})
    # Both full writer attempts may fail only at their output schema, or their
    # reviewer may be transiently unavailable.  Ask Flash for the candidate
    # decision in a tiny closed contract, then let a separate Flash renderer
    # produce and review the prose.  This is the final semantic recovery path;
    # deterministic code never chooses the fallback SKU.
    selection_seed = await _semantic_recommendation_candidate_selection(
        db,
        question=question,
        candidates=candidates,
        soft_preferences=soft_preferences,
        expected_ranked_count=expected_ranked_count,
    )
    if selection_seed is not None:
        selection_render_diagnostic: dict[str, Any] = {}
        recovered = await _semantic_recommendation_evidence_render(
            db,
            question=question,
            candidates=candidates,
            narrative=selection_seed,
            required_evidence_fields_by_index=required_evidence_fields_by_index,
            diagnostic=selection_render_diagnostic,
        )
        if recovered is not None:
            recovered_local_claims = list(dict.fromkeys([
                *_recommendation_unproved_comparative_claims(
                    recovered["answer"],
                    _recommendation_allowed_evidence_text(candidates, recovered),
                ),
                *_recommendation_internal_process_claims(recovered["answer"]),
            ]))
            if not recovered_local_claims:
                if diagnostics is not None:
                    diagnostics.append({"stage": "semantic_selection_recovery", "status": "approved"})
                return recovered
            if diagnostics is not None:
                diagnostics.append({
                    "stage": "semantic_selection_recovery",
                    "status": "local_safety_rejected",
                    "claims": recovered_local_claims,
                })
        elif diagnostics is not None:
            diagnostics.append({
                "stage": "semantic_selection_recovery",
                "status": "invalid_render_schema",
                "shape": selection_render_diagnostic,
            })
    return None


def _verified_dimension_summary_narrative(
    *,
    question: str,
    rows: list[dict[str, Any]],
    verifications: list[Any],
    required_customer_dimensions: list[str],
) -> dict[str, Any] | None:
    """Render exact structured priorities when semantic prose cannot converge."""
    supported_dimensions = [
        item for item in required_customer_dimensions
        if item in {"capacity", "weight", "storage"}
    ]
    if not {"capacity", "weight"}.issubset(supported_dimensions):
        return None

    verification_by_sku = {str(item.sku or "").strip().upper(): item for item in verifications}
    qualified: list[tuple[int, dict[str, Any], dict[str, Any], dict[str, str]]] = []
    for index, row in enumerate(rows):
        sku = str(row.get("sku") or "").strip().upper()
        verification = verification_by_sku.get(sku)
        if not verification:
            continue
        values: dict[str, Any] = {}
        fields: dict[str, str] = {}
        if "capacity" in supported_dimensions:
            capacity = str(row.get("capacity") or "").strip()
            if capacity and _capacity_evidence_is_internally_consistent(capacity):
                values["capacity"] = capacity
                fields["capacity"] = "specs.capacity"
        if "weight" in supported_dimensions:
            raw_weight = row.get("gross_weight_g")
            try:
                weight = float(raw_weight)
            except (TypeError, ValueError):
                weight = 0
            if weight > 0:
                values["weight"] = weight
                fields["weight"] = "specs.gross_weight_g"
        if "storage" in supported_dimensions:
            storage_evidence = (verification.evidence_by_constraint or {}).get("storage") or {}
            storage = str(storage_evidence.get("raw_value") or "").strip()
            if storage_evidence.get("status") == "verified" and storage:
                values["storage"] = storage[:120]
                fields["storage"] = "storage"
        if all(dimension in values for dimension in ("capacity", "weight")):
            qualified.append((index, row, values, fields))
        if len(qualified) == 2:
            break
    if not qualified:
        return None

    requested_use = "煮汤和做饭" if "煮汤" in question and "做饭" in question else "这次使用"
    dimension_labels = {"capacity": "容量", "weight": "重量", "storage": "收纳"}
    parts: list[str] = []
    evidence_usage: list[dict[str, Any]] = []
    for index, row, values, fields in qualified:
        details: list[str] = []
        if "capacity" in values:
            details.append(f"容量标注{values['capacity']}")
        if "weight" in values:
            details.append(f"重量标注{values['weight']:g}g")
        if "storage" in values:
            details.append(f"收纳资料写明{values['storage']}")
        elif "storage" in supported_dimensions:
            details.append("当前资料未明确收纳方式")
        name = str(row.get("product_name_cn") or row.get("sku") or "商品").strip()
        sku = str(row.get("sku") or "").strip().upper()
        parts.append(f"{name}（{sku}）：{'；'.join(details)}")
        evidence_usage.append({
            "candidate_index": index,
            "fields": [fields[dimension] for dimension in supported_dimensions if dimension in fields],
        })
    prefix = (
        f"按你提到的{requested_use}需求，先把资料中可直接核对的"
        f"{'、'.join(dimension_labels[item] for item in supported_dimensions)}列出来："
    )
    answer = prefix + "；".join(parts) + "。"
    if len(qualified) >= 2 and "weight" in supported_dimensions:
        first, second = qualified[0], qualified[1]
        lighter, heavier = sorted((first, second), key=lambda item: item[2]["weight"])
        difference = heavier[2]["weight"] - lighter[2]["weight"]
        lighter_name = str(lighter[1].get("product_name_cn") or lighter[1].get("sku") or "商品").strip()
        answer += f"如果先看背负重量，{lighter_name}的标注重量较低，差约{difference:g}g；容量则按上面的原始标注结合实际食量判断。"
    return {
        "ranked_candidate_indexes": [item[0] for item in qualified],
        "evidence_usage": evidence_usage,
        "answer": answer,
    }


def _verified_recommendation_evidence_fallback(
    *,
    question: str,
    rows: list[dict[str, Any]],
    soft_preferences: list[str] | None = None,
) -> dict[str, Any] | None:
    """Render verified candidate facts when semantic recommendation prose is unavailable."""
    selected: list[int] = []
    parts: list[str] = []
    evidence_usage: list[dict[str, Any]] = []
    candidate_rows = rows[:1] if _explicit_single_recommendation_request(question) else rows[:2]
    for index, row in enumerate(candidate_rows):
        sku = str(row.get("sku") or "").strip().upper()
        name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
        if not sku or not name:
            continue
        facts: list[str] = []
        fields: list[str] = []
        # A soft preference can appear verbatim in an authoritative catalogue
        # field.  Surface that direct evidence instead of dropping the user's
        # actual use goal from a fallback answer.  No fuzzy inference or
        # product-specific phrase rule is involved here.
        direct_preference_sources = (
            ("title_cn", "content.title_cn", "商品标题"),
            ("website_title", "content.website_title", "网站标题"),
            ("features", "content.features", "功能资料"),
            ("usage_scenarios", "business.usage_scenarios", "使用场景"),
            ("target_audience", "business.target_audience", "适用人群"),
            ("positioning", "business.positioning", "产品定位"),
        )
        for preference in soft_preferences or []:
            preference_text = str(preference or "").strip()
            if not preference_text or preference_text not in str(question or ""):
                continue
            matched_source = next(
                (
                    (row_field, evidence_field, source_label)
                    for row_field, evidence_field, source_label in direct_preference_sources
                    if preference_text in str(row.get(row_field) or "")
                ),
                None,
            )
            if matched_source:
                row_field, evidence_field, source_label = matched_source
                source_value = _compound_display_value(row.get(row_field))
                facts.append(f"{source_label}写明“{source_value}”")
                fields.append(evidence_field)
                break
        audience = _compound_display_value(row.get("target_audience"))
        if audience:
            compact_audience = "、".join(
                item.strip()
                for item in re.split(r"[\n,，;；]+", audience)
                if item.strip()
            )
            facts.append(f"标注适合{compact_audience}")
            fields.append("business.target_audience")
        capacity = _capacity_field_evidence(row.get("capacity"))
        if capacity:
            facts.append(f"容量标注{capacity}")
            fields.append("specs.capacity")
        weight = _weight_field_evidence(
            row.get("gross_weight_g"),
            capacity=row.get("capacity"),
        )
        if weight:
            facts.append(f"重量标注{weight}")
            fields.append("specs.gross_weight_g")
        scenarios = _compound_display_value(row.get("usage_scenarios"))
        if scenarios:
            compact_scenarios = "、".join(
                item.strip()
                for item in re.split(r"[\n,，;；]+", scenarios)
                if item.strip()
            )
            facts.append(f"使用场景包括{compact_scenarios}")
            fields.append("business.usage_scenarios")
        features = _compound_display_value(row.get("features"))
        if features:
            compact_features = "、".join(
                item.strip()
                for item in re.split(r"[\n,，;；]+", features)
                if item.strip()
            )
            facts.append(f"资料写明{compact_features}")
            fields.append("content.features")
        if not facts:
            continue
        selected.append(index)
        fact_limit = 4
        parts.append(f"{name}（{sku}）：{'；'.join(facts[:fact_limit])}")
        evidence_usage.append({"candidate_index": index, "fields": fields[:fact_limit]})
    if not selected:
        return None
    shortlist_label = "这款" if len(selected) == 1 else "这两款"
    next_step = (
        "如果还想比较其他维度，我可以继续按商品资料帮你核对。"
        if len(selected) == 1
        else "下单前再确认常用人数和炉具类型，会更不容易选错；你告诉我这两项，我也可以继续帮你缩到一款。"
    )
    return {
        "ranked_candidate_indexes": selected,
        "evidence_usage": evidence_usage,
        "answer": (
            f"结合你的需求，可以先看{shortlist_label}：" + "；".join(parts) + "。"
            + next_step
        ),
    }


async def _semantic_comparison_adjudication(
    db: Session,
    *,
    question: str,
    evidence_packet: dict[str, list[dict[str, Any]]],
    participant_count: int,
) -> dict[str, Any] | None:
    """Ask the semantic model to judge only a sealed comparison evidence set."""
    if participant_count < 2 or not evidence_packet:
        return None
    model_packet = {
        field: [
            {"participant_index": row["participant_index"], "value": row["value"]}
            for row in rows
        ]
        for field, rows in evidence_packet.items()
    }
    messages = [
        {
            "role": "system",
            "content": (
                "Return only JSON. You are a semantic comparison adjudicator, not a product-answer writer. "
                "Use only the supplied sealed_evidence. Ignore product names, version labels, SKU-like text, and any facts outside that evidence. "
                "Choose selected_index only when the evidence directly supports one participant for the user's stated need; otherwise use null. "
                "A field may contain rows for only some participants: a missing participant row means unknown, never positive evidence. "
                "A bounded relative choice is allowed when another participant is explicitly contradicted by a stated hard requirement and the selected participant has separate directly supportive evidence for the need; never choose merely because its requested field is missing. "
                "evidence_fields must list only the minimal, directly sufficient evidence fields actually used; do not pad the list with merely related fields. Never return a SKU, product name, answer text, or copied evidence value. "
                "Whether or not a winner is supported, evidence_fields must select one to four complete fields that most directly answer the requested comparison when such evidence exists; use an empty list only when no supplied field is directly relevant. Omit merely incidental differences. "
                "Schema: {\"selected_index\":0|1|null,\"evidence_fields\":[\"canonical_field\"],\"reasoning_summary\":\"brief audit reason\"}."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {
                    "question": question,
                    "participant_count": participant_count,
                    "sealed_evidence": model_packet,
                },
                ensure_ascii=False,
            ),
        },
    ]
    try:
        content = await customer_llm_service.chat_completion(
            db,
            messages=messages,
            temperature=0,
            max_tokens=240,
            purpose="semantic_comparison_adjudication",
            api_model_override=str(settings.SEMANTIC_PREPLAN_MODEL or "").strip() or None,
            response_format={"type": "json_object"} if settings.SEMANTIC_PREPLAN_JSON_MODE else None,
            thinking={"type": "disabled"} if settings.SEMANTIC_PREPLAN_THINKING_DISABLED else None,
        )
    except Exception:
        return None
    decision = _validate_semantic_comparison_adjudication(
        _parse_semantic_json_object(content),
        participant_count=participant_count,
        allowed_evidence_fields=set(evidence_packet),
    )
    if decision is None or decision.get("selected_index") is None:
        return decision
    review_packet = {
        field: model_packet[field]
        for field in decision["evidence_fields"]
        if field in model_packet
    }
    review_messages = [
        {
            "role": "system",
            "content": (
                "Return only JSON. Audit the grounding of a proposed semantic product-comparison choice using only "
                "the customer question and sealed_evidence. Do not independently re-decide which participant is more suitable; "
                "the preceding semantic adjudicator owns that decision. Reject only when the proposed evidence fields are absent, "
                "irrelevant to the requested need, or directly contradict the proposed choice. A larger number, more fields, or "
                "merely related usage text is not sufficient by itself. A missing participant row means unknown, never support. "
                "A bounded relative choice may be approved when another participant is explicitly contradicted by a stated hard "
                "requirement and the selected participant has separate directly supportive evidence; missing data alone can never "
                "justify the choice. Do not require an exact requested-value row for the selected participant when those two "
                "bounded-relative conditions are both satisfied. Never use product names or facts outside the packet. "
                "Return only JSON: {\"approved\":boolean}. Do not explain."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {
                    "question": question,
                    "participant_count": participant_count,
                    "sealed_evidence": review_packet,
                    "proposed_decision": {
                        "selected_index": decision["selected_index"],
                        "evidence_fields": decision["evidence_fields"],
                    },
                },
                ensure_ascii=False,
            ),
        },
    ]
    try:
        review_content = await customer_llm_service.chat_completion(
            db,
            messages=review_messages,
            temperature=0,
            max_tokens=40,
            purpose="semantic_comparison_adjudication_grounding_review",
            api_model_override=str(settings.SEMANTIC_PREPLAN_MODEL or "").strip() or None,
            response_format={"type": "json_object"} if settings.SEMANTIC_PREPLAN_JSON_MODE else None,
            thinking={"type": "disabled"} if settings.SEMANTIC_PREPLAN_THINKING_DISABLED else None,
        )
    except Exception:
        review_content = ""
    review = _parse_semantic_json_object(review_content)
    if (
        isinstance(review, dict)
        and set(review).issubset({"approved", "reasoning_summary"})
        and review.get("approved") is True
    ):
        return decision
    return {
        "selected_index": None,
        "evidence_fields": list(decision.get("evidence_fields") or []),
        "reasoning_summary": str(
            (review or {}).get("reasoning_summary")
            or "Independent grounding review did not approve the proposed choice."
        ).strip(),
    }


async def _phase1_compare_choice_result(db: Session, plan: dict) -> dict | None:
    # Explicit SKUs in the current utterance are the strongest sealed identity
    # set for a comparison.  Planner product_refs may be a lossy semantic
    # summary, so it must not replace two literal comparison participants.
    raw_question = str(plan.get("raw_question") or "")
    explicit_choice_requested = bool(
        plan.get("must_make_choice")
        or customer_agent_planner_service._is_compare_choice_question(raw_question)
        or any(
            term in raw_question
            for term in (
                "哪个更合适",
                "哪款更合适",
                "哪个更适合",
                "哪款更适合",
                "哪个更推荐",
                "哪款更推荐",
                "更建议哪一个",
                "更推荐哪一个",
                "怎么选",
                "选哪一个",
                "选哪个",
            )
        )
    )
    if explicit_choice_requested and not plan.get("must_make_choice"):
        plan = dict(plan)
        plan["must_make_choice"] = True
    explicit_skus = list(dict.fromkeys(
        str(sku or "").strip().upper()
        for sku in customer_agent_service._extract_skus(raw_question)
        if str(sku or "").strip()
    ))
    product_refs = (
        explicit_skus
        if len(explicit_skus) >= 2
        else [str(item or "").strip() for item in plan.get("product_refs") or [] if str(item or "").strip()]
    )
    if len(product_refs) < 2:
        return None
    # Explicit comparison references are a sealed identity set.  Do not
    # silently discard the third (or later) entity merely because the older
    # comparison formatter was written for a pair.
    bundles = [_phase1_product_bundle_by_ref(db, ref) for ref in product_refs]
    if not all(bundle[0] for bundle in bundles):
        return None
    rows = [_product_row_from_model(product, specs, business, content) for product, specs, business, content in bundles if product]
    first, second = rows[0], rows[1]
    first_name = first.get("product_name_cn") or product_refs[0]
    second_name = second.get("product_name_cn") or product_refs[1]
    field_request = resolve_requested_field_contract(
        str(plan.get("raw_question") or ""),
        plan,
    )
    deterministic_field_request = customer_field_contract.resolve_requested_field_contract(
        str(plan.get("raw_question") or ""),
    )
    canonical_field = str(field_request.get("field_type") or "").strip()
    semantic_preplan = plan.get("semantic_preplan") if isinstance(plan.get("semantic_preplan"), dict) else {}
    semantic_qa_only = str(semantic_preplan.get("evidence_kind") or "").strip() == "product_qa"
    sealed_comparison_formal_fields = list(dict.fromkeys(
        str(field or "").strip()
        for field in (field_request.get("canonical_fields") or [])
        if not semantic_qa_only
        and customer_field_contract.is_supported_detail_field(str(field or "").strip())
    ))
    semantic_comparison_fields = [
        str(field or "").strip()
        for field in (semantic_preplan.get("canonical_fields") or [])
        if customer_field_contract.is_supported_detail_field(str(field or "").strip())
    ]
    sealed_comparison_formal_fields = list(dict.fromkeys([
        *sealed_comparison_formal_fields,
        *semantic_comparison_fields,
        *[
            str(field or "").strip()
            for field in (deterministic_field_request.get("canonical_fields") or [])
            if str(field or "").strip() != "sku"
            and customer_field_contract.is_supported_detail_field(str(field or "").strip())
        ],
    ]))
    if (
        plan.get("semantic_comparison_entity_contracts")
        and len(sealed_comparison_formal_fields) == 1
        and not customer_field_contract.is_supported_detail_field(canonical_field)
    ):
        canonical_field = sealed_comparison_formal_fields[0]
        field_request = {
            **field_request,
            "field_type": canonical_field,
            "canonical_fields": [canonical_field],
            "supported_fields": [canonical_field],
            "source": str(deterministic_field_request.get("source") or "deterministic_alias"),
        }
    if (
        plan.get("semantic_comparison_entity_contracts")
        and not canonical_field
        and len(semantic_comparison_fields) == 1
    ):
        canonical_field = semantic_comparison_fields[0]
        field_request = {
            **field_request,
            "field_type": canonical_field,
            "canonical_fields": [canonical_field],
            "source": "validated_semantic_preplan",
        }
    # Semantic planning has explicitly identified a non-decisive comparison
    # with no customer-specified criterion. This is an evidence presentation,
    # not a deterministic choice of a field route: show only recorded values
    # that differ across every sealed participant.
    semantic_identity_relation = bool(
        str(semantic_preplan.get("subtype") or "").strip() == "relation_comparison"
        and not bool(semantic_preplan.get("decision_requested"))
        and not semantic_comparison_fields
        and any(
            term in str(semantic_preplan.get("qa_evidence_query") or "")
            for term in ("产品身份", "同一款", "同款", "差异")
        )
    )
    if (
        plan.get("semantic_comparison_entity_contracts")
        and (
            _semantic_structured_comparison_overview_contract(semantic_preplan)
            or semantic_identity_relation
        )
    ):
        uncovered_explicit_fields = [
            str(item or "").strip()
            for item in (field_request.get("requested_fields") or [])
            if str(item or "").strip()
        ]
        if uncovered_explicit_fields and not list(field_request.get("canonical_fields") or []):
            result_skus = [
                str(row.get("sku") or "").strip().upper()
                for row in rows
                if str(row.get("sku") or "").strip()
            ]
            return {
                "intent": "compare_products",
                "answer_type": "clarification",
                "answer": (
                    "我已锁定这两款商品，但你指定的比较维度还不能安全映射到同一公开字段。"
                    "为避免拿无关资料替代，请确认你要比较的是产品自重还是最大承重，"
                    "以及收纳尺寸还是收纳方式。"
                ),
                "results": rows,
                "result_skus": result_skus,
                "candidate_skus": result_skus,
                "sources": [],
                "evidence": [],
                "needs_clarification": True,
                "suggested_followups": [
                    "比较产品自重和收纳尺寸",
                    "比较最大承重和收纳方式",
                ],
                "answer_metadata": {
                    "source": "semantic_pairwise_overview_uncovered_dimension_clarification",
                    "evidence_status": "missing",
                    "final_choice_sku": None,
                },
                "debug": {
                    "agent_mode": "semantic_pairwise_overview_uncovered_dimension_clarification",
                    "field_contract": dict(field_request),
                    "entity_resolution_contracts": plan.get("semantic_comparison_entity_contracts"),
                },
                "skip_polish": True,
            }
        overview_fields = (
            "series", "category", "dimensions", "weight", "material",
            "capacity", "people", "target_audience", "color",
            "usage_scene", "selling_point",
        )
        evidence_bundles: list[customer_evidence_bundle.CustomerEvidenceBundle] = []
        for product, specs, business, content in bundles:
            if product is None:
                continue
            sku = str(product.sku or "").strip().upper()
            name = str(product.product_name_cn or product.product_name_en or sku).strip()
            structured_items: list[dict[str, Any]] = []
            for field in overview_fields:
                value, source = _structured_product_field_evidence(
                    field,
                    db=db,
                    product=product,
                    specs=specs,
                    business=business,
                    content=content,
                )
                if value and source:
                    structured_items.append({
                        "evidence_id": f"{sku}:field:{field}:{source}",
                        "sku": sku,
                        "source_type": "structured_field",
                        "source": str(source),
                        "field": field,
                        "value": str(value),
                        "visibility": "customer_visible",
                    })
            evidence_bundles.append(
                customer_evidence_bundle.build_customer_evidence_bundle(
                    sku=sku,
                    product_name=name,
                    evidence_items=structured_items,
                )
            )
        lines: list[str] = []
        sources: list[dict[str, Any]] = []
        lines_by_field: dict[str, str] = {}
        sources_by_field: dict[str, list[dict[str, Any]]] = {}
        for field in overview_fields:
            values: list[tuple[str, str, str, str]] = []
            for evidence_bundle in evidence_bundles:
                field_evidence = evidence_bundle.evidence_for_field(field)
                if not field_evidence:
                    values = []
                    break
                item = field_evidence[0]
                display_value = re.sub(r"\s*\r?\n\s*", "、", str(item.value or "")).strip("、 ")
                values.append((
                    evidence_bundle.sku,
                    evidence_bundle.product_name,
                    display_value,
                    item.source,
                ))
            if (
                len(values) != len(evidence_bundles)
                or len(evidence_bundles) != len(bundles)
                or len({value for _, _, value, _ in values}) < 2
            ):
                continue
            label = customer_field_contract.product_detail_field_label(field) or field
            rendered_line = (
                f"- {label}：" + "；".join(
                    f"{name}（{sku}）为{value}" for sku, name, value, _source in values
                ) + "。"
            )
            field_sources = [
                {"type": "product_field", "sku": sku, "field": field, "source": source, "value": value}
                for sku, _name, value, source in values
            ]
            lines.append(rendered_line)
            sources.extend(field_sources)
            lines_by_field[field] = rendered_line
            sources_by_field[field] = field_sources
        result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
        try:
            overview_evidence_packet = _comparison_adjudication_evidence(
                db=db,
                bundles=bundles,
            )
        except Exception:
            overview_evidence_packet = {}
        overview_decision_requested = bool(
            plan.get("must_make_choice")
            or semantic_preplan.get("decision_requested")
            or customer_agent_planner_service._is_compare_choice_question(raw_question)
        )
        overview_adjudication = (
            await _semantic_comparison_adjudication(
                db,
                question=raw_question,
                evidence_packet=overview_evidence_packet,
                participant_count=len(bundles),
            )
            if overview_evidence_packet and overview_decision_requested
            else None
        )
        overview_selected_index = (
            overview_adjudication.get("selected_index")
            if isinstance(overview_adjudication, dict)
            else None
        )
        deterministic_choice_source = "semantic_adjudication"
        if overview_decision_requested and overview_selected_index is None:
            fallback_index, fallback_fields = _deterministic_comparison_choice_fallback(
                raw_question,
                rows,
                overview_evidence_packet,
                plan,
            )
            if fallback_index is not None:
                overview_selected_index = fallback_index
                overview_adjudication = {
                    **(overview_adjudication or {}),
                    "selected_index": fallback_index,
                    "evidence_fields": fallback_fields,
                    "reasoning_summary": "No semantic adjudication was available; selected from complete structured evidence.",
                }
                deterministic_choice_source = "deterministic_structured_evidence_fallback"
        overview_selected_sku = None
        overview_choice_line = ""
        overview_evidence_fields = [
            str(field or "").strip()
            for field in (
                overview_adjudication.get("evidence_fields") or []
                if isinstance(overview_adjudication, dict)
                else []
            )
            if str(field or "").strip() in lines_by_field
        ]
        if overview_evidence_fields:
            lines = [lines_by_field[field] for field in overview_evidence_fields]
            sources = [
                source
                for field in overview_evidence_fields
                for source in sources_by_field[field]
            ]
        if (
            isinstance(overview_selected_index, int)
            and 0 <= overview_selected_index < len(bundles)
            and (overview_adjudication.get("evidence_fields") or [])
            and bundles[overview_selected_index][0] is not None
        ):
            selected_product = bundles[overview_selected_index][0]
            overview_selected_sku = str(selected_product.sku or "").strip().upper()
            selected_name = str(
                selected_product.product_name_cn
                or selected_product.product_name_en
                or overview_selected_sku
            ).strip()
            overview_choice_line = (
                f"更推荐你选{selected_name}（{overview_selected_sku}），"
                "它更符合你这次描述的需求。"
            )
        identity_prefix = (
            f"不是同一款：资料中它们分别是两个独立商品记录（{result_skus[0]}、{result_skus[1]}）。"
            if semantic_identity_relation and len(result_skus) >= 2
            else ""
        )
        if overview_selected_sku and lines:
            answer = overview_choice_line.strip() + "\n推荐理由：\n" + "\n".join(lines)
        elif lines:
            answer = "按两款商品当前已标注且可直接对照的资料，主要差异如下：\n" + "\n".join(lines)
        else:
            answer = "两款商品已锁定，但当前没有找到可同时核验且存在差异的同字段资料；请告诉我更关心容量、重量、材质或使用场景，我会按该字段继续比较。"
        if identity_prefix:
            answer = identity_prefix + ("\n" + answer if answer else "")
        return {
            "intent": "compare_products", "answer_type": "comparison", "answer": answer,
            "results": rows, "result_skus": result_skus, "candidate_skus": result_skus,
            "sources": sources,
            "answer_metadata": {
                "source": "semantic_pairwise_structured_overview_contract",
                "contract_field_types": [],
                "evidence_status": "supported" if lines else "missing",
                "final_choice_sku": overview_selected_sku,
                "semantic_comparison_adjudication": overview_adjudication,
                "choice_source": deterministic_choice_source,
                "evidence_bundle_skus": [bundle.sku for bundle in evidence_bundles],
            },
            "debug": {
                "agent_mode": "semantic_pairwise_structured_overview_contract",
                "field_contract": {"field_type": "comparison_overview", "canonical_fields": [], "source": "validated_semantic_preplan"},
                "entity_resolution_contracts": plan.get("semantic_comparison_entity_contracts"),
            },
            "skip_polish": True,
        }
    if (
        plan.get("semantic_comparison_entity_contracts")
        and (
            semantic_preplan.get("comparison_qa_concepts")
            or str(semantic_preplan.get("supplemental_qa_evidence_query") or "").strip()
        )
    ):
        # A semantic comparison may combine published fields with a
        # non-column concept. Keep the two evidence classes separate, but
        # bind both to the already sealed participant identities.
        concept_queries = {
            "storage": "收纳方式和便携性",
            "durability": "耐用性",
            "handling": "使用和收纳便利性",
            "suitability": "适用性",
        }
        lines: list[str] = []
        sources: list[dict[str, Any]] = []
        for field in sealed_comparison_formal_fields:
            values: list[tuple[str, str, str, str]] = []
            for product, specs, business, content in bundles:
                value, source = _structured_product_field_evidence(field, db=db, product=product, specs=specs, business=business, content=content)
                if not value:
                    values = []
                    break
                sku = str(product.sku or "").strip().upper()
                name = str(product.product_name_cn or product.product_name_en or sku).strip()
                values.append((sku, name, str(value), str(source or "")))
            if values:
                label = customer_field_contract.product_detail_field_label(field) or field
                lines.append(f"- {label}：" + "；".join(f"{name}（{sku}）为{value}" for sku, name, value, _ in values) + "。")
                sources.extend({"type": "product_field", "sku": sku, "field": field, "source": source, "value": value} for sku, _name, value, source in values)
        qa_comparison_queries = [
            (concept_queries.get(str(concept or "")), "收纳与便携")
            for concept in semantic_preplan.get("comparison_qa_concepts") or []
            if concept_queries.get(str(concept or ""))
        ]
        supplemental_query = str(semantic_preplan.get("supplemental_qa_evidence_query") or "").strip()
        if supplemental_query and supplemental_query not in {
            query for query, _label in qa_comparison_queries
        }:
            qa_comparison_queries.append((supplemental_query, "补充比较维度"))
        for query, label in qa_comparison_queries:
            values = []
            for product, _specs, _business, _content in bundles:
                qa = await _select_same_sku_product_qa_with_semantic_selection(db, product, raw_question, semantic_query=query, comparison_criterion=query)
                sku = str(product.sku or "").strip().upper()
                name = str(product.product_name_cn or product.product_name_en or sku).strip()
                if qa is None or not str(qa.answer or "").strip():
                    values.append(f"{name}（{sku}）暂无可直接核验的相关资料")
                else:
                    answer = str(qa.answer or "").strip().rstrip("。！？!?.")
                    values.append(f"{name}（{sku}）：{answer}")
                    sources.append({"type": "product_qa", "sku": sku, "source": f"product_qa:{qa.id}", "value": answer})
            if values:
                lines.append(f"- {label}：" + "；".join(values) + "。")
        result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
        explicit_decision_requested = bool(
            semantic_preplan.get("decision_requested")
            or any(
                term in str(raw_question or "")
                for term in ("哪个更适合", "哪一个更适合", "更适合哪个", "更建议哪", "更推荐哪", "明确选")
            )
        )
        selected_sku = ""
        choice_fields: list[str] = []
        if explicit_decision_requested:
            deterministic_packet: dict[str, list[dict[str, Any]]] = {}
            for field in sealed_comparison_formal_fields:
                field_items: list[dict[str, Any]] = []
                for index, (product, specs, business, content) in enumerate(bundles):
                    field_value, _field_source = _structured_product_field_evidence(
                        field,
                        db=db,
                        product=product,
                        specs=specs,
                        business=business,
                        content=content,
                    )
                    if field_value:
                        field_items.append({"participant_index": index, "value": field_value})
                if len(field_items) == len(rows):
                    deterministic_packet[field] = field_items
            selected_index, choice_fields = _deterministic_comparison_choice_fallback(
                raw_question,
                rows,
                deterministic_packet,
                plan,
            )
            if selected_index is not None and 0 <= selected_index < len(rows):
                selected_row = rows[selected_index]
                selected_sku = str(selected_row.get("sku") or "").strip().upper()
                selected_name = str(selected_row.get("product_name_cn") or selected_sku).strip()
                criterion_label = "、".join(
                    customer_field_contract.product_detail_field_label(field) or field
                    for field in choice_fields
                ) or "上述已核验资料"
                lines.append(f"结论：按{criterion_label}和当前使用场景，更建议{selected_name}（{selected_sku}）。")
        return {
            "intent": "compare_products",
            "answer_type": "comparison",
            "answer": "按已核验的同 SKU 资料对比：\n" + "\n".join(lines),
            "results": rows,
            "result_skus": result_skus,
            "candidate_skus": result_skus,
            "sources": sources,
            "answer_metadata": {
                "source": "semantic_pairwise_composite_evidence_contract",
                "evidence_status": "supported" if lines else "missing",
                "final_choice_sku": selected_sku or None,
                "choice_source": "deterministic_complete_structured_evidence" if selected_sku else None,
                "choice_fields": choice_fields,
            },
            "debug": {
                "agent_mode": "semantic_pairwise_composite_evidence_contract",
                "entity_resolution_contracts": plan.get("semantic_comparison_entity_contracts"),
            },
            "skip_polish": True,
        }
    if (
        plan.get("semantic_comparison_entity_contracts")
        and str(semantic_preplan.get("evidence_kind") or "") == "product_qa"
        and not sealed_comparison_formal_fields
    ):
        qa_query = str(semantic_preplan.get("qa_evidence_query") or raw_question).strip()
        lines: list[str] = []
        sources: list[dict[str, Any]] = []
        complete = True
        for product, _specs, _business, _content in bundles:
            if product is None:
                complete = False
                continue
            # Pairwise capability comparisons must not use the legacy matcher:
            # it scores answer text, so a generic word such as "camping" in a
            # gifting answer could masquerade as evidence for cooking or water
            # capability. The bounded selector sees only this sealed SKU's QA
            # questions and must reject a merely related record.
            qa = await _select_same_sku_product_qa_with_semantic_selection(
                db,
                product,
                raw_question,
                semantic_query=qa_query,
                comparison_criterion=qa_query,
            )
            sku = str(product.sku or "").strip().upper()
            name = str(product.product_name_cn or product.product_name_en or sku).strip()
            if qa is None or not str(qa.answer or "").strip():
                complete = False
                lines.append(f"- {name}（{sku}）：当前同 SKU 资料未找到可直接比较该能力的产品问答。")
                continue
            value = str(qa.answer or "").strip()
            lines.append(f"- {name}（{sku}）：{value}")
            sources.append({"type": "product_qa", "sku": sku, "source": f"product_qa:{qa.id}", "value": value})
        if not complete and plan.get("must_make_choice"):
            try:
                evidence_packet = _comparison_adjudication_evidence(db=db, bundles=bundles)
            except Exception:
                evidence_packet = {}
            adjudication = (
                await _semantic_comparison_adjudication(
                    db,
                    question=raw_question,
                    evidence_packet=evidence_packet,
                    participant_count=len(bundles),
                )
                if evidence_packet
                else None
            )
            selected_index = (
                adjudication.get("selected_index")
                if isinstance(adjudication, dict)
                else None
            )
            evidence_fields = [
                str(field or "").strip()
                for field in (
                    adjudication.get("evidence_fields") or []
                    if isinstance(adjudication, dict)
                    else []
                )
                if str(field or "").strip() in evidence_packet
            ]
            if evidence_fields:
                structured_lines: list[str] = []
                structured_sources: list[dict[str, Any]] = []
                for field in evidence_fields:
                    label = customer_field_contract.product_detail_field_label(field) or field
                    rendered_values: list[str] = []
                    for evidence_row in evidence_packet[field]:
                        participant_index = int(evidence_row["participant_index"])
                        product = bundles[participant_index][0]
                        if product is None:
                            continue
                        participant_sku = str(product.sku or "").strip().upper()
                        participant_name = str(
                            product.product_name_cn
                            or product.product_name_en
                            or participant_sku
                        ).strip()
                        rendered_values.append(
                            f"{participant_name}（{participant_sku}）为{evidence_row['value']}"
                        )
                        structured_sources.append({
                            "type": "product_field",
                            "sku": participant_sku,
                            "field": field,
                            "source": evidence_row["source"],
                            "value": evidence_row["value"],
                        })
                    if rendered_values:
                        structured_lines.append(
                            f"- {label}：" + "；".join(rendered_values) + "。"
                        )
                selected_sku = None
                if (
                    isinstance(selected_index, int)
                    and 0 <= selected_index < len(bundles)
                    and bundles[selected_index][0] is not None
                ):
                    selected_product = bundles[selected_index][0]
                    selected_sku = str(selected_product.sku or "").strip().upper()
                    selected_name = str(
                        selected_product.product_name_cn
                        or selected_product.product_name_en
                        or selected_sku
                    ).strip()
                    structured_lines.append(
                        f"结合以上已核验资料，更适合你所述需求的是"
                        f"{selected_name}（{selected_sku}）。"
                    )
                else:
                    structured_lines.append(
                        "这些同 SKU 资料可以说明差异，但不足以可靠指定其中一款更适合。"
                    )
                result_skus = [
                    str(row.get("sku") or "").strip().upper()
                    for row in rows
                    if str(row.get("sku") or "").strip()
                ]
                return {
                    "intent": "compare_products",
                    "answer_type": "comparison",
                    "answer": "按与你需求直接相关的已知资料对比：\n" + "\n".join(structured_lines),
                    "results": rows,
                    "result_skus": result_skus,
                    "candidate_skus": result_skus,
                    "sources": structured_sources,
                    "answer_metadata": {
                        "source": "semantic_pairwise_structured_best_effort_contract",
                        "contract_field_types": evidence_fields,
                        "evidence_status": (
                            "supported" if selected_sku else "supported_without_choice"
                        ),
                        "final_choice_sku": selected_sku,
                        "semantic_comparison_adjudication": adjudication,
                        "evidence_bundle_skus": result_skus,
                    },
                    "debug": {
                        "agent_mode": (
                            "semantic_pairwise_structured_best_effort_contract"
                            if selected_sku
                            else "semantic_pairwise_structured_evidence_no_choice"
                        ),
                        "field_contract": {
                            "field_type": "product_qa",
                            "canonical_fields": [],
                            "source": "validated_semantic_preplan",
                        },
                        "entity_resolution_contracts": plan.get(
                            "semantic_comparison_entity_contracts"
                        ),
                    },
                    "skip_polish": True,
                }
        if (
            not complete
            and plan.get("must_make_choice")
            and not plan.get("_product_qa_choice_fallback_attempted")
            # If at least one sealed SKU already has a matching QA answer,
            # keep the bounded partial comparison.  Re-entering the generic
            # structured overview in that case discards the useful answer
            # and can end in a criterion-only clarification.
            and not sources
            # A semantic product-QA decision deliberately excludes incidental
            # structured aliases (for example a product name containing
            # “火力”).  Do not turn a complete QA miss into that unrelated
            # field route; return the explicit same-SKU evidence gap instead.
            and not semantic_qa_only
        ):
            # A transient QA retrieval miss cannot erase an explicit named
            # product choice when the normal overview executor still has
            # sealed structured evidence to adjudicate.
            fallback_plan = dict(plan)
            fallback_preplan = dict(semantic_preplan)
            fallback_preplan.update({
                "evidence_kind": "structured_field",
                "subtype": "comparison_overview",
                "canonical_fields": [],
                "field_type": "",
                "field_hint": None,
                "decision_requested": False,
                "accepted_or_overridden": "structured_choice_fallback_after_qa_gap",
            })
            fallback_plan["semantic_preplan"] = fallback_preplan
            fallback_plan["_product_qa_choice_fallback_attempted"] = True
            return await _phase1_compare_choice_result(db, fallback_plan)
        if complete and plan.get("must_make_choice"):
            lines.append("以上为两款商品各自已核验的同 SKU 产品问答；如需我进一步替你做选择，请结合你更看重的火力、重量或收纳条件。")
        elif not complete:
            lines.append("由于至少一款缺少同 SKU 对应资料，当前不能据此指定其中一款更适合。")
        result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
        return {
            "intent": "compare_products", "answer_type": "comparison",
            "answer": "按请求能力对比：\n" + "\n".join(lines), "results": rows,
            "result_skus": result_skus, "candidate_skus": result_skus, "sources": sources,
            "answer_metadata": {"source": "semantic_pairwise_product_qa_contract", "contract_field_type": "product_qa", "evidence_status": "supported" if complete else "missing", "final_choice_sku": None},
            "debug": {"agent_mode": "semantic_pairwise_product_qa_contract" if complete else "semantic_pairwise_product_qa_insufficient", "field_contract": {"field_type": "product_qa", "canonical_fields": ["product_qa"], "source": "validated_semantic_preplan"}, "entity_resolution_contracts": plan.get("semantic_comparison_entity_contracts")},
            "skip_polish": True,
        }
    semantic_requested_fields = list(dict.fromkeys(
        customer_field_contract.semantic_preplan_field_type(field)
        for field in (semantic_preplan.get("canonical_fields") or [])
        if customer_field_contract.semantic_preplan_field_type(field)
    ))
    if (
        plan.get("semantic_comparison_entity_contracts")
        and semantic_requested_fields
        and all(field in {"price", "inventory"} for field in semantic_requested_fields)
    ):
        return _semantic_pairwise_realtime_fields_result(
            rows,
            list(plan.get("semantic_comparison_entity_contracts") or []),
            semantic_requested_fields,
        )
    if (
        plan.get("semantic_comparison_entity_contracts")
        and plan.get("must_make_choice")
        and not list(field_request.get("canonical_fields") or [])
        and not str(plan.get("scenario") or "").strip()
        and not [item for item in (plan.get("constraints") or []) if str(item or "").strip()]
    ):
        return _semantic_pairwise_missing_criterion_result(
            rows,
            list(plan.get("semantic_comparison_entity_contracts") or []),
        )
    requested_comparison_fields = [
        field
        for field in field_request.get("canonical_fields") or []
        if customer_field_contract.is_supported_detail_field(str(field or ""))
    ]
    # The semantic comparison plan has already interpreted the complete
    # sentence. The legacy field parser may retain only one surface field,
    # which would silently drop other customer-requested dimensions before
    # the sealed comparison renderer sees them. Merge only allowlisted fields
    # from that validated semantic plan; identity and values remain bounded by
    # the pairwise EntityResolutionContracts and same-SKU evidence below.
    if plan.get("semantic_comparison_entity_contracts"):
        requested_comparison_fields = list(dict.fromkeys([
            *requested_comparison_fields,
            *[
                field
                for field in (semantic_preplan.get("canonical_fields") or [])
                if customer_field_contract.is_supported_detail_field(str(field or ""))
            ],
        ]))
    # A semantic pairwise decision can carry several formal criteria.  It must
    # never fall through to the legacy capacity/people heuristic merely
    # because it is not a one-field comparison.  Build only sealed same-SKU
    # evidence for every requested criterion; if any one is unavailable, show
    # that gap and decline to pick a winner.
    if (
        plan.get("semantic_comparison_entity_contracts")
        and len(requested_comparison_fields) > 1
    ):
        evidence_packet = _comparison_adjudication_evidence(
            db=db,
            bundles=bundles,
            include_partial=True,
        )
        complete_fields = [
            field
            for field in requested_comparison_fields
            if len(evidence_packet.get(field) or []) == len(bundles)
        ]
        sources: list[dict[str, Any]] = []
        lines: list[str] = []
        for field in requested_comparison_fields:
            label = customer_field_contract.product_detail_field_label(field) or field
            evidence_rows = evidence_packet.get(field) or []
            if len(evidence_rows) != len(bundles):
                values_by_index = {int(item["participant_index"]): item for item in evidence_rows}
                rendered = []
                for index, bundle in enumerate(bundles):
                    product = bundle[0]
                    if product is None:
                        continue
                    sku = str(product.sku or "").strip().upper()
                    name = str(product.product_name_cn or product.product_name_en or sku).strip()
                    item = values_by_index.get(index)
                    if item is None:
                        rendered.append(f"{name}（{sku}）：当前未维护该项资料。")
                        continue
                    rendered.append(f"{name}（{sku}）：{item['value']}。")
                    sources.append({
                        "type": "product_field",
                        "sku": sku,
                        "field": field,
                        "source": item["source"],
                        "value": item["value"],
                    })
                lines.append(f"- {label}：" + " ".join(rendered))
                continue
            values_by_index = {int(item["participant_index"]): item for item in evidence_rows}
            rendered = []
            for index, bundle in enumerate(bundles):
                product = bundle[0]
                item = values_by_index.get(index)
                if product is None or item is None:
                    continue
                sku = str(product.sku or "").strip().upper()
                name = str(product.product_name_cn or product.product_name_en or sku).strip()
                rendered.append(f"{name}（{sku}）：{item['value']}。")
                sources.append({"type": "product_field", "sku": sku, "field": field, "source": item["source"], "value": item["value"]})
            lines.append(f"- {label}：" + " ".join(rendered))
        adjudication = (
            await _semantic_comparison_adjudication(
                db,
                question=str(plan.get("raw_question") or ""),
                evidence_packet=evidence_packet,
                participant_count=len(bundles),
            )
            if plan.get("must_make_choice") and evidence_packet
            else None
        )
        selected_index = adjudication.get("selected_index") if isinstance(adjudication, dict) else None
        selected_sku = (
            str(bundles[selected_index][0].sku or "").strip().upper()
            if isinstance(selected_index, int) and 0 <= selected_index < len(bundles) and bundles[selected_index][0] is not None
            else None
        )
        for field in [
            str(item or "").strip()
            for item in (
                adjudication.get("evidence_fields") or []
                if isinstance(adjudication, dict)
                else []
            )
            if str(item or "").strip() not in requested_comparison_fields
            and str(item or "").strip() in evidence_packet
        ]:
            label = customer_field_contract.product_detail_field_label(field) or field
            rendered = []
            for item in evidence_packet[field]:
                participant_index = int(item["participant_index"])
                product = bundles[participant_index][0]
                if product is None:
                    continue
                sku = str(product.sku or "").strip().upper()
                name = str(product.product_name_cn or product.product_name_en or sku).strip()
                rendered.append(f"{name}（{sku}）：{item['value']}。")
                sources.append({
                    "type": "product_field",
                    "sku": sku,
                    "field": field,
                    "source": item["source"],
                    "value": item["value"],
                })
            if rendered:
                lines.append(f"- 相关依据（{label}）：" + " ".join(rendered))
        if selected_sku:
            selected_product = bundles[int(selected_index)][0]
            selected_name = str(selected_product.product_name_cn or selected_product.product_name_en or selected_sku).strip()
            if len(complete_fields) == len(requested_comparison_fields):
                lines.append(f"根据以上商品资料，更适合当前需求的是{selected_name}（{selected_sku}）。")
            else:
                lines.append(
                    f"在这两款现有可核验资料范围内，相对更符合该需求的是"
                    f"{selected_name}（{selected_sku}）；未维护的字段仍按未知处理。"
                )
        elif plan.get("must_make_choice"):
            if len(complete_fields) == len(requested_comparison_fields):
                priority_labels = [
                    customer_field_contract.product_detail_field_label(field) or field
                    for field in requested_comparison_fields
                ]
                lines.append(
                    f"这些资料已能直接对照，但你还没有说明{'、'.join(priority_labels)}中哪项优先，"
                    "所以我暂不替你强行指定一款；告诉我最看重哪项，我可以直接给出选择。"
                )
            else:
                lines.append("由于比较条件的同 SKU 资料不完整，当前不能据此指定其中一款更适合。")
        result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
        return {
            "intent": "compare_products",
            "answer_type": "comparison",
            "answer": ("按请求条件对比：\n" if plan.get("must_make_choice") else "按已知资料对比：\n") + "\n".join(lines),
            "results": rows,
            "result_skus": result_skus,
            "candidate_skus": result_skus,
            "sources": sources,
            "answer_metadata": {
                "source": "semantic_pairwise_compound_evidence_contract",
                "contract_field_types": requested_comparison_fields,
                "evidence_status": (
                    "supported"
                    if len(complete_fields) == len(requested_comparison_fields)
                    else "supported_with_requested_field_gaps"
                    if selected_sku
                    else "missing"
                ),
                "final_choice_sku": selected_sku,
                "semantic_comparison_adjudication": adjudication,
            },
            "debug": {
                "agent_mode": "semantic_pairwise_compound_evidence_contract" if selected_sku else "semantic_pairwise_compound_evidence_insufficient",
                "field_contract": field_request,
                "entity_resolution_contracts": list(plan.get("semantic_comparison_entity_contracts") or []),
            },
            "skip_polish": True,
        }
    if (
        canonical_field
        and len(field_request.get("canonical_fields") or []) == 1
        and customer_field_contract.is_supported_detail_field(canonical_field)
        and (
            str(plan.get("comparison_kind") or "") != "multi_sku_intro"
            or bool(plan.get("semantic_comparison_entity_contracts"))
        )
        # In a compound comparison-plus-choice request, the field comparison
        # is evidence for the decision; it must not preempt the recommendation
        # task that the planner has retained in the same plan.
        and (not plan.get("must_make_choice") or plan.get("semantic_comparison_entity_contracts"))
    ):
        label = customer_field_contract.product_detail_field_label(canonical_field) or canonical_field
        lines = []
        sources: list[dict[str, Any]] = []
        evidence_rows: list[dict[str, Any]] = []
        for product, specs, business, content in bundles:
            if product is None:
                continue
            sku = str(product.sku or "").strip().upper()
            name = str(product.product_name_cn or product.product_name_en or sku).strip()
            value, source = _structured_product_field_evidence(
                canonical_field,
                db=db,
                product=product,
                specs=specs,
                business=business,
                content=content,
            )
            if value:
                lines.append(f"- {name}（{sku}）：{label}为 {value}。")
                sources.append({"type": "product_field", "sku": sku, "field": canonical_field, "source": source, "value": value})
                evidence_rows.append({"sku": sku, "value": value})
            else:
                lines.append(f"- {name}（{sku}）：当前资料未直接标注{label}，不能据此推断。")
        # A one-field comparison decision is only meaningful when that exact
        # contracted field is present for every sealed participant.  Do not
        # let a semantic adjudicator compensate for a missing requested value
        # by selecting on weight, dimensions, or any other adjacent evidence.
        # Those fields can be separately requested in a compound comparison.
        requested_field_complete = len(evidence_rows) == len(bundles)
        adjudication = (
            await _semantic_comparison_adjudication(
                db,
                question=str(plan.get("raw_question") or ""),
                evidence_packet={
                    canonical_field: [
                        {
                            "participant_index": index,
                            "sku": str(product.sku or "").strip().upper(),
                            "value": value,
                            "source": source,
                        }
                        for index, (product, _specs, _business, _content) in enumerate(bundles)
                        for value, source in [_structured_product_field_evidence(
                            canonical_field,
                            db=db,
                            product=product,
                            specs=_specs,
                            business=_business,
                            content=_content,
                        )]
                        if product is not None and value
                    ]
                },
                participant_count=len(bundles),
            )
            if plan.get("must_make_choice") and requested_field_complete
            else None
        )
        chosen_index = adjudication.get("selected_index") if isinstance(adjudication, dict) else None
        chosen_sku = (
            str(bundles[chosen_index][0].sku or "").strip().upper()
            if isinstance(chosen_index, int) and 0 <= chosen_index < len(bundles) and bundles[chosen_index][0] is not None
            else None
        )
        if plan.get("must_make_choice"):
            chosen_name = next(
                (
                    str(product.product_name_cn or product.product_name_en or chosen_sku).strip()
                    for product, _specs, _business, _content in bundles
                    if chosen_sku and str(product.sku or "").strip().upper() == chosen_sku
                ),
                "",
            )
            if chosen_sku and chosen_name:
                used_fields = list(adjudication.get("evidence_fields") or []) if isinstance(adjudication, dict) else []
                for field in used_fields:
                    if field == canonical_field:
                        continue
                    field_label = customer_field_contract.product_detail_field_label(field) or field
                    field_rows = evidence_packet.get(field) or []
                    evidence_lines = []
                    for row in field_rows:
                        product = bundles[int(row["participant_index"])][0]
                        if product is not None:
                            name = str(product.product_name_cn or product.product_name_en or product.sku).strip()
                            sku = str(product.sku or "").strip().upper()
                            evidence_lines.append(f"{name}（{sku}）：{field_label}为 {row['value']}。")
                            sources.append({"type": "product_field", "sku": sku, "field": field, "source": row["source"], "value": row["value"]})
                    if evidence_lines:
                        lines.append(f"选择依据（{field_label}）：" + " ".join(evidence_lines))
                lines.append(f"根据以上商品资料，更适合当前需求的是{chosen_name}（{chosen_sku}）。")
            else:
                lines.append(f"当前同 SKU {label}证据不足或不可直接排序，不能据此指定其中一款更适合。")
        result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
        return {
            "intent": "compare_products",
            "answer_type": "comparison",
            "answer": f"按{label}对比：\n" + "\n".join(lines),
            "results": rows,
            "result_skus": result_skus,
            "candidate_skus": result_skus,
            "sources": sources,
            "answer_metadata": {
                "source": "planner_compare_formal_field_contract",
                "contract_field_type": canonical_field,
                "evidence_status": "supported" if requested_field_complete else "missing",
                "final_choice_sku": chosen_sku,
                "semantic_comparison_adjudication": adjudication,
            },
            "debug": {
                "agent_mode": "planner_compare_formal_field_contract",
                "field_contract": field_request,
                "entity_resolution_contracts": plan.get("semantic_comparison_entity_contracts") or [
                    {
                        "status": "resolved",
                        "resolved_sku": sku,
                        "candidate_skus": [sku],
                        "field_type": canonical_field,
                        "status_reason": "comparison_explicit_product_reference",
                    }
                    for sku in result_skus
                ],
            },
            "skip_polish": True,
        }
    relation_request = "关系" in str(plan.get("raw_question") or "")
    if relation_request:
        result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
        categories = [str(row.get("category") or "").strip() for row in rows]
        common_category = categories[0] if categories and categories[0] and all(value == categories[0] for value in categories) else ""
        lines = []
        sources: list[dict[str, Any]] = []
        for row in rows:
            sku = str(row.get("sku") or "").strip().upper()
            name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
            evidence = _phase1_product_evidence_text(row)
            if evidence:
                lines.append(f"- {name}（{sku}）：{evidence}。")
                sources.append({"type": "product_relation_evidence", "sku": sku, "fields": ["category", "capacity", "body_material", "features", "usage_scenarios"]})
            else:
                lines.append(f"- {name}（{sku}）：当前资料仅能确认其商品身份，未标注更多可用于说明关系的字段。")
        common_statement = (
            f"它们当前都归为{common_category}；"
            if common_category
            else "它们的当前分类资料并不完全一致；"
        )
        return {
            "intent": "compare_products",
            "answer_type": "comparison",
            "answer": (
                f"这{len(rows)}件商品的资料关系如下：{common_statement}"
                "资料没有维护明确的套装从属或组成关系，因此以下仅按每件商品自己的资料说明，不把单品推断为套装组件。\n"
                + "\n".join(lines)
            ),
            "results": rows,
            "result_skus": result_skus,
            "candidate_skus": result_skus,
            "sources": sources,
            "answer_metadata": {
                "source": "planner_compare_explicit_relation",
                "evidence_status": "supported" if sources else "missing",
            },
            "debug": {
                "agent_mode": "planner_compare_explicit_relation",
                "entity_resolution_contracts": [
                    {
                        "status": "resolved",
                        "resolved_sku": sku,
                        "candidate_skus": [sku],
                        "status_reason": "comparison_explicit_product_reference",
                    }
                    for sku in result_skus
                ],
            },
            "skip_polish": True,
        }
    if str(plan.get("comparison_kind") or "") == "multi_sku_intro":
        result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
        lines = [f"当前识别到 {len(result_skus)} 个不同 SKU：{'、'.join(result_skus)}。"]
        sources: list[dict[str, Any]] = []
        for row in rows:
            sku = str(row.get("sku") or "").strip().upper()
            name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
            evidence = _phase1_product_evidence_text(row)
            lines.append(f"- {name}（{sku}）：{evidence or '当前资料暂未补充更多字段'}。")
            if evidence:
                sources.append({"type": "product_intro_evidence", "sku": sku, "fields": ["category", "capacity", "body_material", "features", "usage_scenarios"]})
        lines.append("当前资料未提供明确差异字段时，我会如实保留相同项，不会把不同 SKU 合并成同一个产品。")
        return {
            "intent": "compare_products",
            "answer_type": "comparison",
            "answer": "\n".join(lines),
            "results": rows,
            "result_skus": result_skus,
            "candidate_skus": result_skus,
            "sources": sources,
            "answer_metadata": {"source": "planner_multi_sku_intro", "evidence_status": "supported" if sources else "missing"},
            "debug": {"agent_mode": "planner_multi_sku_intro"},
            "skip_polish": True,
        }
    first_evidence = _phase1_product_evidence_text(first, max_fields=3)
    second_evidence = _phase1_product_evidence_text(second, max_fields=3)
    requested_people = _phase1_requested_people_count(plan)
    choice = (
        second
        if _phase1_people_fit_score(second, requested_people) >= _phase1_people_fit_score(first, requested_people)
        else first
    )
    choice_name = choice.get("product_name_cn") or choice.get("sku")
    first_label = f"{first_name}（{first.get('sku')}）"
    second_label = f"{second_name}（{second.get('sku')}）"
    answer = (
        f"简单看，主要区别如下：{first_label}和{second_label}都能作为候选；先按资料完整的容量、材质和重量比较。"
        f"{first_label}：{first_evidence or '当前资料未提供足够细项'}。"
        f"{second_label}：{second_evidence or '当前资料未提供足够细项'}。"
    )
    people_selection = bool(plan.get("must_make_choice") and requested_people is not None)
    if plan.get("must_make_choice"):
        people_basis = (
            f"对{requested_people}人的适用人数匹配"
            if requested_people is not None
            else "现有资料里的容量余量和户外吃饭场景"
        )
        answer += (
            f"如果你现在就要做选择，更建议选{choice_name}（{choice.get('sku')}）。"
            f"原因是它在现有资料里{people_basis}更稳妥；如果还要精确判断饭量，仍建议以实际容量和菜量为准。"
        )
    if people_selection:
        answer = (
            f"优先推荐{choice_name}（{choice.get('sku')}），它更匹配{requested_people}人的明确使用范围。\n"
            f"{answer}"
        )
    ordered_rows = [choice, first if choice is second else second] if people_selection else rows
    result_skus = [str(row.get("sku") or "") for row in ordered_rows if row.get("sku")]
    comparison_explanation_requested = bool(re.search(
        r"(?:\u533a\u522b|\u5dee\u5f02|\u4e0d\u540c|\u4f18\u7f3a\u70b9|\u5dee\u522b)",
        str(plan.get("raw_question") or ""),
    ))
    explicit_decision_comparison = bool(re.search(
        r"^\s*(?:\u5bf9\u6bd4|\u6bd4\u8f83)",
        str(plan.get("raw_question") or ""),
    ))
    comparison_sources = [
        {
            "type": "planner_compare_choice_evidence",
            "sku": str(row.get("sku") or "").strip().upper(),
            "value": evidence,
        }
        for row, evidence in ((first, first_evidence), (second, second_evidence))
        if str(row.get("sku") or "").strip() and str(evidence or "").strip()
    ]
    return {
        # A named-pair question remains a comparison unless the user
        # explicitly frames it as a decision-only comparison. Both paths keep
        # the same sealed pair and same-SKU evidence; only the public response
        # contract differs.
        "intent": "recommendation" if people_selection and explicit_decision_comparison and not comparison_explanation_requested else "compare_products",
        "answer_type": "recommendation" if people_selection and explicit_decision_comparison and not comparison_explanation_requested else "comparison",
        "answer": answer,
        "results": ordered_rows,
        "result_skus": result_skus,
        "candidate_skus": result_skus,
        "sources": comparison_sources,
        "answer_metadata": {
            "source": "planner_compare_choice",
            "final_choice_sku": choice.get("sku") if plan.get("must_make_choice") else None,
            "requested_people": requested_people,
        },
        "debug": {"agent_mode": "planner_compare_choice"},
        "skip_polish": True,
    }


def _phase1_requested_people_count(plan: dict) -> int | None:
    text = " ".join(
        str(plan.get(key) or "")
        for key in ("raw_question", "question", "scenario", "semantic_query")
    )
    match = re.search(r"([1-9]\d?)\s*(?:个)?人", text)
    if match:
        return int(match.group(1))
    # Planner scenarios preserve natural Chinese group sizes (for example
    # “两个人吃饱”).  Normalize that shared semantic constraint once here so
    # comparison and recommendation contracts use the same people evidence
    # rather than treating a Chinese numeral as no constraint at all.
    chinese_match = re.search(r"([一二三四五六七八九十两])\s*(?:个)?人", text)
    if not chinese_match:
        return None
    return {
        "一": 1,
        "二": 2,
        "两": 2,
        "三": 3,
        "四": 4,
        "五": 5,
        "六": 6,
        "七": 7,
        "八": 8,
        "九": 9,
        "十": 10,
    }.get(chinese_match.group(1))


def _phase1_people_fit_score(row: dict, requested_people: int | None) -> int:
    text = " ".join(str(row.get(key) or "") for key in ("capacity", "features", "usage_scenarios", "positioning", "long_description_cn"))
    if requested_people is not None:
        score = 0
        for match in re.finditer(r"(\d+)\s*[-－~至]\s*(\d+)\s*(?:个)?人", text):
            lower, upper = int(match.group(1)), int(match.group(2))
            if lower <= requested_people <= upper:
                score = max(score, 100 - abs((lower + upper) / 2 - requested_people))
            else:
                score = min(score, -100)
        for match in re.finditer(r"(?<![-－~至\d])(\d+)\s*(?:个)?人", text):
            if int(match.group(1)) == requested_people:
                score = max(score, 100)
        return score
    return _phase1_two_person_score(row)


def _phase1_two_person_score(row: dict) -> int:
    text = " ".join(str(row.get(key) or "") for key in ("capacity", "features", "usage_scenarios", "positioning", "long_description_cn"))
    score = 0
    for term in ("两人", "两个人", "2人", "2-3人", "双人"):
        if term in text:
            score += 4
    for term in ("1.5L", "1500", "2.0L", "2000", "锅1.5", "锅2."):
        if term in text:
            score += 2
    if "一个人" in text or "单人" in text:
        score -= 2
    return score


def _context_required_ordinal_compare_result(question: str) -> dict:
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": "我这边没有看到你前面提到的第一个和第二个产品，请把两个产品名或 SKU 发我，我再帮你比较哪个更适合一个人背。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "answer_metadata": {"answer_policy": "context_required"},
        "debug": {"agent_mode": "context_required_ordinal_compare"},
        "skip_polish": True,
    }


def _looks_like_context_required_ordinal_compare(question: str) -> bool:
    text = str(question or "")
    return (
        any(term in text for term in ("刚才", "前面", "上面"))
        and any(term in text for term in ("第一个", "第一款"))
        and any(term in text for term in ("第二个", "第二款"))
        and any(term in text for term in ("哪个", "更适合", "比较", "比"))
    )


def _pronoun_heat_source_followup_target_sku(question: str, recommendation_context: dict[str, Any] | None) -> str:
    text = str(question or "")
    if not (
        any(term in text for term in ("它", "这个", "这款", "刚才推荐", "刚才那个", "前面那个"))
        and any(term in text for term in ("酒精炉", "酒精"))
        and any(term in text for term in ("能不能", "能用", "可以用", "可以配", "配", "支持", "是否支持"))
    ):
        return ""
    context = recommendation_context if isinstance(recommendation_context, dict) else {}
    last_referenced_sku = str(context.get("last_referenced_sku") or "").strip().upper()
    if last_referenced_sku:
        return last_referenced_sku
    top_recommended_sku = str(context.get("top_recommended_sku") or "").strip().upper()
    if top_recommended_sku:
        return top_recommended_sku
    for key in ("recommended_skus", "ordered_result_skus", "candidate_skus"):
        for sku in context.get(key) or []:
            value = str(sku or "").strip().upper()
            if value:
                return value
    return ""


def _followup_requested_field(question: str) -> str:
    text = str(question or "")
    if any(term in text for term in ("酒精炉", "酒精", "热源")):
        return "heat_source"
    return customer_agent_planner_service._requested_field(text)


def _ordered_context_skus(*contexts: dict[str, Any] | None) -> list[str]:
    for context in contexts:
        if not isinstance(context, dict):
            continue
        for key in ("ordered_result_skus", "recommended_skus", "candidate_skus"):
            ordered = [
                str(sku or "").strip().upper()
                for sku in context.get(key) or []
                if str(sku or "").strip()
            ]
            if ordered:
                return ordered
    return []


def _ordinal_followup_target_sku(
    question: str,
    recommendation_context: dict[str, Any] | None,
    candidate_context: dict[str, Any] | None,
) -> str:
    text = str(question or "")
    # An alternative recommendation intentionally removes the previously
    # selected item from the visible list.  Keep ordinal references bound to
    # the parent list, otherwise “第二个” is re-indexed against the shortened
    # replacement list and a later pronoun can jump to a different SKU.
    ordered: list[str] = []
    for context in (recommendation_context, candidate_context):
        if not isinstance(context, dict):
            continue
        ordinal_reference = [
            str(sku or "").strip().upper()
            for sku in context.get("ordinal_reference_skus") or []
            if str(sku or "").strip()
        ]
        if ordinal_reference:
            ordered = ordinal_reference
            break
    if not ordered:
        ordered = _ordered_context_skus(recommendation_context, candidate_context)
    if not ordered:
        return ""
    explicit_scope_ref = _semantic_catalog_product_ref(text)
    explicit_scope = _normalize_recommendation_scope_label(explicit_scope_ref)
    scope_maps = [
        _normalized_recommendation_scope_skus((context or {}).get("ordinal_reference_scope_skus"))
        for context in (recommendation_context, candidate_context)
        if isinstance(context, dict)
    ]
    scope_map = next((item for item in scope_maps if item), {})
    category_terms = (
        "炉具", "炉子", "酒精炉", "燃气炉", "卡式炉", "气炉",
        "水壶", "烧水壶", "水具", "锅具", "锅", "烤盘", "煎盘", "配件",
    )
    has_explicit_scope = explicit_scope_ref != "产品" and any(term in text for term in category_terms)
    if has_explicit_scope:
        scoped_ordered = scope_map.get(explicit_scope, [])
        if not scoped_ordered:
            return ""
        ordered = scoped_ordered
    elif scope_map:
        for context in (candidate_context, recommendation_context):
            if not isinstance(context, dict) or str(context.get("source") or "").strip() != "result":
                continue
            inherited_scope = _normalize_recommendation_scope_label(
                _semantic_catalog_product_ref(str(context.get("product_scope") or ""))
            )
            scoped_ordered = scope_map.get(inherited_scope, [])
            if scoped_ordered:
                ordered = scoped_ordered
                break
    if any(term in text for term in ("最后一个", "最后一款", "最后那个")):
        return ordered[-1]
    mapping = (
        ("第一个", 0),
        ("第一款", 0),
        ("最前面那个", 0),
        ("第二个", 1),
        ("第二款", 1),
        ("第三个", 2),
        ("第三款", 2),
    )
    for token, index in mapping:
        if token in text and index < len(ordered):
            return ordered[index]
    return ""


def _ordinal_followup_target_sku_from_ordered(question: str, ordered_skus: list[str]) -> str:
    ordered = [
        str(sku or "").strip().upper()
        for sku in (ordered_skus or [])
        if str(sku or "").strip()
    ]
    if not ordered:
        return ""
    text = str(question or "")
    ordinal_hits: list[tuple[int, int]] = []
    for token in ("最后一个", "最后一款", "最后那个"):
        position = text.rfind(token)
        if position >= 0:
            ordinal_hits.append((position, len(ordered) - 1))
    mapping = (
        ("第一个", 0),
        ("第一款", 0),
        ("最前面那个", 0),
        ("第二个", 1),
        ("第二款", 1),
        ("第三个", 2),
        ("第三款", 2),
    )
    for token, index in mapping:
        position = text.rfind(token)
        if position >= 0 and index < len(ordered):
            ordinal_hits.append((position, index))
    if ordinal_hits:
        ordinal_hits.sort(key=lambda item: item[0])
        return ordered[ordinal_hits[-1][1]]
    return ""


def _looks_like_ordinal_product_field_followup(question: str) -> bool:
    text = str(question or "")
    return any(term in text for term in ("第一个", "第一款", "最前面那个", "第二个", "第二款", "第三个", "第三款", "最后一个", "最后一款", "最后那个"))


async def _ordinal_freeform_followup_result(
    db: Session,
    *,
    user_id: str,
    question: str,
    resolved_sku: str,
) -> dict | None:
    """Bind an ordinal QA follow-up to its resolved candidate before active-anchor routing."""
    sku = str(resolved_sku or "").strip().upper()
    if not sku:
        return None
    result = await _try_named_product_shortcut(
        db,
        user_id=user_id,
        question=f"{sku} {str(question or '').strip()}".strip(),
    )
    if isinstance(result, dict):
        answer = str(result.get("answer") or "")
        if "请提供具体型号或 SKU" in answer:
            result["answer"] = answer.replace(
                "请提供具体型号或 SKU",
                f"你当前问的是 {sku}",
            )
        debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
        debug["identity_source"] = "recommendation_context_ordinal"
        debug["ordinal_target_sku"] = sku
        result["debug"] = debug
    return result if isinstance(result, dict) else None


def _product_field_followup_result(
    db: Session,
    sku: str,
    requested_field: str,
    question: str,
    *,
    identity_source: str | None = "explicit_sku_exact",
    field_request_override: dict[str, Any] | None = None,
) -> dict | None:
    if not sku or not requested_field:
        return None
    # A context anchor seals only the product. The current utterance still
    # owns field meaning, so establish its FieldContract before building the
    # legacy-compatible execution plan. Previously the plan could consume a
    # stale display label while the correct contract was attached only after
    # formatting, yielding an answer for a different field than the one shown
    # in debug/evidence metadata.
    field_request = dict(field_request_override or resolve_requested_field_contract(question))
    canonical_fields = list(dict.fromkeys(
        str(field or "").strip()
        for field in (field_request.get("canonical_fields") or [])
        if str(field or "").strip() in customer_field_contract.FORMAL_DETAIL_FIELDS
    ))
    if len(canonical_fields) > 1:
        product = db.query(Product).filter(Product.sku == str(sku or "").strip().upper()).first()
        if product is None:
            return None
        canonical_sku = str(product.sku or "").strip().upper()
        entity_text = str(product.product_name_cn or product.product_name_en or canonical_sku).strip()
        context_identity = str(identity_source or "").startswith("recommendation_context")
        contract_match = "recommendation_context_anchor" if context_identity else str(identity_source or "explicit_sku_exact")
        entity_contract = customer_entity_resolution_contract.EntityResolutionContract(
            entity_text=entity_text,
            normalized_entity_text=entity_text.casefold(),
            status="resolved",
            resolved_sku=canonical_sku,
            resolver_candidate_skus=[canonical_sku],
            diagnostic_candidate_skus=[],
            candidate_skus=[canonical_sku],
            matched_by=contract_match,
            confidence="high",
            is_unique=True,
            matched_span=None,
            field_type=None,
            status_reason="resolved_sealed_context_identity" if context_identity else "resolved_explicit_identity",
        )
        compound_plan = {
            "primary_intent": "product_field",
            "answer_type": "product_detail",
            "product_ref": canonical_sku,
            "sku": canonical_sku,
            "requested_fields": list(field_request.get("requested_fields") or []),
            "raw_question": question,
            "compound": True,
            "multi_field": True,
        }
        result = _build_resolved_compound_evidence_result(
            db,
            question=question,
            entity_contract=entity_contract,
            field_request=field_request,
            phase1_plan=compound_plan,
        ) or _build_compound_product_detail_result(
            db,
            resolved_sku=canonical_sku,
            requested_fields=list(field_request.get("requested_fields") or []),
            question=question,
            entity_contract=entity_contract.to_dict(),
            compatibility_plan=compound_plan,
        )
        if isinstance(result, dict):
            debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
            debug.update({
                "agent_mode": "recommendation_context_product_field" if context_identity else "resolved_entity_multi_field_contract",
                "product_identity_source": identity_source,
                "field_contract": dict(field_request),
                "entity_resolution_contract": entity_contract.to_dict(),
                "binding_provenance": "resolved_entity_contract",
            })
            result["debug"] = debug
            return result
    canonical_field = str(field_request.get("field_type") or "").strip()
    canonical_label = customer_field_contract.product_detail_field_label(canonical_field)
    if canonical_label:
        requested_field = canonical_label
    plan = {
        "primary_intent": "product_field",
        "answer_type": "product_detail",
        "product_ref": sku,
        "sku": sku,
        "requested_field": requested_field,
        "field_only": True,
        "confidence": "high",
        "product_identity_source": identity_source,
        "raw_question": question,
        "tasks": [{"type": "product_field", "product_ref": sku, "requested_field": requested_field}],
    }
    if canonical_field:
        plan["field_type_override"] = canonical_field
    result = _phase1_product_field_result(db, plan)
    if not result:
        return None
    if (
        canonical_field == "heat_source"
        and any(term in str(question or "") for term in ("推荐", "优先", "建议"))
        and any(term in str(question or "") for term in ("燃料", "热源"))
    ):
        answer_text = str(result.get("answer") or "").strip()
        if answer_text and not any(term in answer_text for term in ("优先", "按说明书", "推荐")):
            result["answer"] = (
                f"{answer_text.rstrip('。；;')}。"
                "结论：当前资料只标注适用热源，未标注哪一种燃料优先推荐；请按该型号说明书和实际接口选择。"
            )
    # Context/ordinal follow-ups may supply the canonical SKU outside the raw
    # question (for example, "它多大尺寸？").  The SKU is still a sealed
    # identity, not permission for the legacy product-field shortcut to bypass
    # the central contracts.  Expose the same formal FieldContract and
    # EntityResolutionContract consumed by exact-name/SKU requests before the
    # result is returned.
    canonical_field = canonical_field or None
    product = db.query(Product).filter(Product.sku == str(sku or "").strip().upper()).first()
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    debug["agent_mode"] = "recommendation_context_product_field"
    if canonical_field:
        plan["field_contract"] = dict(field_request)
        debug["field_contract"] = dict(field_request)
    if product is not None:
        canonical_sku = str(product.sku or "").strip().upper()
        entity_text = str(product.product_name_cn or product.product_name_en or canonical_sku).strip()
        entity_contract = customer_entity_resolution_contract.EntityResolutionContract(
            entity_text=entity_text,
            normalized_entity_text=entity_text.casefold(),
            status="resolved",
            resolved_sku=canonical_sku,
            resolver_candidate_skus=[canonical_sku],
            diagnostic_candidate_skus=[],
            candidate_skus=[canonical_sku],
            matched_by=str(identity_source or "context_anchor"),
            confidence="high",
            is_unique=True,
            matched_span=None,
            field_type=canonical_field,
            status_reason="resolved_sealed_context_identity",
        )
        debug["entity_resolution_contract"] = entity_contract.to_dict()
        debug["binding_provenance"] = "resolved_entity_contract"
    result["debug"] = debug
    metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    if canonical_field:
        metadata["contract_field_type"] = canonical_field
    result["answer_metadata"] = metadata
    return result


async def _active_product_anchor_field_followup_result(
    db: Session,
    question: str,
    recommendation_context: dict[str, Any] | None,
    phase1_plan: dict[str, Any] | None = None,
    candidate_context: dict[str, Any] | None = None,
) -> dict | None:
    text = str(question or "").strip()
    # An explicit current-turn SKU or product name is a new identity signal;
    # it must be resolved by the ordinary EntityResolutionContract before an
    # older pronoun/context anchor can supply identity.
    if _has_explicit_sku_text(text) or _products_named_in_question(db, text):
        return None
    if _looks_like_ordinal_product_field_followup(text):
        return None
    # A plural anaphor refers to the preserved candidate/pair context.  It
    # must never be collapsed onto the last single-product anchor merely
    # because "它们" begins with the singular token "它".
    if _is_plural_heat_source_followup(text):
        return None
    if _looks_like_negated_selected_result_context_reference(text):
        return None
    explicit_anchor_terms = ("刚才那个", "刚刚那个", "前面那个", "上一个", "刚才说的那个", "刚才推荐的那个")
    # These are grammatical anaphora, not product aliases.  The anchored SKU
    # remains sealed from the preceding turn, while the current turn must
    # independently form its FieldContract through the normal adapter.
    # Leading discourse particles ("那它…" / "这款…") do not create an
    # identity.  They retain the same grammatical anaphora and therefore use
    # only the sealed active-product anchor below.
    pronoun_anchor = re.search(r"(?:它|这个|这款|该商品|该产品|这个商品|这件商品|本产品)", text) is not None
    semantic_preplan = (
        phase1_plan.get("semantic_preplan")
        if isinstance(phase1_plan, dict) and isinstance(phase1_plan.get("semantic_preplan"), dict)
        else {}
    )
    semantic_context_anchor = str(semantic_preplan.get("context_usage") or "").strip() == "entity_anchor"
    ordinal_reference = _looks_like_ordinal_product_field_followup(text)
    selected_result_reference = _looks_like_selected_result_context_reference(text)
    selected_result_sku = (
        _selected_result_context_choice_sku(recommendation_context, candidate_context)
        if selected_result_reference
        else ""
    )
    if selected_result_reference and not selected_result_sku:
        return None
    if (
        not any(term in text for term in explicit_anchor_terms)
        and not pronoun_anchor
        and not semantic_context_anchor
        and not ordinal_reference
        and not selected_result_reference
    ):
        return None
    context = recommendation_context if isinstance(recommendation_context, dict) else {}
    # A replacement list is a second, explicitly labeled discourse target. It
    # does not overwrite the product the user was already discussing: only a
    # new-result anaphor may consume its ordered first candidate.
    replacement_reference = re.match(
        r"^\s*新[^，。！？!?]{0,8}(?:它|这个|这款|该商品|该产品|这个商品|这件商品|本产品)",
        text,
    ) is not None
    ordinal_sku = _ordinal_followup_target_sku(text, context, None) if ordinal_reference else ""
    anchor_sku = str(
        ordinal_sku
        or selected_result_sku
        or (
            context.get("replacement_top_sku")
            if replacement_reference and context.get("replacement_top_sku")
            else context.get("active_single_product_anchor")
        )
        or ""
    ).strip().upper()
    if not anchor_sku:
        return None
    field_request = _merge_explicit_detail_fields_into_request(
        text,
        resolve_requested_field_contract(text, phase1_plan),
    )
    semantic_fields = [
        str(field or "").strip()
        for field in (semantic_preplan.get("canonical_fields") or [])
        if customer_field_contract.is_supported_detail_field(str(field or "").strip())
    ]
    semantic_fields = list(dict.fromkeys(semantic_fields))
    if len(semantic_fields) > 1 and _semantic_preplan_confident(semantic_preplan, minimum=0.9):
        # The semantic plan owns the current-turn field set.  A context anchor
        # may provide identity only; it must not collapse a validated
        # multi-field request back to the legacy parser's single display label.
        field_request = {
            **field_request,
            "field_type": "",
            "canonical_fields": semantic_fields,
            "requested_fields": [
                customer_field_contract.product_detail_field_label(field)
                for field in semantic_fields
                if customer_field_contract.product_detail_field_label(field)
            ],
            "source": "validated_semantic_preplan",
        }
    canonical_fields = list(field_request.get("canonical_fields") or [])
    if len(canonical_fields) > 1:
        product = db.query(Product).filter(Product.sku == anchor_sku).first()
        if product is not None:
            entity_text = str(product.product_name_cn or product.product_name_en or anchor_sku).strip()
            entity_contract = customer_entity_resolution_contract.EntityResolutionContract(
                entity_text=entity_text,
                normalized_entity_text=entity_text.casefold(),
                status="resolved",
                resolved_sku=anchor_sku,
                resolver_candidate_skus=[anchor_sku],
                diagnostic_candidate_skus=[],
                candidate_skus=[anchor_sku],
                matched_by="recommendation_context_anchor",
                confidence="high",
                is_unique=True,
                matched_span=None,
                field_type=None,
                status_reason="resolved_sealed_context_identity",
            )
            compound = _build_resolved_compound_evidence_result(
                db,
                question=text,
                entity_contract=entity_contract,
                field_request=field_request,
                phase1_plan=phase1_plan,
            )
            if not isinstance(compound, dict):
                compound = _build_compound_product_detail_result(
                    db,
                    resolved_sku=anchor_sku,
                    requested_fields=list(field_request.get("requested_fields") or []),
                    question=text,
                    entity_contract=entity_contract.to_dict(),
                    compatibility_plan=phase1_plan,
                )
            if isinstance(compound, dict):
                return compound
    field_contract = customer_field_contract.detect_field_contract(text)
    detected_fields = customer_field_contract.detect_field_types(text)
    field_type = str(field_request.get("field_type") or "").strip() or (
        field_contract.field_type if field_contract else (
            detected_fields[0] if len(detected_fields) == 1 else None
        )
    )
    if not field_type:
        classified_field = customer_field_contract.classify_product_qa_evidence_type(text)
        field_type = classified_field if classified_field == "usage_scene" else None
    if not field_type:
        # A high-confidence semantic QA follow-up still needs the same sealed
        # product identity.  The preplan owns the field meaning; this adapter
        # only supplies that identity to the bounded same-SKU QA selector.
        if _semantic_prefers_sealed_product_qa(phase1_plan or {}):
            product = db.query(Product).filter(Product.sku == anchor_sku).first()
            if product is not None:
                anchored_question = f"{product.product_name_cn or product.product_name_en or anchor_sku} {text}".strip()
                result = _try_product_qa_shortcut(
                    db,
                    anchored_question,
                    phase1_plan=phase1_plan,
                )
                if _is_sealed_product_qa_safe_missing(result):
                    # The context anchor has already sealed one canonical SKU.
                    # An exact QA miss is not an identity miss: continue only
                    # to the same-SKU knowledge provider before allowing the
                    # caller's deictic fail-closed guard to run.
                    result = await _try_sealed_same_sku_knowledge_answer(
                        db,
                        anchored_question,
                        phase1_plan,
                    ) or result
                if isinstance(result, dict):
                    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
                    debug.update({
                        "agent_mode": "recommendation_context_product_qa",
                        "product_identity_source": "recommendation_context_anchor",
                        "active_single_product_anchor": anchor_sku,
                    })
                    result["debug"] = debug
                    return _attach_recommendation_followup_contract(result, text, context, anchor_sku=anchor_sku)
        return None
    detail_label = (
        "适用场景"
        if field_type == "usage_scene"
        else customer_field_contract.product_detail_field_label(field_type)
    )
    if (
        field_type in {"usage_scene", "accessories"}
        or customer_field_contract.is_supported_detail_field(field_type)
    ) and detail_label:
        result = _product_field_followup_result(
            db,
            anchor_sku,
            detail_label,
            text,
            identity_source="recommendation_context_anchor",
            field_request_override=field_request,
        )
    else:
        product = db.query(Product).filter(Product.sku == anchor_sku).first()
        label = _resolved_entity_unknown_field_guard_label(text)
        if not product or not label:
            return None
        decision = customer_entity_resolution_contract.SingleProductResolutionDecision(
            allowed=True,
            resolved_sku=anchor_sku,
            resolved_product_id=str(product.id),
            reason="resolved_context_anchor",
            match_level="recommendation_context_anchor",
        )
        result = _build_resolved_product_unknown_field_result(
            db,
            product,
            label=label,
            source="recommendation_context_unknown_field",
            resolution_decision=decision,
        )
    if not isinstance(result, dict):
        return None
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    debug.update({
        "agent_mode": "recommendation_context_product_field",
        "product_identity_source": "recommendation_context_anchor",
        "active_single_product_anchor": anchor_sku,
    })
    # Safe-unsupported fields (manual, warranty, inventory, price, shipping,
    # etc.) use the guarded missing-field formatter above.  They still consume
    # the same sealed context identity and formal field request as supported
    # detail fields; a safe fallback is not a contract exemption.
    if not isinstance(debug.get("field_contract"), dict):
        field_request = field_request if field_request.get("field_type") else resolve_requested_field_contract(
            text,
            phase1_plan or {"requested_field": detail_label or _resolved_entity_unknown_field_guard_label(text)},
        )
        if field_request.get("field_type"):
            debug["field_contract"] = dict(field_request)
    if not isinstance(debug.get("entity_resolution_contract"), dict):
        product = db.query(Product).filter(Product.sku == anchor_sku).first()
        if product is not None:
            entity_text = str(product.product_name_cn or product.product_name_en or anchor_sku).strip()
            entity_contract = customer_entity_resolution_contract.EntityResolutionContract(
                entity_text=entity_text,
                normalized_entity_text=entity_text.casefold(),
                status="resolved",
                resolved_sku=anchor_sku,
                resolver_candidate_skus=[anchor_sku],
                diagnostic_candidate_skus=[],
                candidate_skus=[anchor_sku],
                matched_by="recommendation_context_anchor",
                confidence="high",
                is_unique=True,
                matched_span=None,
                field_type=field_type,
                status_reason="resolved_sealed_context_identity",
            )
            debug["entity_resolution_contract"] = entity_contract.to_dict()
            debug["binding_provenance"] = "resolved_entity_contract"
    result["debug"] = debug
    metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    metadata["contract_field_type"] = field_type
    result["answer_metadata"] = metadata
    return _attach_recommendation_followup_contract(
        result,
        text,
        context,
        anchor_sku=anchor_sku,
    )


def _ambiguity_candidate_attribute_followup_result(
    db: Session,
    question: str,
    candidate_context: dict[str, Any] | None,
) -> dict | None:
    context = candidate_context if isinstance(candidate_context, dict) else {}
    if context.get("source") != "entity_ambiguity":
        return None
    text = str(question or "").strip()
    if not text or not _is_clarification_followup_product_only_text(text):
        return None
    entity_text = str(context.get("entity_text") or "").strip()
    field_type = str(context.get("field_type") or "").strip()
    if not entity_text or not field_type:
        return None
    products = db.query(Product).order_by(Product.sku.asc()).all()
    selection = _choose_effective_entity_contract(
        f"{entity_text}{text}",
        products,
        override_subject=f"{entity_text}{text}",
    )
    contract = selection.contract
    decision = customer_entity_resolution_contract.can_resolve_single_product(contract, products)
    if not decision.allowed:
        return None
    detail_label = customer_field_contract.product_detail_field_label(field_type)
    if customer_field_contract.is_supported_detail_field(field_type) and detail_label:
        field_contract = next(
            (item for item in customer_field_contract.FIELD_CONTRACTS if item.field_type == field_type),
            None,
        )
        if field_contract is None:
            return None
        result = _build_phase2_entity_state_response(
            db,
            text,
            {
                "action": "resolved_detail",
                "contract": contract,
                "products": products,
                "field": field_contract,
                "requested_field": detail_label,
            },
        )
    else:
        product = next(
            (product for product in products if str(product.sku or "").strip().upper() == decision.resolved_sku),
            None,
        )
        label = customer_field_contract.product_detail_field_label(field_type)
        if not product or not label:
            return None
        result = _build_resolved_product_unknown_field_result(
            db,
            product,
            label=label,
            source="candidate_context_variant_refinement",
            resolution_decision=decision,
        )
    if not isinstance(result, dict):
        return None
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    debug.update({
        "agent_mode": "candidate_context_variant_refinement",
        "entity_resolution_contract": contract.to_dict(),
    })
    result["debug"] = debug
    return result


def _product_heat_source_followup_result(db: Session, question: str, sku: str) -> dict | None:
    result = _product_field_followup_result(
        db,
        sku,
        "heat_source",
        question,
        identity_source="recommendation_context_anchor",
    )
    if isinstance(result, dict):
        debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
        debug["agent_mode"] = "recommendation_context_product_compatibility"
        result["debug"] = debug
    return result


async def _explicit_multi_product_compatibility_contract_result(
    db: Session,
    question: str,
) -> dict | None:
    """Seal explicit co-use requests before any single-product context route."""
    if not customer_agent_intent_service._looks_like_product_compatibility_question(question):
        return None
    named_products = _products_named_in_question(db, question)
    if len(named_products) < 2:
        return None
    result = await customer_agent_intent_service._explicit_product_compatibility_result(db, question)
    if not isinstance(result, dict):
        return None
    result_skus = [
        str(row.get("sku") or "").strip().upper()
        for row in result.get("results") or []
        if isinstance(row, dict) and str(row.get("sku") or "").strip()
    ]
    named_by_sku = {
        str(product.sku or "").strip().upper(): product
        for product in named_products
        if str(product.sku or "").strip()
    }
    if len(result_skus) < 2 or not set(result_skus).issubset(set(named_by_sku)):
        return None
    field_request = customer_field_contract.resolve_requested_field_contract(
        question,
        compatibility_fields=["heat_source"],
    )
    if str(field_request.get("field_type") or "") != "heat_source":
        return None
    entity_contracts = []
    for sku in result_skus:
        product = named_by_sku[sku]
        entity_text = str(product.product_name_cn or product.product_name_en or sku).strip()
        entity_contracts.append(
            customer_entity_resolution_contract.EntityResolutionContract(
                entity_text=entity_text,
                normalized_entity_text=entity_text.casefold(),
                status="resolved",
                resolved_sku=sku,
                resolver_candidate_skus=[sku],
                diagnostic_candidate_skus=[],
                candidate_skus=[sku],
                matched_by="explicit_current_turn_multi_entity",
                confidence="high",
                is_unique=True,
                matched_span=None,
                field_type="heat_source",
                status_reason="resolved_exact_multi_entity_compatibility",
            ).to_dict()
        )
    debug = dict(result.get("debug") or {})
    debug.update(
        {
            "agent_mode": "explicit_multi_entity_compatibility_contract",
            "field_contract": dict(field_request),
            "entity_resolution_contracts": entity_contracts,
            "binding_provenance": "explicit_current_turn_multi_entity",
        }
    )
    result["debug"] = debug
    metadata = dict(result.get("answer_metadata") or {})
    metadata.update(
        {
            "contract_field_type": "heat_source",
            "evidence_skus": result_skus,
            "entity_resolution_contracts": entity_contracts,
        }
    )
    result["answer_metadata"] = metadata
    return result


def _recommendation_context_turn(context: dict[str, Any] | None) -> int:
    values = (context or {}).get("contract_merge_provenance") or {}
    turns = [
        int(item.get("source_turn") or 0)
        for item in values.values()
        if isinstance(item, dict)
    ]
    return max(turns or [1]) + 1


def _attach_recommendation_followup_contract(
    result: dict | None,
    question: str,
    recommendation_context: dict[str, Any] | None,
    *,
    anchor_sku: str | None = None,
) -> dict | None:
    if not isinstance(result, dict) or not isinstance(recommendation_context, dict):
        return result
    inherited_data = (
        recommendation_context.get("effective_recommendation_contract")
        or recommendation_context.get("recommendation_request_contract")
    )
    if not isinstance(inherited_data, dict):
        return result
    inherited = customer_recommendation_verification_contract.RecommendationRequestContract.from_dict(inherited_data)
    current = customer_recommendation_verification_contract.build_recommendation_request_contract(question)
    effective, provenance = customer_recommendation_verification_contract.merge_recommendation_request_contracts(
        inherited,
        current,
        previous_result_skus=list(recommendation_context.get("previous_result_skus") or []),
        anchor_sku=anchor_sku,
        current_turn=_recommendation_context_turn(recommendation_context),
    )
    metadata = dict(result.get("answer_metadata") or {})
    metadata.update(
        {
            "current_recommendation_contract": current.to_dict(),
            "inherited_recommendation_contract": inherited.to_dict(),
            "effective_recommendation_contract": effective.to_dict(),
            "contract_merge_provenance": provenance,
        }
    )
    result["answer_metadata"] = metadata
    return result


async def _alternative_recommendation_followup_result(
    db: Session,
    question: str,
    recommendation_context: dict[str, Any] | None,
    candidate_context: dict[str, Any] | None,
) -> dict | None:
    # A replacement must search the preserved candidate domain, while the
    # recommendation context still supplies the single SKU to exclude.
    ordered = _ordered_context_skus(candidate_context, recommendation_context)
    context = recommendation_context if isinstance(recommendation_context, dict) else {}
    inherited_data = context.get("effective_recommendation_contract") or context.get("recommendation_request_contract")
    if not isinstance(inherited_data, dict):
        return None
    inherited = customer_recommendation_verification_contract.RecommendationRequestContract.from_dict(inherited_data)
    anchor_sku = str(
        context.get("last_referenced_sku")
        or context.get("top_recommended_sku")
        or (ordered[0] if ordered else "")
    ).strip().upper()
    # A prior “recommend one” result has a one-SKU visible set, but that is
    # not a sealed one-product universe.  For an alternative, rebuild the
    # domain from the inherited request contract, then verify and exclude the
    # anchor; never silently re-return the same product.
    if len(ordered) < 2:
        subject_category = str(inherited.subject_category or "").strip()
        catalog_rows = _phase1_catalog_rows(db, "产品")
        if subject_category:
            catalog_rows = [
                row for row in catalog_rows
                if (
                    subject_category in str(row.get("category") or "").strip()
                    if subject_category == "锅具"
                    else str(row.get("category") or "").strip() == subject_category
                )
            ]
        ordered = [
            anchor_sku,
            *[
                str(row.get("sku") or "").strip().upper()
                for row in catalog_rows
                if str(row.get("sku") or "").strip().upper()
                and str(row.get("sku") or "").strip().upper() != anchor_sku
            ],
        ]
    alternative_skus = [sku for sku in ordered if sku and sku != anchor_sku]
    if not alternative_skus:
        alternative_skus = ordered[1:]
    if not alternative_skus:
        return None

    products = db.query(Product).filter(Product.sku.in_(alternative_skus)).all()
    by_sku = {str(product.sku or "").strip().upper(): product for product in products}
    rows: list[dict] = []
    for sku in alternative_skus:
        product = by_sku.get(sku)
        if not product:
            continue
        specs = db.query(ProductSpecs).filter(ProductSpecs.product_id == product.id).first()
        business = db.query(ProductBusiness).filter(ProductBusiness.product_id == product.id).first()
        content = db.query(ProductContent).filter(ProductContent.product_id == product.id).first()
        rows.append(_product_row_from_model(product, specs, business, content))
    if not rows:
        return None

    current = customer_recommendation_verification_contract.build_recommendation_request_contract(question)
    effective, provenance = customer_recommendation_verification_contract.merge_recommendation_request_contracts(
        inherited,
        current,
        previous_result_skus=ordered,
        anchor_sku=anchor_sku,
        current_turn=_recommendation_context_turn(context),
    )
    if current.weight_preference == "lighter" and anchor_sku:
        anchor_product = db.query(Product).filter(Product.sku == anchor_sku).first()
        anchor_specs = (
            db.query(ProductSpecs).filter(ProductSpecs.product_id == anchor_product.id).first()
            if anchor_product is not None
            else None
        )
        anchor_weight = getattr(anchor_specs, "gross_weight_g", None) if anchor_specs is not None else None
        try:
            anchor_weight_value = float(anchor_weight) if anchor_weight is not None else None
        except (TypeError, ValueError):
            anchor_weight_value = None
        if anchor_weight_value is not None:
            lighter_rows = []
            for row in rows:
                try:
                    candidate_weight = float(row.get("gross_weight_g"))
                except (TypeError, ValueError):
                    continue
                if candidate_weight < anchor_weight_value:
                    lighter_rows.append(row)
            if not lighter_rows:
                return {
                    "intent": "recommendation",
                    "answer_type": "recommendation",
                    "answer": (
                        f"已排除刚才推荐的 {anchor_sku}。它的资料重量为{_weight_field_evidence(anchor_weight)}；"
                        "当前其余候选中，没有找到已明确标注重量且比它更轻的商品，因此这次不把更重或重量缺失的商品当作“更轻备选”。"
                        "如果你愿意放宽“必须更轻”，我可以再按烧水、容量或炉具兼容继续选。"
                    ),
                    "results": [],
                    "result_skus": [],
                    "candidate_skus": [],
                    "debug": {
                        "agent_mode": "recommendation_context_no_verified_lighter_alternative",
                        "initial_candidate_skus": [str(row.get("sku") or "").strip().upper() for row in rows],
                    },
                    "answer_metadata": {
                        "source": "recommendation_context_no_verified_lighter_alternative",
                        "current_recommendation_contract": current.to_dict(),
                        "inherited_recommendation_contract": inherited.to_dict(),
                        "effective_recommendation_contract": effective.to_dict(),
                        "contract_merge_provenance": provenance,
                    },
                    "skip_polish": True,
                }
            rows = lighter_rows
    verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(effective, rows)
    selected = customer_recommendation_verification_contract.select_recommendation_candidates(rows, verifications)[:3]
    if effective.heat_sources:
        fully_verified_skus = {
            str(item.sku or "").strip().upper()
            for item in verifications
            if item.verification_level == "fully_verified" and item.all_hard_constraints_verified
        }
        strict_selected = [
            row for row in selected
            if str(row.get("sku") or "").strip().upper() in fully_verified_skus
        ]
        if strict_selected:
            selected = strict_selected
        else:
            # A follow-up asking for an alternative must not claim that the
            # catalogue is empty merely because the inherited people/scenario
            # preferences are absent on every other SKU.  Keep only candidates
            # whose requested heat source is explicitly verified and whose
            # remaining hard constraints have no contradiction; the answer
            # renderer will disclose the missing fields as uncertainty.
            verification_by_sku = {
                str(item.sku or "").strip().upper(): item
                for item in verifications
            }
            selected = [
                row
                for row in rows
                if (
                    verification_by_sku.get(str(row.get("sku") or "").strip().upper())
                    and verification_by_sku[str(row.get("sku") or "").strip().upper()].verification_level == "partially_verified"
                    and not verification_by_sku[str(row.get("sku") or "").strip().upper()].has_hard_constraint_conflict
                    and (verification_by_sku[str(row.get("sku") or "").strip().upper()].evidence_by_constraint.get("heat_source") or {}).get("status") == "verified"
                )
            ][:3]
    if not selected:
        relaxed_budget_rows: list[dict] = []
        relaxed_budget_verifications: list[customer_recommendation_verification_contract.CandidateVerification] = []
        if effective.budget_level in {"affordable", "premium"}:
            # Qualitative catalogue positioning is useful for the initial
            # shortlist, but an explicit request for another option should
            # still be helpful when every otherwise-compatible candidate is
            # rejected only by that label. Surface it with a clear price
            # caveat instead of claiming the catalogue has no alternative.
            relaxed_contract = customer_recommendation_verification_contract.RecommendationRequestContract.from_dict(
                effective.to_dict()
            )
            relaxed_contract.hard_constraints = [
                item for item in relaxed_contract.hard_constraints
                if item != "price_positioning"
            ]
            relaxed_contract.budget_level = None
            relaxed_budget_verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(
                relaxed_contract,
                rows,
            )
            relaxed_budget_rows = customer_recommendation_verification_contract.select_recommendation_candidates(
                rows,
                relaxed_budget_verifications,
            )[:3]
            relaxed_verification_by_sku = {
                str(item.sku or "").strip().upper(): item
                for item in relaxed_budget_verifications
            }
            relaxed_budget_rows = [
                row
                for row in relaxed_budget_rows
                if (
                    relaxed_verification_by_sku.get(str(row.get("sku") or "").strip().upper())
                    and relaxed_verification_by_sku[str(row.get("sku") or "").strip().upper()].all_hard_constraints_verified
                )
            ]
            if relaxed_budget_rows:
                narrative_diagnostics: list[dict[str, Any]] = []
                inherited_people_context = ""
                if inherited.people_min is not None:
                    inherited_people_context = (
                        f"前序已确认使用人数为{inherited.people_min}人"
                        if inherited.people_min == inherited.people_max
                        else f"前序已确认使用人数为{inherited.people_min}-{inherited.people_max}人"
                    )
                semantic_narrative = await _semantic_recommendation_narrative(
                    db,
                    question=question,
                    rows=relaxed_budget_rows,
                    verifications=relaxed_budget_verifications,
                    soft_preferences=[
                        f"不要重复刚才的 {anchor_sku}",
                        "价格需要查看实时售价后再决定",
                        *([inherited_people_context] if inherited_people_context else []),
                    ],
                    diagnostics=narrative_diagnostics,
                )
                if semantic_narrative is not None:
                    selected_indexes = semantic_narrative.get("ranked_candidate_indexes") or []
                    semantic_rows = [
                        relaxed_budget_rows[index]
                        for index in selected_indexes
                        if type(index) is int and 0 <= index < len(relaxed_budget_rows)
                    ]
                    semantic_skus = [
                        str(row.get("sku") or "").strip().upper()
                        for row in semantic_rows
                        if str(row.get("sku") or "").strip()
                    ]
                    return {
                        "intent": "recommendation",
                        "answer_type": "recommendation",
                        "answer": (
                            f"已排除刚才推荐的 {anchor_sku}。当前资料不能核验实时价格，价格请以下单页为准。\n"
                            + semantic_narrative["answer"]
                        ),
                        "results": semantic_rows,
                        "result_skus": semantic_skus,
                        "candidate_skus": semantic_skus,
                        "debug": {
                            "agent_mode": "recommendation_context_alternative_semantic_budget_disclosed",
                            "initial_candidate_skus": [str(row.get("sku") or "").strip().upper() for row in rows],
                            "recommendation_narrative_diagnostics": narrative_diagnostics,
                        },
                        "answer_metadata": {
                            "source": "recommendation_context_alternative_semantic_budget_disclosed",
                            "recommendation_narrative": {
                                "source": "validated_deepseek_grounded_narrative",
                                "ranked_candidate_indexes": list(selected_indexes),
                                "evidence_usage": list(semantic_narrative.get("evidence_usage") or []),
                            },
                            "current_recommendation_contract": current.to_dict(),
                            "inherited_recommendation_contract": inherited.to_dict(),
                            "effective_recommendation_contract": effective.to_dict(),
                            "contract_merge_provenance": provenance,
                        },
                        "skip_polish": True,
                    }
                relaxed_answer = customer_recommendation_verification_contract.build_verified_recommendation_answer(
                    relaxed_contract,
                    relaxed_budget_rows,
                    relaxed_budget_verifications,
                )
                relaxed_skus = [
                    str(row.get("sku") or "").strip().upper()
                    for row in relaxed_budget_rows
                    if str(row.get("sku") or "").strip()
                ]
                return {
                    "intent": "recommendation",
                    "answer_type": "recommendation",
                    "answer": (
                        f"已排除刚才推荐的 {anchor_sku}。严格保留原先的价格定位后没有另一款同时满足条件；"
                        "如果把预算改为查看实时售价后再决定，下面这些候选的其余条件已核验，价格仍需你下单前确认：\n"
                        + relaxed_answer
                    ),
                    "results": relaxed_budget_rows,
                    "result_skus": relaxed_skus,
                    "candidate_skus": relaxed_skus,
                    "debug": {
                        "agent_mode": "recommendation_context_alternative_budget_disclosed",
                        "initial_candidate_skus": [str(row.get("sku") or "").strip().upper() for row in rows],
                    },
                    "answer_metadata": {
                        "source": "recommendation_context_alternative_budget_disclosed",
                        "current_recommendation_contract": current.to_dict(),
                        "inherited_recommendation_contract": inherited.to_dict(),
                        "effective_recommendation_contract": effective.to_dict(),
                        "contract_merge_provenance": provenance,
                    },
                    "skip_polish": True,
                }
        adjacent_heat_source_rows: list[dict] = []
        for row, verification in zip(rows, verifications):
            heat_evidence = verification.evidence_by_constraint.get("heat_source") or {}
            reasons = set(verification.rejection_reasons or [])
            if (
                heat_evidence.get("status") == "verified"
                and reasons == {"subject_category_mismatch"}
            ):
                adjacent_heat_source_rows.append(row)
        if adjacent_heat_source_rows:
            alternatives: list[str] = []
            for row in adjacent_heat_source_rows[:2]:
                sku = str(row.get("sku") or "").strip().upper()
                name = str(row.get("product_name_cn") or sku).strip()
                heat_source = str(row.get("heat_source") or "").strip().replace("\n", "、")
                capacity = _capacity_field_evidence(row.get("capacity"))
                if "水壶" in name:
                    kind = "烧水用水壶"
                elif "炉" in name:
                    kind = "带锅具的炉具套装"
                else:
                    kind = "相邻品类产品"
                detail = f"{name}（{sku}，{kind}，热源标注：{heat_source}"
                if capacity:
                    detail += f"，容量：{capacity}"
                alternatives.append(detail + "）")
            return {
                "intent": "recommendation",
                "answer_type": "recommendation",
                "answer": (
                    f"已排除刚才推荐的 {anchor_sku}。严格按烹饪锅筛选，当前没有另一款同类锅具可确认支持酒精炉。"
                    "不过资料中还有以下明确标注酒精相关热源的替代选择，产品类型不同：\n- "
                    + "\n- ".join(alternatives)
                    + "\n如果你主要是烧水，可以优先看水壶；如果想要煮食，请继续以独立锅具为准。"
                ),
                "results": adjacent_heat_source_rows[:2],
                "result_skus": [str(row.get("sku") or "").strip().upper() for row in adjacent_heat_source_rows[:2]],
                "candidate_skus": [str(row.get("sku") or "").strip().upper() for row in adjacent_heat_source_rows[:2]],
                "debug": {
                    "agent_mode": "recommendation_context_adjacent_heat_source_alternatives",
                    "initial_candidate_skus": [str(row.get("sku") or "").strip().upper() for row in rows],
                },
                "answer_metadata": {
                    "source": "recommendation_context_adjacent_heat_source_alternatives",
                    "current_recommendation_contract": current.to_dict(),
                    "inherited_recommendation_contract": inherited.to_dict(),
                    "effective_recommendation_contract": effective.to_dict(),
                    "contract_merge_provenance": provenance,
                },
                "skip_polish": True,
            }
        subject_label = str(effective.subject_category or inherited.subject_category or "商品").strip()
        condition_labels: list[str] = []
        if effective.heat_sources:
            condition_labels.append("支持" + "、".join(effective.heat_sources))
        if "scenario" in effective.hard_constraints and effective.scenario:
            condition_labels.append("适用于" + "、".join(effective.scenario))
        condition_text = "且".join(condition_labels) or "满足已继承条件"
        excluded_label = anchor_sku or "刚才推荐的商品"
        return {
            "intent": "recommendation",
            "answer_type": "recommendation",
            "answer": (
                f"我已经排除了刚才推荐的 {excluded_label}。"
                f"当前已核验资料中，暂未找到另一款同时{condition_text}的{subject_label}；"
                "为避免把关键条件未标注的商品当作替代推荐，这一轮先不继续扩展。"
            ),
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "debug": {
                "agent_mode": "recommendation_context_alternative_exhausted",
                "initial_candidate_skus": [str(row.get("sku") or "").strip().upper() for row in rows],
                "rejected_candidates": [item.to_dict() for item in verifications if item.verification_level == "rejected"],
            },
            "answer_metadata": {
                "source": "recommendation_context_alternative_exhausted",
                "current_recommendation_contract": current.to_dict(),
                "inherited_recommendation_contract": inherited.to_dict(),
                "effective_recommendation_contract": effective.to_dict(),
                "contract_merge_provenance": provenance,
            },
            "skip_polish": True,
        }
    answer = customer_recommendation_verification_contract.build_verified_recommendation_answer(
        effective,
        selected,
        verifications,
    )
    answer = f"已排除刚才推荐的 {anchor_sku}，下面是其他已核验候选：\n" + answer
    if effective.relative_price_preference:
        answer = (
            "现有资料没有可靠价格信息，无法验证是否比上一款更便宜。"
            "以下候选仅按已继承的其他条件重新核验，并已排除上一款。\n" + answer
        )

    result_skus = [str(row.get("sku") or "").strip().upper() for row in selected if str(row.get("sku") or "").strip()]
    result = {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": selected,
        "result_skus": result_skus,
        "candidate_skus": result_skus,
        "debug": {
            "agent_mode": "recommendation_context_alternative_followup",
            "initial_candidate_skus": [str(row.get("sku") or "").strip().upper() for row in rows],
            "fully_verified_candidates": [item.to_dict() for item in verifications if item.verification_level == "fully_verified"],
            "partially_verified_candidates": [item.to_dict() for item in verifications if item.verification_level == "partially_verified"],
            "rejected_candidates": [item.to_dict() for item in verifications if item.verification_level == "rejected"],
        },
        "answer_metadata": {
            "source": "recommendation_context_alternative_followup",
            "current_recommendation_contract": current.to_dict(),
            "inherited_recommendation_contract": inherited.to_dict(),
            "effective_recommendation_contract": effective.to_dict(),
            "contract_merge_provenance": provenance,
        },
        "skip_polish": True,
    }
    return result


def _budget_recommendation_followup_result(
    db: Session,
    question: str,
    recommendation_context: dict[str, Any] | None,
) -> dict | None:
    """Re-rank a prior recommendation when the customer narrows their budget."""
    text = str(question or "")
    if not (
        any(term in text for term in ("预算不高", "预算有限", "预算低", "便宜点", "别太贵", "性价比"))
        and any(term in text for term in ("推荐", "来一个", "一款", "一个"))
    ):
        return None
    context = recommendation_context if isinstance(recommendation_context, dict) else {}
    inherited_data = context.get("effective_recommendation_contract") or context.get("recommendation_request_contract")
    if not isinstance(inherited_data, dict):
        return None
    inherited = customer_recommendation_verification_contract.RecommendationRequestContract.from_dict(inherited_data)
    current = customer_recommendation_verification_contract.build_recommendation_request_contract(question)
    effective, provenance = customer_recommendation_verification_contract.merge_recommendation_request_contracts(
        inherited,
        current,
        previous_result_skus=_ordered_context_skus(recommendation_context),
        anchor_sku=str(context.get("top_recommended_sku") or context.get("last_referenced_sku") or "").strip().upper(),
        current_turn=_recommendation_context_turn(context),
    )
    if effective.budget_level not in {"low", "affordable"}:
        return None
    # The literal parser names a low-budget request ``low`` while the
    # catalogue verifier represents this customer-facing meaning through the
    # auditable entry/mid-range ``affordable`` price-positioning predicate.
    effective.budget_level = "affordable"
    if "price_positioning" not in effective.hard_constraints:
        effective.hard_constraints.append("price_positioning")
    rows = _phase1_catalog_rows(db, "产品")
    if inherited.subject_category:
        rows = [
            row for row in rows
            if str(row.get("category") or "").strip() == str(inherited.subject_category).strip()
        ]
    verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(effective, rows)
    selected_limit = 1 if _explicit_single_recommendation_request(question) else 3
    selected = customer_recommendation_verification_contract.select_recommendation_candidates(rows, verifications)[:selected_limit]
    if not selected:
        return None
    result_skus = [str(row.get("sku") or "").strip().upper() for row in selected if str(row.get("sku") or "").strip()]
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": customer_recommendation_verification_contract.build_verified_recommendation_answer(
            effective,
            selected,
            verifications,
        ),
        "results": selected,
        "result_skus": result_skus,
        "candidate_skus": result_skus,
        "debug": {
            "agent_mode": "recommendation_context_budget_followup",
            "fully_verified_candidates": [item.to_dict() for item in verifications if item.verification_level == "fully_verified"],
            "partially_verified_candidates": [item.to_dict() for item in verifications if item.verification_level == "partially_verified"],
            "rejected_candidates": [item.to_dict() for item in verifications if item.verification_level == "rejected"],
        },
        "answer_metadata": {
            "source": "recommendation_context_budget_followup",
            "current_recommendation_contract": current.to_dict(),
            "inherited_recommendation_contract": inherited.to_dict(),
            "effective_recommendation_contract": effective.to_dict(),
            "contract_merge_provenance": provenance,
        },
        "skip_polish": True,
    }


def _deterministic_comparison_choice_fallback(
    question: str,
    rows: list[dict[str, Any]],
    evidence_packet: dict[str, list[dict[str, Any]]],
    plan: dict[str, Any],
) -> tuple[int | None, list[str]]:
    """Choose only from complete, directly comparable structured evidence."""
    if len(rows) < 2:
        return None, []
    question_text = str(question or "")
    weight_rows = evidence_packet.get("weight") or []
    weight_indexes = {
        int(item.get("participant_index"))
        for item in weight_rows
        if isinstance(item, dict) and isinstance(item.get("participant_index"), int)
    }
    lightweight_request = any(
        term in question_text
        for term in ("徒步", "背包", "轻量", "轻一点", "便携", "背负")
    )
    if lightweight_request and weight_indexes.issuperset(range(len(rows))):
        comparable: list[tuple[float, int]] = []
        for index, row in enumerate(rows):
            try:
                weight = float(row.get("gross_weight_g"))
            except (TypeError, ValueError):
                continue
            if weight > 0:
                comparable.append((weight, index))
        if len(comparable) == len(rows):
            comparable.sort()
            if len(comparable) == 1 or comparable[0][0] < comparable[1][0]:
                return comparable[0][1], ["weight"]
    requested_people = _phase1_requested_people_count(plan)
    if requested_people is not None:
        scores = [
            _phase1_people_fit_score(row, requested_people)
            for row in rows
        ]
        if len(scores) >= 2 and max(scores) > min(scores):
            return scores.index(max(scores)), ["people"]
    return None, []


def _is_comparison_choice_followup_question(question: str) -> bool:
    text = str(question or "")
    return any(term in text for term in ("更建议哪一个", "更推荐哪一个", "建议哪一个", "推荐哪一个"))


def _comparison_choice_followup_result(
    db: Session,
    question: str,
    recommendation_context: dict[str, Any] | None,
    candidate_context: dict[str, Any] | None,
) -> dict | None:
    """Reuse a previously adjudicated comparison choice for a direct follow-up."""
    if not _is_comparison_choice_followup_question(question):
        return None
    selected_sku = ""
    for context in (recommendation_context, candidate_context):
        if isinstance(context, dict):
            selected_sku = str(context.get("final_choice_sku") or "").strip().upper()
            if selected_sku:
                break
    if not selected_sku:
        return None
    rows_by_sku = {
        str(row.get("sku") or "").strip().upper(): row
        for row in _phase1_catalog_rows(db, "产品")
        if isinstance(row, dict) and str(row.get("sku") or "").strip()
    }
    row = rows_by_sku.get(selected_sku)
    if not row:
        return None
    product_name = str(row.get("product_name_cn") or row.get("product_name_en") or selected_sku).strip()
    reason = _compound_display_value(row.get("usage_scenarios")) or _compound_display_value(row.get("features"))
    answer = f"如果要我明确做选择，更建议{product_name}（{selected_sku}）。"
    comparison_skus: list[str] = []
    for context in (recommendation_context, candidate_context):
        if not isinstance(context, dict):
            continue
        for sku in (
            context.get("ordered_result_skus")
            or context.get("candidate_skus")
            or context.get("previous_result_skus")
            or []
        ):
            normalized = str(sku or "").strip().upper()
            if normalized and normalized not in comparison_skus:
                comparison_skus.append(normalized)
    selected_weight = row.get("gross_weight_g")
    other_weight_rows = [
        rows_by_sku[sku]
        for sku in comparison_skus
        if sku != selected_sku and sku in rows_by_sku
    ]
    if isinstance(selected_weight, (int, float)) and selected_weight > 0:
        comparable = [
            (str(item.get("sku") or "").strip().upper(), item.get("gross_weight_g"))
            for item in other_weight_rows
            if isinstance(item.get("gross_weight_g"), (int, float)) and item.get("gross_weight_g") > 0
        ]
        if comparable:
            other_sku, other_weight = comparable[0]
            difference = float(other_weight) - float(selected_weight)
            if difference > 0:
                answer += (
                    f"商品资料标注重量为{float(selected_weight):g}g，"
                    f"比另一候选 {other_sku} 的{float(other_weight):g}g轻约{difference:g}g。"
                )
    capacity = _capacity_field_evidence(row.get("capacity"))
    if capacity:
        answer += f"容量资料为{capacity}。"
    if reason:
        answer += f"商品资料列出的使用场景包括：{reason}。"
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": [row],
        "result_skus": [selected_sku],
        "candidate_skus": [selected_sku],
        "answer_metadata": {
            "source": "comparison_choice_context_followup",
            "final_choice_sku": selected_sku,
        },
        "debug": {"agent_mode": "comparison_choice_context_followup"},
        "skip_polish": True,
    }


def _recommendation_explanation_followup_result(
    db: Session,
    question: str,
    recommendation_context: dict[str, Any] | None,
    candidate_context: dict[str, Any] | None = None,
) -> dict | None:
    text = str(question or "")
    explanation_terms = (
        "为什么推荐",
        "推荐理由",
        "理由是什么",
        "解释一下为什么",
        "为什么选",
        "为什么是第一个",
        "推荐依据",
        "为什么它",
        "为什么这款",
        "实际使用上的差别",
    )
    if not any(term in text for term in explanation_terms):
        return None
    context = recommendation_context if isinstance(recommendation_context, dict) else {}
    candidate_scope = candidate_context if isinstance(candidate_context, dict) else {}
    ordered = [
        str(sku or "").strip().upper()
        for sku in (context.get("ordered_result_skus") or context.get("recommended_skus") or [])
        if str(sku or "").strip()
    ]
    # “这些产品 / 它们” refers to the product set visibly returned in the
    # preceding answer.  Recommendation contexts may retain only the top
    # recommendation, while candidate context preserves that displayed set.
    # Use the latter only for an explicitly plural explanation; singular
    # follow-ups still explain the recommendation anchor.
    plural_explanation = any(term in text for term in ("这些", "它们", "这几款", "前面推荐的"))
    candidate_ordered = [
        str(sku or "").strip().upper()
        for sku in (candidate_scope.get("ordered_result_skus") or candidate_scope.get("candidate_skus") or [])
        if str(sku or "").strip()
    ]
    if plural_explanation and len(candidate_ordered) >= 2:
        ordered = candidate_ordered
    anchor_sku = str(
        context.get("last_referenced_sku")
        or context.get("top_recommended_sku")
        or (ordered[0] if ordered else "")
    ).strip().upper()
    if not anchor_sku or anchor_sku not in ordered:
        return None
    rows_by_sku = {
        str(item.get("sku") or "").strip().upper(): item
        for item in _phase1_catalog_rows(db, "产品")
        if str(item.get("sku") or "").strip()
    }
    rows = [rows_by_sku[sku] for sku in ordered if isinstance(rows_by_sku.get(sku), dict)]
    if not rows:
        return None
    def explanation_reason(row: dict[str, Any]) -> str:
        reason_parts: list[str] = []
        for key in ("usage_scenarios", "features"):
            value = row.get(key)
            value_text = _compound_display_value(value)
            if value_text and value_text not in reason_parts:
                reason_parts.append(value_text)
        return "；".join(reason_parts[:2]) or "它符合上一轮已核验的推荐条件"
    if plural_explanation and len(rows) >= 2:
        answer = "推荐这些产品的依据：\n" + "\n".join(
            f"- {str(row.get('product_name_cn') or row.get('product_name') or row.get('sku') or '').strip()}（{str(row.get('sku') or '').strip().upper()}）：{explanation_reason(row)}。"
            for row in rows
        )
    else:
        row = next((item for item in rows if str(item.get("sku") or "").strip().upper() == anchor_sku), rows[0])
        product_name = str(row.get("product_name_cn") or row.get("product_name") or anchor_sku).strip()
        comparison_reason = ""
        anchor_weight = row.get("gross_weight_g")
        other_rows = [item for item in rows if str(item.get("sku") or "").strip().upper() != anchor_sku]
        if "比" in text and isinstance(anchor_weight, (int, float)) and anchor_weight > 0 and other_rows:
            other = other_rows[0]
            other_weight = other.get("gross_weight_g")
            if isinstance(other_weight, (int, float)) and other_weight > anchor_weight:
                other_name = str(other.get("product_name_cn") or other.get("sku") or "另一款").strip()
                comparison_reason = (
                    f"在前面两款里它更轻：{anchor_weight:g}g，"
                    f"比{other_name}（{str(other.get('sku') or '').strip().upper()}）轻{other_weight - anchor_weight:g}g；"
                )
        answer = (
            f"推荐 {product_name}（{anchor_sku}）的依据是："
            f"{comparison_reason}{explanation_reason(row)}。"
        )
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": rows if plural_explanation and len(rows) >= 2 else [row],
        "candidate_skus": [str(item.get("sku") or "").strip().upper() for item in rows] if plural_explanation and len(rows) >= 2 else [anchor_sku],
        "result_skus": [str(item.get("sku") or "").strip().upper() for item in rows] if plural_explanation and len(rows) >= 2 else [anchor_sku],
        "debug": {"agent_mode": "recommendation_context_explanation_followup"},
        "answer_metadata": {"source": "recommendation_context_explanation_followup"},
        "skip_polish": True,
    }


def _context_skus_for_pair_followup(*contexts: dict[str, Any] | None) -> list[str]:
    for context in contexts:
        if not isinstance(context, dict):
            continue
        replacement_sku = str(context.get("replacement_top_sku") or "").strip().upper()
        excluded_skus = [
            str(sku or "").strip().upper()
            for sku in (context.get("excluded_skus") or [])
            if str(sku or "").strip()
        ]
        if replacement_sku and excluded_skus:
            return [replacement_sku, excluded_skus[0]]
        for key in ("pair_skus", "ordered_result_skus", "recommended_skus", "candidate_skus"):
            skus = [
                str(sku or "").strip().upper()
                for sku in context.get(key) or []
                if str(sku or "").strip()
            ]
            if len(skus) >= 2:
                return skus[:2]
    return []


def _context_skus_for_power_followup(*contexts: dict[str, Any] | None) -> list[str]:
    """Return the full ordered candidate set for a contextual power ask."""
    for context in contexts:
        if not isinstance(context, dict):
            continue
        for key in ("ordered_result_skus", "candidate_skus", "recommended_skus", "previous_result_skus"):
            skus = list(dict.fromkeys(
                str(sku or "").strip().upper()
                for sku in context.get(key) or []
                if str(sku or "").strip()
            ))
            if len(skus) >= 2:
                return skus[:10]
    return []


def _context_pair_followup_result(db: Session, question: str, skus: list[str]) -> dict | None:
    pair = [str(sku or "").strip().upper() for sku in skus[:2] if str(sku or "").strip()]
    if len(pair) < 2:
        return None
    products = (
        db.query(Product)
        .filter(Product.sku.in_(pair))
        .all()
    )
    by_sku = {str(product.sku or "").strip().upper(): product for product in products}
    rows: list[dict] = []
    for sku in pair:
        product = by_sku.get(sku)
        if not product:
            continue
        specs = db.query(ProductSpecs).filter(ProductSpecs.product_id == product.id).first()
        business = db.query(ProductBusiness).filter(ProductBusiness.product_id == product.id).first()
        content = db.query(ProductContent).filter(ProductContent.product_id == product.id).first()
        rows.append(_product_row_from_model(product, specs, business, content))
    if len(rows) < 2:
        return None
    text = str(question or "")
    final_choice_sku: str | None = None
    if any(term in text for term in ("酒精炉", "酒精")) and any(term in text for term in ("它们", "这两个", "两款", "两个")):
        lines = []
        for row in rows:
            answer, _ = _phase1_alcohol_stove_compatibility_answer(db, row)
            lines.append(f"- {answer}")
        answer = "分别看这两个产品的酒精炉兼容性：\n" + "\n".join(lines)
        answer_type = "product_detail"
        intent = "product_detail"
        agent_mode = "context_pair_product_compatibility"
    else:
        first, second = rows[0], rows[1]
        first_name = first.get("product_name_cn") or first.get("sku")
        second_name = second.get("product_name_cn") or second.get("sku")
        # A contextual comparison should sound like a concise shopping answer.
        # Keep the full row as sealed evidence/results, but do not dump every
        # stored content field (especially long_description_cn) into the reply.
        first_evidence = _phase1_product_evidence_text(first, max_fields=3)
        second_evidence = _phase1_product_evidence_text(second, max_fields=3)
        weight_comparison = any(term in text for term in ("重量", "多重", "重吗", "更重", "更轻", "轻吗", "轻便", "轻一点", "轻量", "背负", "背起来", "一个人背"))
        first_weight = first.get("gross_weight_g")
        second_weight = second.get("gross_weight_g")
        missing_weight = [
            name for name, value in ((first_name, first_weight), (second_name, second_weight))
            if value in (None, "", 0)
        ]
        if weight_comparison and missing_weight:
            answer = (
                f"你前面提到的第一个是{first_name}（{first.get('sku')}），第二个是{second_name}（{second.get('sku')}）。"
                f"{ '、'.join(missing_weight) }\u7f3a\u5c11\u660e\u786e\u91cd\u91cf\u8d44\u6599\uff0c\u56e0\u6b64\u65e0\u6cd5\u51c6\u786e\u6bd4\u8f83\uff0c\u4e5f\u65e0\u6cd5\u51c6\u786e\u5224\u65ad\u8c01\u66f4\u91cd\u6216\u66f4\u8f7b\uff1b\u4e0d\u80fd\u6839\u636e\u5bb9\u91cf\u3001\u6750\u8d28\u6216\u573a\u666f\u63a8\u65ad\u91cd\u91cf\u3002"
            )
        elif weight_comparison:
            first_value = float(first_weight)
            second_value = float(second_weight)
            if first_value == second_value:
                conclusion = "两款重量相同，单看背负重量无法区分优先级。"
            else:
                lighter = first if first_value < second_value else second
                lighter_name = lighter.get("product_name_cn") or lighter.get("sku")
                final_choice_sku = str(lighter.get("sku") or "").strip().upper() or None
                difference = abs(first_value - second_value)
                conclusion = (
                    f"如果主要考虑一个人背负，优先看{lighter_name}（{lighter.get('sku')}）："
                    f"它较轻，约轻{difference:g}g。"
                )
            answer = (
                f"第一个是{first_name}（{first.get('sku')}，重量{first_value:g}g），"
                f"第二个是{second_name}（{second.get('sku')}，重量{second_value:g}g）。"
                f"{conclusion}"
            )
        elif "容量" in text and any(term in text for term in ("人群", "适合", "受众")):
            def _summary(row: dict[str, Any]) -> str:
                name = str(row.get("product_name_cn") or row.get("sku") or "商品").strip()
                sku = str(row.get("sku") or "").strip().upper()
                capacity = _capacity_field_evidence(row.get("capacity")) or "资料未标注"
                audience = _compound_display_value(row.get("target_audience")) or "资料未标注"
                return f"{name}（{sku}）：容量{capacity}；适合人群标注为{audience}。"

            answer = "按容量和适合人群对比：" + _summary(first) + _summary(second)
        elif any(term in text for term in ("新手", "入门", "哪个更适合", "哪款更适合", "哪个更合适", "哪款更合适")):
            def _beginner_score(row: dict[str, Any]) -> int:
                evidence = " ".join(
                    str(row.get(key) or "")
                    for key in ("target_audience", "usage_scenarios", "positioning", "features", "technical_advantages")
                )
                return sum(
                    1
                    for term in ("新手", "入门", "基础款", "易上手", "简单")
                    if term in evidence
                )

            first_score = _beginner_score(first)
            second_score = _beginner_score(second)
            if first_score > second_score:
                selected = first
                final_choice_sku = str(selected.get("sku") or "").strip().upper() or None
                selected_name = selected.get("product_name_cn") or selected.get("sku")
                conclusion = f"如果你是新手，优先看{selected_name}（{selected.get('sku')}）；当前资料对它的新手/入门标注更多。"
            elif second_score > first_score:
                selected = second
                final_choice_sku = str(selected.get("sku") or "").strip().upper() or None
                selected_name = selected.get("product_name_cn") or selected.get("sku")
                conclusion = f"如果你是新手，优先看{selected_name}（{selected.get('sku')}）；当前资料对它的新手/入门标注更多。"
            else:
                conclusion = "当前资料没有对其中一款给出明确的新手/入门倾向，暂时不能可靠断定谁更适合新手；如果你补充更看重的重量、容量或操作偏好，我可以继续细分。"
            answer = (
                f"你前面提到的第一个是{first_name}（{first.get('sku')}），第二个是{second_name}（{second.get('sku')}）。"
                f"{first_name}：{first_evidence or '当前资料未提供足够细项'}。"
                f"{second_name}：{second_evidence or '当前资料未提供足够细项'}。"
                f"{conclusion}"
            )
        elif any(term in text for term in ("新推荐", "新换", "换的")) and any(
            term in text for term in ("上一款", "原来", "之前", "刚才")
        ):
            answer = (
                f"新推荐是{first_name}（{first.get('sku')}），上一款是{second_name}（{second.get('sku')}）。"
                f"新推荐：{first_evidence or '当前资料未提供足够细项'}。"
                f"上一款：{second_evidence or '当前资料未提供足够细项'}。"
            )
        else:
            answer = (
                f"你前面提到的第一个是{first_name}（{first.get('sku')}），第二个是{second_name}（{second.get('sku')}）。"
                f"{first_name}：{first_evidence or '当前资料未提供足够细项'}。"
                f"{second_name}：{second_evidence or '当前资料未提供足够细项'}。"
            )
        answer_type = "comparison"
        intent = "compare_products"
        agent_mode = "context_pair_comparison_followup"
    skus_out = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
    return {
        "intent": intent,
        "answer_type": answer_type,
        "answer": answer,
        "results": rows,
        "result_skus": skus_out,
        "candidate_skus": skus_out,
        "answer_metadata": {"source": agent_mode, "final_choice_sku": final_choice_sku},
        "debug": {
            "agent_mode": agent_mode,
            "raw_results": rows,
            "candidate_skus": skus_out,
            "plan": {
                "primary_intent": intent,
                "answer_type": answer_type,
                "product_refs": skus_out,
                "product_ref": " / ".join(skus_out),
                "requested_field": "heat_source" if answer_type == "product_detail" else "",
                "source": "context_pair_followup",
            },
        },
        "skip_polish": True,
    }


def _context_pair_matches_current_turn_subject(
    db: Session,
    question: str,
    skus: list[str],
) -> bool:
    """Keep a persisted-pair shortcut inside an explicitly named role."""
    contract = customer_recommendation_verification_contract.build_recommendation_request_contract(
        question
    )
    if not contract.subject_kind and not contract.subject_category:
        return True
    pair = {
        str(sku or "").strip().upper()
        for sku in skus[:2]
        if str(sku or "").strip()
    }
    if len(pair) < 2:
        return False
    rows = {
        str(row.get("sku") or "").strip().upper(): row
        for row in _phase1_catalog_rows(db, "产品")
        if str(row.get("sku") or "").strip().upper() in pair
    }
    if len(rows) != len(pair):
        return False
    return all(
        customer_recommendation_verification_contract._subject_evidence(
            contract,
            rows[sku],
        )[0]
        for sku in pair
    )


def _context_power_followup_result(db: Session, question: str, skus: list[str]) -> dict | None:
    """Compare verified power values within the preceding candidate collection."""
    ordered_skus = list(dict.fromkeys(
        str(sku or "").strip().upper()
        for sku in skus[:10]
        if str(sku or "").strip()
    ))
    if len(ordered_skus) < 2:
        return None
    products = db.query(Product).filter(Product.sku.in_(ordered_skus)).all()
    by_sku = {str(product.sku or "").strip().upper(): product for product in products}
    rows: list[dict] = []
    comparable: list[tuple[float, dict, str]] = []
    for sku in ordered_skus:
        product = by_sku.get(sku)
        if not product:
            continue
        specs = db.query(ProductSpecs).filter(ProductSpecs.product_id == product.id).first()
        business = db.query(ProductBusiness).filter(ProductBusiness.product_id == product.id).first()
        content = db.query(ProductContent).filter(ProductContent.product_id == product.id).first()
        row = _product_row_from_model(product, specs, business, content)
        rows.append(row)
        raw_power = str(row.get("power") or "").strip()
        matched_values = [float(value) for value in re.findall(r"(\d+(?:\.\d+)?)\s*[wW瓦]", raw_power)]
        if matched_values:
            comparable.append((max(matched_values), row, raw_power))
    if not rows:
        return None
    if not comparable:
        answer = "前面这些候选当前都没有可直接对照的功率资料，无法确认有没有更大火力的款式；请以具体型号说明书标注为准。"
    else:
        max_power = max(item[0] for item in comparable)
        leaders = [item for item in comparable if item[0] == max_power]
        leader_names = "、".join(
            f"{item[1].get('product_name_cn') or item[1].get('sku')}（{item[1].get('sku')}）"
            for item in leaders
        )
        details = "；".join(
            f"{item[1].get('product_name_cn') or item[1].get('sku')}（{item[1].get('sku')}）{item[2]}"
            for item in comparable
        )
        missing_count = len(rows) - len(comparable)
        missing_rows = [row for row in rows if not any(row is item[1] for item in comparable)]
        missing_names = "、".join(
            f"{row.get('product_name_cn') or row.get('sku')}（{row.get('sku')}）"
            for row in missing_rows
        )
        if missing_count:
            missing_note = (
                f"；未标注功率的候选为{missing_names}，未参与比较，"
                "因此无法确认它们是否存在比已标注款更大的火力"
            )
            answer = (
                f"按前面候选中已标注的功率，{leader_names}最高，为{max_power:g}W。"
                f"已标注的对照为：{details}{missing_note}。"
            )
        else:
            answer = f"按前面候选中已标注的功率对比，{leader_names}的火力最大，为{max_power:g}W。已标注的对照为：{details}。"
    result_skus = [str(row.get("sku") or "").strip().upper() for row in rows if str(row.get("sku") or "").strip()]
    return {
        "intent": "compare_products",
        "answer_type": "comparison",
        "answer": answer,
        "results": rows,
        "result_skus": result_skus,
        "candidate_skus": result_skus,
        "answer_metadata": {"source": "context_collection_power_comparison"},
        "debug": {
            "agent_mode": "context_collection_power_comparison",
            "raw_results": rows,
            "candidate_skus": result_skus,
            "plan": {
                "primary_intent": "compare_products",
                "answer_type": "comparison",
                "product_refs": result_skus,
                "requested_field": "power",
                "source": "context_collection_power_comparison",
            },
        },
        "skip_polish": True,
    }


def _is_plural_heat_source_followup(question: str) -> bool:
    text = str(question or "")
    return (
        any(term in text for term in ("它们", "这两个", "上面两个", "这几款", "两款", "两个"))
        and any(term in text for term in ("酒精炉", "酒精", "热源"))
        and any(term in text for term in ("能不能", "能用", "可以用", "支持", "是否支持"))
    )


def _is_contextual_power_followup_question(question: str) -> bool:
    text = str(question or "").strip()
    return bool(
        any(term in text for term in ("火力", "功率"))
        and any(term in text for term in ("更大", "更强", "最大", "有没有", "哪款大", "哪款强"))
    )


def _phase1_result_skus(agent_result: dict) -> list[str]:
    skus = []
    for key in ("result_skus", "candidate_skus"):
        for sku in agent_result.get(key) or []:
            text = str(sku or "").strip()
            if text and text not in skus:
                skus.append(text)
    for row in agent_result.get("results") or []:
        if isinstance(row, dict):
            sku = str(row.get("sku") or "").strip()
            if sku and sku not in skus:
                skus.append(sku)
    return skus


def _phase1_answer_mentions_product(agent_result: dict) -> bool:
    answer = str(agent_result.get("answer") or "")
    if re.search(r"\b[A-Z]{1,6}-[A-Z0-9][A-Z0-9-]{1,40}\b", answer):
        return True
    return any(term in answer for term in ("单锅", "套锅", "锅具", "烤盘", "水壶", "行山", "激川"))


def _phase1_explicit_recommendation_scope_ref(question: str) -> str:
    text = str(question or "").strip()
    if not text:
        return ""
    # A category mentioned in a comparison is one side of the trade-off, not
    # a hard recommendation scope.  Keep both catalog pools available so the
    # dedicated comparison route can evaluate them.
    if (
        customer_agent_planner_service._is_compare_question(text)
        or customer_agent_planner_service._is_compare_choice_question(text)
        or customer_agent_intent_service._is_compare_question(text)
        or _phase1_is_griddle_vs_cookware_scenario(text)
        or "还是" in text
    ):
        return ""
    ref = _semantic_catalog_product_ref(text)
    if ref in {"炉具", "锅具", "烤盘", "水壶", "水具", "咖啡器具", "茶具", "配件", "餐具", "桌椅"}:
        return ref
    return ""


def _looks_like_product_capability_or_field_question(text: str) -> bool:
    return bool(_explicit_sku_detail_requested_fields(text)) or _looks_like_product_detail_field_question(text)


def _is_generic_field_subject(subject: str) -> bool:
    value = re.sub(r"(?:一般|通常)$", "", str(subject or "").strip())
    return value in {"烤盘", "煎盘", "锅", "锅具", "水壶", "水杯", "杯子", "炉具", "炉子", "配件"}


def _is_generic_company_product_scope(subject: str) -> bool:
    value = re.sub(r"\s+", "", str(subject or "")).strip()
    return bool(re.fullmatch(r"(?:你们|我们|咱们|本店|你家|品牌|公司)(?:的)?(?:产品|商品|货品)", value))


def _entity_scope_pre_route_guard_result(
    db: Session,
    question: str,
    phase1_plan: dict | None,
    semantic_preplan: dict | None,
) -> dict | None:
    text = str(question or "").strip()
    if not text:
        return None
    # Semantic product-QA already has a dedicated sealed path below the
    # shortcut layer.  Its entity contract can use a unique canonical name
    # from the full turn before bounded same-SKU QA/KB retrieval.  This broad
    # scope guard is for catalogue/recommendation ambiguity and must not
    # return a clarification first merely because the semantic subject span
    # includes part of the predicate.
    if _semantic_prefers_sealed_product_qa(phase1_plan or {}):
        return None
    # A formal FieldContract must still reach Phase 2, where the
    # EntityResolutionContract records the unresolved identity.  The semantic
    # scope guard below is only a pre-recommendation safety veto for turns
    # that have no formal field contract at all.
    formal_field_contract = customer_field_contract.resolve_requested_field_contract(
        text,
        {"semantic_preplan": semantic_preplan or {}},
    )
    semantic_entity_scope = _semantic_preplan_entity_scope(semantic_preplan)
    semantic_fields = list(formal_field_contract.get("canonical_fields") or [])
    unsupported_semantic_field = bool(semantic_fields) and not all(
        customer_field_contract.is_supported_detail_field(field) for field in semantic_fields
    )
    if (
        (not semantic_fields or unsupported_semantic_field)
        and _semantic_preplan_route_family(semantic_preplan) == "recommendation"
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and semantic_entity_scope in {"unresolved_product_like", "unresolved_product", "negative_product_like", "negative_product"}
    ):
        semantic_subjects = [
            str(item or "").strip()
            for item in ((semantic_preplan or {}).get("entities") or [])
            if str(item or "").strip()
        ]
        subject = semantic_subjects[0] if semantic_subjects else _product_like_scope_subject(text)
        label = (
            _resolved_entity_realtime_commercial_field_guard_label(text)
            or _detect_unknown_product_fact_label(text)
            or str((semantic_preplan or {}).get("field_hint") or "相关")
        )
        unresolved = _build_unresolved_product_like_unknown_field_clarification_result(
            text,
            label=label,
            subject_override=subject,
        )
        # A semantic unresolved-product scope is itself a sealed safety fact.
        # The optional lexical subject extractor may not isolate a clean span
        # from every natural phrasing (for example a product-like name followed
        # immediately by a commercial question).  Do not let that extraction
        # limitation hand the turn to a catalogue-wide recommendation route.
        # The generic clarification preserves the semantic identity verdict
        # without inventing a subject, SKU, or field value.
        return unresolved or _generic_unresolved_product_like_unknown_field_clarification_result(
            text,
            label=label,
            subject_override=subject,
        )
    # A formal FieldContract owns the field meaning.  This legacy scope guard
    # may still protect genuinely unclassified product-like text, but it must
    # not consume a supported or safely-unsupported field before the central
    # EntityResolutionContract gets the chance to seal an exact identity.
    # Identity ambiguity remains fail-closed downstream; this only removes an
    # obsolete pre-contract shortcut.
    # A lexical alias is not enough to prove that its surrounding text is a
    # real field predicate: a product-like name can itself contain words such
    # as “品牌”.  Keep an unresolved product-like scope in entity arbitration
    # even when a FieldContract candidate exists, so it cannot fall through to
    # catalogue recommendation before the name has been rejected.
    if formal_field_contract.get("canonical_fields") and not _has_unresolved_product_like_scope(db, text):
        return None
    # Interrogative catalogue subjects are not concrete product identities.
    # Let query/recommendation routing own them rather than producing a
    # single-product clarification before the executor runs.
    if re.match(r"^(?:哪种|哪款|哪些|什么|有哪|有没有)", text):
        return None
    if _looks_like_ordinal_product_field_followup(text):
        return None
    category_contract = phase1_plan if isinstance(phase1_plan, dict) else {}
    if (
        str(category_contract.get("primary_intent") or "") == "category_compatibility"
        and bool(category_contract.get("must_stay_within_category"))
        and str(category_contract.get("category_ref") or "").strip()
    ):
        return None
    # A planner-confirmed catalogue aggregation has no single-product
    # identity to arbitrate.  Keep it on the structured count/list contract
    # before the product-scope guard can mistake its category noun for a
    # missing product name.
    if (
        str(category_contract.get("primary_intent") or "") == "catalog_count"
        and _looks_like_semantic_catalog_query(text)
        and not _contains_explicit_negative_product_marker(text)
    ):
        return None
    # A planner-confirmed recommendation contract describes a candidate set,
    # even when it contains negative product constraints such as excluding a
    # two-person option.  It is not a request to resolve that constraint as a
    # single product identity.  Let the recommendation executor filter the
    # catalogue; explicit named-product facts continue through the entity
    # contract below.
    if (
        str(category_contract.get("primary_intent") or "") == "recommendation"
        and not _products_named_in_question(db, text)
        and not _has_unresolved_product_like_scope(db, text)
    ):
        return None
    # A planner-confirmed comparison is a multi-entity operation, not a
    # request to collapse the whole phrase into one product identity.  Let the
    # comparison executor resolve every named participant and make its sealed
    # decision; the single-entity guard below is intentionally inapplicable.
    if (
        str(category_contract.get("primary_intent") or "") in {"comparison", "product_compare_recommendation"}
        or bool(category_contract.get("must_compare_both_products"))
    ):
        return None
    # A formal field does not prove a product reference.  Keep unresolved
    # product-like requests in entity arbitration so price/manual/shipping and
    # other formal fields cannot bypass the sealed-identity requirement.
    if customer_agent_service._extract_skus(text):
        return None
    if customer_agent_intent_service._looks_like_multi_product_relation_question(text):
        return None
    # Commercial/realtime fields are never safe to answer from a fuzzy name
    # recall. Establish the same strong-identity threshold before semantic
    # category hints or generic fallbacks can consume the request.
    early_subject = _product_like_scope_subject(text) or customer_agent_intent_service._detail_subject_from_question(text).strip()
    early_unknown_label = (
        _resolved_entity_realtime_commercial_field_guard_label(text)
        or _detect_unknown_product_fact_label(text)
    )


    if early_unknown_label and early_subject and _looks_like_product_like_scope_subject(early_subject):
        early_candidates = _products_named_in_question(db, text)
        if not any(_subject_strongly_matches_product(early_subject, product) for product in early_candidates):
            unresolved = _build_unresolved_product_like_unknown_field_clarification_result(
                text,
                label=early_unknown_label,
                subject_override=early_subject,
            )
            if unresolved:
                return unresolved
            return _build_unknown_field_product_not_found_result(
                text,
                label=early_unknown_label,
                subject_override=early_subject,
            )
    if _contains_explicit_negative_product_marker(text):
        subject = customer_field_contract.select_entity_subject_for_routing(
            raw_question=text,
            fallback_product_like_subject=_product_like_scope_subject(text),
            fallback_named_subject=customer_agent_intent_service._detail_subject_from_question(text).strip(),
        ).entity_subject
        unknown_label = (
            _resolved_entity_realtime_commercial_field_guard_label(text)
            or _detect_unknown_product_fact_label(text)
        )
        if _looks_like_recommendation_request(text):
            return {
                "intent": "clarify",
                "answer_type": "clarification",
                "answer": f"没有找到“{subject or '该商品'}”对应的产品资料，请确认商品名或提供 SKU。",
                "results": [],
                "result_skus": [],
                "candidate_skus": [],
                "needs_clarification": True,
                "answer_metadata": {"source": "unknown_field_product_not_found", "evidence_status": "product_not_found"},
                "debug": {"agent_mode": "entity_scope_product_not_found"},
                "skip_polish": True,
            }
        if unknown_label:
            unresolved = _build_unresolved_product_like_unknown_field_clarification_result(
                text,
                label=unknown_label,
                subject_override=subject,
            )
            if unresolved:
                return unresolved
            return _build_unknown_field_product_not_found_result(text, label=unknown_label, subject_override=subject)
    route_family_hint = _semantic_preplan_route_family(semantic_preplan)
    entity_scope_hint = _semantic_preplan_entity_scope(semantic_preplan)
    if (
        _semantic_preplan_confident(semantic_preplan)
        and route_family_hint in {"structured_query", "recommendation", "generic_query"}
        and entity_scope_hint in {"category_scope", "generic_scope"}
        and not _has_unresolved_product_like_scope(db, text)
    ):
        return None
    if (
        route_family_hint not in {"unresolved_product_like", "negative_product_like"}
        and entity_scope_hint not in {
            "unresolved_product",
            "negative_product",
            "unresolved_product_like",
            "negative_product_like",
        }
        and _structured_hard_filter_contract(text)
    ):
        return None

    subject_selection = customer_field_contract.select_entity_subject_for_routing(
        raw_question=text,
        fallback_product_like_subject=_product_like_scope_subject(text),
        fallback_named_subject=customer_agent_intent_service._detail_subject_from_question(text).strip(),
    )
    extracted_subject = subject_selection.entity_subject
    subject = extracted_subject or ("" if subject_selection.source == "field_contract" else text)
    if (
        customer_field_contract.is_field_contract_predicate_signal(subject_selection)
        and _is_generic_field_subject(extracted_subject)
    ):
        generic_subject = re.sub(r"(?:一般|通常)$", "", extracted_subject).strip()
        return {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": f"“{generic_subject}”是商品类目，还不能对应到具体商品。请提供完整商品名或 SKU。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "needs_clarification": True,
            "answer_metadata": {"source": "entity_scope_generic_clarification", "evidence_status": "generic_product"},
            "debug": {"agent_mode": "entity_scope_generic_clarification"},
            "skip_polish": True,
        }
    unresolved_scope = _has_unresolved_product_like_scope(db, text)
    subject_product_like = (
        (bool(extracted_subject) and _looks_like_product_like_scope_subject(extracted_subject))
        or unresolved_scope
        or _contains_explicit_negative_product_marker(text)
    )
    if not subject_product_like:
        return None
    if _looks_like_generic_constraint_category_query(text):
        return None

    named_products = _products_named_in_question(db, text)
    if extracted_subject and named_products:
        all_products = db.query(Product).order_by(Product.sku.asc()).all()
        entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
            text,
            all_products,
            resolver_candidates=named_products,
            entity_text_override=extracted_subject,
        )
        entity_decision = customer_entity_resolution_contract.can_resolve_single_product(
            entity_contract,
            all_products,
        )
        if entity_decision.allowed:
            return None
    if (
        subject
        and named_products
        and _looks_like_unresolved_product_like_question(text)
        and not customer_agent_intent_service._looks_like_contents_grounding_question(text)
    ):
        strong_named_products = [product for product in named_products if _subject_strongly_matches_product(subject, product)]
        if strong_named_products:
            named_products = strong_named_products
        else:
            named_products = []
    unknown_label = (
        _resolved_entity_realtime_commercial_field_guard_label(text)
        or _detect_unknown_product_fact_label(text)
    )
    unresolved_named_product_question = bool(
        unresolved_scope
        and _product_like_scope_subject(text)
    )
    product_bound_question = bool(
        unknown_label
        or unresolved_named_product_question
        or customer_agent_intent_service._looks_like_contents_grounding_question(text)
        or customer_agent_intent_service._looks_like_usage_care_question(text)
        or _looks_like_product_capability_or_field_question(text)
        or _looks_like_recommendation_request(text)
        or _looks_like_structured_field_filter_query(text)
    )
    if not product_bound_question:
        return None

    if customer_agent_intent_service._looks_like_contents_grounding_question(text):
        contents_clarification = _unresolved_product_like_contents_clarification_result(db, text)
        if contents_clarification:
            return contents_clarification

    if len(named_products) > 1 or entity_scope_hint in {"ambiguous_product", "ambiguous_product_name"}:
        if unknown_label:
            return _build_generic_ambiguous_unknown_field_clarification_result(
                text,
                label=unknown_label,
                products=named_products,
                subject_override=subject,
            )
        if customer_agent_intent_service._looks_like_contents_grounding_question(text):
            options = "；".join(
                f"{str(product.product_name_cn or product.product_name_en or product.sku).strip()}（{str(product.sku or '').strip().upper()}）"
                for product in named_products[:5]
            )
            answer = (
                f"匹配到多个相关商品：{options}。"
                "请先指定 SKU 或具体款式，我再帮你查包装清单、配件或开箱内容。"
            )
            candidate_skus = [str(product.sku or "").strip().upper() for product in named_products[:5] if getattr(product, "sku", None)]
            return {
                "intent": "clarify",
                "answer_type": "clarification",
                "answer": answer,
                "results": [],
                "result_skus": [],
                "candidate_skus": candidate_skus,
                "needs_clarification": True,
                "answer_metadata": {"source": "unresolved_product_like_contents_clarification", "evidence_status": "ambiguous_product"},
                "debug": {"agent_mode": "unresolved_product_like_contents_clarification"},
                "skip_polish": True,
            }
        return {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": f"我还不能把“{subject}”稳定定位到唯一商品。请补充 SKU 或更完整的商品名。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [str(product.sku or "").strip().upper() for product in named_products if getattr(product, 'sku', None)],
            "needs_clarification": True,
            "answer_metadata": {"source": "entity_scope_ambiguous_clarification", "evidence_status": "ambiguous_product"},
            "debug": {"agent_mode": "entity_scope_ambiguous_clarification"},
            "skip_polish": True,
        }

    if len(named_products) == 1 and not unresolved_scope:
        return None

    if route_family_hint == "product_bound_qa" and entity_scope_hint == "resolved_product":
        return None

    if unknown_label:
        unresolved = _build_unresolved_product_like_unknown_field_clarification_result(
            text,
            label=unknown_label,
            subject_override=subject,
        )
        if unresolved:
            return unresolved
        return _build_unknown_field_product_not_found_result(text, label=unknown_label, subject_override=subject)

    if (
        _looks_like_product_capability_or_field_question(text)
        and not customer_agent_intent_service._looks_like_contents_grounding_question(text)
        and not _looks_like_recommendation_request(text)
        and not _contains_explicit_negative_product_marker(text)
        and not unresolved_scope
    ):
        return None

    if (
        not customer_agent_intent_service._looks_like_contents_grounding_question(text)
        and not _looks_like_recommendation_request(text)
        and not _contains_explicit_negative_product_marker(text)
        and (
            customer_agent_intent_service._looks_like_usage_care_question(text)
            or _looks_like_product_capability_or_field_question(text)
            or route_family_hint == "product_bound_qa"
        )
    ):
        return {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": f"我还不能把“{subject}”稳定定位到唯一商品。请补充 SKU 或更完整的商品名，我再继续确认这个问题。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "needs_clarification": True,
            "answer_metadata": {"source": "entity_scope_ambiguous_clarification", "evidence_status": "ambiguous_product"},
            "debug": {"agent_mode": "entity_scope_ambiguous_clarification"},
            "skip_polish": True,
        }

    if customer_agent_intent_service._looks_like_contents_grounding_question(text):
        no_match_answer = (
            f"没有找到“{subject}”对应的产品资料。"
            "请先指定 SKU 或具体款式，我再帮你查包装清单、配件或开箱内容。"
        )
    else:
        no_match_answer = f"没有找到“{subject}”对应的产品资料，请确认商品名或提供 SKU。"
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": no_match_answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "answer_metadata": {"source": "unknown_field_product_not_found", "evidence_status": "product_not_found"},
        "debug": {"agent_mode": "entity_scope_product_not_found"},
        "skip_polish": True,
    }


def _is_candidate_set_heat_source_filter(question: str) -> bool:
    """Whether a heat-source request filters a prior set, not one product."""
    text = str(question or "")
    return bool(
        any(term in text for term in ("酒精炉", "酒精", "热源"))
        and any(term in text for term in ("哪些", "有哪", "这些", "这几款", "它们", "两款", "两个"))
    )


def _product_like_scope_subject(question: str) -> str:
    text = str(question or "").strip()
    if not text:
        return ""
    if "还是" in text and any(term in text for term in ("更", "优先", "先买", "选", "实用", "合适", "值得")):
        return ""
    patterns = (
        r"^(?P<subject>.+?)\s*(?:包装里有什么|盒子里有什么|包装清单是什么|包装清单是啥|有哪些配件|有什么配件|有啥配件|有没有配件|包含哪些东西|包含哪些内容).*$",
        r"^(?P<subject>.+?)\s*(?:有哪些|有什么|有啥|有没有|包含哪些|包装里有|盒子里有|包装清单是|包装清单是什么).*(?:配件|附件|包装清单|东西|内容|内容物).*$",
        r"^(?P<subject>.+?)(?:能不能|能用|可不可以|可以|多少钱|什么价|有活动吗|有优惠吗|带什么|运费多少|包邮吗|今天能发吗|有吗|有无|在吗).*$",
        r"^(?P<subject>.+?)是(?:钛的|不锈钢|铝合金|硬氧|什么材质|哪种材质|几个人|几人|多大|多少|哪个|哪款|哪种|哪一个|什么).*$",
        r"^(?P<subject>.+?)\s*(?:适合|适不适合).*$",
        r"^(?P<subject>.+?)(?:推荐(?:一|几|一下|一个|几个|几款)?).*$",
        r"^(?P<subject>.+?)(?:的)?(?:材质|容量|重量|尺寸|配件|内容物|赠品|运费|包邮|库存|发货|售后|保修).*$",
    )
    for pattern in patterns:
        match = re.match(pattern, text)
        if not match:
            continue
        subject = str(match.group("subject") or "").strip("，。！？；;：: ")
        if subject:
            return subject
    return ""


def _looks_like_product_like_scope_subject(subject: str) -> bool:
    text = customer_agent_service.normalize_search_text(subject).strip()
    if not text:
        return False
    if text.startswith(("哪些", "有哪些", "哪款", "哪几款", "有什么", "推荐", "给我推荐", "适合")):
        return False
    if any(term in text for term in ("什么", "哪个", "哪些", "哪款", "怎么", "如何")):
        return False
    if text in {"卡式炉", "酒精炉", "燃气炉", "气炉"}:
        return False
    if _is_generic_contents_subject(text) or _is_generic_modifier_prefixed_product_scope(text):
        return False
    if customer_agent_intent_service._looks_like_named_product_term(text):
        return True
    category_tokens = ("水壶", "咖啡壶", "套锅", "锅具", "炉具", "烤盘", "水杯", "杯", "壶", "锅", "炉")
    matched_token = next((token for token in category_tokens if token in text), "")
    if not matched_token:
        return False
    if re.search(r"[A-Za-z0-9\-]", text):
        return True
    generic_constraint_terms = (
        "适合", "露营", "徒步", "野餐", "公园", "卡式炉", "酒精炉", "燃气炉", "气炉", "多人", "双人",
        "两人", "两个人", "轻量", "轻便", "预算", "便宜", "推荐", "能配", "能用", "可以用",
    )
    if any(term in text for term in generic_constraint_terms):
        return False
    prefix = text.split(matched_token, 1)[0].strip()
    if len(prefix) < 2:
        return False
    descriptor_terms = (
        "不锈钢", "铝合金", "硬氧", "钛", "轻量", "轻便", "便携", "徒步", "新手", "预算", "便宜",
        "双人", "两人", "两个人", "公园", "野餐", "户外", "露营", "咖啡",
    )
    descriptor_remainder = prefix
    for term in descriptor_terms:
        descriptor_remainder = descriptor_remainder.replace(term, "")
    return bool(descriptor_remainder.strip())


def _contains_explicit_negative_product_marker(text: str) -> bool:
    value = str(text or "").strip()
    if not value:
        return False
    return any(term in value for term in ("不存在", "完全不存在", "虚构", "假的", "并不存在", "没这个", "没有这个"))


def _is_generic_category_phrase(subject: str) -> bool:
    text = str(subject or "").strip()
    if not text:
        return False
    generic_scopes = {
        "水壶", "水杯", "杯", "杯子", "锅", "锅具", "炉", "炉具", "炉子", "装备", "咖啡器具", "配件", "商品", "产品",
        "咖啡壶", "壶", "水具",
    }
    return text in generic_scopes


def _is_generic_modifier_prefixed_product_scope(subject: str) -> bool:
    text = str(subject or "").strip()
    if not text:
        return True
    generic_prefixes = (
        "轻便", "轻便的", "轻量", "轻量的", "不要太重", "不要太重的", "比较轻", "比较轻的", "适合徒步", "适合徒步的", "适合新手", "适合新手的",
        "预算低一点", "预算低一点的", "预算不高", "预算不高的", "适合卡式炉", "适合卡式炉的", "公园野餐", "夏天徒步", "夏天", "冷水", "补水",
        "热水", "饮水", "有什么", "推荐几个", "推荐一个", "推荐", "两个人", "双人", "新手", "徒步", "露营", "野餐",
        "评价最好", "客户评价最好", "销量最高", "最好", "最高",
    )
    if any(text.startswith(prefix) for prefix in generic_prefixes):
        return True
    if _is_generic_category_phrase(text):
        return True
    product_ref = _semantic_catalog_product_ref(text)
    if product_ref and product_ref != "产品" and text.endswith(product_ref):
        prefix = text[: -len(product_ref)].strip()
        if not prefix:
            return True
        if any(token in prefix for token in ("轻便", "轻量", "不要太重", "比较轻", "预算低", "预算不高", "便宜", "新手", "徒步", "露营", "野餐", "公园", "夏天", "冷水", "补水", "热水", "饮水", "有什么", "双人", "两个人", "两人", "评价最好", "客户评价最好", "销量最高", "最好", "最高")):
            return True
    return False


def _looks_like_generic_constraint_category_query(text: str) -> bool:
    value = str(text or "").strip()
    if not value:
        return False
    if any(term in value for term in ("能用", "可以", "可不可以", "能不能", "多少钱", "运费", "包邮", "库存", "发货", "保修", "售后", "评价", "包含什么", "里有什么", "带什么")):
        return False
    subject = _product_like_scope_subject(value) or customer_agent_intent_service._detail_subject_from_question(value).strip()
    if not subject:
        subject = value
    if _contains_explicit_negative_product_marker(value):
        return False
    # A purchase request with an explicit party-size/usage constraint is a
    # catalogue recommendation even when the sentence contains a descriptive
    # noun phrase such as "5个人...的锅".  Do this before the unresolved-name
    # guard; an unknown named product without these structured constraints
    # still fails closed below.
    category_terms = ("水杯", "杯子", "杯", "水壶", "壶", "锅具", "套锅", "锅", "炉具", "炉子", "炉", "装备", "咖啡器具", "配件", "水具")
    recommendation_constraints = (
        "大容量", "多人", "单人", "双人", "轻便", "轻量", "预算", "便宜",
        "徒步", "露营", "野餐", "公园", "做饭", "烹饪", "不要",
    )
    has_variant_identity = bool(re.search(
        r"\d+(?:\.\d+)?\s*(?:杯|寸|英寸|毫升|ml|升|l)(?:[-－—_/ ]*(?:黑|白|红|蓝|绿|粉|灰|银|金|橙|黄|紫)(?:色)?)?",
        subject,
        re.IGNORECASE,
    ))
    if (
        _looks_like_recommendation_request(value)
        and any(term in value for term in category_terms)
        and not has_variant_identity
        and (
            bool(re.search(r"\d+\s*(?:个)?人", value))
            or any(term in value for term in recommendation_constraints)
        )
    ):
        return True
    if _looks_like_product_like_scope_subject(subject):
        return False
    if _is_generic_modifier_prefixed_product_scope(subject):
        return True
    modifier_terms = ("轻便", "轻量", "不要太重", "比较轻", "预算低", "预算不高", "便宜", "新手", "徒步", "露营", "野餐", "公园", "卡式炉", "酒精炉", "双人", "两个人", "两人")
    return any(term in subject for term in category_terms) and any(term in value for term in modifier_terms)


def _looks_like_unresolved_product_like_question(text: str) -> bool:
    value = str(text or "").strip()
    if not value:
        return False
    subject = _product_like_scope_subject(value) or customer_agent_intent_service._detail_subject_from_question(value).strip()
    if not subject:
        return False
    if _looks_like_generic_constraint_category_query(value):
        return False
    if _contains_explicit_negative_product_marker(value):
        return True
    if _is_generic_modifier_prefixed_product_scope(subject):
        return False
    if _looks_like_product_like_scope_subject(subject):
        return True
    return False


def _subject_strongly_matches_product(subject: str, product: Product) -> bool:
    normalized_subject = customer_agent_service.normalize_search_text(subject).lower().strip()
    if not normalized_subject:
        return False
    normalized_subject_compact = re.sub(r"\s+", "", normalized_subject)
    alias_values: list[str] = []
    seen_aliases: set[str] = set()
    for raw_value in (
        getattr(product, "name", ""),
        getattr(product, "product_name_cn", ""),
        getattr(product, "product_name_en", ""),
    ):
        for alias in (
            customer_agent_service.product_name_aliases(raw_value)
            + customer_agent_service.product_name_family_aliases(raw_value)
        ):
            normalized_alias = customer_agent_service.normalize_search_text(alias).lower().strip()
            if not normalized_alias or normalized_alias in seen_aliases:
                continue
            seen_aliases.add(normalized_alias)
            alias_values.append(normalized_alias)
    for raw_value in (
        *alias_values,
        getattr(product, "name", ""),
        getattr(product, "product_name_cn", ""),
        getattr(product, "product_name_en", ""),
        getattr(product, "sku", ""),
        getattr(product, "barcode", ""),
    ):
        normalized_name = customer_agent_service.normalize_search_text(raw_value).lower().strip()
        if not normalized_name:
            continue
        normalized_name_compact = re.sub(r"\s+", "", normalized_name)
        if normalized_subject == normalized_name or normalized_subject_compact == normalized_name_compact:
            return True
        if len(normalized_subject_compact) >= 4 and normalized_subject_compact in normalized_name_compact:
            return True
    return False


def _has_unresolved_product_like_scope(db: Session, question: str) -> bool:
    text = str(question or "").strip()
    if not text or customer_agent_service._extract_skus(text):
        return False
    subject = _product_like_scope_subject(text) or customer_agent_intent_service._detail_subject_from_question(text).strip()
    if not subject:
        return False
    if _looks_like_generic_constraint_category_query(text):
        return False
    if not _looks_like_unresolved_product_like_question(text):
        return False
    # Fuzzy recall is diagnostic only.  A similarly named product must not
    # suppress unresolved handling unless the extracted subject strongly
    # matches that product's own canonical/alias identity.
    return not any(
        _subject_strongly_matches_product(subject, product)
        for product in _products_named_in_question(db, text)
    )


def _has_resolved_named_product_contract(db: Session, question: str) -> bool:
    """Return true only when the current utterance seals one product identity."""
    products = db.query(Product).order_by(Product.sku.asc()).all()
    default_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        products,
    )
    canonical_subject = customer_entity_resolution_contract.unique_canonical_subject_in_question(
        question,
        products,
    )
    canonical_contract = (
        customer_entity_resolution_contract.build_entity_resolution_contract(
            question,
            products,
            entity_text_override=canonical_subject,
        )
        if canonical_subject
        else None
    )
    selection = customer_entity_resolution_contract.choose_effective_entity_contract(
        default_contract,
        canonical_contract,
        products,
    )
    return bool(
        customer_entity_resolution_contract.can_resolve_single_product(
            selection.contract,
            products,
        ).allowed
    )


def _reject_unsealed_named_product_phase1_result(
    db: Session,
    question: str,
    agent_result: dict | None,
) -> dict | None:
    """Keep generic Phase-1 retrieval from answering a uniquely named product."""
    if not isinstance(agent_result, dict):
        return agent_result
    if str(agent_result.get("answer_type") or "").strip() not in {
        "knowledge_base_answer",
        "knowledge_answer",
        "product_query",
        "query_products",
    }:
        return agent_result
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    if isinstance(debug.get("entity_resolution_contract"), dict):
        return agent_result
    if _has_resolved_named_product_contract(db, question):
        return None
    return agent_result


def _recommendation_question_contract(question: str) -> dict[str, Any]:
    text = str(question or "").strip()
    if not text or not _looks_like_recommendation_request(text):
        return {}
    filters: dict[str, Any] = {}
    negative_filters: dict[str, Any] = {}

    if "水壶" in text:
        filters["product.category"] = "水壶"
    elif any(term in text for term in ("水杯", "杯子", "钛杯", "杯")):
        filters["product.category"] = "水杯"
    elif any(term in text for term in ("锅具", "锅", "套锅", "炊具")):
        filters["product.category"] = "锅具"
    elif any(term in text for term in ("炉具", "炉子", "炉头", "酒精炉", "卡式炉", "气炉", "燃气炉")):
        filters["product.category"] = "炉具"
    elif "咖啡" in text:
        filters["product.category"] = "咖啡器具"

    if "钛" in text:
        filters["specs.body_material"] = "钛"
    elif "不锈钢" in text:
        filters["specs.body_material"] = "不锈钢"
    elif "铝合金" in text:
        filters["specs.body_material"] = "铝合金"

    if "卡式炉" in text:
        filters["specs.heat_source"] = "卡式炉"
    elif "酒精炉" in text:
        filters["specs.heat_source"] = "酒精炉"
    elif "明火直烧" in text:
        filters["specs.heat_source"] = "明火直烧"
    elif "直接加热" in text or "明火" in text:
        filters["specs.heat_source"] = "明火"

    if "徒步" in text:
        filters["_contract.hiking"] = "true"
    if "轻量" in text or "轻便" in text or "不要太重" in text or "别太重" in text or "比较轻" in text:
        filters["_contract.lightweight"] = "true"
    if "新手" in text:
        filters["_contract.beginner"] = "true"
    if "预算低" in text or "预算不高" in text or "预算低一点" in text or "便宜点" in text or "性价比" in text:
        filters["_contract.budget"] = "true"
    if "两个人" in text or "两人" in text or "2人" in text or "双人" in text:
        filters["_contract.two_person"] = "true"
    if "公园" in text or "野餐" in text:
        filters["_contract.lightweight"] = "true"
    if "不能进洗碗机" in text or "不能放洗碗机" in text or "不可进洗碗机" in text:
        negative_filters["_contract.dishwasher"] = "洗碗机"

    return {"filters": filters, "negative_filters": negative_filters}


def _should_use_central_subject_recommendation(
    *,
    question: str,
    recommendation_contract: customer_recommendation_verification_contract.RecommendationRequestContract,
) -> bool:
    text = str(question or "").strip()
    if not text or not recommendation_contract.subject_category:
        return False
    comparison_signal = bool(
        customer_agent_planner_service._is_compare_question(text)
        or customer_agent_planner_service._is_compare_choice_question(text)
        or customer_agent_intent_service._is_compare_question(text)
        or _phase1_is_griddle_vs_cookware_scenario(text)
        or "还是" in text
    )
    if comparison_signal:
        return False
    return _looks_like_recommendation_request(text)


def _rehydrate_recommendation_rows_from_database(
    db: Session,
    agent_result: dict,
) -> dict:
    """Restore tool-backed recommendation candidates before contract filtering.

    Some runtime responses retain product-search output only in the debug
    raw-results list while their public results list is empty. The
    recommendation contract must not interpret that transport shape as a
    verified empty catalogue. Only SKU identifiers are read from the debug
    payload; every returned row is reloaded from the current database before
    verification, so this path never accepts model-supplied product facts.
    """
    if not isinstance(agent_result, dict) or agent_result.get("results"):
        return agent_result
    if str(agent_result.get("answer_type") or "").strip() != "recommendation":
        return agent_result
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    raw_rows = debug.get("raw_results") if isinstance(debug.get("raw_results"), list) else []
    raw_skus = list(dict.fromkeys(
        str(row.get("sku") or "").strip().upper()
        for row in raw_rows
        if isinstance(row, dict) and str(row.get("sku") or "").strip()
    ))
    if not raw_skus:
        return agent_result
    catalog_by_sku = {
        str(row.get("sku") or "").strip().upper(): row
        for row in _phase1_catalog_rows(db, "产品")
        if isinstance(row, dict) and str(row.get("sku") or "").strip()
    }
    rows = [catalog_by_sku[sku] for sku in raw_skus if sku in catalog_by_sku]
    if not rows:
        return agent_result
    result = dict(agent_result)
    result["results"] = rows
    result["result_skus"] = [str(row.get("sku") or "").strip().upper() for row in rows]
    result["candidate_skus"] = list(result["result_skus"])
    result_debug = dict(debug)
    result_debug["recommendation_candidates_rehydrated"] = "database_by_tool_result_sku"
    result["debug"] = result_debug
    return result


def _post_filter_recommendation_result(db: Session, question: str, agent_result: dict) -> dict:
    if not isinstance(agent_result, dict):
        return agent_result
    original_visible_result_skus = [
        str(sku or "").strip().upper()
        for sku in (agent_result.get("result_skus") or [])
        if str(sku or "").strip()
    ]
    agent_result = _rehydrate_recommendation_rows_from_database(db, agent_result)
    recommendation_like_question = _looks_like_recommendation_request(question)
    explicit_recommendation_request = any(
        term in str(question or "")
        for term in ("推荐", "哪款", "选什么", "帮我选", "帮我挑", "推荐一个", "推荐几个")
    )
    allow_recommendation_contract = explicit_recommendation_request or (
        recommendation_like_question and not _looks_like_structured_field_filter_query(question)
    )
    answer_type = str(agent_result.get("answer_type") or "").strip()
    metadata = dict(agent_result.get("answer_metadata") or {})
    metadata_source = str(metadata.get("source") or "").strip()
    sealed_semantic_contract_data = metadata.get("recommendation_contract")
    has_sealed_semantic_contract = bool(
        metadata_source == "validated_semantic_preplan_then_same_sku_verification"
        and isinstance(sealed_semantic_contract_data, dict)
    )
    if metadata.get("verified_single_capacity_fallback") is True:
        return agent_result
    # A pairwise choice has already sealed its candidate domain and rendered
    # a recommendation from that ordered set.  Re-running global catalogue
    # filtering here can leave the result rows scoped while replacing the
    # answer lead with an unrelated SKU.
    if metadata_source == "planner_compare_choice":
        return agent_result
    # A structured dual-domain recommendation has already sealed one
    # evidence-backed option for each requested domain and rendered the
    # requested trade-off.  The generic filter below is a single-domain
    # ranker; rebuilding here would flatten that answer into a generic
    # "primary + backup" list and lose the category comparison.
    if (
        metadata_source == "product_catalog_structured_recommendation"
        and (
            _phase1_is_stove_griddle_combo_scenario(question)
            or _phase1_is_griddle_vs_cookware_scenario(question)
        )
    ):
        return agent_result
    if answer_type != "recommendation" and metadata_source.startswith("structured_"):
        return agent_result
    if answer_type != "recommendation" and not (
        allow_recommendation_contract and answer_type in {"product_query", "query_products"}
    ):
        return agent_result
    contract = _recommendation_question_contract(question)
    if not contract and isinstance(metadata.get("recommendation_contract"), dict):
        # Semantic planning already supplied a validated recommendation
        # contract; do not let an older lexical contract gate bypass its
        # candidate verification.
        contract = {"filters": {}, "negative_filters": {}}
    if not contract:
        return agent_result
    rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
    if not rows:
        if not allow_recommendation_contract:
            return agent_result
        metadata = dict(agent_result.get("answer_metadata") or {})
        debug = dict(agent_result.get("debug") or {})
        agent_result["intent"] = "recommendation"
        agent_result["answer_type"] = "recommendation"
        agent_result["answer"] = "当前未找到符合条件的推荐商品，可以尝试放宽类目、材质或使用条件。"
        agent_result["results"] = []
        agent_result["result_skus"] = []
        agent_result["candidate_skus"] = []
        agent_result["skip_polish"] = True
        metadata["source"] = metadata.get("source") or "product_catalog_structured_recommendation"
        metadata["post_filter_no_match"] = True
        agent_result["answer_metadata"] = metadata
        debug["recommendation_post_filter_applied"] = True
        debug["recommendation_post_filter_matched_count"] = 0
        debug["agent_mode"] = debug.get("agent_mode") or "recommendation_post_filter_no_match"
        agent_result["debug"] = debug
        return agent_result
    tool_search_filters_present = any(
        isinstance(source, dict)
        and source.get("type") == "product_search"
        and source.get("tool") == "hybrid_search_products"
        and isinstance(source.get("filters"), dict)
        and bool(source.get("filters"))
        for source in (agent_result.get("sources") or [])
    )
    if tool_search_filters_present:
        debug = dict(agent_result.get("debug") or {})
        debug["recommendation_post_filter_skipped"] = "verified_tool_search_filters"
        agent_result["debug"] = debug
        return agent_result
    # For cookware recommendations, an explicit liquid/solid alcohol phrase
    # asks for cookware that works with an alcohol stove.  A maintained
    # same-row ``酒精炉`` heat-source label is sufficient evidence for this
    # category recommendation; the stricter liquid-vs-solid distinction stays
    # in single-product fuel-capability answers.
    if customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(question):
        scoped_rows = [
            row for row in rows
            if _is_service_pot_or_cookware_set_candidate(row)
            and _phase1_row_has_explicit_alcohol_stove_support(db, row)
        ]
        if scoped_rows:
            result = dict(agent_result)
            skus = [
                str(row.get("sku") or "").strip().upper()
                for row in scoped_rows
                if str(row.get("sku") or "").strip()
            ]
            result["intent"] = "query_products"
            result["answer_type"] = "product_query"
            current_answer = str(result.get("answer") or "").strip()
            if "酒精炉" not in current_answer:
                result["answer"] = (
                    "以下候选的商品资料均明确标注支持酒精炉。\n"
                    + current_answer
                ).strip()
            return _sync_alcohol_stove_cookware_scope_metadata(
                result,
                skus=skus,
                rows=scoped_rows,
                question=question,
            )
    verification_contract = (
        customer_recommendation_verification_contract.RecommendationRequestContract.from_dict(
            sealed_semantic_contract_data
        )
        if has_sealed_semantic_contract
        else customer_recommendation_verification_contract.build_recommendation_request_contract(question)
    )
    eligibility_filters = dict(contract.get("filters") or {})
    eligibility_filters.pop("_contract.lightweight", None)
    if verification_contract.subject_kind == "stove" and not verification_contract.heat_sources:
        eligibility_filters.pop("specs.heat_source", None)
    eligibility_contract = {
        "filters": eligibility_filters,
        "negative_filters": dict(contract.get("negative_filters") or {}),
    }
    # A semantic recommendation has already converted the complete request
    # into a formal verification contract. Reapplying the older lexical
    # eligibility filter can silently discard its verified candidates (for
    # example, a DB people-range value that does not contain the exact words
    # from the request). Reuse the sealed contract and let the central
    # verifier evaluate the same rows. Legacy results still receive the
    # deterministic eligibility filter.
    qualified_rows = (
        list(rows)
        if has_sealed_semantic_contract
        else [
            row for row in rows
            if _structured_row_matches_contract(row, eligibility_contract)
        ]
    )
    # Runtime ranking may return only a truncated candidate window. When the
    # formal verification contract has no hard factual constraint, an empty
    # intersection with that window is not evidence that the catalogue has no
    # matching product. Re-evaluate the deterministic eligibility contract
    # against the database catalogue before returning an empty recommendation.
    # A true formal hard constraint remains fail-closed.
    if not qualified_rows and not verification_contract.hard_constraints:
        qualified_rows = [
            row
            for row in _phase1_catalog_rows(db, "产品")
            if _structured_row_matches_contract(row, eligibility_contract)
        ]
    explicit_target_category = _structured_target_category_from_question(question)
    comparison_scope = bool(
        customer_agent_planner_service._is_compare_question(question)
        or customer_agent_planner_service._is_compare_choice_question(question)
        or customer_agent_intent_service._is_compare_question(question)
        or _phase1_is_griddle_vs_cookware_scenario(question)
        or "还是" in str(question or "")
    )
    if not explicit_target_category and _looks_like_recommendation_request(question) and not comparison_scope:
        semantic_target = _semantic_catalog_product_ref(question)
        if semantic_target != "产品":
            explicit_target_category = semantic_target
    if explicit_target_category == "烤盘" and not comparison_scope:
        qualified_rows = [
            row
            for row in qualified_rows
            if any(
                term in " ".join(
                    str(row.get(key) or "")
                    for key in ("product_name_cn", "product_name_en", "category", "capacity", "features")
                ).lower()
                for term in ("烤盘", "煎盘", "griddle")
            )
        ]
    debug = dict(agent_result.get("debug") or {})
    subject_entry_enabled = _should_use_central_subject_recommendation(
        question=question,
        recommendation_contract=verification_contract,
    )
    verification_enabled = bool(
        subject_entry_enabled
        or (
            not verification_contract.subject_category
            and (verification_contract.hard_constraints or verification_contract.soft_preferences)
        )
    )
    debug["central_subject_recommendation_eligible"] = subject_entry_enabled
    debug["initial_candidate_skus"] = [
        str(row.get("sku") or "").strip().upper()
        for row in qualified_rows
        if str(row.get("sku") or "").strip()
    ]
    strict_subject_rejections: list[dict[str, Any]] = []
    if (
        verification_enabled
        and verification_contract.subject_category == "锅具"
        and explicit_target_category != "烤盘"
    ):
        subject_rows: list[dict[str, Any]] = []
        for row in qualified_rows:
            if _is_service_pot_or_cookware_set_candidate(row):
                subject_rows.append(row)
                continue
            strict_subject_rejections.append(
                {
                    "sku": str(row.get("sku") or "").strip().upper(),
                    "subject_eligible": False,
                    "hard_constraints_passed": False,
                    "evidence_by_constraint": {
                        "subject": {
                            "status": "conflict",
                            "field_source": "catalog_subject_classifier",
                            "raw_value": str(row.get("sub_category") or row.get("category") or "").strip(),
                        }
                    },
                    "verified_preferences": [],
                    "unsupported_preferences": [],
                    "rejection_reasons": ["subject_category_mismatch"],
                }
            )
        qualified_rows = subject_rows
    verifications = (
        customer_recommendation_verification_contract.verify_recommendation_candidates(
            verification_contract,
            qualified_rows,
        )
        if verification_enabled
        else []
    )
    if verification_enabled:
        matched_rows = customer_recommendation_verification_contract.select_recommendation_candidates(
            qualified_rows,
            verifications,
        )
        # The runtime search is intentionally a small ranked window. If that
        # window has no verified candidate, widen the evidence pool once and
        # run the same verifier over the catalogue before declaring no match.
        if db is not None and not matched_rows and qualified_rows:
            expansion_filters = {
                key: value
                for key, value in eligibility_contract.get("filters", {}).items()
                if not str(key).startswith("_contract.")
            }
            expansion_eligibility = {
                "filters": expansion_filters,
                "negative_filters": eligibility_contract.get("negative_filters", {}),
            }
            expanded_rows = [
                row
                for row in _phase1_catalog_rows(db, "产品")
                if isinstance(row, dict)
                and (
                    has_sealed_semantic_contract
                    or _structured_row_matches_contract(row, expansion_eligibility)
                )
            ]
            if verification_contract.subject_category == "锅具" and explicit_target_category != "烤盘":
                expanded_rows = [
                    row for row in expanded_rows
                    if _is_service_pot_or_cookware_set_candidate(row)
                ]
            if explicit_target_category == "烤盘" and not comparison_scope:
                expanded_rows = [
                    row
                    for row in expanded_rows
                    if any(
                        term in " ".join(
                            str(row.get(key) or "")
                            for key in ("product_name_cn", "product_name_en", "category", "capacity", "features")
                        ).lower()
                        for term in ("烤盘", "煎盘", "griddle")
                    )
                ]
            existing_skus = {
                str(row.get("sku") or "").strip().upper()
                for row in qualified_rows
                if str(row.get("sku") or "").strip()
            }
            expanded_rows = [
                row for row in expanded_rows
                if str(row.get("sku") or "").strip().upper() not in existing_skus
            ]
            if expanded_rows:
                verification_rows = [*qualified_rows, *expanded_rows]
                verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(
                    verification_contract,
                    verification_rows,
                )
                matched_rows = customer_recommendation_verification_contract.select_recommendation_candidates(
                    verification_rows,
                    verifications,
                )
                qualified_rows = verification_rows
        verified_skus = {
            str(row.get("sku") or "").strip().upper()
            for row in matched_rows
            if str(row.get("sku") or "").strip()
        }
        qualified_rows, return_metadata = customer_recommendation_verification_contract.prepare_recommendation_return_rows(
            matched_rows,
            limit=5,
        )
        metadata["recommendation_contract"] = verification_contract.to_dict()
        metadata.update(return_metadata)
        metadata["verification_level"] = (
            "fully_verified"
            if any(item.verification_level == "fully_verified" and item.sku in verified_skus for item in verifications)
            else "partially_verified" if verified_skus else "rejected"
        )
        metadata["fully_verified_count"] = sum(item.verification_level == "fully_verified" for item in verifications)
        metadata["partially_verified_count"] = sum(item.verification_level == "partially_verified" for item in verifications)
        metadata["rejected_count"] = len(strict_subject_rejections) + sum(item.verification_level == "rejected" for item in verifications)
        debug["all_verified_candidate_skus"] = [
            str(row.get("sku") or "").strip().upper()
            for row in matched_rows
            if str(row.get("sku") or "").strip()
        ]
        debug["verified_candidate_skus"] = [
            str(row.get("sku") or "").strip().upper()
            for row in qualified_rows
            if str(row.get("sku") or "").strip()
        ]
        debug["candidate_verifications"] = [item.to_dict() for item in verifications if item.sku in verified_skus]
        debug["fully_verified_candidates"] = [item.to_dict() for item in verifications if item.verification_level == "fully_verified"]
        debug["partially_verified_candidates"] = [item.to_dict() for item in verifications if item.verification_level == "partially_verified"]
        debug["rejected_candidates"] = strict_subject_rejections + [
            item.to_dict() for item in verifications if item.verification_level == "rejected"
        ]
    if not qualified_rows:
        answer = (
            customer_recommendation_verification_contract.build_verified_recommendation_answer(
                verification_contract,
                [],
                verifications,
            )
            if verification_enabled
            else "当前未找到符合条件的推荐商品，可以尝试放宽类目、材质或使用条件。"
        )
        agent_result["intent"] = "recommendation"
        agent_result["answer_type"] = "recommendation"
        agent_result["answer"] = answer
        agent_result["results"] = []
        agent_result["result_skus"] = []
        agent_result["candidate_skus"] = []
        agent_result["skip_polish"] = True
        metadata["source"] = metadata.get("source") or "product_catalog_structured_recommendation"
        metadata["post_filter_no_match"] = True
        agent_result["answer_metadata"] = metadata
        debug["recommendation_post_filter_applied"] = True
        debug["recommendation_post_filter_matched_count"] = 0
        debug["agent_mode"] = debug.get("agent_mode") or "recommendation_post_filter_no_match"
        agent_result["debug"] = debug
        return agent_result

    filtered_skus = [str(row.get("sku") or "").strip().upper() for row in qualified_rows if str(row.get("sku") or "").strip()]
    original_skus = (
        original_visible_result_skus
        if has_sealed_semantic_contract and original_visible_result_skus
        else [
            str(row.get("sku") or "").strip().upper()
            for row in rows
            if str(row.get("sku") or "").strip()
        ]
    )
    agent_result["intent"] = "recommendation"
    agent_result["answer_type"] = "recommendation"
    agent_result["results"] = qualified_rows
    agent_result["result_skus"] = filtered_skus
    agent_result["candidate_skus"] = filtered_skus
    answer = str(agent_result.get("answer") or "")
    normalized_answer = answer.upper()
    filtered_sku_set = set(filtered_skus)
    row_name_map: dict[str, set[str]] = {}
    for row in qualified_rows:
        sku = str(row.get("sku") or "").strip().upper()
        if not sku:
            continue
        names = {
            sku,
            str(row.get("product_name_cn") or "").strip(),
            str(row.get("product_name_en") or "").strip(),
        }
        row_name_map[sku] = {name for name in names if name}
    matched_skus_in_answer = {
        sku
        for sku, names in row_name_map.items()
        if any(name and name.upper() in normalized_answer for name in names)
    }
    answer_mentions_filtered = bool(matched_skus_in_answer)
    answer_mentions_outside = False
    if normalized_answer:
        for row in rows:
            sku = str(row.get("sku") or "").strip().upper()
            if not sku or sku in filtered_sku_set:
                continue
            candidate_names = {
                sku,
                str(row.get("product_name_cn") or "").strip(),
                str(row.get("product_name_en") or "").strip(),
            }
            if any(name and name.upper() in normalized_answer for name in candidate_names):
                answer_mentions_outside = True
                break
    # Mentioning a candidate is insufficient after the verifier changes the
    # order: the visible lead must follow the sealed top result, not the old
    # ranker's first product.
    ranking_changed = filtered_skus != original_skus[:len(filtered_skus)]
    recommendation_narrative = metadata.get("recommendation_narrative")
    validated_semantic_narrative = bool(
        isinstance(recommendation_narrative, dict)
        and recommendation_narrative.get("source") == "validated_deepseek_grounded_narrative"
    )
    should_rebuild_answer = (
        (verification_enabled and not validated_semantic_narrative)
        or comparison_scope
        or (ranking_changed and not validated_semantic_narrative)
        or not answer
        or not answer_mentions_filtered
        or answer_mentions_outside
    )
    debug["recommendation_post_filter_rebuild_reasons"] = [
        reason
        for reason, active in (
            ("unvalidated_verification_answer", verification_enabled and not validated_semantic_narrative),
            ("comparison_scope", comparison_scope),
            ("visible_ranking_changed", ranking_changed),
            ("empty_answer", not answer),
            ("selected_identity_missing", not answer_mentions_filtered),
            ("outside_identity_mentioned", answer_mentions_outside),
        )
        if active
    ]
    if should_rebuild_answer:
        if verification_enabled:
            rebuilt_answer = customer_recommendation_verification_contract.build_verified_recommendation_answer(
                verification_contract,
                qualified_rows,
                verifications,
                total_match_count=metadata.get("total_match_count"),
            )
        else:
            top_row = qualified_rows[0]
            top_name = top_row.get("product_name_cn") or top_row.get("sku")
            top_sku = str(top_row.get("sku") or "").strip().upper()
            top_reason = str(
                top_row.get("usage_scenarios")
                or top_row.get("features")
                or top_row.get("positioning")
                or "匹配当前使用场景"
            ).strip("。；; ")
            rebuilt_answer = f"优先推荐 {top_name}（{top_sku}），因为它更贴合你当前场景，{top_reason}。"
            backups: list[str] = []
            for row in qualified_rows[1:3]:
                name = row.get("product_name_cn") or row.get("sku")
                sku = str(row.get("sku") or "").strip().upper()
                reason = str(row.get("usage_scenarios") or row.get("features") or row.get("positioning") or "匹配当前使用场景").strip("。；; ")
                backups.append(f"{name}（{sku}），{reason}")
            if backups:
                rebuilt_answer += " 备选可以看" + "；".join(backups) + "。"
        preserved_scope_statement = str(metadata.get("preserved_scope_statement") or "").strip()
        if preserved_scope_statement and preserved_scope_statement not in rebuilt_answer:
            rebuilt_answer = f"{preserved_scope_statement}{rebuilt_answer}"
        agent_result["answer"] = rebuilt_answer
        agent_result["skip_polish"] = True
    if verification_enabled and verification_contract.heat_sources:
        current_answer = str(agent_result.get("answer") or "").strip()
        recommendation_narrative = metadata.get("recommendation_narrative")
        has_validated_narrative = bool(
            isinstance(recommendation_narrative, dict)
            and recommendation_narrative.get("source") == "validated_deepseek_grounded_narrative"
        )
        missing_heat_labels = [
            label for label in verification_contract.heat_sources
            if label and label not in current_answer
        ]
        if missing_heat_labels and not has_validated_narrative:
            agent_result["answer"] = (
                f"同 SKU 热源资料已明确核验支持{'、'.join(missing_heat_labels)}。\n"
                f"{current_answer}"
            ).strip()
            agent_result["skip_polish"] = True
            debug["recommendation_heat_source_statement_added"] = missing_heat_labels
    debug["recommendation_post_filter_applied"] = True
    debug["recommendation_post_filter_matched_count"] = len(filtered_skus)
    debug["recommendation_post_filter_answer_rebuilt"] = should_rebuild_answer
    agent_result["debug"] = debug
    agent_result["answer_metadata"] = metadata
    return agent_result


def _attach_formal_field_contract(
    agent_result: dict,
    question: str,
    plan: dict | None,
) -> dict:
    """Expose the FieldContract consumed by an early protected result."""
    if not isinstance(agent_result, dict):
        return agent_result
    field_request = resolve_requested_field_contract(question, plan)
    if not field_request.get("field_type"):
        return agent_result
    if isinstance(plan, dict):
        plan["field_contract"] = dict(field_request)
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    debug["field_contract"] = dict(field_request)
    agent_result["debug"] = debug
    metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    metadata["contract_field_type"] = field_request["field_type"]
    agent_result["answer_metadata"] = metadata
    return agent_result


def _looks_like_coffee_brewing_goal(
    question: str,
    contract: customer_recommendation_verification_contract.RecommendationRequestContract,
) -> bool:
    return contract.subject_kind == "cookware" and any(
        term in str(question or "") for term in ("咖啡", "泡咖啡", "冲咖啡", "手冲")
    )


def _coffee_cookware_goal_score(row: dict[str, Any]) -> int:
    text = " ".join(
        str(row.get(key) or "")
        for key in (
            "product_name_cn",
            "product_name_en",
            "features",
            "usage_scenarios",
            "target_audience",
            "capacity",
            "positioning",
        )
    )
    score = 8 if any(term in text for term in ("速沸", "快速沸腾", "烧水", "煮水")) else 0
    score += 4 if any(term in text for term in ("单锅", "小锅", "小容量")) else 0
    values = [
        float(value)
        for value in re.findall(r"(\d+(?:\.\d+)?)\s*(?:ml|毫升|l|升)", text, re.I)
    ]
    score += 4 if values and min(values) <= 1500 else 0
    score -= 5 if any(term in text for term in ("煎盘", "煎锅", "炒锅")) else 0
    return score


async def _semantic_recommendation_contract_result(
    db: Session,
    question: str,
    plan: dict[str, Any] | None,
    recommendation_context: dict[str, Any] | None = None,
) -> dict | None:
    """Execute a high-confidence semantic recommendation through DB evidence.

    The semantic preplan supplies only allowlisted abstract requirements.  It
    never picks products or facts; every returned row first passes the shared
    recommendation verifier against that row's own structured evidence.
    """
    preplan = (plan or {}).get("semantic_preplan") if isinstance(plan, dict) else None
    literal_scope_contract = (
        customer_recommendation_verification_contract.build_recommendation_request_contract(
            question
        )
    )
    if (
        isinstance(preplan, dict)
        and str(preplan.get("route_family") or "") == "recommendation"
        and literal_scope_contract.subject_kind
        and not str(
            (preplan.get("recommendation_constraints") or {}).get("subject_kind") or ""
        ).strip()
    ):
        # The semantic planner owns the action and constraints; an explicit
        # current-turn product noun owns catalogue scope. Recover that literal
        # scope when the provider omits the optional subject_kind field.
        preplan = dict(preplan)
        recovered_constraints = dict(preplan.get("recommendation_constraints") or {})
        recovered_constraints["subject_kind"] = literal_scope_contract.subject_kind
        preplan["recommendation_constraints"] = recovered_constraints
        preplan["semantic_adapter_source"] = "literal_current_turn_recommendation_subject"
        if isinstance(plan, dict):
            plan["semantic_preplan"] = preplan
    # A pairwise request can be semantically described as a recommendation,
    # but it is not a catalogue search.  Its named participants have already
    # been sealed into EntityResolutionContracts and must continue to the
    # comparison executor rather than be replaced by new catalogue candidates.
    if not _should_execute_semantic_catalog_recommendation(preplan):
        invalid_recommendation_hint = (
            isinstance(preplan, dict)
            and str(preplan.get("semantic_route_family_hint") or "") == "recommendation"
            and str(preplan.get("fallback_reason") or "") == "invalid_recommendation_constraints"
        )
        literal_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(question)
        if not (
            invalid_recommendation_hint
            and literal_contract.subject_kind
            and (literal_contract.people_min is not None or literal_contract.scenario or literal_contract.capacity_requirement == "numeric")
        ):
            return None
        recovered_constraints: dict[str, Any] = {"subject_kind": literal_contract.subject_kind}
        if literal_contract.people_min is not None and literal_contract.people_max is not None:
            recovered_constraints["people"] = {"min": literal_contract.people_min, "max": literal_contract.people_max}
        if literal_contract.scenario:
            recovered_constraints["scenarios"] = list(literal_contract.scenario)
        preplan = {
            "route_family": "recommendation",
            "confidence": 0.9,
            "fallback_reason": "",
            "ambiguity": False,
            "recommendation_constraints": recovered_constraints,
            "unrepresented_recommendation_requirements": [],
            "recommendation_soft_preferences": [],
        }
    constraints = dict(preplan.get("recommendation_constraints") or {})
    unrepresented_requirements = [
        str(item or "").strip()
        for item in (preplan.get("unrepresented_recommendation_requirements") or [])
        if str(item or "").strip()
    ]
    semantic_soft_preferences = [
        str(item or "").strip()
        for item in (preplan.get("recommendation_soft_preferences") or [])
        if str(item or "").strip()
    ]
    literal_question_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(
        question
    )
    literal_people_constraint = bool(
        literal_question_contract.people_min is not None
        and literal_question_contract.people_max is not None
        and "people" not in constraints
    )
    if literal_people_constraint:
        constraints["people"] = {
            "min": literal_question_contract.people_min,
            "max": literal_question_contract.people_max,
        }
    question_text = str(question or "")
    numeric_single_cookware_boiling_goal = (
        _explicit_single_recommendation_request(question_text)
        and literal_question_contract.capacity_requirement == "numeric"
        and "capacity" in literal_question_contract.hard_constraints
        and any(term in question_text for term in ("锅", "锅具", "小锅", "单锅", "套锅", "炊具"))
        and not any(term in question_text for term in ("水壶", "茶壶", "烧水壶", "壶类"))
    )
    cookware_boiling_workflow = (
        str(constraints.get("subject_kind") or "") == "cookware"
        and any(term in question_text for term in ("锅", "锅具", "套锅", "炊具"))
        and not any(term in question_text for term in ("水壶", "水具", "水袋", "水囊", "水杯"))
    )
    stove_pairing_workflow = (
        str(constraints.get("subject_kind") or "") == "stove"
        and _phase1_is_stove_pairing_scenario(question_text)
    )
    stove_pairing_use_goal_terms = (
        "烧烤", "烤肉", "烤东西", "烧水", "煮水", "热饮", "加热饮", "泡茶", "热水", "煮茶",
    )
    stove_pairing_product_terms = (
        "炉", "锅", "壶", "罐", "烤盘", "套装", "配件", "气源",
    )
    retained_unrepresented_requirements: list[str] = []
    for requirement in unrepresented_requirements:
        force_marker = re.search(
            r"(?:必须|务必|只能|一定要|不可|不得|不能接受|硬性|至少|不低于|不少于)",
            requirement,
        )
        if not force_marker and requirement:
            # The planner normalizes an extracted requirement to its semantic
            # core (for example “必须明确支持煮面” -> “煮面”).  Recover only the
            # binding status from the complete turn; product eligibility still
            # comes exclusively from the same-SKU evidence verifier below.
            force_marker = re.search(
                rf"(?:必须|务必|只能|一定要|不可|不得|不能接受|硬性|至少|不低于|不少于)"
                rf"[^，。；;!?！？]{{0,12}}{re.escape(requirement)}",
                question_text,
            )
        if not force_marker:
            # An allowlisted schema cannot encode every natural preference.
            # Keep ordinary, non-mandatory wording as grounded ranking and
            # narrative context instead of turning fluent requests into a
            # clarification. Explicit hard-force language remains fail-closed.
            if requirement not in semantic_soft_preferences:
                semantic_soft_preferences.append(requirement)
            continue
        preference_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(
            requirement
        )
        # The shared request contract already distinguishes verifiable hard
        # predicates from ranking/narrative preferences.  Respect that typed
        # classification generically instead of turning a non-binding use
        # context (for example a brewing style or easier cleaning) into an
        # impossible eligibility requirement merely because the semantic
        # planner placed it in the wrong array.
        if (
            not force_marker
            and preference_contract.soft_preferences
            and not preference_contract.hard_constraints
        ):
            if requirement not in semantic_soft_preferences:
                semantic_soft_preferences.append(requirement)
            continue
        # “不想/不希望……太……” expresses a gradable preference, not a
        # categorical exclusion. Preserve it as decision context unless the
        # customer also used an explicit hard-force marker. This does not
        # weaken noun/category exclusions such as “不要炉具”.
        if (
            not force_marker
            and re.search(r"(?:不想|不希望|别|不要)[^，。；;!?！？]{0,12}(?:太|过于)", requirement)
        ):
            if requirement not in semantic_soft_preferences:
                semantic_soft_preferences.append(requirement)
            continue
        recognized_vague_capacity_preference = (
            not force_marker
            and preference_contract.capacity_requirement == "spacious"
            and "capacity" in preference_contract.soft_preferences
        )
        if recognized_vague_capacity_preference:
            if requirement not in semantic_soft_preferences:
                semantic_soft_preferences.append(requirement)
            continue
        # Stability is an allowlisted soft preference in the recommendation
        # contract. When the customer does not make it a hard requirement,
        # keep it for ranking/narrative context instead of converting an
        # otherwise verifiable recommendation into a clarification.
        if (
            not force_marker
            and preference_contract.soft_preferences == ["stability"]
        ):
            if requirement not in semantic_soft_preferences:
                semantic_soft_preferences.append(requirement)
            continue
        # The semantic plan can describe an explicit numeric capacity phrase as
        # an unknown free-text requirement.  The shared verifier parses that
        # literal current-turn value into a bounded capacity predicate, so it
        # must not be discarded before candidate verification starts.
        if (
            preference_contract.capacity_requirement == "numeric"
            and literal_question_contract.capacity_requirement == "numeric"
            and "capacity" in literal_question_contract.hard_constraints
        ):
            continue
        # Stainless-grade alternatives such as “304或316” are represented by
        # the shared same-SKU material verifier.  Keep the semantic planner's
        # literal phrase from being treated as an unsupported requirement and
        # routed to a clarification before catalogue verification runs.
        if (
            preference_contract.materials
            and literal_question_contract.materials
            and "material" in literal_question_contract.hard_constraints
        ):
            continue
        # For a numerically bounded, single cookware recommendation, boiling
        # water is the requested workflow, not an unverified product attribute.
        # Keep water-container requests conservative and retain every other
        # material requirement (such as a heat-source compatibility request).
        if (
            numeric_single_cookware_boiling_goal
            and any(term in requirement for term in ("煮水", "烧水"))
        ):
            continue
        if (
            cookware_boiling_workflow
            and not force_marker
            and any(term in requirement for term in ("煮水", "烧水"))
        ):
            if requirement not in semantic_soft_preferences:
                semantic_soft_preferences.append(requirement)
            continue
        # In a stove-pairing request these words describe the intended use of
        # the setup, rather than a same-SKU product attribute. Keep them as
        # soft context, but preserve product/bundle requirements for clarity.
        if (
            stove_pairing_workflow
            and not force_marker
            and any(term in requirement for term in stove_pairing_use_goal_terms)
            and not any(term in requirement for term in stove_pairing_product_terms)
        ):
            if requirement not in semantic_soft_preferences:
                semantic_soft_preferences.append(requirement)
            continue
        retained_unrepresented_requirements.append(requirement)
    unrepresented_requirements = retained_unrepresented_requirements
    # The semantic layer has identified material customer requirements that
    # the current DB verification ontology cannot represent.  Do not silently
    # discard them and recommend a broader category; request a verifiable
    # priority instead.  This branch never selects a product or invents a
    # lexical interpretation of the requirement.
    if unrepresented_requirements:
        requirement_labels = "、".join(f"“{item}”" for item in unrepresented_requirements[:3])
        return {
            "intent": "recommendation",
            "answer_type": "clarification",
            "answer": (
                f"你提到的{requirement_labels}，当前还不能用同 SKU 的已验证资料作为筛选条件。"
                "我不会省略这个要求后直接推荐；如果愿意，可以补充可接受的替代条件，我再继续帮你筛选。"
            ),
            "results": [], "result_skus": [], "candidate_skus": [], "evidence": [],
            "debug": {
                "agent_mode": "semantic_recommendation_unrepresented_requirement_clarification",
                "semantic_constraints": dict(constraints or {}),
                "unrepresented_recommendation_requirements": unrepresented_requirements,
                "semantic_soft_preferences": semantic_soft_preferences,
            },
            "answer_metadata": {"source": "validated_semantic_preplan_unrepresented_requirement_clarification"},
            "skip_polish": True,
        }
    semantic_subject = str(preplan.get("subject_text") or "").strip()
    subject_rows = customer_agent_service.search_products(db, semantic_subject, limit=50) if semantic_subject else []
    # The semantic model understands the requested thing; the database owns
    # its actual category. If the model's small abstract subject_kind schema
    # conflicts with a non-empty DB subject scope, do not use it to discard
    # those candidates. This never broadens beyond the database scope.
    if subject_rows and constraints.get("subject_kind"):
        expected_category = {"cookware": "锅具", "waterware": "水具", "stove": "炉具", "coffee_gear": "咖啡器具", "accessories": "配件"}.get(
            str(constraints.get("subject_kind") or "")
        )
        actual_categories = {str(row.get("category") or "").strip() for row in subject_rows}
        if expected_category and actual_categories and all(expected_category not in category for category in actual_categories):
            if literal_scope_contract.subject_category:
                # A negative phrase can be copied into semantic subject_text
                # (for example “不要炉具和配件”).  Keep the customer's explicit
                # positive catalogue scope and discard those off-scope search
                # hits; never let a negated noun erase the requested category.
                subject_rows = []
            else:
                constraints.pop("subject_kind", None)
    contract = customer_recommendation_verification_contract.build_recommendation_request_contract(
        question,
        semantic_constraints=constraints,
    )
    if literal_people_constraint:
        contract.field_provenance["people"] = {"source_turn": 1, "provenance": "current_turn"}
    # Literal context and soft preferences may accompany a semantic category
    # plan. They can inform the writer, but without a semantic hard constraint
    # they must not block the category contract or filter candidates.
    current_turn_soft_fields = {
        str(key or "").strip()
        for key in contract.soft_preferences
        if str(key or "").strip()
    }
    if contract.scenario and "scenario" not in contract.hard_constraints:
        current_turn_soft_fields.add("scenario")
    if not contract.field_provenance or not all(
        item.get("provenance") == "validated_semantic_preplan"
        or (
            key in {"capacity", "people"}
            and item.get("provenance") == "current_turn"
        )
        or (
            key == "heat_sources"
            and item.get("provenance") == "current_turn_explicit_requirement"
        )
        # An explicit current-turn product noun is a safe narrowing boundary.
        # It still needs the same-SKU verifier below before any candidate can
        # be shown, but must not be discarded in favour of a broader model
        # category such as cookware.
        or (
            key == "subject_category"
            and item.get("provenance") == "current_turn_explicit_subject"
        )
        or (
            key in current_turn_soft_fields
            and key not in contract.hard_constraints
            and item.get("provenance") == "current_turn"
        )
        for key, item in contract.field_provenance.items()
    ):
        return None
    required_customer_dimensions = [
        dimension
        for dimension in contract.soft_preferences
        if dimension in {"capacity", "weight", "storage", "portability", "stability"}
    ]
    literal_preference_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(
        question
    )
    for dimension in literal_preference_contract.soft_preferences:
        if (
            dimension in {"capacity", "weight", "storage", "portability", "stability"}
            and dimension not in required_customer_dimensions
        ):
            required_customer_dimensions.append(dimension)
    for preference in semantic_soft_preferences:
        preference_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(
            preference
        )
        if (
            preference_contract.capacity_requirement == "spacious"
            and "capacity" in preference_contract.soft_preferences
            and "capacity" not in required_customer_dimensions
        ):
            required_customer_dimensions.append("capacity")
    coffee_brewing_goal = _looks_like_coffee_brewing_goal(question, contract)
    if coffee_brewing_goal:
        for preference in ("泡咖啡", "烧水"):
            if preference not in semantic_soft_preferences:
                semantic_soft_preferences.append(preference)
    rows = list(subject_rows)
    if coffee_brewing_goal:
        seen_skus = {
            str(row.get("sku") or "").strip().upper()
            for row in rows
            if str(row.get("sku") or "").strip()
        }
        for row in _phase1_catalog_rows(db, "锅具"):
            category = str(row.get("category") or "").strip()
            sku = str(row.get("sku") or "").strip().upper()
            if sku and sku not in seen_skus and (not category or "锅具" in category):
                rows.append(row)
                seen_skus.add(sku)
        rows = [
            row
            for _, row in sorted(
                enumerate(rows),
                key=lambda item: (-_coffee_cookware_goal_score(item[1]), item[0]),
            )
        ]
    elif contract.subject_category or contract.subject_kind:
        verified_subject_category = str(contract.subject_category or "").strip() or {
            "cookware": "锅具",
            "waterware": "水具",
            "stove": "炉具",
            "coffee_gear": "咖啡器具",
            "accessories": "配件",
        }.get(str(contract.subject_kind or "").strip(), "")
        literal_subject_ref = str(literal_scope_contract.subject_category or "").strip()
        semantic_subject_ref = _semantic_catalog_product_ref(question)
        # Keep a positive, more specific catalogue subtype such as 烤盘 over
        # its broad literal parent 锅具. Conversely, a noun explicitly excluded
        # by the customer (不要炉具/配件) can never become the search scope.
        explicit_subject_ref = (
            semantic_subject_ref
            if semantic_subject_ref not in set(contract.excluded_categories or [])
            else literal_subject_ref
        ) or literal_subject_ref
        if explicit_subject_ref not in {"", "产品"}:
            verified_subject_category = explicit_subject_ref
        seen_skus = {
            str(row.get("sku") or "").strip().upper()
            for row in rows
            if str(row.get("sku") or "").strip()
        }
        for row in _phase1_catalog_rows(db, verified_subject_category or "产品"):
            sku = str(row.get("sku") or "").strip().upper()
            if sku and sku not in seen_skus:
                rows.append(row)
                seen_skus.add(sku)
    if not rows:
        rows = _phase1_catalog_rows(db, "产品")
    # The semantic plan is authoritative when it supplies the follow-up
    # action. If it omits that optional field, an explicit generic
    # alternative request may still consume the already sealed recommendation
    # context; deterministic code only removes those persisted SKU identities.
    semantic_alternative = str(preplan.get("recommendation_followup_action") or "") == "alternative"
    context_alternative = bool(
        _asks_for_alternative_recommendation(question)
        and isinstance(recommendation_context, dict)
        and (recommendation_context.get("previous_result_skus") or recommendation_context.get("recommended_skus"))
    )
    if semantic_alternative or context_alternative:
        previous_result_skus = {
            str(sku or "").strip().upper()
            for sku in ((recommendation_context or {}).get("previous_result_skus") or [])
            if str(sku or "").strip()
        }
        if previous_result_skus:
            rows = [
                row for row in rows
                if str(row.get("sku") or "").strip().upper() not in previous_result_skus
            ]
    verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(contract, rows)
    fully_verified = [item for item in verifications if item.verification_level == "fully_verified"]
    # Group size is usually usage context rather than an intrinsic stove
    # attribute. For an ordinary (non-forced) people expression, a stove whose
    # only unknown hard field is people may still be shown with that gap
    # disclosed. Conflicts and every other unknown hard condition remain
    # fail-closed, as do cookware people/capacity checks.
    forced_people_requirement = bool(
        re.search(
            r"(?:必须|务必|只能|一定要|至少|不低于|不少于)[^，。；;!?！？]{0,12}(?:人|人数)",
            question_text,
        )
    )
    safely_partial_stove = [
        item
        for item in verifications
        if (
            contract.subject_kind == "stove"
            and not forced_people_requirement
            and item.verification_level == "partially_verified"
            and not item.has_hard_constraint_conflict
            and set(item.unsupported_constraints or []).issubset({"people"})
        )
    ]
    usable_verifications = fully_verified or safely_partial_stove
    if not usable_verifications:
        evidence_spans = preplan.get("recommendation_constraint_evidence_spans")
        evidence_spans = evidence_spans if isinstance(evidence_spans, dict) else {}
        condition_labels = []
        for key, spans in evidence_spans.items():
            if key == "subject_kind" or not isinstance(spans, list):
                continue
            for span in spans:
                text = str(span or "").strip()
                if text and text not in condition_labels:
                    condition_labels.append(text)
        condition_summary = "、".join(f"“{item}”" for item in condition_labels[:3]) or "你提出的条件"
        return {
            "intent": "recommendation",
            "answer_type": "recommendation",
            "answer": (
                f"当前没有商品能同时用同 SKU 资料明确核验{condition_summary}。"
                "我不会把证据不完整的商品当作推荐候选；你可以放宽其中一项条件后，我再继续筛选。"
            ),
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "debug": {
                "agent_mode": "semantic_recommendation_insufficient_verified_evidence",
                "semantic_constraints": dict(constraints),
                "semantic_soft_preferences": semantic_soft_preferences,
                "candidate_verifications": [item.to_dict() for item in verifications],
            },
            "answer_metadata": {
                "source": "validated_semantic_preplan_insufficient_verified_evidence",
                "recommendation_contract": contract.to_dict(),
                "fully_verified_count": 0,
                "partially_verified_count": sum(item.verification_level == "partially_verified" for item in verifications),
                "rejected_count": sum(item.verification_level == "rejected" for item in verifications),
            },
            "skip_polish": True,
        }
    verified_rows = customer_recommendation_verification_contract.select_recommendation_candidates(rows, verifications)
    returned_rows, return_metadata = customer_recommendation_verification_contract.prepare_recommendation_return_rows(
        verified_rows,
        limit=5,
    )
    narrative_diagnostics: list[dict[str, Any]] = []
    # Let the semantic writer run first.  A structured dimension summary is a
    # safe fallback only when the writer is genuinely unavailable; if it
    # returned a malformed or rejected draft, the diagnostics must remain a
    # fail-closed clarification instead of silently masking the rejection.
    narrative = None
    narrative_source = "validated_deepseek_grounded_narrative"
    # A capacity/weight clarification is only justified when the selected
    # same-SKU rows actually lack one of those fields.  Previously this branch
    # fired solely because the customer asked for both dimensions, preventing
    # the grounded semantic writer from using rows that did contain them.
    dimension_evidence_available = {
        "capacity": any(str(row.get("capacity") or "").strip() for row in returned_rows),
        "weight": any(
            row.get("gross_weight_g") is not None
            or str(row.get("weight") or "").strip()
            for row in returned_rows
        ),
    }
    dimension_evidence_gap = (
        {"capacity", "weight"}.issubset(required_customer_dimensions)
        and any(
            not dimension_evidence_available[dimension]
            for dimension in ("capacity", "weight")
        )
    )
    if not narrative and dimension_evidence_gap:
        dimension_labels = {
            "capacity": "容量",
            "weight": "重量",
            "storage": "收纳",
            "portability": "便携",
            "stability": "稳定性",
        }
        requested_dimension_text = "、".join(
            dimension_labels[item]
            for item in required_customer_dimensions
            if item in dimension_labels
        )
        if len(fully_verified) >= 3 and not _explicit_single_recommendation_request(question):
            narrative = _verified_recommendation_evidence_fallback(
                question=question,
                rows=returned_rows,
                soft_preferences=semantic_soft_preferences,
            )
            if narrative:
                missing_dimensions = [
                    dimension_labels[dimension]
                    for dimension in ("capacity", "weight")
                    if not dimension_evidence_available[dimension]
                ]
                narrative["answer"] += (
                    f" 当前资料未明确标注{'、'.join(missing_dimensions)}，"
                    "因此无法仅按该项判断，建议结合已列出的容量和使用场景选择。"
                )
                narrative_source = "verified_partial_dimension_evidence_fallback"
        if not narrative:
            return {
                "intent": "recommendation",
                "answer_type": "clarification",
                "answer": (
                    "我找到了符合人数和场景的锅具，但这些候选的同 SKU 资料没有同时维护可核对的容量和重量，"
                    f"因此现在不能同时判断你关心的{requested_dimension_text}偏好。你可以给出可接受的重量上限，"
                    "或确认先按容量筛选、把其他缺少的维度作为待确认项，我再继续推荐。"
                ),
                "results": [],
                "result_skus": [],
                "candidate_skus": [],
                "evidence": [],
                "needs_clarification": True,
                "debug": {
                    "agent_mode": "semantic_recommendation_dimension_evidence_gap",
                    "semantic_constraints": dict(constraints),
                    "semantic_soft_preferences": semantic_soft_preferences,
                    "required_customer_dimensions": required_customer_dimensions,
                    "verified_candidate_count": len(fully_verified),
                    "candidate_verifications": [item.to_dict() for item in fully_verified],
                },
                "answer_metadata": {
                    "source": "validated_structured_dimension_evidence_gap",
                    "recommendation_contract": contract.to_dict(),
                    "total_verified_match_count": return_metadata["total_match_count"],
                },
                "skip_polish": True,
            }
    if not narrative:
        narrative = await _semantic_recommendation_narrative(
            db,
            question=question,
            rows=returned_rows,
            verifications=verifications,
            soft_preferences=semantic_soft_preferences,
            required_customer_dimensions=required_customer_dimensions,
            diagnostics=narrative_diagnostics,
        )
    dimension_missing_claims = {
        "capacity": r"容量(?:信息|资料)?(?:暂未|没有|未提供|未标注|缺少)",
        "weight": r"重量(?:信息|资料)?(?:暂未|没有|未提供|未标注|缺少)",
        "storage": r"收纳(?:信息|资料)?(?:暂未|没有|未提供|未标注|缺少)",
    }
    if narrative and any(
        re.search(dimension_missing_claims[dimension], str(narrative.get("answer") or ""))
        for dimension in required_customer_dimensions
        if dimension in dimension_missing_claims
    ):
        narrative_diagnostics.append({"stage": "dimension_coverage", "status": "contradictory_missing_claim"})
        narrative = None
    if not narrative and not narrative_diagnostics:
        narrative = _verified_dimension_summary_narrative(
            question=question,
            rows=returned_rows,
            verifications=verifications,
            required_customer_dimensions=required_customer_dimensions,
        )
        if narrative:
            narrative_source = "verified_structured_dimension_summary"
    fallback_diagnostic_statuses = {
        ("draft", "invalid_schema"),
        ("narrative_safety_gate", "rejected"),
        ("grounding_review", "rejected"),
        ("rewrite", "invalid_schema_or_provider_error"),
        ("rewrite_grounding_review", "unavailable_or_rejected"),
    }
    narrative_fallback_diagnostics_safe = not narrative_diagnostics or all(
        (
            str(item.get("stage") or "").strip(),
            str(item.get("status") or "").strip(),
        ) in fallback_diagnostic_statuses
        for item in narrative_diagnostics
        if isinstance(item, dict)
    )
    if (
        not narrative
        and narrative_fallback_diagnostics_safe
        and (
            contract.hard_constraints
            or (
                len(fully_verified) >= 2
                and _is_plural_catalogue_recommendation_request(question)
            )
            or contract.subject_kind == "accessories"
            or bool(contract.subject_subtype)
        )
    ):
        narrative = _verified_recommendation_evidence_fallback(
            question=question,
            rows=returned_rows,
            soft_preferences=semantic_soft_preferences,
        )
        if narrative:
            narrative_source = "verified_candidate_evidence_fallback"
    # A rejected model draft may preserve a sufficiently large verified
    # catalogue result only under the same bounded request conditions as the
    # preceding fallback. Broad category questions remain fail-closed.
    if (
        not narrative
        and narrative_fallback_diagnostics_safe
        and len(fully_verified) >= 3
        and not _explicit_single_recommendation_request(question)
        and (
            contract.hard_constraints
            or (
                len(fully_verified) >= 2
                and _is_plural_catalogue_recommendation_request(question)
            )
            or contract.subject_kind == "accessories"
            or bool(contract.subject_subtype)
        )
    ):
        narrative = _verified_recommendation_evidence_fallback(
            question=question,
            rows=returned_rows,
            soft_preferences=semantic_soft_preferences,
        )
        if narrative:
            narrative_source = "verified_candidate_evidence_fallback_after_narrative_rejection"
    if not narrative:
        if _explicit_single_recommendation_request(question) and contract.capacity_requirement == "numeric":
            fallback_row = next((row for row in returned_rows if str(row.get("capacity") or "").strip()), None)
            if fallback_row is not None:
                fallback_sku = str(fallback_row.get("sku") or "").strip().upper()
                fallback_name = str(fallback_row.get("product_name_cn") or fallback_sku).strip()
                fallback_capacity = _capacity_field_evidence(fallback_row.get("capacity"))
                fallback_scene = _compound_display_value(fallback_row.get("usage_scenarios"))
                return {
                    "intent": "recommendation", "answer_type": "recommendation",
                    "answer": f"推荐 {fallback_name}（{fallback_sku}）。资料标注容量为{fallback_capacity}" + (f"，使用场景包括{fallback_scene}" if fallback_scene else "。"),
                    "results": [fallback_row], "result_skus": [fallback_sku], "candidate_skus": [fallback_sku], "evidence": [],
                    "debug": {"agent_mode": "semantic_recommendation_verified_single_capacity_fallback"},
                    "answer_metadata": {"source": "validated_semantic_preplan_then_same_sku_verification", "recommendation_contract": contract.to_dict(), "verified_single_capacity_fallback": True, **return_metadata}, "skip_polish": True,
                }
        return {
            "intent": "recommendation",
            "answer_type": "clarification",
            "answer": "我找到了对应品类的商品范围，但当前同 SKU 资料未明确标注你关心的具体用途，因此不敢直接把它们当作推荐。你可以补充人数、炉具、容量或其他可核验条件，我再继续为你筛选。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "debug": {
                "agent_mode": "semantic_recommendation_narrative_unavailable",
                "semantic_constraints": dict(constraints),
                "semantic_soft_preferences": semantic_soft_preferences,
                "verified_candidate_count": len(fully_verified),
                "recommendation_narrative_diagnostics": narrative_diagnostics,
            },
            "answer_metadata": {
                "source": "validated_semantic_preplan_narrative_unavailable_clarification",
                "recommendation_contract": contract.to_dict(),
                "total_verified_match_count": return_metadata["total_match_count"],
                "reason": "no_validated_deepseek_recommendation_narrative",
            },
            "skip_polish": True,
        }
    ranked_indexes = list(narrative.get("ranked_candidate_indexes") or [])
    ranked_rows = [returned_rows[index] for index in ranked_indexes]
    answer = str(narrative.get("answer") or "")
    # Only the products explicitly selected and cited by the approved semantic
    # narrative are recommendation results.  Other fully verified rows remain
    # diagnostic candidates, but must not become hidden recommendations for
    # the UI, follow-up context, or evidence list.
    candidate_skus = [str(row.get("sku") or "").strip().upper() for row in returned_rows if row.get("sku")]
    result_skus = [str(row.get("sku") or "").strip().upper() for row in ranked_rows if row.get("sku")]
    verification_by_sku = {item.sku: item for item in verifications}
    evidence: list[dict[str, Any]] = []
    for sku in result_skus:
        item = verification_by_sku.get(sku)
        if not item:
            continue
        for field, item_evidence in item.evidence_by_constraint.items():
            if item_evidence.get("status") != "verified":
                continue
            evidence.append({
                "type": "recommendation_constraint",
                "source_type": "product_db",
                "sku": sku,
                "field": field,
                "source": item_evidence.get("field_source"),
                "field_label": f"推荐条件（{field}）",
                "value": item_evidence.get("raw_value"),
            })
    # The DeepSeek narrative is validated against a sealed, same-SKU evidence
    # map.  Surface each content value it explicitly cited in the API evidence
    # as well; otherwise an audit can see a grounded customer claim but not the
    # exact first-party record that licensed it.  This projects an already
    # approved ``evidence_usage`` declaration and never grants the writer new
    # fields or product identities.
    for usage in list(narrative.get("evidence_usage") or []):
        if not isinstance(usage, dict):
            continue
        index = usage.get("candidate_index")
        if type(index) is not int or index < 0 or index >= len(returned_rows):
            continue
        row = returned_rows[index]
        sku = str(row.get("sku") or "").strip().upper()
        if sku not in result_skus:
            continue
        for field in list(usage.get("fields") or []):
            if not isinstance(field, str) or not field.startswith("content."):
                continue
            source = field.removeprefix("content.")
            value = row.get(source)
            if not str(value or "").strip():
                continue
            evidence.append({
                "type": "semantic_recommendation_narrative_evidence",
                "source_type": "product_db",
                "sku": sku,
                "field": field,
                "source": source,
                "field_label": f"推荐依据（{source}）",
                "value": value,
            })
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": ranked_rows,
        "result_skus": result_skus,
        "candidate_skus": candidate_skus,
        "evidence": evidence,
        "debug": {
            "agent_mode": (
                "semantic_recommendation_verified_dimension_summary"
                if narrative_source == "verified_structured_dimension_summary"
                else "semantic_recommendation_contract"
            ),
            "semantic_constraints": dict(constraints),
            "semantic_soft_preferences": semantic_soft_preferences,
            "candidate_verifications": [
                item.to_dict()
                for item in verifications
                if item.sku in result_skus
            ],
            "recommendation_narrative_diagnostics": narrative_diagnostics,
            "rejected_candidates": [item.to_dict() for item in verifications if item.verification_level == "rejected"],
        },
        "answer_metadata": {
            "source": "validated_semantic_preplan_then_same_sku_verification",
            "recommendation_contract": contract.to_dict(),
            "semantic_soft_preferences": semantic_soft_preferences,
            "recommendation_narrative": {
                "source": narrative_source,
                "ranked_candidate_indexes": list((narrative or {}).get("ranked_candidate_indexes") or []),
                "evidence_usage": list((narrative or {}).get("evidence_usage") or []),
            },
            **return_metadata,
        },
        "skip_polish": True,
    }


def _semantic_recommendation_constraint_clarification_result(
    preplan: dict[str, Any] | None,
    question: str | None = None,
) -> dict | None:
    """Fail closed when a grounded semantic recommendation lacks usable constraints."""
    invalid_semantic_recommendation = (
        isinstance(preplan, dict)
        and preplan.get("semantic_route_family_hint") == "recommendation"
        and str(preplan.get("fallback_reason") or "") in {
            "invalid_recommendation_constraints",
            "recommendation_constraints_outside_recommendation",
            "semantic_label_without_contract",
        }
    )
    if not isinstance(preplan, dict):
        return None
    # A request to choose between named products may be classified as a
    # recommendation by semantic planning, but its entities still require the
    # sealed pairwise comparison path below.  Do not discard those contracts
    # merely because catalogue-wide constraints are absent.
    if _semantic_pairwise_contract_candidate(preplan) or preplan.get("error"):
        return None
    if (
        not invalid_semantic_recommendation
        and (
            not _semantic_preplan_confident(preplan, minimum=0.65)
            or _semantic_preplan_route_family(preplan) != "recommendation"
            or preplan.get("fallback_reason")
        )
    ):
        return None
    if invalid_semantic_recommendation:
        # A provider may occasionally mark a perfectly explicit category
        # request as an invalid semantic contract after a schema retry.  The
        # lexical contract still gives us a safe subject boundary (for
        # example, “气炉推荐”), so let the catalogue verifier execute that
        # bounded request.  Keep the clarification for genuinely unbound
        # prompts where no category can be recovered.
        lexical_contract = (
            customer_recommendation_verification_contract.build_recommendation_request_contract(question)
            if question
            else None
        )
        if lexical_contract and (lexical_contract.subject_kind or lexical_contract.subject_category):
            return None
        return {
            "intent": "recommendation",
            "answer_type": "clarification",
            "answer": "我理解你是在寻求商品推荐，但当前需求条件还无法用已验证的产品资料安全匹配。请补充希望选锅具、水具还是炉具，以及最重要的使用场景、炉具或人数条件。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "answer_metadata": {
                "source": "semantic_recommendation_invalid_contract_clarification",
                "reason": "invalid_semantic_recommendation_constraint_contract",
            },
            "debug": {
                "agent_mode": "semantic_recommendation_invalid_contract_clarification",
                "recommendation_constraints": {},
            },
            "skip_polish": True,
        }
    if preplan.get("recommendation_constraints"):
        # If the semantic payload omitted a literal heat-source requirement,
        # let the same-SKU verifier execute the request instead of asking the
        # customer for another preference.  The contract parser preserves the
        # explicit span from the original turn; this branch only decides
        # whether the recommendation executor gets a chance to prove it.
        semantic_contract = (
            customer_recommendation_verification_contract.build_recommendation_request_contract(
                question,
                semantic_constraints=preplan.get("recommendation_constraints"),
            )
            if question
            else None
        )
        if (
            semantic_contract
            and not bool(preplan.get("ambiguity"))
            and semantic_contract.subject_kind
        ):
            return None
        # The formal semantic recommendation was deliberately not executable
        # (for example, its entity scope remained ambiguous).  Returning None
        # here used to hand the exact same request to the legacy lexical
        # recommendation guard, which could emit a broad fixed candidate list
        # despite the unresolved semantic contract.  Keep route ownership and
        # fail closed instead.
        has_subject_scope = bool(
            semantic_contract
            and (semantic_contract.subject_kind or semantic_contract.subject_category)
        )
        return {
            "intent": "recommendation",
            "answer_type": "clarification",
            "answer": (
                "我已记下你的使用偏好。我的建议是先缩小商品类型，避免直接猜一件并不合适的礼物：请确认更想选锅具、水具、炉具还是餐具；"
                "如果还没确定，也可以告诉我预算、人数和自驾/徒步场景，我再帮你缩小范围。"
                if not has_subject_scope
                else
                "我已理解你的推荐条件，但当前还不能把它们稳定映射为可核验的商品范围。请补充最重要的一项条件（如人数、炉具、重量或使用场景），我会继续按同 SKU 资料筛选。"
            ),
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "answer_metadata": {
                "source": "validated_semantic_preplan_unexecutable_recommendation_clarification",
            },
            "debug": {
                "agent_mode": "semantic_recommendation_unexecutable_clarification",
                "recommendation_constraints": dict(preplan.get("recommendation_constraints") or {}),
                "semantic_ambiguity": bool(preplan.get("ambiguity")),
            },
            "skip_polish": True,
        }
    return {
        "intent": "recommendation",
        "answer_type": "clarification",
        "answer": "可以帮你从现有商品里认真筛选。为了避免推荐得太泛，请先告诉我更想送锅具、水具、炉具还是餐具，并补充最重要的一项需求，例如预算、人数、露营场景或自驾/徒步方式。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "answer_metadata": {
            "source": "semantic_recommendation_constraint_clarification",
            "reason": "high_confidence_recommendation_without_validated_constraints",
        },
        "debug": {
            "agent_mode": "semantic_recommendation_constraint_clarification",
            "recommendation_constraints": {},
        },
        "skip_polish": True,
    }


def _semantic_structured_query_constraint_clarification_result(
    preplan: dict[str, Any] | None,
    *,
    question: str = "",
) -> dict | None:
    """Fail closed when a semantic multi-filter plan cannot form a contract."""
    if not (
        isinstance(preplan, dict)
        and preplan.get("semantic_route_family_hint") == "structured_query"
        and str(preplan.get("fallback_reason") or "") in {
            "invalid_structured_query_constraints",
            "structured_query_constraints_outside_structured_query",
            "structured_query_constraints_field_mismatch",
            "missing_structured_query_constraints",
        }
    ):
        return None
    # Semantic planning has already selected the structured-query family, but
    # one JSON serialization may still fail its predicate schema.  When the
    # existing contract can independently validate every literal subject and
    # condition from this same customer turn, let that contract execute rather
    # than discard the high-confidence semantic route.  This is validation,
    # not a lexical route fallback: unresolved or generic contracts remain
    # fail-closed below.
    if question:
        validated_contract = customer_structured_query_contract.build_structured_query_contract(question)
        if validated_contract.status == "resolved":
            return None
    return {
        "intent": "query_products",
        "answer_type": "clarification",
        "answer": "我理解你同时限定了多个筛选条件，但当前无法把每项条件都稳定映射为可核验的商品范围。请换一种说法或分别说明最重要的筛选条件，我会按每个条件的真实商品资料筛选。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "evidence": [],
        "answer_metadata": {
            "source": "semantic_structured_query_invalid_contract_clarification",
            "reason": "invalid_semantic_structured_query_constraint_contract",
        },
        "debug": {
            "agent_mode": "semantic_structured_query_invalid_contract_clarification",
        },
        "skip_polish": True,
    }


def _invalid_structured_query_named_detail_contract_result(
    db: Session,
    question: str,
    semantic_preplan: dict[str, Any] | None,
) -> dict | None:
    """Recover one semantic product fact only after deterministic identity sealing.

    A malformed structured-filter payload is normally fail-closed.  The narrow
    exception is a preserved semantic candidate with one allowlisted field and
    a subject that the catalogue resolves uniquely.  The candidate never
    supplies a SKU, database value, or answer.
    """
    preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    candidate = preplan.get("invalid_structured_query_named_detail_candidate")
    if not isinstance(candidate, dict):
        return None
    subject = str(candidate.get("subject_text") or "").strip()
    fields = [
        customer_field_contract.semantic_preplan_field_type(value)
        for value in (candidate.get("canonical_fields") or [])
    ]
    fields = list(dict.fromkeys(
        field for field in fields if field in customer_field_contract.FORMAL_DETAIL_FIELDS
    ))
    if not subject or len(fields) != 1 or float(candidate.get("confidence") or 0) < 0.9:
        return None
    field_type = fields[0]
    products = db.query(Product).order_by(Product.sku.asc()).all()
    entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        products,
        entity_text_override=subject,
        field_type_override=field_type,
    )
    decision = customer_entity_resolution_contract.can_resolve_single_product(entity_contract, products)
    field = next(
        (item for item in customer_field_contract.FIELD_CONTRACTS if item.field_type == field_type),
        None,
    )
    field_label = customer_field_contract.product_detail_field_label(field_type)
    if field is None or not field_label:
        return None
    field_contract = {
        "field_type": field_type,
        "requested_field": field_label,
        "requested_fields": [field_label],
        "canonical_fields": [field_type],
        "supported_fields": [field_type],
        "unsupported_fields": [],
        "subject": subject,
        "requested_scope": "subject",
        "source": "recovered_semantic_named_detail_candidate",
        "confidence": float(candidate.get("confidence") or 0),
        "compound": False,
    }
    if entity_contract.status == "ambiguous":
        # A malformed structured-filter payload still cannot select a SKU,
        # but its independent single-field candidate must survive into the
        # normal entity clarification.  This keeps the field, candidate set,
        # and result_skus contract observable instead of replacing it with a
        # generic multi-filter error.
        result = _build_phase2_entity_state_response(
            db,
            question,
            {
                "action": "ambiguous_clarification",
                "contract": entity_contract,
                "products": products,
                "displayable_candidate_skus": _displayable_phase2_clarification_candidate_skus(entity_contract),
            },
        )
        if not isinstance(result, dict):
            return None
        debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
        debug["agent_mode"] = "invalid_structured_query_named_detail_ambiguous_contract"
        debug["field_contract"] = field_contract
        debug["entity_resolution_contract"] = entity_contract.to_dict()
        result["debug"] = debug
        metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
        metadata["contract_field_type"] = field_type
        metadata["source"] = "invalid_structured_query_named_detail_ambiguous_contract"
        result["answer_metadata"] = metadata
        return result
    if not (
        decision.allowed
        and entity_contract.is_unique
        and entity_contract.confidence == "high"
        and entity_contract.matched_by in _PHASE2_EXACT_ENTITY_MATCHES
    ):
        return None
    result = _build_phase2_entity_state_response(
        db,
        question,
        {
            "action": "resolved_detail",
            "contract": entity_contract,
            "products": products,
            "field": field,
            "requested_field": field_label,
        },
    )
    if not isinstance(result, dict):
        return None
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    debug["agent_mode"] = "invalid_structured_query_named_detail_contract"
    debug["field_contract"] = field_contract
    result["debug"] = debug
    metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    metadata["contract_field_type"] = field_type
    metadata["source"] = "invalid_structured_query_named_detail_contract"
    result["answer_metadata"] = metadata
    return result


def _semantic_outage_page_bounded_result(
    db: Session,
    question: str,
    request_sku_anchor: str,
    preplan: dict[str, Any],
) -> dict | None:
    """Answer a small set of explicit page/category asks during LLM outage.

    This is deliberately a catalogue/evidence adapter, not a replacement
    language router: every branch is gated by a page SKU and a concrete noun,
    and recommendations use only structured values already stored in the DB.
    """
    text = str(question or "").strip()
    if not text or not request_sku_anchor:
        return None

    def _annotate(result: dict | None, source: str) -> dict | None:
        if not result:
            return None
        debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
        debug["agent_mode"] = source
        debug["request_sku_anchor"] = request_sku_anchor
        debug["semantic_fallback_reason"] = str(preplan.get("fallback_reason") or "")
        result["debug"] = debug
        return result

    if _looks_like_ignition_capability_question(text):
        product, specs, _business, _content = _phase1_product_bundle_by_ref(db, request_sku_anchor)
        product_name = str(getattr(product, "product_name_cn", "") or request_sku_anchor).strip()
        usage_instruction = str(getattr(specs, "usage_instruction", "") or "").strip() if specs else ""
        requested_type = "远程点火" if any(term in text for term in ("远程点火", "远程打火")) else "电子点火"
        if requested_type in usage_instruction:
            answer = f"{product_name}（{request_sku_anchor}）的同 SKU 资料标注支持{requested_type}。"
        elif usage_instruction:
            mechanism = "按击点火装置" if "按击点火装置" in usage_instruction else "点火装置"
            answer = (
                f"{product_name}（{request_sku_anchor}）的同 SKU 资料只标注{mechanism}，"
                f"未标注{requested_type}，不能把该点火方式默认等同于{requested_type}。"
            )
        else:
            answer = (
                f"{product_name}（{request_sku_anchor}）的同 SKU 资料未标注是否支持{requested_type}，"
                "当前无法确认。"
            )
        result = _build_bounded_clarification_result(
            answer=answer,
            source="semantic_outage_page_ignition_capability_clarification",
        )
        result["sku"] = request_sku_anchor
        result["result_skus"] = [request_sku_anchor]
        result["candidate_skus"] = [request_sku_anchor]
        return _annotate(result, "semantic_outage_page_ignition_capability_clarification")

    if customer_field_contract.requested_dimension_subtype(text) == "fuel_holder":
        result = _phase1_product_field_result(
            db,
            {
                "product_ref": request_sku_anchor,
                "sku": request_sku_anchor,
                "requested_field": "尺寸",
                "field_type_override": "dimensions",
                "raw_question": text,
                "product_identity_source": "explicit_sku_exact",
            },
        )
        if result:
            return _annotate(result, "semantic_outage_page_fuel_holder_dimension")

    if _looks_like_material_or_surface_detail_question(text):
        product, specs, _business, _content = _phase1_product_bundle_by_ref(db, request_sku_anchor)
        product_name = str(getattr(product, "product_name_cn", "") or request_sku_anchor).strip()
        product_category = str(getattr(product, "category", "") or "").strip()
        body_material = str(getattr(specs, "body_material", "") or "").strip() if specs else ""
        surface_finish = str(getattr(specs, "surface_finish", "") or "").strip() if specs else ""
        lines = [f"{product_name}（{request_sku_anchor}）："]
        if (
            any(term in text for term in ("锅", "锅具", "煎锅", "烤盘"))
            and product_category
            and not any(term in product_category for term in ("锅具", "烤盘", "煎盘"))
        ):
            lines.append(
                f"当前页面商品属于{product_category}，不是锅具；如果你问的是搭配的锅具，请提供锅具商品名或 SKU。"
            )
        if any(term in text for term in ("材质", "材料")):
            lines.append(f"材质：{body_material or '当前资料未标注，无法确认'}。")
        if any(term in text for term in ("涂层", "表面处理", "表面工艺")):
            lines.append(f"涂层/表面处理：{surface_finish or '当前资料未标注，无法确认'}。")
        if any(term in text for term in ("不粘", "不沾")):
            normalized_surface = surface_finish.replace(" ", "")
            if any(term in normalized_surface for term in ("不粘", "不沾", "防粘")):
                lines.append(f"不粘/不沾标注：{surface_finish}。")
            elif surface_finish:
                lines.append(
                    f"不粘结论：同 SKU 资料只标注表面处理为{surface_finish}，"
                    "未标注不粘/不沾涂层，不能确认是不粘锅。"
                )
            else:
                lines.append("不粘结论：当前资料未标注不粘/不沾处理，无法确认。")
        result = _build_bounded_clarification_result(
            answer="\n".join(lines),
            source="semantic_outage_page_material_detail_clarification",
        )
        result["sku"] = request_sku_anchor
        result["result_skus"] = [request_sku_anchor]
        result["candidate_skus"] = [request_sku_anchor]
        return _annotate(result, "semantic_outage_page_material_detail_clarification")

    if _looks_like_page_cookware_fit_question(text, request_sku_anchor):
        result = _build_bounded_clarification_result(
            answer=(
                f"当前资料未标注页面商品 {request_sku_anchor} 可承载锅具的最大尺寸，暂时无法确认。"
                "请以说明书或炉架/支撑面的实际内径和允许锅底直径为准，不要仅按锅具容量推断。"
            ),
            source="semantic_outage_page_cookware_fit_clarification",
        )
        result["sku"] = request_sku_anchor
        result["result_skus"] = [request_sku_anchor]
        result["candidate_skus"] = [request_sku_anchor]
        return _annotate(result, "semantic_outage_page_cookware_fit_clarification")

    if _looks_like_missing_component_question(text):
        return _annotate(
            _build_bounded_clarification_result(
                answer=(
                    f"当前页面商品 {request_sku_anchor} 的包装清单未直接核验，无法确认你提到的组件是否漏发或本来就不包含。"
                    "请提供套装型号、订单信息或包装清单照片，我再按同 SKU 核对。"
                ),
                source="semantic_outage_page_missing_component_clarification",
            ),
            "semantic_outage_page_missing_component_clarification",
        )

    # Explicit category availability: preserve the category boundary but do
    # not make a page product look like the requested category.
    if "烤盘" in text and any(term in text for term in ("有没有", "有卖", "卖吗", "推荐")):
        rows = _phase1_catalog_rows(db, "烤盘")
        return _annotate(
            _structured_product_query_result(
                question=text,
                product_ref="烤盘",
                rows=rows,
                source="semantic_outage_page_griddle_catalog",
                reason_label="显式烤盘类目",
                answer_type="product_query",
                metadata_source="semantic_outage_page_griddle_catalog",
            )
            or _build_bounded_clarification_result(
                answer="当前产品库未维护可核验的烤盘商品，暂时无法确认是否有对应款式。",
                source="semantic_outage_page_griddle_no_match",
            ),
            "semantic_outage_page_griddle_catalog",
        )

    # A table-side gas-canister clip is an exact accessory noun.  Do not let a
    # generic accessory category return unrelated bags, adapters, or stoves.
    if "气罐桌边夹" in text:
        accessory_rows = _phase1_catalog_rows(db, "配件")
        exact_rows = [
            row for row in accessory_rows
            if "桌边夹" in " ".join(
                str(row.get(key) or "") for key in ("product_name_cn", "product_name_en", "features", "long_description_cn")
            )
        ]
        result = _structured_product_query_result(
            question=text,
            product_ref="配件",
            rows=exact_rows,
            source="semantic_outage_page_table_clip_catalog",
            reason_label="明确气罐桌边夹配件",
            answer_type="product_query",
            metadata_source="semantic_outage_page_table_clip_catalog",
        )
        return _annotate(
            result
            or _build_bounded_clarification_result(
                answer="当前产品库未找到名称或资料明确标注为“气罐桌边夹”的配件，不能把其他配件当作替代品；请提供具体型号或联系人工客服确认。",
                source="semantic_outage_page_table_clip_no_match",
            ),
            "semantic_outage_page_table_clip_catalog",
        )

    if any(term in text for term in ("配套的炉具", "匹配的炉具", "适配的炉具")):
        return _annotate(
            _build_bounded_clarification_result(
                answer=f"当前资料未标注 {request_sku_anchor} 配套或兼容的炉具，不能把其他炉具直接当作配套商品。请提供锅具/配件的接口或具体型号，我再核对同款兼容性。",
                source="semantic_outage_page_stove_pairing_no_match",
            ),
            "semantic_outage_page_stove_pairing_no_match",
        )

    if any(term in text for term in ("最大火力", "最大功率", "火力最大")) and any(
        term in text for term in ("炉头", "炉具", "炉子", "气炉")
    ):
        rows = _phase1_catalog_rows(db, "炉具")
        comparable: list[tuple[float, dict, str]] = []
        for row in rows:
            raw_power = str(row.get("power") or "")
            values = [float(value) for value in re.findall(r"(\d+(?:\.\d+)?)\s*[wW瓦]", raw_power)]
            if values:
                comparable.append((max(values), row, raw_power))
        if comparable:
            max_power = max(item[0] for item in comparable)
            leaders = [item for item in comparable if item[0] == max_power]
            lead_text = "、".join(
                f"{item[1].get('product_name_cn') or item[1].get('sku')}（{item[1].get('sku')}）"
                for item in leaders
            )
            choose_one = bool(re.search(r"推荐\s*(?:一个|一款)?", text))
            selected = leaders[0]
            selected_label = f"{selected[1].get('product_name_cn') or selected[1].get('sku')}（{selected[1].get('sku')}）"
            if choose_one:
                answer = (
                    f"按当前资料已标注的功率，最高为 {max_power:g}W；如果只选一款，优先推荐{selected_label}。"
                    "它与其他并列最高款的功率相同，是否适合你的炉具还要核对接口和燃料类型。"
                )
                result_rows = [selected[1]]
                result_skus = [str(selected[1].get("sku") or "").strip().upper()] if selected[1].get("sku") else []
            else:
                answer = f"按当前资料已标注的功率，{lead_text}的火力最大，为 {max_power:g}W。这里仅按功率比较，是否适合你的炉具还要核对接口和燃料类型。"
                result_rows = [item[1] for item in leaders]
                result_skus = [str(item[1].get("sku") or "").strip().upper() for item in leaders if item[1].get("sku")]
            result = {
                "intent": "recommendation",
                "answer_type": "recommendation",
                "answer": answer,
                "results": result_rows,
                "result_skus": result_skus,
                "candidate_skus": result_skus,
                "answer_metadata": {"source": "semantic_outage_page_power_catalog"},
                "skip_polish": True,
            }
            return _annotate(result, "semantic_outage_page_power_catalog")
        return _annotate(
            _build_bounded_clarification_result(
                answer="当前炉具资料没有可直接对照的功率标注，无法确认哪款火力最大；请提供具体型号或以说明书为准。",
                source="semantic_outage_page_power_no_match",
            ),
            "semantic_outage_page_power_no_match",
        )

    if "凹槽" in text or "针头" in text:
        return _annotate(
            _build_bounded_clarification_result(
                answer=f"当前资料未标注 {request_sku_anchor} 炉盘针头与凹槽的具体位置示意，不能凭文字确认你指的是哪一个凹槽；请对照说明书/安装图或发照片给人工客服核对。",
                source="semantic_outage_page_installation_clarification",
            ),
            "semantic_outage_page_installation_clarification",
        )

    if "气罐" in text and any(term in text for term in ("推荐", "单买", "用什么", "有吗", "有没有")):
        product, specs, _business, _content = _phase1_product_bundle_by_ref(db, request_sku_anchor)
        heat_source = str(getattr(specs, "heat_source", "") or "").strip() if specs else ""
        heat_note = f"{request_sku_anchor}资料标注的适用热源为：{heat_source}。" if heat_source else "当前页商品的适用热源资料未完整标注。"
        return _annotate(
            _build_bounded_clarification_result(
                answer=f"{heat_note}当前产品库未维护可直接核验的气罐单品或库存，不能把转接头当作气罐；请按气罐规格和说明书核对后再购买。",
                source="semantic_outage_page_canister_scope_clarification",
            ),
            "semantic_outage_page_canister_scope_clarification",
        )

    if any(term in text for term in ("气罐", "一个罐", "一罐", "罐子")) and any(
        term in text for term in ("几个炉", "多个炉", "分体", "同时", "一拖多", "并联")
    ):
        return _annotate(
            _build_bounded_clarification_result(
                answer=(
                    f"当前资料未标注 {request_sku_anchor} 支持一个气罐同时连接多个炉头，也未提供一拖多或并联配件说明；"
                    "不能把“分体炉”理解成一罐多炉。请按说明书和接口规格核对，避免自行改装。"
                ),
                source="semantic_outage_page_canister_multi_burner_clarification",
            ),
            "semantic_outage_page_canister_multi_burner_clarification",
        )

    if "带锅" in text and any(term in text for term in ("有没有", "有吗", "带锅的", "不带锅")):
        rows = _phase1_catalog_rows(db, "锅具")
        result = _structured_product_query_result(
            question=text,
            product_ref="锅具",
            rows=rows,
            source="semantic_outage_page_cookware_bundle_catalog",
            reason_label="显式带锅套装/锅具类目",
            answer_type="product_query",
            metadata_source="semantic_outage_page_cookware_bundle_catalog",
        )
        return _annotate(
            result or _build_bounded_clarification_result(
                answer="当前产品库没有可直接核验的带锅套装或锅具款式，暂时无法确认。",
                source="semantic_outage_page_cookware_bundle_no_match",
            ),
            "semantic_outage_page_cookware_bundle_catalog",
        )

    pairing_terms = ("配哪个", "配的哪个", "配什么", "配的什么", "配套", "匹配", "适配", "能配", "适合")
    if any(term in text for term in pairing_terms):
        product, specs, _business, _content = _phase1_product_bundle_by_ref(db, request_sku_anchor)
        heat_source = str(getattr(specs, "heat_source", "") or "").strip() if specs else ""
        if "酒精" in text and any(term in text for term in ("配", "用", "适用")):
            heat_note = f"同 SKU {request_sku_anchor} 的适用热源资料标注为：{heat_source}。" if heat_source else f"当前资料未完整标注 {request_sku_anchor} 的适用热源。"
            return _annotate(
                _build_bounded_clarification_result(
                    answer=heat_note + "具体酒精浓度或燃料型号未单独核验，不能仅凭“酒精”二字确认可配哪一种；请以说明书或燃料标注为准。",
                    source="semantic_outage_page_alcohol_pairing_clarification",
                ),
                "semantic_outage_page_alcohol_pairing_clarification",
            )
        if any(term in text for term in ("水壶", "烧水壶", "锅具", "锅里具", "锅", "烤盘")):
            target = "水壶" if any(term in text for term in ("水壶", "烧水壶")) else "锅具/烤盘"
            return _annotate(
                _build_bounded_clarification_result(
                    answer=f"当前资料未标注页面商品 {request_sku_anchor} 与具体{target}的配套或兼容关系，不能把目录里的其他商品直接当作匹配款；请提供接口、口径或具体型号，我再按同款资料核对。",
                    source="semantic_outage_page_pairing_clarification",
                ),
                "semantic_outage_page_pairing_clarification",
            )

    if (
        any(term in text for term in ("餐具", "碗"))
        and any(term in text for term in ("装在", "放在", "收纳", "携带"))
        and any(term in text for term in ("这个锅", "这款锅", "锅里面", "锅里"))
    ):
        return _annotate(
            _build_bounded_clarification_result(
                answer=f"当前资料未标注页面商品 {request_sku_anchor} 与具体碗/餐具的嵌套收纳尺寸，不能凭“能装进去”直接推荐某款；请提供锅具内径和餐具外径，我再核对同款尺寸。",
                source="semantic_outage_page_nested_tableware_clarification",
            ),
            "semantic_outage_page_nested_tableware_clarification",
        )

    if re.search(r"(?:这个|这款).{0,10}(?:有|有没有).{0,8}(?:烧的)?炉子", text):
        return _annotate(
            _build_bounded_clarification_result(
                answer=f"当前页面商品 {request_sku_anchor} 的资料未标注是否随附或配套单独炉具，不能把其他炉具当作本页商品内容；请查看包装清单或提供具体套装型号。",
                source="semantic_outage_page_stove_inclusion_clarification",
            ),
            "semantic_outage_page_stove_inclusion_clarification",
        )

    # Explicit category asks remain safe during a planner outage when the
    # category boundary itself is present in the customer turn.  The page SKU
    # is only context; it is never substituted for the requested category.
    if _looks_like_material_catalogue_request(text):
        rows = _phase1_catalog_rows(db, "锅具")
        requested_grades = [grade for grade in ("304", "316") if grade in text]
        if requested_grades:
            rows = [
                row for row in rows
                if any(
                    grade in " ".join(
                        str(row.get(key) or "")
                        for key in ("body_material", "features", "long_description_cn", "product_name_cn")
                    )
                    for grade in requested_grades
                )
            ]
        result = _structured_product_query_result(
            question=text,
            product_ref="锅具",
            rows=rows,
            source="semantic_outage_page_material_catalog",
            reason_label="显式304/316材质筛选",
            answer_type="recommendation" if "推荐" in text else "product_query",
            metadata_source="semantic_outage_page_material_catalog",
        )
        return _annotate(
            result or _build_bounded_clarification_result(
                answer="当前产品库没有找到资料明确标注为304或316不锈钢的锅具，暂时无法确认有符合条件的款式；如果可以接受其他材质，我再继续筛选。",
                source="semantic_outage_page_material_no_match",
            ),
            "semantic_outage_page_material_catalog",
        )

    category_requests = (
        (("烧水壶", "水壶"), ("有没有", "有卖", "卖吗", "推荐", "哪个"), "水壶", "显式水壶类目"),
        (("大锅", "套锅", "锅具"), ("有没有", "有卖", "卖吗", "推荐"), "锅具", "显式锅具类目"),
        (("炉头",), ("有没有", "有卖", "卖吗", "推荐"), "配件", "显式炉头配件类目"),
        (("炉具", "炉子"), ("有没有", "有卖", "卖吗", "推荐"), "炉具", "显式炉具类目"),
        (("餐具", "碗"), ("有没有", "有卖", "卖吗", "推荐"), "餐具", "显式餐具类目"),
    )
    for subject_terms, action_terms, category, reason_label in category_requests:
        if any(term in text for term in subject_terms) and any(term in text for term in action_terms):
            rows = _phase1_catalog_rows(db, category)
            result = _structured_product_query_result(
                question=text,
                product_ref=category,
                rows=rows,
                source="semantic_outage_page_explicit_category_catalog",
                reason_label=reason_label,
                answer_type="recommendation" if "推荐" in text else "product_query",
                metadata_source="semantic_outage_page_explicit_category_catalog",
            )
            return _annotate(
                result or _build_bounded_clarification_result(
                    answer=f"当前产品库没有可直接核验的{category}商品，暂时无法确认是否有对应款式。",
                    source="semantic_outage_page_explicit_category_no_match",
                ),
                "semantic_outage_page_explicit_category_catalog",
            )

    # Fuel subtype questions must not be widened into a generic gas-stove
    # recommendation.  The general usage composer gives a bounded answer when
    # the page does not expose a same-SKU fuel record.
    if (
        any(term in text for term in ("固体酒精", "液体酒精", "酒精炉"))
        and any(term in text for term in ("怎么点", "怎么用", "怎么使用", "能用", "可以用", "是哪款"))
    ):
        fuel_rows = [
            row for row in _phase1_catalog_rows(db, "炉具")
            if any(term in " ".join(str(row.get(key) or "") for key in ("heat_source", "product_name_cn", "features", "long_description_cn"))
                   for term in ("酒精炉", "固体酒精", "液体酒精"))
        ]
        if "是哪款" in text and fuel_rows:
            result = _structured_product_query_result(
                question=text,
                product_ref="酒精炉",
                rows=fuel_rows,
                source="semantic_outage_page_alcohol_stove_catalog",
                reason_label="显式酒精炉类目",
                answer_type="product_query",
                metadata_source="semantic_outage_page_alcohol_stove_catalog",
            )
            return _annotate(result, "semantic_outage_page_alcohol_stove_catalog")
        return _annotate(
            _build_bounded_clarification_result(
                answer="当前资料未标注这款商品支持固体酒精还是液体酒精，不能把“酒精炉”三个字直接等同于具体燃料类型；请提供 SKU 或核对说明书。",
                source="semantic_outage_page_alcohol_fuel_subtype_clarification",
            ),
            "semantic_outage_page_alcohol_fuel_subtype_clarification",
        )

    # A few page-bound safety/feature questions use natural wording that is
    # not a formal field alias.  Keep the answer tied to the page SKU and to
    # explicitly stored same-row feature text instead of exposing an outage.
    if any(term in text for term in ("桌面上使用", "桌面使用", "下面不需要", "额外垫", "挡风")):
        product, specs, business, content = _phase1_product_bundle_by_ref(db, request_sku_anchor)
        facts = " ".join(
            str(getattr(item, field, "") or "")
            for item, field in (
                (specs, "technical_advantages"),
                (business, "top_selling_points"),
                (business, "usage_scenarios"),
                (content, "long_description_cn"),
            )
            if item is not None
        )
        if "挡风" in text and "防风" in facts:
            answer = f"同 SKU 资料标注 {request_sku_anchor} 具备防风相关设计；具体挡风效果仍取决于风力和摆放方式。"
        elif "挡风" in text:
            answer = f"当前资料未标注 {request_sku_anchor} 是否带有防风或挡风设计，暂时无法确认；具体效果还会受风力和摆放方式影响，请以说明书或实物结构为准。"
        elif "桌面" in text:
            answer = f"当前资料未标注 {request_sku_anchor} 是否可以直接放在桌面或是否需要额外垫物，暂时无法确认；请按说明书和耐热摆放要求核对。"
        else:
            answer = f"当前资料未标注 {request_sku_anchor} 炉体下方是否需要额外垫物，暂时无法确认；请保持平稳、耐热并避开可燃表面。"
        return _annotate(
            _build_bounded_clarification_result(answer=answer, source="semantic_outage_page_same_sku_safety_clarification"),
            "semantic_outage_page_same_sku_safety_clarification",
        )

    # If the page identity is visibly for another product family, do not
    # silently use it to answer a material complaint about a kettle/teapot.
    if any(term in text for term in ("茶壶", "紫铜")):
        product, _specs, _business, _content = _phase1_product_bundle_by_ref(db, request_sku_anchor)
        page_name = str(getattr(product, "product_name_cn", "") or request_sku_anchor) if product else request_sku_anchor
        return _annotate(
            _build_bounded_clarification_result(
                answer=f"你问的是茶壶材质，但当前页面商品是{page_name}（{request_sku_anchor}），两者不是同一商品；请提供茶壶的商品名或 SKU，我再核对材质。",
                source="semantic_outage_page_cross_family_identity_clarification",
            ),
            "semantic_outage_page_cross_family_identity_clarification",
        )
    return None


def _semantic_preplan_unavailable_clarification_result(
    preplan: dict[str, Any] | None,
    *,
    db: Session | None = None,
    question: str = "",
    phase1_plan: dict[str, Any] | None = None,
    request_sku_anchor: str | None = None,
) -> dict | None:
    """Prevent an unavailable semantic planner from reopening legacy routing.

    A deterministic FieldContract adapter remains usable for a canonical,
    allowlisted field that the legacy planner has already recognized.  That is a
    narrow outage fallback, not a second language router: it must still seal an
    entity and consume same-SKU evidence.  Without that formal contract, an LLM
    outage conveys no safe meaning for an ordinary natural language turn, so
    legacy QA/usage/recommendation shortcuts must not guess.
    """
    if not isinstance(preplan, dict) or not preplan.get("called"):
        return None
    if not str(preplan.get("fallback_reason") or "").startswith("llm_error:"):
        return None
    if preplan.get("semantic_adapter_source"):
        return None
    named_products = _products_named_in_question(db, question) if db is not None else []
    outage_field_contract = resolve_requested_field_contract(question, phase1_plan)
    explicit_family_listing = any(
        term in str(question or "")
        for term in ("有哪些", "有哪", "哪几款", "列出", "产品清单")
    )
    if db is not None and request_sku_anchor:
        page_bounded_result = _semantic_outage_page_bounded_result(
            db,
            question,
            request_sku_anchor,
            preplan,
        )
        if page_bounded_result:
            return page_bounded_result
    if db is not None:
        # The search provider already normalizes the full utterance against
        # live catalogue fields.  During a semantic-planner outage, accept its
        # result only when it forms a bounded family (not one guessed product
        # and not the whole catalogue).  This is data-scope validation rather
        # than a second keyword intent router.
        catalogue_rows = _phase1_catalog_rows(db, "产品")
        bounded_rows = customer_agent_service.search_products(db, question, limit=50)
        identity_family_rows = _database_question_product_family_scope_rows(bounded_rows, question)
        if (
            1 < len(identity_family_rows) < len(catalogue_rows)
            and (explicit_family_listing or not named_products)
        ):
            outage_catalogue_result = _structured_product_query_result(
                question=question,
                product_ref=str(question or "").strip(" ？?。！!"),
                rows=identity_family_rows,
                source="semantic_catalog_family_scope",
                reason_label="数据库商品族范围",
                answer_type="query_products",
                metadata_source="semantic_outage_catalog_family_query",
            )
            if outage_catalogue_result:
                outage_catalogue_result.setdefault("debug", {})["semantic_fallback_reason"] = str(
                    preplan.get("fallback_reason") or ""
                )
                return outage_catalogue_result
    # An outage must not erase a comparison or multi-field detail contract
    # whose participants are already explicit and can be sealed
    # deterministically.  These paths do not infer a recommendation from
    # wording: every named participant still receives an
    # EntityResolutionContract and every field still needs same-SKU evidence.
    if len(named_products) >= 2:
        return None
    if (
        outage_field_contract.get("canonical_fields")
        and named_products
    ):
        return None
    # A generic accessory catalogue request already has an explicit,
    # allowlisted FieldContract and a first-party category boundary.  It can
    # be executed safely during a semantic-planner outage without guessing a
    # product preference or reopening the broad legacy recommendation router.
    if (
        db is not None
        and str(outage_field_contract.get("field_type") or "").strip() == "accessories"
        and (
            _looks_like_recommendation_request(question)
            or _is_explicit_broad_catalogue_request(question)
        )
    ):
        accessory_rows = _phase1_catalog_rows(db, "配件")
        accessory_result = _structured_product_query_result(
            question=str(question or "").strip(),
            product_ref="配件",
            rows=accessory_rows,
            source="semantic_outage_explicit_accessory_catalog",
            reason_label="显式配件类目",
            answer_type="recommendation",
            metadata_source="semantic_outage_explicit_accessory_catalog",
        )
        if accessory_result:
            accessory_result.setdefault("debug", {})["semantic_fallback_reason"] = str(
                preplan.get("fallback_reason") or ""
            )
            return accessory_result

    # Usage/safety turns have conservative, product-agnostic composers that
    # do not need an LLM to identify a SKU.  Use them during a planner outage
    # so a real safety question gets a useful bounded answer instead of an
    # internal service-status message.  A named product still waits for the
    # later same-SKU evidence guard above.
    if (
        db is not None
        and not named_products
        and not _looks_like_recommendation_request(question)
        and customer_agent_intent_service._looks_like_usage_care_question(question)
    ):
        usage_subtype = customer_agent_intent_service._detect_usage_care_subtype(question)
        if usage_subtype == "safety":
            usage_answer = customer_agent_intent_service._compose_safety_usage_care_answer(question)
        elif usage_subtype == "installation":
            usage_answer = customer_agent_intent_service._compose_installation_usage_care_answer(question, [], [])
        elif usage_subtype == "duration":
            usage_answer = customer_agent_intent_service._compose_duration_usage_care_answer(question, [], [])
        elif usage_subtype == "power_tradeoff":
            usage_answer = customer_agent_intent_service._compose_power_tradeoff_usage_care_answer(question, [], [])
        elif usage_subtype == "composition":
            usage_answer = "请提供具体商品名或 SKU，我再按该商品的包装清单核对是否包含你提到的组件。"
        else:
            usage_answer = customer_agent_intent_service._compose_usage_care_answer(
                question,
                [],
                [],
                response_style="usage_guidance",
            )
        return {
            "intent": "product_usage_care",
            "answer_type": "product_usage_care",
            "answer": usage_answer,
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "answer_metadata": {"source": "semantic_outage_general_usage_boundary"},
            "debug": {
                "agent_mode": "semantic_outage_general_usage_boundary",
                "usage_care_subtype": usage_subtype,
                "semantic_fallback_reason": str(preplan.get("fallback_reason") or ""),
            },
            "skip_polish": True,
        }
    # Recommendation intent is semantic-owned. During an outage, never let a
    # later lexical field match decide that a recommendation is merely a
    # filter; return the same fail-closed clarification before any shortcut.
    if _looks_like_recommendation_request(question):
        return {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": "我还缺少足够的商品范围或使用条件，暂时不能可靠给出推荐。请补充目标品类、商品名/SKU或想比较的条件，我再按已核对资料筛选。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "answer_metadata": {"source": "semantic_preplan_unavailable_clarification"},
            "debug": {
                "agent_mode": "semantic_preplan_unavailable_clarification",
                "semantic_fallback_reason": str(preplan.get("fallback_reason") or ""),
            },
            "skip_polish": True,
        }
    if str(preplan.get("field_type") or "").strip() or preplan.get("canonical_fields"):
        # A recommendation remains a semantic decision even if one of its
        # words resembles a formal detail field (for example, a heat source).
        # During a model outage do not let that partial lexical contract reopen
        # a legacy catalogue recommender; it cannot know the requested subject
        # or the customer's preference priority.
        if (
            (isinstance(phase1_plan, dict) and str(phase1_plan.get("primary_intent") or "") == "recommendation")
            or _looks_like_recommendation_request(question)
        ):
            return {
                "intent": "clarify",
                "answer_type": "clarification",
                "answer": "我还缺少足够的商品范围或使用条件，暂时不能可靠给出推荐。请补充目标品类、商品名/SKU或想比较的条件，我再按已核对资料筛选。",
                "results": [],
                "result_skus": [],
                "candidate_skus": [],
                "evidence": [],
                "answer_metadata": {"source": "semantic_preplan_unavailable_clarification"},
                "debug": {
                    "agent_mode": "semantic_preplan_unavailable_clarification",
                    "semantic_fallback_reason": str(preplan.get("fallback_reason") or ""),
                },
                "skip_polish": True,
            }
        return None
    field_contract = resolve_requested_field_contract(question, phase1_plan)
    if str(field_contract.get("field_type") or "").strip() in customer_field_contract.FORMAL_DETAIL_FIELDS:
        return None
    # Do not preempt the later named-product outage guard.  That guard first
    # seals EntityResolutionContract and returns a clarification carrying the
    # resolved identity; the generic branch below is only for genuinely
    # unbound natural-language turns.
    if db is not None and _products_named_in_question(db, question):
        return None
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": "我还需要具体商品名或 SKU 才能按同款资料核对；也可以补充你要查询的字段，例如材质、容量或适用热源。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "evidence": [],
        "answer_metadata": {"source": "semantic_preplan_unavailable_clarification"},
        "debug": {
            "agent_mode": "semantic_preplan_unavailable_clarification",
            "semantic_fallback_reason": str(preplan.get("fallback_reason") or ""),
        },
        "skip_polish": True,
    }


def _phase1_structured_recommendation_result(db: Session, plan: dict) -> dict | None:
    scenario = str(plan.get("scenario") or plan.get("raw_question") or "").strip()
    if not scenario:
        return None
    semantic_preplan = plan.get("semantic_preplan") if isinstance(plan, dict) else None
    if isinstance(semantic_preplan, dict) and semantic_preplan.get("called"):
        # Once semantic planning has seen a recommendation turn, the legacy
        # scenario recognizers are no longer allowed to decide its product
        # scope or emit a fixed candidate list.  A valid semantic contract is
        # executed by the central recommendation/comparison path; an invalid
        # one is deliberately clarified there.  This boundary is based only
        # on semantic provenance, never on wording or a product identifier.
        return None
    # The planner's recommendation label is not authority to discard an
    # unresolved product-like subject.  Candidate generation is allowed only
    # for a genuine category/constraint request; a named-but-unmatched product
    # must first receive an unresolved EntityResolutionContract response.
    if _has_unresolved_product_like_scope(db, scenario):
        return None
    # An explicit accessory FieldContract outranks an adjacent subject noun
    # such as “炉具” in “炉具配件推荐”.  Keep the query inside the accessory
    # category (and, when present, the same explicit subject scope) instead of
    # letting the legacy scorer reinterpret it as a stove recommendation.
    accessory_contract = plan.get("field_contract") if isinstance(plan.get("field_contract"), dict) else {}
    if (
        str(accessory_contract.get("field_type") or "").strip() == "accessories"
        and not customer_agent_intent_service._extract_skus(scenario)
        and not _products_named_in_question(db, scenario)
    ):
        accessory_rows = _phase1_catalog_rows(db, "配件")
        subject = str(accessory_contract.get("subject") or "").strip()
        if subject and subject not in {"配件", "产品"}:
            scoped_rows = [
                row
                for row in accessory_rows
                if subject in " ".join(
                    str(row.get(key) or "")
                    for key in (
                        "product_name_cn",
                        "product_name_en",
                        "category",
                        "title_cn",
                        "website_title",
                        "features",
                        "usage_scenarios",
                        "positioning",
                        "long_description_cn",
                    )
                )
            ]
            if scoped_rows:
                accessory_rows = scoped_rows
        return _structured_product_query_result(
            question=scenario,
            product_ref="配件",
            rows=accessory_rows,
            source="planner_explicit_accessory_catalog",
            reason_label="显式配件类目",
            answer_type="recommendation",
            metadata_source="planner_explicit_accessory_catalog",
        )
    scope_text = " ".join(
        part for part in (
            str(plan.get("scenario") or "").strip(),
            str(plan.get("raw_question") or "").strip(),
        )
        if part
    )
    rows = _phase1_catalog_rows(db, "产品")
    if (
        any(term in scope_text for term in ("酒精炉", "液体酒精", "固体酒精", "alcohol stove"))
        and any(term in scope_text for term in ("锅", "锅具", "套锅", "单锅", "炊具"))
        and not any(term in scope_text for term in ("水壶", "茶壶", "烧水壶", "壶类"))
    ):
        exact_rows = [
            row for row in rows
            if _is_service_pot_or_cookware_set_candidate(row) and _phase1_row_has_explicit_alcohol_stove_support(db, row)
        ]
        if exact_rows:
            selected = exact_rows[:5]
            lines = ["当前资料里明确标注适合酒精炉的锅具，优先看下面这些："]
            for row in selected[:3]:
                name = row.get("product_name_cn") or row.get("sku")
                sku = str(row.get("sku") or "").strip().upper()
                heat_source = str(row.get("heat_source") or "").strip() or "同SKU资料/问答明确显示支持酒精炉"
                lines.append(f"- {name}（{sku}），明确证据：{heat_source}")
            skus = [str(row.get("sku") or "").strip().upper() for row in selected if row.get("sku")]
            return {
                "intent": "recommendation",
                "answer_type": "recommendation",
                "answer": "\n".join(lines),
                "results": selected,
                "result_skus": skus,
                "candidate_skus": skus,
                "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
                "answer_metadata": {"source": "product_catalog_structured_recommendation"},
                "skip_polish": True,
            }
        fallback_rows = [row for row in rows if _is_service_pot_or_cookware_set_candidate(row)][:3]
        if fallback_rows:
            fallback_lines = [
                "当前资料里没有找到明确标注适合酒精炉的锅具。",
                "下面这些只能作为普通锅具替代参考；它们没有酒精炉证据，不能直接当成酒精炉适配推荐：",
            ]
            for row in fallback_rows:
                name = row.get("product_name_cn") or row.get("sku")
                sku = str(row.get("sku") or "").strip().upper()
                reason = str(row.get("usage_scenarios") or row.get("features") or row.get("positioning") or "可作为同类替代").strip("。；; ")
                fallback_lines.append(f"- {name}（{sku}），{reason}")
            skus = [str(row.get("sku") or "").strip().upper() for row in fallback_rows if row.get("sku")]
            return {
                "intent": "recommendation",
                "answer_type": "recommendation",
                "answer": "\n".join(fallback_lines),
                "results": fallback_rows,
                "result_skus": skus,
                "candidate_skus": skus,
                "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
                "answer_metadata": {"source": "product_catalog_structured_recommendation"},
                "skip_polish": True,
            }
    if _phase1_is_water_kettle_selection_scenario(scenario):
        water_kettle_result = _phase1_structured_water_kettle_result(scenario, rows)
        if water_kettle_result:
            return water_kettle_result
    if _phase1_is_light_budget_cookware_set_scenario(scenario):
        light_budget_set_result = _phase1_structured_light_budget_cookware_set_result(scenario, rows)
        if light_budget_set_result:
            return light_budget_set_result
    if _phase1_is_griddle_vs_cookware_scenario(scenario):
        griddle_vs_cookware_result = _phase1_structured_griddle_vs_cookware_result(scenario, rows)
        if griddle_vs_cookware_result:
            return griddle_vs_cookware_result
    if _phase1_is_stove_pairing_scenario(scenario):
        stove_pairing_result = _phase1_structured_stove_pairing_result(scenario, rows)
        if stove_pairing_result:
            return stove_pairing_result
    if _phase1_is_stove_griddle_combo_scenario(scenario):
        combo_result = _phase1_structured_stove_griddle_result(scenario, rows)
        if combo_result:
            return combo_result
    scope_ref = _phase1_explicit_recommendation_scope_ref(scope_text)
    if scope_ref:
        rows = _phase1_catalog_rows(db, scope_ref)
        if scope_ref == "烤盘":
            rows = [
                row
                for row in rows
                if any(
                    term in " ".join(
                        str(row.get(key) or "")
                        for key in ("product_name_cn", "product_name_en", "category", "capacity", "features")
                    ).lower()
                    for term in ("烤盘", "煎盘", "griddle")
                )
            ]
    scored: list[tuple[int, dict]] = []
    for row in rows:
        haystack = " ".join(
            str(row.get(key) or "")
            for key in (
                "sku",
                "product_name_cn",
                "category",
                "capacity",
                "features",
                "usage_scenarios",
                "positioning",
                "long_description_cn",
            )
        )
        category = str(row.get("category") or "")
        score = 0
        if any(term in scenario for term in ("锅", "锅具", "套锅", "单锅", "炊具")):
            if "锅" in category:
                score += 8
            elif not _phase1_is_stove_griddle_combo_scenario(scenario):
                score -= 10
        if any(term in scenario for term in ("套锅", "套装")):
            if any(term in haystack for term in ("套锅", "套装", "锅5件套", "锅7件套", "锅10件套")):
                score += 8
            elif "锅" in category:
                score += 2
            else:
                score -= 8
        if "野餐" in scenario and "野餐" in haystack:
            score += 6
        if any(term in scenario for term in ("两个人", "2人", "两人", "双人")) and any(term in haystack for term in ("2人", "2-3人", "两人", "两个人", "双人")):
            score += 5
        if any(term in scenario for term in ("轻露营", "轻一点", "轻量", "轻便")) and any(term in haystack for term in ("轻量", "轻便", "便携", "收纳", "套娃")):
            score += 4
        if any(term in scenario for term in ("别太单薄", "稳一点", "稳定")) and any(term in haystack for term in ("稳", "稳定", "硬质氧化", "不锈钢", "厚实")):
            score += 4
        if "周末" in scenario and any(term in haystack for term in ("周末", "短途", "公园", "家庭野餐")):
            score += 2
        if "徒步" in scenario and "徒步" in haystack:
            score += 8
        if any(term in scenario for term in ("长途徒步", "只想带一个锅", "一个锅", "单锅")) and any(term in haystack for term in ("单锅", "轻量", "徒步", "套娃")):
            score += 7
        if any(term in scenario for term in ("一个人", "单人", "1人")) and any(term in haystack for term in ("单人", "1人", "1-2人", "1000ML", "轻量")):
            score += 5
        if any(term in scenario for term in ("三口之家", "四五个", "多人", "家庭")) and any(term in haystack for term in ("3L", "3700ML", "多人", "家庭", "2-4人")):
            score += 7
        if any(term in scenario for term in ("容量", "正餐")) and any(term in haystack for term in ("3L", "3700ML", "大容量", "多人做饭")):
            score += 6
        if any(term in scenario for term in ("收纳", "别太差")) and any(term in haystack for term in ("收纳", "套娃", "便携")):
            score += 4
        if any(term in scenario for term in ("烧水", "简单餐食")) and any(term in haystack for term in ("水壶", "烧水", "做饭", "套锅")):
            score += 4
        if "轻量" in haystack or "便携" in haystack:
            score += 2
        if any(term in haystack for term in ("锅", "炉", "水壶", "套装")):
            score += 1
        if any(term in scenario for term in ("套锅", "套装", "锅")) and any(term in haystack for term in ("调料瓶", "三角架", "配件", "餐具")):
            score -= 12
        if any(term in haystack for term in ("烤盘", "煎盘", "griddle")):
            score -= 3
        if score > 0:
            scored.append((score, row))
    scored.sort(key=lambda item: (-item[0], str(item[1].get("sku") or "")))
    selected = [row for _, row in scored[:5]]
    if not selected:
        return None
    skus = [str(row.get("sku") or "").strip().upper() for row in selected if row.get("sku")]
    top_row = selected[0]
    top_name = top_row.get("product_name_cn") or top_row.get("sku")
    top_sku = top_row.get("sku")
    top_reason = top_row.get("usage_scenarios") or top_row.get("features") or top_row.get("positioning") or "匹配当前使用场景"
    answer = f"优先推荐 {top_name}（{top_sku}），因为它更贴合你当前场景，{top_reason}。"
    backups: list[str] = []
    for row in selected[1:3]:
        name = row.get("product_name_cn") or row.get("sku")
        sku = row.get("sku")
        reason = row.get("usage_scenarios") or row.get("features") or row.get("positioning") or "匹配当前使用场景"
        backups.append(f"{name}（{sku}），{reason}")
    if backups:
        answer += " 备选可以看" + "；".join(backups) + "。"
    scenario_label = "、".join(_phase1_scenario_labels(scenario)) or "当前"
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": f"按你“{scenario_label}”的需求，{answer}",
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
        "answer_metadata": {"source": "product_catalog_structured_recommendation"},
        "skip_polish": True,
    }


def _phase1_should_use_structured_recommendation(plan: dict) -> bool:
    scenario = str((plan or {}).get("scenario") or (plan or {}).get("raw_question") or "")
    if not scenario:
        return False
    if any(
        checker(scenario)
        for checker in (
            _phase1_is_light_budget_cookware_set_scenario,
            _phase1_is_griddle_vs_cookware_scenario,
            _phase1_is_stove_pairing_scenario,
            _phase1_is_stove_griddle_combo_scenario,
        )
    ):
        return True
    if any(term in scenario for term in ("想买套锅", "想买锅", "买套锅", "买锅", "买哪个", "应该买", "该买", "推荐", "怎么选", "希望", "想选")):
        return True
    return any(term in scenario for term in ("露营", "野餐", "徒步", "公园", "自驾", "家庭", "烧烤", "早餐")) and any(
        term in scenario for term in ("锅", "锅具", "套锅", "单锅", "炊具", "炉具", "炉子", "烤盘", "正餐", "做正餐", "烧水", "做简单餐食", "煎东西")
    )


def _phase1_force_structured_recommendation_rebuild(question: str) -> bool:
    scenario = str(question or "").strip()
    if not scenario:
        return False
    return any(
        checker(scenario)
        for checker in (
            _phase1_is_light_budget_cookware_set_scenario,
            _phase1_is_griddle_vs_cookware_scenario,
            _phase1_is_stove_pairing_scenario,
            _phase1_is_stove_griddle_combo_scenario,
        )
    )


def _phase1_scenario_labels(scenario: str) -> list[str]:
    labels = []
    for term in ("家庭", "周末", "三口之家", "两个人", "三个人", "一个人", "多人", "野餐", "公园", "露营", "徒步", "自驾", "火锅", "烧烤", "早餐", "轻量", "轻便", "容量优先", "收纳", "套锅", "锅具", "炉具", "烤盘"):
        if term in scenario and term not in labels:
            labels.append(term)
    return labels


def _phase1_repair_recommendation_result(db: Session, agent_result: dict, plan: dict) -> dict:
    if not isinstance(agent_result, dict) or not _phase1_should_use_structured_recommendation(plan):
        return agent_result
    scenario = str((plan or {}).get("scenario") or (plan or {}).get("raw_question") or "")
    scope_text = " ".join(
        part for part in (
            str((plan or {}).get("scenario") or "").strip(),
            str((plan or {}).get("raw_question") or "").strip(),
        )
        if part
    )
    if (
        any(term in scope_text for term in ("酒精炉", "液体酒精", "固体酒精", "alcohol stove"))
        and any(term in scope_text for term in ("锅", "锅具", "套锅", "单锅", "炊具"))
        and not any(term in scope_text for term in ("水壶", "茶壶", "烧水壶", "壶类"))
    ):
        return agent_result
    force_rebuild = _phase1_force_structured_recommendation_rebuild(scenario)
    if (
        str(agent_result.get("answer_type") or "").strip() == "recommendation"
        and _phase1_answer_mentions_product(agent_result)
        and _phase1_result_skus(agent_result)
        and not force_rebuild
    ):
        return agent_result
    if (plan or {}).get("primary_intent") not in {"recommendation", "product_query"} and str(agent_result.get("answer_type") or "").strip() not in {"query_products", "product_query", "recommendation"}:
        return agent_result
    rebuilt = _phase1_structured_recommendation_result(db, plan)
    return rebuilt or agent_result


def _phase1_is_stove_griddle_combo_scenario(question: str) -> bool:
    value = str(question or "").strip()
    if not value:
        return False
    # A stove named as the compatible heat source modifies the cookware
    # object; it does not request one product from each category.
    if (
        customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(value)
        or re.search(r"适合.{0,10}炉的(?:双人|单人|\d+人)?(?:锅|锅具|炊具|烤盘|煎盘)", value)
    ):
        return False
    if customer_agent_intent_service._is_barbecue_stove_griddle_dual_scope_question(value):
        return True
    has_stove_scope = any(term in value for term in ("炉具", "炉子"))
    has_griddle_scope = any(term in value for term in ("烤盘", "煎盘", "煎东西"))
    has_pairing_or_selection = any(
        term in value
        for term in ("怎么搭", "怎么配", "搭配", "更合适", "更稳", "稳妥", "先买哪类", "先买哪个")
    )
    return has_stove_scope and has_griddle_scope and has_pairing_or_selection


def _phase1_is_light_budget_cookware_set_scenario(question: str) -> bool:
    value = str(question or "").strip()
    if not value:
        return False
    has_two_person = any(term in value for term in ("双人", "两人", "两个人", "2人", "情侣"))
    has_camping = any(term in value for term in ("露营", "野营", "户外"))
    has_lightweight = any(term in value for term in ("轻", "轻量", "轻便", "不想太重", "别太重"))
    has_budget = any(term in value for term in ("不想买太贵", "不想太贵", "别太贵", "不太贵", "预算", "入门", "性价比"))
    has_set_selection = any(term in value for term in ("哪套", "推荐哪套", "买哪套", "选哪套", "套锅", "套装"))
    has_cookware_scope = any(term in value for term in ("锅具", "炊具", "套锅", "锅", "买套", "一套"))
    has_cooking_usage = any(term in value for term in ("做饭", "煮饭", "煮面", "正餐", "烹饪"))
    has_purchase_or_selection = any(
        term in value for term in ("想买", "买", "推荐", "怎么选", "该买", "选", "用什么", "推荐几款", "推荐几个", "有什么推荐")
    )
    if not (has_two_person and has_camping):
        return False
    if has_cookware_scope and has_purchase_or_selection:
        return True
    if has_budget and (has_set_selection or has_cookware_scope):
        return True
    return has_lightweight and has_cookware_scope and has_purchase_or_selection


def _phase1_is_water_kettle_selection_scenario(question: str) -> bool:
    value = str(question or "").strip()
    if not value:
        return False
    if _phase1_is_stove_pairing_scenario(value) or _phase1_is_stove_griddle_combo_scenario(value):
        return False
    has_water_scope = any(term in value for term in ("水壶", "烧水壶", "茶壶", "水具", "壶"))
    if not has_water_scope:
        return False
    has_selection = any(term in value for term in ("怎么选", "推荐", "买", "想买", "选", "哪个", "哪款", "更稳"))
    has_water_usage = any(term in value for term in ("喝热水", "热水", "烧水", "煮水", "泡茶", "热饮", "饮水", "补水"))
    has_lightweight = any(term in value for term in ("轻", "轻量", "轻便", "好带", "便携"))
    has_outdoor = any(term in value for term in ("露营", "野营", "户外", "野餐"))
    if has_selection and (has_water_usage or (has_lightweight and has_outdoor)):
        return True
    return has_lightweight and has_outdoor and any(term in value for term in ("水具", "水壶", "壶"))


def _phase1_is_griddle_vs_cookware_scenario(question: str) -> bool:
    value = str(question or "").strip()
    if not value:
        return False
    has_griddle_scope = any(term in value for term in ("烤盘", "煎盘"))
    has_cookware_scope = any(term in value for term in ("锅具", "锅", "套锅", "单锅"))
    has_compare_or_selection = any(
        term in value
        for term in ("还是", "哪个更合适", "更合适", "推荐", "更实用", "更好", "优先选", "更适用", "哪个更值得", "更值得", "先买")
    )
    has_grill_breakfast_signal = any(term in value for term in ("煎烤", "煎东西", "煎烤食物", "早餐"))
    return (
        has_griddle_scope
        and has_cookware_scope
        and has_compare_or_selection
        and has_grill_breakfast_signal
    )


def _phase1_is_water_kettle_candidate(row: dict[str, Any]) -> bool:
    if not isinstance(row, dict):
        return False
    category = str(row.get("category") or "").strip()
    text = " ".join(
        str(row.get(key) or "")
        for key in (
            "sku",
            "product_name_cn",
            "product_name_en",
            "category",
            "sub_category",
            "capacity",
            "features",
            "usage_scenarios",
            "positioning",
            "long_description_cn",
        )
    )
    if any(term in text for term in ("调料瓶", "配件", "三角架", "收纳袋", "收纳包", "点火器", "炉具")):
        return False
    if category in {"水具", "水壶"}:
        return True
    return any(term in text for term in ("水壶", "烧水壶", "保温壶", "冷水壶", "便携水具", "户外杯", "保温杯"))


def _phase1_is_strict_water_kettle_candidate(row: dict[str, Any]) -> bool:
    return customer_structured_query_contract.resolve_structured_subject_scope(
        row=row,
        subject_category="水壶",
    )["matched"]


def _phase1_structured_water_kettle_result(scenario: str, rows: list[dict]) -> dict | None:
    water_rows = [row for row in rows if _phase1_is_water_kettle_candidate(row)]
    if not water_rows:
        return None

    def _rank(row: dict) -> tuple[int, str]:
        category = str(row.get("category") or "").strip()
        text = " ".join(
            str(row.get(key) or "")
            for key in (
                "product_name_cn",
                "category",
                "capacity",
                "features",
                "usage_scenarios",
                "positioning",
                "price_positioning",
                "long_description_cn",
            )
        )
        score = 0
        if category == "水壶":
            score += 9
        elif category == "水具":
            score += 7
        if any(term in text for term in ("烧水", "煮水", "热水", "泡茶", "热饮", "煮茶")):
            score += 6
        if any(term in text for term in ("轻量", "轻便", "便携", "随行", "补水")):
            score += 4
        if any(term in scenario for term in ("双人", "两人", "两个人", "情侣")) and any(term in text for term in ("双人", "两人", "两个人", "情侣")):
            score += 3
        if any(term in scenario for term in ("泡茶", "热饮")) and any(term in text for term in ("泡茶", "煮茶", "热饮")):
            score += 3
        try:
            weight_value = float(row.get("gross_weight_g") or 0)
        except (TypeError, ValueError):
            weight_value = 0
        if 0 < weight_value <= 250:
            score += 3
        elif 250 < weight_value <= 400:
            score += 2
        return (-score, str(row.get("sku") or ""))

    selected = sorted(water_rows, key=_rank)[:5]
    if not selected:
        return None
    top_row = selected[0]
    top_name = top_row.get("product_name_cn") or top_row.get("sku")
    top_sku = str(top_row.get("sku") or "").strip().upper()
    top_reason = str(top_row.get("usage_scenarios") or top_row.get("features") or top_row.get("positioning") or "更适合露营热饮和烧水场景").strip("。；; ")
    answer = f"这类需求我会优先从水壶/水具域里选，不把调料瓶或普通配件放到主推前排。主推 {top_name}（{top_sku}），更贴合喝热水、烧水或泡茶这类场景。"
    if top_reason:
        answer += f" 主要理由是：{top_reason}。"
    backups: list[str] = []
    for row in selected[1:3]:
        name = row.get("product_name_cn") or row.get("sku")
        sku = str(row.get("sku") or "").strip().upper()
        reason = str(row.get("usage_scenarios") or row.get("features") or row.get("positioning") or "可作为同域备选").strip("。；; ")
        backups.append(f"{name}（{sku}），{reason}")
    if backups:
        answer += " 备选可以看" + "；".join(backups) + "。"
    answer += " 如果你更偏向频繁烧水或泡茶，优先看壶型和热饮场景更明确的款；如果更偏向日常补水，再看轻便杯具。"
    skus = [str(row.get("sku") or "").strip().upper() for row in selected if row.get("sku")]
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
        "answer_metadata": {"source": "product_catalog_structured_recommendation"},
        "skip_polish": True,
    }


def _phase1_is_stove_pairing_scenario(question: str) -> bool:
    value = str(question or "").strip()
    if not value:
        return False
    if _phase1_is_stove_griddle_combo_scenario(value):
        return False
    has_stove_scope = any(term in value for term in ("炉具", "炉子"))
    has_pairing_or_selection = any(
        term in value
        for term in ("怎么搭", "怎么配", "怎么选", "该怎么选", "更稳", "稳一点", "兼顾", "先买", "先补", "先选", "先选什么", "先选哪个", "还是水壶")
    )
    has_bbq_signal = any(term in value for term in ("烧烤", "烤肉", "烤东西"))
    has_water_signal = any(term in value for term in ("烧水", "煮水", "热饮", "加热饮", "泡茶", "热水"))
    has_stove_scene_signal = any(term in value for term in ("营地聚餐", "风大", "风比较大", "大风", "防风")) or (has_bbq_signal and has_water_signal)
    return has_stove_scope and has_pairing_or_selection and has_stove_scene_signal


def _phase1_structured_light_budget_cookware_set_result(scenario: str, rows: list[dict]) -> dict | None:
    cookware_rows = [row for row in rows if _is_service_pot_or_cookware_set_candidate(row)]
    if not cookware_rows:
        return None
    scenario_text = str(scenario or "")
    has_lightweight = any(term in scenario_text for term in ("轻", "轻量", "轻便", "不想太重", "别太重"))
    has_budget = any(term in scenario_text for term in ("不想买太贵", "不想太贵", "别太贵", "不太贵", "预算", "入门", "性价比"))
    has_set_selection = any(term in scenario_text for term in ("哪套", "推荐哪套", "买哪套", "选哪套", "套锅", "套装"))
    has_cooking_usage = any(term in scenario_text for term in ("做饭", "煮饭", "煮面", "正餐", "烹饪"))

    def _rank(row: dict) -> tuple[int, str]:
        text = " ".join(
            str(row.get(key) or "")
            for key in (
                "product_name_cn",
                "category",
                "capacity",
                "features",
                "usage_scenarios",
                "positioning",
                "price_positioning",
                "long_description_cn",
            )
        )
        score = 0
        if any(term in text for term in ("套锅", "套装", "锅5件套", "锅7件套", "锅10件套", "野餐锅")):
            score += 10
        if any(term in text for term in ("双人", "两人", "两个人", "2人", "2-3人")):
            score += 8
        if any(term in text for term in ("轻量", "轻便", "便携", "收纳", "套娃")):
            score += 6
        if has_cooking_usage and any(term in text for term in ("做饭", "煮饭", "煮面", "正餐", "烹饪")):
            score += 4
        if any(term in text for term in ("入门", "性价比", "基础", "中端", "周末野餐")):
            score += 4
        if any(term in text for term in ("高端", "礼盒")):
            score -= 3
        weight = row.get("gross_weight_g")
        try:
            weight_value = float(weight or 0)
        except (TypeError, ValueError):
            weight_value = 0
        if 0 < weight_value <= 800:
            score += 5
        elif 800 < weight_value <= 1200:
            score += 3
        elif weight_value >= 1800:
            score -= 3
        return (-score, str(row.get("sku") or ""))

    selected = sorted(cookware_rows, key=_rank)[:5]
    if not selected:
        return None
    top_row = selected[0]
    top_name = top_row.get("product_name_cn") or top_row.get("sku")
    top_sku = str(top_row.get("sku") or "").strip().upper()
    top_reason = str(top_row.get("usage_scenarios") or top_row.get("features") or top_row.get("positioning") or "").strip("。；; ")
    demand_bits = ["双人露营"]
    if has_lightweight:
        demand_bits.append("轻便好带")
    if has_budget:
        demand_bits.append("入门取舍")
    if has_set_selection:
        demand_bits.append("套锅/套装")
    if has_cooking_usage:
        demand_bits.append("做饭用锅")
    demand_label = "、".join(dict.fromkeys(demand_bits))
    answer = (
        f"这类需求我会优先从双人露营的锅具/套锅里选，不先把水壶当主推。"
        f" 优先推荐 {top_name}（{top_sku}），更贴合“{demand_label}”这类需求。"
    )
    if top_reason:
        answer += f" 主要理由是：{top_reason}。"
    backups: list[str] = []
    for row in selected[1:3]:
        name = row.get("product_name_cn") or row.get("sku")
        sku = str(row.get("sku") or "").strip().upper()
        reason = str(row.get("usage_scenarios") or row.get("features") or row.get("positioning") or "更偏向双人露营的套装取舍").strip("。；; ")
        backups.append(f"{name}（{sku}），{reason}")
    if backups:
        answer += " 备选可以看" + "；".join(backups) + "。"
    if has_lightweight:
        answer += " 如果你更在意极致轻量，就优先看更轻便的小套锅；"
    if has_cooking_usage or not has_lightweight:
        answer += " 如果你更在意两个人做饭的完整度，再看容量和组件更全的套装。"
    else:
        answer += " 如果你更在意两个人做饭的完整度，再看容量和组件更全的套装。"
    skus = [str(row.get("sku") or "").strip().upper() for row in selected if row.get("sku")]
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
        "answer_metadata": {
            "source": "product_catalog_structured_recommendation",
            "preserved_scope_statement": "这类需求我会优先从双人露营的锅具/套锅里选，不先把水壶当主推。",
        },
        "skip_polish": True,
    }


def _phase1_structured_griddle_vs_cookware_result(scenario: str, rows: list[dict]) -> dict | None:
    griddle_rows = [row for row in rows if customer_agent_intent_service._is_barbecue_griddle_candidate_row(row)]
    cookware_rows = [row for row in rows if _is_service_pot_or_cookware_set_candidate(row)]
    if not griddle_rows or not cookware_rows:
        return None

    def _rank_griddle(row: dict) -> tuple[int, str]:
        text = " ".join(str(row.get(key) or "") for key in ("product_name_cn", "features", "usage_scenarios", "positioning", "category"))
        score = 0
        if any(term in text for term in ("烤盘", "煎盘", "煎烤", "早餐")):
            score += 8
        if "导热盘" in text:
            score -= 8
        return (-score, str(row.get("sku") or ""))

    def _rank_cookware(row: dict) -> tuple[int, str]:
        text = " ".join(str(row.get(key) or "") for key in ("product_name_cn", "features", "usage_scenarios", "positioning", "category"))
        score = 0
        if any(term in text for term in ("锅", "套锅", "烧水", "正餐", "通用", "多场景")):
            score += 6
        if any(term in text for term in ("轻量", "便携", "收纳")):
            score += 2
        return (-score, str(row.get("sku") or ""))

    best_griddle = sorted(griddle_rows, key=_rank_griddle)[0]
    best_cookware = sorted(cookware_rows, key=_rank_cookware)[0]
    selected = [best_griddle]
    if str(best_cookware.get("sku") or "").strip().upper() != str(best_griddle.get("sku") or "").strip().upper():
        selected.append(best_cookware)

    griddle_name = best_griddle.get("product_name_cn") or best_griddle.get("sku")
    griddle_sku = best_griddle.get("sku")
    cookware_name = best_cookware.get("product_name_cn") or best_cookware.get("sku")
    cookware_sku = best_cookware.get("sku")
    answer = (
        f"如果你的重点是早餐煎烤，我会优先推荐烤盘 {griddle_name}（{griddle_sku}），配合炉具使用会更贴合煎、烤这类用法。"
        f"如果你还想兼顾烧水、煮面或正餐，备选可以看锅具 {cookware_name}（{cookware_sku}），会更通用。"
        "简单说：偏煎烤先烤盘，想一套多用再补锅具。"
    )
    skus = [str(row.get('sku') or '').strip().upper() for row in selected if row.get('sku')]
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": answer,
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
        "answer_metadata": {"source": "product_catalog_structured_recommendation"},
        "skip_polish": True,
    }


def _phase1_structured_stove_pairing_result(scenario: str, rows: list[dict]) -> dict | None:
    stove_rows = [
        row
        for row in rows
        if str(row.get("category") or "").strip() == "炉具"
        and customer_agent_intent_service._is_barbecue_stove_candidate_row(row)
    ]
    if not stove_rows:
        return None

    def _rank_stove(row: dict) -> tuple[int, str]:
        text = " ".join(
            str(row.get(key) or "")
            for key in ("product_name_cn", "features", "usage_scenarios", "positioning", "category")
        )
        score = 0
        if any(term in text for term in ("炉具", "炉子", "桌面炉", "卡式炉", "烧烤炉")):
            score += 8
        if any(term in text for term in ("稳定", "火力", "稳", "防风")):
            score += 6
        if any(term in text for term in ("烧烤", "烤肉", "桌面聚餐", "露营烧烤", "营地聚餐")):
            score += 5
        if any(term in text for term in ("烧水", "煮水", "热饮", "煮茶", "泡茶", "加热饮", "热水")):
            score += 3
        return (-score, str(row.get("sku") or ""))

    stove_rows = sorted(stove_rows, key=_rank_stove)
    selected = stove_rows[:4]
    if not selected:
        return None

    top_row = selected[0]
    top_name = top_row.get("product_name_cn") or top_row.get("sku")
    top_sku = top_row.get("sku")
    top_reason = top_row.get("features") or top_row.get("usage_scenarios") or "更适合烧烤主加热场景"

    answer_parts = [
        f"这类需求我会优先按炉具域来配，先用 {top_name}（{top_sku}）承担烧烤和主加热，{top_reason}。"
    ]
    if any(term in scenario for term in ("烧水", "煮水", "热饮", "加热饮", "泡茶", "热水")):
        answer_parts.append("如果还要兼顾烧水或热饮，建议在同一套炉具体系下搭配壶或锅处理，不要先把水壶本身当主设备。")
    if any(term in scenario for term in ("风大", "海边", "防风")):
        answer_parts.append("风大的场景优先看防风能力、支撑稳定性和低重心结构，先把炉具本体选稳，再考虑其他搭配。")
    if len(selected) > 1:
        backup = selected[1]
        backup_name = backup.get("product_name_cn") or backup.get("sku")
        backup_sku = backup.get("sku")
        backup_reason = backup.get("features") or backup.get("usage_scenarios") or "更适合作为同气源备选"
        answer_parts.append(
            f"备选炉具可以看 {backup_name}（{backup_sku}），{backup_reason}。"
        )
    answer_parts.append("搭配上优先考虑同一气源、稳定支撑和可控火力，这样烧烤和热饮切换会更稳，不建议先把调料瓶、水壶或普通配件放到主推前排。")

    skus = [str(row.get("sku") or "").strip().upper() for row in selected if row.get("sku")]
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": " ".join(answer_parts),
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
        "answer_metadata": {"source": "product_catalog_structured_recommendation"},
        "skip_polish": True,
    }


def _phase1_structured_stove_griddle_result(scenario: str, rows: list[dict]) -> dict | None:
    stove_rows = [
        row
        for row in rows
        if str(row.get("category") or "").strip() == "炉具"
        and customer_agent_intent_service._is_barbecue_stove_candidate_row(row)
    ]
    griddle_rows = [row for row in rows if customer_agent_intent_service._is_barbecue_griddle_candidate_row(row)]
    if not stove_rows and not griddle_rows:
        return None

    def _rank_stove(row: dict) -> tuple[int, str]:
        text = " ".join(str(row.get(key) or "") for key in ("product_name_cn", "features", "usage_scenarios", "positioning", "category"))
        score = 0
        if any(term in text for term in ("烧烤", "火力", "稳定", "桌面炉", "卡式炉", "炉具")):
            score += 6
        if any(term in text for term in ("桌面聚餐", "露营烧烤", "家庭露营")):
            score += 4
        return (-score, str(row.get("sku") or ""))

    def _rank_griddle(row: dict) -> tuple[int, str]:
        text = " ".join(str(row.get(key) or "") for key in ("product_name_cn", "features", "usage_scenarios", "positioning", "category"))
        score = 0
        if any(term in text for term in ("烤盘", "煎盘", "煎烤", "早餐")):
            score += 7
        if "导热盘" in text:
            score -= 10
        return (-score, str(row.get("sku") or ""))

    stove_rows = sorted(stove_rows, key=_rank_stove)
    griddle_rows = sorted(griddle_rows, key=_rank_griddle)
    selected: list[dict] = []
    if stove_rows:
        selected.append(stove_rows[0])
    if len(stove_rows) > 1:
        selected.append(stove_rows[1])
    if griddle_rows:
        selected.append(griddle_rows[0])
    for row in stove_rows[2:] + griddle_rows[1:]:
        sku = str(row.get("sku") or "").strip().upper()
        if sku and sku not in {str(item.get("sku") or "").strip().upper() for item in selected}:
            selected.append(row)
        if len(selected) >= 4:
            break
    if not selected:
        return None

    stove_row = stove_rows[0] if stove_rows else None
    griddle_row = griddle_rows[0] if griddle_rows else None
    parts: list[str] = []
    if stove_row:
        stove_name = stove_row.get("product_name_cn") or stove_row.get("sku")
        stove_sku = stove_row.get("sku")
        stove_reason = stove_row.get("features") or stove_row.get("usage_scenarios") or "更适合露营烧烤场景"
        parts.append(f"炉具方向：优先推荐 {stove_name}（{stove_sku}），{stove_reason}。")
    if len(stove_rows) > 1:
        backup_stove = stove_rows[1]
        backup_name = backup_stove.get("product_name_cn") or backup_stove.get("sku")
        backup_sku = backup_stove.get("sku")
        backup_reason = backup_stove.get("features") or backup_stove.get("usage_scenarios") or "可作为同场景备选"
        parts.append(f"备选炉具可以看 {backup_name}（{backup_sku}），{backup_reason}。")
    if griddle_row:
        griddle_name = griddle_row.get("product_name_cn") or griddle_row.get("sku")
        griddle_sku = griddle_row.get("sku")
        griddle_reason = griddle_row.get("features") or griddle_row.get("usage_scenarios") or "适合煎烤搭配"
        parts.append(f"烤盘方向：备选可以看 {griddle_name}（{griddle_sku}），{griddle_reason}。")
    if "先买哪类" in scenario or "先买哪个" in scenario:
        parts.insert(0, "如果你这次只能先补一类，我更推荐先把炉具补齐，再搭配烤盘。")
    elif "煎东西" in scenario or "早餐" in scenario:
        parts.insert(0, "如果你的重点是煎东西，烤盘会比普通锅具更对路；有稳定火力时再搭配炉具更完整。")
    else:
        parts.insert(0, "这类场景更适合按“炉具 + 烤盘”去搭，不建议先回到普通锅具或导热盘。")

    skus = [str(row.get("sku") or "").strip().upper() for row in selected if row.get("sku")]
    return {
        "intent": "recommendation",
        "answer_type": "recommendation",
        "answer": " ".join(parts),
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "debug": {"agent_mode": "planner_recommendation_guard_rebuild"},
        "answer_metadata": {"source": "product_catalog_structured_recommendation"},
        "skip_polish": True,
    }


def _phase1_is_alcohol_stove_cookware_question(
    question: str,
    candidate_context: dict[str, Any] | None = None,
    agent_result: dict | None = None,
) -> bool:
    text = str(question or "")
    if not any(term in text for term in ("酒精炉", "液体酒精", "固体酒精")) and "alcohol stove" not in text.lower():
        return False
    if SKU_RE.search(text) and any(term in text for term in ("能不能", "能否", "是否", "可不可以", "支持", "适用")):
        return False
    context = candidate_context if isinstance(candidate_context, dict) else {}
    if any(term in text for term in ("水壶", "茶壶", "烧水壶", "壶类")):
        return False
    if not customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(text):
        # A cookware scenario noun such as “火锅” does not change the
        # requested product.  Keep an explicit alcohol-stove request out of
        # the cookware-only evidence filter.
        return False
    if _is_service_pot_cookware_scope(
        str(context.get("product_scope") or ""),
        " ".join([text, str(context.get("user_question") or "")]),
    ):
        return True
    result_text = ""
    if isinstance(agent_result, dict):
        result_text = " ".join(
            str(value or "")
            for row in (agent_result.get("results") or [])
            if isinstance(row, dict)
            for value in (
                row.get("category"),
                row.get("product_name_cn"),
                row.get("product_name_en"),
            )
        )
    return _is_service_pot_cookware_scope(
        "",
        " ".join([text, result_text]),
    )


def _phase1_filter_alcohol_stove_cookware_result(
    db: Session,
    agent_result: dict | None,
    *,
    question: str,
    candidate_context: dict[str, Any] | None = None,
) -> dict | None:
    if not isinstance(agent_result, dict) or not _phase1_is_alcohol_stove_cookware_question(question, candidate_context, agent_result):
        return agent_result
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    intent_debug = debug.get("intent") if isinstance(debug.get("intent"), dict) else {}
    if (
        str(intent_debug.get("source_context") or "") == "previous_results"
        and not customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(question)
    ):
        # The candidate-domain executor has already evaluated the sealed rows
        # against same-SKU evidence.  A later global catalogue filter has a
        # narrower evidence view and must not erase that validated subset.
        return agent_result
    if str(debug.get("agent_mode") or "").startswith("recommendation_context_product_"):
        return agent_result
    is_list_query = bool(
        customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(question)
        and not customer_agent_service._extract_skus(question)
    )
    rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
    if not rows:
        result_skus = {
            str(sku or "").strip().upper()
            for sku in (agent_result.get("result_skus") or [])
            if str(sku or "").strip()
        }
        if result_skus:
            rows = [
                row for row in _phase1_catalog_rows(db, "产品")
                if str(row.get("sku") or "").strip().upper() in result_skus
            ]
    if not rows:
        return agent_result
    products = db.query(Product).filter(Product.sku.in_([
        str(row.get("sku") or "").strip().upper()
        for row in rows
        if str(row.get("sku") or "").strip()
    ])).all()
    product_by_sku = {str(product.sku or "").strip().upper(): product for product in products}
    cookware_filtered = [
        row for row in rows
        if _is_service_pot_or_cookware_set_candidate(row, product_by_sku.get(str(row.get("sku") or "").strip().upper()))
    ]
    filtered = [
        row for row in cookware_filtered
        if _phase1_row_has_explicit_alcohol_stove_support(db, row)
    ]
    if len(filtered) == len(rows):
        result = dict(agent_result)
        skus = [str(row.get("sku") or "").strip().upper() for row in filtered if str(row.get("sku") or "").strip()]
        if is_list_query and str(result.get("answer_type") or "").strip() == "product_detail":
            result["intent"] = "query_products"
            result["answer_type"] = "product_query"
        return _sync_alcohol_stove_cookware_scope_metadata(result, skus=skus, rows=filtered, question=question)
    skus = [str(row.get("sku") or "").strip().upper() for row in filtered if str(row.get("sku") or "").strip()]
    result = dict(agent_result)
    result["results"] = filtered
    if is_list_query and str(result.get("answer_type") or "").strip() == "product_detail":
        result["intent"] = "query_products"
        result["answer_type"] = "product_query"
    result = _sync_alcohol_stove_cookware_scope_metadata(result, skus=skus, rows=filtered, question=question)
    lines = [
        "筛选条件：类目包含 锅具；必须有明确酒精炉支持证据。",
        "当前资料里只确认以下 SKU 明确支持酒精炉；其他锅具虽然可能支持明火、卡式炉、分体炉或一体炉，但没有明确酒精炉证据，我不建议直接归为酒精炉适用。",
    ]
    for row in filtered[:10]:
        name = row.get("product_name_cn") or row.get("name") or row.get("sku")
        sku = row.get("sku")
        heat_source = row.get("heat_source") or ((row.get("field_values") or {}).get("热源") if isinstance(row.get("field_values"), dict) else "")
        lines.append(f"- {name}（{sku}），明确证据：{heat_source or '同SKU资料/问答明确显示支持酒精炉'}")
    result["answer"] = "\n".join(lines)
    result["skip_polish"] = True
    return result


def _compound_display_value(value: Any) -> str:
    text = str(value or "").strip()
    if not text:
        return ""
    if text.startswith("[") and text.endswith("]"):
        try:
            return _display_value(json.loads(text))
        except (TypeError, ValueError, json.JSONDecodeError):
            items = [item.strip().strip("'\"") for item in text[1:-1].split(",")]
            return "，".join(item for item in items if item)
    # Imported catalogue text occasionally stores line breaks as the two
    # literal characters ``\n``. They are separators, not part of the product
    # value a customer should see.
    text = text.replace("\\n", "、")
    return re.sub(r"[、；;]{2,}", "、", text).strip("、；; ")


def _record_semantic_compound_child(
    *,
    query: str,
    candidate: dict[str, Any] | None,
    compound_answers: list[dict[str, Any]],
    answered_requests: list[tuple[str, str]],
) -> bool:
    """Record one independently verified semantic child and its provenance.

    Related child requests may legitimately share one same-SKU evidence item.
    Deduplicating on a coarse provider label turned that valid support into a
    false missing-data tail. Each candidate has already passed its own
    semantic selection and grounding gates, so this layer records coverage
    without re-deciding relevance.
    """
    if not isinstance(candidate, dict):
        return False
    metadata = (
        candidate.get("answer_metadata")
        if isinstance(candidate.get("answer_metadata"), dict)
        else {}
    )
    evidence_ids = [
        str(item or "").strip()
        for item in (metadata.get("evidence_ids") or [])
        if str(item or "").strip()
    ]
    receipt = "|".join(dict.fromkeys(evidence_ids))
    if not receipt:
        receipt = str(
            metadata.get("evidence_source")
            or metadata.get("source")
            or "same_sku_child_evidence"
        ).strip()
    compound_answers.append(candidate)
    answered_requests.append((str(query or "").strip(), receipt))
    return True


async def _try_semantic_compound_child_answer(
    db: Session,
    child_question: str,
    child_plan: dict[str, Any],
) -> dict[str, Any] | None:
    """Resolve one LLM-issued child against the same sealed entity/evidence.

    A transient conservative reranker or grounding vote must not turn valid
    same-SKU file evidence into a false missing answer. Retry the identical
    sealed RAG request once; no wording, candidate, SKU, or contract is
    changed between attempts.
    """
    candidate = await _try_product_qa_shortcut_with_semantic_selection(
        db,
        child_question,
        phase1_plan=child_plan,
    )
    if _is_sealed_product_qa_safe_missing(candidate):
        candidate = None
    if candidate:
        return candidate
    for _rag_attempt in range(2):
        candidate = await _try_sealed_same_sku_knowledge_answer(
            db,
            child_question,
            child_plan,
        )
        if candidate:
            return candidate
    return None


async def _execute_composite_customer_question(
    db: Session,
    *,
    user_id: str,
    composite_question: dict[str, str],
    sku: str | None,
) -> dict | None:
    """Execute independently resolvable fact and recommendation tasks.

    The separator is semantic structure, not a product or sentence exception:
    each part receives its own identity/evidence route before their readable
    answers are merged.  This prevents the fact part's entity contract from
    treating the recommendation clause as part of its product subject.
    """
    fact_part = str(composite_question.get("fact_part") or "").strip()
    recommendation_part = _normalize_composite_recommendation_part(
        composite_question.get("recommendation_part") or ""
    )
    if not fact_part or not recommendation_part:
        return None
    options = {"allow_search_top1_promotion": True}
    # A compound fact still needs the same formal field/entity/evidence chain
    # as a standalone product question.  Do not hand it to the legacy intent
    # executor merely because a recommendation follows in the same utterance.
    fact_plan = customer_agent_planner_service.plan_customer_question(fact_part)
    fact_field_request = resolve_requested_field_contract(fact_part, fact_plan)
    fact_field = str(fact_field_request.get("field_type") or "").strip()
    fact_sku = _resolve_sku(db, fact_part, None)
    fact_label = customer_field_contract.product_detail_field_label(fact_field) if fact_field else ""
    explicit_multi_product_comparison = bool(
        len(customer_agent_service._extract_skus(fact_part)) >= 2
        and customer_agent_planner_service._is_compare_question(fact_part)
    )
    # A comparison is a multi-entity contract.  Even where it mentions a
    # field, its first SKU must not be treated as the whole fact clause; let
    # the comparison executor bind every named product before merging the
    # independently requested recommendation.
    fact_result = (
        _product_field_followup_result(
            db,
            fact_sku,
            fact_label,
            fact_part,
            # ``fact_sku`` was resolved from an explicit SKU token above.
            # Reuse the existing exact-SKU provenance rather than introducing
            # a parallel composite-only trust class.
            identity_source="explicit_sku_exact",
            field_request_override=fact_field_request,
        )
        if (
            not explicit_multi_product_comparison
            and fact_sku
            and fact_label
            and customer_field_contract.is_supported_detail_field(fact_field)
        )
        else None
    )
    if fact_result is None:
        fact_result = await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=fact_part,
            sku=sku,
            previous_result_skus=[],
            allow_llm_fallback=False,
            **options,
        )
    fact_previous_result_skus = list(dict.fromkeys(
        str(item.get("sku") or "").strip().upper()
        for item in ((fact_result or {}).get("results") or [])
        if isinstance(item, dict) and str(item.get("sku") or "").strip()
    ) or [
        str(item or "").strip().upper()
        for item in ((fact_result or {}).get("result_skus") or [])
        if str(item or "").strip()
    ])
    recommendation_result = await customer_agent_intent_service.process_intent_request(
        db,
        user_id=user_id,
        question=recommendation_part,
        sku=None,
        previous_result_skus=fact_previous_result_skus,
        allow_llm_fallback=False,
        scoped_comparison_candidates=bool(
            len(fact_previous_result_skus) >= 2
            and any(term in fact_part for term in ("比较", "对比"))
            and "更适合" in recommendation_part
            and re.search(r"(?:[1-9]\d?|[一二两三四五六七八九十])\s*(?:个)?人", recommendation_part)
        ),
        **options,
    )
    if not fact_result and not recommendation_result:
        return None
    merged_result = dict(recommendation_result or fact_result or {})
    if fact_result and recommendation_result:
        if composite_question.get("order") == "recommendation_first":
            merged_result["answer"] = _merge_composite_answers(recommendation_result.get("answer", ""), fact_result.get("answer", ""))
        else:
            merged_result["answer"] = _merge_composite_answers(fact_result.get("answer", ""), recommendation_result.get("answer", ""))
        merged_result["intent"] = "recommendation"
        merged_result["answer_type"] = recommendation_result.get("answer_type") or fact_result.get("answer_type")
        merged_result["results"] = recommendation_result.get("results") or fact_result.get("results") or []
        merged_result["result_skus"] = recommendation_result.get("result_skus") or fact_result.get("result_skus") or []
    merged_result.setdefault("debug", {})
    merged_result["debug"]["composite_question"] = {
        "fact_part": fact_part,
        "recommendation_part": recommendation_part,
        "order": composite_question.get("order") or "fact_first",
        "strategy": "fact_contract_plus_recommendation",
    }
    return merged_result


def _heat_source_field_evidence(value: Any) -> str:
    """Normalize a structured heat-source cell into one readable list."""
    display = _compound_display_value(value)
    if not display:
        return ""
    items = [
        item.strip()
        for item in re.split(r"[、,，;；\r\n]+", display)
        if item.strip()
    ]
    return "、".join(dict.fromkeys(items))


def _selling_point_heat_source_evidence(value: Any) -> str:
    """Use a selling-point fallback only when it explicitly names a heat source."""
    text = _compound_display_value(value).strip()
    if not text:
        return ""
    markers = (
        "电陶炉", "电磁炉", "卡式炉", "酒精炉", "柴火炉", "燃气炉",
        "明火", "直火", "炭火", "固体燃料", "气罐",
    )
    clauses = [
        clause.strip()
        for clause in re.split(r"[\r\n]+|(?<=[。！？!?；;])", text)
        if clause.strip() and any(marker in clause for marker in markers)
    ]
    return " ".join(dict.fromkeys(clauses)).strip()


_CAPACITY_VALUE_PATTERN = re.compile(
    r"(?<![\d.])\d+(?:\.\d+)?\s*(?:ml|毫升|l|升)(?![A-Za-z])",
    re.IGNORECASE,
)

_LABELED_COMPONENT_SIZE_PATTERN = re.compile(
    r"^\S.{0,20}(?:\s+|[：:])\s*\d+(?:\.\d+)?\s*寸$",
    re.IGNORECASE,
)


def _capacity_field_evidence(value: Any) -> str:
    """Return only values that are structurally valid capacity evidence.

    The imported catalogue uses a JSON list for ``specs.capacity`` but also
    contains explicit placeholders and a few values copied from unrelated
    columns.  Same-SKU provenance alone is therefore insufficient: a capacity
    provider must also prove capacity-unit semantics before it can be exposed.
    """
    if value in (None, ""):
        return ""
    parsed: Any = value
    if isinstance(value, str):
        text = value.strip()
        if not text or text in {"[]", "/", "null", "None"}:
            return ""
        if text.startswith(("[", "{")):
            try:
                parsed = json.loads(text)
            except (TypeError, ValueError, json.JSONDecodeError):
                parsed = text

    items = parsed if isinstance(parsed, list) else [parsed]
    accepted: list[str] = []
    for item in items:
        display = _display_value(item).strip()
        if not display or not (
            _CAPACITY_VALUE_PATTERN.search(display)
            or _LABELED_COMPONENT_SIZE_PATTERN.search(display)
        ):
            continue
        accepted.append(display)
    return "，".join(dict.fromkeys(accepted))


def _color_field_evidence(value: Any) -> str:
    """Keep only explicit color tokens from the structured color column."""
    display = _compound_display_value(value)
    if not display:
        return ""
    items = [item.strip() for item in re.split(r"[、,，;；\r\n]+", display) if item.strip()]
    accepted = [
        item
        for item in items
        if "色" in item or any(char in _COLOR_FAMILY_CHARACTERS for char in item)
    ]
    return "、".join(dict.fromkeys(accepted))


def _dishwasher_specific_evidence(value: Any) -> str:
    """Return only clauses that explicitly state dishwasher compatibility."""
    text = str(value or "").strip()
    if not text or text in {"[]", "/"} or "洗碗机" not in text:
        return ""
    clauses = [
        clause.strip()
        for clause in re.split(r"[\r\n]+|(?<=[。！？!?；;])", text)
        if clause.strip() and "洗碗机" in clause
    ]
    return " ".join(dict.fromkeys(clauses)).strip()


def _machine_wash_specific_evidence(value: Any) -> str:
    """Return only clauses that explicitly discuss laundry-machine washing."""
    text = str(value or "").strip()
    if not text or text in {"[]", "/"} or not any(term in text for term in ("洗衣机", "机洗")):
        return ""
    clauses = [
        clause.strip()
        for clause in re.split(r"[\r\n]+|(?<=[。！？!?；;])", text)
        if clause.strip() and any(term in clause for term in ("洗衣机", "机洗"))
    ]
    return " ".join(dict.fromkeys(clauses)).strip()


_USAGE_INSTRUCTION_FIELD_MARKERS: dict[str, tuple[str, ...]] = {
    "cleaning": (
        "清洁", "清洗", "冲洗", "洗净", "擦洗", "擦拭", "刷洗",
        "去污", "污渍", "油污", "洗涤", "湿布", "温水", "洗洁精",
    ),
    "care": (
        "保养", "养护", "存放", "收纳", "晾干", "烘干", "擦干",
        "干燥", "防锈", "防霉", "通风", "长期不用", "避免潮湿",
        "定期检查",
    ),
}


def _liquid_temperature_capability_evidence(value: Any) -> str:
    """Keep only clauses that explicitly support liquid-temperature capability.

    A generic instruction such as "may contact hot food" does not establish
    whether a product can contain hot water.  This is evidence validation after
    the semantic FieldContract has selected ``usage_instruction``; it does not
    classify the user's wording or infer a capability from a product category.
    """
    text = str(value or "").strip()
    if not text or text in {"[]", "/"}:
        return ""
    markers = (
        "热水", "冷水", "开水", "沸水", "饮水", "补水",
        "保温", "热饮", "冷饮", "装水", "盛水", "液体",
        "装温水", "盛温水", "饮用温水",
    )
    clauses = [
        re.sub(r"\s+", " ", clause).strip()
        for clause in re.split(r"(?<=[。！？!?；;])\s*", text)
        if clause and clause.strip()
    ]
    return " ".join(
        clause for clause in clauses if any(marker in clause for marker in markers)
    ).strip()


def _usage_instruction_field_evidence(
    value: Any,
    canonical_field: str | None,
    *,
    requested_subtype: str | None = None,
) -> str:
    """Select only usage-instruction clauses compatible with the requested field.

    ``specs.usage_instruction`` is a shared storage column, not proof that every
    row contains cleaning and care guidance.  The central FieldContract may use
    it as a provider, but the provider must fail closed when no clause carries
    the requested semantics.
    """
    text = str(value or "").strip()
    field_type = str(canonical_field or "").strip()
    if not text or text in {"[]", "/"}:
        return ""

    def clean_segment(segment: str) -> str:
        cleaned = re.sub(r"\s+", " ", str(segment or "")).strip()
        # Imported instruction cells occasionally contain a stray terminal
        # ASCII token glued to Chinese prose.  It is not actionable evidence
        # and should not be repeated to customers.
        cleaned = re.sub(r"(?<=[\u4e00-\u9fff])[A-Za-z]{3,}\s*$", "", cleaned).strip()
        return cleaned

    text = re.sub(
        r"(?<=[\u4e00-\u9fff])[A-Za-z]{3,}(?=\s*(?:$|\r?\n))",
        "",
        text,
        flags=re.MULTILINE,
    ).strip()
    if field_type == "usage_instruction":
        return clean_segment(text)
    markers = _USAGE_INSTRUCTION_FIELD_MARKERS.get(field_type)
    if not markers:
        return text

    def has_field_marker(segment: str, target_field: str, target_markers: tuple[str, ...]) -> bool:
        matched = [marker for marker in target_markers if marker in segment]
        if not matched:
            return False
        if (
            target_field == "care"
            and set(matched) == {"收纳"}
            and re.search(r"(?:从.{0,8}收纳(?:包|袋).{0,8}取出|取出.{0,8}收纳(?:包|袋))", segment)
        ):
            return False
        return True

    selected: list[str] = []
    # Work with complete headings and numbered items. Splitting on commas or
    # line breaks produced fragments such as "1.使用后清洁泥土，" and could
    # join unrelated continuation lines into an unusable answer.
    sections = [
        section.strip()
        for section in re.split(r"(?=[【\[].*?[】\]])", text)
        if section and section.strip()
    ]
    for section in sections:
        heading_match = re.match(r"^([【\[].*?[】\]])", section)
        heading = heading_match.group(1) if heading_match else ""
        body = section[heading_match.end():] if heading_match else section
        heading_matches = bool(heading and any(marker in heading for marker in markers))
        items = [
            clean_segment(item)
            for item in re.split(r"(?m)(?=^\s*\d+\s*[.、)])", body)
            if clean_segment(item)
        ]
        if not items:
            items = [clean_segment(body)] if clean_segment(body) else []
        for item in items:
            if requested_subtype == "first_use" and not any(
                marker in item for marker in ("首次", "第一次", "第一次用", "开箱初洗")
            ):
                continue
            own_matches = has_field_marker(item, field_type, markers)
            competing_matches = any(
                has_field_marker(item, other_field, other_markers)
                for other_field, other_markers in _USAGE_INSTRUCTION_FIELD_MARKERS.items()
                if other_field != field_type
            )
            if own_matches and competing_matches:
                # A single imported sentence can join distinct instructions,
                # for example "用清水冲洗，使用后擦干并通风存放".  Keep
                # complete punctuation-delimited clauses for the requested
                # field instead of returning the other field's guidance too.
                clauses = [
                    clean_segment(clause)
                    for clause in re.split(r"(?<=[，,。！？!?；;])\s*", item)
                    if clean_segment(clause)
                ]
                keep_following = False
                for clause in clauses:
                    clause_matches_own = has_field_marker(clause, field_type, markers)
                    clause_matches_other = any(
                        has_field_marker(clause, other_field, other_markers)
                        for other_field, other_markers in _USAGE_INSTRUCTION_FIELD_MARKERS.items()
                        if other_field != field_type
                    )
                    if clause_matches_own:
                        selected.append(clause)
                        keep_following = True
                    elif clause_matches_other:
                        keep_following = False
                    elif keep_following:
                        # Unmarked continuation clauses often complete the
                        # preceding numbered instruction (for example the
                        # second half of an alternative drying method).
                        selected.append(clause)
            elif own_matches or (heading_matches and not competing_matches):
                selected.append(item)
    normalized_selected: list[str] = []
    for item in dict.fromkeys(selected):
        # Once clauses from different source sections are filtered into one
        # field answer, their original per-section numbering is no longer a
        # valid sequence.  Remove only the leading list marker; keep all
        # evidence text and any numbers that are part of the instruction.
        normalized = re.sub(r"^\d+\s*[.、)]\s*", "", item).strip()
        if normalized:
            normalized_selected.append(normalized)
    return " ".join(normalized_selected).strip().rstrip("，,；;：:")


def _usage_instruction_matches_product(product: Product, value: Any) -> bool:
    """Fail closed when an instruction cell clearly describes another domain."""
    return bool(product_vector_index_service.customer_visible_usage_instruction(
        product_name_cn=product.product_name_cn,
        product_name_en=product.product_name_en,
        category=product.category,
        sub_category=product.sub_category,
        value=value,
    ))


def _capacity_evidence_conflict(product_name: Any, structured_value: Any) -> tuple[str, str] | None:
    """Detect explicit capacity-unit contradictions without guessing a winner."""
    pattern = re.compile(r"(?<![\d.])(\d+(?:\.\d+)?)\s*(ml|毫升|l|升)", re.IGNORECASE)

    def values(text: Any) -> list[tuple[float, str]]:
        found: list[tuple[float, str]] = []
        for match in pattern.finditer(str(text or "")):
            number = float(match.group(1))
            unit = match.group(2).lower()
            milliliters = number if unit in {"ml", "毫升"} else number * 1000
            found.append((round(milliliters, 3), match.group(0).strip()))
        return found

    name_values = values(product_name)
    structured_values = values(structured_value)
    if not name_values or not structured_values:
        return None
    if {item[0] for item in name_values} & {item[0] for item in structured_values}:
        return None
    return name_values[0][1], structured_values[0][1]


_COLOR_FAMILY_CHARACTERS = frozenset("黑白红橙黄绿蓝紫粉灰银金棕褐咖锖")


def _color_evidence_conflict(product_name: Any, structured_value: Any) -> tuple[str, str] | None:
    """Detect an explicit color in a product name that conflicts with specs.

    Product names frequently carry the sellable variant in parentheses or
    after a dash.  Only those explicit variant spans are compared, so ordinary
    brand/name characters are not interpreted as colors.
    """
    name_text = str(product_name or "").strip()
    value_text = str(structured_value or "").strip()
    if not name_text or not value_text:
        return None
    name_spans = re.findall(r"[（(]([^（）()]{1,12})[）)]", name_text)
    name_spans.extend(re.findall(r"[-—]([^\s（）()]{1,12})$", name_text))
    name_colors = {
        char
        for span in name_spans
        for char in span
        if char in _COLOR_FAMILY_CHARACTERS
    }
    structured_colors = {char for char in value_text if char in _COLOR_FAMILY_CHARACTERS}
    if not name_colors or not structured_colors or name_colors & structured_colors:
        return None
    explicit_name_span = next(
        (span for span in name_spans if any(char in _COLOR_FAMILY_CHARACTERS for char in span)),
        "",
    )
    return explicit_name_span, value_text


def _resolve_single_product_field_evidence(
    db: Session,
    *,
    resolved_sku: str,
    requested_field: str,
    question: str,
) -> dict[str, Any]:
    product, specs, business, content = _phase1_product_bundle_by_ref(db, resolved_sku)
    if not product:
        return {
            "requested_field": requested_field,
            "evidence_field": None,
            "evidence_value": "",
            "evidence_source": None,
            "evidence_scope": None,
            "evidence_sku": None,
            "missing": True,
        }

    material_component = customer_field_contract.material_component_from_detail_label(requested_field)
    if material_component == "body":
        raw_material = str(getattr(specs, "body_material", "") or "").strip() if specs else ""
        body_material, _handle_material = customer_agent_intent_service._split_primary_and_handle_material(raw_material)
        value = _compound_display_value(body_material)
        return {
            "requested_field": requested_field,
            "evidence_field": "material" if value else None,
            "evidence_value": value,
            "evidence_source": "specs.body_material" if value else None,
            "evidence_scope": "body" if value else None,
            "evidence_sku": resolved_sku if value else None,
            "missing": not bool(value),
        }
    if material_component == "handle":
        raw_material = str(getattr(specs, "body_material", "") or "").strip() if specs else ""
        _body_material, handle_material = customer_agent_intent_service._split_primary_and_handle_material(raw_material)
        value = _compound_display_value(handle_material)
        return {
            "requested_field": requested_field,
            "evidence_field": "material" if value else None,
            "evidence_value": value,
            "evidence_source": "specs.body_material" if value else None,
            "evidence_scope": "handle" if value else None,
            "evidence_sku": resolved_sku if value else None,
            "missing": not bool(value),
        }
    if material_component == "lid":
        # General body-material cells do not evidence a lid.  Only a same-SKU
        # QA/knowledge statement that explicitly names both the lid and its
        # material is admissible; otherwise keep the missing-field boundary.
        value = customer_agent_intent_service._best_same_sku_evidence_text(
            db,
            sku=resolved_sku,
            supporting=None,
            term_groups=[("锅盖", "盖子", "杯盖"), ("材质", "材料")],
        )
        return {
            "requested_field": requested_field,
            "evidence_field": "material" if value else None,
            "evidence_value": _compound_display_value(value),
            "evidence_source": "product_qa:component_material" if value else None,
            "evidence_scope": "lid" if value else None,
            "evidence_sku": resolved_sku if value else None,
            "missing": not bool(value),
        }

    if requested_field == "适用场景":
        value = _compound_display_value(getattr(business, "usage_scenarios", "") if business else "")
        return {
            "requested_field": requested_field,
            "evidence_field": "usage_scene" if value else None,
            "evidence_value": value,
            "evidence_source": "business.usage_scenarios" if value else None,
            "evidence_scope": "subject" if value else None,
            "evidence_sku": resolved_sku if value else None,
            "missing": not bool(value),
        }

    field_contract = customer_field_contract.detect_field_contract(requested_field)
    canonical_field = (
        customer_field_contract.field_type_from_detail_label(requested_field)
        or (field_contract.field_type if field_contract else None)
    )
    detail_label = customer_field_contract.product_detail_field_label(canonical_field) if canonical_field else None
    if not canonical_field or not customer_field_contract.is_supported_detail_field(canonical_field) or not detail_label:
        return {
            "requested_field": requested_field,
            "evidence_field": canonical_field,
            "evidence_value": "",
            "evidence_source": None,
            "evidence_scope": None,
            "evidence_sku": None,
            "missing": True,
        }

    result = _product_field_followup_result(
        db,
        resolved_sku,
        detail_label,
        question,
        identity_source="phase2_resolved_entity",
        field_request_override={"field_type": canonical_field, "requested_field": detail_label, "source": "compound_field"},
    )
    metadata = result.get("answer_metadata") if isinstance(result, dict) and isinstance(result.get("answer_metadata"), dict) else {}
    evidence_status = str(metadata.get("evidence_status") or "").strip()
    missing = evidence_status in {"", "missing"}
    value = str(metadata.get("evidence_value") or "").strip()
    if not missing and not value:
        answer = str(result.get("answer") or "")
        marker = f"的{detail_label}："
        value = answer.split(marker, 1)[1].rstrip("。") if marker in answer else answer
    return {
        "requested_field": requested_field,
        "evidence_field": metadata.get("evidence_field") or (canonical_field if not missing else None),
        "evidence_value": _compound_display_value(value),
        "evidence_source": metadata.get("evidence_source"),
        "evidence_scope": metadata.get("evidence_scope"),
        "evidence_sku": metadata.get("evidence_sku") or (resolved_sku if not missing else None),
        "missing": missing,
    }


def _build_compound_product_detail_result(
    db: Session,
    *,
    resolved_sku: str,
    requested_fields: list[str],
    question: str,
    entity_contract: dict[str, Any] | None = None,
    compatibility_plan: dict[str, Any] | None = None,
) -> dict | None:
    product, specs, business, content = _phase1_product_bundle_by_ref(db, resolved_sku)
    if not product:
        return None
    # Compatibility extraction may emit product-component nouns (for example
    # "煎盘") alongside an actual field.  A component noun is not a second
    # FieldContract and must not preempt the same-SKU evidence route.  Keep
    # only labels that map to an existing canonical field contract.
    ordered_fields = list(dict.fromkeys(
        label
        for label in (str(field or "").strip() for field in requested_fields)
        if label and (
            customer_field_contract.field_type_from_detail_label(label)
            or (customer_field_contract.detect_field_contract(label) is not None)
        )
    ))
    if len(ordered_fields) < 2:
        return None
    field_results = [
        _resolve_single_product_field_evidence(
            db,
            resolved_sku=resolved_sku,
            requested_field=field,
            question=question,
        )
        for field in ordered_fields
    ]
    name = str(product.product_name_cn or product.product_name_en or resolved_sku).strip()
    lines = [f"{name}（{resolved_sku}）："]
    for field_result in field_results:
        label = field_result["requested_field"]
        value = str(field_result.get("evidence_value") or "").strip()
        if value:
            lines.append(f"- {label}：{value}")
        elif customer_field_contract.material_component_from_detail_label(label) == "lid":
            lines.append(f"- {label}：当前资料未单独提供锅盖材质。")
        else:
            lines.append(f"- {label}：当前资料未标注。")
    if _looks_like_portability_detail_question(question):
        lines.append(f"- {_same_sku_portability_detail_answer(db, resolved_sku)}")
    row = _product_row_from_model(product, specs, business, content)
    original_plan = compatibility_plan if isinstance(compatibility_plan, dict) else {}
    debug_plan = dict(original_plan)
    debug_plan.update({
        "primary_intent": str(original_plan.get("primary_intent") or "product_field"),
        "product_ref": resolved_sku,
        "sku": resolved_sku,
        "requested_fields": ordered_fields,
        "raw_question": question,
        "compound": True,
        "multi_field": True,
    })
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "answer": "\n".join(lines),
        "sku": resolved_sku,
        "results": [row],
        "result_skus": [resolved_sku],
        "candidate_skus": [resolved_sku],
        "answer_metadata": {
            "source": "resolved_entity_compound_detail",
            "answer_policy": "compound_field_evidence",
            "compound_fields": field_results,
        },
        "debug": {
            "agent_mode": "resolved_entity_compound_detail",
            "entity_resolution_contract": entity_contract or {},
            "requested_fields": ordered_fields,
            "compound": True,
            "multi_field": True,
            "plan": debug_plan,
        },
        "skip_polish": True,
    }


def _is_sealed_product_qa_safe_missing(result: dict | None) -> bool:
    """Whether a sealed QA attempt had identity but no direct evidence.

    This is a route-state check, not intent classification.  A safe-missing
    QA result remains the fallback for a compound supplement, but must not
    prevent the same sealed SKU from trying the RAG evidence provider.
    """
    return bool(
        isinstance(result, dict)
        and str((result.get("debug") or {}).get("agent_mode") or "")
        == "sealed_product_qa_safe_missing"
    )


async def _resolve_sealed_supplemental_product_evidence(
    db: Session,
    *,
    question: str,
    semantic_preplan: dict[str, Any] | None,
    base_plan: dict[str, Any] | None = None,
) -> tuple[dict | None, str]:
    """Use QA first, then sealed same-SKU RAG, for a semantic supplement."""
    effective_preplan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    supplemental_query = str(effective_preplan.get("supplemental_qa_evidence_query") or "").strip()
    if not supplemental_query:
        return None, ""
    supplemental_preplan = dict(effective_preplan)
    supplemental_preplan.update({
        "canonical_fields": [], "field_type": "", "field_hint": None,
        "evidence_kind": "product_qa", "qa_evidence_query": supplemental_query,
        "compound": False,
    })
    supplemental_plan = dict(base_plan or {})
    supplemental_plan["semantic_preplan"] = supplemental_preplan
    supplemental_question = " ".join(
        item for item in (
            str(supplemental_preplan.get("subject_text") or "").strip(),
            supplemental_query,
        ) if item
    ) or question
    supplemental = await _try_product_qa_shortcut_with_semantic_selection(
        db, supplemental_question, phase1_plan=supplemental_plan,
    )
    qa_safe_missing = supplemental if _is_sealed_product_qa_safe_missing(supplemental) else None
    if qa_safe_missing:
        supplemental = None
    if not supplemental:
        for _supplemental_attempt in range(3):
            supplemental = await _try_sealed_same_sku_knowledge_answer(
                db, supplemental_question, supplemental_plan,
            )
            if supplemental:
                break
    return supplemental or qa_safe_missing, supplemental_query


def _compose_alcohol_cause_boundary_answer(question: str) -> str | None:
    text = str(question or "").strip()
    if not (
        any(term in text for term in ("酒精", "燃料"))
        and any(term in text for term in ("问题", "原因", "导致", "是不是", "是否"))
    ):
        return None
    return "关于酒精/燃料是否导致你描述的问题：当前同 SKU 资料未标注该因果关系，不能仅凭材质判断。"


def _build_resolved_compound_evidence_result(
    db: Session,
    *,
    question: str,
    entity_contract: Any | None,
    field_request: dict[str, Any] | None,
    phase1_plan: dict[str, Any] | None = None,
) -> dict | None:
    """Execute a compound same-SKU evidence answer behind sealed contracts.

    The legacy compound formatter contains product-agnostic secondary evidence
    checks (for example a material question plus a durability question).  It
    is not permitted to resolve identity or choose the primary field here:
    both must already be sealed by Phase 2.  Returning ``None`` leaves normal
    one-field execution unchanged.
    """
    contract = entity_contract
    if not contract or not (
        contract.status == "resolved"
        and contract.resolved_sku
        and contract.is_unique
        and contract.confidence == "high"
        and contract.matched_by in _PHASE2_EXACT_ENTITY_MATCHES
        and contract.resolver_candidate_skus == [contract.resolved_sku]
    ):
        return None
    request = field_request if isinstance(field_request, dict) else {}
    canonical_fields = list(dict.fromkeys(
        str(field or "").strip()
        for field in (request.get("canonical_fields") or [])
        if str(field or "").strip() in customer_field_contract.FORMAL_DETAIL_FIELDS
    ))
    if not canonical_fields:
        return None
    product, specs, business, content = _phase1_product_bundle_by_ref(db, contract.resolved_sku)
    if not product:
        return None
    if len(canonical_fields) > 1:
        if str(contract.matched_by or "") == "recommendation_context_anchor":
            # This executor has already verified the anchor's resolved,
            # unique, high-confidence one-SKU contract above.  The context
            # provenance is not a raw-turn exact name, but it is a sealed
            # identity from the immediately preceding conversation state.
            resolution_decision = customer_entity_resolution_contract.SingleProductResolutionDecision(
                allowed=True,
                resolved_sku=str(contract.resolved_sku or "").strip().upper(),
                resolved_product_id=str(product.id),
                reason="resolved_context_anchor",
                match_level="recommendation_context_anchor",
            )
        else:
            resolution_decision = customer_entity_resolution_contract.can_resolve_single_product(contract, [product])
        if not resolution_decision.allowed:
            return None
        answers: list[str] = []
        evidence_items: list[dict[str, Any]] = []
        requested_labels_by_field: dict[str, list[str]] = {}
        for requested_label in request.get("requested_fields") or []:
            requested_type = customer_field_contract.field_type_from_detail_label(requested_label)
            if requested_type:
                requested_labels_by_field.setdefault(requested_type, []).append(str(requested_label))
        for field_type in canonical_fields:
            # A component qualifier refines the already-sealed canonical
            # material field.  Preserve that scope into the same-SKU evidence
            # provider instead of replacing it with the generic material
            # label in a compound answer.
            label = (requested_labels_by_field.get(field_type) or [
                customer_field_contract.product_detail_field_label(field_type)
            ])[0]
            if not label:
                return None
            supported = customer_field_contract.is_supported_detail_field(field_type)
            single_request = {
                "field_type": field_type,
                "requested_field": label,
                "requested_fields": [label],
                "canonical_fields": [field_type],
                "supported_fields": [field_type] if supported else [],
                "unsupported_fields": [] if supported else [field_type],
                "subject": str(request.get("subject") or ""),
                "requested_scope": "subject",
                "source": str(request.get("source") or "validated_semantic_preplan"),
                "confidence": float(request.get("confidence") or 0.0),
                "compound": False,
            }
            material_component = customer_field_contract.material_component_from_detail_label(label)
            if supported and material_component:
                # Component labels are evidence scopes for the already sealed
                # ``material`` contract.  The generic single-field formatter
                # would otherwise reread the whole body-material cell and
                # discard that scope in a compound answer.
                component_evidence = _resolve_single_product_field_evidence(
                    db,
                    resolved_sku=contract.resolved_sku,
                    requested_field=label,
                    question=question,
                )
                component_value = str(component_evidence.get("evidence_value") or "").strip()
                component_missing = bool(component_evidence.get("missing"))
                if component_value:
                    component_answer = f"{product.product_name_cn}（{contract.resolved_sku}）的{label}：{component_value}。"
                elif material_component == "lid":
                    component_answer = f"当前资料未单独提供{product.product_name_cn}（{contract.resolved_sku}）的锅盖材质。"
                else:
                    component_answer = f"当前资料未单独提供{product.product_name_cn}（{contract.resolved_sku}）的{label}。"
                field_result = {
                    "intent": "product_detail",
                    "answer_type": "product_detail",
                    "answer": component_answer,
                    "result_skus": [contract.resolved_sku],
                    "answer_metadata": {
                        "evidence_status": "missing" if component_missing else "structured",
                        "evidence_field": component_evidence.get("evidence_field"),
                        "evidence_source": component_evidence.get("evidence_source"),
                        "evidence_value": component_value,
                        "evidence_sku": component_evidence.get("evidence_sku"),
                    },
                }
            elif supported:
                field_result = _product_field_followup_result(
                    db,
                    contract.resolved_sku,
                    label,
                    question,
                    identity_source="phase2_resolved_entity",
                    field_request_override=single_request,
                )
            else:
                field_result = _build_resolved_product_unknown_field_result(
                    db,
                    product,
                    label=label,
                    source="resolved_entity_unknown_field_fallback",
                    resolution_decision=resolution_decision,
                )
            if not field_result or field_result.get("result_skus") != [contract.resolved_sku]:
                return None
            answer = str(field_result.get("answer") or "").strip()
            if not answer:
                return None
            answers.append(answer)
            field_metadata = field_result.get("answer_metadata") if isinstance(field_result.get("answer_metadata"), dict) else {}
            evidence_items.append({
                "field_type": field_type,
                "requested_field": label,
                "evidence_status": field_metadata.get("evidence_status"),
                "evidence_field": field_metadata.get("evidence_field"),
                "evidence_source": field_metadata.get("evidence_source"),
                "evidence_value": field_metadata.get("evidence_value"),
                "evidence_sku": field_metadata.get("evidence_sku"),
            })
        alcohol_cause_boundary = _compose_alcohol_cause_boundary_answer(question)
        if alcohol_cause_boundary:
            answers.insert(0, alcohol_cause_boundary)
        if _looks_like_portability_detail_question(question):
            answers.append(_same_sku_portability_detail_answer(db, contract.resolved_sku))
        row = _product_row_from_model(product, specs, business, content)
        plan = dict(phase1_plan or {})
        plan.update({
            "product_ref": contract.resolved_sku,
            "sku": contract.resolved_sku,
            "requested_fields": list(request.get("requested_fields") or []),
            "field_contract": dict(request),
            "compound": True,
            "raw_question": question,
        })
        evidence_skus = list(dict.fromkeys(
            str(item.get("evidence_sku") or "").strip().upper()
            for item in evidence_items
            if str(item.get("evidence_sku") or "").strip()
        ))
        return {
            "intent": "product_detail",
            "answer_type": "product_detail",
            "answer": "\n".join(answers),
            "sku": contract.resolved_sku,
            "results": [row],
            "result_skus": [contract.resolved_sku],
            "candidate_skus": [contract.resolved_sku],
            "answer_metadata": {
                "source": "resolved_entity_multi_field_contract",
                "answer_policy": "sealed_same_sku_multi_field_evidence",
                "contract_field_types": canonical_fields,
                "evidence_items": evidence_items,
                # Retain the established compound audit surface.  It is the
                # same per-field, sealed-SKU evidence list as ``evidence_items``
                # (not a second resolver or formatter path).
                "compound_fields": evidence_items,
                "evidence_skus": evidence_skus,
                "evidence_status": "structured" if any(item.get("evidence_value") for item in evidence_items) else "missing",
            },
            "debug": {
                "agent_mode": "resolved_entity_multi_field_contract",
                "binding_provenance": "resolved_entity_contract",
                "entity_resolution_contract": contract.to_dict(),
                "field_contract": dict(request),
                "plan": plan,
            },
            "skip_polish": True,
        }
    # This adapter is deliberately multi-field only. A single formal field
    # must continue to the ordinary Phase-2 provider, where field-specific
    # validation and same-SKU QA/RAG fallback are available.
    return None


_PHASE2_EXACT_ENTITY_MATCHES = {
    "sku_exact",
    "normalized_sku_exact",
    "canonical_name_exact",
    "normalized_alias_exact",
    # Identity was sealed by the immediately preceding single-product turn;
    # it is eligible for the same compound evidence executor as an exact name.
    "recommendation_context_anchor",
}


def _choose_effective_entity_contract(
    question: str,
    products: list[Product],
    *,
    resolver_candidates: list[Product] | None = None,
    override_subject: str | None = None,
) -> customer_entity_resolution_contract.EntityContractSelection:
    default_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        products,
        resolver_candidates=resolver_candidates,
    )
    override_contract = None
    normalized_override = customer_agent_service.normalize_search_text(override_subject or "").strip()
    if normalized_override and normalized_override != default_contract.normalized_entity_text:
        override_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
            question,
            products,
            resolver_candidates=resolver_candidates,
            entity_text_override=override_subject,
        )
    return customer_entity_resolution_contract.choose_effective_entity_contract(
        default_contract,
        override_contract,
        products,
    )


def _phase2_requested_fields(question: str) -> list[str]:
    return resolve_requested_field_contract(question)["requested_fields"]


def _semantic_exact_subject_for_field_contract(
    db: Session,
    question: str,
    semantic_preplan: dict[str, Any] | None,
) -> str:
    """Return a semantic subject only after catalogue identity validates it.

    The semantic model supplies a user-text span, never an SKU or product
    fact.  This adapter merely establishes whether that span is a unique
    exact identity so the formal FieldContract can retain a valid structured
    field plan whose route shape was otherwise catalogue-oriented.
    """
    plan = semantic_preplan if isinstance(semantic_preplan, dict) else {}
    subject = str(plan.get("subject_text") or "").strip()
    fields = [
        str(field or "").strip()
        for field in (plan.get("canonical_fields") or [])
        if str(field or "").strip() in customer_field_contract.FORMAL_DETAIL_FIELDS
    ]
    if not (
        subject
        and len(fields) == 1
        and _semantic_preplan_confident(plan, minimum=0.9)
        and str(plan.get("route_family") or "").strip() == "structured_query"
        and str(plan.get("route_hint") or "").strip() == "query_products"
        and customer_agent_service.normalize_search_text(subject).lower()
        in customer_agent_service.normalize_search_text(question).lower()
    ):
        return ""
    products = db.query(Product).order_by(Product.sku.asc()).all()
    contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        products,
        entity_text_override=subject,
    )
    if (
        contract.status == "resolved"
        and contract.resolved_sku
        and contract.is_unique
        and contract.matched_by in _PHASE2_EXACT_ENTITY_MATCHES
    ):
        return subject
    return ""


def resolve_requested_field_contract(
    question: str,
    phase1_plan: dict | None = None,
    *,
    subject_override: str | None = None,
) -> dict[str, Any]:
    """Central Phase 2 adapter over the stable legacy field extractor."""
    compatibility_fields = customer_agent_intent_service._requested_fields_for_detail_question(question)
    selection = customer_field_contract.select_entity_subject_for_routing(raw_question=question)
    subject = str(subject_override or "").strip()
    if not subject:
        subject = selection.entity_subject if selection.source == "field_contract" else ""
    if not subject:
        subject = customer_agent_intent_service._detail_subject_from_question(question).strip()
    if not subject:
        # The broad SKU extractor intentionally emits a short compatibility
        # suffix as well as the full SKU for some forms (for example CW-C95
        # and C95).  A full token that contains every other extraction is one
        # unambiguous identity, not a multi-product request.
        explicit_skus = list(dict.fromkeys(
            str(item or "").strip().upper()
            for item in customer_agent_service._extract_skus(str(question or ""))
            if str(item or "").strip()
        ))
        longest = max(explicit_skus, key=len, default="")
        if longest and all(item in longest for item in explicit_skus):
            subject = longest
    if subject and compatibility_fields:
        # Legacy recognition may see a field-looking token inside the product
        # name itself (for example a product whose name contains "煎盘"). Only
        # retain compatibility fields that also occur outside the selected
        # subject; the formal FieldContract remains authoritative.
        predicate_text = customer_agent_intent_service._remove_subject_once(question, subject)
        predicate_fields = customer_agent_intent_service._requested_fields_for_detail_question(predicate_text)
        compatibility_fields = [field for field in compatibility_fields if field in predicate_fields]
        # The old extractor treats component nouns such as “手柄” and product
        # title text such as “10件套” as material/accessory hints.  They are not
        # field predicates by themselves.  Keep a legacy mapped field only
        # when the remaining predicate carries the formal field signal (or a
        # concrete material assertion); semantic planning remains the primary
        # route for broader natural-language meaning.
        predicate_types = set(customer_field_contract.detect_field_types(predicate_text))
        material_assertion = bool(re.search(
            r"材质|材料|304|不锈钢|铝合金|硬质氧化铝|钛|木质|白蜡木|胡桃木|榉木",
            predicate_text,
            flags=re.IGNORECASE,
        ))
        compatibility_fields = [
            field
            for field in compatibility_fields
            if not (field_type := customer_field_contract.field_type_from_detail_label(field))
            or field_type in predicate_types
            or (field_type == "material" and material_assertion)
        ]
    # When semantic planning is unavailable, a high-confidence field-only
    # planner task is the controlled fallback for natural wording that has no
    # literal alias (for example a size question phrased without “尺寸”).
    # Adapt that canonical planner label into the one FieldContract instead
    # of allowing an entity or QA shortcut to answer first.
    planned_label = str((phase1_plan or {}).get("requested_field") or "").strip()
    planned_field = customer_field_contract.field_type_from_detail_label(planned_label)
    if (
        not compatibility_fields
        and str((phase1_plan or {}).get("primary_intent") or "") == "product_field"
        and bool((phase1_plan or {}).get("field_only"))
        and customer_field_contract.is_supported_detail_field(planned_field)
    ):
        compatibility_fields = [planned_label]
    resolved_contract = customer_field_contract.resolve_requested_field_contract(
        question,
        phase1_plan,
        compatibility_fields=compatibility_fields,
        subject=subject,
        requested_scope=selection.requested_scope,
        subject_is_catalog_exact=bool(str(subject_override or "").strip()),
    )
    # The semantic planner may correctly classify a named-product
    # compatibility turn as product QA while leaving ``canonical_fields``
    # empty.  If the customer's wording contains an explicit heat-source
    # predicate, recover the existing deterministic FieldContract so Phase 2
    # can read the same-SKU heat evidence instead of falling into generic
    # safety prose.  This is a planner/provider arbitration fallback, not a
    # product- or SKU-specific answer rule.
    detected_contract = customer_field_contract.detect_field_contract(question)
    if (
        not str(resolved_contract.get("field_type") or "").strip()
        and detected_contract is not None
        and str(getattr(detected_contract, "field_type", "") or "").strip() == "heat_source"
        and _looks_like_compatibility_usage_question(question)
    ):
        resolved_contract = customer_field_contract.resolve_requested_field_contract(
            question,
            None,
            compatibility_fields=(customer_field_contract.product_detail_field_label("heat_source") or "适用热源",),
            subject=subject,
            requested_scope=selection.requested_scope,
            subject_is_catalog_exact=bool(str(subject_override or "").strip()),
        )
        resolved_contract["source"] = "deterministic_compatibility_field_recovery"
    return resolved_contract


def _build_phase2_entity_resolution_context(
    db: Session,
    question: str,
    field_request: dict[str, Any] | None = None,
    phase1_plan: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Build the deterministic Phase 2 entity snapshot once for this request."""
    try:
        products = db.query(Product).order_by(Product.sku.asc()).all()
        subject_selection = customer_field_contract.select_entity_subject_for_routing(
            raw_question=question,
        )
        canonical_subject = customer_entity_resolution_contract.unique_canonical_subject_in_question(
            question,
            products,
        )
        if canonical_subject:
            normalized_subject = customer_agent_service.normalize_search_text(canonical_subject)
            raw_canonical_owners = {
                str(product.sku or "").strip().upper()
                for product in products
                if any(
                    customer_agent_service.normalize_search_text(raw_name) == normalized_subject
                    for raw_name in (
                        str(product.product_name_cn or "").strip(),
                        str(product.product_name_en or "").strip(),
                    )
                    if raw_name
                )
            }
            subject_start = str(question or "").lower().find(str(canonical_subject).lower())
            suffix = (
                str(question or "")[subject_start + len(canonical_subject):]
                if subject_start >= 0
                else ""
            ).lstrip(" 的，,：:；;。.!！?？")
            explicit_variant_suffix = bool(re.match(
                r"^(?:(?:pro|plus)(?![A-Za-z0-9])|基础款|标准版|升级款|旗舰版|"
                r"\d+(?:\.\d+)?\s*(?:杯|寸|英寸|ml|毫升|l|升)|"
                r"(?:黑|白|红|橙|黄|绿|蓝|紫|粉|灰|银|金)(?:色)?)",
                suffix,
                flags=re.IGNORECASE,
            ))
            if len(raw_canonical_owners) != 1 or explicit_variant_suffix:
                canonical_subject = ""
        # A previously formed semantic FieldContract is authoritative here.
        # Re-running the legacy parser solely to obtain a canonical product
        # subject can otherwise replace that field with an incidental word in
        # the question (e.g. ``brand`` in a price-tier request).  The exact
        # catalogue subject remains an input to EntityResolution only; it does
        # not come from semantic classification.
        if (
            isinstance(field_request, dict)
            and field_request.get("source") in {
                "validated_semantic_preplan",
                "deterministic_explicit_multi_field",
            }
        ):
            resolved_field_request = dict(field_request)
            if canonical_subject:
                resolved_field_request["subject"] = canonical_subject
        else:
            resolved_field_request = dict(
                resolve_requested_field_contract(
                    question,
                    phase1_plan,
                    subject_override=canonical_subject,
                )
                if canonical_subject
                else (field_request or resolve_requested_field_contract(question, phase1_plan))
            )
        semantic_preplan = (
            (phase1_plan or {}).get("semantic_preplan")
            if isinstance((phase1_plan or {}).get("semantic_preplan"), dict)
            else {}
        )
        recovered_semantic_subject = str(semantic_preplan.get("subject_text") or "").strip()
        if (
            semantic_preplan.get("semantic_identity_recovery") == "catalog_unique_one_edit"
            and recovered_semantic_subject
        ):
            # The semantic layer established that this is a product-bound
            # field request; catalogue uniqueness then corrected one literal
            # character. Keep the already parsed fields, but replace the
            # polluted legacy subject before EntityResolutionContract runs.
            resolved_field_request["subject"] = recovered_semantic_subject
            resolved_field_request["subject_is_catalog_exact"] = True
        entity_subject = str(resolved_field_request.get("subject") or "").strip() or None
        if not entity_subject and resolved_field_request.get("source") == "validated_semantic_preplan":
            # The semantic preplan owns field meaning and may also identify a
            # subject span for natural wording whose predicate has no fixed
            # deterministic alias (for example an included-items question).
            # Accept it only when the exact normalized text occurs in this
            # turn; EntityResolutionContract still decides whether it binds a
            # unique catalogue SKU.
            semantic_subject = str(
                ((phase1_plan or {}).get("semantic_preplan") or {}).get("subject_text") or ""
            ).strip()
            normalized_question = customer_agent_service.normalize_search_text(question)
            normalized_semantic_subject = customer_agent_service.normalize_search_text(semantic_subject)
            if normalized_semantic_subject and normalized_semantic_subject in normalized_question:
                entity_subject = semantic_subject
                resolved_field_request["subject"] = entity_subject
        if not entity_subject and resolved_field_request.get("source") == "validated_semantic_preplan":
            # Reuse only the catalogue subject that already passed the raw
            # canonical-owner and explicit-variant-suffix checks above.  A
            # second unguarded lookup here would promote display aliases or a
            # base name followed by an unavailable variant (for example a
            # version suffix), bypassing the fail-closed entity contract.
            entity_subject = canonical_subject or None
            if entity_subject:
                resolved_field_request["subject"] = entity_subject
        contract = customer_entity_resolution_contract.build_entity_resolution_contract(
            question,
            products,
            entity_text_override=entity_subject,
            field_type_override=resolved_field_request.get("field_type"),
        )
        return {
            "products_snapshot": products,
            "contract": contract,
            "field_request": resolved_field_request,
            "entity_subject_selection": subject_selection,
            "error": None,
        }
    except Exception as exc:
        return {"products_snapshot": [], "contract": None, "error": f"{type(exc).__name__}: {exc}"}


def _phase2_single_field_arbitration_eligible(
    question: str,
    phase1_plan: dict | None,
    *,
    conversation_id: str | None,
    field_request_override: dict[str, Any] | None = None,
) -> bool:
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    field_request = field_request_override or resolve_requested_field_contract(question, plan)
    current_subject = str(field_request.get("subject") or "").strip().strip("「」『』\"'“”")
    # A field predicate can describe a catalogue request ("哪种适合露营")
    # rather than a fact about one product.  Interrogative subjects never
    # establish single-product identity, so leave them to query/recommendation
    # routing instead of manufacturing an entity clarification.
    if re.match(r"^(?:哪种|哪款|哪些|什么|有哪|有没有)", current_subject):
        return False
    validated_semantic_subject = bool(
        current_subject
        and field_request.get("source") == "validated_semantic_preplan"
    )
    if (
        str(field_request.get("field_type") or "") in {"cleaning", "care", "usage_instruction"}
        and customer_agent_intent_service._looks_like_usage_care_question(question)
        and not _looks_like_product_like_scope_subject(current_subject)
        and not SKU_RE.fullmatch(current_subject)
        and not validated_semantic_subject
    ):
        # Generic care questions have no product identity to seal. Preserve
        # the established usage/care knowledge path; explicit products still
        # form EntityResolutionContract below.
        return False
    if conversation_id:
        # A conversation anchor is only a supplement to identity.  When the
        # current turn carries a formal field predicate and a concrete subject,
        # it must still enter EntityResolutionContract; otherwise an explicit
        # correction such as “我想问「…」的热源” falls through to generic tool
        # calling despite having a sealed current-turn identity.  Pure
        # pronouns retain the established anchor-specific follow-up path.
        if not current_subject or _looks_like_ordinal_product_field_followup(question) or re.match(
            r"^(?:它|它的|这个|这个商品|这款|该商品|该产品|这件商品|本产品|那个|那款)",
            current_subject,
        ):
            return False
    # The legacy compatibility extractor can still label in-box wording as
    # "contents". Let the authoritative FieldContract keep those requests in
    # entity arbitration instead of falling through to a dedicated usage route.
    non_usage_field_request = _has_non_usage_care_field_contract(question)
    # A validated semantic field is the same central contract signal as a
    # deterministic alias.  It must be allowed into Phase 2 before legacy
    # usage/QA shortcuts can interpret the surrounding natural language.
    semantic_field_request = bool(
        field_request.get("source") == "validated_semantic_preplan"
        and field_request.get("canonical_fields")
    )
    primary_intent = str(plan.get("primary_intent") or "").strip()
    if primary_intent not in {"", "product_detail", "product_field"} and not non_usage_field_request and not semantic_field_request:
        return False
    if plan.get("routing_conflict") or plan.get("must_compare_both_products") or plan.get("must_make_choice"):
        return False
    # A planner may expose an exact product reference while still assigning a
    # broad compatibility intent.  That is not a competing product-list
    # request when a validated FieldContract has already identified one
    # supported detail field; Phase 2 must arbitrate identity instead of
    # handing the request back to the legacy executor.
    if (
        (plan.get("must_return_products") or plan.get("product_refs"))
        and not non_usage_field_request
        and not semantic_field_request
    ):
        return False
    tasks = [task for task in (plan.get("tasks") or []) if isinstance(task, dict)]
    if (
        len(tasks) > 1
        or any(str(task.get("type") or "") not in {"", "product_field"} for task in tasks)
    ) and not non_usage_field_request and not semantic_field_request:
        return False
    if len(field_request["requested_fields"]) > 1:
        return False
    return len(field_request["canonical_fields"]) == 1


def _explicit_current_turn_comparison_skus(db: Session, question: str) -> list[str]:
    """Return two or more non-overlapping, catalogue-exact comparison identities."""
    return _explicit_current_turn_comparison_skus_from_products(
        question,
        db.query(Product).all(),
    )


def _explicit_current_turn_comparison_skus_from_products(question: str, products: list[Product]) -> list[str]:
    """Resolve literal comparison mentions against a supplied catalogue snapshot."""
    text = customer_agent_service.normalize_search_text(question)
    if not text or not any(
        term in str(question or "")
        for term in ("对比", "比较", "怎么比", "区别", "差异", "不同", "一样", "哪个", "哪款", "谁更", "更大", "更小", "更轻", "更重")
    ):
        return []

    matches: list[tuple[int, int, str]] = []
    for product in products:
        sku = str(product.sku or "").strip().upper()
        if not sku:
            continue
        candidates = {
            customer_agent_service.normalize_search_text(sku),
            customer_agent_service.normalize_search_text(product.product_name_cn or ""),
            customer_agent_service.normalize_search_text(product.product_name_en or ""),
        }
        for candidate in candidates:
            if not candidate:
                continue
            start = text.find(candidate)
            if start >= 0:
                matches.append((start, start + len(candidate), sku))

    # Prefer the longest identity at an overlapping span (for example a Pro
    # name over its base-family substring). Identical catalogue names remain
    # one unresolved mention rather than being misread as two participants.
    skus_by_span: dict[tuple[int, int], set[str]] = {}
    for start, end, sku in matches:
        skus_by_span.setdefault((start, end), set()).add(sku)
    unique_matches = [
        (start, end, next(iter(skus)))
        for (start, end), skus in skus_by_span.items()
        if len(skus) == 1
    ]
    selected: list[tuple[int, int, str]] = []
    for start, end, sku in sorted(unique_matches, key=lambda item: (-(item[1] - item[0]), item[0], item[2])):
        if any(not (end <= kept_start or start >= kept_end) for kept_start, kept_end, _ in selected):
            continue
        selected.append((start, end, sku))
    selected.sort(key=lambda item: (item[0], item[1], item[2]))
    return list(dict.fromkeys(sku for _, _, sku in selected)) if len(selected) >= 2 else []


def _dedicated_semantic_route(question: str, *, has_named_product: bool = False) -> str:
    if customer_agent_intent_service._looks_like_contents_grounding_question(question):
        return "contents_grounding"
    if _is_unbound_product_usage_question(question):
        return "usage_care" if has_named_product else "unbound_product_usage"
    if customer_agent_intent_service._looks_like_usage_care_question(question) or _is_product_usage_care_question(question):
        return "usage_care"
    return str(_classify_customer_faq_intent(question) or "")


def _looks_like_compound_contents_usage_question(question: str) -> bool:
    text = str(question or "").strip()
    if not customer_agent_intent_service._looks_like_contents_grounding_question(text):
        return False
    return any(
        term in text
        for term in ("直接用", "直接使用", "买回来", "到手", "收到货")
    )


def _explicit_sku_compound_contents_usage_result(
    db: Session,
    question: str,
    *,
    resolved_sku: str | None = None,
) -> dict | None:
    """Bound a literal SKU contents/use-preparation question deterministically."""
    if not _looks_like_compound_contents_usage_question(question):
        return None
    candidate_skus = [
        str(item or "").strip().upper()
        for item in customer_agent_service._extract_skus(question)
        if str(item or "").strip()
    ]
    if resolved_sku:
        candidate_skus.insert(0, str(resolved_sku).strip().upper())
    candidate_skus = list(dict.fromkeys(candidate_skus))
    if len(candidate_skus) != 1:
        return None
    bundle = _phase1_product_bundle_by_ref(db, candidate_skus[0])
    product = bundle[0]
    if product is None:
        return None
    sku = str(product.sku or candidate_skus[0]).strip().upper()
    name = str(product.product_name_cn or product.product_name_en or sku).strip()
    row = _product_row_from_model(*bundle)
    return {
        "intent": "product_detail",
        "answer_type": "product_detail",
        "answer": "\n".join((
            f"{name}（{sku}）",
            "包装内容包括：当前同 SKU 资料未直接确认具体清单。",
            "使用准备：当前资料未确认拆箱后即可直接使用；首次使用前请先核对包装清单、检查部件，并按说明书完成清洁或安装。",
        )),
        "sku": sku,
        "results": [row],
        "result_skus": [sku],
        "candidate_skus": [sku],
        "answer_metadata": {
            "source": "explicit_sku_compound_contents_usage_boundary",
            "evidence_status": "missing",
            "contract_field_type": "accessories",
            "compound": True,
            "usage_preparation_boundary": True,
        },
        "debug": {
            "agent_mode": "explicit_sku_compound_contents_usage_boundary",
            "field_contract": {
                "field_type": "accessories",
                "requested_field": "包装内容",
                "requested_fields": ["包装内容", "使用准备"],
                "canonical_fields": ["accessories", "usage_instruction"],
                "source": "deterministic_explicit_sku_compound_contents_usage",
            },
            "product_identity_source": "explicit_sku_exact",
        },
        "skip_polish": True,
    }


def _phase2_entity_arbitration_signals(
    question: str,
    phase1_plan: dict | None,
    semantic_preplan: dict | None,
    *,
    field_request_override: dict[str, Any] | None = None,
    has_named_product: bool = False,
) -> dict[str, bool]:
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    primary_intent = str(plan.get("primary_intent") or "").strip()
    tasks = [task for task in (plan.get("tasks") or []) if isinstance(task, dict)]
    field_request = _merge_explicit_detail_fields_into_request(
        question,
        field_request_override or resolve_requested_field_contract(question, plan),
    )
    field_types = field_request["canonical_fields"]
    requested_fields = field_request["requested_fields"]
    semantic_owned_field = bool(
        field_request.get("source") == "validated_semantic_preplan"
        and float(field_request.get("confidence") or 0.0) >= 0.9
        and field_types
        and not str((semantic_preplan or {}).get("semantic_adapter_source") or "").strip()
    )
    # The selected FieldContract is intentionally singular for single-detail
    # execution, but compound customer questions can carry additional formal
    # aliases.  Preserve those independent signals here so a first contract
    # never lets the single-field gate preempt orchestration.
    observed_field_types = (
        set(str(item or "").strip() for item in field_types if str(item or "").strip())
        if semantic_owned_field
        else set(customer_field_contract.detect_field_types(question))
    )
    # Remove a shorter alias when it occurs wholly inside a longer formal
    # alias in the same predicate (e.g. “价格” inside “价格定位”).
    normalized_question = str(question or "").lower()
    contracts = tuple(customer_field_contract.FIELD_CONTRACTS)
    for shorter in tuple(observed_field_types):
        short_aliases = [alias.lower() for item in contracts if item.field_type == shorter for alias in item.aliases]
        if any(
            short_alias in long_alias and len(long_alias) > len(short_alias)
            and long_alias in normalized_question
            for short_alias in short_aliases
            for item in contracts if item.field_type in observed_field_types and item.field_type != shorter
            for long_alias in (alias.lower() for alias in item.aliases)
        ):
            observed_field_types.discard(shorter)
    observed_field_types.update(str(item or "").strip() for item in field_types if str(item or "").strip())
    multi_field = len(requested_fields) > 1 or len(observed_field_types) > 1
    # Once a high-confidence semantic field has passed the allowlist and
    # FieldContract validator, lexical route detectors may no longer reinterpret
    # a word inside the product title as a second intent. True semantic
    # multi-intent requests are already preserved in canonical_fields.
    dedicated_route = "" if semantic_owned_field else _dedicated_semantic_route(
        question,
        has_named_product=has_named_product,
    )
    deterministic_field = (
        customer_field_contract.field_contract_for_type(field_types[0])
        if semantic_owned_field and len(field_types) == 1
        else customer_field_contract.detect_field_contract(question)
    )
    # Scenario group-size wording can contain the same "几个人" alias used
    # by the product people field.  It is a constraint only when it appears
    # in a recommendation scenario; other explicit field predicates retain
    # their normal exact-product priority.
    scenario_people_constraint = bool(
        deterministic_field
        and deterministic_field.field_type == "people"
        and re.search(r"(?:[一二三四五六七八九十百千\d]+|几|十几)\s*个人", str(question or ""))
    )
    scenario_route_without_product_field = bool(
        deterministic_field is None or scenario_people_constraint
    )
    # A heat-source predicate plus a category selection phrase is a catalog
    # request, not an incomplete reference to one product.  This deliberately
    # relies on the canonical field and generic product vocabulary rather than
    # any SKU/name or individual audit wording.
    generic_heat_source_selection = bool(
        deterministic_field
        and deterministic_field.field_type == "heat_source"
        and not SKU_RE.search(str(question or ""))
        and any(term in str(question or "") for term in ("锅", "锅具", "套锅", "炊具", "烤盘", "煎盘"))
        and any(term in str(question or "") for term in ("有没有", "有哪些", "推荐", "适合", "能配", "可以用"))
    )
    generic_accessory_catalog = bool(
        deterministic_field
        and deterministic_field.field_type == "accessories"
        and re.match(r"^\s*(?:有|都)?\s*哪些\s*(?:配件|附件)(?:产品|商品)?\s*[？?！!]?$", str(question or ""))
    )
    realtime_guardrail = customer_enterprise_guardrail_service.evaluate_question(question)
    realtime_weather = bool(
        isinstance(realtime_guardrail, dict)
        and str((realtime_guardrail.get("debug") or {}).get("guardrail_category") or "")
        in {"realtime_weather", "casual_weather_chat"}
        # A deterministic non-scenario field is a product fact request even
        # when its wording contains a temporal word such as "现在".  Weather
        # remains eligible for usage-scene questions, where current conditions
        # can genuinely be the user's intent.
        and not (
            deterministic_field is not None
            and deterministic_field.field_type != "usage_scene"
        )
    )
    return {
        "structured_query": bool(
            (
                _semantic_preplan_route_family(semantic_preplan) == "structured_query"
                and _semantic_preplan_confident(semantic_preplan)
            )
            or _looks_like_structured_field_filter_query(question)
        ),
        "multi_condition_recommendation": _looks_like_multi_condition_cookware_recommendation(question),
        # A quantity phrase inside a scenario (for example a group size in
        # "十几个人露营") is not a product's people field.  Preserve the
        # recommendation route before any field alias can turn the prose
        # preceding that phrase into a fictitious product subject.
        "scenario_recommendation": bool(
            scenario_route_without_product_field
            and (
                customer_agent_intent_service._looks_like_scenario_recommendation_question(question)
                or _phase1_is_light_budget_cookware_set_scenario(question)
                or _phase1_is_griddle_vs_cookware_scenario(question)
                or _phase1_is_stove_pairing_scenario(question)
                or _phase1_is_stove_griddle_combo_scenario(question)
                or _phase1_is_water_kettle_selection_scenario(question)
            )
        ),
        "realtime_weather": realtime_weather,
        "generic_heat_source_selection": generic_heat_source_selection,
        "generic_accessory_catalog": generic_accessory_catalog,
        "recommendation": primary_intent == "recommendation",
        "comparison": bool(
            primary_intent in {"comparison", "product_compare_recommendation"}
            or plan.get("must_compare_both_products")
            or plan.get("must_make_choice")
        ),
        "multi_field": multi_field,
        "additional_user_intent": multi_field,
        "compound": bool(plan.get("routing_conflict") or len(tasks) > 1 or len(field_types) > 1 or multi_field),
        "multi_product": bool(len(plan.get("product_refs") or []) > 1),
        "dedicated_route": bool(dedicated_route),
    }


def _semantic_field_ambiguity_clarification_result(
    semantic_preplan: dict | None,
    entity_resolution_context: dict[str, Any] | None,
    field_request: dict[str, Any] | None = None,
) -> dict | None:
    """Fail closed when semantic planning reports mutually plausible fields."""
    if not isinstance(semantic_preplan, dict) or not semantic_preplan.get("called"):
        return None
    if not semantic_preplan.get("ambiguity"):
        return None
    if str(semantic_preplan.get("route_hint") or "").strip() != "product_detail":
        return None
    try:
        confidence = float(semantic_preplan.get("confidence") or 0.0)
    except (TypeError, ValueError):
        return None
    if confidence < 0.5:
        return None
    canonical_fields = list(dict.fromkeys(
        str(item or "").strip()
        for item in (semantic_preplan.get("canonical_fields") or [])
        if customer_field_contract.is_supported_detail_field(str(item or "").strip())
    ))
    if len(canonical_fields) < 2:
        return None

    # Multiple independently requested fields are a compound request, not an
    # ambiguity that requires the customer to choose one. The deterministic
    # field contract is only a schema-grounded validation of the semantic plan:
    # it must have found the same explicit fields in the current turn. Genuine
    # wording ambiguity still fails closed through the clarification path.
    validated_request = field_request if isinstance(field_request, dict) else {}
    validated_fields = list(dict.fromkeys(
        str(item or "").strip()
        for item in (validated_request.get("canonical_fields") or [])
        if customer_field_contract.is_supported_detail_field(str(item or "").strip())
    ))
    if (
        validated_request.get("source") == "deterministic_explicit_multi_field"
        and len(validated_fields) > 1
        and set(validated_fields) == set(canonical_fields)
    ):
        return None

    context = entity_resolution_context if isinstance(entity_resolution_context, dict) else {}
    entity_contract = context.get("contract")
    entity_dict = entity_contract.to_dict() if entity_contract is not None else {}
    candidate_skus = [
        str(item or "").strip().upper()
        for item in (entity_dict.get("candidate_skus") or [])
        if str(item or "").strip()
    ]
    labels = [
        customer_field_contract.product_detail_field_label(field) or field
        for field in canonical_fields
    ]
    if len(labels) == 2:
        choices = f"{labels[0]}还是{labels[1]}"
    else:
        choices = "、".join(labels[:-1]) + f"还是{labels[-1]}"
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": f"这个问法可能对应多个字段。请确认你想查{choices}。",
        "results": [],
        "result_skus": [],
        "candidate_skus": candidate_skus,
        "needs_clarification": True,
        "answer_metadata": {
            "source": "semantic_field_ambiguity_clarification",
            "evidence_status": "field_ambiguity",
            "canonical_fields": canonical_fields,
        },
        "debug": {
            "agent_mode": "semantic_field_ambiguity_clarification",
            "field_contract": {
                "field_type": None,
                "canonical_fields": canonical_fields,
                "supported_fields": canonical_fields,
                "unsupported_fields": [],
                "source": "validated_semantic_preplan_ambiguous",
                "confidence": confidence,
                "compound": False,
            },
            "entity_resolution_contract": entity_dict,
        },
        "skip_polish": True,
    }


def _category_general_exclusion_signals(
    *,
    phase1_plan: dict | None,
    entity_contract: Any | None,
    entity_arbitration_signals: dict[str, bool] | None,
) -> dict[str, bool]:
    """Build the broader exclusions used only by category-field aggregation."""
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    signals = entity_arbitration_signals or {}
    primary_intent = str(plan.get("primary_intent") or "").strip()
    raw_question = str(plan.get("raw_question") or "").strip()
    entity_status = str(getattr(entity_contract, "status", "") or "")
    return {
        "entity_resolved": bool(getattr(entity_contract, "resolved_sku", None)),
        "entity_ambiguous": entity_status == "ambiguous" or bool(
            list(getattr(entity_contract, "resolver_candidate_skus", None) or [])
        ),
        "recommendation_explicit": bool(
            primary_intent == "recommendation"
            or signals.get("recommendation")
            or _looks_like_recommendation_request(raw_question)
        ),
        "scenario_like": bool(
            signals.get("scenario_like") or _phase1_should_use_structured_recommendation(plan)
        ),
        "multi_condition": bool(signals.get("multi_condition_recommendation")),
        "comparison": bool(
            primary_intent in {"comparison", "product_compare_recommendation"}
            or signals.get("comparison")
        ),
        "compound": bool(signals.get("compound")),
        "multi_product": bool(signals.get("multi_product")),
        "dedicated_route": bool(signals.get("dedicated_route")),
    }


def _should_use_category_field_general(
    *,
    structured_contract: customer_structured_query_contract.StructuredQueryContract,
    exclusions: dict[str, bool] | None,
) -> bool:
    if (
        structured_contract.status != "generic"
        or not structured_contract.subject_category
        or not structured_contract.field
        or structured_contract.operator is not None
        or structured_contract.value is not None
        or structured_contract.relation is not None
    ):
        return False
    return not any(bool(value) for value in (exclusions or {}).values())


def _classify_phase2_entity_state_action(
    question: str,
    phase1_plan: dict | None = None,
    *,
    conversation_id: str | None = None,
    signals: dict[str, bool] | None = None,
    entity_resolution_context: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Classify Phase 2 entity handling without formatting a response."""
    field_request = (
        (entity_resolution_context or {}).get("field_request")
        or resolve_requested_field_contract(question, phase1_plan)
    )
    if not _phase2_single_field_arbitration_eligible(
        question,
        phase1_plan,
        conversation_id=conversation_id,
        field_request_override=field_request,
    ):
        return {"action": "pass_through", "reason": "ineligible"}
    if (signals or {}).get("scenario_recommendation"):
        return {"action": "pass_through", "reason": "scenario_recommendation"}
    if (signals or {}).get("realtime_weather"):
        return {"action": "pass_through", "reason": "realtime_weather"}
    if (signals or {}).get("generic_heat_source_selection"):
        return {"action": "pass_through", "reason": "generic_heat_source_selection"}
    if (signals or {}).get("generic_accessory_catalog"):
        return {"action": "pass_through", "reason": "generic_accessory_catalog"}
    field = customer_field_contract.field_contract_for_type(field_request.get("field_type"))
    requested_field = field_request.get("requested_field")
    if not field or not requested_field:
        return {"action": "pass_through", "reason": "no_detail_field"}
    subject_selection = (entity_resolution_context or {}).get("entity_subject_selection")
    if subject_selection and _is_generic_company_product_scope(
        str(getattr(subject_selection, "entity_subject", "") or "")
    ):
        return {"action": "pass_through", "reason": "generic_company_product_scope"}
    # Every formal customer field, including safely unsupported realtime and
    # service fields, must arbitrate entity identity here.  The distinction
    # between supported evidence execution and safe missing is applied only
    # after the canonical SKU has been sealed.

    context = entity_resolution_context or {}
    products = list(context.get("products_snapshot") or [])
    contract = context.get("contract")
    if contract is None:
        return {"action": "pass_through", "reason": "entity_contract_unavailable"}

    if subject_selection and _is_generic_field_subject(str(getattr(subject_selection, "entity_subject", "") or "")):
        if (signals or {}).get("category_general"):
            # A formally parsed category+field aggregation (for example a
            # category's available capacities) owns this generic subject.
            # It must run before the single-product fail-closed clarification,
            # while exact/ambiguous named products remain protected by the
            # category-general exclusion gate.
            return {"action": "pass_through", "reason": "category_field_general"}
        # A category label plus a product-bound field is not permission to
        # promote search results into a single-product answer.  Keep the
        # FieldContract and return the same explicit clarification used for
        # any other generic entity state.
        return {"action": "generic_clarification", "reason": "generic_field_subject", "contract": contract}

    plan_primary_intent = str((phase1_plan or {}).get("primary_intent") or "").strip()
    planner_confirms_detail = plan_primary_intent in {"product_detail", "product_field"}
    field_contract_confirms_detail = bool(
        contract.entity_text
        and requested_field
        and (
            field_request.get("source") == "validated_semantic_preplan"
            or
            customer_field_contract.is_field_contract_predicate_signal(
                context.get("entity_subject_selection")
            )
            or _has_non_usage_care_field_contract(question)
        )
    )
    rich_field_compatibility = not plan_primary_intent and field.field_type in {
        "model",
        "dimensions",
        "specification",
    }

    high_priority_signal = any(
        bool(value)
        for key, value in (signals or {}).items()
        if key != "dedicated_route"
        and not (
            key == "compound"
            and field.field_type == "accessories"
            and _looks_like_compound_contents_usage_question(question)
        )
    ) or bool(
        (signals or {}).get("dedicated_route")
        and not _has_non_usage_care_field_contract(question)
    )

    resolved_exact = bool(
        contract.status == "resolved"
        and contract.resolved_sku
        and contract.is_unique
        and contract.confidence == "high"
        and contract.matched_by in _PHASE2_EXACT_ENTITY_MATCHES
        and contract.resolver_candidate_skus == [contract.resolved_sku]
    )
    resolved_exact_blocked = bool(
        (signals or {}).get("recommendation")
        or (signals or {}).get("comparison")
        or (
            (signals or {}).get("compound")
            and not (
                field.field_type == "accessories"
                and _looks_like_compound_contents_usage_question(question)
            )
        )
        or (signals or {}).get("multi_product")
        or (signals or {}).get("multi_condition_recommendation")
    )

    if not resolved_exact or resolved_exact_blocked:
        if high_priority_signal:
            return {"action": "pass_through", "reason": "higher_priority_route"}

        displayable_candidate_skus = _displayable_phase2_clarification_candidate_skus(contract)
        if (
            contract.status == "ambiguous"
            and displayable_candidate_skus
            and (planner_confirms_detail or rich_field_compatibility or field_contract_confirms_detail)
        ):
            return {
                "action": "ambiguous_clarification",
                "reason": "formal_candidates_ambiguous",
                "contract": contract,
                "products": products,
                "displayable_candidate_skus": displayable_candidate_skus,
            }
        if (
            contract.status == "ambiguous"
            and contract.resolver_candidate_skus
            and (planner_confirms_detail or rich_field_compatibility or field_contract_confirms_detail)
        ):
            return {
                "action": "unresolved_clarification",
                "reason": "weak_single_candidate_not_displayable",
                "contract": contract,
            }
        if (
            contract.status == "ambiguous"
            and contract.diagnostic_candidate_skus
            and (planner_confirms_detail or rich_field_compatibility or field_contract_confirms_detail)
        ):
            return {
                "action": "ambiguous_clarification",
                "reason": "diagnostic_candidates_not_displayable",
                "contract": contract,
                "products": products,
                "displayable_candidate_skus": [],
            }

        generic_scope = contract.status == "generic" or (
            not contract.resolver_candidate_skus
            and _looks_like_generic_constraint_category_query(question)
        )
        if generic_scope and field_contract_confirms_detail:
            return {"action": "generic_clarification", "reason": "generic_field_subject", "contract": contract}
        if generic_scope and planner_confirms_detail and isinstance(phase1_plan, dict):
            product_ref = _semantic_catalog_product_ref(question) or "产品"
            phase1_plan.update(
                {
                    "primary_intent": "catalog_count",
                    "answer_type": "query_products",
                    "product_ref": product_ref,
                    "field_only": False,
                    "allow_search_top1_promotion": False,
                    "entity_resolution_contract": contract.to_dict(),
                }
            )
            return {"action": "pass_through", "reason": "generic_category_constraint", "contract": contract}

        if (
            contract.status == "unresolved"
            and contract.entity_text
            and (planner_confirms_detail or rich_field_compatibility or field_contract_confirms_detail)
        ):
            return {"action": "unresolved_clarification", "reason": "concrete_entity_unresolved", "contract": contract}
        return {"action": "pass_through", "reason": "no_entity_action", "contract": contract}

    return {
        "action": "resolved_detail",
        "reason": "resolved_exact_supported_field",
        "contract": contract,
        "products": products,
        "field": field,
        "requested_field": requested_field,
    }


def _displayable_phase2_clarification_candidate_skus(contract: Any) -> list[str]:
    if str(getattr(contract, "status", "") or "") != "ambiguous":
        return []
    unique_skus = list(dict.fromkeys(
        str(sku or "").strip().upper()
        for sku in (getattr(contract, "resolver_candidate_skus", None) or [])
        if str(sku or "").strip()
    ))
    return unique_skus if len(unique_skus) >= 2 else []


def _safe_customer_entity_text(entity_text: str | None, question: str | None = None) -> str | None:
    """Return an entity label only when it is safe to show to the customer.

    The semantic planner may emit a prefix of a sentence as ``entity_text``
    (for example ``“带炉子吗，炉子”``).  Echoing that prefix makes a
    clarification look like a product name and hides the real request.  Keep
    explicit SKU-like references and canonical product-like names, but fail
    closed for sentence fragments, interrogative wording, punctuation, and
    unmatched brackets.
    """
    value = str(entity_text or "").strip(" \t\r\n'\"“”‘’「」『』（）()，,。！？!?；;：:")
    if not value:
        return None

    # An explicit SKU is a stable customer-facing reference even when the
    # catalogue does not contain it; it is useful in the follow-up request.
    if re.fullmatch(r"[A-Za-z0-9][A-Za-z0-9._()（）/\-\u4e00-\u9fff]{2,}", value) and "-" in value:
        return value

    if any(mark in value for mark in (
        "，", ",", "。", "！", "!", "？", "?", "；", ";", "：", ":",
        "吗", "嘛", "呢", "怎么", "如何", "为什么", "是不是", "是否",
        "能不能", "能否", "可以", "带", "还是", "多大", "多少", "哪里",
        "什么", "有没有", "有无", "会不会", "买", "放进去", "之前",
        "因为", "我", "他", "这个", "那个", "最高", "火力",
    )):
        return None
    if len(value) > 24 or any(value.count(left) != value.count(right) for left, right in (("（", "）"), ("(", ")"))):
        return None
    if not _looks_like_product_like_scope_subject(value):
        return None

    # If the planner selected a prefix of this turn, only accept it when the
    # remaining text clearly starts a supported product-field question.
    raw_question = str(question or "").strip()
    if raw_question.startswith(value) and len(raw_question) > len(value):
        tail = raw_question[len(value):].lstrip(" \t，,：:")
        allowed_tail = re.compile(
            r"^(?:的)?(?:是什么|啥|哪种|型号|材质|主体材质|容量|重量|尺寸|功率|热源|燃料|配件|包装|内容|可以|能不能|能否|是否|是不是|适合|有多重|多大|多少|怎么|如何|有没有|有无|支持|能用|价格|多少钱|保修|售后|清洗|使用|安装|点火|调节|火力)",
        )
        if tail and not allowed_tail.match(tail):
            return None
    return value


def _build_phase2_entity_state_response(db: Session, question: str, decision: dict[str, Any]) -> dict | None:
    """Format a previously classified Phase 2 entity-state decision."""
    action = str(decision.get("action") or "pass_through")
    if action == "pass_through":
        return None
    contract = decision.get("contract")
    products = list(decision.get("products") or [])
    display_entity_text = _safe_customer_entity_text(
        getattr(contract, "entity_text", ""),
        question,
    )
    display_entity_label = display_entity_text or (
        "当前页面商品"
        if any(marker in str(question or "") for marker in ("当前商品", "当前页面", "页面商品"))
        else "这个商品"
    )
    if action == "ambiguous_clarification":
        displayable_candidate_skus = list(decision.get("displayable_candidate_skus") or [])
        product_by_sku = {
            str(product.sku or "").strip().upper(): product
            for product in products
        }
        options = [
            product_by_sku[sku]
            for sku in displayable_candidate_skus
            if sku in product_by_sku
        ]
        option_text = "；".join(
            f"{str(product.product_name_cn or product.product_name_en or product.sku).strip()}（{str(product.sku or '').strip().upper()}）"
            for product in options[:5]
        )
        route_source = (
            "named_product_unknown_field_clarification"
            if str(getattr(contract, "field_type", "") or "") in {"gift", "manual"}
            else "entity_state_detail_ambiguous"
        )
        compatibility_question = any(
            term in str(question or "")
            for term in ("能不能接", "能否接", "能不能用", "适配", "兼容", "通用气罐")
        )
        if compatibility_question:
            answer = (
                f"“{display_entity_label}”匹配到多个商品：{option_text}。"
                if option_text
                else f"“{display_entity_label}”对应多个相关商品。"
            ) + "由于具体款式未确定，当前无法确认通用气罐能否连接；请指定具体款式或 SKU 后再核对。"
        else:
            answer = (
                f"“{display_entity_label}”匹配到多个商品：{option_text}。请指定具体款式或 SKU。"
                if option_text
                else f"“{display_entity_label}”对应多个相关商品，请先指定具体款式或 SKU。"
            )
        return {
            "intent": "clarify", "answer_type": "clarification",
            "answer": answer,
            "results": [], "result_skus": [], "candidate_skus": displayable_candidate_skus,
            "needs_clarification": True,
            "answer_metadata": {"source": route_source, "evidence_status": "ambiguous_product"},
            "debug": {"agent_mode": route_source, "entity_resolution_contract": contract.to_dict()},
            "skip_polish": True,
        }
    if action == "unresolved_clarification":
        route_source = (
            "unresolved_product_like_contents_clarification"
            if str(getattr(contract, "field_type", "") or "") == "accessories"
            else (
                "unresolved_product_like_unknown_field_clarification"
                if str(getattr(contract, "field_type", "") or "") == "gift"
                else "entity_state_detail_unresolved"
            )
        )
        answer = (
            f"没有找到“{display_entity_text}”对应的商品资料，请确认商品名或提供 SKU。"
            if display_entity_text
            else "当前还无法锁定具体商品，暂时没有找到可核对的商品资料；请提供完整商品名或具体 SKU。"
        )
        return {
            "intent": "clarify", "answer_type": "clarification",
            "answer": answer,
            "results": [], "result_skus": [], "candidate_skus": [], "needs_clarification": True,
            "answer_metadata": {"source": route_source, "evidence_status": "product_not_found"},
            "debug": {"agent_mode": route_source, "entity_resolution_contract": contract.to_dict()},
            "skip_polish": True,
        }
    if action == "generic_clarification":
        # The subject-selection guard has concluded that the phrase is a
        # generic category-like scope.  An earlier resolver observation may
        # have found a coincidental canonical-name match, but that observation
        # must not leak as an exact identity alongside a fail-closed generic
        # clarification.  This is an output-contract correction only: it
        # neither promotes a SKU nor changes the field's semantic meaning.
        generic_contract = contract.to_dict()
        generic_contract.update({
            "status": "generic",
            "resolved_sku": None,
            "resolver_candidate_skus": [],
            "diagnostic_candidate_skus": [],
            "candidate_skus": [],
            "matched_by": "none",
            "confidence": "low",
            "is_unique": False,
            "matched_span": None,
            "status_reason": "generic_field_subject",
        })
        return {
            "intent": "clarify", "answer_type": "clarification",
            "answer": f"“{display_entity_label}”还不是明确的单个商品，请提供完整商品名或 SKU。",
            "results": [], "result_skus": [], "candidate_skus": [], "needs_clarification": True,
            "answer_metadata": {"source": "entity_state_detail_generic", "evidence_status": "generic_product"},
            "debug": {
                "agent_mode": "entity_state_detail_generic",
                # A generic subject is deliberately never promoted to a product.
                # Keep that binding decision observable just like resolved/ambiguous
                # detail contracts, so downstream diagnostics cannot mistake this
                # clarification for a missing contract.
                "binding_provenance": "none",
                "search_top1_promotion_blocked": True,
                "entity_resolution_contract": generic_contract,
            },
            "skip_polish": True,
        }

    field = decision["field"]
    requested_field = decision["requested_field"]
    product = next(
        (item for item in products if str(item.sku or "").strip().upper() == contract.resolved_sku),
        None,
    )
    shipping_qa_evidence_available = bool(
        str(field.field_type or "") == "shipping"
        and product is not None
        and _best_product_qa_match(
            db,
            product,
            question,
            requested_field_type="shipping",
        ) is not None
    )

    if not customer_field_contract.is_supported_detail_field(field.field_type) and not shipping_qa_evidence_available:
        if not product:
            return None
        resolution_decision = customer_entity_resolution_contract.can_resolve_single_product(
            contract,
            products,
        )
        result = _build_resolved_product_unknown_field_result(
            db,
            product,
            label=requested_field,
            source="resolved_entity_detail_contract",
            resolution_decision=resolution_decision,
            entity_contract=contract,
        )
    else:
        result = _product_field_followup_result(
            db,
            contract.resolved_sku,
            requested_field,
            question,
            identity_source="phase2_resolved_entity",
        )
        if not result:
            return None

    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    if metadata.get("evidence_status") == "missing" and contract.resolved_sku not in str(result.get("answer") or ""):
        product = next(
            (item for item in products if str(item.sku or "").strip().upper() == contract.resolved_sku),
            None,
        )
        name = str(getattr(product, "product_name_cn", "") or getattr(product, "product_name_en", "") or "").strip()
        prefix = f"{name}（{contract.resolved_sku}）" if name else contract.resolved_sku
        result["answer"] = f"{prefix}：{str(result.get('answer') or '').strip()}"
    preserves_unknown_field_route = not customer_field_contract.is_supported_detail_field(field.field_type) and not shipping_qa_evidence_available
    route_source = (
        "resolved_entity_unknown_field_fallback"
        if preserves_unknown_field_route
        else "resolved_entity_detail_contract"
    )
    debug["agent_mode"] = route_source
    debug["binding_provenance"] = "resolved_entity_contract"
    debug["search_top1_promotion_blocked"] = False
    debug["entity_resolution_contract"] = contract.to_dict()
    metadata["source"] = route_source
    metadata["contract_field_type"] = field.field_type
    result["debug"] = debug
    result["answer_metadata"] = metadata
    return result


def _phase2_entity_state_arbitration_result(
    db: Session,
    question: str,
    phase1_plan: dict | None = None,
    *,
    conversation_id: str | None = None,
    signals: dict[str, bool] | None = None,
    entity_resolution_context: dict[str, Any] | None = None,
) -> dict | None:
    """Arbitrate one-field entity states before legacy product-field routing."""
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    if (
        str(plan.get("primary_intent") or "") == "category_compatibility"
        and bool(plan.get("must_stay_within_category"))
        and str(plan.get("category_ref") or "").strip()
    ):
        return None
    # Every recognised formal field, including safely unsupported and
    # realtime-bound fields, must first seal identity here.  The formatter
    # below selects same-SKU evidence or the field-specific safe-missing
    # response; returning early here previously leaked such requests to
    # generic recommendation/tool routes without an entity contract.
    context = entity_resolution_context
    if context is None:
        predecision = _classify_phase2_entity_state_action(
            question,
            phase1_plan,
            conversation_id=conversation_id,
            signals=signals,
            entity_resolution_context={},
        )
        if predecision.get("reason") != "entity_contract_unavailable":
            return _build_phase2_entity_state_response(db, question, predecision)
        context = _build_phase2_entity_resolution_context(
            db,
            question,
            resolve_requested_field_contract(question, phase1_plan),
            phase1_plan,
        )
    decision = _classify_phase2_entity_state_action(
        question,
        phase1_plan,
        conversation_id=conversation_id,
        signals=signals,
        entity_resolution_context=context,
    )
    result = _build_phase2_entity_state_response(db, question, decision)
    effective_field_request = context.get("field_request") if isinstance(context, dict) else None
    if isinstance(result, dict) and isinstance(effective_field_request, dict):
        debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
        debug["field_contract"] = dict(effective_field_request)
        debug_plan = debug.get("plan") if isinstance(debug.get("plan"), dict) else {}
        debug_plan["field_contract"] = dict(effective_field_request)
        debug["plan"] = debug_plan
        result["debug"] = debug
    return result


def _normalize_alcohol_stove_cookware_list_answer_type(question: str, agent_result: dict | None) -> dict | None:
    if not isinstance(agent_result, dict):
        return agent_result
    if str(agent_result.get("answer_type") or "").strip() != "product_detail":
        return agent_result
    if customer_agent_service._extract_skus(question):
        return agent_result
    if not customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(question):
        return agent_result
    rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
    if not rows:
        return agent_result
    normalized = dict(agent_result)
    normalized["intent"] = "query_products"
    normalized["answer_type"] = "product_query"
    return normalized


def _phase1_row_has_explicit_alcohol_stove_support(db: Session, row: dict[str, Any]) -> bool:
    if not isinstance(row, dict):
        return False
    heat_source = str(row.get("heat_source") or "").strip()
    if customer_agent_intent_service._alcohol_stove_support_verdict(heat_source) is True:
        return True
    field_values = row.get("field_values")
    if isinstance(field_values, dict):
        field_heat_source = str(field_values.get("热源") or field_values.get("适用热源") or "").strip()
        if customer_agent_intent_service._alcohol_stove_support_verdict(field_heat_source) is True:
            return True
    return False


def _sync_alcohol_stove_cookware_scope_metadata(
    result: dict,
    *,
    skus: list[str],
    rows: list[dict[str, Any]],
    question: str,
) -> dict:
    normalized_skus = [
        str(sku or "").strip().upper()
        for sku in skus
        if str(sku or "").strip()
    ]
    result["result_skus"] = normalized_skus
    result["candidate_skus"] = normalized_skus
    sources = list(result.get("sources") or [])
    scoped_context = {
        "candidate_skus": normalized_skus,
        "ordered_result_skus": normalized_skus,
        "filtered_skus": normalized_skus,
        "recommended_skus": [
            str(sku or "").strip().upper()
            for sku in (result.get("recommended_skus") or [])
            if str(sku or "").strip().upper() in set(normalized_skus)
        ],
        "product_scope": "锅具",
        "user_question": question,
        "applied_filter": {"specs.heat_source": "酒精炉", "product.category": "锅具"},
        "empty_subset": False,
    }
    meta = next((item for item in sources if isinstance(item, dict) and item.get("type") == "agent_meta"), None)
    if isinstance(meta, dict):
        meta["candidate_context"] = dict(scoped_context)
    else:
        sources.append({"type": "agent_meta", "candidate_context": dict(scoped_context)})
    result["sources"] = sources
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    intent_debug = debug.get("intent") if isinstance(debug.get("intent"), dict) else {}
    filters = intent_debug.get("filters") if isinstance(intent_debug.get("filters"), dict) else {}
    filters["product.category"] = "锅具"
    filters["specs.heat_source"] = "酒精炉"
    intent_debug["filters"] = filters
    debug["intent"] = intent_debug
    debug["raw_results"] = list(rows)
    debug["candidate_skus"] = normalized_skus
    debug["alcohol_stove_cookware_scope"] = "pot_or_cookware_set_only"
    result["debug"] = debug
    return result


def _run_phase1_answer_guard(agent_result: dict, plan: dict) -> dict:
    if not isinstance(agent_result, dict) or not isinstance(plan, dict):
        return agent_result
    primary_intent = str(plan.get("primary_intent") or "")
    plan_question = ""
    for key in ("raw_question", "question", "scenario", "semantic_query", "recommendation_query"):
        value = str(plan.get(key) or "").strip()
        if value:
            plan_question = value
            break
    recommendation_like_contract = bool(
        primary_intent == "recommendation"
        or plan.get("must_return_products")
        or _looks_like_recommendation_request(plan_question)
        or customer_agent_intent_service._looks_like_scenario_recommendation_question(plan_question)
    )
    if recommendation_like_contract and agent_result.get("answer_type") == "knowledge_base_answer":
        skus = _phase1_result_skus(agent_result)
        if skus:
            agent_result["answer"] = f"更建议先看 {skus[0]}。它更贴近你的使用场景，建议结合容量、重量、便携性和适用人数确认。"
        else:
            agent_result["answer"] = "当前资料里没有找到足够明确、可推荐的具体产品；建议补充人数、场景、预算或希望的品类后再推荐。"
        agent_result["answer_type"] = "recommendation"
        agent_result["intent"] = "recommendation"
        agent_result["skip_polish"] = True
    if plan.get("must_return_products") and not _phase1_answer_mentions_product(agent_result):
        skus = _phase1_result_skus(agent_result)
        if skus:
            agent_result["answer"] = f"更建议先看 {skus[0]}。它更贴近你的使用场景，建议结合容量、重量、便携性和适用人数确认。"
            agent_result["answer_type"] = "recommendation"
            agent_result["intent"] = "recommendation"
            agent_result["skip_polish"] = True
    if primary_intent == "product_field" and plan.get("field_only"):
        requested_field = str(plan.get("requested_field") or "").strip()
        answer = str(agent_result.get("answer") or "")
        field_answered = requested_field in answer
        if requested_field == "heat_source":
            field_answered = any(term in answer for term in ("酒精炉", "适用热源", "热源"))
        if requested_field and not field_answered:
            product_ref = str(plan.get("product_ref") or "").strip()
            agent_result["answer"] = f"当前资料里没有找到{product_ref}的明确{requested_field}。"
            agent_result["answer_type"] = "product_detail"
            agent_result["intent"] = "product_detail"
            agent_result["skip_polish"] = True
    if plan.get("must_not_recommend_other_categories"):
        rows = [item for item in agent_result.get("results") or [] if isinstance(item, dict)]
        skus = [str(item.get("sku") or "").strip().upper() for item in rows if str(item.get("sku") or "").strip()]
        agent_result["intent"] = "product_detail"
        agent_result["answer_type"] = "product_detail"
        agent_result["results"] = rows
        agent_result["result_skus"] = skus
        agent_result["candidate_skus"] = skus
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["raw_results"] = rows
        debug["candidate_skus"] = skus
        agent_result["debug"] = debug
        agent_result["skip_polish"] = True
    comparison_contract = bool(
        primary_intent == "comparison"
        or plan.get("must_compare_both_products")
        or str(agent_result.get("answer_type") or "").strip() == "comparison"
    )
    explicit_comparison_skus = list(dict.fromkeys(
        str(sku or "").strip().upper()
        for sku in customer_agent_service._extract_skus(plan_question)
        if str(sku or "").strip()
    ))
    if comparison_contract and len(explicit_comparison_skus) >= 2:
        answer = str(agent_result.get("answer") or "")
        if not all(sku in answer.upper() for sku in explicit_comparison_skus):
            first_sku, second_sku = explicit_comparison_skus[:2]
            agent_result["answer"] = (
                f"{first_sku} 和 {second_sku} 是两个不同的 SKU，不能视为同一款商品。"
                "如需继续比较规格或适用场景，请指定要对比的字段。"
            )
            matching_rows = [
                row for row in (agent_result.get("results") or [])
                if isinstance(row, dict)
                and str(row.get("sku") or "").strip().upper() in set(explicit_comparison_skus)
            ]
            agent_result["results"] = matching_rows
            agent_result["result_skus"] = explicit_comparison_skus
            agent_result["candidate_skus"] = explicit_comparison_skus
            agent_result["intent"] = "compare_products"
            agent_result["answer_type"] = "comparison"
            agent_result["skip_polish"] = True
    return agent_result


async def _attach_debug_supporting_knowledge(db: Session, result: dict, question: str) -> dict:
    """Attach QA/KB evidence for dev observability without changing the chosen answer."""
    if not isinstance(result, dict):
        return result
    try:
        return await customer_agent_intent_service.attach_supporting_knowledge_evidence(
            db,
            result,
            question,
            primary_source=(result.get("answer_metadata") or {}).get("final_decision", {}).get("primary_source") or "existing_route",
        )
    except Exception:
        return result


def list_conversations(db: Session, user_id: str, skip: int = 0, limit: int = 30) -> dict:
    user_id = str(user_id)
    query = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.user_id == user_id
    ).order_by(CustomerServiceConversation.updated_at.desc())
    total = query.count()
    items = query.offset(skip).limit(limit).all()
    previews = _conversation_previews(db, [item.id for item in items])
    return {
        "items": [_conversation_list_item(item, previews.get(item.id, {})) for item in items],
        "total": total,
    }


def _conversation_previews(db: Session, conversation_ids: list[str]) -> dict[str, dict]:
    if not conversation_ids:
        return {}
    previews: dict[str, dict] = {conversation_id: {} for conversation_id in conversation_ids}
    rows = (
        db.query(CustomerServiceMessage)
        .filter(CustomerServiceMessage.conversation_id.in_(conversation_ids))
        .order_by(CustomerServiceMessage.conversation_id.asc(), CustomerServiceMessage.created_at.desc())
        .all()
    )
    for message in rows:
        preview = previews.setdefault(message.conversation_id, {})
        preview.setdefault("last_any", message)
        if message.role == "assistant":
            preview.setdefault("last_assistant", message)
    return previews


def _conversation_list_item(item: CustomerServiceConversation, preview: dict) -> dict:
    preview_message = preview.get("last_assistant") or preview.get("last_any")
    preview = str(preview_message.content or "") if preview_message else ""
    return {
        "id": item.id,
        "title": item.title,
        "sku": item.sku,
        "last_message": preview[:120],
        "last_message_role": preview_message.role if preview_message else None,
        "last_message_at": str(preview_message.created_at) if preview_message and preview_message.created_at else None,
        "created_at": str(item.created_at),
        "updated_at": str(item.updated_at),
    }


def get_conversation(db: Session, conversation_id: str, user_id: str) -> dict:
    user_id = str(user_id)
    conversation = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.id == conversation_id,
        CustomerServiceConversation.user_id == user_id,
    ).first()
    if not conversation:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="客服会话不存在")

    messages = db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation_id
    ).order_by(CustomerServiceMessage.created_at.asc()).all()
    payload = []
    for item in messages:
        meta = _message_meta(item.sources_json)
        payload.append(
            {
                "id": item.id,
                "role": item.role,
                "content": item.content,
                "sku": item.sku,
                "sources": _safe_json(item.sources_json, []),
                "steps": _steps_from_sources(item.sources_json),
                "intent": meta.get("intent"),
                "answer_type": meta.get("answer_type"),
                "confidence": meta.get("confidence"),
                "uncertainty": meta.get("uncertainty"),
                "needs_clarification": meta.get("needs_clarification", False),
                "anomalies": meta.get("anomalies", []),
                "suggested_followups": meta.get("suggested_followups", meta.get("followups", [])),
                "followups": meta.get("followups", meta.get("suggested_followups", [])),
                "warnings": meta.get("warnings", []),
                "evidence": meta.get("evidence", []),
                "agent_quality": meta.get("agent_quality", {}),
                "debug": meta.get("debug", {}),
                "feedback": meta.get("feedback"),
                "created_at": str(item.created_at),
            }
        )
    return {
        "id": conversation.id,
        "title": conversation.title,
        "sku": conversation.sku,
        "created_at": str(conversation.created_at),
        "updated_at": str(conversation.updated_at),
        "messages": payload,
    }


def delete_conversation(db: Session, conversation_id: str, user_id: str) -> dict:
    user_id = str(user_id)
    conversation = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.id == conversation_id,
        CustomerServiceConversation.user_id == user_id,
    ).first()
    if not conversation:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="客服会话不存在")

    # Do not rely solely on the database FK's ON DELETE CASCADE here.  Some
    # development/test SQLite connections do not enforce foreign keys, which
    # leaves orphaned turns behind and can make a deleted chat appear again in
    # later previews.  The ownership check above scopes both deletes to this
    # exact conversation.
    db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation_id
    ).delete(synchronize_session=False)
    db.delete(conversation)
    db.commit()
    return {"deleted": True, "id": conversation_id}


def save_message_feedback(
    db: Session,
    *,
    user_id: str,
    message_id: str,
    rating: str,
    reason: str | None = None,
    comment: str | None = None,
) -> dict:
    allowed = {"helpful", "incorrect", "missing_data"}
    if rating not in allowed:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="反馈类型不支持")
    message = (
        db.query(CustomerServiceMessage)
        .join(CustomerServiceConversation, CustomerServiceConversation.id == CustomerServiceMessage.conversation_id)
        .filter(
            CustomerServiceMessage.id == message_id,
            CustomerServiceMessage.role == "assistant",
            CustomerServiceConversation.user_id == str(user_id),
        )
        .first()
    )
    if not message:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="客服消息不存在")

    sources = _safe_json(message.sources_json, [])
    meta = None
    for source in sources:
        if isinstance(source, dict) and source.get("type") == "agent_meta":
            meta = source
            break
    if meta is None:
        meta = {"type": "agent_meta", "label": "客服回复元数据"}
        sources.append(meta)
    feedback = {
        "rating": rating,
        "reason": reason or "",
        "comment": comment or "",
    }
    meta["feedback"] = feedback
    message.sources_json = json.dumps(sources, ensure_ascii=False, default=str)
    db.commit()
    return {"message_id": message.id, "feedback": feedback}


async def _try_explicit_customer_mutation_result(
    db: Session,
    *,
    user_id: str,
    question: str,
    sku: str | None,
    conversation_id: str | None,
) -> dict | None:
    """Create pending actions for a parseable write request before read-only guards.

    The normal agent/runtime path intentionally refuses catalogue mutations.
    Customer-service actions are a separate, confirmation-gated capability, so
    only the deterministic proposal intents may cross this boundary.
    """
    previous_result_skus = _previous_result_skus_for_pre_runtime(
        db,
        user_id=user_id,
        conversation_id=conversation_id,
        question=question,
    )
    intent = customer_agent_intent_service.parse_intent(
        question,
        sku=sku,
        previous_result_skus=previous_result_skus,
    )
    if not intent or intent.intent not in {"propose_update", "propose_delete"}:
        return None
    result = await customer_agent_intent_service.process_intent_request(
        db,
        user_id=user_id,
        question=question,
        sku=sku,
        previous_result_skus=previous_result_skus,
        allow_llm_fallback=False,
    )
    if not isinstance(result, dict) or result.get("intent") not in {"propose_update", "propose_delete"}:
        return None
    return result


async def _try_explicit_internal_catalogue_filter_result(
    db: Session,
    *,
    user_id: str,
    question: str,
    sku: str | None,
    conversation_id: str | None,
) -> dict | None:
    """Execute an explicit owner/lifecycle filter before semantic shortcuts."""
    if not _is_internal_catalogue_filter_query(question):
        return None
    previous_result_skus = _previous_result_skus_for_pre_runtime(
        db,
        user_id=user_id,
        conversation_id=conversation_id,
        question=question,
    )
    intent = customer_agent_intent_service.parse_intent(
        question,
        sku=sku,
        previous_result_skus=previous_result_skus,
    )
    if not intent or intent.intent != "query_products":
        return None
    result = await customer_agent_intent_service.process_intent_request(
        db,
        user_id=user_id,
        question=question,
        sku=sku,
        previous_result_skus=previous_result_skus,
        allow_llm_fallback=False,
    )
    if not isinstance(result, dict) or result.get("intent") != "query_products":
        return None
    return result


def _customer_mutation_boundary_result(question: str) -> dict | None:
    """Keep customer support read-only when a turn explicitly asks to alter catalogue data."""
    text = str(question or "").strip()
    normalized = customer_agent_service.normalize_search_text(text)
    if not normalized:
        return None
    update_requested = bool(re.search(
        r"(?:把|将).{1,100}?(?:修改为|更改为|更新为|改成|改为|设为|设置为)"
        r"|(?:修改|更改|更新).{0,60}?(?:为|成|改成|改为|设为|设置为)",
        text,
    ))
    delete_requested = bool(re.search(r"(?:删除|清空).{0,80}(?:产品|商品|资料|字段|SKU|CW[-\s]?[A-Z0-9])", text, re.IGNORECASE))
    if not (update_requested or delete_requested):
        # Read-only questions about internal business fields still belong to
        # the enterprise guardrail; only an explicit write/delete request is
        # handled by the customer-service mutation boundary below.
        if (
            any(term in text for term in customer_enterprise_guardrail_service.INTERNAL_BUSINESS_TERMS)
            and not _is_internal_catalogue_filter_query(text)
        ):
            guarded = customer_enterprise_guardrail_service.evaluate_question(text)
            if guarded and guarded.get("intent") == "safety_refusal":
                return guarded
        return None
    # A pronoun-based write into an internal business field must retain the
    # enterprise safety refusal.  The customer-facing mutation boundary may
    # explain a direct, explicit catalogue edit, but it must not resolve “他 /
    # 前面那款” into an internal record or allow the runtime tool path to see
    # the request.
    if (
        any(term in text for term in customer_enterprise_guardrail_service.INTERNAL_BUSINESS_TERMS)
        and re.search(r"(?:他|她|它|这个|这款|前面|刚才|上一个|该商品|该产品)", text)
    ):
        guarded = customer_enterprise_guardrail_service.evaluate_question(text)
        if guarded and guarded.get("intent") == "safety_refusal":
            return guarded
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": "当前客服只提供商品查询、比较和推荐，不会代为修改或删除商品资料；本次请求没有写入任何数据。请通过商品管理功能由有权限的人员处理。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "sources": [],
        "answer_metadata": {"source": "customer_mutation_boundary", "evidence_status": "not_applicable"},
        "debug": {"agent_mode": "customer_mutation_boundary"},
        "skip_polish": True,
    }


def _is_internal_catalogue_filter_query(question: str) -> bool:
    """Allow allowlisted operational filters to resolve product context.

    Owner and lifecycle are searchable product metadata here.  They must not
    be exposed as general business advice, but an explicit catalogue filter is
    needed to identify products before a confirmation-gated update.
    """
    text = str(question or "").strip()
    if not text or any(term in text for term in ("修改", "更改", "更新", "改成", "改为", "删除", "清空")):
        return False
    intent = customer_agent_intent_service.parse_intent(text, previous_result_skus=[])
    if not intent or intent.intent != "query_products":
        return False
    if not any(
        intent.filters.get(field)
        for field in ("product.person_in_charge", "product.lifecycle_status")
    ):
        return False
    return any(term in text for term in ("哪些", "有哪些", "产品", "商品", "查询", "查一下", "列出"))


def _looks_like_portability_detail_question(question: str) -> bool:
    """Recognize an explicit portability/carrying-field supplement."""
    text = str(question or "").strip()
    if not text:
        return False
    if any(term in text for term in ("能装", "装在", "装进", "放进", "嵌套收纳")):
        return False
    return any(term in text for term in ("便携", "便携带", "轻便", "好带", "方便携带", "随身携带"))


def _same_sku_portability_detail_answer(db: Session, sku: str) -> str:
    """Compose a bounded portability line from same-SKU structured text."""
    product, specs, business, content = _phase1_product_bundle_by_ref(db, sku)
    if not product:
        return "便携/携带：当前资料未单独标注，无法确认。"
    row = _product_row_from_model(product, specs, business, content)
    evidence_text = " ".join(
        str(row.get(key) or "")
        for key in (
            "product_name_cn", "features", "usage_scenarios", "target_audience",
            "positioning", "long_description_cn", "bullet_points",
        )
    )
    if any(term in evidence_text for term in ("便携", "轻便", "轻量")):
        return "便携/携带：同 SKU 资料标注为便携或轻量使用。"
    if any(term in evidence_text for term in ("提手", "把手", "手柄")):
        return "便携/携带：同 SKU 资料标注有提手/把手设计；具体携带便利性仍需结合实际重量。"
    if "收纳" in evidence_text:
        return "便携/携带：同 SKU 资料提到收纳方式，但未单独量化携带便利性。"
    return "便携/携带：当前资料未单独标注，无法确认。"


def _build_bounded_clarification_result(*, answer: str, source: str) -> dict:
    """Build a no-catalogue clarification for an unbound product question."""
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "sources": [],
        "evidence": [],
        "needs_clarification": True,
        "answer_metadata": {"source": source, "evidence_status": "missing"},
        "debug": {"agent_mode": source},
        "skip_polish": True,
    }


def _looks_like_unbound_thickness_question(question: str) -> bool:
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("厚度", "厚薄", "锅壁厚", "锅底厚")):
        return False
    if customer_agent_service._extract_skus(text) or SKU_RE.search(text):
        return False
    return any(term in text for term in ("锅", "锅具", "炊具", "炉", "壶", "杯"))


def _looks_like_gas_canister_value_comparison(question: str) -> bool:
    text = str(question or "").strip()
    if not text or "气罐" not in text:
        return False
    if not any(term in text for term in ("性价比", "划算", "便宜", "价格")):
        return False
    if not any(term in text for term in ("一个", "两个", "一罐", "两罐", "大小", "小气罐", "大气罐", "容量")):
        return False
    return not (customer_agent_service._extract_skus(text) or SKU_RE.search(text))


def _looks_like_ambiguous_component_comparison(question: str) -> bool:
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("炉芯", "炉心", "炉头")):
        return False
    if not any(term in text for term in ("两款", "两种", "两个", "两只", "区别", "不同", "有啥", "比较")):
        return False
    return not (
        customer_agent_service._extract_skus(text)
        or SKU_RE.search(text)
        or _products_named_in_question_from_cache(text)
    )


def _looks_like_page_extension_burner_replacement(question: str, request_sku_anchor: str | None) -> bool:
    """Recognize a page-bound replacement ask without broadening to all stoves."""
    text = str(question or "").strip()
    if not request_sku_anchor or not text:
        return False
    if not any(term in text for term in ("加长炉头", "延长炉头", "延长燃烧头", "加长燃烧头")):
        return False
    return any(term in text for term in ("替代", "代替", "替换", "换一个", "你们店有", "有吗"))


def _looks_like_page_gas_canister_availability(question: str, request_sku_anchor: str | None) -> bool:
    """Recognize a page-bound gas-canister purchase/inclusion ask.

    Compatibility questions are deliberately excluded: those must go through
    the same-SKU heat-source contract and receive a compatibility conclusion.
    """
    text = str(question or "").strip()
    if not request_sku_anchor or not text or "气罐" not in text or "气罐桌边夹" in text:
        return False
    if any(term in text for term in ("转接头", "转换头", "适配头", "接口配件")):
        return False
    if (
        customer_agent_intent_service._looks_like_canister_compatibility_question(text)
        and not customer_agent_intent_service._looks_like_canister_addon_question(text)
    ):
        return False
    return (
        customer_agent_intent_service._looks_like_canister_addon_question(text)
        or any(term in text for term in ("有没有", "有吗", "有卖", "卖吗", "单买", "增配", "加配", "随附", "配有", "包含", "买完炉子"))
    )


def _looks_like_page_gas_canister_storage_question(question: str, request_sku_anchor: str | None) -> bool:
    text = str(question or "").strip()
    if not request_sku_anchor or "气罐" not in text:
        return False
    return any(term in text for term in (
        "后备箱", "车内", "车上", "车里", "高温", "夏天", "存放", "保存", "收纳",
        "放到", "放进", "装到", "装进", "装下",
    ))


def _build_page_accessory_scope_clarification(
    *,
    request_sku_anchor: str,
    question: str,
    kind: str,
) -> dict:
    if kind == "extension_burner":
        answer = (
            f"替代结论：当前资料未标注有可替代 {request_sku_anchor} 的加长炉头或延长配件，"
            "不能把其他炉具当作替代品。请提供原配接口、型号或说明书信息，我再核对是否存在同接口配件。"
        )
        source = "page_extension_burner_replacement_scope_guard"
    elif kind == "gas_storage":
        answer = (
            f"存放结论：当前页面商品 {request_sku_anchor} 的资料未标注气罐在车内、后备箱或锅具内收纳是否安全，"
            "不能凭尺寸或外观确认可以这样放置。气罐应与炉具分离，按说明书放在阴凉通风处，远离火源、热源和阳光直晒。"
        )
        source = "page_gas_canister_storage_scope_guard"
    else:
        answer = (
            f"配件结论：当前资料未维护 {request_sku_anchor} 页面对应气罐的随附、单独购买或实时库存信息，"
            "不能把其他炉具或转接头当作气罐。请以商品页包装/配件清单或人工客服确认。"
        )
        source = "page_gas_canister_availability_scope_guard"
    result = _build_bounded_clarification_result(answer=answer, source=source)
    result.setdefault("debug", {})["request_sku_anchor"] = request_sku_anchor
    result["debug"]["question"] = str(question or "")
    return result


def _looks_like_stove_separate_purchase_question(question: str) -> bool:
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("气炉", "燃气炉", "卡式炉", "炉具", "炉子", "炉头")):
        return False
    return any(term in text for term in ("单拍", "单独", "单独买", "分开买", "分开拍", "需要另买", "另买", "另购"))


def _looks_like_page_terse_category_identity_question(question: str, request_sku_anchor: str | None) -> bool:
    """Keep a terse page-bound category noun on the current product.

    A one-word turn such as “炉子？” is often a deictic page follow-up, not
    permission to list every stove. The API SKU supplies identity; the noun
    still determines the category shown in the clarification.
    """
    text = str(question or "").strip()
    if not request_sku_anchor or not text:
        return False
    normalized = customer_agent_service.normalize_search_text(text).strip("?？!！。 ")
    category_pattern = "炉|炉子|炉具|水壶|锅|锅具|烤盘|煎盘|餐具|配件"
    return bool(re.fullmatch(
        rf"(?:这个|那个|这款|那款|它)?(?:是)?(?:{category_pattern})(?:吗|么)?",
        normalized,
    ))


def _looks_like_page_package_scope_question(question: str, request_sku_anchor: str | None) -> bool:
    """Recognize page-bound price/package wording before catalogue fallback."""
    text = str(question or "").strip()
    if not request_sku_anchor or not text:
        return False
    has_price = bool(re.search(r"\d+(?:\.\d+)?\s*元", text))
    has_package_signal = any(term in text for term in ("包装", "清单", "不含", "含不含", "只有", "包含", "配件", "炉具", "炉子"))
    return has_price and has_package_signal


def _looks_like_unbound_electromagnetic_compatibility_question(question: str) -> bool:
    """Require a product identity for a bare induction-cooker compatibility ask."""
    text = str(question or "").strip()
    if not text or "电磁炉" not in text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return False
    if any(term in text for term in ("推荐", "有哪些", "哪些", "哪款", "怎么选", "买哪个")):
        return False
    return any(term in text for term in ("是否可用", "可用吗", "能不能用", "可以用吗", "适用吗", "兼容", "适配", "能放在", "放在电磁炉"))


def _looks_like_ambiguous_electric_stove_question(question: str) -> bool:
    """Recognize the underspecified marketplace noun ``电炉``.

    ``电炉`` is used for several different heat sources.  Without a more
    precise type, recommending a gas or alcohol stove would be an unsafe
    semantic substitution, so the caller should ask for the missing type.
    """
    text = str(question or "").strip()
    if not text or "电炉" not in text:
        return False
    if any(term in text for term in ("电磁炉", "电陶炉", "电热炉", "电炉丝")):
        return False
    return any(term in text for term in ("哪个", "哪款", "可以用", "能用", "适合", "推荐", "怎么选"))


def _looks_like_unbound_cookware_compatibility_question(question: str, request_sku_anchor: str | None) -> bool:
    text = str(question or "").strip()
    if request_sku_anchor or not text:
        return False
    if not any(term in text for term in ("炒锅", "烤盘", "煎盘")):
        return False
    # A named product's size/storage question can contain “能不能放进收纳箱”
    # without asking whether an unrelated pan can be mounted on a stove. Keep
    # the formal dimension/packaging contract ahead of this unbound guard.
    if any(term in text for term in ("尺寸", "规格", "直径", "多大", "大小", "收纳箱")):
        return False
    # A bare product name such as “瓦片烤盘可以放电磁炉上吗” is a normal
    # heat-source field question. The unbound guard is for the customer's own
    # external cookware, signalled by 炒锅 or an ownership marker.
    if "炒锅" not in text and not any(term in text for term in ("我的", "自有", "自家", "自用")):
        return False
    return any(term in text for term in ("能不能放", "能否放", "可以放", "兼容", "适配", "叠放", "架在"))


def _looks_like_page_cookware_fit_question(question: str, request_sku_anchor: str | None) -> bool:
    """Recognize a page-bound vessel-fit question without inferring a SKU.

    A page SKU already seals the product identity.  Phrases about the largest
    vessel that can be placed on it are a fit/capability question, not a
    request to resolve the noun phrase before ``能放`` as a product name.
    The helper only describes the language contract; the caller still reads
    same-SKU evidence and falls back conservatively when the limit is absent.
    """
    text = str(question or "").strip()
    if not request_sku_anchor or not text:
        return False
    if _is_explicit_broad_catalogue_request(text) or _looks_like_recommendation_request(text):
        return False
    vessel_terms = ("锅", "锅具", "水壶", "烧水壶", "烤盘", "煎盘")
    fit_terms = (
        "能放", "放多大", "最大", "最小", "承载", "承重", "锅底", "底径", "口径",
        "直径", "适配", "匹配", "兼容",
    )
    return any(term in text for term in vessel_terms) and any(term in text for term in fit_terms)


def _looks_like_unresolved_page_compound_material_safety_question(
    question: str,
    inline_page_subject: str | None,
    request_sku_anchor: str | None,
) -> bool:
    """Keep both fields visible when a generic page title is unresolved."""
    text = str(question or "").strip()
    if not text or not inline_page_subject or request_sku_anchor:
        return False
    if not _looks_like_material_or_surface_detail_question(text):
        return False
    # Coating/material wording is itself classified as usage-care by the
    # legacy cleaner, but that does not mean the customer asked about safety.
    # Require an independent safety/use predicate before composing a two-field
    # material-plus-safety clarification.
    return (
        customer_agent_intent_service._looks_like_usage_care_question(text)
        and any(term in text for term in ("安全", "喝", "饮用", "煮水", "开水", "入口", "中毒", "使用"))
    )


def _looks_like_unverified_steamer_compatibility(question: str) -> bool:
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("蒸屉", "蒸笼")):
        return False
    if not any(term in text for term in ("锅", "锅具", "炊具")):
        return False
    return any(term in text for term in ("能放", "放个", "兼容", "适配", "蒸米饭", "蒸饭", "能蒸"))


def _products_named_in_question_from_cache(question: str) -> bool:
    # This lightweight check intentionally only catches explicit component
    # identities; full DB entity resolution remains the authoritative path.
    text = str(question or "")
    return bool(re.search(r"(?:炉芯|炉心|炉头)\s*[A-Za-z0-9一-龥]{1,12}\s*(?:和|与|跟|、)", text))


def _is_explicit_broad_catalogue_request(question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    if _looks_like_bundle_catalogue_request(text):
        return True
    if _looks_like_semantic_catalog_query(text):
        return True
    # Availability/listing wording with an explicit category is a catalogue
    # request, not a product-page identity question.  A named-but-unresolved
    # subject such as “激川锅” has no canonical category and therefore does
    # not enter this allowlist.
    target_category = _structured_target_category_from_question(text)
    if target_category and any(term in text for term in ("有没有", "有卖", "卖吗", "有哪些", "哪些", "多少款", "几款", "推荐", "帮我选", "帮我挑")):
        return True
    # Some category-selection wording puts the selection criterion between
    # the category noun and the recommendation predicate, e.g. “气罐品牌有
    # 推荐吗”.  Let the generic category recognizer establish scope instead
    # of treating the page product's formal field as the subject.
    if _looks_like_unconstrained_category_recommendation_question(text):
        return True
    if customer_agent_service.normalize_search_text(text).strip("？?!！。 ") in {
        "炉子", "炉具", "水壶", "水具", "锅", "锅具", "烤盘", "煎盘", "配件", "餐具"
    }:
        return True
    return any(term in text for term in ("看炉具", "看一下炉具", "单独卖炉具", "有哪些炉具", "有什么炉具", "列出炉具"))


def _looks_like_bundle_catalogue_request(question: str) -> bool:
    """Recognize a category-level bundle lookup without selecting a SKU."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if not any(term in text for term in ("套餐", "套装", "组合", "搭配")):
        return False
    if any(term in text for term in ("还有其他", "其他套装", "就这一套", "只有这一套", "哪套")):
        return any(term in text for term in ("推荐", "选择", "款式", "有没有", "有吗", "找"))
    scope_count = sum(
        1
        for term in ("水壶", "水具", "炉具", "炉子", "酒精炉", "锅", "餐具", "杯")
        if term in text
    )
    return scope_count >= 2 and any(term in text for term in ("有没有", "有哪些", "有吗", "推荐", "找", "想买"))


def _looks_like_material_catalogue_request(question: str) -> bool:
    """Recognize a material-filtered catalogue ask even with page context."""
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("锅", "锅具", "套锅", "炊具")):
        return False
    if not any(term in text for term in ("304", "316", "不锈钢")):
        return False
    if not any(term in text for term in ("有没有", "有哪些", "有卖", "卖吗", "推荐", "找")):
        return False
    return not bool(customer_agent_service._extract_skus(text))


def _looks_like_accessory_catalogue_request(question: str) -> bool:
    """Recognize a product-agnostic browse request for the accessory category."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if not any(term in text for term in ("配件", "附件")):
        return False
    if not any(term in text for term in ("哪些", "哪类", "有什么", "有没有", "有哪", "列出", "推荐")):
        return False
    storage_request = any(term in text for term in ("收纳", "整理", "装备包", "餐具包", "厨具包", "收纳袋"))
    storage_request = storage_request or bool(re.search(r"(?:配件|附件)(?:包|袋)", text))
    if storage_request:
        return True
    return bool(re.match(
        r"^\s*(?:有)?\s*(?:哪些|哪类|有什么|有哪些|有哪)\s*(?:配件|附件)"
        r"(?:产品|商品)?\s*[？?！!。.]?\s*$",
        text,
    ))


def _looks_like_material_or_surface_detail_question(question: str) -> bool:
    """Keep factual material/coating predicates out of the care shortcut.

    ``涂层`` is also a usage-care keyword (for example, how to protect a
    coating), but questions asking what the material/coating is are formal
    product-detail requests.  The distinction is semantic and deliberately
    independent of any SKU.
    """
    text = str(question or "").strip()
    if not text:
        return False
    field = customer_field_contract.detect_field_contract(text)
    if not field or field.field_type not in {"material", "surface_finish"}:
        return False
    if customer_agent_intent_service._looks_like_utensil_material_safety_question(text):
        return False
    operational_terms = (
        "怎么", "如何", "处理", "清洗", "清洁", "保养", "护理", "掉", "脱落",
        "剥落", "粘锅", "刮擦", "钢丝球", "泡水", "浸泡",
    )
    factual_predicate_terms = (
        "是什么", "哪种", "什么材质", "什么材料", "有没有", "有吗", "是否",
        "是不是", "采用", "用的", "吗", "吧",
    )
    operational_hit = any(term in text for term in operational_terms if term != "粘锅")
    # ``不粘锅/不沾锅`` is a surface-finish fact; the substring ``粘锅``
    # inside it must not turn a yes/no coating question into a cleaning ask.
    if "粘锅" in text and "不粘锅" not in text and "不沾锅" not in text:
        operational_hit = True
    return any(term in text for term in factual_predicate_terms) and not operational_hit


def _looks_like_unbound_quality_accessory_question(question: str) -> bool:
    """Recognize a quality complaint that still lacks a resolvable SKU."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return False
    has_component = any(term in text for term in ("支架", "炉架", "配件", "替换件", "附件"))
    has_quality_problem = any(
        term in text
        for term in ("变形", "变软", "变弯", "坏了", "损坏", "质量", "用了一次", "用过一次", "不耐用")
    )
    has_followup = any(term in text for term in ("有没有", "有其他", "更好", "替换", "备用", "配吗", "能买吗"))
    return has_component and has_quality_problem and has_followup


def _looks_like_unbound_pairing_identity_question(question: str) -> bool:
    """Require the original product identity for a standalone pairing ask."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return False
    has_pairing = any(term in text for term in ("配套", "匹配", "适配", "配哪个", "配什么", "能配"))
    has_target = any(term in text for term in ("气炉", "炉具", "炉子", "水壶", "烧水壶", "锅具", "烤盘"))
    return has_pairing and has_target


def _looks_like_unbound_historical_fuel_question(question: str) -> bool:
    """Avoid guessing a past purchase's fuel type from a broad stove list."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return False
    return (
        any(term in text for term in ("上次", "以前", "之前", "买的", "购买的"))
        and any(term in text for term in ("炉", "炉具", "气炉", "酒精炉"))
        and any(term in text for term in ("工业酒精", "酒精", "燃料", "热源"))
    )


def _looks_like_unbound_installation_support_question(question: str) -> bool:
    """Require product identity for an installation/fitment fault report."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return False
    has_installation_signal = any(
        term in text
        for term in ("安装", "装不上", "拧不上", "孔太小", "拧不进去", "怎么卡", "卡进去", "接口", "接不上")
    )
    has_product_signal = any(term in text for term in ("炉", "炉具", "炉头", "气炉", "气罐", "锅", "水壶", "配件"))
    return has_installation_signal and has_product_signal


def _looks_like_missing_component_question(question: str) -> bool:
    """Recognize a missing-in-box complaint without choosing a product."""
    text = str(question or "").strip()
    if not text:
        return False
    has_missing = any(term in text for term in (
        "没收到", "没发", "没给", "怎么没", "少了", "缺少", "漏发", "没配",
        "不是一起到", "没有一起到", "没一起到", "未一起到", "未随货", "没随货",
    ))
    has_component = any(term in text for term in (
        "杯子", "水壶", "配件", "赠品", "套装", "炉具", "转接头", "转换头", "转向头", "转头",
        "连接管", "管线", "气管", "软管",
    ))
    return has_missing and has_component


def _looks_like_unbound_cause_and_cleaning_question(question: str) -> bool:
    """Recognize an identity-free compound cause plus cleaning question."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    has_cause = any(term in text for term in ("酒精的问题", "酒精导致", "是不是酒精", "为什么会这样", "原因"))
    has_cleaning = any(term in text for term in ("怎么清洗", "如何清洗", "清洗干净", "怎么清洁", "如何清洁"))
    has_material_or_surface = any(term in text for term in ("材质", "材料", "涂层", "污渍", "黑", "熏"))
    return has_cause and has_cleaning and has_material_or_surface


def _looks_like_multi_product_replacement_accessory_question(question: str) -> bool:
    """Recognize replacement shopping for accessories belonging to two items."""
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("收纳袋", "收纳包", "袋子", "包装袋")):
        return False
    if not any(term in text for term in ("丢了", "丢失", "不见", "没有了", "怎么买", "怎么补", "哪里买")):
        return False
    product_families = (
        ("烧水壶", "水壶", "烧壶水"),
        ("酒精炉", "酒精炉头", "炉具"),
        ("锅具", "套锅", "单锅"),
    )
    matched_families = sum(any(term in text for term in family) for family in product_families)
    return matched_families >= 2


def _looks_like_mixed_stove_fuel_catalogue_question(question: str) -> bool:
    """Recognize a catalogue question that asks for alcohol and gas stoves."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    has_alcohol = any(term in text for term in ("酒精炉", "酒精炉头"))
    has_gas = any(term in text for term in ("瓦斯炉", "燃气炉", "气炉", "卡式炉"))
    has_catalogue_predicate = any(term in text for term in ("包括", "有哪些", "有吗", "有没有", "都有什么", "啥"))
    return has_alcohol and has_gas and has_catalogue_predicate


def _looks_like_unbound_package_contents_question(question: str) -> bool:
    """Recognize a package-content yes/no ask without inventing a product."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return False
    has_single_item_claim = any(term in text for term in (
        "只有锅", "只是锅", "只是一口锅", "单独一个锅", "一个锅", "一个单锅", "只有一个锅", "除了锅",
    ))
    has_exclusion_claim = any(term in text for term in ("没有其他东西", "没有其他", "不含其他", "不带其他", "没别的", "就一个"))
    has_confirmation_marker = any(term in text for term in ("吗", "是吧", "是不是", "是否", "?", "？"))
    return has_confirmation_marker and (has_single_item_claim or has_exclusion_claim)


def _looks_like_stove_support_compatibility_question(question: str) -> bool:
    """Recognize a support-bracket/cookware fitment question.

    A request such as “which pot can skip this bracket” is a compatibility
    question, not an unconstrained cookware recommendation.  The predicate
    is intentionally based on the customer language (stove + vessel + support
    relation); it does not name a product or choose a SKU.
    """
    text = str(question or "").strip()
    if not text:
        return False
    has_support = any(term in text for term in ("支架", "炉架", "支撑架", "支撑"))
    has_stove = any(term in text for term in ("炉", "炉具", "炉子", "酒精炉", "气炉", "卡式炉"))
    has_vessel = any(term in text for term in ("锅", "锅具", "锅子", "煎盘", "烤盘", "水壶"))
    has_relation = any(term in text for term in ("不用", "不需要", "必用", "必须", "哪个", "哪款", "能不能", "可以不"))
    return has_support and has_stove and has_vessel and has_relation


def _has_only_generic_product_subject(question: str) -> bool:
    """Whether a duration/usage question names only a broad product category."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return False
    subject = _product_like_scope_subject(text) or customer_agent_intent_service._detail_subject_from_question(text)
    subject = str(subject or "").strip()
    if not subject:
        return False
    return (
        _is_generic_category_phrase(subject)
        or subject in {"气罐", "燃料", "酒精", "酒精炉", "卡式炉", "气炉"}
        or _is_generic_modifier_prefixed_product_scope(subject)
    )


def _unresolved_named_product_subject(db: Session | None, question: str) -> str:
    """Extract an explicit but unresolved product-like subject before “的”."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text) or customer_agent_service._extract_skus(text):
        return ""
    # A catalogue sentence such as “列出支持酒精炉的锅具” has a ``的``
    # boundary, but the text before it is a predicate fragment, not a named
    # product.  Let the catalogue/semantic route own it instead of treating
    # that fragment as an unresolved identity.
    if _is_explicit_broad_catalogue_request(text):
        return ""
    match = re.match(r"^\s*(?P<subject>[^，,。！？?!]{2,32})\s*的", text)
    if not match:
        return ""
    subject = re.sub(r"^(?:你好|您好)\s*[,，:：]?\s*", "", match.group("subject")).strip()
    subject = re.sub(r"^(?:这个|那个|这款|那款|该商品|该产品)\s*", "", subject).strip()
    # First-person purchase wording and deictic follow-ups are not explicit
    # product names.  The former should stay on the generic usage/material
    # route; the latter must be resolved from conversation context.
    if re.match(
        r"^(?:我|你|他|她|咱们|我们|顾客|客户|另外|再|想|要|买|购买|推荐|列出|支持|适合|可以|能否|能不能|是否|有没有|有|请|帮我)",
        subject,
    ):
        return ""
    if re.match(
        r"^(?:前面|刚才|之前|上次|上一轮|上一个|前一个|前一款|最后一个|最后一款|第[一二三四五六七八九十0-9])",
        subject,
    ):
        return ""
    if not subject or _is_generic_category_phrase(subject) or _is_generic_modifier_prefixed_product_scope(subject):
        return ""
    if not any(term in subject for term in ("锅", "炉", "壶", "杯", "盘", "罐", "配件", "餐具")):
        return ""
    if db is not None and _resolve_exact_page_subject_sku(db, subject):
        return ""
    return subject if _looks_like_product_like_scope_subject(subject) else ""


def _looks_like_page_bundle_inclusion_question(question: str) -> bool:
    text = str(question or "").strip()
    return bool(
        "带锅" in text
        and any(term in text for term in ("有没有", "有吗", "带锅的", "不带锅"))
    )


def _looks_like_bundle_heat_source_question(question: str) -> bool:
    """Recognize a bundle ask about whether a stove is included.

    A phrase such as “有没有带炉的套装” is neither a generic cookware
    browse nor a page-field question.  Treating it as an unconstrained search
    lets the catalogue formatter dump ordinary cookware sets that never claim
    to include a stove.  This semantic scope check deliberately uses bundle
    and inclusion language, not any SKU or product-name whitelist.
    """
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    has_bundle = any(term in text for term in ("套装", "套锅", "套餐", "组合"))
    has_inclusion = any(term in text for term in ("带炉", "含炉", "有炉", "配炉", "炉具在里面"))
    return has_bundle and has_inclusion and any(term in text for term in ("有没有", "有吗", "哪些", "哪款", "推荐", "卖"))


def _looks_like_unbound_product_purchase_question(question: str) -> bool:
    """Recognize an underspecified product-buying ask without inventing a SKU.

    ``杯子怎么买`` is a product-selection/purchase question, but it does not
    identify a concrete item or a channel. Leaving it to a stochastic LLM
    route can attach an unrelated kettle or utensil card. This boundary asks
    for the missing identity/selection dimensions instead of guessing.
    """
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if any(term in text for term in ("购买渠道", "销售渠道", "购买链接", "官网", "旗舰店", "哪里买", "哪儿买", "去哪买", "哪里可以买到", "可以到哪里买", "在哪买", "在哪里买", "下单")):
        return False
    if not any(term in text for term in ("怎么买", "想买", "买到", "买一个", "买一款")):
        return False
    if any(term in text for term in (
        "推荐", "哪款", "哪个", "怎么选", "选什么", "适合", "野餐", "露营", "徒步",
        "一个人", "一人", "两个人", "双人", "家庭", "周末", "场景", "预算", "轻量", "便携",
    )):
        return False
    return any(
        term in text
        for term in (
            "杯子", "水杯", "水壶", "茶壶", "锅具", "套锅", "锅", "炉具", "炉子", "酒精炉",
            "卡式炉", "气炉", "餐具", "收纳包", "收纳袋", "配件",
        )
    )


def _looks_like_unconstrained_category_recommendation_question(question: str) -> bool:
    """Recognize a plain category recommendation with no personal criteria."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    # Include natural category-selection wording such as “用什么样的炉头”
    # and “用哪种炉具”.  This is a language-level intent boundary; it does
    # not identify a particular SKU or promote a fixed product.
    if not any(term in text for term in (
        "推荐", "哪款", "哪个好", "哪个", "买哪个", "选哪个", "用什么样", "用哪种", "哪种",
    )):
        return False
    if any(term in text for term in (
        "适合", "徒步", "露营", "野餐", "自驾", "一个人", "一人", "两个人", "双人", "多人",
        "新手", "预算", "轻量", "便携", "容量", "重量", "收纳", "材质", "火力", "烧水", "煮饭",
        "能不能", "能否", "能用", "能配", "搭配", "配合", "适配", "兼容", "接口", "用什么燃料",
    )):
        return False
    return _semantic_catalog_product_ref(text) != "产品"


def _looks_like_price_banded_package_inclusion_question(question: str) -> bool:
    """Recognize a price-band package-inclusion ask without inventing a SKU.

    Marketplace chat often shortens a price expression to “400多套装” or
    “四百左右的套装”.  The phrase is still a price constraint, not a product
    identity.  When the catalogue has no maintained price mapping, returning a
    broad semantic result cannot prove which package belongs to that band.
    """
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if not any(term in text for term in ("套装", "套锅", "套餐", "组合")):
        return False
    if not any(term in text for term in (
        "里面", "里", "包含", "包括", "带不带", "带有", "带水壶", "有水壶", "有没有水壶",
    )):
        return False
    explicit_band = re.search(
        r"(?:\d{2,6}\s*(?:多|左右|上下|元|块|价位)|"
        r"[一二两三四五六七八九十百千万]+\s*(?:多|左右|元|块)|"
        r"几百(?:多|左右)?|百元(?:左右|以内)?)",
        text,
    )
    return bool(explicit_band) or any(term in text for term in ("预算", "价位", "价格区间"))


def _explicit_category_recommendation_result(db: Session, question: str) -> dict | None:
    """Build a bounded category recommendation when no extra criteria exist."""
    product_ref = _semantic_catalog_product_ref(question)
    if product_ref == "产品":
        return None
    rows = _phase1_catalog_rows(db, product_ref)
    if product_ref == "配件":
        # The accessory category is intentionally broad.  When the customer
        # names a concrete accessory noun, keep only rows whose maintained
        # evidence contains that same semantic target; otherwise a condiment
        # kit or water bag can masquerade as a storage/burner recommendation.
        row_text = lambda row: _structured_query_row_text(row)
        if any(term in question for term in ("收纳包", "收纳袋", "装备包", "野炊包", "餐具包", "厨具包")):
            rows = [
                row for row in rows
                if any(term in row_text(row) for term in ("收纳包", "收纳袋", "装备包", "野炊包", "餐具包", "厨具包", "收纳"))
            ]
        elif _looks_like_standalone_burner_head_accessory_request(question):
            rows = [
                row for row in rows
                if any(term in row_text(row) for term in ("炉头", "炉芯", "炉心", "燃烧头"))
            ]
    if product_ref == "炉具" and "卡式炉" in question:
        # Match the requested subject against the product name or the
        # structured heat-source field only.  Marketing prose such as an
        # alcohol stove's “火力同卡式炉” must not make it a card-stove hit.
        scoped = [
            row for row in rows
            if (
                "卡式炉" in " ".join(
                    str(row.get(key) or "").strip()
                    for key in ("product_name_cn", "product_name_en")
                )
                or "卡式炉" in " ".join(
                    str(row.get(key) or "").strip()
                    for key in ("heat_source", "applicable_heat_source")
                )
            )
        ]
        rows = scoped
    elif product_ref == "炉具" and "酒精炉" in question:
        scoped = [
            row for row in rows
            if "酒精炉" in _structured_query_row_text(row) or "酒精" in _structured_query_row_text(row)
        ]
        rows = scoped or rows
    elif product_ref == "水具" and any(term in question for term in ("杯子", "水杯")):
        scoped = [
            row for row in rows
            if any(term in _structured_query_row_text(row) for term in ("杯", "水具"))
        ]
        rows = scoped or rows
    if product_ref == "锅具" and any(term in question for term in ("套装", "套锅", "套餐", "组合")):
        # “套装还有其他推荐吗” is a set-level request.  Do not let a
        # cookware category search select a single pot or kettle as the top
        # recommendation merely because those rows ranked higher semantically.
        bundle_rows = [
            row
            for row in rows
            if any(
                term in _structured_query_row_text(row)
                for term in ("套装", "套锅", "锅具套装", "炊具套装", "组合套装")
            )
        ]
        rows = bundle_rows
    # Category recommendations are customer-facing choices, so lifecycle
    # rows that are explicitly unavailable must not be presented as purchase
    # candidates.  This is a shared lifecycle contract, not a SKU allow/deny
    # list.
    rows = [row for row in rows if _multi_category_recommendation_row_is_sellable(row)]
    # Category wording is still a recommendation request.  Apply the shared
    # semantic/evidence contract before rendering cards so an explicit
    # subtype such as “固体酒精” cannot fall through to a neighbouring gas
    # or liquid-fuel product merely because its marketing text mentions
    # alcohol.  This is a generic evidence boundary, not a SKU rule.
    recommendation_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(question)
    verifications = []
    if (
        recommendation_contract.hard_constraints
        or recommendation_contract.subject_category
        or recommendation_contract.subject_subtype
        or recommendation_contract.heat_sources
    ):
        verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(
            recommendation_contract,
            rows,
        )
        rows = customer_recommendation_verification_contract.select_recommendation_candidates(
            rows,
            verifications,
        )
    if not rows:
        # The user has made a recommendation request even when this catalogue
        # has no verified row in the requested category.  Preserve that intent
        # (and the recommendation answer contract) instead of turning a valid
        # new question into a clarification slot.  This mirrors the existing
        # post-filter no-match path: no cards are shown, but the customer sees
        # a direct, bounded no-match answer.
        requested_fuel_label = "、".join(
            str(value or "").strip()
            for value in (
                recommendation_contract.fuel_subtypes
                or ([recommendation_contract.fuel_subtype] if recommendation_contract.fuel_subtype else [])
            )
            if str(value or "").strip()
        )
        no_match_subject = (
            f"明确支持{requested_fuel_label}的{product_ref}"
            if requested_fuel_label
            else f"可核对的{product_ref}"
        )
        result = _build_bounded_clarification_result(
            answer=f"当前资料中没有找到{no_match_subject}推荐候选；请补充具体商品名或 SKU。",
            source="explicit_category_recommendation_no_match",
        )
        result["intent"] = "recommendation"
        result["answer_type"] = "recommendation"
        result["needs_clarification"] = False
        result["answer_metadata"] = {
            **dict(result.get("answer_metadata") or {}),
            "evidence_status": "missing",
            "post_filter_no_match": True,
        }
        result["debug"] = {
            **dict(result.get("debug") or {}),
            "recommendation_post_filter_applied": True,
            "recommendation_post_filter_matched_count": 0,
            "agent_mode": "explicit_category_recommendation_no_match",
        }
        return result
    selected = rows[:5]
    result = _structured_product_query_result(
        question=question,
        product_ref=product_ref,
        rows=selected,
        source="explicit_category_recommendation_guard",
        reason_label="明确品类推荐",
        answer_type="recommendation",
        metadata_source="explicit_category_recommendation_guard",
    )
    if result:
        result["intent"] = "recommendation"
        if verifications:
            result["answer"] = customer_recommendation_verification_contract.build_verified_recommendation_answer(
                recommendation_contract,
                selected,
                verifications,
                total_match_count=len(rows),
            )
            result["skip_polish"] = True
            result.setdefault("answer_metadata", {})["recommendation_contract"] = recommendation_contract.to_dict()
    return result


def _exact_category_availability_result(db: Session, question: str) -> dict | None:
    """Answer a specific catalogue subtype availability question directly."""
    subject = _specific_category_availability_subject(question)
    if not subject:
        return None
    rows = [
        row for row in _phase1_catalog_rows(db, "产品")
        if _multi_category_recommendation_row_is_sellable(row)
        and subject in _structured_query_row_text(row)
    ]
    if not rows:
        result = _build_bounded_clarification_result(
            answer=(
                f"当前精确目录中没有“{subject}”商品；"
                f"未找到名称、品类或功能明确标注为“{subject}”的在售资料。"
            ),
            source="exact_category_availability_no_match",
        )
        result.update({
            "intent": "product_query",
            "answer_type": "product_query",
            "needs_clarification": False,
        })
        return result

    selected = rows[:10]
    names = [
        f"{str(row.get('product_name_cn') or row.get('product_name_en') or row.get('sku') or '').strip()}"
        f"（{str(row.get('sku') or '').strip().upper()}）"
        for row in selected
    ]
    skus = [
        str(row.get("sku") or "").strip().upper()
        for row in selected
        if str(row.get("sku") or "").strip()
    ]
    return {
        "intent": "product_query",
        "answer_type": "product_query",
        "answer": f"当前精确目录中找到 {len(selected)} 款“{subject}”商品：{'、'.join(names)}。",
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "sources": [],
        "evidence": [],
        "needs_clarification": False,
        "answer_metadata": {"source": "exact_category_availability"},
        "debug": {"agent_mode": "exact_category_availability", "raw_results": selected},
        "skip_polish": True,
    }


def _requested_accessory_combination_groups(question: str) -> list[tuple[str, tuple[str, ...]]]:
    text = str(question or "").strip()
    definitions = (
        ("点火器", ("点火器", "点火装置", "电子点火", "点火")),
        ("火力调节开关", ("调节开关", "火力调节", "调节旋钮", "火力阀")),
        ("转接头/转换头", ("转接头", "转换头", "适配头", "转接器")),
        ("连接管/管线", ("管线", "连接管", "连接软管", "气管", "软管")),
    )
    return [(label, terms) for label, terms in definitions if any(term in text for term in terms)]


def _accessory_combination_availability_result(db: Session, question: str) -> dict | None:
    groups = _requested_accessory_combination_groups(question)
    has_combination_predicate = any(
        term in str(question or "")
        for term in ("一起", "组合", "同时", "一体", "一套")
    )
    if (
        len(groups) < 2
        or not has_combination_predicate
        or not _is_unbound_accessory_availability_question(question)
    ):
        return None
    rows = [
        row for row in _phase1_catalog_rows(db, "配件")
        if _multi_category_recommendation_row_is_sellable(row)
        and all(any(term in _structured_query_row_text(row) for term in terms) for _, terms in groups)
    ]
    labels = "、".join(label for label, _ in groups)
    if not rows:
        result = _build_bounded_clarification_result(
            answer=(
                f"当前精确配件目录未找到同时明确标注{labels}的组合商品，"
                "不能用只包含其中一项的配件替代。若要核对某款炉具的兼容配件，请再提供炉具 SKU 和接口型号。"
            ),
            source="accessory_combination_availability_no_match",
        )
        result.update({"intent": "product_query", "answer_type": "product_query", "needs_clarification": False})
        return result
    selected = rows[:10]
    skus = [str(row.get("sku") or "").strip().upper() for row in selected if str(row.get("sku") or "").strip()]
    names = [
        f"{str(row.get('product_name_cn') or row.get('product_name_en') or row.get('sku') or '').strip()}（{str(row.get('sku') or '').strip().upper()}）"
        for row in selected
    ]
    return {
        "intent": "product_query",
        "answer_type": "product_query",
        "answer": f"当前精确配件目录找到同时明确标注{labels}的商品：{'、'.join(names)}。",
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "sources": [],
        "evidence": [],
        "needs_clarification": False,
        "answer_metadata": {"source": "accessory_combination_availability"},
        "debug": {"agent_mode": "accessory_combination_availability", "raw_results": selected},
        "skip_polish": True,
    }


def _mixed_stove_fuel_catalogue_result(db: Session, question: str) -> dict | None:
    if not _looks_like_mixed_stove_fuel_catalogue_question(question):
        return None
    rows = [
        row for row in _phase1_catalog_rows(db, "炉具")
        if _multi_category_recommendation_row_is_sellable(row)
    ]
    alcohol_rows: list[dict] = []
    gas_rows: list[dict] = []
    for row in rows:
        heat_source = str(row.get("heat_source") or "").strip()
        if customer_agent_intent_service._alcohol_stove_support_verdict(heat_source) is True:
            alcohol_rows.append(row)
        if any(term in heat_source for term in ("气罐", "燃气", "瓦斯", "卡式")):
            gas_rows.append(row)
    selected_by_sku: dict[str, dict] = {}
    for row in (*alcohol_rows, *gas_rows):
        sku = str(row.get("sku") or "").strip().upper()
        if sku:
            selected_by_sku.setdefault(sku, row)
    selected = list(selected_by_sku.values())[:10]
    skus = list(selected_by_sku)[:10]

    def render_group(label: str, group_rows: list[dict]) -> str:
        if not group_rows:
            return f"{label}：当前目录没有同 SKU 热源证据可确认。"
        items = [
            f"{str(row.get('product_name_cn') or row.get('product_name_en') or row.get('sku') or '').strip()}"
            f"（{str(row.get('sku') or '').strip().upper()}，热源：{str(row.get('heat_source') or '未标注').strip()}）"
            for row in group_rows[:5]
        ]
        return f"{label}：{'、'.join(items)}。"

    answer = "\n".join((
        "目录结论：酒精炉与瓦斯/燃气炉需要按各自同 SKU 的热源字段分别核对，不能把气罐炉统一说成两类都没有。",
        render_group("酒精炉", alcohol_rows),
        render_group("瓦斯/燃气炉", gas_rows),
        "以上回答的是产品目录有无；如果你问的是某个套装是否同时包含两种炉具，请提供该套装 SKU 再核对包装清单。",
    ))
    return {
        "intent": "query_products",
        "answer_type": "product_query",
        "answer": answer,
        "results": selected,
        "result_skus": skus,
        "candidate_skus": skus,
        "sources": [],
        "evidence": [],
        "needs_clarification": False,
        "answer_metadata": {"source": "mixed_stove_fuel_catalogue"},
        "debug": {"agent_mode": "mixed_stove_fuel_catalogue", "raw_results": selected},
        "skip_polish": True,
    }


def _bundle_heat_source_catalogue_result(db: Session, question: str) -> dict:
    """Return only bundles whose own evidence explicitly includes a stove."""
    rows = _phase1_catalog_rows(db, "锅具")
    explicit_rows: list[dict] = []
    for row in rows:
        haystack = " ".join(
            str(row.get(key) or "")
            for key in (
                "product_name_cn",
                "product_name_en",
                "category",
                "features",
                "usage_scenarios",
                "positioning",
                "long_description_cn",
            )
        )
        # “支持/兼容某种炉具” is a heat-source capability, not proof that
        # the stove is included in the package.  Only inclusion wording passes.
        if any(term in haystack for term in ("带炉", "含炉", "炉具套装", "火锅炉套装", "配炉具")):
            explicit_rows.append(row)
    if explicit_rows:
        names = []
        for row in explicit_rows[:8]:
            sku = str(row.get("sku") or "").strip().upper()
            name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
            names.append(f"{name}（{sku}）" if sku else name)
        result = _structured_product_query_result(
            question=question,
            product_ref="含炉套装",
            rows=explicit_rows[:8],
            source="explicit_bundle_heat_source_catalogue",
            reason_label="明确标注含炉的套装",
            answer_type="product_query",
            metadata_source="explicit_bundle_heat_source_catalogue",
        )
        if result:
            result["answer"] = "当前资料明确标注含炉的套装有：" + "、".join(names) + "。"
            return result
    return _build_bounded_clarification_result(
        answer=(
            "当前资料中未找到明确标注‘套装含炉’的商品；普通套锅或锅具套装不能仅凭名称推断随附炉具。"
            "如果你有具体商品名或 SKU，我可以再按该款包装清单核对。"
        ),
        source="bundle_heat_source_inclusion_unverified",
    )


def _answer_negates_requested_noun(question: str, answer: str) -> bool:
    """Detect an exact availability no-match before showing alternatives.

    A response may mention nearby products as explanatory examples while
    explicitly saying the requested item was not found.  Those examples are
    not customer-facing result cards for the requested item.  This helper
    extracts the noun after a generic availability prefix and checks that the
    same noun occurs near a negation in the answer; it never selects a SKU.
    """
    text = str(question or "").strip()
    response = str(answer or "").strip()
    if not text or not response:
        return False
    if not any(marker in response for marker in ("暂未查到", "未找到", "没有找到", "没有出现", "未出现", "暂无", "暂未")):
        return False
    noun = re.sub(r"^[\s，,：:、]*(?:有没有|有无|是否有|有吗|能否有|有卖)", "", text)
    noun = re.split(r"(?:推荐|能买吗|卖吗|有吗|吗|呢|吧|呀|啊|\?|？)", noun, maxsplit=1)[0]
    noun = noun.strip(" ，,。！？；;：:")
    if len(noun) < 2:
        return False
    for match in re.finditer(re.escape(noun), response):
        window = response[max(0, match.start() - 28): match.end() + 28]
        if any(marker in window for marker in ("暂未查到", "未找到", "没有找到", "没有出现", "未出现", "暂无", "暂未", "没有")):
            return True
    return False


def _is_unbound_knowledge_meta_preplan(preplan: dict | None) -> bool:
    """Keep an unscoped knowledge-base question out of catalogue fallbacks."""
    if not isinstance(preplan, dict):
        return False
    return bool(
        _semantic_preplan_confident(preplan, minimum=0.9)
        and (
            _semantic_preplan_route_family(preplan) == "knowledge_base_meta"
            or str(preplan.get("information_scope") or "").strip() == "knowledge_base_meta"
        )
        and not (preplan.get("entities") or [])
        and not (preplan.get("canonical_fields") or [])
    )


def _clear_unrelated_catalogue_cards(question: str, agent_result: dict) -> dict:
    """Remove broad retrieval cards from a non-browse, uncertain answer.

    Retrieval may still be useful internally, but a customer-facing detail or
    safety answer must not display dozens of unrelated products.  Explicit
    browse/recommend/compare turns keep their candidate set.
    """
    if not isinstance(agent_result, dict):
        return agent_result
    def _filter_cards_to_answer_skus(answer_skus: list[str]) -> None:
        """Align visible cards with the SKU identities actually named in text."""
        allowed = {str(item or "").strip().upper() for item in answer_skus if str(item or "").strip()}
        if not allowed:
            return
        allowed_identity = {_sku_identity_key(item) for item in allowed if _sku_identity_key(item)}
        existing = {
            str(item or "").strip().upper()
            for item in (agent_result.get("result_skus") or [])
            if str(item or "").strip()
        }
        if not any(_sku_identity_key(item) in allowed_identity for item in existing):
            return
        for key in ("result_skus", "candidate_skus"):
            values = [
                str(item or "").strip().upper()
                for item in (agent_result.get(key) or [])
                if str(item or "").strip()
            ]
            agent_result[key] = [item for item in values if _sku_identity_key(item) in allowed_identity]
        rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
        agent_result["results"] = [
            row
            for row in rows
            if _sku_identity_key(str(row.get("sku") or "").strip().upper()) in allowed_identity
        ]
        sources = agent_result.get("sources")
        if isinstance(sources, list):
            agent_result["sources"] = [
                source
                for source in sources
                if not isinstance(source, dict)
                or not str(source.get("sku") or "").strip()
                or _sku_identity_key(str(source.get("sku") or "").strip().upper()) in allowed_identity
            ]

    answer = str(agent_result.get("answer") or "")
    answer_skus = [
        str(item or "").strip().upper()
        for item in customer_agent_service._extract_skus(answer)
        if str(item or "").strip()
    ]
    result_skus = [
        str(item or "").strip().upper()
        for item in (agent_result.get("result_skus") or [])
        if str(item or "").strip()
    ]
    answer_metadata = agent_result.get("answer_metadata")
    metadata_source = str(
        answer_metadata.get("source") if isinstance(answer_metadata, dict) else ""
    ).strip()
    explicit_target_category = _structured_target_category_from_question(question)
    if explicit_target_category == "烤盘" and not _phase1_is_griddle_vs_cookware_scenario(question):
        rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
        griddle_rows = [
            row
            for row in rows
            if any(
                term in " ".join(
                    str(row.get(key) or "")
                    for key in ("product_name_cn", "product_name_en", "category", "capacity", "features")
                ).lower()
                for term in ("烤盘", "煎盘", "griddle")
            )
        ]
        if griddle_rows:
            allowed_skus = {
                str(row.get("sku") or "").strip().upper()
                for row in griddle_rows
                if str(row.get("sku") or "").strip()
            }
            agent_result["results"] = griddle_rows
            agent_result["result_skus"] = [
                str(row.get("sku") or "").strip().upper()
                for row in griddle_rows
                if str(row.get("sku") or "").strip()
            ]
            agent_result["candidate_skus"] = list(agent_result["result_skus"])
            if isinstance(agent_result.get("sources"), list):
                agent_result["sources"] = [
                    source
                    for source in agent_result["sources"]
                    if not isinstance(source, dict)
                    or not str(source.get("sku") or "").strip()
                    or str(source.get("sku") or "").strip().upper() in allowed_skus
                ]
            if isinstance(agent_result.get("evidence"), list):
                agent_result["evidence"] = [
                    evidence
                    for evidence in agent_result["evidence"]
                    if not isinstance(evidence, dict)
                    or not str(evidence.get("sku") or "").strip()
                    or str(evidence.get("sku") or "").strip().upper() in allowed_skus
                ]
            debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
            if isinstance(debug.get("raw_results"), list):
                debug["raw_results"] = griddle_rows
            debug["explicit_target_category_scope"] = "烤盘"
            agent_result["debug"] = debug
            answer = str(agent_result.get("answer") or "")
            outside_answer = any(
                str(row.get("sku") or "").strip().upper() not in allowed_skus
                and any(
                    identity and identity.upper() in answer.upper()
                    for identity in (
                        str(row.get("sku") or "").strip(),
                        str(row.get("product_name_cn") or "").strip(),
                        str(row.get("product_name_en") or "").strip(),
                    )
                )
                for row in rows
            )
            if outside_answer or not any(
                identity and identity.upper() in answer.upper()
                for row in griddle_rows
                for identity in (
                    str(row.get("sku") or "").strip(),
                    str(row.get("product_name_cn") or "").strip(),
                    str(row.get("product_name_en") or "").strip(),
                )
            ):
                top = griddle_rows[0]
                top_name = top.get("product_name_cn") or top.get("sku")
                top_sku = str(top.get("sku") or "").strip().upper()
                top_reason = str(
                    top.get("usage_scenarios")
                    or top.get("features")
                    or top.get("positioning")
                    or "匹配当前露营场景"
                ).strip("。；; ")
                rebuilt = f"优先推荐 {top_name}（{top_sku}），{top_reason}。"
                backups = [
                    f"{row.get('product_name_cn') or row.get('sku')}（{str(row.get('sku') or '').strip().upper()}）"
                    for row in griddle_rows[1:3]
                ]
                if backups:
                    rebuilt += " 也可以看" + "、".join(backups) + "。"
                agent_result["answer"] = rebuilt
            answer_skus = [
                str(item or "").strip().upper()
                for item in customer_agent_service._extract_skus(str(agent_result.get("answer") or ""))
                if str(item or "").strip()
            ]
    # A structured recommendation may be useful even when no candidate proves
    # every requested dimension.  Its answer explicitly labels the rows as
    # reference-only; keep those evidence-bounded cards aligned with the text
    # instead of treating the uncertainty notice as a non-catalogue answer.
    if (
        metadata_source == "product_catalog_structured_recommendation"
        and str(answer_metadata.get("verification_level") or "").strip() == "partially_verified"
        and any(
            marker in answer
            for marker in ("仅供参考", "部分条件缺少资料", "未发现明确冲突")
        )
    ):
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["catalogue_cards_preserved"] = True
        debug["catalogue_cards_preserve_reason"] = "structured_partial_recommendation_reference_only"
        agent_result["debug"] = debug
        return agent_result
    if metadata_source == "mixed_stove_fuel_catalogue":
        if answer_skus:
            _filter_cards_to_answer_skus(answer_skus)
        return agent_result
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    sealed_single_product_safe_missing = bool(
        str(debug.get("agent_mode") or "") == "sealed_product_qa_safe_missing"
        and len(result_skus) == 1
        and any(
            _sku_identity_key(answer_sku) == _sku_identity_key(result_skus[0])
            for answer_sku in answer_skus
        )
    )
    if (
        not sealed_single_product_safe_missing
        and _answer_negates_requested_noun(question, answer)
        and result_skus
    ):
        agent_result["results"] = []
        agent_result["result_skus"] = []
        agent_result["candidate_skus"] = []
        agent_result["sources"] = []
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["catalogue_cards_cleared"] = True
        debug["catalogue_cards_clear_reason"] = "exact_requested_noun_not_found"
        debug["catalogue_cards_cleared_count"] = len(result_skus)
        if isinstance(debug.get("raw_results"), list):
            debug["raw_results"] = []
        agent_result["debug"] = debug
        return agent_result
    if _looks_like_page_current_item_availability_question(question):
        page_match = re.search(r"当前商品\s*[：:]\s*(.+?)(?:）|\))\s*$", str(question or "").strip())
        page_subject = str(page_match.group(1) if page_match else "当前页面商品").strip()
        component = next(
            (
                term
                for term in ("炉头", "炉芯", "配件", "水壶", "锅", "炉具")
                if term in str(question or "")
            ),
            "该配件",
        )
        agent_result["intent"] = "clarify"
        agent_result["answer_type"] = "clarification"
        agent_result["answer"] = (
            f"{page_subject}：当前页面资料未标注是否包含或单独提供“{component}”，"
            "不能把其他炉具或目录中的配件当作这款商品的随附部件。请按该商品包装清单或配件说明核对。"
        )
        agent_result["needs_clarification"] = True
        agent_result["result_skus"] = []
        agent_result["candidate_skus"] = []
        agent_result["results"] = []
        agent_result["sources"] = []
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["agent_mode"] = "natural_page_current_item_availability_scope_guard"
        debug["catalogue_cards_cleared"] = True
        debug["catalogue_cards_clear_reason"] = "page_current_item_availability"
        agent_result["debug"] = debug
        return agent_result
    # Keep an explicit fuel subtype from degrading into the generic gas-stove
    # safety paragraph.  If one verified row is present, answer from that
    # row's heat-source evidence; otherwise state that the subtype is
    # unverified.  Recommendation turns are left to the recommendation
    # contract, which may return a verified no-match result.
    explicit_fuel_terms = ("固体酒精", "固态酒精", "酒精块", "固体燃料", "液体酒精", "木炭", "炭火", "柴火", "煤炭")
    requested_fuels = _requested_fuel_capabilities(question)
    answer_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    same_sku_product_qa = bool(
        str(answer_metadata.get("evidence_field") or "").strip() == "product_qa"
        and str(answer_metadata.get("evidence_source") or "").strip().startswith("product_qa:")
        and len(result_skus) == 1
    )
    if (
        requested_fuels
        and not _is_unbound_environmental_fuel_safety_question(question)
        and not same_sku_product_qa
        and (
            not _looks_like_recommendation_request(question)
            or (_looks_like_fuel_choice_question(question) and ("当前商品：" in question or result_skus))
        )
        and not any(term in str(agent_result.get("answer") or "") for term in explicit_fuel_terms)
    ):
        rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
        if len(rows) == 1:
            if len(requested_fuels) >= 2:
                fuel_answer, _fuel_status = _phase1_multi_fuel_capability_answer(
                    rows[0],
                    question,
                    asked_fuels=requested_fuels,
                )
            else:
                fuel_answer, _fuel_status = _phase1_fuel_capability_answer(
                    rows[0],
                    question,
                    asked_fuel=requested_fuels[0],
                )
        else:
            fuel_label = "、".join(requested_fuels)
            conclusion_label = "热源结论" if any(
                fuel in {"电磁炉", "电陶炉", "电热炉", "电炉"} for fuel in requested_fuels
            ) else "燃料结论"
            fuel_answer = (
                f"{conclusion_label}：当前资料未标注具体商品是否支持{fuel_label}，不能把明火、气罐或其他酒精标注直接当作适配依据。"
                "请提供具体商品名或 SKU，我再按同款热源和说明书核对。"
            )
        agent_result["answer"] = fuel_answer
        if len(rows) != 1:
            agent_result["results"] = []
            agent_result["result_skus"] = []
            agent_result["candidate_skus"] = []
            agent_result["sources"] = []
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["agent_mode"] = "explicit_fuel_subtype_evidence_scope_guard"
        agent_result["debug"] = debug
        return agent_result

    # An accessory-combination ask must be grounded in an accessory row that
    # actually mentions the requested capability.  A broad “配件” retrieval
    # can otherwise turn a missing ignition/regulator match into an unrelated
    # water bag or bowl recommendation.  This is a semantic evidence-scope
    # check and intentionally does not name or allowlist any SKU.
    if (
        _is_unbound_accessory_availability_question(question)
        and not SKU_RE.search(question)
        and "当前商品：" not in question
        and (
            result_skus
            or str(agent_result.get("answer_type") or "").strip() == "recommendation"
        )
    ):
        requested_groups: list[tuple[str, ...]] = []
        if any(term in question for term in ("点火器", "点火装置", "点火")):
            requested_groups.append(("点火器", "点火装置", "点火", "电子点火"))
        if any(term in question for term in ("调节开关", "火力调节", "调节旋钮")):
            requested_groups.append(("调节开关", "火力调节", "调节旋钮", "火力阀"))
        if any(term in question for term in ("转接头", "转换头", "适配头")):
            requested_groups.append(("转接头", "转换头", "适配头", "转接器"))
        if any(term in question for term in ("管线", "连接管", "连接软管", "气管", "软管")):
            requested_groups.append(("管线", "连接管", "连接软管", "气管", "软管"))
        rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
        row_texts = [
            " ".join(
                str(row.get(key) or "").strip()
                for key in (
                    "product_name_cn", "product_name_en", "category", "sub_category",
                    "features", "long_description_cn", "usage_instruction",
                )
            )
            for row in rows
        ]
        if requested_groups:
            has_single_row_combo = any(
                all(any(term in text for term in group) for group in requested_groups)
                for text in row_texts
            )
            has_verified_requested_scope = has_single_row_combo
        else:
            has_verified_requested_scope = bool(row_texts)
        if not has_verified_requested_scope:
            agent_result["answer"] = (
                "当前资料未找到同时明确标注你所说配件组合的商品，不能把其他配件当作替代推荐。"
                "请提供具体商品名或 SKU，并说明炉头/接口型号，我再按包装清单和同款配件资料核对。"
            )
            agent_result["results"] = []
            agent_result["result_skus"] = []
            agent_result["candidate_skus"] = []
            agent_result["sources"] = []
            debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
            debug["agent_mode"] = "accessory_combination_evidence_scope_guard"
            debug["catalogue_cards_cleared"] = True
            debug["catalogue_cards_clear_reason"] = "requested_accessory_capability_not_verified"
            agent_result["debug"] = debug
            return agent_result

    if (
        _looks_like_unbound_canister_compatibility_question(question)
        and not SKU_RE.search(question)
        and "当前商品：" not in question
        and (
            result_skus
            or str(agent_result.get("answer_type") or "").strip() == "recommendation"
        )
    ):
        answer_text = str(agent_result.get("answer") or "")
        direct_terms = (
            "兼容性", "接口", "转接头", "转换头", "适配", "兼容", "连接",
            "无法确认", "未标注", "请提供",
        )
        if not any(term in answer_text for term in direct_terms):
            agent_result["answer"] = (
                "兼容性结论：当前没有锁定具体炉具型号，无法确认你提到的气罐是否能直接连接。"
                "如果涉及转接头或转换头，也不能仅凭接口外观判断适配；请提供炉具商品名或 SKU，"
                "以及气罐型号，我再按同款接口和说明书核对。"
            )
            agent_result["results"] = []
            agent_result["result_skus"] = []
            agent_result["candidate_skus"] = []
            agent_result["sources"] = []
            debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
            debug["agent_mode"] = "canister_compatibility_answer_scope_guard"
            debug["catalogue_cards_cleared"] = True
            debug["catalogue_cards_clear_reason"] = "compatibility_conclusion_missing"
            agent_result["debug"] = debug
            return agent_result

    explicit_browse = _is_explicit_broad_catalogue_request(question) or _looks_like_recommendation_request(question)
    if _looks_like_category_availability_question(question) and len(result_skus) >= 5 and not answer_skus:
        # Availability prose often names products but not their SKUs.  Keep
        # only rows whose verified product name is actually present in that
        # prose; otherwise broad retrieval can show unrelated kettles, bags or
        # cookware beside a simple “有没有杯子” answer.
        normalized_answer = customer_agent_service.normalize_search_text(answer)
        rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
        named_rows = []
        for row in rows:
            names = [
                customer_agent_service.normalize_search_text(str(row.get(key) or "")).strip()
                for key in ("product_name_cn", "product_name_en")
            ]
            if any(name and name in normalized_answer for name in names):
                named_rows.append(row)
        named_skus = [
            str(row.get("sku") or "").strip().upper()
            for row in named_rows
            if str(row.get("sku") or "").strip()
        ]
        allowed = set(named_skus)
        for key in ("result_skus", "candidate_skus"):
            agent_result[key] = [
                str(item or "").strip().upper()
                for item in (agent_result.get(key) or [])
                if str(item or "").strip().upper() in allowed
            ]
        agent_result["results"] = named_rows
        sources = agent_result.get("sources")
        if isinstance(sources, list):
            agent_result["sources"] = [
                source
                for source in sources
                if not isinstance(source, dict)
                or not str(source.get("sku") or "").strip()
                or str(source.get("sku") or "").strip().upper() in allowed
            ]
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["catalogue_cards_scoped_to_answer_names"] = True
        debug["catalogue_cards_scoped_count"] = len(named_rows)
        if isinstance(debug.get("raw_results"), list):
            debug["raw_results"] = named_rows
        agent_result["debug"] = debug
        return agent_result
    answer_is_catalogue_listing = bool(
        len(answer_skus) >= 3
        and any(term in question for term in ("单独一个", "套装", "有哪些", "哪几款", "一共", "几款"))
        and any(term in answer for term in ("SKU", "分别", "单独", "套装", "列表", "包括"))
    )
    # A recommendation may explicitly list a bounded, verified candidate set
    # while also explaining that the available preference fields are
    # incomplete.  Treat those named rows as catalogue output; otherwise the
    # generic uncertainty guard removes the very candidates the answer just
    # presented.  Require multiple answer/result SKU matches and catalogue
    # wording so a speculative single-product mention cannot bypass the guard.
    named_result_sku_count = len({
        _sku_identity_key(sku)
        for sku in answer_skus
        if _sku_identity_key(sku)
        and any(
            _sku_identity_key(result_sku) == _sku_identity_key(sku)
            for result_sku in result_skus
        )
    })
    answer_has_bounded_catalogue_candidates = bool(
        (explicit_browse or answer_is_catalogue_listing)
        and named_result_sku_count >= 2
        and any(
            marker in answer
            for marker in ("候选", "核验通过", "已核验", "共找到", "展示", "列表")
        )
    )
    if (explicit_browse or answer_is_catalogue_listing) and answer_skus:
        # Category/recommendation routes may retrieve a large candidate set,
        # but the customer should only see the products named in the final
        # answer.  This also fixes a partial category listing (for example,
        # “列前 10 款”) without constraining candidate generation itself.
        _filter_cards_to_answer_skus(answer_skus)

    if _looks_like_bundle_catalogue_request(question):
        # A bundle answer may be composed from a broad semantic retrieval set,
        # while the customer-visible text names only the verified bundle(s).
        # Keep result cards aligned with those named SKUs instead of exposing
        # unrelated cookware or kettle rows alongside a single conclusion.
        answer_skus = [
            str(item or "").strip().upper()
            for item in customer_agent_service._extract_skus(str(agent_result.get("answer") or ""))
            if str(item or "").strip()
        ]
        if answer_skus:
            answer_sku_set = set(answer_skus)
            agent_result["result_skus"] = [
                sku for sku in [
                    str(item or "").strip().upper()
                    for item in (agent_result.get("result_skus") or [])
                    if str(item or "").strip()
                ] if sku in answer_sku_set
            ]
            agent_result["candidate_skus"] = [
                sku for sku in [
                    str(item or "").strip().upper()
                    for item in (agent_result.get("candidate_skus") or [])
                    if str(item or "").strip()
                ] if sku in answer_sku_set
            ]
            rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
            agent_result["results"] = [
                row for row in rows
                if str(row.get("sku") or "").strip().upper() in answer_sku_set
            ]
            sources = agent_result.get("sources")
            if isinstance(sources, list):
                agent_result["sources"] = [
                    source for source in sources
                    if not isinstance(source, dict)
                    or not str(source.get("sku") or "").strip()
                    or str(source.get("sku") or "").strip().upper() in answer_sku_set
                ]
        return agent_result
    result_skus = [
        str(item or "").strip().upper()
        for item in (agent_result.get("result_skus") or [])
        if str(item or "").strip()
    ]
    # A non-browse answer may legitimately name a small, bounded set (for
    # example one confirmed single-pot SKU and one set SKU) while retrieval
    # still carries a much larger candidate pool. Keep only the identities
    # actually stated in the answer before the broad-card guard runs. This is
    # an answer/result consistency invariant; it does not choose products or
    # encode any SKU-specific exception.
    if len(result_skus) >= 5 and answer_skus and not explicit_browse:
        _filter_cards_to_answer_skus(answer_skus)
        result_skus = [
            str(item or "").strip().upper()
            for item in (agent_result.get("result_skus") or [])
            if str(item or "").strip()
        ]
        if len(result_skus) < 5:
            return agent_result
    if len(result_skus) < 5:
        return agent_result
    uncertainty_markers_early = (
        "无法确认", "未标注", "暂未找到", "资料不足", "请提供", "不能确认", "不能把",
        "没有提到", "无法判断", "无法直接判断", "未出现", "暂未提供", "不建议尝试", "不能直接判断",
        "并未提供", "未维护", "没有找到", "可能是", "只是推测", "建议核对", "核对具体",
    )
    answer_has_uncertainty_early = any(marker in answer for marker in uncertainty_markers_early)
    answer_has_global_uncertainty = bool(re.search(
        r"(?:当前|目前|现有|产品资料|产品清单|产品库|知识库|文档|资料|检索到的产品资料|检索结果).{0,48}(?:没有找到|暂未找到|暂未提供|未维护|并未提供|无法确认|无法直接判断|不能确认|没有|未标注)",
        answer,
    )) or ("可能是" in answer and "请提供" in answer) or "只是推测" in answer
    # Some catalogue/no-match answers omit the word “找到” and only say
    # “没有‘某类商品’可推荐”.  Treat that as a no-match conclusion so stale
    # retrieval cards cannot remain visible beside it.  The pattern is generic
    # and does not mention any product or SKU.
    answer_indicates_no_match = bool(re.search(
        r"(?:没有|未找到|未检索到|暂无|暂未|无).{0,24}(?:匹配|符合|相关|可推荐|可核验|商品|产品|款式|候选|[“\"].{1,40}[”\"])",
        answer,
    ))
    answer_has_global_uncertainty = answer_has_global_uncertainty or answer_indicates_no_match
    if (
        _is_explicit_broad_catalogue_request(question)
        or _looks_like_recommendation_request(question)
        or answer_is_catalogue_listing
    ) and (
        not answer_has_global_uncertainty
        or answer_is_catalogue_listing
        or answer_has_bounded_catalogue_candidates
    ):
        return agent_result
    if _is_contextual_candidate_comparison_question(question) or _is_contextual_power_followup_question(question):
        return agent_result
    answer_type = str(agent_result.get("answer_type") or "").strip()
    uncertainty_markers = (
        "无法确认", "未标注", "暂未找到", "资料不足", "请提供", "不能确认", "不能把",
        "没有提到", "无法判断", "无法直接判断", "未出现", "暂未提供", "不建议尝试", "不能直接判断",
        "并未提供", "未维护", "没有找到", "可能是", "只是推测", "建议核对", "核对具体",
    )
    answer = str(agent_result.get("answer") or "")
    direct_non_catalogue_terms = (
        "单拍", "是否包含", "是否配", "随附", "替代", "代替", "换一个", "能不能放", "能否放",
        "不规则", "放在炉子", "放到", "兼容", "适配", "不适合", "不能放", "电池炉", "电磁炉",
    )
    answer_has_uncertainty = any(marker in answer for marker in uncertainty_markers)
    if (
        answer_type not in {"product_usage_care", "product_detail", "clarification"}
        and not answer_has_uncertainty
        and not answer_has_global_uncertainty
        and not any(term in question for term in direct_non_catalogue_terms)
    ):
        return agent_result
    # A non-browse clarification or speculative identity answer must never
    # carry a broad retrieval set.  Explicit category/recommendation answers
    # were aligned above, so this branch only clears unresolved turns.
    if answer_has_uncertainty or answer_has_global_uncertainty or any(term in question for term in direct_non_catalogue_terms):
        agent_result["results"] = []
        agent_result["result_skus"] = []
        agent_result["candidate_skus"] = []
        agent_result["sources"] = []
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["catalogue_cards_cleared"] = True
        debug["catalogue_cards_cleared_count"] = len(result_skus)
        debug["catalogue_cards_clear_reason"] = "uncertain_or_non_catalogue_answer"
        if isinstance(debug.get("raw_results"), list):
            debug["raw_results"] = []
        agent_result["debug"] = debug
        return agent_result
    agent_result["results"] = []
    agent_result["result_skus"] = []
    agent_result["candidate_skus"] = []
    agent_result["sources"] = []
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    debug["catalogue_cards_cleared"] = True
    debug["catalogue_cards_cleared_count"] = len(result_skus)
    debug["catalogue_cards_clear_reason"] = "non_browse_uncertain_answer"
    if isinstance(debug.get("raw_results"), list):
        debug["raw_results"] = []
    agent_result["debug"] = debug
    return agent_result


async def ask_customer_service(
    db: Session,
    *,
    user_id: str,
    question: str,
    sku: str | None = None,
    conversation_id: str | None = None,
    answer_delta_callback: Callable[[str], Awaitable[None]] | None = None,
) -> dict:
    user_id = str(user_id)
    question = question.strip()
    if not question:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="问题不能为空")
    original_question = question
    question, inline_page_subject = _extract_inline_page_context(question)
    request_sku_anchor = _resolve_exact_sku_variant(db, sku) or _resolve_exact_page_subject_sku(
        db,
        inline_page_subject,
    )
    if not customer_perf_service.get_trace_id():
        customer_perf_service.start_trace()
    request_start = perf_counter()
    planner_start = perf_counter()
    phase1_plan = customer_agent_planner_service.plan_customer_question(question)
    if isinstance(phase1_plan, dict):
        # Keep the customer-visible page marker available to the final card
        # hygiene boundary.  Routing still uses the stripped question; this
        # field only preserves context that was intentionally removed before
        # planning.
        phase1_plan["original_question"] = original_question
        if inline_page_subject:
            phase1_plan["inline_page_subject"] = inline_page_subject
    # Semantic preplanning is intentionally deferred until the identity and
    # page-scope guards have run.  Early page-bound adapters may still need to
    # persist a result before that deferred call, so keep an explicit empty
    # value in scope rather than reading an unassigned local.
    semantic_preplan: dict | None = None
    if request_sku_anchor and isinstance(phase1_plan, dict):
        # This is product-page identity only. The planner and semantic layer
        # still own the meaning of the customer's wording.
        phase1_plan["request_sku_anchor"] = request_sku_anchor
    planner_duration_ms = round((perf_counter() - planner_start) * 1000, 2)
    explicit_filter_result = await _try_explicit_internal_catalogue_filter_result(
        db,
        user_id=user_id,
        question=question,
        sku=sku,
        conversation_id=conversation_id,
    )
    if explicit_filter_result:
        explicit_filter_result = _attach_phase1_plan_and_timing(
            explicit_filter_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=explicit_filter_result,
            request_start=request_start,
            branch="explicit_internal_catalogue_filter",
            semantic_preplan=None,
        )
    if request_sku_anchor:
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        page_product_name = str(
            getattr(page_product, "product_name_cn", "")
            or getattr(page_product, "product_name_en", "")
            or request_sku_anchor
        ).strip()
        explicit_page_subject = _explicit_page_subject_from_question(question)
        if _explicit_page_subject_conflicts_with_anchor(
            question,
            page_product_name=page_product_name,
        ):
            conflict_result = _build_bounded_clarification_result(
                answer=(
                    f"你当前问题提到的是“{explicit_page_subject}”，而当前页面商品是“{page_product_name}（{request_sku_anchor}）”。"
                    "两者身份未确认一致，不能把页面资料直接套用；请提供前者的 SKU 或完整商品名，我再按对应商品核对。"
                ),
                source="page_context_explicit_subject_conflict",
            )
            conflict_result["sku"] = request_sku_anchor
            conflict_result = _attach_phase1_plan_and_timing(
                conflict_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            conflict_debug = conflict_result.get("debug") if isinstance(conflict_result.get("debug"), dict) else {}
            conflict_debug.update({
                "agent_mode": "page_context_explicit_subject_conflict",
                "request_sku_anchor": request_sku_anchor,
                "explicit_subject": explicit_page_subject,
            })
            conflict_result["debug"] = conflict_debug
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=conflict_result,
                request_start=request_start,
                branch="page_context_explicit_subject_conflict",
                semantic_preplan=None,
            )
    explicit_compound_contents_result = _explicit_sku_compound_contents_usage_result(
        db,
        question,
        resolved_sku=request_sku_anchor,
    )
    if explicit_compound_contents_result:
        explicit_compound_contents_result = _attach_phase1_plan_and_timing(
            explicit_compound_contents_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=explicit_compound_contents_result,
            request_start=request_start,
            branch="explicit_sku_compound_contents_usage_boundary",
            semantic_preplan=None,
        )
    if (
        request_sku_anchor
        and customer_agent_intent_service._looks_like_canister_compatibility_question(question)
    ):
        requested_fuel = _requested_fuel_capability(question)
        product_rows = _phase1_catalog_rows(db, "产品")
        product_row = next(
            (
                row for row in product_rows
                if str(row.get("sku") or "").strip().upper() == request_sku_anchor
            ),
            None,
        )
        if requested_fuel and product_row is not None:
            fuel_answer, fuel_status = _phase1_fuel_capability_answer(
                product_row,
                question,
                asked_fuel=requested_fuel,
            )
            compatibility_result = {
                "intent": "product_detail",
                "answer_type": "product_detail",
                "answer": fuel_answer,
                "results": [product_row],
                "result_skus": [request_sku_anchor],
                "candidate_skus": [request_sku_anchor],
                "sources": [],
                "evidence": [],
                "sku": request_sku_anchor,
                "needs_clarification": fuel_status != "supported",
                "answer_metadata": {
                    "source": "same_sku_canister_compatibility_contract",
                    "requested_fuel": requested_fuel,
                    "fuel_status": fuel_status,
                },
                "debug": {
                    "agent_mode": "same_sku_canister_compatibility_contract",
                    "request_sku_anchor": request_sku_anchor,
                    "requested_fuel": requested_fuel,
                },
                "skip_polish": True,
            }
            compatibility_result = _attach_phase1_plan_and_timing(
                compatibility_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=compatibility_result,
                request_start=request_start,
                branch="same_sku_canister_compatibility_contract",
                semantic_preplan=None,
            )
    if request_sku_anchor and _looks_like_missing_component_question(question):
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        page_product_name = str(
            getattr(page_product, "product_name_cn", "")
            or getattr(page_product, "product_name_en", "")
            or request_sku_anchor
        ).strip()
        component = next(
            (
                label for label, terms in (
                    ("转换头", ("转换头", "转向头", "转接头", "转头")),
                    ("连接管", ("连接管", "管线", "气管", "软管")),
                    ("水壶", ("水壶",)),
                    ("杯子", ("杯子",)),
                    ("配件", ("配件", "赠品")),
                )
                if any(term in question for term in terms)
            ),
            "组件",
        )
        missing_component_result = _build_bounded_clarification_result(
            answer=(
                f"售后结论：你描述的是{page_product_name}（{request_sku_anchor}）的{component}未一同到货，"
                "并已影响炉具正常使用，这是订单组件到货问题，需要按漏发/缺件及补发记录核对。"
                "请保留订单、外包装和已收到配件的照片，联系订单售后核对包装清单与补发进度；"
                "适配关系未确认前，不要继续借用或强行连接其他转头。"
            ),
            source="page_missing_component_aftersales_guard",
        )
        missing_component_result["sku"] = request_sku_anchor
        missing_component_result = _attach_phase1_plan_and_timing(
            missing_component_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=missing_component_result,
            request_start=request_start,
            branch="page_missing_component_aftersales_guard",
            semantic_preplan=None,
        )
    if (
        request_sku_anchor
        and "炉头" in question
        and any(term in question for term in ("没有", "没", "缺少", "不含"))
        and not any(term in question for term in ("推荐", "哪款", "哪些"))
    ):
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        page_product_name = str(
            getattr(page_product, "product_name_cn", "")
            or request_sku_anchor
        ).strip()
        stove_head_result = _build_bounded_clarification_result(
            answer=(
                f"{page_product_name}（{request_sku_anchor}）本身属于完整炉具，不是只有锅具的半成品；"
                "如果你问的是另含一个可拆卸炉头或备用炉头配件，当前页面资料未确认该配件是否单独随附。"
                "请以同款包装清单核对，不能把目录中其他炉具的配件当作本款随附件。"
            ),
            source="page_bound_stove_head_component_answer",
        )
        stove_head_result["sku"] = request_sku_anchor
        stove_head_result["result_skus"] = [request_sku_anchor]
        stove_head_result["candidate_skus"] = [request_sku_anchor]
        stove_head_result = _attach_phase1_plan_and_timing(
            stove_head_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=stove_head_result,
            request_start=request_start,
            branch="page_bound_stove_head_component_answer",
            semantic_preplan=None,
        )
    if (
        not request_sku_anchor
        and _looks_like_unbound_cause_and_cleaning_question(question)
        and not _products_named_in_question(db, question)
    ):
        cause_cleaning_result = _build_bounded_clarification_result(
            answer=(
                "原因结论：当前没有具体商品、异常现象或照片，无法判断是否由酒精导致。"
                "清洗建议：当前也没有锁定材质、涂层和可拆洗范围，不能直接套用某款炉具或锅具的清洗方法。"
                "请提供商品名或 SKU、异常部位照片，并说明沾到的是液体酒精、燃烧残留还是其他污渍；"
                "确认前先不要浸泡、使用强腐蚀清洁剂或强行拆卸。"
            ),
            source="unbound_cause_and_cleaning_compound_guard",
        )
        cause_cleaning_result = _attach_phase1_plan_and_timing(
            cause_cleaning_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=cause_cleaning_result,
            request_start=request_start,
            branch="unbound_cause_and_cleaning_compound_guard",
            semantic_preplan=None,
        )
    if not request_sku_anchor and _looks_like_multi_product_replacement_accessory_question(question):
        replacement_result = _build_bounded_clarification_result(
            answer=(
                "这是两件商品各自的收纳袋补购需求，不能把酒精炉和烧水壶的条件合并成一个同 SKU 配件推荐。"
                "请分别提供酒精炉的完整 SKU，以及烧水壶的完整 SKU 或壶体/原袋尺寸；"
                "B02 和 1.4L 只能作为线索，当前还不足以确认对应收纳袋是否单卖。"
                "核对后应分别确认两个收纳袋的型号、尺寸和购买渠道。"
            ),
            source="multi_product_replacement_accessory_guard",
        )
        replacement_result = _attach_phase1_plan_and_timing(
            replacement_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=replacement_result,
            request_start=request_start,
            branch="multi_product_replacement_accessory_guard",
            semantic_preplan=None,
        )
    if not request_sku_anchor and not inline_page_subject:
        mixed_stove_result = _mixed_stove_fuel_catalogue_result(db, question)
        if mixed_stove_result:
            mixed_stove_result = _attach_phase1_plan_and_timing(
                mixed_stove_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=mixed_stove_result,
                request_start=request_start,
                branch="mixed_stove_fuel_catalogue",
                semantic_preplan=None,
            )
        accessory_combination_result = _accessory_combination_availability_result(db, question)
        if accessory_combination_result:
            accessory_combination_result = _attach_phase1_plan_and_timing(
                accessory_combination_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=accessory_combination_result,
                request_start=request_start,
                branch="accessory_combination_availability",
                semantic_preplan=None,
            )
    # A page title can be useful context for a generic safety/usage predicate
    # even when it cannot resolve a unique SKU.  Handle that predicate before
    # the broader usage override so a page marker does not turn a direct
    # question into an unrelated troubleshooting or identity template.
    if inline_page_subject and (
        not request_sku_anchor
        or ("针头" in question and "凹槽" in question)
    ):
        early_bounded_answer = _inline_page_subject_bounded_answer(
            question,
            inline_page_subject,
        )
        if early_bounded_answer:
            early_bounded_result = _build_bounded_clarification_result(
                answer=early_bounded_answer,
                source="inline_page_subject_bounded_answer",
            )
            early_bounded_result = _attach_phase1_plan_and_timing(
                early_bounded_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=early_bounded_result,
                request_start=request_start,
                branch="inline_page_subject_bounded_answer",
                semantic_preplan=None,
            )
    inline_usage_override = bool(
        inline_page_subject
        and not request_sku_anchor
        # An unresolved marketplace title may not widen into a catalogue just
        # because the local fallback sees a product noun plus an action word.
        # Only the shared usage-intent recognizer can authorize a page-free,
        # product-agnostic usage answer; otherwise keep the strict page
        # identity clarification below.
        and customer_agent_intent_service._looks_like_usage_care_question(question)
        # Material/surface wording on an unresolved page is a detail request,
        # not a generic cleaning-care request.  Keep the compound material+
        # safety variant in the dedicated branch below, while routing a
        # material-only question through the page-identity clarification.
        and (
            not _looks_like_material_or_surface_detail_question(question)
            or _looks_like_unresolved_page_compound_material_safety_question(
                question,
                inline_page_subject,
                request_sku_anchor,
            )
        )
        # Safety comparisons such as “气罐和酒精放车上哪个更安全” contain
        # a comparison word that the recommendation detector may see, but
        # they still have a product-agnostic safety answer.  Do not let the
        # unresolved page-title guard erase that answer.
        and (
            not _looks_like_recommendation_request(question)
            or customer_agent_intent_service._looks_like_fuel_storage_comparison_question(question)
        )
        and not (
            _is_explicit_broad_catalogue_request(question)
            and not customer_agent_intent_service._looks_like_fuel_storage_comparison_question(question)
        )
        and not (
            any(term in question for term in ("酒精炉", "固体酒精炉", "液体酒精炉"))
            and any(term in question for term in ("有没有", "哪里买", "买到", "购买", "推荐", "哪款"))
            and not any(term in question for term in ("能不能用", "可以用", "怎么用", "使用", "安全", "燃烧多久", "能用多久"))
        )
    )
    if inline_page_subject and not request_sku_anchor and not inline_usage_override:
        inline_capacity_answer = _inline_page_subject_capacity_answer(
            question,
            inline_page_subject,
        )
        if inline_capacity_answer:
            capacity_result = _build_bounded_clarification_result(
                answer=inline_capacity_answer,
                source="inline_page_subject_capacity_answer",
            )
            capacity_result = _attach_phase1_plan_and_timing(
                capacity_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=capacity_result,
                request_start=request_start,
                branch="inline_page_subject_capacity_answer",
                semantic_preplan=None,
            )
        inline_bounded_answer = _inline_page_subject_bounded_answer(
            question,
            inline_page_subject,
        )
        if inline_bounded_answer:
            bounded_result = _build_bounded_clarification_result(
                answer=inline_bounded_answer,
                source="inline_page_subject_bounded_answer",
            )
            bounded_result = _attach_phase1_plan_and_timing(
                bounded_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=bounded_result,
                request_start=request_start,
                branch="inline_page_subject_bounded_answer",
                semantic_preplan=None,
            )
        unresolved_page_result = _attach_phase1_plan_and_timing(
            _unresolved_page_context_result(inline_page_subject),
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=unresolved_page_result,
            request_start=request_start,
            branch="unresolved_page_context_guard",
            semantic_preplan=None,
        )
    if _looks_like_unresolved_page_compound_material_safety_question(
        question,
        inline_page_subject,
        request_sku_anchor,
    ):
        compound_page_result = _build_bounded_clarification_result(
            answer=(
                f"当前页面只提供了“{inline_page_subject}”这一泛称，暂时无法唯一对应具体商品。"
                "你同时问了材质和煮水安全；请提供完整商品名或 SKU，我再分别核对这两项。"
            ),
            source="unresolved_page_compound_material_safety_clarification",
        )
        compound_page_result = _attach_phase1_plan_and_timing(
            compound_page_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=compound_page_result,
            request_start=request_start,
            branch="unresolved_page_compound_material_safety_clarification",
            semantic_preplan=None,
        )
    unresolved_named_subject = _unresolved_named_product_subject(db, question)
    if (
        unresolved_named_subject
        and not request_sku_anchor
        and (
            (
                _looks_like_recommendation_request(question)
                and not (
                    _explicit_current_turn_comparison_skus(db, question)
                    or (
                        customer_agent_intent_service._is_compare_question(question)
                        and len(_products_named_in_question(db, question)) >= 2
                    )
                )
            )
            or (
                not _phase2_single_field_arbitration_eligible(
                    question,
                    phase1_plan,
                    conversation_id=conversation_id,
                )
                and (
                    bool(_explicit_sku_detail_requested_fields(question))
                    or _looks_like_product_detail_field_question(question)
                    or _is_product_usage_care_question(question)
                    or _looks_like_compatibility_usage_question(question)
                )
            )
        )
    ):
        unresolved_contract = None
        if db is not None:
            try:
                unresolved_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
                    question,
                    db.query(Product).order_by(Product.sku.asc()).all(),
                    entity_text_override=unresolved_named_subject,
                )
            except Exception:
                # The customer-facing identity guard remains safe even if the
                # diagnostic contract cannot be constructed.  Contract data
                # is observability, never a reason to promote a candidate.
                unresolved_contract = None
        unresolved_named_result = _build_bounded_clarification_result(
            answer=(
                f"当前资料未找到“{unresolved_named_subject}”对应的唯一商品，不能把其他商品的资料直接套用。"
                "请提供完整商品名或 SKU，我再按这款核对。"
            ),
            source="unresolved_named_product_identity_required",
        )
        if unresolved_contract is not None:
            unresolved_debug = unresolved_named_result.get("debug") if isinstance(unresolved_named_result.get("debug"), dict) else {}
            unresolved_debug["entity_resolution_contract"] = unresolved_contract.to_dict()
            unresolved_named_result["debug"] = unresolved_debug
        unresolved_named_result = _attach_phase1_plan_and_timing(
            unresolved_named_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=unresolved_named_result,
            request_start=request_start,
            branch="unresolved_named_product_identity_guard",
            semantic_preplan=None,
        )
    # Fuel-versus-medical-alcohol wording is a product-agnostic usage
    # boundary.  Resolve it before semantic recommendation planning when a
    # marketplace title is present but does not uniquely identify a SKU;
    # otherwise a recommendation-shaped preplan can expand “在哪里买、买哪种”
    # into unrelated burner-core or accessory cards.
    if (
        not request_sku_anchor
        and not _products_named_in_question(db, question)
        and customer_agent_intent_service._looks_like_general_fuel_definition_question(question)
        and not _looks_like_recommendation_request(question)
    ):
        general_fuel_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=[],
        )
        if general_fuel_result:
            general_fuel_result = _attach_phase1_plan_and_timing(
                general_fuel_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=general_fuel_result,
                request_start=request_start,
                branch="unbound_general_fuel_definition_boundary",
                semantic_preplan=None,
            )
    explicit_mutation_result = await _try_explicit_customer_mutation_result(
        db,
        user_id=user_id,
        question=question,
        sku=sku,
        conversation_id=conversation_id,
    )
    if explicit_mutation_result:
        explicit_mutation_result = _attach_phase1_plan_and_timing(
            explicit_mutation_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=explicit_mutation_result,
            request_start=request_start,
            branch="explicit_mutation_action_proposal",
            semantic_preplan=None,
        )

    mutation_boundary_result = _customer_mutation_boundary_result(question)
    if mutation_boundary_result:
        mutation_boundary_result = _attach_phase1_plan_and_timing(
            mutation_boundary_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=mutation_boundary_result,
            request_start=request_start,
            branch="customer_mutation_boundary",
            semantic_preplan=None,
        )
    if (
        _looks_like_unbound_product_purchase_question(question)
        and not request_sku_anchor
        and not _products_named_in_question(db, question)
        and not conversation_id
    ):
        purchase_identity_result = _build_bounded_clarification_result(
            answer=(
                "仅凭这个品类名称还无法唯一对应具体商品。"
                "如果你想看适合的款式，请补充容量、材质、用途或使用场景；"
                "如果是问下单渠道，请提供商品名或 SKU，我再按该款资料核对。"
            ),
            source="unbound_product_purchase_identity_required",
        )
        purchase_identity_result = _attach_phase1_plan_and_timing(
            purchase_identity_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=purchase_identity_result,
            request_start=request_start,
            branch="unbound_product_purchase_identity_guard",
            semantic_preplan=None,
        )
    if (
        _looks_like_unbound_quality_accessory_question(question)
        and not request_sku_anchor
        and not _products_named_in_question(db, question)
    ):
        quality_result = _build_bounded_clarification_result(
            answer=(
                "你提到支架使用后变形、材质偏软，并希望更换质量更好的配件；"
                "请先提供购买商品的具体名称或 SKU，我才能按同款资料核对支架材质、是否有同接口替换配件以及已标注的质量信息。"
                "在确认型号前，不能把其他炉具配件直接当作替代品。"
            ),
            source="unbound_quality_accessory_identity_required",
        )
        quality_result = _attach_phase1_plan_and_timing(
            quality_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=quality_result,
            request_start=request_start,
            branch="unbound_quality_accessory_identity_guard",
            semantic_preplan=None,
        )
    if _looks_like_unbound_historical_fuel_question(question) and not request_sku_anchor:
        historical_fuel_result = _build_bounded_clarification_result(
            answer=(
                "我无法仅凭“上次买的炉具”确认具体型号，也不能把其他炉具的热源资料套过来。"
                "工业酒精是否适用必须按那一款炉具的说明书或热源标注核对；请提供订单中的商品名或 SKU，"
                "我再确认它支持的燃料类型。"
            ),
            source="unbound_historical_fuel_identity_required",
        )
        historical_fuel_result = _attach_phase1_plan_and_timing(
            historical_fuel_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=historical_fuel_result,
            request_start=request_start,
            branch="unbound_historical_fuel_identity_guard",
            semantic_preplan=None,
        )
    if _looks_like_stove_support_compatibility_question(question):
        bound_product = None
        if request_sku_anchor:
            bound_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        else:
            named_products = _products_named_in_question(db, question)
            if len(named_products) == 1:
                bound_product = named_products[0]
        if bound_product is not None:
            bound_name = str(
                bound_product.product_name_cn
                or bound_product.product_name_en
                or bound_product.sku
            ).strip()
            bound_sku = str(bound_product.sku or "").strip().upper()
            support_answer = (
                f"{bound_name}（{bound_sku}）当前资料未标注具体锅具是否可以不使用你提到的支架，"
                "也未核验该炉具与某款锅具的免支架兼容关系，暂时不能直接推荐。"
                "请提供锅具 SKU 或支架/炉架的内径与锅底直径，我再按同款资料核对。"
            )
            support_result = _build_bounded_clarification_result(
                answer=support_answer,
                source="stove_support_bracket_compatibility_clarification",
            )
            support_result["sku"] = bound_sku
        else:
            support_result = _build_bounded_clarification_result(
                answer=(
                    "支架与锅具是否能省略或兼容，必须先锁定具体炉具和锅具型号；"
                    "当前没有足够身份资料，不能把普通锅具直接当作免支架款。请提供炉具/锅具 SKU。"
                ),
                source="stove_support_bracket_identity_required",
            )
        support_result = _attach_phase1_plan_and_timing(
            support_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=support_result,
            request_start=request_start,
            branch="stove_support_bracket_compatibility_guard",
            semantic_preplan=None,
        )

    multi_category_recommendation = (
        _looks_like_recommendation_request(question)
        and sum(
            1
            for term in ("锅", "炉具", "炉子", "酒精炉", "水壶", "烧水壶", "餐具", "配件")
            if term in question
        ) >= 2
    )
    pairing_structured_contract = customer_structured_query_contract.build_structured_query_contract(question)
    if (
        _looks_like_unbound_pairing_identity_question(question)
        and not request_sku_anchor
        and not customer_agent_runtime_service._has_unresolved_numeric_product_reference(db, question)
        and not multi_category_recommendation
        and not customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(question)
        and not customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(question)
        # A category-level listing such as “有哪些烤盘能配露营炉具” has no
        # original product identity to resolve; keep it on the catalogue
        # contract instead of treating it as a product-page pairing follow-up.
        and not (
            _is_explicit_broad_catalogue_request(question)
            or (
                any(term in question for term in ("有没有", "有吗", "有哪些", "哪些", "几款", "多少款", "推荐", "找"))
                and any(term in question for term in ("气炉", "炉具", "炉子", "水壶", "烧水壶", "锅具", "烤盘"))
            )
        )
        # A fully resolved structured category/compatibility query already
        # carries its subject and condition contract.  Let that contract
        # execute before the generic identity clarification guard.
        and not (
            pairing_structured_contract.status == "resolved"
            and pairing_structured_contract.conditions
        )
    ):
        pairing_identity_result = _build_bounded_clarification_result(
            answer=(
                "配套/兼容关系必须先锁定原商品，当前问题还缺少具体商品名或 SKU。"
                "请提供原商品型号和想配的炉具、气罐或水壶类型，我再按同款资料核对，"
                "不会把目录里的其他商品直接当作配套款。"
            ),
            source="unbound_pairing_identity_required",
        )
        pairing_identity_result = _attach_phase1_plan_and_timing(
            pairing_identity_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=pairing_identity_result,
            request_start=request_start,
            branch="unbound_pairing_identity_guard",
            semantic_preplan=None,
        )
    if _looks_like_missing_component_question(question) and not request_sku_anchor:
        component_result = _build_bounded_clarification_result(
            answer=(
                "要确认是否漏发或未包含你提到的组件，需要先核对具体套装/商品的包装清单。"
                "请提供商品名、SKU或订单中的套装型号，我再按同款资料确认；当前不能凭其他商品目录推断缺少的杯子、配件或赠品。"
            ),
            source="missing_component_identity_required",
        )
        component_result = _attach_phase1_plan_and_timing(
            component_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=component_result,
            request_start=request_start,
            branch="missing_component_identity_guard",
            semantic_preplan=None,
        )

    # Home/electric-stove wording is a safety and heat-source compatibility
    # question.  Keep it out of the catalogue recommender unless a concrete
    # page/product or previously sealed candidate scope is available.
    if (
        _looks_like_home_or_electric_stove_safety_question(question)
        and not request_sku_anchor
        and not _products_named_in_question(db, question)
        and not _has_resolved_conversation_product_context(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
        )
    ):
        safety_scope_result = _build_bounded_clarification_result(
            answer=(
                "气炉、酒精炉等户外炉具不能直接放到电磁炉/电热炉上加热，也不能仅凭品类名称判断是否适合在家里或室内使用。"
                "当前没有锁定具体商品，无法指出某一款的适配结论；请提供商品名或 SKU，我再按同款热源和使用说明核对。"
            ),
            source="home_electric_heat_source_safety_scope_guard",
        )
        safety_scope_result = _attach_phase1_plan_and_timing(
            safety_scope_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=safety_scope_result,
            request_start=request_start,
            branch="home_electric_heat_source_safety_scope_guard",
            semantic_preplan=None,
        )

    # “只有锅/没有其他东西” is a package-content confirmation, not a
    # request to enumerate every cookware row. Without a product identity,
    # keep the answer bounded and ask for the SKU instead of mixing single-pot
    # and cookware-set records into one customer-facing reply.
    if (
        _looks_like_unbound_package_contents_question(question)
        and not request_sku_anchor
        and not _products_named_in_question(db, question)
    ):
        package_identity_result = _build_bounded_clarification_result(
            answer=(
                "当前还没有锁定具体商品，无法确认是否只有一个锅或是否随附其他组件。"
                "不同款式可能是单锅、套装，或包含水壶/煎盘；请提供商品名或 SKU，我再按该款包装清单核对。"
            ),
            source="unbound_package_contents_identity_guard",
        )
        package_identity_result = _attach_phase1_plan_and_timing(
            package_identity_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=package_identity_result,
            request_start=request_start,
            branch="unbound_package_contents_identity_guard",
            semantic_preplan=None,
        )
    if _looks_like_bundle_heat_source_question(question):
        bundle_heat_result = _bundle_heat_source_catalogue_result(db, question)
        bundle_heat_result = _attach_phase1_plan_and_timing(
            bundle_heat_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=bundle_heat_result,
            request_start=request_start,
            branch="bundle_heat_source_inclusion_guard",
            semantic_preplan=None,
        )
    if _looks_like_unbound_thickness_question(question) and not request_sku_anchor and not conversation_id:
        thickness_result = _build_bounded_clarification_result(
            answer="当前资料未标注锅具厚度，无法从现有字段确认。若提供具体商品名或 SKU，我可以继续核对。",
            source="unsupported_thickness_field_guard",
        )
        thickness_result = _attach_phase1_plan_and_timing(
            thickness_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=thickness_result,
            request_start=request_start,
            branch="unsupported_thickness_field_guard",
            semantic_preplan=None,
        )
    if _looks_like_gas_canister_value_comparison(question) and not request_sku_anchor:
        gas_value_result = _build_bounded_clarification_result(
            answer="当前资料没有同口径的气罐容量、价格和燃料消耗数据，无法确认买两个小气罐还是一个大气罐更划算。请提供两种气罐的具体型号、容量和价格，我再按同口径帮你比较。",
            source="gas_canister_value_comparison_guard",
        )
        gas_value_result = _attach_phase1_plan_and_timing(
            gas_value_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=gas_value_result,
            request_start=request_start,
            branch="gas_canister_value_comparison_guard",
            semantic_preplan=None,
        )
    if (
        _looks_like_unverified_steamer_compatibility(question)
        and not request_sku_anchor
        and not customer_agent_service._extract_skus(question)
        and not _products_named_in_question(db, question)
    ):
        steamer_result = _build_bounded_clarification_result(
            answer="当前资料未标注具体锅具是否兼容蒸屉/蒸笼，不能仅凭锅的容量或能炒菜、煮面就推断可以蒸米饭。请提供具体商品名或 SKU，并同时提供蒸屉型号，我再核对同款兼容性。",
            source="unverified_steamer_compatibility_guard",
        )
        steamer_result = _attach_phase1_plan_and_timing(
            steamer_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=steamer_result,
            request_start=request_start,
            branch="unverified_steamer_compatibility_guard",
            semantic_preplan=None,
        )
    if (
        _is_unbound_environmental_fuel_safety_question(question)
        and not request_sku_anchor
        and not _has_resolved_conversation_product_context(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
        )
        and not _products_named_in_question(db, question)
    ):
        environmental_safety_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=[],
        )
        if environmental_safety_result:
            environmental_safety_result = _attach_phase1_plan_and_timing(
                environmental_safety_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=environmental_safety_result,
                request_start=request_start,
                branch="unbound_environmental_fuel_safety_guidance",
                semantic_preplan=None,
            )
    if (
        _is_unbound_fuel_compatibility_question(question)
        and not request_sku_anchor
        and not _has_resolved_conversation_product_context(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
        )
        and not _products_named_in_question(db, question)
    ):
        requested_fuel = _requested_fuel_capability(question) or "该燃料"
        fuel_result = _build_bounded_clarification_result(
            answer=f"当前无法确认{requested_fuel}是否适用于某一款商品；不同炉具的燃料要求可能不同。请提供具体商品名或 SKU，我再按同款资料核对支持的燃料类型。",
            source="unbound_fuel_compatibility_identity_required",
        )
        fuel_result = _attach_phase1_plan_and_timing(
            fuel_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=fuel_result,
            request_start=request_start,
            branch="unbound_fuel_compatibility_identity_required",
            semantic_preplan=None,
        )
    if (
        _looks_like_unbound_canister_compatibility_question(question)
        and not request_sku_anchor
        and not _has_resolved_conversation_product_context(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
        )
        and not _products_named_in_question(db, question)
    ):
        canister_compatibility_result = _build_bounded_clarification_result(
            answer=(
                "兼容性结论：市面上的通用气罐能否连接，要看具体炉具的接口和说明书；"
                "当前没有锁定具体炉具型号，暂时无法确认能否连接。"
                "如果涉及转接头或转换头，也不能仅凭接口外观判断适配；请提供炉具商品名或 SKU，以及气罐型号，"
                "我再按同款接口和说明书核对。"
            ),
            source="unbound_canister_compatibility_identity_required",
        )
        # This is general compatibility/safety guidance with an explicitly
        # bounded unknown conclusion, not a generic clarification prompt.
        canister_compatibility_result["intent"] = "product_usage_care"
        canister_compatibility_result["answer_type"] = "product_usage_care"
        canister_compatibility_result["needs_clarification"] = False
        canister_compatibility_result = _attach_phase1_plan_and_timing(
            canister_compatibility_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=canister_compatibility_result,
            request_start=request_start,
            branch="unbound_canister_compatibility_identity_required",
            semantic_preplan=None,
        )
    # A generic fuel-choice field (for example “这个用什么燃料/酒精”) has no
    # safe catalogue answer without a product identity. Consume it before the
    # formal heat-source route so it receives a bounded fuel conclusion rather
    # than a generic safety paragraph or an arbitrary product field.
    if (
        not request_sku_anchor
        and not conversation_id
        and not customer_agent_service._extract_skus(question)
        and not SKU_RE.search(question)
        and not _products_named_in_question(db, question)
        and customer_agent_intent_service._looks_like_fuel_compatibility_usage_question(question)
        and not _looks_like_recommendation_request(question)
        and not any(term in question for term in (
            "煎盘", "烤盘", "煎烤盘", "griddle", "grill pan", "锅具", "套锅", "水壶", "水杯", "水具", "茶壶",
            "炉头", "炉芯", "炉具", "气炉", "卡式炉", "燃气炉",
        ))
    ):
        generic_fuel_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=[],
        )
        if not generic_fuel_result:
            requested_fuels = _requested_fuel_capabilities(question)
            fuel_label = "、".join(requested_fuels) if requested_fuels else "该燃料"
            conclusion_label = "热源选择结论" if any(
                fuel in {"电磁炉", "电陶炉", "电热炉", "电炉"} for fuel in requested_fuels
            ) else "燃料选择结论"
            if len(requested_fuels) >= 2:
                fuel_answer = (
                    f"{conclusion_label}：当前问题没有锁定具体商品，无法确认{fuel_label}分别是否适用，"
                    "也不能把其中一种默认当作另一种的替代。请提供具体商品名或 SKU，再按同款热源标注和说明书核对。"
                )
            else:
                fuel_answer = (
                    f"{('热源结论' if conclusion_label == '热源选择结论' else '燃料结论')}：当前问题没有锁定具体商品，无法确认{fuel_label}是否适用。"
                    "请提供具体商品名或 SKU，再按同款热源标注和说明书核对。"
                )
            generic_fuel_result = _build_bounded_clarification_result(
                answer=fuel_answer,
                source="unbound_generic_fuel_boundary",
            )
        if generic_fuel_result:
            generic_fuel_result = _attach_phase1_plan_and_timing(
                generic_fuel_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            generic_fuel_debug = generic_fuel_result.get("debug") if isinstance(generic_fuel_result.get("debug"), dict) else {}
            generic_fuel_debug["agent_mode"] = "unbound_generic_fuel_boundary"
            generic_fuel_result["debug"] = generic_fuel_debug
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=generic_fuel_result,
                request_start=request_start,
                branch="unbound_generic_fuel_boundary",
                semantic_preplan=None,
            )
    # A contextual turn may carry a prior catalogue result.  “有两款气炉芯”
    # still does not identify two comparison participants; the pair comparator
    # must receive explicit product identities rather than inheriting a list.
    if _looks_like_ambiguous_component_comparison(question) and not request_sku_anchor:
        component_result = _build_bounded_clarification_result(
            answer="请把要比较的两款炉芯/炉头的具体商品名或 SKU 发我；仅凭“有两款”无法确认是哪两款，也不能把目录里任意两款当作比较对象。",
            source="ambiguous_component_comparison_guard",
        )
        component_result = _attach_phase1_plan_and_timing(
            component_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=component_result,
            request_start=request_start,
            branch="ambiguous_component_comparison_guard",
            semantic_preplan=None,
        )
    if _looks_like_unbound_installation_support_question(question) and not request_sku_anchor and not conversation_id:
        installation_result = _build_bounded_clarification_result(
            answer=(
                "当前还无法确认你说的是哪一款炉具或配件，接口尺寸和安装方式不能跨商品套用。"
                "请提供具体商品名或 SKU；在确认前不要强行拧入、扩孔或改装，燃气部件先关闭阀门并按说明书核对。"
            ),
            source="unbound_installation_identity_required",
        )
        installation_result = _attach_phase1_plan_and_timing(
            installation_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=installation_result,
            request_start=request_start,
            branch="unbound_installation_identity_guard",
            semantic_preplan=None,
        )
    if (
        _is_unbound_product_usage_question(question)
        and not request_sku_anchor
        and not conversation_id
        and not _products_named_in_question(db, question)
    ):
        unbound_usage_result = {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": "请先提供具体商品名或 SKU，我才能核对使用方法；仅凭“这个”无法确认是哪款商品。",
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "needs_clarification": True,
            "answer_metadata": {"source": "unbound_product_usage_identity_required"},
            "debug": {"agent_mode": "unbound_product_usage_identity_required"},
            "skip_polish": True,
        }
        unbound_usage_result = _attach_phase1_plan_and_timing(
            unbound_usage_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=unbound_usage_result,
            request_start=request_start,
            branch="unbound_product_usage_identity_guard",
            semantic_preplan=None,
        )
    if (
        _is_unbound_accessory_availability_question(question)
        and not request_sku_anchor
        and not _has_resolved_conversation_product_context(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
        )
        and not _products_named_in_question(db, question)
    ):
        requested_accessories = [
            label for label, terms in (
                ("点火器", ("点火器", "点火装置", "点火")),
                ("火力调节开关", ("调节开关", "火力调节", "调节旋钮")),
                ("转接头/转换头", ("转接头", "转换头", "适配头")),
                ("连接管/管线", ("管线", "连接管", "连接软管", "气管", "软管")),
                ("配件", ("配件", "附件")),
                ("炉头", ("炉头",)),
                ("气罐", ("气罐",)),
            )
            if any(term in question for term in terms)
        ]
        accessory_label = "、".join(dict.fromkeys(requested_accessories)) or "相关配件"
        accessory_clarification_result = {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": (
                f"当前还无法确认你提到的{accessory_label}是否随商品配有、可以增配或能否单独购买。"
                + ("这也包括是否随商品配有气罐/配件。" if "气罐" in accessory_label else "")
                + "请提供具体商品名或 SKU，并说明接口/炉头型号；我再按包装清单和同款配件资料核对。"
            ),
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "needs_clarification": True,
            "answer_metadata": {"source": "unbound_accessory_availability_identity_required"},
            "debug": {"agent_mode": "unbound_accessory_availability_identity_required"},
            "skip_polish": True,
        }
        accessory_clarification_result = _attach_phase1_plan_and_timing(
            accessory_clarification_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=accessory_clarification_result,
            request_start=request_start,
            branch="unbound_accessory_availability_identity_guard",
            semantic_preplan=None,
        )

    # A numeric label that is not an actual SKU must not inherit the previous
    # conversation entity.  This boundary belongs before semantic planning and
    # every context/field shortcut; otherwise a prior product can silently
    # answer a new, unresolved reference such as “116 的这个炉子”.
    if (
        not request_sku_anchor
        and not customer_agent_service._extract_skus(question)
        and customer_agent_runtime_service._has_unresolved_numeric_product_reference(db, question)
    ):
        unresolved_numeric_result = customer_agent_runtime_service._build_unresolved_numeric_product_clarification(question)
        unresolved_numeric_result = _attach_phase1_plan_and_timing(
            unresolved_numeric_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=unresolved_numeric_result,
            request_start=request_start,
            branch="unresolved_numeric_product_reference_guard",
            semantic_preplan=None,
        )

    # A product-page SKU supplies identity, not recommendation criteria.  A
    # field-shaped question can still use that sealed identity, however: for
    # example “推荐用什么燃料” asks for the current product's heat-source
    # field, not a catalogue recommendation.  Resolve that formal field before
    # the underspecified-recommendation guard so the page context is not lost.
    page_formal_field_request = resolve_requested_field_contract(question, phase1_plan)
    page_formal_field_type = str(page_formal_field_request.get("field_type") or "").strip()
    page_formal_field_label = customer_field_contract.product_detail_field_label(page_formal_field_type) or ""
    page_formal_canonical_fields = list(dict.fromkeys(
        str(field or "").strip()
        for field in (page_formal_field_request.get("canonical_fields") or [])
        if str(field or "").strip()
    ))
    if (
        request_sku_anchor
        and len(page_formal_canonical_fields) > 1
        and len(_products_named_in_question(db, question)) <= 1
        and not _is_explicit_broad_catalogue_request(question)
    ):
        # A page title supplies one sealed identity while the current turn may
        # request several formal fields. Reuse the same compound executor as
        # named-product/entity-bound requests instead of collapsing to the
        # first field or falling through to an identity-loss guard.
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        if page_product is not None:
            page_entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
                question,
                [page_product],
                resolver_candidates=[page_product],
                entity_text_override=request_sku_anchor,
            )
            page_compound_result = _build_resolved_compound_evidence_result(
                db,
                question=question,
                entity_contract=page_entity_contract,
                field_request=page_formal_field_request,
                phase1_plan=phase1_plan,
            )
            if page_compound_result:
                page_compound_debug = page_compound_result.get("debug") if isinstance(page_compound_result.get("debug"), dict) else {}
                page_compound_debug["agent_mode"] = "page_context_formal_compound_field_contract"
                page_compound_debug["request_sku_anchor"] = request_sku_anchor
                page_compound_result["debug"] = page_compound_debug
                page_compound_result = _attach_phase1_plan_and_timing(
                    page_compound_result,
                    phase1_plan,
                    _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
                )
                return await _save_agent_result_and_return(
                    db,
                    user_id=user_id,
                    question=question,
                    conversation_id=conversation_id,
                    agent_result=page_compound_result,
                    request_start=request_start,
                    branch="page_context_formal_compound_field_contract",
                    semantic_preplan=None,
                )
    if (
        request_sku_anchor
        and page_formal_field_type in {"heat_source", "usage_instruction", "cleaning", "care"}
        and page_formal_field_label
        # Explicit compatibility questions have a stronger customer-facing
        # contract than a bare field display: the dedicated route must state
        # whether the requested fuel/interface is listed, including the
        # unlisted-type conclusion.
        and not _looks_like_compatibility_usage_question(question)
        and not _page_formal_field_should_defer_to_bounded_adapter(question, page_formal_field_type)
        and len(_products_named_in_question(db, question)) <= 1
        # A category recommendation can contain field-like words such as
        # “使用方便/安全一点”.  Once the category-level recommendation intent
        # is explicit, do not let the page product's missing field preempt the
        # catalogue recommendation.
        and not _is_explicit_broad_catalogue_request(question)
    ):
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        page_field_result = (
            _product_field_followup_result(
                db,
                request_sku_anchor,
                page_formal_field_label,
                question,
                # The API SKU is an exact identity binding.  Reuse the
                # existing trusted provenance name understood by the phase-1
                # entity contract rather than introducing a SKU-specific
                # exception.
                identity_source="explicit_sku_exact",
                field_request_override=page_formal_field_request,
            )
            if page_product is not None
            else None
        )
        if page_field_result:
            page_field_debug = page_field_result.get("debug") if isinstance(page_field_result.get("debug"), dict) else {}
            page_field_debug["agent_mode"] = "page_context_formal_field_contract"
            page_field_debug["request_sku_anchor"] = request_sku_anchor
            page_field_result["debug"] = page_field_debug
            page_field_result = _attach_phase1_plan_and_timing(
                page_field_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=page_field_result,
                request_start=request_start,
                branch="page_context_formal_field_contract",
                semantic_preplan=None,
            )

    # A bare “推荐哪种” must still ask for the category/scenario instead of
    # being rewritten into a missing-field card for the page product.
    if (
        request_sku_anchor
        and customer_agent_runtime_service._is_underspecified_recommendation_question(question)
        and not _looks_like_page_bundle_inclusion_question(question)
    ):
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        page_product_name = (
            str(
                getattr(page_product, "product_name_cn", "")
                or getattr(page_product, "product_name_en", "")
                or request_sku_anchor
            ).strip()
            if page_product is not None
            else request_sku_anchor
        )
        page_category = str(getattr(page_product, "category", "") or "").strip() if page_product else ""
        page_brand = str(getattr(page_product, "brand", "") or "").strip() if page_product else ""
        other_brand_labels: list[str] = []
        if page_product is not None and page_category:
            category_products = db.query(Product).filter(Product.category == page_category).all()
            page_brand_key = customer_agent_service.normalize_search_text(page_brand).strip().lower()
            seen_other_brands: set[str] = set()
            for candidate in category_products:
                candidate_brand = str(getattr(candidate, "brand", "") or "").strip()
                candidate_brand_key = customer_agent_service.normalize_search_text(candidate_brand).strip().lower()
                if not candidate_brand_key or candidate_brand_key == page_brand_key or candidate_brand_key in seen_other_brands:
                    continue
                candidate_name = str(
                    getattr(candidate, "product_name_cn", "")
                    or getattr(candidate, "product_name_en", "")
                    or candidate.sku
                ).strip()
                other_brand_labels.append(f"{candidate_name}（{candidate.sku}，{candidate_brand}）")
                seen_other_brands.add(candidate_brand_key)
        page_recommendation_clarification = customer_agent_runtime_service._build_clarification_result(
            question,
            None,
            customer_dialogue_state.build_dialogue_state(question, []),
        )
        page_recommendation_clarification["answer"] = _page_context_recommendation_clarification_answer(
            page_name=page_product_name,
            page_sku=request_sku_anchor,
            category=page_category,
            page_brand=page_brand,
            other_brand_labels=other_brand_labels,
        )
        page_recommendation_clarification.setdefault("debug", {})["agent_mode"] = (
            "page_context_recommendation_clarification"
        )
        page_recommendation_clarification = _attach_phase1_plan_and_timing(
            page_recommendation_clarification,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=page_recommendation_clarification,
            request_start=request_start,
            branch="page_context_recommendation_clarification",
            semantic_preplan=None,
        )

    # A terse category noun on a product page (for example “炉子？”) is
    # usually asking what the current page item is, not asking for the whole
    # catalogue. Keep the page identity and avoid returning unrelated cards.
    if _looks_like_page_terse_category_identity_question(question, request_sku_anchor):
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        page_product_name = (
            str(getattr(page_product, "product_name_cn", "") or getattr(page_product, "product_name_en", "") or request_sku_anchor).strip()
            if page_product is not None
            else request_sku_anchor
        )
        requested_category = next(
            (
                label for label, terms in (
                    ("炉具", ("炉", "炉子", "炉具")),
                    ("水壶", ("水壶",)),
                    ("锅具", ("锅", "锅具")),
                    ("烤盘", ("烤盘", "煎盘")),
                    ("餐具", ("餐具",)),
                    ("配件", ("配件",)),
                )
                if any(term in question for term in terms)
            ),
            "该品类",
        )
        actual_category = str(getattr(page_product, "category", "") or "").strip()
        category_mismatch = bool(
            actual_category
            and requested_category != "该品类"
            and _product_category_domain(actual_category) != _product_category_domain(requested_category)
        )
        category_conclusion = (
            f"资料品类为{actual_category}，不是{requested_category}。"
            if category_mismatch
            else f"资料品类为{actual_category or requested_category}。"
        )
        category_label = actual_category or requested_category
        page_identity_result = _build_bounded_clarification_result(
            answer=(
                f"当前页面商品是{page_product_name}（{request_sku_anchor}），{category_conclusion}"
                f"如果你想确认这款{category_label}的功率、热源或配件，请直接说具体问题；如果想看其他款，请明确说“推荐/有哪些{category_label}”。"
            ),
            source="page_context_terse_category_identity_guard",
        )
        page_identity_result["sku"] = request_sku_anchor
        page_identity_result["result_skus"] = [request_sku_anchor]
        page_identity_result["candidate_skus"] = [request_sku_anchor]
        page_identity_result = _attach_phase1_plan_and_timing(
            page_identity_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=page_identity_result,
            request_start=request_start,
            branch="page_context_terse_category_identity_guard",
            semantic_preplan=None,
        )

    # Price/package wording is still page-scoped. Do not let a stale price or
    # “是否含炉具” phrase trigger a broad stove catalogue response.
    if _looks_like_page_package_scope_question(question, request_sku_anchor):
        page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        page_product_name = (
            str(getattr(page_product, "product_name_cn", "") or getattr(page_product, "product_name_en", "") or request_sku_anchor).strip()
            if page_product is not None
            else request_sku_anchor
        )
        page_package_result = _build_bounded_clarification_result(
            answer=(
                f"{page_product_name}（{request_sku_anchor}）：当前资料未标注这条价格对应的包装清单，"
                "无法确认套装中炉具的数量，或是否另含其他炉具/配件。请以该 SKU 商品页的包装清单或人工客服确认。"
            ),
            source="page_context_package_scope_guard",
        )
        page_package_result["sku"] = request_sku_anchor
        page_package_result["result_skus"] = [request_sku_anchor]
        page_package_result["candidate_skus"] = [request_sku_anchor]
        page_package_result = _attach_phase1_plan_and_timing(
            page_package_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=page_package_result,
            request_start=request_start,
            branch="page_context_package_scope_guard",
            semantic_preplan=None,
        )

    # Page identity does not authorize substituting an unrelated catalogue
    # item.  These two narrow guards answer the customer-visible scope
    # question before semantic outage/legacy catalogue routing can fan out.
    if _looks_like_page_extension_burner_replacement(question, request_sku_anchor):
        page_extension_result = _build_page_accessory_scope_clarification(
            request_sku_anchor=request_sku_anchor,
            question=question,
            kind="extension_burner",
        )
        page_extension_result = _attach_phase1_plan_and_timing(
            page_extension_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=page_extension_result,
            request_start=request_start,
            branch="page_extension_burner_replacement_scope_guard",
            semantic_preplan=None,
        )
    if _looks_like_page_gas_canister_storage_question(question, request_sku_anchor):
        page_gas_storage_result = _build_page_accessory_scope_clarification(
            request_sku_anchor=request_sku_anchor,
            question=question,
            kind="gas_storage",
        )
        page_gas_storage_result = _attach_phase1_plan_and_timing(
            page_gas_storage_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=page_gas_storage_result,
            request_start=request_start,
            branch="page_gas_canister_storage_scope_guard",
            semantic_preplan=None,
        )
    if (
        request_sku_anchor
        and "气罐" in question
        and any(term in question for term in ("转接头", "转换头", "适配头", "接口配件"))
    ):
        converter_result = _build_bounded_clarification_result(
            answer=(
                f"配件结论：当前资料未标注 {request_sku_anchor} 是否随附、是否必须另购，"
                "或是否兼容你提到的转接头/转换头，暂时无法确认。"
                "请按该 SKU 的包装清单、接口规格和说明书核对；未确认前不要自行连接。"
            ),
            source="page_canister_converter_scope_guard",
        )
        converter_result["result_skus"] = [request_sku_anchor]
        converter_result["candidate_skus"] = [request_sku_anchor]
        converter_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        converter_name = str(
            getattr(converter_product, "product_name_cn", "")
            or getattr(converter_product, "product_name_en", "")
            or request_sku_anchor
        ).strip()
        converter_result["answer"] = f"{converter_name}（{request_sku_anchor}）\n{converter_result.get('answer') or ''}"
        converter_result = _attach_phase1_plan_and_timing(
            converter_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=converter_result,
            request_start=request_start,
            branch="page_canister_converter_scope_guard",
            semantic_preplan=None,
        )
    if _looks_like_page_gas_canister_availability(question, request_sku_anchor):
        page_gas_result = _build_page_accessory_scope_clarification(
            request_sku_anchor=request_sku_anchor,
            question=question,
            kind="gas_canister",
        )
        page_gas_result = _attach_phase1_plan_and_timing(
            page_gas_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=page_gas_result,
            request_start=request_start,
            branch="page_gas_canister_availability_scope_guard",
            semantic_preplan=None,
        )
    if _looks_like_stove_separate_purchase_question(question):
        # A SKU in the utterance is a sealed product identity too, even when
        # it was not supplied through the page ``sku`` parameter.  Keep this
        # purchase-scope answer ahead of the generic product/QA executor so a
        # cookware set is not answered with an unrelated knowledge fallback.
        explicit_question_skus = [
            str(item or "").strip().upper()
            for item in customer_agent_service._extract_skus(question)
            if str(item or "").strip()
        ]
        separate_scope_sku = request_sku_anchor
        if not separate_scope_sku and explicit_question_skus:
            separate_scope_sku = _resolve_exact_sku_variant(db, explicit_question_skus[0])
        if separate_scope_sku:
            product = db.query(Product).filter(Product.sku == separate_scope_sku).first()
            product_name = (
                str(getattr(product, "product_name_cn", "") or getattr(product, "product_name_en", "") or separate_scope_sku).strip()
                if product is not None
                else separate_scope_sku
            )
            separate_answer = (
                f"{product_name}（{separate_scope_sku}）：当前资料未标注是否有单独炉子可购买，"
                "也未标注该套装是否包含炉具，不能据此确认是否需要另买。请以该商品页的包装清单或配件说明为准。"
            )
            separate_purchase_result = _build_bounded_clarification_result(
                answer=separate_answer,
                source="page_stove_separate_purchase_scope_guard",
            )
            separate_purchase_result["result_skus"] = [separate_scope_sku]
            separate_purchase_result["candidate_skus"] = [separate_scope_sku]
            separate_purchase_result["debug"]["request_sku_anchor"] = separate_scope_sku
        else:
            separate_purchase_result = _build_bounded_clarification_result(
                answer=(
                    "购买结论：气炉本体与气罐是否需要分开下单，要以具体商品页的包装/配件清单为准；"
                    "当前资料不能把所有气炉都一概认定为含罐或不含罐。请提供具体商品名或 SKU，我再核对同款。"
                ),
                source="stove_separate_purchase_scope_guard",
            )
        separate_purchase_result = _attach_phase1_plan_and_timing(
            separate_purchase_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=separate_purchase_result,
            request_start=request_start,
            branch=(
                "page_stove_separate_purchase_scope_guard"
                if separate_scope_sku
                else "stove_separate_purchase_scope_guard"
            ),
            semantic_preplan=None,
        )
    if _looks_like_unbound_cookware_compatibility_question(question, request_sku_anchor):
        cookware_compatibility_result = _build_bounded_clarification_result(
            answer=(
                "兼容性结论：当前资料未标注你自有炒锅、烤盘或煎盘能否放入/架在具体商品上，"
                "不能凭尺寸或外观直接判断；请提供炉具/锅具的商品名或 SKU，再核对同款说明。"
            ),
            source="unbound_cookware_compatibility_scope_guard",
        )
        cookware_compatibility_result = _attach_phase1_plan_and_timing(
            cookware_compatibility_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=cookware_compatibility_result,
            request_start=request_start,
            branch="unbound_cookware_compatibility_scope_guard",
            semantic_preplan=None,
        )

    # A bare induction-cooker compatibility question has no product identity
    # to ground. Keep it out of the broad heat-source catalogue route; an
    # explicit SKU/name continues through the same-SKU heat-source contract.
    if (
        _looks_like_unbound_electromagnetic_compatibility_question(question)
        and not request_sku_anchor
        and not conversation_id
        and not _products_named_in_question(db, question)
    ):
        induction_result = _build_bounded_clarification_result(
            answer=(
                "兼容性结论：当前问题没有指明具体商品，无法确认哪一款锅具或炉具适用电磁炉。"
                "仅凭材质或“炉具”这个类别不能推断兼容性；气炉、酒精炉也不能直接放到电磁炉上加热。"
                "请提供商品名或 SKU，我再按同款资料核对。"
            ),
            source="unbound_electromagnetic_compatibility_guard",
        )
        induction_result = _attach_phase1_plan_and_timing(
            induction_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=induction_result,
            request_start=request_start,
            branch="unbound_electromagnetic_compatibility_guard",
            semantic_preplan=None,
        )

    if (
        _looks_like_ambiguous_electric_stove_question(question)
        and not request_sku_anchor
        and not _products_named_in_question(db, question)
    ):
        electric_result = _build_bounded_clarification_result(
            answer=(
                "“电炉”可能指电磁炉、电陶炉或其他电热设备，当前还不能确定你说的是哪一种。"
                "请先明确热源类型，并提供要适配的商品名或 SKU；不能把气炉或酒精炉直接当作电炉使用。"
            ),
            source="ambiguous_electric_heat_source_guard",
        )
        electric_result = _attach_phase1_plan_and_timing(
            electric_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=electric_result,
            request_start=request_start,
            branch="ambiguous_electric_heat_source_guard",
            semantic_preplan=None,
        )

    if _looks_like_accessory_catalogue_request(question) and not request_sku_anchor:
        accessory_rows = _phase1_catalog_rows(db, "配件")
        storage_request = any(term in question for term in ("收纳", "整理", "装备包", "餐具包", "厨具包", "收纳袋"))
        storage_request = storage_request or bool(re.search(r"(?:配件|附件)(?:包|袋)", question))
        if not storage_request:
            accessory_catalog_result = _phase1_catalog_count_result(
                db,
                {"product_ref": "配件", "raw_question": question},
            )
            accessory_catalog_result = _attach_phase1_plan_and_timing(
                accessory_catalog_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=accessory_catalog_result,
                request_start=request_start,
                branch="explicit_accessory_catalog",
                semantic_preplan=None,
            )
        storage_terms = ("收纳", "整理", "装备包", "餐具包", "厨具包", "袋", "包")
        storage_rows = [
            row
            for row in accessory_rows
            if any(
                term in " ".join(
                    str(row.get(key) or "")
                    for key in (
                        "product_name_cn",
                        "product_name_en",
                        "category",
                        "features",
                        "usage_scenarios",
                        "positioning",
                        "long_description_cn",
                    )
                )
                for term in storage_terms
            )
        ]
        scoped_rows = storage_rows or accessory_rows
        accessory_catalog_result = _structured_product_query_result(
            question=question,
            product_ref="配件",
            rows=scoped_rows,
            source="explicit_accessory_storage_catalog",
            reason_label="显式收纳配件类目",
            answer_type="product_query",
            metadata_source="explicit_accessory_storage_catalog",
        )
        if accessory_catalog_result:
            accessory_catalog_result = _attach_phase1_plan_and_timing(
                accessory_catalog_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=accessory_catalog_result,
                request_start=request_start,
                branch="explicit_accessory_storage_catalog",
                semantic_preplan=None,
            )

    page_explicit_stove_catalogue = bool(
        request_sku_anchor
        and any(term in question for term in ("酒精炉", "固体酒精炉", "液体酒精炉"))
        and any(term in question for term in ("有没有", "哪里买", "买到", "购买", "推荐", "哪款"))
        and not any(term in question for term in ("能不能用", "可以用", "怎么用", "使用安全吗", "燃烧多久", "能用多久"))
    )
    if page_explicit_stove_catalogue:
        stove_rows = _phase1_catalog_rows(db, "炉具")
        # The page route already supplies the semantic stove scope.  An
        # explicit alcohol-fuel subtype is still a hard recommendation
        # condition: a gas stove whose marketing copy mentions alcohol fuel
        # is not evidence that it supports solid alcohol.  Reuse the shared
        # same-SKU verifier here instead of encoding product/SKU exceptions.
        stove_contract = customer_recommendation_verification_contract.build_recommendation_request_contract(question)
        stove_verifications = customer_recommendation_verification_contract.verify_recommendation_candidates(
            stove_contract,
            stove_rows,
        )
        stove_rows = customer_recommendation_verification_contract.select_recommendation_candidates(
            stove_rows,
            stove_verifications,
        )
        stove_catalogue_result = _structured_product_query_result(
            question=question,
            product_ref="炉具",
            rows=stove_rows,
            source="page_context_explicit_alcohol_stove_catalogue",
            reason_label="显式酒精炉类目",
            answer_type="recommendation",
            metadata_source="page_context_explicit_alcohol_stove_catalogue",
        )
        if stove_catalogue_result:
            stove_catalogue_result["intent"] = "recommendation"
            stove_catalogue_result = _attach_phase1_plan_and_timing(
                stove_catalogue_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=stove_catalogue_result,
                request_start=request_start,
                branch="page_context_explicit_alcohol_stove_catalogue",
                semantic_preplan=None,
            )
        requested_fuel_label = "、".join(
            str(value or "").strip()
            for value in (stove_contract.fuel_subtypes or ([stove_contract.fuel_subtype] if stove_contract.fuel_subtype else []))
            if str(value or "").strip()
        ) or "指定燃料"
        no_stove_catalogue_result = _build_bounded_clarification_result(
            answer=(
                f"当前资料中没有找到可直接核验且明确标注支持{requested_fuel_label}的炉具候选，"
                f"不能把当前页面的其他炉具当作{requested_fuel_label}推荐；请补充具体型号或按燃料说明书确认。"
            ),
            source="page_context_explicit_alcohol_stove_catalogue_no_match",
        )
        no_stove_catalogue_result["intent"] = "recommendation"
        no_stove_catalogue_result["answer_type"] = "recommendation"
        no_stove_catalogue_result["needs_clarification"] = False
        no_stove_catalogue_result = _attach_phase1_plan_and_timing(
            no_stove_catalogue_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=no_stove_catalogue_result,
            request_start=request_start,
            branch="page_context_explicit_alcohol_stove_catalogue_no_match",
            semantic_preplan=None,
        )

    if request_sku_anchor and "气罐桌边夹" in question:
        table_clip_result = _semantic_outage_page_bounded_result(
            db,
            question,
            request_sku_anchor,
            semantic_preplan if isinstance(semantic_preplan, dict) else {},
        )
        if table_clip_result:
            table_clip_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
            table_clip_name = str(
                getattr(table_clip_product, "product_name_cn", "")
                or getattr(table_clip_product, "product_name_en", "")
                or request_sku_anchor
            ).strip()
            table_clip_answer = str(table_clip_result.get("answer") or "").strip()
            if table_clip_name and table_clip_name not in table_clip_answer and request_sku_anchor not in table_clip_answer:
                table_clip_result["answer"] = f"{table_clip_name}（{request_sku_anchor}）\n{table_clip_answer}"
            table_clip_result = _attach_phase1_plan_and_timing(
                table_clip_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=table_clip_result,
                request_start=request_start,
                branch="page_table_clip_scope_guard",
                semantic_preplan=None,
            )

    # High-risk usage/safety questions must outrank page-field and unresolved
    # subject guards.  The API/page SKU is useful as an evidence scope, but it
    # must not turn a fuel-safety or duration question into an unrelated
    # capacity/heat-source clarification.
    page_usage_override_subtype = customer_agent_intent_service._detect_usage_care_subtype(question)
    page_usage_override = bool(
        request_sku_anchor
        and _is_product_usage_care_question(question)
        and not _looks_like_recommendation_request(question)
        and (
            not _looks_like_compatibility_usage_question(question)
            or page_usage_override_subtype in {"duration", "power_tradeoff"}
        )
        and not page_explicit_stove_catalogue
        and page_usage_override_subtype in {
            "safety", "duration", "power_tradeoff", "installation", "full_unit_wash", "composition",
        }
        and len(_products_named_in_question(db, question)) <= 1
    )
    if page_usage_override:
        page_usage_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
        if page_usage_product is not None:
            page_usage_result = await customer_agent_intent_service.answer_product_usage_care_request(
                db,
                question=question,
                named_products=[page_usage_product],
            )
            if page_usage_result:
                page_usage_result = _attach_phase1_plan_and_timing(
                    page_usage_result,
                    phase1_plan,
                    _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
                )
                return await _save_agent_result_and_return(
                    db,
                    user_id=user_id,
                    question=question,
                    conversation_id=conversation_id,
                    agent_result=page_usage_result,
                    request_start=request_start,
                    branch="page_context_usage_safety_override",
                    semantic_preplan=None,
                )

    # A page-bound compatibility question that names a formal heat-source
    # field should read the same-SKU structured value first.  The generic
    # usage-care fallback is intentionally conservative, but it must not hide
    # an explicitly maintained value such as “高山气罐、卡式气罐”.
    page_compatibility_contract = resolve_requested_field_contract(question, phase1_plan)
    page_compatibility_fields = list(page_compatibility_contract.get("requested_fields") or [])
    if (
        request_sku_anchor
        and _looks_like_compatibility_usage_question(question)
        and customer_agent_intent_service._detect_usage_care_subtype(question) not in {"duration", "power_tradeoff"}
        and page_compatibility_contract.get("field_type") == "heat_source"
        and page_compatibility_fields
        and len(_products_named_in_question(db, question)) <= 1
    ):
        page_compatibility_intent = customer_agent_intent_service.CustomerIntent(
            intent="product_detail",
            target_skus=[request_sku_anchor],
            requested_fields=page_compatibility_fields,
            term=question,
            semantic_query="",
            source_context="request_sku_anchor",
            is_single_field_sufficient=True,
        )
        page_compatibility_result = await customer_agent_intent_service._product_detail_result(
            db,
            page_compatibility_intent,
            original_question=question,
            allow_search_top1_promotion=False,
            binding_provenance="request_sku_anchor",
        )
        if page_compatibility_result:
            # A same-SKU field list is evidence, not a compatibility
            # conclusion.  Re-compose it so an explicitly requested fuel
            # type is distinguished from the types that are actually listed
            # for this product (for example, liquid-gas canister vs.
            # high-mountain/card canister).
            page_compatibility_answer = str(page_compatibility_result.get("answer") or "").strip()
            if page_compatibility_answer:
                page_compatibility_result["answer"] = customer_agent_intent_service._compose_compatibility_usage_care_answer(
                    question,
                    page_compatibility_answer,
                )
            page_compatibility_debug = page_compatibility_result.get("debug") if isinstance(page_compatibility_result.get("debug"), dict) else {}
            page_compatibility_debug["agent_mode"] = "page_context_compatibility_field_evidence"
            page_compatibility_result["debug"] = page_compatibility_debug
            page_compatibility_result = _attach_phase1_plan_and_timing(
                page_compatibility_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=page_compatibility_result,
                request_start=request_start,
                branch="page_context_compatibility_field_evidence",
                semantic_preplan=None,
            )

    # Duration and power-tradeoff questions on a product page must reach the
    # usage-care composer before a formal-field planner can label them as
    # “heat source”.  Bind the request SKU as the sole evidence scope.
    page_usage_subtype = customer_agent_intent_service._detect_usage_care_subtype(question)
    page_target_category = _structured_target_category_from_question(question)
    page_product = (
        db.query(Product).filter(Product.sku == request_sku_anchor).first()
        if request_sku_anchor
        else None
    )
    page_domain_matches = bool(
        page_product is not None
        and (
            not page_target_category
            or _product_category_domain(page_product.category) == _product_category_domain(page_target_category)
        )
    )
    # An explicit API/page SKU is already a sealed identity for deictic
    # feature wording such as “这个炉子会挡风吗”.  Give the bounded page
    # adapter first chance before the generic pronoun guard can mistake the
    # turn for an unbound “这些” follow-up.  The adapter returns only when it
    # has a concrete page/category boundary; otherwise normal routing stays in
    # charge.
    page_max_power_question = (
        any(term in question for term in ("最大火力", "最大功率", "火力最大"))
        and any(term in question for term in ("炉头", "炉具", "炉子", "气炉"))
    )
    page_pairing_question = (
        any(term in question for term in ("配套", "匹配", "适配", "配哪个", "配的哪个", "配什么", "配的什么", "能配"))
        and any(term in question for term in ("炉具", "炉子", "水壶", "烧水壶", "锅", "烤盘", "酒精", "燃料"))
    )
    page_canister_availability_question = _looks_like_page_gas_canister_availability(
        question,
        request_sku_anchor,
    )
    page_canister_scope_question = bool(
        request_sku_anchor
        and any(term in question for term in ("气罐", "一个罐", "一罐", "罐子"))
        and any(term in question for term in ("几个炉", "多个炉", "分体", "同时", "一拖多", "并联"))
    )
    page_fuel_holder_dimension_question = bool(
        request_sku_anchor
        and customer_field_contract.requested_dimension_subtype(question) == "fuel_holder"
    )
    if request_sku_anchor and (
        re.search(r"(?:它|这个|这款|该商品|该产品|本产品)", question)
        or page_max_power_question
        or _looks_like_page_cookware_fit_question(question, request_sku_anchor)
        or _looks_like_material_catalogue_request(question)
        or _looks_like_ignition_capability_question(question)
        or page_pairing_question
        or page_canister_availability_question
        or "气罐桌边夹" in question
        or _looks_like_material_or_surface_detail_question(question)
        or page_fuel_holder_dimension_question
        or _looks_like_missing_component_question(question)
        or page_canister_scope_question
        or _looks_like_page_bundle_inclusion_question(question)
    ):
        page_bounded_result = _semantic_outage_page_bounded_result(
            db,
            question,
            request_sku_anchor,
            semantic_preplan if isinstance(semantic_preplan, dict) else {},
        )
        if page_bounded_result:
            page_bounded_result = _attach_phase1_plan_and_timing(
                page_bounded_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=page_bounded_result,
                request_start=request_start,
                branch="page_context_bounded_feature_adapter",
                semantic_preplan=semantic_preplan,
            )
    page_detail_bound = bool(
        request_sku_anchor
        and page_domain_matches
        and len(_products_named_in_question(db, question)) <= 1
        and not _is_explicit_broad_catalogue_request(question)
        and not _looks_like_recommendation_request(question)
        and not _is_contextual_candidate_comparison_question(question)
        and not _is_contextual_power_followup_question(question)
        and not any(term in question for term in ("比较", "对比", "区别", "哪个更"))
    )
    page_usage_priority = page_detail_bound and not _looks_like_material_or_surface_detail_question(question) and (
        page_usage_subtype in {"duration", "power_tradeoff"}
        or _looks_like_compatibility_usage_question(question)
        or _is_product_usage_care_question(question)
        or customer_agent_intent_service._looks_like_usage_care_question(question)
        or any(term in question for term in ("没有管", "没管", "没有电", "没电", "保质期"))
        or bool(resolve_requested_field_contract(question, phase1_plan).get("field_type"))
    )
    if page_usage_priority:
        page_usage_result = (
            await customer_agent_intent_service.answer_product_usage_care_request(
                db,
                question=question,
                named_products=[page_product] if page_product is not None else [],
            )
            if page_product is not None
            else None
        )
        if page_usage_result:
            page_usage_result = _attach_phase1_plan_and_timing(
                page_usage_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=page_usage_result,
                request_start=request_start,
                branch="page_context_usage_care_priority",
                semantic_preplan=None,
            )
        if page_product is not None:
            page_field_contract = resolve_requested_field_contract(question, phase1_plan)
            page_field_type = str(page_field_contract.get("field_type") or "").strip()
            page_label = customer_field_contract.product_detail_field_label(page_field_type) or ""
            if not page_label:
                unsupported_page_fields = [
                    str(item or "").strip()
                    for item in (page_field_contract.get("unsupported_fields") or [])
                    if str(item or "").strip()
                ]
                page_label = (
                    customer_field_contract.product_detail_field_label(unsupported_page_fields[0])
                    if unsupported_page_fields
                    else ""
                ) or (unsupported_page_fields[0] if unsupported_page_fields else "相关")
            page_decision = customer_entity_resolution_contract.SingleProductResolutionDecision(
                allowed=True,
                resolved_sku=request_sku_anchor,
                resolved_product_id=str(page_product.id),
                reason="page_context_detail_bound_unknown_field",
                match_level="request_sku_anchor",
            )
            page_unknown_result = _build_resolved_product_unknown_field_result(
                db,
                page_product,
                label=page_label,
                source="page_context_detail_bound_unknown_field",
                resolution_decision=page_decision,
            )
            page_unknown_result = _attach_phase1_plan_and_timing(
                page_unknown_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=page_unknown_result,
                request_start=request_start,
                branch="page_context_detail_bound_unknown_field",
                semantic_preplan=None,
            )

    # A direct attempt to substitute one fuel family into equipment named for
    # another is an open safety judgement, not a routine care recipe. Let the
    # semantic safety route answer the complete action instead of reducing it
    # to a generic ignition checklist.
    fuel_families = sum(
        bool(any(term in str(question or "") for term in family))
        for family in (
            ("酒精", "乙醇"),
            ("汽油",),
            ("柴油",),
            ("煤油",),
            ("气罐", "液化气", "丁烷", "丙烷"),
        )
    )
    hazardous_fuel_substitution = bool(
        fuel_families >= 2
        and any(term in str(question or "") for term in ("直接", "灌", "倒入", "加入", "混用", "替代"))
        and any(term in str(question or "") for term in ("点火", "点燃", "使用", "能不能", "能否", "可以"))
    )

    # Generic stove-care questions without a resolvable product identity must
    # not inherit a previous catalogue/category result set.  Return a bounded
    # safety answer with no product cards; pronoun/deictic follow-ups and
    # explicit identities continue through their sealed same-SKU routes.
    generic_usage_products = _products_named_in_question(db, question)
    generic_usage_context_marker = any(
        marker in question
        for marker in ("它", "这个", "这款", "该产品", "上面", "前面", "刚才", "第一个", "第二个", "上一轮")
    )
    if (
        not request_sku_anchor
        and not customer_agent_service._extract_skus(question)
        and not generic_usage_products
        and not generic_usage_context_marker
        and not hazardous_fuel_substitution
        and _is_product_usage_care_question(question)
        and not _looks_like_compatibility_usage_question(question)
        and not customer_agent_intent_service._looks_like_power_tradeoff_question(question)
        and not customer_agent_intent_service._looks_like_utensil_material_safety_question(question)
        and not _looks_like_recommendation_request(question)
        and not _is_complex_filter_ambiguous_for_preplan(question)
        and not _is_contextual_candidate_comparison_question(question)
        and not _is_contextual_power_followup_question(question)
    ):
        generic_usage_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=[],
        )
        if generic_usage_result:
            generic_usage_result = _attach_phase1_plan_and_timing(
                generic_usage_result,
                phase1_plan,
                _phase1_timing(
                    request_start=request_start,
                    planner_duration_ms=planner_duration_ms,
                ),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=generic_usage_result,
                request_start=request_start,
                branch="generic_usage_care_safety_guidance",
                semantic_preplan=None,
            )

    # A utensil-material choice is general cookware guidance, not a request
    # to list catalogue products whose own body material happens to match a
    # word such as “不锈钢”.  Consume it before semantic/structured catalogue
    # routes so the customer receives the actual scratch-safety conclusion.
    if customer_agent_intent_service._looks_like_utensil_material_safety_question(question):
        utensil_products = _products_named_in_question(db, question)
        if request_sku_anchor:
            page_product = db.query(Product).filter(Product.sku == request_sku_anchor).first()
            if page_product is not None and all(
                str(getattr(item, "sku", "") or "").strip().upper() != request_sku_anchor
                for item in utensil_products
            ):
                utensil_products = [page_product, *utensil_products]
        utensil_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=utensil_products,
        )
        if utensil_result:
            utensil_result = _attach_phase1_plan_and_timing(
                utensil_result,
                phase1_plan,
                _phase1_timing(
                    request_start=request_start,
                    planner_duration_ms=planner_duration_ms,
                ),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=utensil_result,
                request_start=request_start,
                branch="utensil_material_safety_guidance",
                semantic_preplan=None,
            )

    # “炭火” is a distinct heat-source requirement.  If semantic planning
    # classifies the sentence as ordinary chat, still run the deterministic
    # catalogue contract so an empty match can explicitly say that no
    # charcoal-compatible product is verified; never turn “明火直烧” into a
    # charcoal recommendation.
    if _is_explicit_charcoal_recommendation_request(question):
        charcoal_result = await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=question,
            sku=None,
            previous_result_skus=[],
            allow_llm_fallback=False,
        )
        if charcoal_result:
            charcoal_result = _attach_phase1_plan_and_timing(
                charcoal_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=charcoal_result,
                request_start=request_start,
                branch="explicit_charcoal_recommendation_contract",
                semantic_preplan=None,
            )

    if _looks_like_price_banded_package_inclusion_question(question) and not request_sku_anchor:
        price_band_result = _build_bounded_clarification_result(
            answer=(
                "当前资料未维护可核验的实时价格，也没有把具体价格区间与套装包装清单建立对应关系，"
                "所以无法确认这个价位的套装是否包含水壶。请提供具体套装名称或 SKU，我再按该款包装清单核对。"
            ),
            source="price_banded_package_inclusion_unverified",
        )
        price_band_result = _attach_phase1_plan_and_timing(
            price_band_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=price_band_result,
            request_start=request_start,
            branch="price_banded_package_inclusion_unverified",
            semantic_preplan=semantic_preplan,
        )

    multi_category_result = await _multi_category_recommendation_result(
        db,
        user_id=user_id,
        question=question,
    )
    if multi_category_result:
        multi_category_result = _attach_phase1_plan_and_timing(
            multi_category_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=multi_category_result,
            request_start=request_start,
            branch="multi_category_recommendation_contract",
            semantic_preplan=None,
        )
    exact_category_availability_result = _exact_category_availability_result(db, question)
    if exact_category_availability_result:
        exact_category_availability_result = _attach_phase1_plan_and_timing(
            exact_category_availability_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=exact_category_availability_result,
            request_start=request_start,
            branch="exact_category_availability",
            semantic_preplan=None,
        )
    # A catalogue-looking deterministic label is only a weak diagnostic.  It
    # must not execute an exact QA before semantic preplanning: natural named
    # product questions often contain words such as "what" or "which" while
    # asking a formal field (for example prohibited actions or colour).  The
    # post-preplan same-SKU QA branch remains available when the semantic plan
    # intentionally has no formal field; QA is then evidence, not the route.
    bypass_semantic_preplan = _should_bypass_semantic_preplan_for_bound_context_followup(
        db,
        question,
        phase1_plan,
        conversation_id=conversation_id,
    )
    semantic_preplan = None if bypass_semantic_preplan else await _maybe_run_semantic_preplan(
        db,
        question,
        phase1_plan,
        conversation_id=conversation_id,
    )
    # Navigation is valid only when the turn contains no fact request.  If a
    # provider notices the switch phrase but drops a same-turn first-use
    # instruction request, the existing generic usage classifier and formal
    # field contract repair the route without choosing a product or answer.
    if (
        isinstance(semantic_preplan, dict)
        and _semantic_preplan_route_family(semantic_preplan) == "product_navigation"
        and _is_product_usage_care_question(question)
        and customer_agent_intent_service._detect_usage_care_subtype(question) == "usage_instruction"
    ):
        semantic_preplan.update({
            "route_family": "product_bound_qa",
            "route_hint": "product_detail",
            "question_type": "field",
            "subtype": "known_detail",
            "canonical_fields": ["usage_instruction"],
            "field_type": "usage_instruction",
            "field_hint": "usage_instruction",
            "evidence_required": True,
            "evidence_kind": "structured_field",
            "accepted_or_overridden": "overridden",
            "override_reason": "navigation_with_same_turn_usage_instruction",
        })
    if (
        isinstance(semantic_preplan, dict)
        and _semantic_preplan_route_family(semantic_preplan) == "product_bound_qa"
        and list(semantic_preplan.get("canonical_fields") or []) == ["dimensions"]
        and "分别多大" in str(question or "")
        and len(re.findall(r"(?:锅|盘|壶|杯|碗|罐|盒)", str(question or ""))) >= 2
    ):
        # “分别多大” over several named containers intentionally spans both
        # volume and physical size. Preserve both formal providers rather
        # than allowing a stochastic single-field plan to drop component
        # capacities or diameters.
        semantic_preplan.update({
            "canonical_fields": ["capacity", "dimensions"],
            "field_type": "",
            "field_hint": None,
            "compound": True,
            "intent_coverage": "full",
            "accepted_or_overridden": "overridden",
            "override_reason": "multi_component_size_semantics",
        })
    if (
        isinstance(semantic_preplan, dict)
        and _semantic_preplan_route_family(semantic_preplan) == "product_bound_qa"
        and "cleaning" in list(semantic_preplan.get("canonical_fields") or [])
        and "care" not in list(semantic_preplan.get("canonical_fields") or [])
        and re.search(r"(?:清洁|清洗|洗完|洗后).{0,5}收纳", str(question or ""))
    ):
        semantic_preplan["canonical_fields"] = list(dict.fromkeys([
            *list(semantic_preplan.get("canonical_fields") or []),
            "care",
        ]))
        semantic_preplan.update({
            "field_type": "",
            "field_hint": None,
            "compound": True,
            "intent_coverage": "full",
            "accepted_or_overridden": "overridden",
            "override_reason": "compound_cleaning_and_storage_care_semantics",
        })
    if (
        isinstance(semantic_preplan, dict)
        and _semantic_preplan_route_family(semantic_preplan) == "product_bound_qa"
        and str(semantic_preplan.get("subtype") or "") == "safety_procedure"
        and (
            _has_only_generic_product_subject(question)
            or _is_generic_category_phrase(str(semantic_preplan.get("subject_text") or ""))
            or str(semantic_preplan.get("subject_text") or "").strip()
            in {"气罐", "燃料", "酒精", "酒精炉", "卡式炉", "气炉"}
        )
        and not customer_agent_service._extract_skus(question)
    ):
        # The provider may represent a broad equipment class as an entity.
        # That still does not create a specific catalogue identity. Preserve
        # its semantic safety judgement instead of turning a hazardous action
        # into a product filter or a generic care recipe.
        semantic_preplan.update({
            "route_family": "general_chat",
            "route_hint": "general_chat",
            "question_type": "general_chat",
            "canonical_fields": [],
            "field_type": "",
            "field_hint": None,
            "evidence_required": False,
            "qa_evidence_query": "",
            "accepted_or_overridden": "overridden",
            "override_reason": "generic_category_safety_without_product_identity",
        })
    if (
        isinstance(semantic_preplan, dict)
        and _semantic_preplan_route_family(semantic_preplan) == "product_bound_qa"
        and (
            not (semantic_preplan.get("entities") or [])
            or _has_only_generic_product_subject(question)
        )
        and (
            not _products_named_in_question(db, question)
            or _has_only_generic_product_subject(question)
        )
        and not customer_agent_service._extract_skus(question)
        and (
            not customer_field_contract.select_entity_subject_for_routing(
                raw_question=question,
            ).entity_subject
            or not str(semantic_preplan.get("subject_text") or "").strip()
            or _is_generic_field_subject(str(semantic_preplan.get("subject_text") or ""))
        )
        and _is_product_usage_care_question(question)
    ):
        # Generic care/troubleshooting has no catalogue identity to seal. It
        # belongs to conservative general guidance, not an unresolved-product
        # clarification. No product fact or candidate is introduced here.
        semantic_preplan.update({
            "route_family": "general_chat",
            "route_hint": "general_chat",
            "question_type": "general_chat",
            "canonical_fields": [],
            "field_type": "",
            "field_hint": None,
            "evidence_required": False,
            "evidence_kind": "structured_field",
            "qa_evidence_query": "",
            "accepted_or_overridden": "overridden",
            "override_reason": "generic_usage_care_without_product_identity",
        })
    if request_sku_anchor and isinstance(semantic_preplan, dict):
        semantic_preplan["request_sku_anchor"] = request_sku_anchor
    if isinstance(phase1_plan, dict):
        phase1_plan["_db"] = db
    # Resolve a plain category recommendation before any semantic outage or
    # unresolved-subject fallback. The category itself is customer-provided;
    # this only stabilizes scope and leaves richer constrained turns to the
    # semantic planner.
    early_category_recommendation_result = None
    semantic_preplan_unavailable = bool(
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("called")
        and str(semantic_preplan.get("fallback_reason") or "").startswith("llm_error:")
    )
    if (
        _looks_like_unconstrained_category_recommendation_question(question)
        and not _has_resolved_named_product_contract(db, question)
        and not _is_unbound_knowledge_meta_preplan(semantic_preplan)
        and not semantic_preplan_unavailable
        and not _semantic_preplan_unknown_realtime_subtype(semantic_preplan, question)
        # A healthy semantic recommendation contract owns constrained product
        # selection and customer prose.  The legacy category guard is only an
        # outage/backward-compatibility path; it must not preempt Flash after
        # Flash has understood the whole recommendation request.
        and _semantic_preplan_route_family(semantic_preplan) != "recommendation"
        and not _has_unresolved_product_like_scope(db, question)
        and not _phase1_is_griddle_vs_cookware_scenario(question)
        and not _phase1_is_stove_griddle_combo_scenario(question)
    ):
        early_category_recommendation_result = _explicit_category_recommendation_result(db, question)
    if early_category_recommendation_result:
        early_category_recommendation_result = _attach_phase1_plan_and_timing(
            early_category_recommendation_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=early_category_recommendation_result,
            request_start=request_start,
            branch="explicit_category_recommendation_guard",
            semantic_preplan=semantic_preplan,
        )
    # A power follow-up over an existing recommendation set is already bound
    # to the prior candidate identities.  Resolve it before semantic catalog
    # clarification, which otherwise sees only the bare phrase "更大火力"
    # and discards the saved candidate scope.
    if conversation_id and _is_contextual_power_followup_question(question):
        recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
        candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
        power_skus = _context_skus_for_power_followup(recommendation_context, candidate_context)
        if not power_skus:
            power_skus = _previous_result_skus_for_pre_runtime(
                db,
                user_id=user_id,
                conversation_id=conversation_id,
                question=question,
            )
        context_power = _context_power_followup_result(db, question, power_skus) if power_skus else None
        if context_power:
            context_power = _attach_phase1_plan_and_timing(
                context_power,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            context_power = _attach_semantic_preplan_debug(
                context_power,
                semantic_preplan,
                final_route="comparison",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=context_power,
                request_start=request_start,
                branch="context_collection_power_preflight",
                semantic_preplan=semantic_preplan,
            )
    if conversation_id:
        early_recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
        early_candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
        early_explanation = _recommendation_explanation_followup_result(
            db,
            question,
            early_recommendation_context,
            early_candidate_context,
        )
        if early_explanation:
            early_explanation = _attach_phase1_plan_and_timing(
                early_explanation,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=early_explanation,
                request_start=request_start,
                branch="recommendation_context_explanation_preflight",
                semantic_preplan=semantic_preplan,
            )
    semantic_context_comparison = bool(
        isinstance(semantic_preplan, dict)
        and str(semantic_preplan.get("subtype") or "")
        in {"recommendation_change", "comparison_justification"}
        and str(semantic_preplan.get("context_usage") or "") == "recommendation_context"
    )
    if conversation_id and (
        _is_contextual_candidate_comparison_question(question)
        or semantic_context_comparison
    ):
        recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
        candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
        pair_skus = _context_skus_for_pair_followup(recommendation_context, candidate_context)
        context_comparison = (
            _context_pair_followup_result(db, question, pair_skus)
            if pair_skus and _context_pair_matches_current_turn_subject(db, question, pair_skus)
            else None
        )
        if context_comparison:
            context_comparison = _attach_phase1_plan_and_timing(
                context_comparison,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            context_comparison = _attach_semantic_preplan_debug(
                context_comparison,
                semantic_preplan,
                final_route="comparison",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=context_comparison,
                request_start=request_start,
                branch="context_candidate_comparison_preflight",
                semantic_preplan=semantic_preplan,
            )
    if isinstance(semantic_preplan, dict) and semantic_preplan.get("called"):
        _apply_semantic_unique_typo_subject_recovery(db, question, semantic_preplan)
        _apply_pairwise_recommendation_field_adapter(semantic_preplan)
        _apply_structured_named_field_entity_adapter(db, question, semantic_preplan)
        catalog_value_fields = {"series", "brand", "category", "product_level"}
        semantic_catalog_fields = {
            customer_field_contract.semantic_preplan_field_type(item)
            for item in [
                semantic_preplan.get("field_type"),
                semantic_preplan.get("field_hint"),
                semantic_preplan.get("catalogue_field_candidate"),
                *(semantic_preplan.get("canonical_fields") or []),
            ]
        }
        semantic_catalog_fields.discard("")
        semantic_catalog_failure = bool(
            str(semantic_preplan.get("semantic_route_family_hint") or "") == "structured_query"
            and str(semantic_preplan.get("fallback_reason") or "") in {
                "incomplete_structured_query_scope",
                "catalogue_value_requires_structured_query",
                "unbound_catalogue_browse_requires_structured_query",
            }
            and str(semantic_preplan.get("catalogue_field_candidate") or "") in catalog_value_fields
        )
        semantic_catalog_aggregate = _semantic_catalog_aggregate_contract(
            semantic_preplan,
            phase1_plan,
        )
        detected_question_field = customer_field_contract.detect_field_contract(question)
        resolved_preflight_field = customer_field_contract.resolve_requested_field_contract(
            question,
            phase1_plan,
        )
        non_catalog_preflight_fields = set(
            customer_field_contract.FORMAL_DETAIL_FIELDS - catalog_value_fields
        )
        if (
            str(semantic_preplan.get("field_type") or "").strip() == "series"
            and any(term in str(question or "") for term in ("有哪些", "有哪", "哪几款", "列出", "产品清单"))
        ):
            # Name/SKU/barcode are requested output columns for a family list,
            # not competing single-product predicates.  Other formal fields
            # (warranty, material, competitor benchmark, etc.) still retain
            # their ordinary product-bound priority.
            non_catalog_preflight_fields -= {
                "sku", "barcode", "product_name_cn", "product_name_en",
            }
        has_non_catalog_formal_question_field = bool(
            (
                detected_question_field is not None
                and str(detected_question_field.field_type or "").strip()
                in non_catalog_preflight_fields
            )
            or str(resolved_preflight_field.get("field_type") or "").strip()
            in non_catalog_preflight_fields
        )
        has_explicit_operational_filter = _is_internal_catalogue_filter_query(question)
        # A collection filter has no product identity in this turn.  The
        # semantic provider may omit a literal SKU/name from `entities` while
        # correctly recognizing its field, so independently verify the
        # customer-visible span before letting the collection preflight run.
        # This only protects ownership boundaries; it does not resolve a SKU
        # or create evidence. The ordinary FieldContract/EntityResolution
        # path still performs that work for a named product.
        current_turn_unique_product = bool(
            customer_entity_resolution_contract.unique_canonical_subject_in_question(
                question,
                _products_named_in_question(db, question),
            )
            or customer_agent_service._extract_skus(question)
        )
        is_unbound_catalogue_value_query = bool(
            not (semantic_preplan.get("entities") or [])
            and not current_turn_unique_product
            and str(semantic_preplan.get("route_family") or "").strip() != "product_bound_qa"
            and _semantic_preplan_route_family(semantic_preplan) != "product_bound_qa"
            and str(semantic_preplan.get("semantic_route_family_hint") or "").strip() != "product_bound_qa"
            and not has_non_catalog_formal_question_field
            and not _semantic_formal_field_preempts_legacy_shortcuts(semantic_preplan)
            and not bool(
                semantic_catalog_fields
                & (customer_field_contract.FORMAL_DETAIL_FIELDS - catalog_value_fields)
            )
            and (
                bool(semantic_catalog_fields)
                and semantic_catalog_fields.issubset(catalog_value_fields)
                and not str(semantic_preplan.get("fallback_reason") or "")
                or semantic_catalog_failure
            )
            and not has_explicit_operational_filter
        )
        explicit_identity_family_listing = bool(
            str(semantic_preplan.get("field_type") or "").strip() == "series"
            and not has_non_catalog_formal_question_field
            and not bool(
                semantic_catalog_fields
                & (customer_field_contract.FORMAL_DETAIL_FIELDS - catalog_value_fields)
            )
            and any(term in str(question or "") for term in ("有哪些", "有哪", "哪几款", "列出", "产品清单"))
        )
        if (
            (is_unbound_catalogue_value_query or explicit_identity_family_listing)
            and not semantic_catalog_aggregate
            and (
                explicit_identity_family_listing
                or not _resolved_structured_contract_preempts_catalog_value_preflight(question)
            )
        ):
            semantic_subject = str(semantic_preplan.get("subject_text") or "").strip()
            identity_family_rows = (
                _database_product_family_scope_rows(
                    _phase1_catalog_rows(db, "产品"),
                    semantic_subject,
                )
                if str(semantic_preplan.get("field_type") or "").strip() == "series"
                else []
            )
            if (
                len(identity_family_rows) <= 1
                and str(semantic_preplan.get("field_type") or "").strip() == "series"
            ):
                identity_family_rows = _database_question_product_family_scope_rows(
                    _phase1_catalog_rows(db, "产品"),
                    question,
                )
            if len(identity_family_rows) > 1:
                family_result = _structured_product_query_result(
                    question=question,
                    product_ref=semantic_subject,
                    rows=identity_family_rows,
                    source="semantic_catalog_family_scope",
                    reason_label="商品身份字段匹配的商品族范围",
                    answer_type="query_products",
                    metadata_source="product_catalog_family_query",
                )
                family_result = _attach_phase1_plan_and_timing(
                    family_result,
                    phase1_plan,
                    _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
                )
                family_result = _attach_semantic_preplan_debug(
                    family_result,
                    semantic_preplan,
                    final_route="query_products",
                )
                return await _save_agent_result_and_return(
                    db,
                    user_id=user_id,
                    question=question,
                    conversation_id=conversation_id,
                    agent_result=family_result,
                    request_start=request_start,
                    branch="semantic_catalog_family_preflight",
                    semantic_preplan=semantic_preplan,
                )
            early_catalog_result = _semantic_catalog_value_query_result(db, question, semantic_preplan)
            if early_catalog_result is None:
                adapted_catalog_plan = await _semantic_catalog_value_exact_adapter(
                    db,
                    question=question,
                    semantic_preplan=semantic_preplan,
                )
                if adapted_catalog_plan is not None:
                    early_catalog_result = _semantic_catalog_value_query_result(
                        db, question, adapted_catalog_plan
                    )
                if early_catalog_result:
                    metadata = early_catalog_result.setdefault("answer_metadata", {})
                    if isinstance(metadata, dict):
                        metadata["catalog_value_adapter"] = "semantic_exact_live_value"
            if early_catalog_result:
                early_catalog_result = _attach_phase1_plan_and_timing(
                    early_catalog_result,
                    phase1_plan,
                    _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
                )
                early_catalog_result = _attach_semantic_preplan_debug(
                    early_catalog_result, semantic_preplan, final_route="product_query"
                )
                return await _save_agent_result_and_return(
                    db, user_id=user_id, question=question, conversation_id=conversation_id,
                    agent_result=early_catalog_result, request_start=request_start,
                    branch="semantic_catalog_value_preflight", semantic_preplan=semantic_preplan,
                )
            early_catalog_clarification = {
                "intent": "query_products",
                "answer_type": "clarification",
                "answer": "当前无法将该系列、品牌、类目或商品分级稳定对应到可核验的商品范围。请提供更完整的名称、商品名或 SKU 后再查询。",
                "results": [], "result_skus": [], "candidate_skus": [], "evidence": [],
                "debug": {"agent_mode": "semantic_catalog_value_preflight_clarification"},
                "answer_metadata": {"source": "semantic_catalog_value_preflight_clarification"},
                "skip_polish": True,
            }
            early_catalog_clarification = _attach_phase1_plan_and_timing(
                early_catalog_clarification,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            early_catalog_clarification = _attach_semantic_preplan_debug(
                early_catalog_clarification, semantic_preplan, final_route="clarification"
            )
            return await _save_agent_result_and_return(
                db, user_id=user_id, question=question, conversation_id=conversation_id,
                agent_result=early_catalog_clarification, request_start=request_start,
                branch="semantic_catalog_value_preflight_clarification", semantic_preplan=semantic_preplan,
            )
        if (
            str(semantic_preplan.get("route_family") or "") == "generic_query"
            and str(semantic_preplan.get("route_hint") or "") == "query_products"
            and str(semantic_preplan.get("question_type") or "") in {"filter", "list", "count"}
            and not semantic_preplan.get("entities")
            and not current_turn_unique_product
        ):
            # A provider can understand that this is a catalogue browse while
            # omitting the formal category field. The current turn's explicit
            # category noun is still a bounded, database-verifiable scope, so
            # execute that scope before asking the customer to repeat it.
            recovered_catalog_ref = _semantic_catalog_product_ref(question)
            recovered_catalog_rows = (
                _phase1_catalog_rows(db, recovered_catalog_ref)
                if recovered_catalog_ref and recovered_catalog_ref != "产品"
                else []
            )
            if recovered_catalog_rows:
                recovered_catalog_result = _structured_product_query_result(
                    question=question,
                    product_ref=recovered_catalog_ref,
                    rows=recovered_catalog_rows,
                    source="semantic_generic_catalog_category_recovery",
                    reason_label="当前问题中明确给出的商品类目",
                    answer_type="query_products",
                )
                recovered_catalog_result = _attach_phase1_plan_and_timing(
                    recovered_catalog_result,
                    phase1_plan,
                    _phase1_timing(
                        request_start=request_start,
                        planner_duration_ms=planner_duration_ms,
                    ),
                )
                recovered_catalog_result = _attach_semantic_preplan_debug(
                    recovered_catalog_result,
                    semantic_preplan,
                    final_route="product_query",
                )
                return await _save_agent_result_and_return(
                    db,
                    user_id=user_id,
                    question=question,
                    conversation_id=conversation_id,
                    agent_result=recovered_catalog_result,
                    request_start=request_start,
                    branch="semantic_generic_catalog_category_recovery",
                    semantic_preplan=semantic_preplan,
                )
            generic_catalog_clarification = {
                "intent": "query_products",
                "answer_type": "clarification",
                "answer": "我识别到你想查询产品集合，但当前没有找到可核验的系列、品牌、类目或分级值。请补充中文名称、SKU，或确认筛选条件。",
                "results": [], "result_skus": [], "candidate_skus": [], "evidence": [],
                "debug": {"agent_mode": "semantic_catalog_unresolved_value_clarification"},
                "answer_metadata": {"source": "semantic_catalog_unresolved_value_clarification"},
                "skip_polish": True,
            }
            return await _save_agent_result_and_return(
                db, user_id=user_id, question=question, conversation_id=conversation_id,
                agent_result=generic_catalog_clarification, request_start=request_start,
                branch="semantic_catalog_unresolved_value_clarification", semantic_preplan=semantic_preplan,
            )
        # Keep route ownership with the validated whole-sentence semantic
        # plan.  In particular, an unbound recommendation is not ordinary
        # chat merely because its first JSON object lacks an executable
        # catalogue constraint.  The recommendation contract below will
        # either verify candidates or ask one safe narrowing question.
        phase1_plan["semantic_preplan"] = semantic_preplan
        if _semantic_prefers_sealed_product_qa(phase1_plan):
            formal_field_request = customer_field_contract.resolve_requested_field_contract(
                question,
                phase1_plan,
            )
            formal_field_type = str(formal_field_request.get("field_type") or "").strip()
            if formal_field_type in customer_field_contract.FORMAL_DETAIL_FIELDS:
                # A provider may label a natural formal-field paraphrase as
                # product QA. Normalize that plan once at the central
                # arbitration boundary so no later shortcut can consume it.
                semantic_preplan.update({
                    "route_family": "product_bound_qa",
                    "evidence_kind": "structured_field",
                    "field_type": formal_field_type,
                    "field_hint": formal_field_type,
                    "canonical_fields": [formal_field_type],
                    "qa_evidence_query": "",
                    "qa_evidence_queries": [],
                    "accepted_or_overridden": "formal_field_contract",
                })
                phase1_plan["semantic_preplan"] = semantic_preplan
            else:
            # The semantic plan has explicitly classified this as a
            # non-formal product QA turn. Legacy planner hints may still have
            # extracted a noun such as “规格” from the sentence, but cannot
            # turn that noun into the answer field after semantic arbitration.
            # Keep only the product-bound QA route; identity and evidence are
            # still sealed by their ordinary central contracts below.
                phase1_plan["requested_field"] = ""
                phase1_plan["requested_fields"] = []
                phase1_plan["field_only"] = False
                phase1_plan["tasks"] = [
                    task
                    for task in phase1_plan.get("tasks") or []
                    if not (isinstance(task, dict) and task.get("type") == "product_field")
                ]
        # A sealed exact QA outside the formal field ontology has stronger
        # evidence than a semantic attempt to force it into an unrelated
        # formal field.  Formal FieldContracts do not enter this branch.
        # Do not probe the QA shortcut before Phase 2 unless semantic planning
        # has explicitly classified the turn as product-bound QA. Otherwise a
        # failed/absent semantic call still rebuilds entity identity before the
        # formal FieldContract gets its one authoritative resolution pass.
        exact_generic_qa = (
            await _try_product_qa_shortcut_with_semantic_selection(
                db,
                question,
                phase1_plan=phase1_plan,
            )
            if (
                _semantic_allows_exact_sealed_product_qa(phase1_plan)
                and not _formal_field_contract_preempts_product_qa(question, phase1_plan)
            )
            else None
        )
        if (
            exact_generic_qa
            and not _is_sealed_product_qa_safe_missing(exact_generic_qa)
            and (
                ((exact_generic_qa.get("debug") or {}).get("field_contract") or {}).get("field_type") == "product_qa"
                or _semantic_preplan_route_family(semantic_preplan) == "structured_query"
            )
            and not _formal_field_contract_preempts_product_qa(question, phase1_plan)
        ):
            exact_generic_qa = _attach_phase1_plan_and_timing(
                exact_generic_qa,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=exact_generic_qa,
                request_start=request_start,
                branch="sealed_exact_product_qa_before_semantic_field",
                semantic_preplan=semantic_preplan,
            )
        if _semantic_pairwise_contract_candidate(semantic_preplan) and _semantic_preplan_confident(
            semantic_preplan,
            minimum=0.9,
        ):
            # Semantic planning owns comparison meaning and the participant
            # spans.  The catalogue is consulted only to validate each span
            # into its own EntityResolutionContract; it never receives an LLM
            # SKU choice or a lexical comparison rule.
            comparison_context_skus = (
                _latest_active_product_skus(db, conversation_id, user_id)
                if conversation_id
                else []
            )
            comparison_context_anchor_sku = (
                comparison_context_skus[0]
                if len(comparison_context_skus) == 1
                else None
            )
            comparison_contracts = _apply_semantic_comparison_plan(
                question,
                phase1_plan,
                semantic_preplan,
                db.query(Product).order_by(Product.sku.asc()).all(),
                context_anchor_sku=comparison_context_anchor_sku,
            )
            if not comparison_contracts:
                semantic_comparison_clarification = _semantic_comparison_fail_closed_result(semantic_preplan)
                if semantic_comparison_clarification:
                    semantic_comparison_clarification = _attach_phase1_plan_and_timing(
                        semantic_comparison_clarification,
                        phase1_plan,
                        _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
                    )
                    semantic_comparison_clarification = _attach_semantic_preplan_debug(
                        semantic_comparison_clarification,
                        semantic_preplan,
                        final_route="clarification",
                    )
                    return await _save_agent_result_and_return(
                        db,
                        user_id=user_id,
                        question=question,
                        conversation_id=conversation_id,
                        agent_result=semantic_comparison_clarification,
                        request_start=request_start,
                        branch="semantic_comparison_unsealed_pre_phase2",
                        semantic_preplan=semantic_preplan,
                    )
        semantic_field = str(semantic_preplan.get("field_type") or "").strip()
        semantic_catalog_aggregate = _semantic_catalog_aggregate_contract(semantic_preplan, phase1_plan)
        semantic_catalog_value_result = _semantic_catalog_value_query_result(
            db,
            question,
            semantic_preplan,
        )
        if semantic_catalog_value_result is None:
            adapted_catalog_plan = await _semantic_catalog_value_exact_adapter(
                db,
                question=question,
                semantic_preplan=semantic_preplan,
            )
            if adapted_catalog_plan is not None:
                semantic_catalog_value_result = _semantic_catalog_value_query_result(
                    db,
                    question,
                    adapted_catalog_plan,
                )
                if semantic_catalog_value_result:
                    metadata = semantic_catalog_value_result.setdefault("answer_metadata", {})
                    if isinstance(metadata, dict):
                        metadata["catalog_value_adapter"] = "semantic_exact_live_value"
        if semantic_catalog_value_result:
            semantic_catalog_value_result = _attach_phase1_plan_and_timing(
                semantic_catalog_value_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            semantic_catalog_value_result = _attach_semantic_preplan_debug(
                semantic_catalog_value_result,
                semantic_preplan,
                final_route="product_query",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=semantic_catalog_value_result,
                request_start=request_start,
                branch="semantic_database_value_catalog_filter",
                semantic_preplan=semantic_preplan,
            )
        if (
            semantic_field in {"series", "brand", "category", "product_level"}
            and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
            and _semantic_preplan_route_family(semantic_preplan) == "structured_query"
            and str(semantic_preplan.get("route_hint") or "") == "query_products"
            and not (semantic_preplan.get("entities") or [])
            and not any(token in question for token in ("多少", "几款", "几种", "几个"))
            and not semantic_catalog_aggregate
            and not has_explicit_operational_filter
        ):
            label = customer_field_contract.product_detail_field_label(semantic_field) or "筛选条件"
            safe_catalog_miss = {
                "intent": "query_products",
                "answer_type": "clarification",
                "answer": f"当前资料中未能将该{label}条件稳定对应到可核验的商品范围，请提供更完整的系列、品牌或类目名称后再查询。",
                "results": [], "result_skus": [], "candidate_skus": [], "evidence": [],
                "debug": {"agent_mode": "semantic_catalog_value_unverified_clarification"},
                "answer_metadata": {"source": "semantic_catalog_value_unverified_clarification"},
                "skip_polish": True,
            }
            return await _save_agent_result_and_return(
                db, user_id=user_id, question=question, conversation_id=conversation_id,
                agent_result=safe_catalog_miss, request_start=request_start,
                branch="semantic_catalog_value_unverified_clarification", semantic_preplan=semantic_preplan,
            )
        if (
            semantic_field in customer_field_contract.FORMAL_DETAIL_FIELDS
            and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
            and str(semantic_preplan.get("route_family") or "").strip() != "comparison"
            and not semantic_catalog_aggregate
        ):
            # The semantic preplan has only classified the requested field; it
            # has not supplied an entity or an answer.  Normalize the legacy
            # planner label before any Phase-1 executor can use its weaker
            # keyword interpretation, leaving Phase 2 to seal identity and
            # obtain structured same-SKU evidence.
            _apply_semantic_single_field_route_precedence(
                phase1_plan,
                semantic_preplan,
            )
            phase1_plan["requested_field"] = customer_field_contract.product_detail_field_label(semantic_field)
        if _should_execute_semantic_catalog_recommendation(semantic_preplan):
            # Route ownership comes from the full-sentence semantic plan.
            # A legacy catalogue/count label may remain as diagnostics, but it
            # cannot suppress an allowlisted recommendation contract.
            phase1_plan["primary_intent"] = "recommendation"
            phase1_plan["answer_type"] = "recommendation"
            phase1_plan["scenario"] = question

    if (
        _semantic_preplan_confident(semantic_preplan, minimum=0.65)
        and _semantic_preplan_route_family(semantic_preplan) == "general_chat"
        and (
            not (semantic_preplan.get("entities") or [])
            or str((semantic_preplan or {}).get("subtype") or "") == "safety_procedure"
        )
        and not (semantic_preplan.get("canonical_fields") or [])
        and not bool(semantic_preplan.get("evidence_required"))
        and (
            str((semantic_preplan or {}).get("subtype") or "") == "safety_procedure"
            or not _products_named_in_question(db, question)
        )
        and not _is_comparison_choice_followup_question(question)
        and (
            str((semantic_preplan or {}).get("subtype") or "") == "safety_procedure"
            or (
                not _structured_target_category_from_question(question)
                and not _phase1_is_stove_pairing_scenario(question)
            )
        )
    ):
        answer = ""
        try:
            semantic_runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
            messages = [
                {
                    "role": "system",
                    "content": "You are a concise Chinese outdoor-product customer assistant. Respond naturally to ordinary conversation and general decision guidance. You may explain preparation checklists, beginner considerations, category-level tradeoffs, general outdoor safety, and conservative cleaning/care troubleshooting when no catalogue product is named. For generic care, give practical ordered steps, explicitly account for unknown material or coating, and state any necessary cooling or other safety prerequisite before adding water or cleaning. When material or coating is unknown, stay with full cooling, warm-water soaking, neutral detergent, and a soft cloth or sponge; do not advise heating or boiling a cleaner, baking soda/alkali, vinegar/acid, abrasive powder, steel wool, or a hard scraper. Recommend stopping use when a coating is blistered, peeling, or deeply scratched. Do not state or infer any catalogue product fact, SKU, price, stock, shipping, warranty, promotion, or after-sales policy. Do not pretend to have searched the product catalogue and do not recommend a specific product. For a broad gift or beginner request with no chosen product category, give a compact selection framework based on the customer's usage, budget, group size, transport mode, and storage needs, then ask at most one focused follow-up; do not guess a concrete gear category or imply that an unverified category is sold in the catalogue. Ask one focused follow-up only when it is genuinely needed to make the general guidance useful.",
                },
                {"role": "user", "content": question},
            ]
            # A provider can occasionally return a blank successful response.
            # Retry the same non-evidentiary request once; this preserves the
            # semantic route without inventing a product-specific fallback.
            for _attempt in range(2):
                answer = str(await customer_llm_service.chat_completion(
                    db,
                    messages,
                    temperature=0.4,
                    # General guidance can legitimately need a short
                    # comparison plus a practical next step.  Keep one
                    # shared completion budget so the response is complete
                    # instead of being cut off mid-sentence.
                    max_tokens=420,
                    purpose="general_customer_chat",
                    # The default chat model can return an empty successful
                    # completion for ordinary advice. Use the same configured
                    # DeepSeek runtime that has already produced the semantic
                    # plan for this turn; it remains non-evidentiary and does
                    # not gain access to product facts or catalogue results.
                    api_model_override=semantic_runtime["model"],
                    thinking=semantic_runtime["thinking"],
                ) or "").strip()
                if answer:
                    break
            if answer:
                reviewed_answer = str(await customer_llm_service.chat_completion(
                    db,
                    [
                        {
                            "role": "system",
                            "content": "You are the final grounding editor for a Chinese outdoor customer assistant. Review the draft against the customer's actual request. Preserve useful, safe general guidance and natural tone. Never add catalogue facts, SKUs, prices, stock, policies, or claims that a product/category is sold. When the customer has explicitly not chosen a product category, or asks for broad gift/beginner selection guidance, do not guess or name concrete gear categories or product types; rewrite the answer as a compact decision framework using needs such as usage scenario, budget, group size, transport mode, and storage, followed by at most one focused question. Return only the final Chinese customer-facing answer, with no critique, labels, or JSON.",
                        },
                        {
                            "role": "user",
                            "content": f"Customer question:\n{question}\n\nDraft answer:\n{answer}",
                        },
                    ],
                    temperature=0.1,
                    max_tokens=420,
                    purpose="general_customer_chat_grounding_review",
                    api_model_override=semantic_runtime["model"],
                    thinking=semantic_runtime["thinking"],
                ) or "").strip()
                if reviewed_answer:
                    answer = reviewed_answer
        except Exception:
            answer = ""
        # Providers can return an empty successful completion.  A general
        # conversation turn has no product evidence to wait for, so preserve a
        # useful, non-product-specific response instead of emitting an empty
        # customer message.
        answer = str(answer or "").strip() or (
            "可以先从人数、行程、天气、是否需要做热食和收纳空间来梳理。"
            "你告诉我最在意的一项，我可以帮你把准备重点排个顺序。"
        )
        general_chat_result = {
            "answer": answer,
            "intent": "general_chat",
            "answer_type": "chat",
            "confidence": "high",
            "uncertainty": "resolved",
            "needs_clarification": False,
            "anomalies": [], "suggested_followups": [], "followups": [], "warnings": [],
            "evidence": [], "sources": [{"type": "semantic_chat", "label": "DeepSeek ordinary conversation"}],
            "actions": [], "results": [], "result_skus": [], "candidate_skus": [],
            "answer_metadata": {"source": "semantic_general_chat", "evidence_status": "not_applicable"},
            "debug": {"agent_mode": "semantic_general_chat"},
            "skip_polish": True,
        }
        general_chat_result = _attach_phase1_plan_and_timing(
            general_chat_result, phase1_plan, _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms)
        )
        return await _save_agent_result_and_return(
            db, user_id=user_id, question=question, conversation_id=conversation_id,
            agent_result=general_chat_result, request_start=request_start,
            branch="semantic_general_chat", semantic_preplan=semantic_preplan,
        )

    if _is_unbound_knowledge_meta_preplan(semantic_preplan):
        knowledge_meta_result = {
            "answer": "当前问题需要先定位具体知识库文件或商品资料；在没有可核验来源绑定时，不能据此生成商品推荐。请提供文件名称、商品名或 SKU 后再继续。",
            "intent": "clarification",
            "answer_type": "clarification",
            "confidence": "high",
            "uncertainty": "needs_source_scope",
            "needs_clarification": True,
            "anomalies": [],
            "suggested_followups": [],
            "followups": [],
            "warnings": ["unbound_knowledge_source"],
            "evidence": [],
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "answer_metadata": {
                "answer_policy": "source_scope_clarification",
                "evidence_status": "unbound",
                "final_decision": {"primary_source": "semantic_preplan", "priority": 1, "single_source_of_truth": True, "llm_allowed": False},
            },
            "debug": {"agent_mode": "semantic_unbound_knowledge_meta_clarification"},
        }
        knowledge_meta_result = _attach_phase1_plan_and_timing(
            knowledge_meta_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        knowledge_meta_result = _attach_semantic_preplan_debug(
            knowledge_meta_result,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=knowledge_meta_result,
            request_start=request_start,
            branch="semantic_unbound_knowledge_meta_clarification",
            semantic_preplan=semantic_preplan,
        )

    semantic_preplan_unavailable_result = _semantic_preplan_unavailable_clarification_result(
        semantic_preplan,
        db=db,
        question=question,
        phase1_plan=phase1_plan,
        request_sku_anchor=request_sku_anchor,
    )
    if semantic_preplan_unavailable_result:
        semantic_preplan_unavailable_result = _attach_phase1_plan_and_timing(
            semantic_preplan_unavailable_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_preplan_unavailable_result,
            request_start=request_start,
            branch="semantic_preplan_unavailable_clarification",
            semantic_preplan=semantic_preplan,
        )

    # A validated semantic plan can explicitly classify a catalogue-level
    # realtime/commercial question (for example sales or reviews) as safely
    # unsupported.  Preserve that field meaning before generic field
    # clarification or recommendation routing sees category words in the same
    # sentence.  This does not infer a field from wording: the semantic subtype
    # authorizes only the existing safe-missing formatter.
    if (
        _semantic_preplan_unknown_realtime_subtype(semantic_preplan, question)
        and _semantic_preplan_entity_scope(semantic_preplan) in {"category_scope", "generic_scope"}
        and str((semantic_preplan or {}).get("subtype") or "").strip() != "no_match"
        and not [item for item in ((semantic_preplan or {}).get("entities") or []) if str(item or "").strip()]
        and not _products_named_in_question(db, question)
    ):
        semantic_unknown_catalog_result = _unknown_product_fact_result(
            question,
            semantic_authorized=True,
        )
        if semantic_unknown_catalog_result:
            semantic_unknown_catalog_result = _attach_phase1_plan_and_timing(
                semantic_unknown_catalog_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            semantic_unknown_catalog_result = _attach_semantic_preplan_debug(
                semantic_unknown_catalog_result,
                semantic_preplan,
                final_route="product_detail",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=semantic_unknown_catalog_result,
                request_start=request_start,
                branch="semantic_unknown_realtime_catalog_safe_missing",
                semantic_preplan=semantic_preplan,
            )

    invalid_semantic_pairwise_result = _invalid_semantic_pairwise_preplan_clarification_result(
        question,
        phase1_plan,
        semantic_preplan,
        _products_named_in_question(db, question),
    )
    # An unavailable semantic planner must not erase a fully explicit SKU pair
    # and an explicit “which is more suitable” criterion.  The deterministic
    # Phase-1 comparator can seal both rows and make the requested choice using
    # the stored people/capacity evidence; it does not infer a product from a
    # keyword or emit a broad catalogue.
    if (
        invalid_semantic_pairwise_result
        and customer_agent_planner_service._is_compare_choice_question(question)
    ):
        explicit_pair_skus = _explicit_current_turn_comparison_skus(db, question)
        if len(explicit_pair_skus) >= 2:
            deterministic_choice_plan = dict(phase1_plan or {})
            deterministic_choice_plan.update({
                "raw_question": question,
                "product_refs": explicit_pair_skus,
                "must_make_choice": True,
                "semantic_preplan": {},
                "semantic_comparison_entity_contracts": [],
            })
            deterministic_choice_result = await _phase1_compare_choice_result(
                db,
                deterministic_choice_plan,
            )
            if deterministic_choice_result:
                deterministic_choice_result = _attach_phase1_plan_and_timing(
                    deterministic_choice_result,
                    deterministic_choice_plan,
                    _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
                )
                debug = deterministic_choice_result.get("debug") if isinstance(deterministic_choice_result.get("debug"), dict) else {}
                debug["agent_mode"] = "deterministic_explicit_pair_choice_during_semantic_outage"
                debug["semantic_fallback_reason"] = str((semantic_preplan or {}).get("fallback_reason") or "")
                deterministic_choice_result["debug"] = debug
                return await _save_agent_result_and_return(
                    db,
                    user_id=user_id,
                    question=question,
                    conversation_id=conversation_id,
                    agent_result=deterministic_choice_result,
                    request_start=request_start,
                    branch="deterministic_explicit_pair_choice_during_semantic_outage",
                    semantic_preplan=semantic_preplan,
                )
    if invalid_semantic_pairwise_result:
        invalid_semantic_pairwise_result = _attach_phase1_plan_and_timing(
            invalid_semantic_pairwise_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=invalid_semantic_pairwise_result,
            request_start=request_start,
            branch="semantic_pairwise_invalid_preplan_clarification",
            semantic_preplan=semantic_preplan,
        )

    # Recommendation constraints may be semantically valid, yet their stated
    # subject can still be an unresolved product-like name.  Bind or clarify
    # that identity before any recommendation contract or its empty-constraint
    # clarification can consume the turn as a generic catalogue request.
    semantic_entity_scope = _semantic_preplan_entity_scope(semantic_preplan)
    unresolved_entity_scope_result = None
    if (
        _semantic_preplan_route_family(semantic_preplan) == "recommendation"
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and semantic_entity_scope in {
            "unresolved_product_like",
            "unresolved_product",
            "negative_product_like",
            "negative_product",
        }
    ):
        unresolved_entity_scope_result = _entity_scope_pre_route_guard_result(
            db,
            question,
            phase1_plan,
            semantic_preplan,
        )
    if unresolved_entity_scope_result:
        unresolved_entity_scope_result = _attach_phase1_plan_and_timing(
            unresolved_entity_scope_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        unresolved_entity_scope_result = _attach_semantic_preplan_debug(
            unresolved_entity_scope_result,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=unresolved_entity_scope_result,
            request_start=request_start,
            branch="unresolved_entity_scope_before_semantic_recommendation",
            semantic_preplan=semantic_preplan,
        )

    # A named product field request cannot be consumed as a catalogue
    # recommendation merely because its canonical name contains numbers or
    # product-type words.  This path forms the normal FieldContract first;
    # same-SKU QA remains its fallback evidence rather than a route override.
    named_detail_before_recommendation = None
    if (
        _semantic_preplan_route_family(semantic_preplan) == "recommendation"
        and not _looks_like_recommendation_request(question)
        and (
            bool(semantic_preplan.get("entities") or [])
            or bool(str(semantic_preplan.get("subject_text") or "").strip())
        )
    ):
        named_detail_before_recommendation = _try_product_qa_shortcut(db, question)
        if not named_detail_before_recommendation:
            named_detail_before_recommendation = await _try_named_product_shortcut(
                db,
                user_id=user_id,
                question=question,
            )
    if named_detail_before_recommendation:
        named_detail_before_recommendation = _attach_phase1_plan_and_timing(
            named_detail_before_recommendation,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=named_detail_before_recommendation,
            request_start=request_start,
            branch="named_product_contract_before_semantic_recommendation",
            semantic_preplan=semantic_preplan,
        )

    deterministic_structured_preflight = (
        customer_structured_query_contract.build_structured_query_contract(question)
    )
    # Fuel/category nouns can make a duration question look like a structured
    # heat-source filter.  Without a resolved product, keep the temporal
    # request on the usage-care boundary instead of returning an empty search.
    unbound_duration_usage = (
        customer_agent_intent_service._detect_usage_care_subtype(question) == "duration"
        and not request_sku_anchor
        # Generic nouns such as “气罐” can produce coincidental resolver
        # candidates (including adapters).  They are not a sealed product
        # identity, so keep the duration answer on the product-agnostic route.
        and (
            not _products_named_in_question(db, question)
            or _has_only_generic_product_subject(question)
        )
        and not _looks_like_recommendation_request(question)
    )
    if unbound_duration_usage:
        duration_result = {
            "intent": "product_usage_care",
            "answer_type": "product_usage_care",
            "answer": customer_agent_intent_service._compose_duration_usage_care_answer(question, [], []),
            "results": [],
            "result_skus": [],
            "candidate_skus": [],
            "evidence": [],
            "needs_clarification": False,
            "answer_metadata": {"source": "unbound_duration_usage_boundary", "evidence_status": "missing"},
            "debug": {"agent_mode": "unbound_duration_usage_boundary", "usage_care_subtype": "duration"},
        }
        duration_result = _attach_phase1_plan_and_timing(
            duration_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        duration_result = _attach_semantic_preplan_debug(
            duration_result,
            semantic_preplan,
            final_route="product_usage_care",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=duration_result,
            request_start=request_start,
            branch="unbound_duration_usage_boundary",
            semantic_preplan=semantic_preplan,
        )
    if (
        deterministic_structured_preflight.status == "resolved"
        and deterministic_structured_preflight.conditions
        and not (
            _semantic_preplan_route_family(semantic_preplan) == "general_chat"
            and str((semantic_preplan or {}).get("subtype") or "") == "safety_procedure"
        )
        and not _looks_like_recommendation_request(question)
        and not (
            conversation_id
            and any(term in str(question or "") for term in ("这款", "这个", "它", "该商品", "该产品"))
            and not _has_explicit_sku_text(question)
            and bool(_latest_persisted_agent_context_sku(db, conversation_id))
            and str(resolve_requested_field_contract(question, phase1_plan).get("field_type") or "")
            in customer_field_contract.FORMAL_DETAIL_FIELDS
        )
        # Broad category nouns (for example ``锅具`` in
        # ``哪些锅具适配燃气炉``) may resolve to many catalogue rows, but
        # they are still unbound category queries.  Do not let candidate
        # resolution suppress the structured contract for these requests.
        and (
            not _products_named_in_question(db, question)
            or _has_only_generic_product_subject(question)
        )
    ):
        deterministic_structured_result = _build_structured_query_contract_result(
            db,
            deterministic_structured_preflight,
        )
        deterministic_structured_result = _attach_phase1_plan_and_timing(
            deterministic_structured_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        deterministic_structured_result = _attach_semantic_preplan_debug(
            deterministic_structured_result,
            semantic_preplan,
            final_route="query_products",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=deterministic_structured_result,
            request_start=request_start,
            branch="structured_query_contract_preflight",
            semantic_preplan=semantic_preplan,
        )

    # A validated semantic recommendation constraint is already the complete
    # meaning of the request.  Consume it before every legacy recommendation
    # grammar/scoring path; the executor below only verifies actual same-SKU
    # catalogue evidence and never asks lexical rules to reinterpret intent.
    # A bare catalogue listing is an inventory/set query, even when the
    # semantic provider labels the noun as a category recommendation.  Keep
    # this preflight ahead of the semantic recommendation executor; otherwise
    # the executor can consume "有哪些炉具" and turn it into a personalized
    # top-three recommendation before the listing contract gets a chance to
    # preserve the full candidate set for a later comparison turn.
    plain_catalog_listing_preflight = (
        _looks_like_semantic_catalog_query(question)
        and phase1_plan.get("primary_intent") != "catalog_count"
        and not customer_agent_service._extract_skus(question)
        and not _products_named_in_question(db, question)
        and not any(term in question for term in ("推荐", "适合", "怎么选", "买", "选哪", "更"))
    )
    semantic_preplan_active = bool(
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("called") is True
        and (
            str(semantic_preplan.get("route_family") or "").strip() == "recommendation"
            or str(semantic_preplan.get("route_hint") or "").strip() == "recommendation"
        )
    )
    if _asks_for_alternative_recommendation(question) and conversation_id and semantic_preplan_active:
        semantic_alternative_result = await _alternative_recommendation_followup_result(
            db,
            question,
            _latest_recommendation_context_for_sources(db, conversation_id),
            _latest_candidate_context_for_sources(db, conversation_id),
        )
        if semantic_alternative_result:
            semantic_alternative_result = _attach_phase1_plan_and_timing(
                semantic_alternative_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            semantic_alternative_result = _attach_semantic_preplan_debug(
                semantic_alternative_result,
                semantic_preplan,
                final_route="recommendation",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=semantic_alternative_result,
                request_start=request_start,
                branch="recommendation_context_alternative_before_semantic_recommendation",
                semantic_preplan=semantic_preplan,
            )
    semantic_recommendation_result = None if plain_catalog_listing_preflight else await _semantic_recommendation_contract_result(
        db,
        question,
        phase1_plan,
        _latest_recommendation_context_for_sources(db, conversation_id) if conversation_id else None,
    )
    if semantic_recommendation_result:
        semantic_recommendation_result = _post_filter_recommendation_result(
            db,
            question,
            semantic_recommendation_result,
        )
        semantic_recommendation_result = _attach_phase1_plan_and_timing(
            semantic_recommendation_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        semantic_recommendation_result = _attach_semantic_preplan_debug(
            semantic_recommendation_result,
            semantic_preplan,
            final_route="recommendation",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_recommendation_result,
            request_start=request_start,
            branch="semantic_recommendation_contract",
            semantic_preplan=semantic_preplan,
        )

    # The semantic provider can recognize a stove-pairing scenario while
    # failing to serialize its multi-domain recommendation constraints.  This
    # bounded scenario contract remains safe because it has no product identity
    # to resolve and selects only verified stove-domain rows.
    if (
        _phase1_is_stove_pairing_scenario(question)
        and not request_sku_anchor
        and not customer_agent_service._extract_skus(question)
        and not _products_named_in_question(db, question)
    ):
        stove_pairing_result = _phase1_structured_recommendation_result(
            db,
            {
                "primary_intent": "recommendation",
                "scenario": question,
                "raw_question": question,
            },
        )
        if stove_pairing_result:
            stove_pairing_result = _post_filter_recommendation_result(
                db,
                question,
                stove_pairing_result,
            )
            stove_pairing_result = _attach_phase1_plan_and_timing(
                stove_pairing_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            stove_pairing_result = _attach_semantic_preplan_debug(
                stove_pairing_result,
                semantic_preplan,
                final_route="recommendation",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=stove_pairing_result,
                request_start=request_start,
                branch="stove_pairing_contract_after_semantic_failure",
                semantic_preplan=semantic_preplan,
            )

    semantic_recommendation_clarification = _semantic_recommendation_constraint_clarification_result(
        semantic_preplan,
        question,
    )
    # A failed recommendation JSON must not hide an independently validated
    # formal catalogue filter from the same turn.  This preserves the
    # semantic decision (the dishwasher field) while letting the deterministic
    # layer execute only the category/evidence contract it can prove.
    semantic_structured_fields = [
        str(item or "").strip()
        for item in [
            *((semantic_preplan or {}).get("canonical_fields") or []),
            (semantic_preplan or {}).get("field_type"),
            (semantic_preplan or {}).get("field_hint"),
        ]
        if str(item or "").strip()
    ]
    if (
        semantic_recommendation_clarification
        and semantic_structured_fields
        and not _products_named_in_question(db, question)
    ):
        structured_catalog_result = _structured_field_filter_result(db, question)
        if structured_catalog_result:
            structured_catalog_result = _attach_phase1_plan_and_timing(
                structured_catalog_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            structured_catalog_result = _attach_semantic_preplan_debug(
                structured_catalog_result,
                semantic_preplan,
                final_route="query_products",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=structured_catalog_result,
                request_start=request_start,
                branch="semantic_failed_recommendation_structured_filter",
                semantic_preplan=semantic_preplan,
            )
    if semantic_recommendation_clarification:
        semantic_recommendation_clarification = _attach_phase1_plan_and_timing(
            semantic_recommendation_clarification,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        semantic_recommendation_clarification = _attach_semantic_preplan_debug(
            semantic_recommendation_clarification,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_recommendation_clarification,
            request_start=request_start,
            branch="semantic_recommendation_constraint_clarification",
            semantic_preplan=semantic_preplan,
        )

    recovered_invalid_structured_named_detail = _invalid_structured_query_named_detail_contract_result(
        db,
        question,
        semantic_preplan,
    )
    if recovered_invalid_structured_named_detail:
        recovered_invalid_structured_named_detail = _attach_phase1_plan_and_timing(
            recovered_invalid_structured_named_detail,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=recovered_invalid_structured_named_detail,
            request_start=request_start,
            branch="invalid_structured_query_named_detail_contract",
            semantic_preplan=semantic_preplan,
        )

    semantic_structured_query_clarification = _semantic_structured_query_constraint_clarification_result(
        semantic_preplan,
        question=question,
    )
    if semantic_structured_query_clarification:
        semantic_structured_query_clarification = _attach_phase1_plan_and_timing(
            semantic_structured_query_clarification,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        semantic_structured_query_clarification = _attach_semantic_preplan_debug(
            semantic_structured_query_clarification,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_structured_query_clarification,
            request_start=request_start,
            branch="semantic_structured_query_constraint_clarification",
            semantic_preplan=semantic_preplan,
        )

    # Once semantic planning has sealed a named pair, execute that comparison
    # before any later product-query fallback can reinterpret the same names.
    if phase1_plan.get("semantic_comparison_entity_contracts"):
        pairwise_result = await _phase1_compare_choice_result(db, phase1_plan)
        if pairwise_result:
            pairwise_result = _attach_phase1_plan_and_timing(
                pairwise_result, phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db, user_id=user_id, question=question, conversation_id=conversation_id,
                agent_result=pairwise_result, request_start=request_start,
                branch="semantic_pairwise_contract", semantic_preplan=semantic_preplan,
            )

    semantic_navigation_result = _semantic_product_navigation_contract_result(
        db,
        question,
        semantic_preplan,
    )
    if semantic_navigation_result:
        semantic_navigation_result = _attach_phase1_plan_and_timing(
            semantic_navigation_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_navigation_result,
            request_start=request_start,
            branch="semantic_product_navigation_contract",
            semantic_preplan=semantic_preplan,
        )

    # Resolve a compound utterance before whole-question entity arbitration.
    # Otherwise a valid fact field can seal the first SKU and discard the
    # separately requested recommendation task.
    composite_question = _split_composite_customer_question(question)
    if composite_question:
        composite_result = await _execute_composite_customer_question(
            db,
            user_id=user_id,
            composite_question=composite_question,
            sku=sku,
        )
        if composite_result:
            composite_result = _attach_phase1_plan_and_timing(
                composite_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            composite_result = _attach_semantic_preplan_debug(
                composite_result,
                semantic_preplan,
                final_route=str(composite_result.get("answer_type") or "recommendation"),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=composite_result,
                request_start=request_start,
                branch="composite_multi_intent_pre_entity_arbitration",
                semantic_preplan=semantic_preplan,
            )

    active_anchor_context = _latest_recommendation_context_for_sources(db, conversation_id)
    active_candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    if (
        isinstance(active_anchor_context, dict)
        and isinstance(active_candidate_context, dict)
        and not str(active_anchor_context.get("active_single_product_anchor") or "").strip()
    ):
        visible_recommendations = list(dict.fromkeys(
            str(value or "").strip().upper()
            for value in (active_candidate_context.get("recommended_skus") or [])
            if str(value or "").strip()
        ))
        final_choice_sku = str(active_candidate_context.get("final_choice_sku") or "").strip().upper()
        last_referenced_sku = str(active_candidate_context.get("last_referenced_sku") or "").strip().upper()
        if final_choice_sku:
            active_anchor_context = dict(active_anchor_context)
            active_anchor_context["active_single_product_anchor"] = final_choice_sku
            active_anchor_context["anchor_update_reason"] = "explicit_final_choice"
        elif len(visible_recommendations) == 1:
            active_anchor_context = dict(active_anchor_context)
            active_anchor_context["active_single_product_anchor"] = visible_recommendations[0]
            active_anchor_context["anchor_update_reason"] = "unique_visible_recommendation"
        elif last_referenced_sku:
            active_anchor_context = dict(active_anchor_context)
            active_anchor_context["active_single_product_anchor"] = last_referenced_sku
            active_anchor_context["anchor_update_reason"] = "latest_candidate_reference"
        else:
            # A normal product-detail/QA turn is not a recommendation, so it
            # need not populate recommendation_context.  A later pronoun may
            # still safely refer to its one sealed result: recover only the
            # most recent single current SKU already persisted in this same
            # conversation.  The current semantic contract still decides the
            # field or product-QA meaning; this supplies identity only.
            latest_active_skus = _latest_active_product_skus(db, conversation_id, user_id)
            if len(latest_active_skus) == 1:
                active_anchor_context = dict(active_anchor_context)
                active_anchor_context["active_single_product_anchor"] = latest_active_skus[0]
                active_anchor_context["anchor_update_reason"] = "latest_single_product_result"
    if _should_force_runtime_empty_subset_followup(
        question,
        explicit_sku=sku,
        named_products=_products_named_in_question(db, question),
        candidate_context=active_candidate_context,
    ):
        empty_subset_result = _empty_candidate_subset_followup_result(
            question,
            active_candidate_context,
        )
        empty_subset_result = _attach_phase1_plan_and_timing(
            empty_subset_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=empty_subset_result,
            request_start=request_start,
            branch="empty_candidate_subset_followup",
            semantic_preplan=semantic_preplan,
        )
    recommendation_explanation_result = _recommendation_explanation_followup_result(
        db,
        question,
        active_anchor_context,
        active_candidate_context,
    )
    if recommendation_explanation_result:
        recommendation_explanation_result = _attach_phase1_plan_and_timing(
            recommendation_explanation_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=recommendation_explanation_result,
            request_start=request_start,
            branch="recommendation_context_explanation_followup",
            semantic_preplan=semantic_preplan,
        )
    explicit_compatibility_result = await _explicit_multi_product_compatibility_contract_result(db, question)
    if explicit_compatibility_result:
        explicit_compatibility_result = _attach_phase1_plan_and_timing(
            explicit_compatibility_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=explicit_compatibility_result,
            request_start=request_start,
            branch="explicit_multi_entity_compatibility_contract",
            semantic_preplan=semantic_preplan,
        )
    active_anchor_context = dict(active_anchor_context or {})
    if not str(active_anchor_context.get("active_single_product_anchor") or "").strip():
        persisted_anchor_sku = _latest_persisted_agent_context_sku(db, conversation_id)
        if persisted_anchor_sku:
            active_anchor_context["active_single_product_anchor"] = persisted_anchor_sku
        else:
            latest_active_skus = _latest_active_product_skus(db, conversation_id, user_id)
            if len(latest_active_skus) == 1:
                active_anchor_context["active_single_product_anchor"] = latest_active_skus[0]
                active_anchor_context["anchor_update_reason"] = "latest_single_product_result_fallback"
    active_anchor_result = None
    if not _is_contextual_power_followup_question(question):
        active_anchor_result = await _active_product_anchor_field_followup_result(
            db,
            question,
            active_anchor_context,
            phase1_plan,
            active_candidate_context,
        )
    if active_anchor_result:
        active_anchor_result = _attach_phase1_plan_and_timing(
            active_anchor_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=active_anchor_result,
            request_start=request_start,
            branch="active_product_anchor_field",
            semantic_preplan=semantic_preplan,
        )

    # A deictic singular subject is an entity reference, not a catalogue
    # category.  If the anchor resolver did not bind it above, fail closed
    # before semantic structured-query execution can turn “this pot” into a
    # search across every pot.  This deliberately contains no category or
    # field-specific routing: a later turn may supply any SKU/name and resume
    # the field request through the normal contract path.
    # A preserved pair is a sealed plural entity scope.  Let the dedicated
    # pair-followup contract consume it later; the singular deictic guard must
    # not intercept "它们" merely because it begins with "它".  Without a
    # preserved pair the same wording still fails closed below.
    preserved_pair_skus = (
        _context_skus_for_pair_followup(active_anchor_context, active_candidate_context)
        if _is_plural_heat_source_followup(question)
        else []
    )
    # A recommendation refinement is not a request for facts about the
    # deictic product.  Its prior recommendation set is the context contract
    # and is consumed by the recommendation path below; treating “这个太重，
    # 换个轻点的” as an unbound product fact loses that safe candidate scope.
    has_recommendation_followup_context = _has_followup_result_context(active_anchor_context)
    semantic_anchor_pending = bool(
        str((semantic_preplan or {}).get("context_usage") or "").strip() == "entity_anchor"
        and str((active_anchor_context or {}).get("active_single_product_anchor") or "").strip()
    )
    unbound_deictic_result = None if (
        preserved_pair_skus
        or request_sku_anchor
        or (has_recommendation_followup_context and _is_recommendation_followup_question(question))
        or _is_product_usage_care_question(question)
        # Semantic planning already identified the current turn as a
        # context-bound product QA request and the conversation has one sealed
        # active SKU. Let only the ordinary same-SKU QA/RAG path consume it;
        # without both signals, retain the fail-closed pronoun clarification.
        or semantic_anchor_pending
    ) else _unbound_deictic_product_reference_result(
        db,
        question,
        field_type=(phase1_plan.get("field_contract") or {}).get("field_type"),
    )
    if unbound_deictic_result:
        unbound_deictic_result = _attach_phase1_plan_and_timing(
            unbound_deictic_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        unbound_deictic_result = _attach_semantic_preplan_debug(
            unbound_deictic_result,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=unbound_deictic_result,
            request_start=request_start,
            branch="unbound_deictic_entity_contract_guard",
            semantic_preplan=semantic_preplan,
        )

    ambiguity_candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    ambiguity_refinement_result = _ambiguity_candidate_attribute_followup_result(
        db,
        question,
        ambiguity_candidate_context,
    )
    if ambiguity_refinement_result:
        ambiguity_refinement_result = _attach_phase1_plan_and_timing(
            ambiguity_refinement_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=ambiguity_refinement_result,
            request_start=request_start,
            branch="candidate_context_variant_refinement",
            semantic_preplan=semantic_preplan,
        )

    # Before the realtime/commercial guard produces a safe-missing answer,
    # give an explicit same-SKU shipping QA one chance to satisfy the formal
    # field contract. The helper still requires unique entity resolution and
    # field-compatible QA evidence; absence of either falls through to the
    # guard unchanged.
    pre_guard_shipping_qa = None
    semantic_shipping_field = bool(
        _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and str((semantic_preplan or {}).get("field_type") or "").strip() == "shipping"
    )
    if customer_field_contract.detect_shipping_intent_signal(question) or semantic_shipping_field:
        pre_guard_shipping_qa = _try_product_qa_shortcut(
            db,
            question,
            phase1_plan=phase1_plan,
        )
    if pre_guard_shipping_qa:
        pre_guard_shipping_qa = _attach_phase1_plan_and_timing(
            pre_guard_shipping_qa,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=pre_guard_shipping_qa,
            request_start=request_start,
            branch="sealed_shipping_qa_before_high_risk_guard",
            semantic_preplan=semantic_preplan,
        )

    high_risk_contract_result = _pre_route_high_risk_contract_result(
        db,
        question,
        semantic_preplan,
    )
    if high_risk_contract_result:
        high_risk_contract_result = _attach_formal_field_contract(
            high_risk_contract_result,
            question,
            phase1_plan,
        )
        high_risk_contract_result = _attach_phase1_plan_and_timing(
            high_risk_contract_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        high_risk_contract_result = _attach_semantic_preplan_debug(
            high_risk_contract_result,
            semantic_preplan,
            final_route=str(high_risk_contract_result.get("answer_type") or high_risk_contract_result.get("intent") or ""),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=high_risk_contract_result,
            request_start=request_start,
            branch="pre_route_high_risk_contract",
            semantic_preplan=semantic_preplan,
        )

    # A product-only reply to an explicit earlier clarification is already a
    # formal conversational field contract: the current turn seals identity
    # and the saved clarification supplies the requested field.  It must be
    # applied before the semantic-outage guard, otherwise a transient planner
    # transport error turns a valid contract continuation into a new generic
    # clarification.
    clarification_followup_start = perf_counter()
    clarification_followup_result = await _try_pending_clarification_followup(
        db,
        user_id=user_id,
        conversation_id=conversation_id,
        question=question,
    )
    customer_perf_service.log_stage(
        "clarification_followup_guard",
        clarification_followup_start,
        hit=bool(clarification_followup_result),
        agent_mode=(clarification_followup_result.get("debug") or {}).get("agent_mode") if clarification_followup_result else None,
    )
    if clarification_followup_result:
        clarification_followup_result = _attach_phase1_plan_and_timing(
            clarification_followup_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        clarification_followup_result = _attach_semantic_preplan_debug(
            clarification_followup_result,
            semantic_preplan,
            final_route="product_detail",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=clarification_followup_result,
            request_start=request_start,
            branch="clarification_followup_guard",
            semantic_preplan=semantic_preplan,
        )

    semantic_outage_result = _semantic_outage_named_product_field_clarification_result(
        db,
        question,
        semantic_preplan,
    )
    if semantic_outage_result:
        semantic_outage_result = _attach_phase1_plan_and_timing(
            semantic_outage_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        semantic_outage_result = _attach_semantic_preplan_debug(
            semantic_outage_result,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_outage_result,
            request_start=request_start,
            branch="semantic_outage_named_product_field_guard",
            semantic_preplan=semantic_preplan,
        )

    explicit_comparison_skus = _explicit_current_turn_comparison_skus(db, question)
    if explicit_comparison_skus and isinstance(phase1_plan, dict):
        # Literal catalogue-exact SKUs are stronger than a stochastic route
        # label.  Seal each participant now so an invalid semantic preplan
        # cannot send a two-product comparison into single-entity arbitration.
        _apply_explicit_sku_comparison_plan(
            question,
            phase1_plan,
            db.query(Product).order_by(Product.sku.asc()).all(),
        )
    phase2_arbitration_signals = _phase2_entity_arbitration_signals(
        question,
        phase1_plan,
        semantic_preplan,
        has_named_product=bool(_products_named_in_question(db, question)),
    )
    structured_query_contract = customer_structured_query_contract.build_structured_query_contract(question)
    # Semantic planning decides which independent customer constraints are
    # present in the whole sentence.  The adapter accepts only a validated,
    # literal predicate schema and keeps the deterministic catalogue subject
    # from the existing contract; it never accepts an LLM-provided product or
    # database fact.  This prevents a legacy single-filter parser from
    # silently broadening a high-confidence multi-condition request.
    semantic_structured_query_contract = (
        customer_structured_query_contract.adapt_semantic_structured_query_contract(
            question=question,
            base_contract=structured_query_contract,
            semantic_preplan=semantic_preplan,
        )
    )
    if semantic_structured_query_contract is not None:
        structured_query_contract = semantic_structured_query_contract
    semantic_structured_query_contract_active = semantic_structured_query_contract is not None
    if structured_query_contract.status == "resolved":
        phase2_arbitration_signals["structured_query"] = True
    # Keep the validated semantic field signal explicit at the one formal
    # FieldContract boundary.  Planner compatibility output may contain a
    # broad lexical label (for example ``brand`` inside a question about a
    # brand's price tier); it is not permitted to replace this schema-checked
    # field classification.  The semantic payload supplies neither entity nor
    # evidence: Phase 2 still seals both from the catalogue and same-SKU
    # structured provider.
    phase2_field_plan = dict(phase1_plan) if isinstance(phase1_plan, dict) else {}
    if isinstance(semantic_preplan, dict) and semantic_preplan.get("called"):
        phase2_field_plan["semantic_preplan"] = semantic_preplan
    semantic_subject = _semantic_exact_subject_for_field_contract(
        db,
        question,
        semantic_preplan,
    )
    phase2_field_request = resolve_requested_field_contract(
        question,
        phase2_field_plan,
        subject_override=semantic_subject or None,
    )
    # A semantic plan owns field meaning, but it may omit one of several
    # independently and explicitly requested formal fields.  Preserve the
    # complete current-turn field contract before entity arbitration; the
    # downstream provider still reads every value from the same sealed SKU.
    phase2_field_request = _merge_explicit_detail_fields_into_request(
        question,
        phase2_field_request,
    )
    if isinstance(phase1_plan, dict):
        phase1_plan["field_contract"] = dict(phase2_field_request)
        phase1_plan["requested_fields"] = phase2_field_request["requested_fields"]
        phase1_plan["multi_field"] = bool(phase2_arbitration_signals.get("multi_field"))
        phase1_plan["additional_user_intent"] = bool(phase2_arbitration_signals.get("additional_user_intent"))
        phase1_plan["compound"] = bool(phase2_arbitration_signals.get("compound"))
        phase1_plan["dedicated_route_signal"] = _dedicated_semantic_route(
            question,
            has_named_product=bool(_products_named_in_question(db, question)),
        )
    phase2_context_needed = bool(
        (
            phase2_field_request.get("source") == "validated_semantic_preplan"
            and len(phase2_field_request.get("supported_fields") or []) == 1
        )
        or (
            _phase2_single_field_arbitration_eligible(
                question,
                phase1_plan,
                conversation_id=conversation_id,
            )
            and (
                len(phase2_field_request["supported_fields"]) == 1
                or _has_non_usage_care_field_contract(question)
            )
        )
        or (
            phase2_arbitration_signals.get("compound")
            and phase2_arbitration_signals.get("multi_field")
            and not phase2_arbitration_signals.get("recommendation")
            and not phase2_arbitration_signals.get("comparison")
            and not phase2_arbitration_signals.get("structured_query")
            and not phase2_arbitration_signals.get("multi_product")
            and not phase2_arbitration_signals.get("dedicated_route")
        )
    )
    # Build one catalogue-aware context even when the preliminary field parser
    # saw multiple tokens.  An exact product name may itself contain a field
    # word (for example a stove name containing "酒精炉"); only the catalogue-
    # aware subject span can distinguish that identity text from the predicate.
    phase2_entity_context = _build_phase2_entity_resolution_context(
        db,
        question,
        phase2_field_request,
        phase1_plan,
    )
    effective_field_request = (phase2_entity_context or {}).get("field_request")
    if isinstance(effective_field_request, dict):
        phase2_field_request = effective_field_request
        phase2_arbitration_signals = _phase2_entity_arbitration_signals(
            question,
            phase1_plan,
            semantic_preplan,
            field_request_override=effective_field_request,
            has_named_product=bool(_products_named_in_question(db, question)),
        )
        if structured_query_contract.status == "resolved":
            phase2_arbitration_signals["structured_query"] = True
        if isinstance(phase1_plan, dict):
            phase1_plan["field_contract"] = dict(effective_field_request)
            phase1_plan["requested_fields"] = effective_field_request["requested_fields"]
            phase1_plan["multi_field"] = bool(phase2_arbitration_signals.get("multi_field"))
            phase1_plan["additional_user_intent"] = bool(phase2_arbitration_signals.get("additional_user_intent"))
            phase1_plan["compound"] = bool(phase2_arbitration_signals.get("compound"))
    candidate_field_filter_result = _candidate_context_formal_field_filter_result(
        db,
        question=question,
        field_request=phase2_field_request,
        candidate_context=active_candidate_context,
    )
    if candidate_field_filter_result:
        candidate_field_filter_result = _attach_phase1_plan_and_timing(
            candidate_field_filter_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        candidate_field_filter_result = _attach_semantic_preplan_debug(
            candidate_field_filter_result,
            semantic_preplan,
            final_route="product_query",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=candidate_field_filter_result,
            request_start=request_start,
            branch="candidate_context_formal_field_filter",
            semantic_preplan=semantic_preplan,
        )
    phase2_contract = (phase2_entity_context or {}).get("contract")
    phase2_field_type = str(phase2_field_request.get("field_type") or "").strip()
    resolved_formal_field_scope = bool(
        phase2_field_type in customer_field_contract.FORMAL_DETAIL_FIELDS
        and (
            (
                phase2_contract
                and str(getattr(phase2_contract, "status", "") or "") == "resolved"
                and str(getattr(phase2_contract, "resolved_sku", "") or "").strip()
            )
            or str(phase1_plan.get("primary_intent") or "").strip() == "category_compatibility"
        )
    )
    semantic_field_ambiguity_result = _semantic_field_ambiguity_clarification_result(
        semantic_preplan,
        phase2_entity_context,
        phase2_field_request,
    )
    if semantic_field_ambiguity_result:
        semantic_field_ambiguity_result = _attach_phase1_plan_and_timing(
            semantic_field_ambiguity_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        semantic_field_ambiguity_result = _attach_semantic_preplan_debug(
            semantic_field_ambiguity_result,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_field_ambiguity_result,
            request_start=request_start,
            branch="semantic_field_ambiguity_clarification",
            semantic_preplan=semantic_preplan,
        )
    category_general_exclusions = _category_general_exclusion_signals(
        phase1_plan=phase1_plan,
        entity_contract=(phase2_entity_context or {}).get("contract"),
        entity_arbitration_signals=phase2_arbitration_signals,
    )
    category_general_eligible = _should_use_category_field_general(
        structured_contract=structured_query_contract,
        exclusions=category_general_exclusions,
    )
    if isinstance(phase1_plan, dict):
        phase1_plan["entity_arbitration_signals"] = dict(phase2_arbitration_signals)
        phase1_plan["category_general_exclusions"] = category_general_exclusions
        phase1_plan["category_general_eligible"] = category_general_eligible
    phase2_arbitration_signals["category_general"] = category_general_eligible
    semantic_field_is_validated = bool(
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("called")
        and not semantic_preplan.get("error")
        and not semantic_preplan.get("fallback_reason")
        and _semantic_formal_field_preempts_legacy_shortcuts(semantic_preplan)
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
    )
    sealed_compound_evidence_result = None
    if not phase1_plan.get("semantic_comparison_entity_contracts"):
        sealed_compound_evidence_result = _build_resolved_compound_evidence_result(
            db,
            question=question,
            entity_contract=(phase2_entity_context or {}).get("contract"),
            field_request=phase2_field_request,
            phase1_plan=phase1_plan,
        )
    if sealed_compound_evidence_result:
        supplemental_query = str((semantic_preplan or {}).get("supplemental_qa_evidence_query") or "").strip()
        # Semantic planning may correctly mark a mixed turn as compound while
        # omitting the optional retrieval paraphrase. In that case the whole
        # customer sentence is the only safe second-pass query: it preserves
        # the model's intent decision without letting deterministic code name
        # or infer the missing capability.
        if (
            not supplemental_query
            and bool((semantic_preplan or {}).get("compound"))
            and str((semantic_preplan or {}).get("evidence_kind") or "").strip() == "structured_field"
        ):
            supplemental_query = question
        if supplemental_query:
            supplemental, supplemental_query = await _resolve_sealed_supplemental_product_evidence(
                db,
                question=question,
                semantic_preplan=semantic_preplan,
                base_plan=phase1_plan,
            )
            if supplemental and supplemental.get("result_skus") == sealed_compound_evidence_result.get("result_skus"):
                structured_answer = str(sealed_compound_evidence_result.get("answer") or "").strip()
                supplemental_answer = str(supplemental.get("answer") or "").strip()
                if structured_answer and supplemental_answer:
                    sealed_compound_evidence_result["answer"] = structured_answer + "\n" + supplemental_answer
                    metadata = sealed_compound_evidence_result.setdefault("answer_metadata", {})
                    metadata["supplemental_product_qa"] = supplemental.get("answer_metadata") or {}
                    metadata["evidence_skus"] = sealed_compound_evidence_result.get("result_skus") or []
                    debug = sealed_compound_evidence_result.setdefault("debug", {})
                    debug["supplemental_product_qa_contract"] = (supplemental.get("debug") or {}).get("field_contract") or {}
        sealed_compound_evidence_result = _attach_phase1_plan_and_timing(
            sealed_compound_evidence_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        sealed_compound_evidence_result = _attach_semantic_preplan_debug(
            sealed_compound_evidence_result,
            semantic_preplan,
            final_route="product_detail",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=sealed_compound_evidence_result,
            request_start=request_start,
            branch="resolved_entity_compound_evidence_contract",
            semantic_preplan=semantic_preplan,
        )
    explicit_alcohol_exclusion_result = _phase1_except_alcohol_stove_cookware_result(db, question)
    if explicit_alcohol_exclusion_result:
        explicit_alcohol_exclusion_result = _attach_phase1_plan_and_timing(
            explicit_alcohol_exclusion_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=explicit_alcohol_exclusion_result,
            request_start=request_start,
            branch="structured_alcohol_stove_cookware_exclusion",
            semantic_preplan=semantic_preplan,
        )
    # A failed or low-confidence semantic attempt must not let a lossy legacy
    # alias reclassify an otherwise explicit cleaning/usage/care request.  In
    # that fallback state, defer to the existing same-SKU usage/care provider;
    # a validated formal semantic field still retains the normal Phase-2
    # FieldContract -> EntityResolutionContract route.
    phase2_contract = (phase2_entity_context or {}).get("contract")
    sealed_usage_care_contract = bool(
        len(phase2_field_request.get("supported_fields") or []) == 1
        and str(phase2_field_request.get("field_type") or "") in {"usage_instruction", "cleaning", "care"}
        and phase2_contract
        and str(getattr(phase2_contract, "status", "") or "") == "resolved"
        and str(getattr(phase2_contract, "resolved_sku", "") or "").strip()
    )
    fallback_usage_care = bool(
        _is_product_usage_care_question(question)
        and not resolved_formal_field_scope
        and not _is_unbound_product_usage_question(question)
        and (
            not _has_non_usage_care_field_contract(question)
            or _looks_like_compatibility_usage_question(question)
            or customer_agent_intent_service._looks_like_power_tradeoff_question(question)
            or customer_agent_intent_service._looks_like_general_fuel_definition_question(question)
        )
        and not semantic_field_is_validated
        and not _semantic_product_qa_preempts_legacy_shortcuts(semantic_preplan)
        and (not sealed_usage_care_contract or _looks_like_compatibility_usage_question(question))
    )
    # Ordinal follow-ups seal identity from the ordered conversation result
    # set below.  The phrase "第二个" is not an entity name for Phase 2 to
    # fuzzy-resolve against the whole catalogue.
    defer_ordinal_entity_arbitration = bool(
        conversation_id and _looks_like_ordinal_product_field_followup(question)
    )
    defer_generic_structured_arbitration = _should_prioritize_semantic_structured_route(question, phase1_plan)
    if (
        _detect_unknown_product_fact_label(question) == "发货时效"
        and not customer_agent_service._extract_skus(question)
        and not _products_named_in_question(db, question)
    ):
        generic_shipping_result = _unknown_product_fact_result(
            question,
            semantic_authorized=True,
        )
        if generic_shipping_result:
            generic_shipping_result = _attach_phase1_plan_and_timing(
                generic_shipping_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=generic_shipping_result,
                request_start=request_start,
                branch="generic_shipping_commitment_boundary",
                semantic_preplan=semantic_preplan,
            )
    phase2_entity_detail_result = None if (
        fallback_usage_care
        or _semantic_product_qa_preempts_legacy_shortcuts(semantic_preplan)
        or (_looks_like_compatibility_usage_question(question) and not resolved_formal_field_scope)
        or customer_agent_intent_service._looks_like_power_tradeoff_question(question)
        or defer_ordinal_entity_arbitration
        or defer_generic_structured_arbitration
        or (
            conversation_id
            and _is_contextual_candidate_comparison_question(question)
            and len(_latest_ordered_result_skus_for_followup(db, conversation_id, user_id)) >= 2
        )
    ) else _phase2_entity_state_arbitration_result(
        db,
        question,
        phase1_plan,
        conversation_id=conversation_id,
        signals=phase2_arbitration_signals,
        entity_resolution_context=phase2_entity_context,
    )
    if phase2_entity_detail_result:
        phase2_metadata = (
            phase2_entity_detail_result.get("answer_metadata")
            if isinstance(phase2_entity_detail_result.get("answer_metadata"), dict)
            else {}
        )
        # Some formally supported, non-realtime customer fields are imported
        # only as same-SKU QA or customer content rather than a dedicated
        # structured column. Reuse sealed RAG only after the formal contract,
        # exact entity contract, and ordinary provider all completed. A failed
        # selection deliberately leaves the field-specific missing result
        # untouched; live availability, price, promotion, and inventory never
        # enter this static-evidence adapter.
        phase2_missing_rag_fields = {"accessories", "warranty"}
        phase2_missing_field_type = str(phase2_field_request.get("field_type") or "")
        if (
            phase2_missing_field_type in phase2_missing_rag_fields
            and phase2_metadata.get("field_evidence_missing") is True
            and phase2_contract
            and str(getattr(phase2_contract, "status", "") or "") == "resolved"
            and str(getattr(phase2_contract, "resolved_sku", "") or "").strip()
        ):
            missing_field_preplan = dict(semantic_preplan or {})
            missing_field_preplan.update({
                "route_family": "product_bound_qa",
                "route_hint": "product_detail",
                "question_type": "field",
                "subject_text": str(phase2_contract.resolved_sku),
                "canonical_fields": [],
                "field_type": "",
                "field_hint": None,
                "evidence_required": True,
                "evidence_kind": "product_qa",
                "qa_evidence_query": question,
                "fallback_reason": "",
            })
            # Imported QA can be the only authoritative static record for a
            # sealed warranty or package-accessory field. The helper still
            # resolves the same product and binds the selected QA row; it
            # does not infer a duration, policy term, or package component.
            missing_field_evidence = None
            if phase2_missing_field_type in {"warranty", "accessories"}:
                sealed_product = db.query(Product).filter(
                    Product.sku == phase2_contract.resolved_sku
                ).first()
                sealed_qa = (
                    _best_product_qa_match(db, sealed_product, question)
                    if sealed_product is not None
                    else None
                )
                if sealed_qa is not None:
                    missing_field_evidence = _try_product_qa_shortcut(
                        db,
                        question,
                        phase1_plan={"semantic_preplan": missing_field_preplan},
                        selected_qa=sealed_qa,
                    )
            # Product QA has a purpose-built semantic Q/A selector. Prefer it
            # before free-form RAG so a concise stored answer (for example a
            # warranty statement without a duration) is not discarded merely
            # because it cannot be expanded into a broader prose answer.
            if not missing_field_evidence:
                missing_field_evidence = await _try_product_qa_shortcut_with_semantic_selection(
                    db,
                    question,
                    phase1_plan={"semantic_preplan": missing_field_preplan},
                )
            if _is_sealed_product_qa_safe_missing(missing_field_evidence):
                missing_field_evidence = None
            if not missing_field_evidence:
                for _missing_field_attempt in range(3):
                    missing_field_evidence = await _try_sealed_same_sku_knowledge_answer(
                        db,
                        question,
                        {"semantic_preplan": missing_field_preplan},
                    )
                    if missing_field_evidence:
                        break
            # Conservative last-mile fallback for package contents: when the
            # sealed same-SKU customer content itself contains an explicit
            # composition sentence, return that source sentence verbatim. This
            # never derives contents from title/specs and remains fail-closed
            # when no explicit composition evidence exists.
            if not missing_field_evidence and phase2_missing_field_type == "accessories":
                try:
                    content_rows = knowledge_service.same_sku_customer_context(
                        db, phase2_contract.resolved_sku, limit=3,
                    )
                except Exception:
                    content_rows = []
                explicit_composition = ""
                for row in content_rows:
                    source_text = str((row or {}).get("content") or "")
                    for sentence in re.split(r"(?<=[。！？!?])\s*", source_text):
                        sentence = sentence.strip()
                        if sentence and _is_complete_package_contents_evidence(sentence):
                            explicit_composition = sentence
                            break
                    if explicit_composition:
                        break
                if explicit_composition:
                    missing_field_evidence = dict(phase2_entity_detail_result)
                    missing_field_evidence["answer"] = explicit_composition
                    missing_field_evidence["skip_polish"] = True
                    missing_field_evidence["evidence"] = [{
                        "sku": phase2_contract.resolved_sku,
                        "field_label": "套装组成",
                        "value": explicit_composition,
                        "source_type": "knowledge_base",
                        "source_layer": "RAG",
                        "matched_by": "explicit_same_sku_composition_sentence",
                    }]
                    missing_field_evidence["sources"] = [{
                        "type": "knowledge_base",
                        "label": "同 SKU 文件知识库",
                        "sku": phase2_contract.resolved_sku,
                    }]
                    fallback_debug = missing_field_evidence.get("debug") if isinstance(missing_field_evidence.get("debug"), dict) else {}
                    fallback_debug["agent_mode"] = "resolved_entity_accessories_same_sku_explicit_evidence"
                    missing_field_evidence["debug"] = fallback_debug
                    fallback_metadata = missing_field_evidence.get("answer_metadata") if isinstance(missing_field_evidence.get("answer_metadata"), dict) else {}
                    fallback_metadata.update({
                        "source": "resolved_entity_accessories_same_sku_explicit_evidence",
                        "contract_field_type": "accessories",
                        "evidence_field": "accessories",
                        "field_evidence_match": True,
                        "field_evidence_missing": False,
                        "evidence_status": "matched",
                        "evidence_sku": phase2_contract.resolved_sku,
                        "evidence_skus": [phase2_contract.resolved_sku],
                    })
                    missing_field_evidence["answer_metadata"] = fallback_metadata
            if missing_field_evidence and missing_field_evidence.get("result_skus") == [phase2_contract.resolved_sku]:
                rag_debug = missing_field_evidence.get("debug") if isinstance(missing_field_evidence.get("debug"), dict) else {}
                rag_debug["agent_mode"] = f"resolved_entity_{phase2_missing_field_type}_same_sku_rag"
                rag_debug["field_contract"] = dict(phase2_field_request)
                rag_debug["entity_resolution_contract"] = phase2_contract.to_dict()
                missing_field_evidence["debug"] = rag_debug
                rag_metadata = missing_field_evidence.get("answer_metadata") if isinstance(missing_field_evidence.get("answer_metadata"), dict) else {}
                rag_metadata.update({
                    "source": f"resolved_entity_{phase2_missing_field_type}_same_sku_rag",
                    "contract_field_type": phase2_missing_field_type,
                    "evidence_field": phase2_missing_field_type,
                    "field_evidence_match": True,
                    "field_evidence_missing": False,
                    "entity_resolution_contract": phase2_contract.to_dict(),
                })
                missing_field_evidence["answer_metadata"] = rag_metadata
                # This is now a formal field response, not free-form QA.
                # Its sealed evidence answer has already passed grounding and
                # coverage; a later style pass must not remove its concrete
                # package-composition facts.
                missing_field_evidence["skip_polish"] = True
                phase2_entity_detail_result = missing_field_evidence
        if (
            phase2_field_type == "accessories"
            and _looks_like_compound_contents_usage_question(question)
            and phase2_contract
            and str(getattr(phase2_contract, "resolved_sku") or "").strip()
        ):
            sealed_product = db.query(Product).filter(
                Product.sku == phase2_contract.resolved_sku
            ).first()
            product_name = str(
                getattr(sealed_product, "product_name_cn", "")
                or getattr(sealed_product, "product_name_en", "")
                or phase2_contract.resolved_sku
            ).strip()
            prefix = f"{product_name}（{phase2_contract.resolved_sku}）"
            existing_answer = str(phase2_entity_detail_result.get("answer") or "").strip()
            package_value = existing_answer or "当前同 SKU 资料未直接确认具体清单。"
            phase2_entity_detail_result["answer"] = "\n".join([
                prefix,
                f"包装内容包括：{package_value}",
                "使用准备：当前资料未确认拆箱后即可直接使用；首次使用前请先核对包装清单、检查部件，并按说明书完成清洁或安装。",
            ])
            compound_metadata = phase2_entity_detail_result.get("answer_metadata")
            if not isinstance(compound_metadata, dict):
                compound_metadata = {}
            compound_metadata.update({
                "source": "resolved_entity_compound_contents_usage_boundary",
                "compound": True,
                "usage_preparation_boundary": True,
            })
            phase2_entity_detail_result["answer_metadata"] = compound_metadata
            phase2_entity_detail_result["skip_polish"] = True

        phase2_entity_detail_result = _attach_phase1_plan_and_timing(
            phase2_entity_detail_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        phase2_entity_detail_result = _attach_semantic_preplan_debug(
            phase2_entity_detail_result,
            semantic_preplan,
            final_route="product_detail",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=phase2_entity_detail_result,
            request_start=request_start,
            branch="resolved_entity_detail_contract",
            semantic_preplan=semantic_preplan,
        )

    compound_detail_result = None
    if (
        phase2_arbitration_signals.get("compound")
        and phase2_arbitration_signals.get("multi_field")
        and not phase2_arbitration_signals.get("recommendation")
        and not phase2_arbitration_signals.get("comparison")
        and not phase2_arbitration_signals.get("structured_query")
        and not phase2_arbitration_signals.get("multi_product")
        and not phase2_arbitration_signals.get("dedicated_route")
    ):
        try:
            compound_contract = (phase2_entity_context or {}).get("contract")
            if (
                compound_contract
                and compound_contract.status == "resolved"
                and compound_contract.resolved_sku
                and compound_contract.is_unique
                and compound_contract.confidence == "high"
                and compound_contract.matched_by in _PHASE2_EXACT_ENTITY_MATCHES
                and compound_contract.resolver_candidate_skus == [compound_contract.resolved_sku]
            ):
                compound_detail_result = _build_compound_product_detail_result(
                    db,
                    resolved_sku=compound_contract.resolved_sku,
                    requested_fields=phase2_field_request["requested_fields"],
                    question=question,
                    entity_contract=compound_contract.to_dict(),
                    compatibility_plan=phase1_plan,
                )
        except Exception:
            compound_detail_result = None
    if compound_detail_result:
        compound_detail_result = _attach_phase1_plan_and_timing(
            compound_detail_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        compound_detail_result = _attach_semantic_preplan_debug(
            compound_detail_result,
            semantic_preplan,
            final_route="product_detail",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=compound_detail_result,
            request_start=request_start,
            branch="resolved_entity_compound_detail",
            semantic_preplan=semantic_preplan,
        )

    structured_candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    scoped_heat_source_structured_followup = _should_keep_preruntime_for_scoped_heat_source_followup(
        question,
        candidate_context=structured_candidate_context,
    )
    contextual_field_request = resolve_requested_field_contract(question, phase1_plan)
    contextual_field_type = str(contextual_field_request.get("field_type") or "").strip()
    contextual_anchor_sku = (
        _latest_persisted_agent_context_sku(db, conversation_id)
        if (
            conversation_id
            and any(term in str(question or "") for term in ("这款", "这个", "它", "该商品", "该产品"))
            and not _has_explicit_sku_text(question)
            and contextual_field_type in customer_field_contract.FORMAL_DETAIL_FIELDS
        )
        else ""
    )
    if contextual_anchor_sku:
        contextual_field_result = _product_field_followup_result(
            db,
            contextual_anchor_sku,
            customer_field_contract.product_detail_field_label(contextual_field_type),
            question,
            identity_source="latest_single_product_result",
            field_request_override=contextual_field_request,
        )
        if contextual_field_result:
            contextual_field_result = _attach_phase1_plan_and_timing(
                contextual_field_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=contextual_field_result,
                request_start=request_start,
                branch="persisted_single_product_field_before_structured_query",
                semantic_preplan=semantic_preplan,
            )
    semantic_catalog_subject = str((semantic_preplan or {}).get("subject_text") or "").strip()
    semantic_catalog_unresolved = bool(
        _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and _semantic_preplan_route_family(semantic_preplan) == "structured_query"
        and list((semantic_preplan or {}).get("canonical_fields") or []) == ["category"]
        and semantic_catalog_subject
        and not customer_agent_service.search_products(db, semantic_catalog_subject, limit=1)
    )
    if semantic_catalog_unresolved:
        clarification = {
            "intent": "clarify",
            "answer_type": "clarification",
            "answer": f"没有找到“{semantic_catalog_subject}”对应的商品范围。请确认商品名、SKU 或希望查询的实际品类后，我再按数据库资料查询。",
            "results": [], "result_skus": [], "candidate_skus": [], "evidence": [],
            "answer_metadata": {"source": "semantic_catalog_subject_unresolved", "evidence_status": "subject_not_found"},
            "debug": {"agent_mode": "semantic_catalog_subject_unresolved"},
            "skip_polish": True,
        }
        clarification = _attach_phase1_plan_and_timing(
            clarification,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=clarification,
            request_start=request_start,
            branch="semantic_catalog_subject_unresolved",
            semantic_preplan=semantic_preplan,
        )
    if (
        structured_query_contract.status == "resolved" or category_general_eligible
    ) and (
        semantic_structured_query_contract_active
        or not (
            _looks_like_structured_field_filter_query(question)
            or _looks_like_semantic_waterware_capability_query(question)
        )
    ) and not scoped_heat_source_structured_followup and not (
        conversation_id and _looks_like_ordinal_product_field_followup(question)
    ) and not (
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("called")
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and str(semantic_preplan.get("field_type") or "") in customer_field_contract.FORMAL_DETAIL_FIELDS
        and not semantic_structured_query_contract_active
        and _semantic_preplan_route_family(semantic_preplan) != "structured_query"
    ):
        structured_contract_result = (
            _build_structured_query_contract_result(db, structured_query_contract)
            if structured_query_contract.status == "resolved"
            else _build_category_field_general_result(db, structured_query_contract)
        )
        structured_contract_result = _attach_phase1_plan_and_timing(
            structured_contract_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=structured_contract_result,
            request_start=request_start,
            branch="structured_query_contract",
            semantic_preplan=semantic_preplan,
        )

    intent_execution_options = {
        "allow_search_top1_promotion": bool(phase1_plan.get("allow_search_top1_promotion", True)),
    }

    # A plural heat-source filter can be an explicit continuation of the
    # immediately preceding candidate set even without a pronoun.  Preserve
    # that sealed domain before any global structured-query route is allowed
    # to infer a category from the field value itself (for example 酒精炉 -> 炉具).
    pre_arbiter_candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    scoped_heat_source_candidate_followup = _should_keep_preruntime_for_scoped_heat_source_followup(
        question,
        candidate_context=pre_arbiter_candidate_context,
    )
    # Do not let a loose token overlap in a product name suppress a genuine
    # category filter. This gate is intentionally based on the same sealed
    # entity contract used by product-detail execution.
    # Phase 2 already built the authoritative entity contract for this
    # request. Reuse that snapshot for the structured-query guard rather than
    # parsing the same utterance a second time: a single request must have one
    # EntityResolutionContract decision.
    has_resolved_product_before_structured_arbiter = bool(
        phase2_contract
        and str(getattr(phase2_contract, "status", "") or "") == "resolved"
        and str(getattr(phase2_contract, "resolved_sku", "") or "").strip()
    )
    # An entity-less category request constrained by a formally supported
    # field is a catalogue operation, even if the semantic field is otherwise
    # eligible for product-detail arbitration.  Do not let that formal-field
    # safeguard route this request into a legacy usage/QA executor.
    generic_structured_filter_pending = bool(
        not _products_named_in_question(db, question)
        and _looks_like_structured_field_filter_query(question)
    )
    semantic_schema_failed_hard_filter_repair = _semantic_schema_failed_hard_filter_repair(
        semantic_preplan,
        question=question,
    )

    if (
        not has_resolved_product_before_structured_arbiter
        and (
            generic_structured_filter_pending
            or not (
                isinstance(semantic_preplan, dict)
                and semantic_preplan.get("called")
                and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
                and _semantic_formal_field_preempts_legacy_shortcuts(semantic_preplan)
            )
        )
        and (
            (
                _semantic_preplan_route_family(semantic_preplan) == "structured_query"
                and _semantic_preplan_confident(semantic_preplan)
            )
            or (
                _looks_like_structured_field_filter_query(question)
                and not _looks_like_recommendation_request(question)
            )
            or semantic_schema_failed_hard_filter_repair
        )
        and not scoped_heat_source_candidate_followup
    ):
        arbiter_structured_result = _structured_field_filter_result(db, question)
        if arbiter_structured_result:
            structured_phase1_plan = dict(phase1_plan) if isinstance(phase1_plan, dict) else {}
            structured_metadata = arbiter_structured_result.get("answer_metadata") if isinstance(arbiter_structured_result.get("answer_metadata"), dict) else {}
            if str(structured_metadata.get("product_ref") or "").strip():
                structured_phase1_plan["product_ref"] = str(structured_metadata.get("product_ref") or "").strip()
            arbiter_structured_result = _attach_phase1_plan_and_timing(
                arbiter_structured_result,
                structured_phase1_plan,
                _phase1_timing(
                    request_start=request_start,
                    planner_duration_ms=planner_duration_ms,
                ),
            )
            arbiter_structured_result = _attach_semantic_preplan_debug(
                arbiter_structured_result,
                semantic_preplan,
                final_route=str(arbiter_structured_result.get("answer_type") or "query_products"),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=arbiter_structured_result,
                request_start=request_start,
                branch="semantic_preplan_structured_query",
                semantic_preplan=semantic_preplan,
            )

    # “除了 SKU” is a catalogue exclusion constraint, not an entity detail
    # request for the SKU being excluded. Resolve it before exact-identity
    # arbitration can bind the exclusion token as the answer subject.
    explicit_alcohol_exclusion_result = _phase1_except_alcohol_stove_cookware_result(db, question)
    if explicit_alcohol_exclusion_result:
        explicit_alcohol_exclusion_result = _attach_phase1_plan_and_timing(
            explicit_alcohol_exclusion_result,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=explicit_alcohol_exclusion_result,
            request_start=request_start,
            branch="structured_alcohol_stove_cookware_exclusion",
            semantic_preplan=semantic_preplan,
        )

    # Resolve an exact same-SKU QA before an entity-scope clarification can
    # reinterpret a canonical variant name plus its predicate as ambiguous.
    # The QA helper returns None for real multi-product candidates.
    # Phase 1 already owns the semantic decision at this point.  Passing that
    # decision through is essential: an unplanned fuzzy QA lookup must not
    # choose an arbitrary same-SKU answer before the sealed QA intent has a
    # chance to constrain its evidence retrieval.
    exact_qa_before_scope_guard = None
    if not _formal_field_contract_preempts_product_qa(question, phase1_plan):
        exact_qa_before_scope_guard = await _try_product_qa_shortcut_with_semantic_selection(
            db,
            question,
            phase1_plan=phase1_plan,
        )
    if (
        exact_qa_before_scope_guard
        and not _is_sealed_product_qa_safe_missing(exact_qa_before_scope_guard)
        and not ((semantic_preplan or {}).get("qa_evidence_queries") or [])
        and not _formal_field_contract_preempts_product_qa(question, phase1_plan)
        and (
            (exact_qa_before_scope_guard.get("answer_metadata") or {}).get("qa_match_type") == "exact"
            or (
                ((exact_qa_before_scope_guard.get("debug") or {}).get("field_contract") or {}).get("field_type")
                == str((semantic_preplan or {}).get("field_type") or "").strip()
                and str((exact_qa_before_scope_guard.get("answer_metadata") or {}).get("evidence_sku") or "").strip()
            )
            or (
                ((exact_qa_before_scope_guard.get("debug") or {}).get("field_contract") or {}).get("field_type") == "product_qa"
                and not _formal_field_contract_preempts_product_qa(question, phase1_plan)
            )
        )
    ):
        exact_qa_before_scope_guard = _attach_phase1_plan_and_timing(
            exact_qa_before_scope_guard,
            phase1_plan,
            _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=exact_qa_before_scope_guard,
            request_start=request_start,
            branch="sealed_exact_product_qa_before_entity_scope_guard",
            semantic_preplan=semantic_preplan,
        )

    current_turn_field_contract = customer_field_contract.detect_field_contract(question)
    current_turn_field_type = str(
        getattr(current_turn_field_contract, "field_type", "") or ""
    ).strip()

    usage_care_preempt_entity_scope = bool(
        _is_product_usage_care_question(question)
        and not _looks_like_recommendation_request(question)
        and not request_sku_anchor
        and not _looks_like_compatibility_usage_question(question)
        and not _products_named_in_question(db, question)
        and customer_agent_intent_service._detect_usage_care_subtype(question) in {
            "safety", "duration", "power_tradeoff", "installation", "full_unit_wash", "composition",
        }
    )

    # A plain catalogue listing (for example “有哪些炉具”) is a set query,
    # not a personalized recommendation.  Semantic planning can reasonably
    # describe the same noun as a recommendation, but the customer did not
    # ask us to choose one.  Resolve the category against the live catalogue
    # and preserve all listed products for a later comparison follow-up.
    plain_catalog_listing = (
        _looks_like_semantic_catalog_query(question)
        and phase1_plan.get("primary_intent") != "catalog_count"
        and not customer_agent_service._extract_skus(question)
        and not _products_named_in_question(db, question)
        and not any(term in question for term in ("推荐", "适合", "怎么选", "买", "选哪", "更"))
    )
    if plain_catalog_listing:
        catalog_rows = _phase1_catalog_rows(db, "产品")
        semantic_catalog_subject = str((semantic_preplan or {}).get("subject_text") or "").strip()
        if not semantic_catalog_subject:
            semantic_catalog_subject = next(
                (term for term in ("锅具", "套锅", "单锅", "炉具", "炉子", "水具", "水壶", "配件", "附件", "餐具", "烤盘", "煎盘") if term in question),
                "",
            )
        catalog_ref = _database_category_scope_ref(catalog_rows, semantic_catalog_subject)
        if catalog_ref:
            catalog_listing_result = _phase1_catalog_count_result(
                db,
                {"product_ref": catalog_ref, "raw_question": question},
            )
            catalog_listing_result = _attach_phase1_plan_and_timing(
                catalog_listing_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            catalog_listing_result = _attach_semantic_preplan_debug(
                catalog_listing_result,
                semantic_preplan,
                final_route="query_products",
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=catalog_listing_result,
                request_start=request_start,
                branch="plain_catalog_listing_override",
                semantic_preplan=semantic_preplan,
            )

    # Warranty is a formal static field whose sealed QA/RAG fallback is owned
    # by Phase 2. Do not let an early unknown-field scope guard decide it is
    # absent before the formal entity/evidence chain can run.
    entity_scope_contract_result = None if (
        (conversation_id and _looks_like_ordinal_product_field_followup(question))
        or request_sku_anchor
        or current_turn_field_type == "warranty"
        or page_usage_override
        or usage_care_preempt_entity_scope
    ) else _entity_scope_pre_route_guard_result(
        db,
        question,
        phase1_plan,
        semantic_preplan,
    )
    if entity_scope_contract_result:
        entity_scope_contract_result = _attach_phase1_plan_and_timing(
            entity_scope_contract_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        entity_scope_contract_result = _attach_semantic_preplan_debug(
            entity_scope_contract_result,
            semantic_preplan,
            final_route=str(entity_scope_contract_result.get("answer_type") or "clarification"),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=entity_scope_contract_result,
            request_start=request_start,
            branch="entity_scope_contract_guard",
            semantic_preplan=semantic_preplan,
        )

    # A compound explicit-SKU request must retain every formal field and the
    # sealed entity/evidence contracts.  The legacy single-detail shortcut
    # only renders its first field, so leave compound requests for the
    # contract-aware multi-field branch below.
    explicit_sku_direct_result = (
        None
        if (
            len(_explicit_sku_detail_requested_fields(question)) > 1
            or _semantic_prefers_sealed_product_qa(phase1_plan)
            or current_turn_field_type == "warranty"
            or _looks_like_compatibility_usage_question(question)
            or customer_agent_intent_service._looks_like_power_tradeoff_question(question)
            or usage_care_preempt_entity_scope
        )
        else await _try_explicit_sku_detail_shortcut(db, question)
    )
    if explicit_sku_direct_result:
        explicit_sku_direct_result = _attach_semantic_preplan_debug(
            explicit_sku_direct_result,
            semantic_preplan,
            final_route="product_detail",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=explicit_sku_direct_result,
            request_start=request_start,
            branch="phase1_explicit_sku_detail",
            semantic_preplan=semantic_preplan,
        )

    semantic_formal_field = str((semantic_preplan or {}).get("field_type") or "").strip()
    resolved_unknown_field_result = None
    if not (
        _semantic_allows_exact_sealed_product_qa(phase1_plan)
        and semantic_formal_field in customer_field_contract.FORMAL_DETAIL_FIELDS
    ) and current_turn_field_type != "warranty":
        resolved_unknown_field_result = _try_resolved_product_unknown_field_shortcut(db, question)
    if resolved_unknown_field_result and not _is_product_usage_care_question(question):
        resolved_unknown_field_result = _attach_semantic_preplan_debug(
            resolved_unknown_field_result,
            semantic_preplan,
            final_route=resolved_unknown_field_result.get("answer_type") or "product_detail",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=resolved_unknown_field_result,
            request_start=request_start,
            branch="resolved_entity_unknown_field_fallback",
            semantic_preplan=semantic_preplan,
        )

    unresolved_product_like_contents_result = None if (
        page_usage_override or usage_care_preempt_entity_scope
    ) else _unresolved_product_like_contents_clarification_result(db, question)
    if unresolved_product_like_contents_result:
        unresolved_product_like_contents_result = _attach_phase1_plan_and_timing(
            unresolved_product_like_contents_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        unresolved_product_like_contents_result = _attach_semantic_preplan_debug(
            unresolved_product_like_contents_result,
            semantic_preplan,
            final_route=str(unresolved_product_like_contents_result.get("answer_type") or "clarification"),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=unresolved_product_like_contents_result,
            request_start=request_start,
            branch="unresolved_product_like_contents_clarification",
            semantic_preplan=semantic_preplan,
        )

    semantic_compound_product_qa = bool(
        isinstance(semantic_preplan, dict)
        and bool(semantic_preplan.get("compound"))
        and len(semantic_preplan.get("qa_evidence_queries") or []) > 1
        and _semantic_prefers_sealed_product_qa(phase1_plan)
        and len(_products_named_in_question(db, question)) == 1
    )
    semantic_safety_procedure = bool(
        _semantic_preplan_route_family(semantic_preplan) == "general_chat"
        and str((semantic_preplan or {}).get("subtype") or "") == "safety_procedure"
    )
    if (
        (
            _looks_like_compatibility_usage_question(question)
            or customer_agent_intent_service._looks_like_power_tradeoff_question(question)
            or not conversation_id
        )
        and _is_product_usage_care_question(question)
        # A resolved formal field/entity contract owns product-bound facts,
        # including compatibility wording.  Keep the generic usage composer
        # for unbound safety questions, but do not let it discard same-SKU or
        # category-scoped evidence that Phase 2 can answer directly.
        and not resolved_formal_field_scope
        and not _is_unbound_product_usage_question(question)
        and not (len(_products_named_in_question(db, question)) > 1)
        and not semantic_compound_product_qa
        and not semantic_safety_procedure
        and not customer_agent_intent_service._looks_like_utensil_material_safety_question(question)
        and (
            _looks_like_compatibility_usage_question(question)
            or customer_agent_intent_service._looks_like_power_tradeoff_question(question)
            or (
                not _has_non_usage_care_field_contract(question)
                and not _semantic_formal_field_preempts_legacy_shortcuts(semantic_preplan)
                and not _semantic_product_qa_preempts_legacy_shortcuts(semantic_preplan)
                and not _semantic_prefers_sealed_product_qa(phase1_plan)
                and not _should_prioritize_semantic_structured_route(question, phase1_plan)
            )
        )
    ):
        early_usage_care_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=_products_named_in_question(db, question),
        )
        if len((early_usage_care_result or {}).get("result_skus") or []) > 1:
            early_usage_care_result = None
        if early_usage_care_result:
            early_usage_care_result = _attach_phase1_plan_and_timing(
                early_usage_care_result,
                phase1_plan,
                _phase1_timing(
                    request_start=request_start,
                    planner_duration_ms=planner_duration_ms,
                ),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=early_usage_care_result,
                request_start=request_start,
                branch="product_usage_care_fast_path",
                semantic_preplan=semantic_preplan,
            )

    if (
        _should_prioritize_semantic_structured_route(question, phase1_plan)
        and not _semantic_preplan_unknown_realtime_subtype(semantic_preplan, question)
    ):
        semantic_structured_result = _semantic_structured_query_result(
            db,
            question,
            candidate_context=_latest_candidate_context_for_sources(db, conversation_id),
            semantic_preplan=semantic_preplan,
        )
        if semantic_structured_result:
            semantic_structured_result = _post_filter_recommendation_result(db, question, semantic_structured_result)
            structured_phase1_plan = _semantic_structured_override_plan(question, phase1_plan, semantic_structured_result)
            semantic_structured_result = _attach_phase1_plan_and_timing(
                semantic_structured_result,
                structured_phase1_plan,
                _phase1_timing(
                    request_start=request_start,
                    planner_duration_ms=planner_duration_ms,
                ),
            )
            semantic_structured_result = _attach_semantic_preplan_debug(
                semantic_structured_result,
                semantic_preplan,
                final_route=str(semantic_structured_result.get("answer_type") or "query_products"),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=semantic_structured_result,
                request_start=request_start,
                branch="phase1_semantic_structured_priority_guard",
                semantic_preplan=semantic_preplan,
            )

    phase1_direct_result = None
    executor_start = perf_counter()
    # Exact same-SKU QA for a fact outside the formal field taxonomy must not
    # be reinterpreted as an unrelated formal field by a later planner.  Do
    # not preempt formal fields here: those continue into FieldContract.
    # This is after semantic preplanning, so never re-enter the legacy QA
    # matcher without the plan.  In particular, a high-confidence
    # ``product_qa`` intent must use its sealed same-SKU retrieval query; an
    # unrelated stored QA is not an acceptable answer merely because it has
    # the right product identity.
    generic_qa_contract = None
    if not _formal_field_contract_preempts_product_qa(question, phase1_plan):
        generic_qa_contract = await _try_product_qa_shortcut_with_semantic_selection(
            db,
            question,
            phase1_plan=phase1_plan,
        )
    if (
        generic_qa_contract
        and not _is_sealed_product_qa_safe_missing(generic_qa_contract)
        and not ((semantic_preplan or {}).get("qa_evidence_queries") or [])
        and ((generic_qa_contract.get("debug") or {}).get("field_contract") or {}).get("field_type") == "product_qa"
        and not _formal_field_contract_preempts_product_qa(question, phase1_plan)
    ):
        phase1_direct_result = generic_qa_contract
    if (
        conversation_id
        and _looks_like_ordinal_product_field_followup(question)
        and not _is_ordinal_compare_followup_question(question)
        and "哪个更适合" not in question
    ):
        ordinal_recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
        ordinal_candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
        ordinal_followup_sku = _ordinal_followup_target_sku(
            question,
            ordinal_recommendation_context,
            ordinal_candidate_context,
        )
        latest_ordered_result_skus = _latest_ordered_result_skus_for_followup(
            db,
            conversation_id,
            user_id,
        )
        if not ordinal_followup_sku and len(latest_ordered_result_skus) >= 2:
            ordinal_followup_sku = _ordinal_followup_target_sku_from_ordered(
                question,
                latest_ordered_result_skus,
            )
        if not ordinal_followup_sku:
            ordinal_followup_sku = _ordinal_followup_target_sku_from_ordered(
                question,
                _conversation_explicit_skus_in_mention_order(db, conversation_id, user_id),
            )
        ordinal_field_request = _merge_explicit_detail_fields_into_request(
            question,
            resolve_requested_field_contract(question, phase1_plan),
        )
        ordinal_canonical_fields = list(ordinal_field_request.get("canonical_fields") or [])
        ordinal_requested_fields = list(ordinal_field_request.get("requested_fields") or [])
        ordinal_requested_field = (
            ordinal_requested_fields[0]
            if ordinal_requested_fields
            else _followup_requested_field(question)
        )
        if ordinal_followup_sku and len(ordinal_canonical_fields) > 1:
            phase1_direct_result = _build_compound_product_detail_result(
                db,
                resolved_sku=ordinal_followup_sku,
                requested_fields=ordinal_requested_fields,
                question=question,
                compatibility_plan=phase1_plan,
            )
        elif ordinal_followup_sku and ordinal_requested_field:
            phase1_direct_result = _product_field_followup_result(
                db,
                ordinal_followup_sku,
                ordinal_requested_field,
                question,
                identity_source="recommendation_context_ordinal",
                field_request_override=ordinal_field_request,
            )
        elif ordinal_followup_sku:
            phase1_direct_result = await _ordinal_freeform_followup_result(
                db,
                user_id=user_id,
                question=question,
                resolved_sku=ordinal_followup_sku,
            )
        if phase1_direct_result:
            phase1_direct_result = _attach_phase1_plan_and_timing(
                phase1_direct_result,
                phase1_plan,
                _phase1_timing(request_start=request_start, planner_duration_ms=planner_duration_ms),
            )
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=phase1_direct_result,
                request_start=request_start,
                branch="recommendation_context_ordinal_field",
                semantic_preplan=semantic_preplan,
            )
    plural_heat_source_followup = bool(
        phase1_plan.get("primary_intent") == "product_field"
        and _is_plural_heat_source_followup(question)
    )
    alcohol_stove_cookware_list_query = bool(
        customer_agent_intent_service._looks_like_alcohol_stove_cookware_recommendation_question(question)
        and not customer_agent_service._extract_skus(question)
    )
    if (
        not phase1_direct_result
        and phase1_plan.get("primary_intent") == "product_field"
        and not plural_heat_source_followup
        and not alcohol_stove_cookware_list_query
        and not _candidate_context_formal_field_filter_eligible(
            question,
            field_request=phase2_field_request,
            candidate_context=active_candidate_context,
        )
        # A high-confidence semantic product-QA contract owns the requested
        # capability.  The legacy phase-one planner may still see an adjacent
        # field token in the product name or question (for example a fuel
        # word) but must not turn that into a structured-field answer before
        # the sealed same-SKU QA path can answer or fail closed.
        and not _semantic_prefers_sealed_product_qa(phase1_plan)
        # A validated semantic FieldContract is authoritative over the
        # planner's compatibility label.  Do not let the phase-1 executor
        # return before central Phase 2 can seal entity identity and select
        # same-SKU structured evidence.
        and not (
            isinstance(semantic_preplan, dict)
            and semantic_preplan.get("called")
            and str(semantic_preplan.get("field_type") or "") in customer_field_contract.FORMAL_DETAIL_FIELDS
            and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
            and not _semantic_catalog_aggregate_contract(semantic_preplan, phase1_plan)
        )
    ):
        phase1_direct_result = _phase1_product_field_result(db, phase1_plan)
    elif phase1_plan.get("primary_intent") == "category_compatibility":
        phase1_direct_result = _phase1_category_compatibility_result(db, phase1_plan)
    elif alcohol_stove_cookware_list_query:
        alcohol_recommendation_plan = dict(phase1_plan)
        alcohol_recommendation_plan.update({
            "primary_intent": "recommendation",
            "answer_type": "recommendation",
            "scenario": question,
            "raw_question": question,
        })
        phase1_direct_result = _phase1_structured_recommendation_result(db, alcohol_recommendation_plan)
    elif (
        phase1_plan.get("primary_intent") == "catalog_count"
        and _looks_like_semantic_catalog_query(question)
        # A product-navigation contract may look count-like ("有哪些款"),
        # but its subject is a product family, not permission to count every
        # row when that family cannot be mapped to a database category.
        and not _semantic_product_navigation_preempts_catalog_count(semantic_preplan)
        # A constrained, allowlisted product-field preplan must reach the
        # central FieldContract/EntityResolution path before a legacy count
        # heuristic can reinterpret a named-product fact as a category query.
        and not (
            isinstance(semantic_preplan, dict)
            and semantic_preplan.get("called")
            and str(semantic_preplan.get("field_type") or "") in customer_field_contract.FORMAL_DETAIL_FIELDS
            and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
            and not _semantic_catalog_aggregate_contract(semantic_preplan, phase1_plan)
        )
        and not _has_resolved_entity_contents_accessories_question(db, question)
        # Operational owner/lifecycle predicates must remain part of the
        # deterministic structured query instead of falling back to a broad
        # category count.
        and not _is_internal_catalogue_filter_query(question)
        # Count vocabulary embedded inside a named product question (for
        # example a shipping-price "多少") is not a category count.  Preserve
        # the product scope for the central field/entity contracts.
        and not _products_named_in_question(db, question)
        and not _has_unresolved_product_like_scope(db, question)
    ):
        phase1_direct_result = _phase1_catalog_count_result(db, phase1_plan)
    elif phase1_plan.get("primary_intent") in {"product_compare_recommendation", "comparison"}:
        phase1_direct_result = await _phase1_compare_choice_result(db, phase1_plan)
    elif phase1_plan.get("primary_intent") == "recommendation" and _phase1_should_use_structured_recommendation(phase1_plan):
        phase1_direct_result = _phase1_structured_recommendation_result(db, phase1_plan)
    phase1_executor_duration_ms = round((perf_counter() - executor_start) * 1000, 2) if phase1_direct_result else 0
    if phase1_direct_result:
        guard_start = perf_counter()
        phase1_direct_result = _run_phase1_answer_guard(phase1_direct_result, phase1_plan)
        phase1_direct_result = _post_filter_recommendation_result(db, question, phase1_direct_result)
        guard_duration_ms = round((perf_counter() - guard_start) * 1000, 2)
        phase1_direct_result = _attach_phase1_plan_and_timing(
            phase1_direct_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
                executor_duration_ms=phase1_executor_duration_ms,
                guard_duration_ms=guard_duration_ms,
            ),
        )
        phase1_direct_result = _attach_semantic_preplan_debug(
            phase1_direct_result,
            semantic_preplan,
            final_route=str(phase1_direct_result.get("answer_type") or phase1_plan.get("answer_type") or ""),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=phase1_direct_result,
            request_start=request_start,
            branch=f"phase1_{phase1_plan.get('primary_intent') or 'planner'}",
            semantic_preplan=semantic_preplan,
        )

    semantic_comparison_clarification = None if phase1_plan.get("semantic_comparison_entity_contracts") else _semantic_comparison_fail_closed_result(semantic_preplan)
    if semantic_comparison_clarification:
        semantic_comparison_clarification = _attach_phase1_plan_and_timing(
            semantic_comparison_clarification,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
                executor_duration_ms=phase1_executor_duration_ms,
            ),
        )
        semantic_comparison_clarification = _attach_semantic_preplan_debug(
            semantic_comparison_clarification,
            semantic_preplan,
            final_route="clarification",
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_comparison_clarification,
            request_start=request_start,
            branch="semantic_comparison_unsealed_clarification",
            semantic_preplan=semantic_preplan,
        )

    semantic_structured_result = None
    if (
        not _semantic_preplan_unknown_realtime_subtype(semantic_preplan, question)
        and not _looks_like_recommendation_request(question)
        and not (
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("called")
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and str(semantic_preplan.get("field_type") or "") in customer_field_contract.FORMAL_DETAIL_FIELDS
        )
    ):
        semantic_structured_result = _semantic_structured_query_result(
            db,
            question,
            candidate_context=_latest_candidate_context_for_sources(db, conversation_id),
            semantic_preplan=semantic_preplan,
        )
    if semantic_structured_result:
        semantic_structured_result = _post_filter_recommendation_result(db, question, semantic_structured_result)
        structured_phase1_plan = _semantic_structured_override_plan(question, phase1_plan, semantic_structured_result)
        semantic_structured_result = _attach_phase1_plan_and_timing(
            semantic_structured_result,
            structured_phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        semantic_structured_result = _attach_semantic_preplan_debug(
            semantic_structured_result,
            semantic_preplan,
            final_route=str(semantic_structured_result.get("answer_type") or "query_products"),
        )
        return await _save_agent_result_and_return(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=semantic_structured_result,
            request_start=request_start,
            branch="phase1_semantic_structured_guard",
            semantic_preplan=semantic_preplan,
        )

    composite_question = _split_composite_customer_question(question)
    if composite_question:
        fact_part = composite_question["fact_part"]
        recommendation_part = _normalize_composite_recommendation_part(composite_question["recommendation_part"])
        fact_result = await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=fact_part,
            sku=sku,
            previous_result_skus=[],
            allow_llm_fallback=False,
            **intent_execution_options,
        )
        fact_previous_result_skus = []
        if fact_result:
            fact_previous_result_skus = list(dict.fromkeys([
                str(item.get("sku") or "").strip().upper()
                for item in (fact_result.get("results") or [])
                if isinstance(item, dict) and str(item.get("sku") or "").strip()
            ] or [
                str(sku or "").strip().upper()
                for sku in (fact_result.get("result_skus") or [])
                if str(sku or "").strip()
            ]))
        recommendation_result = await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=recommendation_part,
            sku=None,
            previous_result_skus=fact_previous_result_skus,
            allow_llm_fallback=False,
            scoped_comparison_candidates=bool(
                len(fact_previous_result_skus) >= 2
                and any(term in fact_part for term in ("比较", "对比"))
                and "更适合" in recommendation_part
                and re.search(r"(?:[1-9]\d?|[一二两三四五六七八九十])\s*(?:个)?人", recommendation_part)
            ),
            **intent_execution_options,
        )
        if fact_result or recommendation_result:
            merged_result = dict(recommendation_result or fact_result or {})
            if fact_result and recommendation_result:
                if composite_question.get("order") == "recommendation_first":
                    merged_result["answer"] = _merge_composite_answers(recommendation_result.get("answer", ""), fact_result.get("answer", ""))
                else:
                    merged_result["answer"] = _merge_composite_answers(fact_result.get("answer", ""), recommendation_result.get("answer", ""))
                merged_result["intent"] = "recommendation"
                merged_result["answer_type"] = recommendation_result.get("answer_type") or fact_result.get("answer_type")
                merged_result["results"] = recommendation_result.get("results") or fact_result.get("results") or []
                merged_result["result_skus"] = recommendation_result.get("result_skus") or fact_result.get("result_skus") or []
                merged_result.setdefault("debug", {})
                merged_result["debug"]["composite_question"] = {
                    "fact_part": fact_part,
                    "recommendation_part": recommendation_part,
                    "original_recommendation_part": composite_question["recommendation_part"],
                    "order": composite_question.get("order") or "fact_first",
                    "strategy": "detail_usage_compare_plus_recommendation",
                }
            elif fact_result:
                merged_result = fact_result
            elif recommendation_result:
                merged_result = recommendation_result
            return await _save_agent_result_and_return(
                db,
                user_id=user_id,
                question=question,
                conversation_id=conversation_id,
                agent_result=merged_result,
                request_start=request_start,
                branch="composite_multi_intent",
            )

    named_products = _products_named_in_question(db, question)
    recommendation_followup_context = _latest_recommendation_context_for_sources(db, conversation_id)
    candidate_followup_context = _latest_candidate_context_for_sources(db, conversation_id)
    active_product_context_skus = _latest_active_product_skus(db, conversation_id, user_id)
    context_pair_result = None
    if (
        _is_ordinal_compare_followup_question(question)
        or _is_contextual_candidate_comparison_question(question)
        or _is_contextual_power_followup_question(question)
        or "哪个更适合" in question
        or _is_plural_heat_source_followup(question)
    ):
        pair_skus = (
            _previous_result_skus_for_pre_runtime(
                db,
                user_id=user_id,
                conversation_id=conversation_id,
                question=question,
            )
            if _is_contextual_power_followup_question(question)
            else _context_skus_for_pair_followup(recommendation_followup_context, candidate_followup_context)
        )
        if not pair_skus and _is_plural_heat_source_followup(question):
            pair_skus = _previous_result_skus_for_pre_runtime(
                db,
                user_id=user_id,
                conversation_id=conversation_id,
                question=question,
            )[:2]
        if pair_skus:
            context_pair_result = (
                _context_power_followup_result(db, question, pair_skus)
                if _is_contextual_power_followup_question(question)
                else (
                    _context_pair_followup_result(db, question, pair_skus)
                    if _context_pair_matches_current_turn_subject(db, question, pair_skus)
                    else None
                )
            )
    followup_runtime_bypass = _should_bypass_usage_care_and_faq_for_followup(
        db,
        conversation_id=conversation_id,
        question=question,
        conversation_history=_build_conversation_history(db, conversation_id, user_id) if conversation_id else [],
    )
    customer_perf_service.log_stage(
        "followup_runtime_guard",
        perf_counter(),
        bypass=bool(followup_runtime_bypass),
        has_followup=bool(_is_recommendation_followup_question(question)),
        conversation_id=conversation_id,
    )
    usage_care_start = perf_counter()
    usage_care_result = None
    if (
        not context_pair_result
        and _is_product_usage_care_question(question)
        and not customer_agent_intent_service._looks_like_utensil_material_safety_question(question)
        and not _is_unbound_product_usage_question(question)
        and not _has_non_usage_care_field_contract(question)
        and not _semantic_formal_field_preempts_legacy_shortcuts(semantic_preplan)
        and not _semantic_product_qa_preempts_legacy_shortcuts(semantic_preplan)
        and not followup_runtime_bypass
        and not (len(named_products) > 1)
        and not _semantic_prefers_sealed_product_qa(phase1_plan)
        and not _should_prioritize_semantic_structured_route(question, phase1_plan)
        and not semantic_safety_procedure
    ):
        usage_care_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=named_products,
        )
        if len((usage_care_result or {}).get("result_skus") or []) > 1:
            usage_care_result = None
    customer_perf_service.log_stage(
        "product_usage_care_fast_path",
        usage_care_start,
        hit=bool(usage_care_result),
        intent=usage_care_result.get("intent") if usage_care_result else None,
        agent_mode=(usage_care_result.get("debug") or {}).get("agent_mode") if usage_care_result else None,
    )
    if usage_care_result:
        usage_care_result = _attach_phase1_plan_and_timing(
            usage_care_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        usage_care_result = _apply_request_sku_anchor_guard(
            db,
            question=question,
            agent_result=usage_care_result,
            request_anchor_sku=request_sku_anchor,
            phase1_plan=phase1_plan,
        )
        usage_care_result = _finalize_answer(usage_care_result)
        usage_care_result = _shape_answer_for_output(usage_care_result)
        usage_care_result = customer_final_answer_arbiter.arbitrate_final_answer(
            usage_care_result
        )
        stage_start = perf_counter()
        conversation = _get_or_create_conversation(db, user_id, question, usage_care_result.get("sku"), conversation_id)
        db.add(CustomerServiceMessage(
            conversation_id=conversation.id,
            role="user",
            content=question,
            sku=usage_care_result.get("sku"),
        ))
        assistant_turn_index = _assistant_turn_index(db, conversation.id)
        sources_with_context = _sources_with_result_context(
            usage_care_result,
            turn_index=assistant_turn_index,
            user_question=question,
        )
        assistant_message = CustomerServiceMessage(
            conversation_id=conversation.id,
            role="assistant",
            content=usage_care_result["answer"],
            sku=usage_care_result.get("sku"),
            sources_json=json.dumps(sources_with_context, ensure_ascii=False, default=str),
        )
        db.add(assistant_message)
        _touch_conversation(conversation, usage_care_result.get("sku"))
        conversation_id_value = conversation.id
        message_id_value = assistant_message.id
        db.commit()
        customer_perf_service.log_stage("save_messages_and_commit", stage_start, branch="product_usage_care_fast_path", intent=usage_care_result.get("intent"))
        customer_perf_service.log_stage("ask_customer_service.total", request_start, branch="product_usage_care_fast_path", intent=usage_care_result.get("intent"), agent_mode=(usage_care_result.get("debug") or {}).get("agent_mode"))
        customer_perf_service.summarize_request(
            final_answer=usage_care_result.get("answer"),
            intent=usage_care_result.get("intent"),
            agent_mode=(usage_care_result.get("debug") or {}).get("agent_mode"),
        )
        release_session_connection(db)
        public_intent = _public_intent_name(usage_care_result.get("intent"), usage_care_result.get("answer_type"))
        return {
            "conversation_id": conversation_id_value,
            "message_id": message_id_value,
            "intent": public_intent,
            "answer_type": usage_care_result.get("answer_type"),
            "confidence": usage_care_result.get("confidence"),
            "uncertainty": usage_care_result.get("uncertainty"),
            "needs_clarification": usage_care_result.get("needs_clarification", False),
            "anomalies": usage_care_result.get("anomalies") or [],
            "suggested_followups": usage_care_result.get("suggested_followups") or [],
            "followups": usage_care_result.get("followups") or usage_care_result.get("suggested_followups") or [],
            "warnings": usage_care_result.get("warnings") or [],
            "evidence": usage_care_result.get("evidence") or [],
            "agent_quality": usage_care_result.get("agent_quality") or {},
            "answer_metadata": usage_care_result.get("answer_metadata") or {},
            "debug": usage_care_result.get("debug") or {},
            "sku": usage_care_result.get("sku"),
            "answer": usage_care_result["answer"],
            "sources": sources_with_context,
            "actions": usage_care_result.get("actions") or [],
            "results": usage_care_result.get("results") or [],
            "steps": usage_care_result.get("steps") or [],
            "result_skus": usage_care_result.get("result_skus") or [],
            "agent_mode": (usage_care_result.get("debug") or {}).get("agent_mode"),
        }
    faq_start = perf_counter()
    semantic_route = _semantic_preplan_route_family(semantic_preplan)
    semantic_contract_preempts_faq = bool(
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("called")
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and semantic_route in {
            "structured_query",
            "recommendation",
            "comparison",
            "product_bound_qa",
            "contents_accessories",
        }
    )
    faq_intent = None if (
        context_pair_result
        or named_products
        or followup_runtime_bypass
        or semantic_contract_preempts_faq
    ) else _classify_customer_faq_intent(question)
    faq_result = None
    if faq_intent:
        faq_result = await _answer_customer_faq_fast_path(db, question, faq_intent)
    customer_perf_service.log_stage(
        "customer_faq_fast_path",
        faq_start,
        hit=bool(faq_result),
        intent=faq_intent,
        agent_mode=(faq_result.get("debug") or {}).get("agent_mode") if faq_result else None,
    )
    if faq_result:
        faq_result = _attach_phase1_plan_and_timing(
            faq_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
            ),
        )
        faq_result = _apply_request_sku_anchor_guard(
            db,
            question=question,
            agent_result=faq_result,
            request_anchor_sku=request_sku_anchor,
            phase1_plan=phase1_plan,
        )
        faq_result = _finalize_answer(faq_result)
        faq_result = _shape_answer_for_output(faq_result)
        faq_result = await _attach_debug_supporting_knowledge(db, faq_result, question)
        faq_result = _shape_answer_for_output(faq_result)
        faq_result = customer_final_answer_arbiter.arbitrate_final_answer(faq_result)
        stage_start = perf_counter()
        conversation = _get_or_create_conversation(db, user_id, question, faq_result.get("sku"), conversation_id)
        db.add(CustomerServiceMessage(
            conversation_id=conversation.id,
            role="user",
            content=question,
            sku=faq_result.get("sku"),
        ))
        assistant_turn_index = _assistant_turn_index(db, conversation.id)
        sources_with_context = _sources_with_result_context(
            faq_result,
            turn_index=assistant_turn_index,
            user_question=question,
        )
        assistant_message = CustomerServiceMessage(
            conversation_id=conversation.id,
            role="assistant",
            content=faq_result["answer"],
            sku=faq_result.get("sku"),
            sources_json=json.dumps(sources_with_context, ensure_ascii=False, default=str),
        )
        db.add(assistant_message)
        _touch_conversation(conversation, faq_result.get("sku"))
        conversation_id_value = conversation.id
        message_id_value = assistant_message.id
        db.commit()
        customer_perf_service.log_stage("save_messages_and_commit", stage_start, branch="faq_fast_path", intent=faq_result.get("intent"))
        customer_perf_service.log_stage("ask_customer_service.total", request_start, branch="faq_fast_path", intent=faq_result.get("intent"), agent_mode=(faq_result.get("debug") or {}).get("agent_mode"))
        customer_perf_service.summarize_request(
            final_answer=faq_result.get("answer"),
            intent=faq_result.get("intent"),
            agent_mode=(faq_result.get("debug") or {}).get("agent_mode"),
        )
        release_session_connection(db)
        public_intent = _public_intent_name(faq_result.get("intent"), faq_result.get("answer_type"))
        return {
            "conversation_id": conversation_id_value,
            "message_id": message_id_value,
            "intent": public_intent,
            "answer_type": faq_result.get("answer_type"),
            "confidence": faq_result.get("confidence"),
            "uncertainty": faq_result.get("uncertainty"),
            "needs_clarification": faq_result.get("needs_clarification", False),
            "anomalies": faq_result.get("anomalies") or [],
            "suggested_followups": faq_result.get("suggested_followups") or [],
            "followups": faq_result.get("followups") or faq_result.get("suggested_followups") or [],
            "warnings": faq_result.get("warnings") or [],
            "evidence": faq_result.get("evidence") or [],
            "agent_quality": faq_result.get("agent_quality") or {},
            "answer_metadata": faq_result.get("answer_metadata") or {},
            "debug": faq_result.get("debug") or {},
            "sku": faq_result.get("sku"),
            "answer": faq_result["answer"],
            "sources": sources_with_context,
            "actions": faq_result.get("actions") or [],
            "results": faq_result.get("results") or [],
            "steps": faq_result.get("steps") or [],
            "result_skus": faq_result.get("result_skus") or [],
            "agent_mode": (faq_result.get("debug") or {}).get("agent_mode"),
        }

    stage_start = perf_counter()
    shipping_intent_signal = customer_field_contract.detect_shipping_intent_signal(question)
    guardrail_field_contract = customer_field_contract.detect_field_contract(question)
    formal_non_weather_field_signal = bool(
        guardrail_field_contract is not None
        and guardrail_field_contract.field_type != "usage_scene"
    )
    agent_result = customer_enterprise_guardrail_service.evaluate_question(question)
    semantic_catalog_request = bool(
        isinstance(semantic_preplan, dict)
        and _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and str(semantic_preplan.get("route_family") or "").strip() == "structured_query"
        and str(semantic_preplan.get("route_hint") or "").strip() == "query_products"
    )
    if (
        shipping_intent_signal
        or formal_non_weather_field_signal
        or semantic_catalog_request
        or _is_internal_catalogue_filter_query(question)
    ):
        agent_result = None
    customer_perf_service.log_stage("guardrail.evaluate_question", stage_start, matched=bool(agent_result))
    if not agent_result and context_pair_result:
        agent_result = context_pair_result
    if agent_result:
        stage_start = perf_counter()
        guard_start = perf_counter()
        agent_result = _run_phase1_answer_guard(agent_result, phase1_plan)
        agent_result = _apply_sealed_context_anchor_cross_sku_guard(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=agent_result,
        )
        guard_duration_ms = round((perf_counter() - guard_start) * 1000, 2)
        agent_result = _attach_phase1_plan_and_timing(
            agent_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
                guard_duration_ms=guard_duration_ms,
            ),
        )
        agent_result = _attach_semantic_preplan_debug(
            agent_result,
            semantic_preplan,
            final_route=str(agent_result.get("answer_type") or agent_result.get("intent") or ""),
        )
        agent_result = _apply_request_sku_anchor_guard(
            db,
            question=question,
            agent_result=agent_result,
            request_anchor_sku=request_sku_anchor,
            phase1_plan=phase1_plan,
        )
        agent_result = _finalize_answer(agent_result)
        agent_result = _shape_answer_for_output(agent_result)
        agent_result = await _attach_debug_supporting_knowledge(db, agent_result, question)
        agent_result = _attach_semantic_preplan_debug(
            agent_result,
            semantic_preplan,
            final_route=str(agent_result.get("answer_type") or agent_result.get("intent") or ""),
        )
        agent_result = _shape_answer_for_output(agent_result)
        agent_result = customer_final_answer_arbiter.arbitrate_final_answer(agent_result)
        agent_result = _attach_agent_quality(agent_result, question)
        conversation = _get_or_create_conversation(db, user_id, question, agent_result.get("sku"), conversation_id)
        db.add(CustomerServiceMessage(
            conversation_id=conversation.id,
            role="user",
            content=question,
            sku=agent_result.get("sku"),
        ))
        assistant_turn_index = _assistant_turn_index(db, conversation.id)
        sources_with_context = _sources_with_result_context(
            agent_result,
            turn_index=assistant_turn_index,
            user_question=question,
        )
        assistant_message = CustomerServiceMessage(
            conversation_id=conversation.id,
            role="assistant",
            content=agent_result["answer"],
            sku=agent_result.get("sku"),
            sources_json=json.dumps(sources_with_context, ensure_ascii=False, default=str),
        )
        db.add(assistant_message)
        _touch_conversation(conversation, agent_result.get("sku"))
        db.flush()
        conversation_id_value = conversation.id
        message_id_value = assistant_message.id
        db.commit()
        customer_perf_service.log_stage("save_messages_and_commit", stage_start, branch="guardrail")
        customer_perf_service.log_stage("ask_customer_service.total", request_start, branch="guardrail", intent=agent_result.get("intent"))
        customer_perf_service.summarize_request(final_answer=agent_result.get("answer"), intent=agent_result.get("intent"), agent_mode=(agent_result.get("debug") or {}).get("agent_mode"))
        release_session_connection(db)
        agent_result = _attach_semantic_preplan_debug(
            agent_result,
            semantic_preplan,
            final_route=str(agent_result.get("answer_type") or agent_result.get("intent") or ""),
        )
        public_intent = _public_intent_name(agent_result.get("intent"), agent_result.get("answer_type"))
        return {
            "conversation_id": conversation_id_value,
            "message_id": message_id_value,
            "intent": public_intent,
            "answer_type": agent_result.get("answer_type"),
            "confidence": agent_result.get("confidence"),
            "uncertainty": agent_result.get("uncertainty"),
            "needs_clarification": agent_result.get("needs_clarification", False),
            "anomalies": agent_result.get("anomalies") or [],
            "suggested_followups": agent_result.get("suggested_followups") or [],
            "followups": agent_result.get("followups") or agent_result.get("suggested_followups") or [],
            "warnings": agent_result.get("warnings") or [],
            "evidence": agent_result.get("evidence") or [],
            "agent_quality": agent_result.get("agent_quality") or {},
            "answer_metadata": agent_result.get("answer_metadata") or {},
            "debug": agent_result.get("debug") or {},
            "sku": agent_result.get("sku"),
            "answer": agent_result["answer"],
            "sources": sources_with_context,
            "actions": agent_result.get("actions") or [],
            "results": agent_result.get("results") or [],
            "steps": agent_result.get("steps") or [],
            "result_skus": agent_result.get("result_skus") or [
                str(item.get("sku") or "").strip().upper()
                for item in (agent_result.get("results") or [])
                if isinstance(item, dict) and str(item.get("sku") or "").strip()
            ],
            "candidate_skus": agent_result.get("candidate_skus") or agent_result.get("result_skus") or [
                str(item.get("sku") or "").strip().upper()
                for item in (agent_result.get("results") or [])
                if isinstance(item, dict) and str(item.get("sku") or "").strip()
            ],
            "agent_mode": (agent_result.get("debug") or {}).get("agent_mode"),
        }

    preloaded_entity_stack: list[dict] | None = None
    preloaded_conversation_history: list[dict] | None = None

    sku_identity_start = perf_counter()
    agent_result = _try_product_sku_identity_shortcut(db, question)
    customer_perf_service.log_stage(
        "product_sku_identity_shortcut",
        sku_identity_start,
        hit=bool(agent_result),
        agent_mode=(agent_result.get("debug") or {}).get("agent_mode") if agent_result else None,
    )

    explicit_multi_field_start = perf_counter()
    explicit_multi_field_result = None
    if not agent_result:
        explicit_multi_field_result = _phase1_explicit_sku_alcohol_people_result(db, question)
        agent_result = explicit_multi_field_result
    customer_perf_service.log_stage(
        "explicit_sku_compatibility_people",
        explicit_multi_field_start,
        hit=bool(explicit_multi_field_result),
        agent_mode=(explicit_multi_field_result.get("debug") or {}).get("agent_mode") if explicit_multi_field_result else None,
    )

    explicit_detail_start = perf_counter()
    explicit_detail_result = None
    if not agent_result and not _semantic_prefers_sealed_product_qa(phase1_plan):
        explicit_detail_result = await _try_explicit_sku_detail_shortcut(db, question)
        agent_result = explicit_detail_result
    customer_perf_service.log_stage(
        "explicit_sku_detail_shortcut",
        explicit_detail_start,
        hit=bool(explicit_detail_result),
        agent_mode=(explicit_detail_result.get("debug") or {}).get("agent_mode") if explicit_detail_result else None,
    )

    alcohol_exclusion_start = perf_counter()
    alcohol_exclusion_result = None
    if not agent_result:
        alcohol_exclusion_result = _phase1_except_alcohol_stove_cookware_result(db, question)
        agent_result = alcohol_exclusion_result
    customer_perf_service.log_stage(
        "structured_alcohol_stove_cookware_exclusion",
        alcohol_exclusion_start,
        hit=bool(alcohol_exclusion_result),
        agent_mode=(alcohol_exclusion_result.get("debug") or {}).get("agent_mode") if alcohol_exclusion_result else None,
    )

    qa_start = perf_counter()
    qa_result = None
    if not agent_result and shipping_intent_signal:
        # Shipping remains a safety-sensitive formal field.  A response is
        # allowed here only when the normal helper seals the product and finds
        # field-compatible QA evidence on that same SKU; otherwise it returns
        # None and the existing safe-missing path remains authoritative.
        qa_result = _try_product_qa_shortcut(db, question, phase1_plan=phase1_plan)
        agent_result = qa_result
    elif (
        not agent_result
        and customer_field_contract.detect_field_contract(question) is None
        and not _formal_field_contract_preempts_product_qa(question, phase1_plan)
    ):
        qa_result = await _try_product_qa_shortcut_with_semantic_selection(
            db,
            question,
            phase1_plan=phase1_plan,
        )
        agent_result = qa_result
    customer_perf_service.log_stage(
        "product_qa_shortcut",
        qa_start,
        hit=bool(qa_result),
        agent_mode=(qa_result.get("debug") or {}).get("agent_mode") if qa_result else None,
    )

    # A high-confidence semantic QA turn without a sealed same-SKU record is
    # not eligible for generic KB or legacy-agent fallback.  Preserve its
    # EntityResolutionContract and either clarify an unknown subject or give
    # the product-specific safe-missing response instead of fabricating a
    # product fact from a top retrieval hit.
    # DeepSeek's evidence selector and entailment auditor are independent
    # semantic calls. A transient conservative verdict must not erase a valid
    # same-SKU answer. Each bounded retry remains fully sealed by the same
    # entity, quote, and strict-entailment contracts; failure after all tries
    # still returns the product-specific safe-missing response.
    qa_safe_missing = agent_result if _is_sealed_product_qa_safe_missing(agent_result) else None
    if qa_safe_missing:
        # A QA miss is not proof that the sealed product has no relevant
        # knowledge-base evidence. Preserve this result solely as the final
        # fail-closed fallback while the same-SKU RAG provider evaluates the
        # semantic question (including a supported part of a compound turn).
        agent_result = None
    compound_preplan = (
        (phase1_plan.get("semantic_preplan") or {})
        if isinstance(phase1_plan, dict)
        else {}
    )
    # Some Phase-2 adapters replace the executable plan with a narrowed copy
    # while the accepted semantic preplan is retained separately for audit.
    # Compound sub-intents belong to that accepted semantic decision, so do
    # not lose them merely because the execution copy omitted the list.
    if not (compound_preplan.get("qa_evidence_queries") or []) and isinstance(semantic_preplan, dict):
        compound_preplan = semantic_preplan
    compound_queries = [
        str(item or "").strip()
        for item in (compound_preplan.get("qa_evidence_queries") or [])
        if str(item or "").strip()
    ]
    if agent_result is not None and compound_queries:
        # A single exact QA may cover one child, but it cannot terminate a
        # semantic multi-question contract before the remaining children have
        # been independently evaluated.
        qa_safe_missing = agent_result
        agent_result = None
    if compound_queries and (agent_result is not None or qa_safe_missing is not None):
        compound_answers: list[dict] = []
        compound_missing_queries: list[str] = []
        compound_answered_requests: list[tuple[str, str]] = []
        parent_preplan = compound_preplan
        for query in compound_queries:
            child_preplan = dict(parent_preplan)
            child_preplan.update({"qa_evidence_query": query, "qa_evidence_queries": [], "compound": False, "strict_compound_child_qa": True})
            child_plan = dict(phase1_plan)
            child_plan["semantic_preplan"] = child_preplan
            child_question = " ".join(item for item in (str(child_preplan.get("subject_text") or "").strip(), query) if item) or question
            candidate = await _try_semantic_compound_child_answer(
                db,
                child_question,
                child_plan,
            )
            if not _record_semantic_compound_child(
                query=query,
                candidate=candidate,
                compound_answers=compound_answers,
                answered_requests=compound_answered_requests,
            ):
                compound_missing_queries.append(query)
        if compound_answers:
            base_result = agent_result or compound_answers[0]
            agent_result = base_result
            base_text = str(agent_result.get("answer") or "").strip()
            extras = [str(item.get("answer") or "").strip() for item in compound_answers if str(item.get("answer") or "").strip() and str(item.get("answer") or "").strip() not in base_text]
            if extras:
                agent_result["answer"] = "\n".join([base_text, *extras])
            if compound_missing_queries:
                agent_result["answer"] = "\n".join([
                    str(agent_result.get("answer") or "").strip(),
                    *(f"关于“{query}”：当前同 SKU 资料未直接确认，无法确认。" for query in compound_missing_queries),
                ])
            compound_metadata = agent_result.setdefault("answer_metadata", {})
            compound_metadata["compound_product_qa_queries"] = compound_queries
            compound_metadata["compound_missing_child_queries"] = compound_missing_queries
            missing_query_set = set(compound_missing_queries)
            compound_metadata["answer_coverage_contract"] = (
                customer_answer_coverage_contract.build_answer_coverage_contract(
                    compound_queries,
                    answered_requests=[
                        item for item in compound_answered_requests
                        if item[0] not in missing_query_set
                    ],
                    unsupported_requests=compound_missing_queries,
                ).to_dict()
            )
        elif qa_safe_missing is not None:
            # No child yielded distinct evidence. Preserve the already sealed
            # parent QA and explicitly fail closed for every remaining child
            # instead of falling back to a single-question shortcut later.
            agent_result = qa_safe_missing
            agent_result["answer"] = "\n".join([
                str(agent_result.get("answer") or "").strip(),
                *(f"关于“{query}”：当前同 SKU 资料未直接确认，无法确认。" for query in compound_queries[1:]),
            ])
            compound_metadata = agent_result.setdefault("answer_metadata", {})
            compound_metadata["compound_product_qa_queries"] = compound_queries
            compound_metadata["compound_missing_child_queries"] = compound_queries[1:]
            parent_evidence_id = str(
                compound_metadata.get("evidence_source")
                or compound_metadata.get("source")
                or "same_sku_parent_evidence"
            )
            compound_metadata["answer_coverage_contract"] = (
                customer_answer_coverage_contract.build_answer_coverage_contract(
                    compound_queries,
                    answered_requests=[(compound_queries[0], parent_evidence_id)],
                    unsupported_requests=compound_queries[1:],
                ).to_dict()
            )
        elif agent_result is not None and len(compound_queries) > 1:
            # The parent QA may be valid while every narrowed child misses.
            # Keep the parent answer, but never silently drop the remaining
            # semantically independent questions.
            agent_result["answer"] = "\n".join([
                str(agent_result.get("answer") or "").strip(),
                *(f"关于“{query}”：当前同 SKU 资料未直接确认，无法确认。" for query in compound_queries[1:]),
            ])
            compound_metadata = agent_result.setdefault("answer_metadata", {})
            compound_metadata["compound_product_qa_queries"] = compound_queries
            compound_metadata["compound_missing_child_queries"] = compound_queries[1:]
            parent_evidence_id = str(
                compound_metadata.get("evidence_source")
                or compound_metadata.get("source")
                or "same_sku_parent_evidence"
            )
            compound_metadata["answer_coverage_contract"] = (
                customer_answer_coverage_contract.build_answer_coverage_contract(
                    compound_queries,
                    answered_requests=[(compound_queries[0], parent_evidence_id)],
                    unsupported_requests=compound_queries[1:],
                ).to_dict()
            )
    for _sealed_attempt in range(3):
        if agent_result or not _semantic_prefers_sealed_product_qa(phase1_plan):
            break
        agent_result = await _try_sealed_same_sku_knowledge_answer(
            db,
            question,
            phase1_plan,
        )
    if not agent_result:
        agent_result = qa_safe_missing
    if not agent_result:
        agent_result = _sealed_semantic_product_qa_entity_guard(
            db,
            question,
            phase1_plan,
        )
    if _is_sealed_product_qa_safe_missing(agent_result):
        recovered_structured_qa = await _try_same_sku_structured_best_effort_answer(
            db,
            question=question,
            safe_missing=agent_result,
            semantic_preplan=compound_preplan,
        )
        if recovered_structured_qa is not None:
            agent_result = recovered_structured_qa

    # The entity guard may be the first place that constructs a sealed
    # safe-missing response.  Run the already-planned compound children here
    # as well, before that fallback reaches any legacy shortcut or the final
    # save boundary.  No new intent is inferred: every child is an LLM-issued
    # semantic sub-query and remains bound to the same resolved SKU.
    if _is_sealed_product_qa_safe_missing(agent_result) and compound_queries:
        compound_answers: list[dict] = []
        compound_answered_requests: list[tuple[str, str]] = []
        compound_missing_queries: list[str] = []
        for query in compound_queries:
            child_preplan = dict(compound_preplan)
            child_preplan.update({"qa_evidence_query": query, "qa_evidence_queries": [], "compound": False})
            child_plan = dict(phase1_plan)
            child_plan["semantic_preplan"] = child_preplan
            child_question = " ".join(
                item for item in (str(child_preplan.get("subject_text") or "").strip(), query) if item
            ) or question
            candidate = await _try_semantic_compound_child_answer(
                db,
                child_question,
                child_plan,
            )
            if not _record_semantic_compound_child(
                query=query,
                candidate=candidate,
                compound_answers=compound_answers,
                answered_requests=compound_answered_requests,
            ):
                compound_missing_queries.append(query)
        if compound_answers:
            agent_result = compound_answers[0]
            extras = [str(item.get("answer") or "").strip() for item in compound_answers[1:] if str(item.get("answer") or "").strip()]
            if extras:
                agent_result["answer"] = "\n".join([str(agent_result.get("answer") or "").strip(), *extras])
            compound_metadata = agent_result.setdefault("answer_metadata", {})
            compound_metadata["compound_product_qa_queries"] = compound_queries
            compound_metadata["compound_missing_child_queries"] = compound_missing_queries
            compound_metadata["answer_coverage_contract"] = (
                customer_answer_coverage_contract.build_answer_coverage_contract(
                    compound_queries,
                    answered_requests=compound_answered_requests,
                    unsupported_requests=compound_missing_queries,
                ).to_dict()
            )

    shortcut_start = perf_counter()
    if (
        not agent_result
        and _semantic_preplan_route_family((phase1_plan or {}).get("semantic_preplan")) != "recommendation"
        and not _semantic_prefers_sealed_product_qa(phase1_plan)
        and not _formal_field_contract_preempts_product_qa(question, phase1_plan)
    ):
        agent_result = await _try_named_product_shortcut(db, user_id=user_id, question=question)
    customer_perf_service.log_stage("named_product_shortcut", shortcut_start, hit=bool(agent_result), agent_mode=(agent_result.get("debug") or {}).get("agent_mode") if agent_result else None)
    recommendation_followup_context = _latest_recommendation_context_for_sources(db, conversation_id)
    candidate_followup_context = _latest_candidate_context_for_sources(db, conversation_id)
    active_product_context_skus = _latest_active_product_skus(db, conversation_id, user_id)
    if (
        not agent_result
        and _looks_like_context_required_ordinal_compare(question)
        and not _has_followup_result_context(recommendation_followup_context)
        and not _has_followup_result_context(candidate_followup_context)
    ):
        agent_result = _context_required_ordinal_compare_result(question)
    if not agent_result and (
        _is_ordinal_compare_followup_question(question)
        or _is_contextual_candidate_comparison_question(question)
        or "哪个更适合" in question
        or _is_plural_heat_source_followup(question)
    ):
        pair_skus = _context_skus_for_pair_followup(recommendation_followup_context, candidate_followup_context)
        if not pair_skus and _is_plural_heat_source_followup(question):
            pair_skus = _previous_result_skus_for_pre_runtime(
                db,
                user_id=user_id,
                conversation_id=conversation_id,
                question=question,
            )[:2]
        if pair_skus and _context_pair_matches_current_turn_subject(db, question, pair_skus):
            agent_result = _context_pair_followup_result(db, question, pair_skus)
    if not agent_result:
        # The current turn's validated FieldContract owns field meaning. The
        # follow-up helper below is legacy compatibility only; it must not
        # reinterpret a word in the subject matter (for example “酒精” in a
        # storage question) as a different field such as heat_source.
        followup_field_request = resolve_requested_field_contract(question, phase1_plan)
        followup_canonical_field = str(followup_field_request.get("field_type") or "").strip()
        requested_followup_field = (
            customer_field_contract.product_detail_field_label(followup_canonical_field)
            if followup_canonical_field
            else _followup_requested_field(question)
        )
        ordinal_followup_sku = _ordinal_followup_target_sku(
            question,
            recommendation_followup_context,
            candidate_followup_context,
        )
        if not ordinal_followup_sku and any(term in question for term in ("第一个", "第一款", "最前面那个", "第二个", "第二款", "第三个", "第三款", "最后一个", "最后一款", "最后那个")):
            ordinal_followup_sku = _ordinal_followup_target_sku_from_ordered(
                question,
                _latest_ordered_result_skus_for_followup(
                    db,
                    conversation_id=conversation_id,
                    user_id=user_id,
                ),
            )
        if ordinal_followup_sku and requested_followup_field:
            agent_result = _product_field_followup_result(
                db,
                ordinal_followup_sku,
                requested_followup_field,
                question,
                identity_source="recommendation_context_ordinal",
                field_request_override=followup_field_request,
            )
            agent_result = _attach_recommendation_followup_contract(
                agent_result,
                question,
                recommendation_followup_context,
            )
    # A sealed follow-up anchor only supplies identity when the current turn
    # does not name a product. Explicit product references are current-turn
    # authority; otherwise a heat-source term can collapse a two-product
    # compatibility request onto the previous anchor.
    if not agent_result and not named_products:
        heat_source_followup_sku = ""
        latest_ordered_followup_skus: list[str] = []
        if conversation_id:
            latest_ordered_followup_skus = _latest_ordered_result_skus_for_followup(
                db,
                conversation_id,
                user_id,
            )
            previous_user_question = _latest_user_question_for_followup(
                db,
                conversation_id,
                user_id,
                question,
            )
            # Prefer the preserved parent order for replacement
            # recommendations.  The latest visible answer may omit the
            # previously selected item, so numbering it again would bind a
            # pronoun to the wrong SKU.
            ordinal_reference_skus = []
            if isinstance(recommendation_followup_context, dict):
                ordinal_reference_skus = [
                    str(sku or "").strip().upper()
                    for sku in recommendation_followup_context.get("ordinal_reference_skus") or []
                    if str(sku or "").strip()
                ]
            if not ordinal_reference_skus and isinstance(candidate_followup_context, dict):
                ordinal_reference_skus = [
                    str(sku or "").strip().upper()
                    for sku in candidate_followup_context.get("ordinal_reference_skus") or []
                    if str(sku or "").strip()
                ]
            heat_source_followup_sku = _ordinal_followup_target_sku_from_ordered(
                previous_user_question,
                ordinal_reference_skus or latest_ordered_followup_skus,
            )
        if isinstance(candidate_followup_context, dict):
            candidate_ordered_skus = _ordered_context_skus(candidate_followup_context)
            if not heat_source_followup_sku:
                heat_source_followup_sku = _ordinal_followup_target_sku_from_ordered(
                    str(candidate_followup_context.get("user_question") or ""),
                    candidate_ordered_skus,
                )
            if not heat_source_followup_sku:
                heat_source_followup_sku = str(candidate_followup_context.get("last_referenced_sku") or "").strip().upper()
        if not heat_source_followup_sku:
            heat_source_followup_sku = _pronoun_heat_source_followup_target_sku(question, recommendation_followup_context)
        if not heat_source_followup_sku and isinstance(candidate_followup_context, dict):
            heat_source_followup_sku = str(candidate_followup_context.get("last_referenced_sku") or "").strip().upper()
        if not heat_source_followup_sku and isinstance(candidate_followup_context, dict):
            ordered_candidate_skus = _ordered_context_skus(candidate_followup_context)
            if ordered_candidate_skus:
                heat_source_followup_sku = ordered_candidate_skus[0]
        if not heat_source_followup_sku and latest_ordered_followup_skus:
            heat_source_followup_sku = latest_ordered_followup_skus[0]
        # A plural field filter addresses the sealed candidate set, never its
        # first member.  Selecting index zero here silently converted a
        # candidate-domain query into a single-product fact answer.
        if (
            heat_source_followup_sku
            and requested_followup_field
            and not _is_candidate_set_heat_source_filter(question)
        ):
            agent_result = _product_field_followup_result(
                db,
                heat_source_followup_sku,
                requested_followup_field,
                question,
                identity_source="recommendation_context_pronoun",
                field_request_override=followup_field_request,
            )
            agent_result = _attach_recommendation_followup_contract(
                agent_result,
                question,
                recommendation_followup_context,
            )
    if not agent_result:
        agent_result = _comparison_choice_followup_result(
            db,
            question,
            recommendation_followup_context,
            candidate_followup_context,
        )
    if not agent_result:
        agent_result = _budget_recommendation_followup_result(
            db,
            question,
            recommendation_followup_context,
        )
    vague_price_start = perf_counter()
    vague_price_clarification = bool(
        not agent_result
        and _should_clarify_vague_single_product_price(
            question,
            explicit_sku=sku,
            named_products=named_products,
            recommendation_context=recommendation_followup_context,
            candidate_context=candidate_followup_context,
            active_product_skus=active_product_context_skus,
        )
    )
    if vague_price_clarification:
        agent_result = _vague_single_product_price_clarification_result()
    customer_perf_service.log_stage(
        "vague_single_product_price_guard",
        vague_price_start,
        hit=vague_price_clarification,
        agent_mode=(agent_result.get("debug") or {}).get("agent_mode") if vague_price_clarification else None,
    )
    force_runtime_ordinal_compare_followup = bool(
        not agent_result
        and _is_ordinal_compare_followup_question(question)
        and (
            _has_followup_result_context(recommendation_followup_context)
            or _has_followup_result_context(candidate_followup_context)
        )
    )
    force_runtime_empty_subset_followup = bool(
        not agent_result
        and _should_force_runtime_empty_subset_followup(
            question,
            explicit_sku=sku,
            named_products=named_products,
            candidate_context=candidate_followup_context,
        )
    )
    force_runtime_followup = bool(
        not agent_result
        and (
            force_runtime_empty_subset_followup
            or (
                _is_recommendation_followup_question(question)
                and (recommendation_followup_context or candidate_followup_context)
            )
        )
    )
    keep_preruntime_for_scoped_heat_source = bool(
        not agent_result
        and candidate_followup_context
        and _should_keep_preruntime_for_scoped_heat_source_followup(
            question,
            candidate_context=candidate_followup_context,
        )
    )
    category_reference_detail = _is_category_reference_detail_question(question)
    if not agent_result and keep_preruntime_for_scoped_heat_source and not category_reference_detail:
        scoped_previous_result_skus = _refine_scoped_followup_previous_result_skus(
            db,
            question=question,
            candidate_context=candidate_followup_context,
            previous_result_skus=_previous_result_skus_for_pre_runtime(
                db,
                user_id=user_id,
                conversation_id=conversation_id,
                question=question,
            ),
        )
        stage_start = perf_counter()
        scoped_agent_result = await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=question,
            sku=None,
            previous_result_skus=scoped_previous_result_skus,
            allow_llm_fallback=False,
            scoped_comparison_candidates=True,
            **intent_execution_options,
        )
        scoped_agent_result = _normalize_candidate_context_followup_result(
            scoped_agent_result,
            db,
            question=question,
            candidate_context=candidate_followup_context,
        )
        customer_perf_service.log_stage(
            "process_intent_request_scoped_heat_source_pre_runtime",
            stage_start,
            hit=bool(scoped_agent_result),
            intent=scoped_agent_result.get("intent") if scoped_agent_result else None,
            previous_result_skus_count=len(scoped_previous_result_skus or []),
        )
        if scoped_agent_result:
            agent_result = scoped_agent_result
    bypass_stateless_pre_runtime = bool(
        (recommendation_followup_context or candidate_followup_context)
        and not force_runtime_followup
        and not keep_preruntime_for_scoped_heat_source
    )
    bypass_pre_runtime_for_detail_context = False
    if not agent_result and not bypass_stateless_pre_runtime and not category_reference_detail:
        preloaded_entity_stack = _latest_entity_stack(db, conversation_id, user_id)
        preloaded_conversation_history = _build_conversation_history(db, conversation_id, user_id)
        bypass_pre_runtime_for_detail_context = _should_bypass_preruntime_for_runtime_direct_detail(
            db,
            question=question,
            entity_stack=preloaded_entity_stack,
            conversation_history=preloaded_conversation_history,
        )
    if (
        not agent_result
        and not followup_runtime_bypass
        and not bypass_stateless_pre_runtime
        and not force_runtime_followup
        and not force_runtime_empty_subset_followup
        and not force_runtime_ordinal_compare_followup
        and not category_reference_detail
        and not bypass_pre_runtime_for_detail_context
        and not semantic_safety_procedure
    ):
        deterministic_previous_result_skus = _previous_result_skus_for_pre_runtime(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
            question=question,
        )
        stage_start = perf_counter()
        try:
            agent_result = await customer_agent_intent_service.process_intent_request(
                db,
                user_id=user_id,
                question=question,
                sku=None,
                previous_result_skus=deterministic_previous_result_skus,
                allow_llm_fallback=False,
                **intent_execution_options,
            )
        except HTTPException as exc:
            unresolved_skus = customer_agent_service._extract_skus(question)
            if exc.status_code == status.HTTP_404_NOT_FOUND and unresolved_skus:
                unresolved_sku = str(unresolved_skus[0]).strip().upper()
                agent_result = {
                    "intent": "clarify",
                    "answer_type": "clarification",
                    "answer": (
                        f"当前未找到 SKU {unresolved_sku} 对应的商品资料，暂时无法核对它是否满足你的需求。"
                        "请检查 SKU 是否输入正确，或提供商品名称让我继续查找。"
                    ),
                    "results": [],
                    "result_skus": [],
                    "needs_clarification": True,
                    "debug": {"agent_mode": "missing_sku_clarification"},
                    "skip_polish": True,
                }
            else:
                raise
        customer_perf_service.log_stage("process_intent_request_pre_runtime", stage_start, hit=bool(agent_result), intent=agent_result.get("intent") if agent_result else None)
        scenario_recommendation_like = (
            customer_agent_intent_service._looks_like_scenario_recommendation_question(question)
            or _phase1_is_water_kettle_selection_scenario(question)
            or _phase1_is_stove_griddle_combo_scenario(question)
            or customer_agent_planner_service._looks_like_scenario_statement_recommendation(question)
        )
        # Let the runtime own scenario recommendations when deterministic
        # intent parsing has no result. Calling _recommend_result here first
        # bypasses the runtime context/tool contract and can broaden a test or
        # customer candidate pool before filters are applied. The normal
        # deterministic fallback remains below the runtime path.
        planner_requires_recommendation = bool(
            phase1_plan.get("primary_intent") == "recommendation"
            and phase1_plan.get("must_return_products")
        )
        if (
            agent_result
            and str(agent_result.get("answer_type") or "").strip() in {"query_products", "product_query", "product_detail", "knowledge_base_answer"}
            and (scenario_recommendation_like or planner_requires_recommendation)
            and not customer_agent_intent_service._extract_skus(question)
        ):
            rebuilt = _phase1_structured_recommendation_result(
                db,
                {"primary_intent": "recommendation", "scenario": question},
            )
            if rebuilt and _phase1_result_skus(rebuilt):
                agent_result = rebuilt
        # A low-confidence planner may leave primary_intent empty even when the
        # current question is one of the structured, multi-domain recommendation
        # scenarios.  Do not let the generic recommendation formatter collapse
        # that scenario to its first retrieval hit: rebuild from the current
        # scenario before any result is persisted or shaped for display.
        if (
            agent_result
            and str(agent_result.get("answer_type") or "").strip() == "recommendation"
            and _phase1_force_structured_recommendation_rebuild(question)
            and not customer_agent_intent_service._extract_skus(question)
        ):
            rebuilt = _phase1_structured_recommendation_result(
                db,
                {"primary_intent": "recommendation", "scenario": question},
            )
            if rebuilt and _phase1_result_skus(rebuilt):
                agent_result = rebuilt
        if (
            agent_result
            and str(agent_result.get("answer_type") or "").strip() == "product_detail"
            and scenario_recommendation_like
            and not customer_agent_intent_service._looks_like_product_detail_question(question)
            and not customer_agent_intent_service._extract_skus(question)
        ):
            result_rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
            if not result_rows:
                result_rows = [
                    row for row in customer_agent_intent_service._all_product_rows(db)
                    if "锅" in str(row.get("category") or "")
                ][:3]
            if result_rows:
                top_row = result_rows[0]
                top_sku = str(top_row.get("sku") or "").strip().upper()
                top_name = str(top_row.get("product_name_cn") or top_row.get("product_name_en") or top_sku).strip()
                top_reason = "；".join(
                    str(top_row.get(key) or "").strip("。；; ")
                    for key in ("features", "usage_scenarios", "positioning")
                    if str(top_row.get(key) or "").strip()
                )
                answer = f"更推荐{top_name}（{top_sku}）。"
                if top_reason:
                    answer += f"它更贴合你当前这类场景，{top_reason[:80].rstrip('；;,，')}。"
                if len(result_rows) > 1:
                    backups: list[str] = []
                    for row in result_rows[1:3]:
                        sku = str(row.get('sku') or '').strip().upper()
                        name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
                        backups.append(f"{name}（{sku}）")
                    if backups:
                        answer += " 备选可以看" + "、".join(backups) + "。"
                agent_result["intent"] = "recommend_products"
                agent_result["answer_type"] = "recommendation"
                agent_result["answer"] = answer
                agent_result["skip_polish"] = True
    if not agent_result and conversation_id:
        active_anchor_result = await _active_product_anchor_field_followup_result(
            db,
            question,
            _latest_recommendation_context_for_sources(db, conversation_id),
            phase1_plan,
        )
        if active_anchor_result:
            agent_result = active_anchor_result
    if not agent_result and not followup_runtime_bypass and not bypass_stateless_pre_runtime and not force_runtime_followup and not force_runtime_empty_subset_followup and not force_runtime_ordinal_compare_followup and not bypass_pre_runtime_for_detail_context and not category_reference_detail:
        stage_start = perf_counter()
        agent_result = customer_agent_service.try_numeric_english_name_query(db, question)
        customer_perf_service.log_stage("legacy_rule_agent_fallback", stage_start, hit=bool(agent_result), intent=agent_result.get("intent") if agent_result else None)
    if not agent_result and not followup_runtime_bypass and not bypass_stateless_pre_runtime and not force_runtime_followup and not force_runtime_empty_subset_followup and not force_runtime_ordinal_compare_followup and not bypass_pre_runtime_for_detail_context and not category_reference_detail:
        stage_start = perf_counter()
        agent_result = customer_agent_service.process_agent_request(
            db,
            user_id=user_id,
            question=question,
            sku=None,
        )
        customer_perf_service.log_stage("legacy_rule_agent_total", stage_start, hit=bool(agent_result), intent=agent_result.get("intent") if agent_result else None)
        if _should_defer_legacy_rule_result_to_runtime(question, agent_result):
            agent_result = None
    if not agent_result:
        context_start = perf_counter()
        entity_stack = preloaded_entity_stack if preloaded_entity_stack is not None else _latest_entity_stack(db, conversation_id, user_id)
        persisted_anchor_sku = _latest_persisted_agent_context_sku(db, conversation_id)
        if (
            persisted_anchor_sku
            and not _has_explicit_sku_text(question)
            and not _products_named_in_question(db, question)
            and not _is_plural_heat_source_followup(question)
            and re.search(r"(?:它|这个|这款|该商品|该产品|本产品)", question)
        ):
            entity_stack = [{"sku": persisted_anchor_sku, "role": "current", "source": "persisted_single_sku_anchor"}]
        conversation_history = (
            preloaded_conversation_history
            if preloaded_conversation_history is not None
            else _build_conversation_history(db, conversation_id, user_id)
        )
        recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
        candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
        previous_result_skus = _previous_result_skus_for_pre_runtime(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
            question=question,
        )
        customer_perf_service.log_stage(
            "context_read",
            context_start,
            entity_stack_count=len(entity_stack or []),
            conversation_history_count=len(conversation_history or []),
            previous_result_skus_count=len(previous_result_skus or []),
            recommendation_context_present=bool(recommendation_context),
            candidate_context_present=bool(candidate_context),
        )
        contextual_conversation_history = conversation_history
        stage_start = perf_counter()
        recognized_intent = _recognized_intent_for_agent_fast_path(db, question, conversation_id)
        feedback_lessons = _build_feedback_lessons(db, user_id)
        release_session_connection(db)
        agent_result = await customer_agent_runtime_service.process_agent_request(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
            question=question,
            sku=None,
            previous_result_skus=previous_result_skus,
            entity_stack=entity_stack,
            conversation_history=contextual_conversation_history,
            feedback_lessons=feedback_lessons,
            recognized_intent=recognized_intent,
            answer_delta_callback=answer_delta_callback,
        )
        agent_result = _synchronize_context_read_trace(
            agent_result,
            previous_result_skus=previous_result_skus,
            recommendation_context=recommendation_context,
            candidate_context=candidate_context,
        )
        agent_result = _normalize_candidate_context_followup_result(
            agent_result,
            db,
            question=question,
            candidate_context=candidate_context,
        )
        customer_perf_service.log_stage("process_agent_request", stage_start, hit=bool(agent_result), intent=agent_result.get("intent") if agent_result else None, agent_mode=(agent_result.get("debug") or {}).get("agent_mode") if agent_result else None)
    if _should_retry_with_deterministic_agent(agent_result):
        stage_start = perf_counter()
        retry_result = await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=question,
            sku=None,
            previous_result_skus=[],
            allow_llm_fallback=False,
            **intent_execution_options,
        )
        customer_perf_service.log_stage("process_intent_request_retry", stage_start, hit=bool(retry_result), intent=retry_result.get("intent") if retry_result else None)
        if retry_result and retry_result.get("results"):
            agent_result = _prepare_deterministic_retry_result(retry_result)
    if not agent_result:
        stage_start = perf_counter()
        agent_result = await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=question,
            sku=None,
            previous_result_skus=[],
            **intent_execution_options,
        )
        customer_perf_service.log_stage("process_intent_request_fallback", stage_start, hit=bool(agent_result), intent=agent_result.get("intent") if agent_result else None)
    if force_runtime_followup and not agent_result:
        stage_start = perf_counter()
        context_start = perf_counter()
        entity_stack = preloaded_entity_stack if preloaded_entity_stack is not None else _latest_entity_stack(db, conversation_id, user_id)
        conversation_history = (
            preloaded_conversation_history
            if preloaded_conversation_history is not None
            else _build_conversation_history(db, conversation_id, user_id)
        )
        recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
        candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
        previous_result_skus = _previous_result_skus_for_pre_runtime(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
            question=question,
        )
        customer_perf_service.log_stage(
            "context_read",
            context_start,
            entity_stack_count=len(entity_stack or []),
            conversation_history_count=len(conversation_history or []),
            previous_result_skus_count=len(previous_result_skus or []),
            recommendation_context_present=bool(recommendation_context),
            candidate_context_present=bool(candidate_context),
        )
        recognized_intent = _recognized_intent_for_agent_fast_path(db, question, conversation_id)
        feedback_lessons = _build_feedback_lessons(db, user_id)
        release_session_connection(db)
        agent_result = await customer_agent_runtime_service.process_agent_request(
            db,
            user_id=user_id,
            conversation_id=conversation_id,
            question=question,
            sku=None,
            previous_result_skus=previous_result_skus,
            entity_stack=entity_stack,
            conversation_history=conversation_history,
            feedback_lessons=feedback_lessons,
            recognized_intent=recognized_intent,
            answer_delta_callback=answer_delta_callback,
        )
        agent_result = _synchronize_context_read_trace(
            agent_result,
            previous_result_skus=previous_result_skus,
            recommendation_context=recommendation_context,
            candidate_context=candidate_context,
        )
        agent_result = _normalize_candidate_context_followup_result(
            agent_result,
            db,
            question=question,
            candidate_context=candidate_context,
        )
        customer_perf_service.log_stage("process_agent_request_followup", stage_start, hit=bool(agent_result), intent=agent_result.get("intent") if agent_result else None, agent_mode=(agent_result.get("debug") or {}).get("agent_mode") if agent_result else None)
    if _asks_for_alternative_recommendation(question):
        runtime_debug = agent_result.get("debug") if isinstance(agent_result, dict) else {}
        runtime_result_skus = _phase1_result_skus(agent_result) if isinstance(agent_result, dict) else []
        ordered_context_skus = _ordered_context_skus(
            candidate_followup_context,
            recommendation_followup_context,
        )
        runtime_repeated_anchor = bool(
            ordered_context_skus
            and any(
                str(sku or "").strip().upper() == ordered_context_skus[0]
                for sku in runtime_result_skus
            )
        )
        runtime_declared_exhaustion = bool(
            isinstance(runtime_debug, dict)
            and "previous_recommended_skus_excluded" in runtime_debug
        )
        relative_price_alternative = any(
            term in question
            for term in ("更便宜", "便宜一点", "便宜点", "预算更低", "低预算")
        )
        if (
            not runtime_declared_exhaustion
            and (
                relative_price_alternative
                or not runtime_result_skus
                or runtime_repeated_anchor
            )
        ):
            alternative_result = await _alternative_recommendation_followup_result(
                db,
                question,
                recommendation_followup_context,
                candidate_followup_context,
            )
            if alternative_result:
                agent_result = alternative_result
    if (
        agent_result
        and str(agent_result.get("answer_type") or "").strip() == "product_detail"
        and not (agent_result.get("result_skus") or [])
        and _looks_like_ordinal_product_field_followup(question)
    ):
        ordered_followup_sku = _ordinal_followup_target_sku(
            question,
            recommendation_followup_context,
            candidate_followup_context,
        )
        if not ordered_followup_sku:
            ordered_followup_sku = _ordinal_followup_target_sku_from_ordered(
                question,
                _latest_ordered_result_skus_for_followup(db, conversation_id, user_id),
            )
        requested_followup_field = _followup_requested_field(question)
        if ordered_followup_sku and requested_followup_field:
            repaired_followup_result = _product_field_followup_result(
                db,
                ordered_followup_sku,
                requested_followup_field,
                question,
                identity_source="recommendation_context_anchor",
            )
            if repaired_followup_result:
                agent_result = repaired_followup_result
    if agent_result:
        agent_result = _apply_sealed_context_anchor_cross_sku_guard(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=agent_result,
        )
    agent_result = _phase1_filter_alcohol_stove_cookware_result(
        db,
        agent_result,
        question=question,
        candidate_context=candidate_followup_context or _latest_candidate_context_for_sources(db, conversation_id),
    )
    agent_result = _reject_unsealed_named_product_phase1_result(
        db,
        question,
        agent_result,
    )
    if isinstance(agent_result, dict):
        agent_result = _phase1_repair_recommendation_result(db, agent_result, phase1_plan)
        agent_result = _post_filter_recommendation_result(db, question, agent_result)
    if agent_result:
        stage_start = perf_counter()
        guard_start = perf_counter()
        agent_result = _run_phase1_answer_guard(agent_result, phase1_plan)
        agent_result = _normalize_alcohol_stove_cookware_list_answer_type(question, agent_result)
        guard_duration_ms = round((perf_counter() - guard_start) * 1000, 2)
        agent_result = _attach_phase1_plan_and_timing(
            agent_result,
            phase1_plan,
            _phase1_timing(
                request_start=request_start,
                planner_duration_ms=planner_duration_ms,
                guard_duration_ms=guard_duration_ms,
            ),
        )
        agent_result = _finalize_answer(agent_result)
        agent_result = _enforce_field_evidence_policy(db, question, agent_result)
        agent_result = _apply_sealed_context_anchor_cross_sku_guard(
            db,
            user_id=user_id,
            question=question,
            conversation_id=conversation_id,
            agent_result=agent_result,
        )
        agent_result = _apply_request_sku_anchor_guard(
            db,
            question=question,
            agent_result=agent_result,
            request_anchor_sku=request_sku_anchor,
            phase1_plan=phase1_plan,
        )
        agent_result = _ensure_debug_plan_raw_question(agent_result, question)
        answer_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
        skip_polish = bool(agent_result.get("skip_polish"))
        if _should_skip_polish_for_agent_result(agent_result):
            skip_polish = True
        if answer_metadata.get("evidence_insufficient") is True or answer_metadata.get("answer_policy") == "insufficient_evidence":
            skip_polish = True
        if not skip_polish:
            agent_result["answer"] = await _polish_customer_answer(db, question, agent_result)
        final_compound_queries = [
            str(item or "").strip()
            for item in ((phase1_plan.get("semantic_preplan") or {}).get("qa_evidence_queries") or [])
            if str(item or "").strip()
        ]
        coverage_contract = customer_answer_coverage_contract.AnswerCoverageContract.from_dict(
            answer_metadata.get("answer_coverage_contract")
        )
        if coverage_contract is not None:
            agent_result = _apply_answer_coverage_contract(agent_result)
        elif len(final_compound_queries) > 1:
            answer_text = str(agent_result.get("answer") or "").strip()
            uncovered_queries = _uncovered_compound_queries_for_output(
                answer_text,
                final_compound_queries,
                answer_metadata,
            )
            if uncovered_queries:
                agent_result["answer"] = "\n".join([
                    answer_text,
                    *(f"关于“{query}”：当前同 SKU 资料未直接确认，无法确认。" for query in uncovered_queries),
                ])
        agent_result = _shape_answer_for_output(agent_result)
        agent_result["skip_polish"] = skip_polish
        agent_result = customer_final_answer_arbiter.arbitrate_final_answer(agent_result)
        agent_result = _attach_agent_quality(agent_result, question)
        conversation = _get_or_create_conversation(db, user_id, question, agent_result.get("sku"), conversation_id)
        db.add(CustomerServiceMessage(
            conversation_id=conversation.id,
            role="user",
            content=question,
            sku=agent_result.get("sku"),
        ))
        assistant_turn_index = _assistant_turn_index(db, conversation.id)
        inherited_recommendation_context = _latest_recommendation_context_for_sources(
            db,
            conversation.id,
        )
        inherited_candidate_context = _latest_candidate_context_for_sources(
            db,
            conversation.id,
        )
        sources_with_context = _sources_with_result_context(
            agent_result,
            turn_index=assistant_turn_index,
            user_question=question,
            inherited_recommendation_context=inherited_recommendation_context,
            inherited_candidate_context=inherited_candidate_context,
        )
        assistant_message = CustomerServiceMessage(
            conversation_id=conversation.id,
            role="assistant",
            content=agent_result["answer"],
            sku=agent_result.get("sku"),
            sources_json=json.dumps(sources_with_context, ensure_ascii=False, default=str),
        )
        db.add(assistant_message)
        _touch_conversation(conversation, agent_result.get("sku"))
        db.flush()
        conversation_id_value = conversation.id
        message_id_value = assistant_message.id
        db.commit()
        customer_perf_service.log_stage("save_messages_and_commit", stage_start, branch="agent", skip_polish=bool(agent_result.get("skip_polish")))
        customer_perf_service.log_stage("ask_customer_service.total", request_start, branch="agent", intent=agent_result.get("intent"), agent_mode=(agent_result.get("debug") or {}).get("agent_mode"))
        customer_perf_service.summarize_request(final_answer=agent_result.get("answer"), intent=agent_result.get("intent"), agent_mode=(agent_result.get("debug") or {}).get("agent_mode"))
        release_session_connection(db)
        public_intent = _public_intent_name(agent_result.get("intent"), agent_result.get("answer_type"))
        return {
            "conversation_id": conversation_id_value,
            "message_id": message_id_value,
            "intent": public_intent,
            "answer_type": agent_result.get("answer_type"),
            "confidence": agent_result.get("confidence"),
            "uncertainty": agent_result.get("uncertainty"),
            "needs_clarification": agent_result.get("needs_clarification", False),
            "anomalies": agent_result.get("anomalies") or [],
            "suggested_followups": agent_result.get("suggested_followups") or [],
            "followups": agent_result.get("followups") or agent_result.get("suggested_followups") or [],
            "warnings": agent_result.get("warnings") or [],
            "evidence": agent_result.get("evidence") or [],
            "agent_quality": agent_result.get("agent_quality") or {},
            "answer_metadata": agent_result.get("answer_metadata") or {},
            "debug": agent_result.get("debug") or {},
            "sku": agent_result.get("sku"),
            "answer": agent_result["answer"],
            "sources": sources_with_context,
            "actions": agent_result.get("actions") or [],
            "results": agent_result.get("results") or [],
            "steps": agent_result.get("steps") or [],
            "result_skus": agent_result.get("result_skus") if "result_skus" in agent_result else [
                str(item.get("sku") or "").strip().upper()
                for item in (agent_result.get("results") or [])
                if isinstance(item, dict) and str(item.get("sku") or "").strip()
            ],
            "candidate_skus": agent_result.get("candidate_skus") if "candidate_skus" in agent_result else (
                agent_result.get("result_skus") if "result_skus" in agent_result else [
                str(item.get("sku") or "").strip().upper()
                for item in (agent_result.get("results") or [])
                if isinstance(item, dict) and str(item.get("sku") or "").strip()
                ]
            ),
            "agent_mode": (agent_result.get("debug") or {}).get("agent_mode"),
        }

    resolved_sku = _resolve_sku(db, question, sku)
    if not resolved_sku:
        customer_perf_service.log_stage("ask_customer_service.total", request_start, branch="guidance")
        customer_perf_service.summarize_request(final_answer="guidance", intent="clarify", agent_mode="guidance")
        return _save_and_return_guidance(db, user_id, question, conversation_id)

    knowledge_start = perf_counter()
    product = db.query(Product).filter(Product.sku == resolved_sku).first()
    if not product:
        return _save_and_return_guidance(
            db,
            user_id,
            question,
            conversation_id,
            unresolved_sku=resolved_sku,
        )

    context, sources = build_product_context(db, resolved_sku, question)
    release_session_connection(db)
    messages = [
        {
            "role": "system",
            "content": (
                "你是内部产品客服助手。只能依据提供的产品上下文回答。"
                "如果上下文没有答案，必须明确说需要人工确认。"
                "不要编造参数、认证、价格、库存或售后政策。"
                "回答请先给结论，再给依据，保持中文、简洁、客服口吻。"
            ),
        },
        {
            "role": "user",
            "content": f"产品上下文：\n{context}\n\n客服问题：{question}",
        },
    ]

    try:
        answer = await customer_llm_service.chat_completion(db, messages, purpose="knowledge_answer")
    except Exception as exc:
        answer = f"聊天模型暂时不可用：{exc}"
    customer_perf_service.log_stage("single_sku_knowledge_llm", knowledge_start, sku=resolved_sku)

    conversation = _get_or_create_conversation(db, user_id, question, resolved_sku, conversation_id)
    db.add(CustomerServiceMessage(
        conversation_id=conversation.id,
        role="user",
        content=question,
        sku=resolved_sku,
    ))
    sources_with_meta = list(sources)
    sources_with_meta.append({
        "type": "agent_meta",
        "label": "客服回复元数据",
        "intent": "knowledge_base_answer",
        "answer_type": "knowledge_answer",
        "confidence": "medium",
        "uncertainty": _uncertainty_from_answer(answer, [], [], False),
        "needs_clarification": False,
        "anomalies": [],
        "suggested_followups": [],
        "followups": [],
        "warnings": [],
        "evidence": [],
        "debug": {"intent": "knowledge_base_answer", "steps": [], "warnings": [], "anomalies": [], "raw_results": []},
        "feedback": None,
    })
    assistant_message = CustomerServiceMessage(
        conversation_id=conversation.id,
        role="assistant",
        content=answer,
        sku=resolved_sku,
        sources_json=json.dumps(sources_with_meta, ensure_ascii=False, default=str),
    )
    db.add(assistant_message)
    _touch_conversation(conversation, resolved_sku)
    conversation_id_value = conversation.id
    message_id_value = assistant_message.id
    db.commit()
    customer_perf_service.log_stage("save_messages_and_commit", knowledge_start, branch="knowledge")
    customer_perf_service.log_stage("ask_customer_service.total", request_start, branch="knowledge", sku=resolved_sku)
    customer_perf_service.summarize_request(final_answer=answer, intent="knowledge_base_answer", agent_mode="single_sku_knowledge")
    release_session_connection(db)

    return {
        "conversation_id": conversation_id_value,
        "message_id": message_id_value,
        "intent": "knowledge_base_answer",
        "answer_type": "knowledge_answer",
        "confidence": "medium",
        "uncertainty": _uncertainty_from_answer(answer, [], [], False),
        "needs_clarification": False,
        "anomalies": [],
        "suggested_followups": [],
        "followups": [],
        "warnings": [],
        "evidence": [],
        "debug": {"intent": "knowledge_base_answer", "steps": [], "warnings": [], "anomalies": [], "raw_results": []},
        "sku": resolved_sku,
        "answer": answer,
        "sources": sources,
        "actions": [],
        "results": [],
        "steps": [],
    }


def _finalize_answer(agent_result: dict) -> dict:
    result = _normalize_agent_result(agent_result)
    primary = _pick_primary_answer_source(result)
    sources = _tag_and_order_sources(result.get("sources") or [], primary)
    result["sources"] = sources
    result["answer"] = _sanitize_final_answer_text(str(result.get("answer") or ""), primary)
    answer_metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    answer_metadata["final_decision"] = {
        "primary_source": primary.get("type"),
        "priority": primary.get("priority"),
        "single_source_of_truth": True,
        "llm_allowed": _llm_allowed_for_final_answer(result, primary),
    }
    result["answer_metadata"] = answer_metadata
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    debug["final_answer"] = answer_metadata["final_decision"]
    result["debug"] = debug
    if not answer_metadata["final_decision"]["llm_allowed"]:
        result["skip_polish"] = True
    return result


def build_product_context(db: Session, sku: str, question: str) -> tuple[str, list[dict]]:
    try:
        detail = product_service.get_product_detail(db, sku)
    except HTTPException as exc:
        # A generated/legacy context reference is not customer-supplied
        # product identity.  Do not turn an otherwise ordinary conversation
        # into a transport-level 404; the calling route can continue without
        # this optional product context.
        if exc.status_code == status.HTTP_404_NOT_FOUND:
            return "", []
        raise
    sources: list[dict] = []
    lines = [
        f"SKU: {detail.get('sku')}",
        f"中文名: {detail.get('product_name_cn') or ''}",
        f"英文名: {detail.get('product_name_en') or ''}",
        f"品牌: {detail.get('brand') or ''}",
        f"系列: {detail.get('series') or ''}",
        f"类目: {detail.get('category') or ''} / {detail.get('sub_category') or ''}",
        f"等级: {detail.get('product_level') or ''}",
        f"生命周期: {detail.get('lifecycle_status') or ''}",
    ]
    sources.append({"type": "product", "label": "产品基础信息", "sku": sku})

    specs = detail.get("specs") or {}
    if specs:
        lines.append("规格信息:")
        for key, label in [
            ("capacity", "容量"),
            ("gross_weight_g", "毛重g"),
            ("body_material", "材质"),
            ("color", "颜色"),
            ("surface_finish", "表面工艺"),
            ("heat_source", "适用热源"),
            ("power", "功率"),
            ("technical_advantages", "技术优势"),
            ("usage_instruction", "使用说明"),
        ]:
            value = specs.get(key)
            if value not in (None, "", []):
                lines.append(f"- {label}: {_stringify(value)}")
        sources.append({"type": "product_specs", "label": "产品规格", "sku": sku})

    business = detail.get("business") or {}
    if business:
        lines.append("业务信息:")
        for key, label in [
            ("top_selling_points", "核心卖点"),
            ("target_audience", "目标人群"),
            ("positioning", "定位"),
            ("price_positioning", "价格定位"),
            ("emotional_value", "情绪价值"),
            ("usage_scenarios", "使用场景"),
            ("competitor_benchmark", "竞品信息"),
        ]:
            value = business.get(key)
            if value not in (None, "", []):
                lines.append(f"- {label}: {_stringify(value)}")
        sources.append({"type": "product_business", "label": "产品业务信息", "sku": sku})

    qa_items = product_service.customer_visible_product_qas(db, detail["id"])
    if qa_items:
        lines.append("产品 QA:")
        for item in qa_items[:20]:
            lines.append(f"- Q: {item.question}\n  A: {item.answer}")
        sources.append({"type": "product_qa", "label": "产品 QA", "sku": sku, "count": len(qa_items)})

    negative = db.query(ProductQaNegative).filter(ProductQaNegative.product_id == detail["id"]).first()
    if negative:
        lines.append("差评/负面问题应答:")
        if negative.high_freq_negative_words:
            lines.append(f"- 高频负面词: {negative.high_freq_negative_words}")
        if negative.response_tone:
            lines.append(f"- 应答口径: {negative.response_tone}")
        sources.append({"type": "product_qa_negative", "label": "差评应答", "sku": sku})

    knowledge = knowledge_service.keyword_retrieve(db, question, sku=sku, limit=5)
    if knowledge:
        lines.append("知识库补充:")
        for item in knowledge:
            lines.append(f"- {item['content']}")
        sources.append({"type": "knowledge_base", "label": "向量/知识库补充", "sku": sku, "count": len(knowledge)})

    return "\n".join(lines), sources


def review_samples(db: Session, user_id: str, limit: int = 100) -> dict:
    user_id = str(user_id)
    messages = (
        db.query(CustomerServiceMessage, CustomerServiceConversation)
        .join(CustomerServiceConversation, CustomerServiceConversation.id == CustomerServiceMessage.conversation_id)
        .filter(
            CustomerServiceConversation.user_id == user_id,
            CustomerServiceMessage.role == "assistant",
        )
        .order_by(CustomerServiceMessage.created_at.desc())
        .limit(limit)
        .all()
    )
    items = []
    frequent_questions: dict[str, int] = {}
    clarification_samples = 0
    anomaly_samples = 0
    for assistant_message, conversation in messages:
        meta = _message_meta(assistant_message.sources_json)
        user_message = (
            db.query(CustomerServiceMessage)
            .filter(
                CustomerServiceMessage.conversation_id == conversation.id,
                CustomerServiceMessage.role == "user",
                CustomerServiceMessage.created_at <= assistant_message.created_at,
            )
            .order_by(CustomerServiceMessage.created_at.desc())
            .first()
        )
        question = (user_message.content if user_message else "").strip()
        if question:
            frequent_questions[question] = frequent_questions.get(question, 0) + 1
        if meta.get("needs_clarification"):
            clarification_samples += 1
        if meta.get("anomalies"):
            anomaly_samples += 1
        items.append({
            "conversation_id": conversation.id,
            "message_id": assistant_message.id,
            "question": question,
            "answer": assistant_message.content,
            "intent": meta.get("intent"),
            "confidence": meta.get("confidence"),
            "agent_quality": meta.get("agent_quality", {}),
            "needs_clarification": meta.get("needs_clarification", False),
            "anomalies": meta.get("anomalies", []),
            "warnings": meta.get("warnings", []),
            "suggested_followups": meta.get("suggested_followups", []),
            "created_at": str(assistant_message.created_at),
        })

    top_questions = sorted(frequent_questions.items(), key=lambda item: item[1], reverse=True)[:20]
    quality_summary = _review_quality_summary(items)
    return {
        "items": items,
        "summary": {
            "total_samples": len(items),
            "clarification_samples": clarification_samples,
            "anomaly_samples": anomaly_samples,
            "quality": quality_summary,
            "top_questions": [{"question": question, "count": count} for question, count in top_questions],
        },
    }


def _resolve_sku(db: Session, question: str, sku: str | None) -> str | None:
    if sku:
        resolved = _resolve_exact_sku_variant(db, sku)
        return resolved or sku.strip()
    candidates = customer_agent_service._extract_skus(question)
    for candidate in candidates:
        resolved = _resolve_exact_sku_variant(db, candidate)
        if resolved:
            return resolved
    return None


def _sku_lookup_variants(value: str | None) -> list[str]:
    raw = str(value or "").strip()
    if not raw:
        return []
    variants: list[str] = []
    compact_raw = raw.upper().replace("_", "-")
    parenthetical_variants: list[str] = []
    paren_match = re.match(r"^(?P<prefix>[A-Z0-9]+(?:-[A-Z0-9]+)+)[-](?P<suffix>[A-Z0-9]{2,8})$", compact_raw)
    if paren_match:
        parenthetical_variants.extend(
            [
                f"{paren_match.group('prefix')}({paren_match.group('suffix')})",
                f"{paren_match.group('prefix')}（{paren_match.group('suffix')}）",
            ]
        )
    suffix_match = re.match(r"^(?P<prefix>[A-Z0-9]+(?:-[A-Z0-9]+)+)[(（](?P<suffix>[A-Z0-9]{2,8})[)）]$", compact_raw)
    if suffix_match:
        parenthetical_variants.append(f"{suffix_match.group('prefix')}-{suffix_match.group('suffix')}")
    candidates = [
        raw,
        compact_raw,
        raw.replace("(", "（").replace(")", "）"),
        raw.replace("（", "(").replace("）", ")"),
        customer_agent_service.normalize_search_text(raw).upper().replace("_", "-"),
        *parenthetical_variants,
    ]
    for candidate in candidates:
        normalized = str(candidate or "").strip()
        if normalized and normalized not in variants:
            variants.append(normalized)
    return variants


def _resolve_exact_sku_variant(db: Session, sku: str | None) -> str | None:
    variants = _sku_lookup_variants(sku)
    if not variants:
        return None
    for candidate in variants:
        product = db.query(Product).filter(Product.sku == candidate).first()
        if product:
            return product.sku
    normalized_targets = {
        customer_agent_service.normalize_search_text(candidate).upper().replace("_", "-")
        for candidate in variants
    }
    for product in db.query(Product).all():
        product_sku = str(getattr(product, "sku", "") or "").strip()
        if not product_sku:
            continue
        normalized_product = customer_agent_service.normalize_search_text(product_sku).upper().replace("_", "-")
        if normalized_product in normalized_targets:
            return product_sku
    for candidate in variants:
        product = db.query(Product).filter(Product.sku.ilike(candidate)).first()
        if product:
            return product.sku
    return None


def _try_product_sku_identity_shortcut(db: Session, question: str) -> dict | None:
    subject = _sku_identity_subject(question)
    if not subject:
        return None

    normalized_subject = customer_agent_service.normalize_search_text(subject).lower()
    if not normalized_subject:
        return None

    exact_matches: list[Product] = []
    family_matches: list[Product] = []
    for product in db.query(Product).all():
        names = (product.product_name_cn, product.product_name_en)
        normalized_names = [
            customer_agent_service.normalize_search_text(name or "").lower()
            for name in names
            if str(name or "").strip()
        ]
        if normalized_subject in normalized_names:
            exact_matches.append(product)
        elif any(normalized_subject in name for name in normalized_names):
            family_matches.append(product)

    candidates = exact_matches or family_matches
    if not candidates:
        return None
    candidates.sort(key=lambda item: (len(str(item.product_name_cn or item.product_name_en or "")), item.sku))

    rows = [{
        "sku": str(product.sku or "").strip().upper(),
        "product_name_cn": product.product_name_cn,
        "product_name_en": product.product_name_en,
        "category": product.category,
    } for product in candidates]
    if len(rows) == 1:
        row = rows[0]
        product_name = row.get("product_name_cn") or row.get("product_name_en") or subject
        answer = f"{product_name}对应的 SKU 是 {row['sku']}。"
        sku = row["sku"]
    else:
        options = "\n".join(
            f"- {row.get('product_name_cn') or row.get('product_name_en') or '未命名产品'}：{row['sku']}"
            for row in rows
        )
        answer = f"“{subject}”可能对应以下产品：\n{options}\n请确认你指的是哪一款。"
        sku = None

    result_skus = [row["sku"] for row in rows]
    steps = [{
        "type": "product_sku_identity_shortcut",
        "label": "产品 SKU 身份查询",
        "detail": "、".join(result_skus),
        "ok": True,
    }]
    return {
        "answer": answer,
        "intent": "product_detail",
        "answer_type": "product_detail",
        "confidence": "high" if len(rows) == 1 else "medium",
        "uncertainty": "resolved" if len(rows) == 1 else "ambiguous_product",
        "needs_clarification": len(rows) > 1,
        "anomalies": [],
        "suggested_followups": [] if len(rows) == 1 else ["请提供完整产品名或直接选择上面的 SKU。"],
        "followups": [],
        "evidence": [],
        "sources": [{"type": "product", "label": "产品 SKU", "sku": row["sku"]} for row in rows],
        "actions": [],
        "results": rows,
        "steps": steps,
        "warnings": [],
        "sku": sku,
        "result_skus": result_skus,
        "debug": {
            "agent_mode": "product_sku_identity_shortcut",
            "intent": "product_detail",
            "steps": steps,
            "warnings": [],
            "anomalies": [],
            "raw_results": rows,
        },
        "skip_polish": True,
    }


def _should_clarify_vague_single_product_price(
    question: str,
    *,
    explicit_sku: str | None,
    named_products: list[Product],
    recommendation_context: dict | None,
    candidate_context: dict | None,
    active_product_skus: list[str],
) -> bool:
    text = str(question or "").strip()
    if (
        not text
        or explicit_sku
        or named_products
        or recommendation_context
        or candidate_context
        or active_product_skus
    ):
        return False
    vague_references = ("刚才那个", "上面那个", "那个", "这个", "它")
    price_terms = ("多少钱", "价格", "售价", "什么价", "几块", "几元")
    return any(term in text for term in vague_references) and any(term in text for term in price_terms)


def _unbound_deictic_product_reference_result(
    db: Session,
    question: str,
    *,
    field_type: str | None = None,
) -> dict | None:
    """Require identity context for a leading singular product reference."""
    text = str(question or "").strip()
    if not re.match(
        r"^(?:那|这)?(?:它|这个|这款|该商品|该产品|这个商品|这件商品|本产品)",
        text,
    ):
        return None
    entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        text,
        db.query(Product).order_by(Product.sku.asc()).all(),
        field_type_override=str(field_type or "").strip() or None,
    )
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": "请先说明你指的是哪款商品，或提供 SKU；确认商品后我再查询对应信息。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "answer_metadata": {
            "source": "unbound_deictic_entity_contract_guard",
            "evidence_status": "missing_entity_context",
        },
        "debug": {
            "agent_mode": "unbound_deictic_entity_contract_guard",
            "entity_resolution_contract": entity_contract.to_dict(),
        },
        "skip_polish": True,
    }


def _vague_single_product_price_clarification_result() -> dict:
    answer = "你说的这款产品我还不能确定是哪一款。请发一下产品名或 SKU，我再帮你查价格。"
    steps = [{
        "type": "clarify",
        "label": "需要明确产品范围",
        "detail": "模糊单数指代缺少可用产品上下文",
        "ok": True,
    }]
    return {
        "answer": answer,
        "intent": "clarify",
        "answer_type": "clarification",
        "confidence": "low",
        "uncertainty": "ambiguous_product",
        "needs_clarification": True,
        "anomalies": [],
        "suggested_followups": ["请提供产品名或 SKU。"],
        "followups": ["请提供产品名或 SKU。"],
        "evidence": [],
        "sources": [{"type": "agent_clarification", "label": "需要明确产品范围"}],
        "actions": [],
        "results": [],
        "steps": steps,
        "warnings": [],
        "sku": None,
        "result_skus": [],
        "debug": {
            "agent_mode": "vague_single_product_price_clarification",
            "intent": "clarify",
            "steps": steps,
            "warnings": [],
            "anomalies": [],
            "raw_results": [],
        },
        "skip_polish": True,
    }


def _pending_clarification_context_for_result(agent_result: dict, user_question: str | None = None) -> dict | None:
    if agent_result.get("answer_type") != "clarification" or not agent_result.get("needs_clarification"):
        return None
    text = str(user_question or "").strip()
    if not text:
        return None
    normalized = customer_agent_service.normalize_search_text(text)
    product_scope = customer_dialogue_state.product_scope_from_text(text)
    price_terms = ("多少钱", "价格", "售价", "什么价", "几块", "几元")
    if any(term in text for term in price_terms):
        return {
            "intent": "product_detail",
            "requested_field": "price",
            "product_scope": product_scope,
            "original_question": text,
        }
    if "酒精炉" in text and any(term in text for term in ("能不能", "能否", "是否", "可不可以", "支持", "适用")):
        return {
            "intent": "product_detail",
            "requested_field": "heat_source",
            "capability_question": "能不能用酒精炉",
            "product_scope": product_scope,
            "original_question": text,
        }
    if normalized in {"这个锅", "那个锅", "这个水壶", "那个水壶"}:
        return {
            "intent": "product_detail",
            "requested_field": "",
            "product_scope": product_scope,
            "original_question": text,
        }
    return {
        "intent": "product_context_required",
        "requested_field": "",
        "product_scope": product_scope,
        "original_question": text,
    }


def _sku_identity_subject(question: str) -> str:
    text = str(question or "").strip()
    if not text:
        return ""
    patterns = (
        r"(?:是|的)?\s*哪(?:一)?个\s*SKU\b",
        r"\bSKU\s*编号\s*(?:是)?什么",
        r"\bSKU\s*(?:是)?什么",
        r"对应\s*SKU\b",
    )
    match = next((match for pattern in patterns if (match := re.search(pattern, text, flags=re.I))), None)
    if not match:
        return ""
    subject = text[:match.start()].strip("。！？!? ，,；;：: ")
    subject = re.sub(r"^(?:请问|麻烦|帮我查(?:一下)?|查一下)\s*", "", subject).strip()
    return subject


def _bound_gifting_qa_answer_to_evidence(question: str, answer: str) -> str:
    value = str(answer or "").strip()
    question_text = str(question or "")
    unsupported_markers = (
        "包装精美",
        "品质出众",
        "绝佳礼物",
        "非常适合",
        "送给户外露营爱好者",
    )
    if not any(marker in value for marker in unsupported_markers):
        return value
    question_requests_marketing_judgement = any(
        term in question_text
        for term in ("送人", "送礼", "礼物", "赠送", "品质", "质量", "做工", "包装")
    )
    answer_makes_gifting_claim = any(term in value for term in ("礼物", "送给"))
    if not question_requests_marketing_judgement and not answer_makes_gifting_claim:
        return value
    requested_dimensions: list[str] = []
    if any(term in question_text for term in ("品质", "质量", "做工")):
        requested_dimensions.append("品质")
    if "包装" in question_text:
        requested_dimensions.append("包装")
    if any(term in question_text for term in ("送人", "送礼", "礼物", "赠送")):
        requested_dimensions.append("礼赠适配")
    if not requested_dimensions:
        if "品质出众" in value:
            requested_dimensions.append("品质")
        if "包装精美" in value:
            requested_dimensions.append("包装")
        if answer_makes_gifting_claim:
            requested_dimensions.append("礼赠适配")
    dimension_text = "、".join(dict.fromkeys(requested_dimensions)) or "品质、包装或礼赠适配"
    return f"当前同 SKU 资料不足以直接支持上述{dimension_text}判断，暂时无法确认。"


def _sealed_product_qa_safe_missing_result(
    product: Product,
    answer: str,
    *,
    field_contract: dict[str, Any],
    debug: dict[str, Any] | None = None,
) -> dict[str, Any]:
    sku = str(product.sku or "").strip().upper()
    result_debug = {
        "agent_mode": "sealed_product_qa_safe_missing",
        "field_contract": dict(field_contract),
    }
    if isinstance(debug, dict):
        result_debug.update(debug)
    return {
        "answer": str(answer or "").strip(),
        "intent": "product_detail",
        "answer_type": "product_detail",
        "confidence": "medium",
        "uncertainty": "missing_data",
        "needs_clarification": False,
        "results": [{
            "sku": sku,
            "product_name_cn": product.product_name_cn,
            "product_name_en": product.product_name_en,
            "category": product.category,
        }],
        "result_skus": [sku],
        "candidate_skus": [sku],
        "sku": sku,
        "evidence": [],
        "sources": [],
        "answer_metadata": {
            "contract_field_type": "product_qa",
            "requested_field": "产品问答",
            "evidence_status": "missing",
            "field_evidence_missing": True,
            "evidence_sku": sku,
            "evidence_skus": [],
            "answer_policy": "insufficient_evidence",
        },
        "debug": result_debug,
        "skip_polish": True,
    }


def _try_product_qa_shortcut(
    db: Session,
    question: str,
    *,
    phase1_plan: dict[str, Any] | None = None,
    selected_qa: ProductQa | None = None,
) -> dict | None:
    # Exact stored QA is a database fact, not a lexical shortcut.  It still
    # must pass entity arbitration and same-SKU evidence below, but it must
    # not be discarded merely because its natural wording resembles an
    # unknown detail or a contents question.
    exact_qa_records = (
        db.query(ProductQa)
        .filter(
            ProductQa.question == str(question or "").strip(),
            ProductQa.integrity_status == "approved",
        )
        .all()
    )
    exact_qa_product_ids = {record.product_id for record in exact_qa_records if record.product_id}
    exact_qa_record = exact_qa_records[0] if len(exact_qa_product_ids) == 1 else None
    exact_qa_product = (
        db.query(Product).filter(Product.id == exact_qa_record.product_id).first()
        if exact_qa_record else None
    )
    has_exact_stored_qa = bool(exact_qa_product)
    named_products = _products_named_in_question(db, question)
    if exact_qa_product:
        # A unique database QA identity is stronger than fuzzy alias expansion.
        named_products = [exact_qa_product]
    if len(named_products) >= 2:
        return None
    if customer_agent_intent_service._is_compare_question(question):
        return None
    if any(term in str(question or "") for term in ("哪个", "哪款")):
        normalized_question = customer_agent_service.normalize_search_text(question)
        explicit_canonical_skus = {
            str(product.sku or "").strip().upper()
            for product in db.query(Product).all()
            if str(product.sku or "").strip()
            and any(
                normalized_name and normalized_name in normalized_question
                for normalized_name in (
                    customer_agent_service.normalize_search_text(product.product_name_cn or ""),
                    customer_agent_service.normalize_search_text(product.product_name_en or ""),
                )
            )
        }
        if len(explicit_canonical_skus) >= 2:
            return None
    if customer_agent_intent_service._looks_like_multi_product_relation_question(question):
        return None
    if (
        customer_agent_intent_service._looks_like_usage_care_question(question)
        and not _semantic_prefers_sealed_product_qa(phase1_plan or {})
        and not has_exact_stored_qa
    ):
        return None
    if customer_agent_intent_service._looks_like_contents_grounding_question(question) and not has_exact_stored_qa:
        return None
    if (
        _is_generic_named_product_question(question)
        and any(term in str(question or "") for term in ("再看", "改看", "看看", "看一下"))
        and not customer_field_contract.detect_field_contract(question)
    ):
        return None
    # A selected QA record may come from the semantic supplemental-intent
    # contract after a separate realtime/commercial field has already been
    # safely handled. Its same-SKU identity and evidence were sealed before
    # this formatter, so the broad unknown-field guard must not erase that
    # independent supported answer.
    if _detect_resolved_entity_unknown_fact_label(question) and not has_exact_stored_qa and selected_qa is None:
        return None
    product = _explicit_product_from_question(db, question)
    if not product:
        return None
    fallback_field_contract = customer_field_contract.detect_field_contract(question)
    subject_selection = customer_field_contract.select_entity_subject_for_routing(
        raw_question=question,
        fallback_product_like_subject=_product_like_scope_subject(question),
        fallback_named_subject=customer_agent_intent_service._detail_subject_from_question(question).strip() or question,
    )
    entity_subject = subject_selection.entity_subject
    # An exact stored QA identifies its product record before natural-language
    # subject extraction. Use that product's canonical name as the resolver
    # subject whenever it is literally present, so trailing predicates such
    # as “正常能用多久” cannot turn a colour/variant name into ambiguity.
    if product:
        normalized_question = customer_agent_service.normalize_search_text(question)
        canonical_names = [
            str(product.product_name_cn or "").strip(),
            str(product.product_name_en or "").strip(),
        ]
        canonical_subject = next(
            (
                name
                for name in canonical_names
                if name and customer_agent_service.normalize_search_text(name) in normalized_question
            ),
            "",
        )
        if canonical_subject:
            entity_subject = canonical_subject
    # A high-confidence semantic QA decision with no formal field is sealed
    # before this shortcut reaches deterministic entity/evidence validation.
    # Do not derive a different field again from text inside the product name.
    sealed_product_qa = _semantic_prefers_sealed_product_qa(phase1_plan or {})
    semantic_field_request = (
        {}
        if sealed_product_qa
        else customer_field_contract.resolve_requested_field_contract(
            question,
            phase1_plan,
            subject=entity_subject,
            subject_is_catalog_exact=bool(entity_subject),
        )
    )
    semantic_preplan = phase1_plan.get("semantic_preplan") if isinstance(phase1_plan, dict) else {}
    semantic_field_type = str((semantic_preplan or {}).get("field_type") or "").strip()
    semantic_field_allowed = bool(
        _semantic_preplan_confident(semantic_preplan, minimum=0.9)
        and str((semantic_preplan or {}).get("route_family") or "").strip() == "product_bound_qa"
        and str((semantic_preplan or {}).get("route_hint") or "").strip() == "product_detail"
        and semantic_field_type in customer_field_contract.FORMAL_DETAIL_FIELDS
    )
    field_type = None if sealed_product_qa else semantic_field_type if semantic_field_allowed else str(semantic_field_request.get("field_type") or "").strip() or (
        fallback_field_contract.field_type if fallback_field_contract else None
    )
    all_products = db.query(Product).order_by(Product.sku.asc()).all()
    entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        all_products,
        resolver_candidates=named_products or [product],
        entity_text_override=entity_subject or None,
        field_type_override=field_type,
    )
    identity_decision = customer_entity_resolution_contract.can_resolve_single_product(
        entity_contract,
        all_products,
    )
    product_sku = str(product.sku or "").strip().upper()
    if not identity_decision.allowed or identity_decision.resolved_sku != product_sku:
        if field_type:
            displayable_candidate_skus = _displayable_phase2_clarification_candidate_skus(entity_contract)
            if displayable_candidate_skus:
                return _build_phase2_entity_state_response(
                    db,
                    question,
                    {
                        "action": "ambiguous_clarification",
                        "contract": entity_contract,
                        "products": all_products,
                        "displayable_candidate_skus": displayable_candidate_skus,
                    },
                )
            if entity_contract.status in {"ambiguous", "unresolved"}:
                return _build_phase2_entity_state_response(
                    db,
                    question,
                    {"action": "unresolved_clarification", "contract": entity_contract},
                )
        return None
    if selected_qa is not None and str(selected_qa.product_id or "") != str(product.id):
        # A semantic selector receives only same-SKU candidates, but keep the
        # identity boundary local to the evidence formatter as well.
        return None
    if field_type:
        qa = selected_qa or _best_product_qa_match(
            db,
            product,
            question,
            requested_field_type=field_type,
        )
        if not qa:
            return None
    elif not sealed_product_qa and _looks_like_product_detail_field_question(question):
        return None
    else:
        semantic_qa_query = (
            str((semantic_preplan or {}).get("qa_evidence_query") or "").strip()
            if sealed_product_qa and isinstance(semantic_preplan, dict)
            else ""
        )
        qa = selected_qa or _best_product_qa_match(
            db,
            product,
            question,
            semantic_query=semantic_qa_query or None,
        )
    if not qa:
        return None
    exact_qa_match = _is_exact_product_qa_question_match(qa, question)
    if not exact_qa_match and selected_qa is None:
        # Lexical overlap is retrieval evidence, not authorization to publish
        # an old answer. A non-identical question must pass the semantic
        # full-coverage selection contract below.
        return None
    # An exact QA can prove a product fact that is outside the formal field
    # ontology.  A semantic field label is not allowed to relabel that fact
    # when its own evidence policy rejects the matched QA record (for example
    # durability is not lifecycle status).  Keep formal-field QA untouched.
    if (
        field_type
        and exact_qa_match
        and not customer_field_contract.qa_evidence_matches_field(
            str(qa.question or ""),
            str(getattr(qa, "tags", "") or ""),
            field_type,
        )
    ):
        field_type = None
        entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
            question,
            all_products,
            resolver_candidates=named_products or [product],
            entity_text_override=entity_subject or None,
            field_type_override=None,
        )
    if (
        _has_direct_structured_detail_answer(db, product, question)
        and not exact_qa_match
        and selected_qa is None
    ):
        return None
    answer = str(qa.answer or "").strip()
    if not answer:
        return None
    sku = product_sku
    bounded_gifting_answer = _bound_gifting_qa_answer_to_evidence(question, answer)
    if bounded_gifting_answer != answer:
        field_contract = {
            "field_type": "product_qa",
            "requested_field": "产品问答",
            "requested_fields": ["产品问答"],
            "canonical_fields": ["product_qa"],
            "supported_fields": ["product_qa"],
            "unsupported_fields": [],
            "subject": entity_subject,
            "requested_scope": "subject",
            "source": "gifting_evidence_boundary",
            "confidence": 1.0,
            "compound": False,
        }
        return _sealed_product_qa_safe_missing_result(
            product,
            bounded_gifting_answer,
            field_contract=field_contract,
            debug={"gifting_evidence_boundary": True},
        )
    qa_bundle = customer_evidence_bundle.build_customer_evidence_bundle(
        sku=sku,
        product_name=product.product_name_cn or product.product_name_en or sku,
        evidence_items=[{
            "evidence_id": f"product_qa:{qa.id}",
            "sku": sku,
            "source_type": "product_qa",
            "source": "product_qa",
            "field": field_type or "product_qa",
            "value": answer,
            "visibility": customer_evidence_bundle.CUSTOMER_VISIBLE,
        }],
    )
    if len(qa_bundle.items) != 1:
        return None
    qa_evidence = qa_bundle.items[0]
    steps = [{"type": "product_qa_shortcut", "label": "产品 QA 快速命中", "detail": sku, "ok": True}]
    qa_field_contract = {
        "field_type": field_type,
        "requested_field": customer_field_contract.product_detail_field_label(field_type),
        "requested_fields": [customer_field_contract.product_detail_field_label(field_type)],
        "canonical_fields": [field_type],
        "supported_fields": [field_type],
        "unsupported_fields": [],
        "subject": entity_subject,
        "requested_scope": "subject",
        "source": "deterministic_field_contract",
        "confidence": 1.0,
        "compound": False,
    } if field_type else {
        # This generic contract is intentionally created only after both an
        # exact entity contract and a same-SKU QA record have been sealed.
        # It has no aliases and cannot become a keyword route.
        "field_type": "product_qa",
        "requested_field": "产品问答",
        "requested_fields": ["产品问答"],
        "canonical_fields": ["product_qa"],
        "supported_fields": ["product_qa"],
        "unsupported_fields": [],
        "subject": entity_subject,
        "requested_scope": "subject",
        "source": "sealed_same_sku_product_qa",
        "confidence": 1.0,
        "compound": False,
    }
    result = {
        "answer": answer,
        "intent": "product_detail",
        "answer_type": "product_detail",
        "confidence": "high",
        "uncertainty": "resolved",
        "needs_clarification": False,
        "anomalies": [],
        "suggested_followups": [],
        "followups": [],
        "evidence": [{
            "evidence_id": qa_evidence.evidence_id,
            "sku": sku,
            "product_name": product.product_name_cn or product.product_name_en or "",
            "field_label": "产品 QA",
            "value": qa_evidence.value,
            "source_layer": "QA",
            "matched_by": "product_qa",
            "source_type": "product_qa",
            "source_label": "产品 QA",
            "evidence_text": answer,
        }],
        "sources": [{"type": "product_qa", "label": "产品 QA", "sku": sku, "qa_id": qa.id}],
        "actions": [],
        "results": [{
            "sku": sku,
            "product_name_cn": product.product_name_cn,
            "product_name_en": product.product_name_en,
            "category": product.category,
        }],
        "steps": steps,
        "warnings": [],
        "sku": sku,
        "result_skus": [sku],
        "answer_metadata": {
            "qa_match_type": "exact" if exact_qa_match else "semantic",
            "contract_field_type": qa_field_contract["field_type"],
            "requested_field": qa_field_contract["requested_field"],
            "evidence_status": "matched",
            "evidence_field": "product_qa",
            "evidence_source": f"product_qa:{qa.id}",
            "evidence_value": answer,
            "evidence_sku": sku,
            "evidence_skus": [sku],
            "evidence_bundle_skus": [qa_bundle.sku],
            "evidence_ids": [qa_evidence.evidence_id],
            "field_evidence_match": True,
            "field_evidence_missing": False,
        },
        "debug": {
            "agent_mode": "product_qa_fast_path",
            "field_contract": qa_field_contract,
            "entity_resolution_contract": entity_contract.to_dict(),
            "intent": "product_detail",
            "steps": steps,
            "warnings": [],
            "anomalies": [],
            "raw_results": [],
        },
        "skip_polish": False,
    }
    return result


async def _try_product_qa_shortcut_with_semantic_selection(
    db: Session,
    question: str,
    *,
    phase1_plan: dict[str, Any] | None = None,
) -> dict | None:
    """Resolve sealed QA with a bounded same-SKU candidate selector.

    The preplan decides that a customer asks product QA; this helper never
    lets the model select a product, a database value, or an answer.  It is
    only used when normal same-SKU retrieval cannot align a natural paraphrase
    to a stored QA question.  The selected QA id is revalidated by the normal
    entity/evidence contract before its stored answer is returned.
    """
    direct = _try_product_qa_shortcut(db, question, phase1_plan=phase1_plan)
    # A sealed safe-missing has identity but no validated QA evidence. It is a
    # fail-closed fallback, not a positive QA match, so it must not prevent
    # the semantic selector from checking the same SKU. This matters for a
    # compound turn after semantic splitting: each narrow child can have a
    # valid QA match even though the original combined turn does not.
    direct_safe_missing = _is_sealed_product_qa_safe_missing(direct)
    semantic_preplan = (phase1_plan or {}).get("semantic_preplan") if isinstance(phase1_plan, dict) else {}
    strict_compound_child_qa = bool((semantic_preplan or {}).get("strict_compound_child_qa"))
    if (
        (direct is not None and not direct_safe_missing and not strict_compound_child_qa)
        or not _semantic_prefers_sealed_product_qa(phase1_plan or {})
    ):
        return direct

    subject_text = str((semantic_preplan or {}).get("subject_text") or "").strip()
    product = _explicit_product_from_question(db, question)
    named_products = _products_named_in_question(db, question)
    if not product or (named_products and len(named_products) != 1):
        return direct
    if named_products and str(named_products[0].id) != str(product.id):
        return direct
    normalized_question = customer_agent_service.normalize_search_text(question)
    canonical_names = (
        customer_agent_service.normalize_search_text(product.product_name_cn or ""),
        customer_agent_service.normalize_search_text(product.product_name_en or ""),
    )
    explicit_sku_tokens = {
        str(item or "").strip().upper()
        for item in customer_agent_service._extract_skus(question)
        if str(item or "").strip()
    }
    if not any(name and name in normalized_question for name in canonical_names) and str(product.sku or "").strip().upper() not in explicit_sku_tokens:
        # An unresolved shorthand is not permitted to ask the selector to bind
        # an identity. The normal entity clarification path owns it instead.
        return direct

    selected = await _select_same_sku_product_qa_with_semantic_selection(
        db,
        product,
        question,
        semantic_query=str((semantic_preplan or {}).get("qa_evidence_query") or "").strip(),
        subject_text=subject_text,
        strict_entailment=strict_compound_child_qa,
    )
    if selected is None:
        # A compound child question is already an independently scoped
        # semantic intent. Do not let a broad lexical QA match answer a
        # different sibling property; the caller renders this child safely
        # missing when no same-SKU selector can cover it.
        return direct if not strict_compound_child_qa or direct_safe_missing else None

    # The selector has already chosen a high-confidence QA id from this sealed
    # SKU. Pass that immutable record into the normal contract/evidence
    # formatter; do not make legacy lexical retrieval choose it a second time.
    result = _try_product_qa_shortcut(
        db,
        question,
        phase1_plan=phase1_plan,
        selected_qa=selected,
    )
    if result is not None:
        debug = result.setdefault("debug", {})
        debug["qa_evidence_selector"] = {
            "selected_qa_id": str(selected.id),
            "confidence": "high",
            "selection_scope": "same_sku_only",
        }
    return result


async def _select_same_sku_product_qa_with_semantic_selection(
    db: Session,
    product: Product,
    question: str,
    *,
    semantic_query: str = "",
    subject_text: str = "",
    comparison_criterion: str = "",
    strict_entailment: bool = False,
) -> ProductQa | None:
    """Let the semantic model choose one QA record from an already sealed SKU."""
    qas = product_service.customer_visible_product_qas(db, product.id)[:20]
    candidates = [
        {
            "id": str(qa.id),
            "question": str(qa.question or "").strip(),
            "tags": str(getattr(qa, "tags", "") or "").strip(),
        }
        for qa in qas
        if str(qa.id or "").strip() and str(qa.question or "").strip() and str(qa.answer or "").strip()
    ]
    if not candidates:
        return None
    target_question = str(semantic_query or question).strip()
    payload = {
        "customer_question": target_question,
        "original_customer_question": question,
        "semantic_query": semantic_query,
        "subject_text": subject_text,
        "comparison_criterion": comparison_criterion,
        "sealed_product_name": str(product.product_name_cn or product.product_name_en or "").strip(),
        "qa_candidates": candidates,
    }
    selection_instruction = (
        "The customer is comparing already sealed products. For this one sealed participant, select a candidate only when its QUESTION "
        "directly supplies evidence relevant to comparison_criterion or a concrete enabling capability materially related to that criterion. "
        "The QA does not need to answer which participant is better, and related evidence alone never proves the participant wins. "
        "Do not infer a comparison result or a product identity. "
        if comparison_criterion else
                        "Select at most one candidate whose QUESTION directly answers the customer's current question. "
    )
    semantic_runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        raw = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "Return only JSON: {qa_id:string|null,coverage:full|partial|none,confidence:high|medium|low,reasoning_summary:string}. "
                        + selection_instruction +
                        "You may select only an id listed in qa_candidates. customer_question is the sole evidence-selection target; "
                        "original_customer_question is identity context only and must not add a second condition. Do not infer a product, write an answer, use external knowledge, "
                        "or select a merely related question. The selected candidate must address the same operative property as the customer's "
                        "question: a specification such as fuel, heat source, material, size, weight, capacity, or a generic product description "
                        "does not prove a separate performance, weather/environment, durability, procedure, safety, or compatibility question. "
                        "Set coverage=full only when the selected QA completely answers every operative part of the current customer question. "
                        "For an overview or decision-support question asking for useful information, key points, or considerations about the product as a whole, "
                        "a single-property QA cannot have full coverage unless that QA question itself explicitly covers multiple product traits. "
                        "For a narrow single-property candidate in that situation, return qa_id null and coverage=none so complementary same-SKU evidence can be selected. "
                        "Use partial when it is relevant but leaves any requested condition, comparison, procedure, compatibility, limitation, or second question unanswered. "
                        "Return qa_id null and coverage=none when no candidate directly matches."
                    ),
                },
                {"role": "user", "content": json.dumps(payload, ensure_ascii=False)},
            ],
            temperature=0,
            max_tokens=min(int(semantic_runtime["max_tokens"]), 240),
            purpose="semantic_product_qa_evidence_selection",
            api_model_override=semantic_runtime["model"],
            response_format=semantic_runtime["response_format"],
            thinking=semantic_runtime["thinking"],
        )
    except Exception:
        return None
    selected = customer_agent_planner_service._extract_json_object(str(raw or "")) or {}
    selected_id = str(selected.get("qa_id") or "").strip()
    if (
        str(selected.get("confidence") or "").strip().lower() != "high"
        or str(selected.get("coverage") or "").strip().lower() != "full"
        or not selected_id
    ):
        return None
    candidate = next((qa for qa in qas if str(qa.id) == selected_id), None)
    if candidate is None:
        return None
    if not strict_entailment:
        return candidate
    # The first selector chooses from a sealed same-SKU set. A second narrow
    # entailment check prevents a broadly related QA (for example durability)
    # from being reused as a different capability, safety, temperature,
    # compatibility, procedure, or limitation answer in a compound turn.
    try:
        verification_raw = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "Return only JSON: {direct:boolean}. direct is true only when candidate_question and candidate_answer "
                        "directly determine the narrow customer_question without inference. A generic selling point, material, "
                        "durability, size, capacity, or usage statement does not determine a separate safety, temperature, "
                        "compatibility, cleaning, procedure, limitation, or performance question."
                    ),
                },
                {
                    "role": "user",
                    "content": json.dumps(
                        {
                            "customer_question": target_question,
                            "candidate_question": str(candidate.question or "").strip(),
                            "candidate_answer": str(candidate.answer or "").strip(),
                        },
                        ensure_ascii=False,
                    ),
                },
            ],
            temperature=0,
            max_tokens=40,
            purpose="semantic_product_qa_direct_entailment",
            api_model_override=semantic_runtime["model"],
            response_format=semantic_runtime["response_format"],
            thinking=semantic_runtime["thinking"],
        )
    except Exception:
        return None
    verification = customer_agent_planner_service._extract_json_object(str(verification_raw or "")) or {}
    return candidate if verification.get("direct") is True else None


def _same_sku_knowledge_evidence_selection_messages(
    question: str,
    sku: str,
    candidates: list[dict[str, Any]],
    *,
    product_identity: dict[str, Any] | None = None,
    allow_partial_compound: bool = False,
) -> list[dict[str, str]]:
    """Build the semantic-only relevance contract for sealed product RAG.

    Identity has already been verified by the entity contract before this
    selector runs.  The model must therefore judge relevance from the complete
    customer question, rather than treating every same-SKU chunk as an answer.
    """
    compound_instruction = (
        "The semantic plan has confirmed that this turn asks several independently requested product facts. "
        "Select an item when it directly supports one independently requested part, but never treat that item as support for another part; "
        "a later sealed answer will state any separately unsupported part safely. "
        if allow_partial_compound
        else ""
    )
    return [
        {
            "role": "system",
            "content": (
                "Return only JSON: {indexes:number[],confidence:high|medium|low,identity_consistent:boolean}. "
                "Select only directly relevant same-product evidence. Same-SKU identity alone is insufficient: "
                "every selected item must directly respond to the customer's intended question. "
                "Treat product_identity (canonical product name and catalogue category) as the authority for what physical product is being discussed. "
                "A stored SKU tag, the customer's wording, and a candidate that repeats the question do not prove that a capability belongs to that product. "
                "Set identity_consistent=false and indexes=[] when any evidence needed for the answer describes an operation or capability that plainly belongs to a different device, component, or product function not identified by product_identity, "
                "even when that candidate is tagged with this SKU. Missing identity detail is not itself a conflict, but evidence may never override a clear product identity. "
                "For a broad overview or decision question, select only complementary product-specific traits, "
                "capabilities, applicable scenes, compatibility, stated limitations, or trade-offs that help answer it. "
                "When the customer asks what a set, package, box, or unboxing includes, an evidence item that explicitly says the set includes, "
                "contains, or is composed of named main components directly answers that contents question. Do not treat generic selling points, "
                "usage instructions, or a product title alone as package-contents evidence. "
                "A selected same-SKU record in the form 'Q: ... A: ...' is direct evidence when its stored question semantically addresses the "
                "customer's question: its A text may then support the answer. Do not reject it merely because the customer used a natural paraphrase. "
                "An explicit numeric operating boundary is directly relevant when the customer's named condition can be transparently and conservatively "
                "checked against that boundary. Select the boundary evidence, but do not invent a hidden tolerance, failure mode, or safety guarantee. "
                "Do not select a comparison claim unless the customer explicitly asks for a comparison or names a comparison target. "
                "Do not include merely related material such as purchase, authenticity, delivery, returns, warranty, "
                "gifting, first-use, cleaning, maintenance, or other after-sales information unless the customer directly asks about it. "
                + compound_instruction
                + "Do not infer facts. Return indexes [] with low confidence when no candidate directly matches."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {
                    "question": question,
                    "sku": sku,
                    "product_identity": product_identity or {"sku": sku},
                    "candidates": candidates,
                },
                ensure_ascii=False,
            ),
        },
    ]


async def _same_sku_knowledge_identity_is_consistent(
    db: Session,
    *,
    question: str,
    product_identity: dict[str, Any],
    selected_evidence: list[dict[str, Any]],
) -> bool:
    """Independently audit whether selected claims belong to the product kind."""
    if not str(product_identity.get("canonical_name") or product_identity.get("category") or "").strip():
        # Older/migrating records can lack a descriptive identity. The
        # upstream entity and same-SKU contracts still apply, but an ontology
        # audit has no authority to compare against in that case.
        return True
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        raw = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "Return only JSON: {identity_consistent:boolean,confidence:high|medium|low,reason:string}. "
                        "This is an independent physical-product identity audit. Ignore every prior classification, plan, answer, and relevance decision. "
                        "Use only product_identity.canonical_name and product_identity.category to determine what physical product the SKU represents. "
                        "The customer's requested operation and the supplied evidence are claims to audit; they must never redefine the product identity. "
                        "Set identity_consistent=false when an operation or capability plainly requires a different kind of device or a separate component "
                        "not identified by the canonical product, even when the evidence repeats the question, asserts the capability, or carries the same SKU. "
                        "A capability merely being absent from the short canonical name is not by itself a conflict. "
                        "Do not judge ordinary relevance, completeness, or whether a valid product specification is true."
                    ),
                },
                {
                    "role": "user",
                    "content": json.dumps(
                        {
                            "question": question,
                            "product_identity": product_identity,
                            "candidates": selected_evidence,
                        },
                        ensure_ascii=False,
                    ),
                },
            ],
            temperature=0,
            max_tokens=100,
            purpose="semantic_product_knowledge_identity_consistency",
            api_model_override=runtime["model"],
            response_format=runtime["response_format"],
            thinking=runtime["thinking"],
        )
    except Exception:
        return False
    verdict = customer_agent_planner_service._extract_json_object(str(raw or "")) or {}
    return bool(
        verdict.get("identity_consistent") is True
        and str(verdict.get("confidence") or "").lower() in {"high", "medium"}
    )


def _knowledge_rows_with_approved_qa_provenance(
    db: Session,
    rows: list[dict[str, Any]] | None,
    *,
    sku: str,
) -> list[dict[str, Any]]:
    """Exclude stale product-QA chunks unless their live source is approved.

    The vector index is a cache of customer evidence, not the authority for QA
    eligibility. A rejected, pending-review, or deleted ProductQa row must stop
    being usable immediately even if an older chunk has not been reindexed yet.
    """
    normalized_sku = str(sku or "").strip().upper()
    prefix = f"product:{normalized_sku}:qa:"
    qa_ids: set[str] = set()
    prepared: list[tuple[dict[str, Any], str]] = []
    for row in rows or []:
        if not isinstance(row, dict):
            continue
        metadata = row.get("metadata") if isinstance(row.get("metadata"), dict) else {}
        source_id = str(metadata.get("source_id") or "").strip()
        qa_id = source_id[len(prefix):].strip() if source_id.startswith(prefix) else ""
        if qa_id:
            qa_ids.add(qa_id)
        prepared.append((row, qa_id))
    if not qa_ids:
        return [row for row, _qa_id in prepared]
    approved_ids = {
        str(qa_id)
        for (qa_id,) in db.query(ProductQa.id).join(
            Product,
            Product.id == ProductQa.product_id,
        ).filter(
            ProductQa.id.in_(qa_ids),
            ProductQa.integrity_status == "approved",
            Product.sku == normalized_sku,
        ).all()
    }
    return [row for row, qa_id in prepared if not qa_id or qa_id in approved_ids]


def _is_complete_package_contents_evidence(text: str) -> bool:
    """Whether one source explicitly supports a complete in-box list.

    A marketing claim that a cookware set "contains" its main pot and pan
    describes product composition, but cannot establish the complete package
    because it says nothing about handles, bags, manuals, or other standard
    items.  This guard only bounds the claim made for package-list questions;
    it does not infer products or select evidence.
    """
    value = re.sub(r"\s+", "", str(text or "")).casefold()
    if not value:
        return False
    return bool(re.search(
        r"(?:包装(?:清单|内|里|盒里|盒内)|开箱(?:清单|内容)|标配|随附|附带|随箱|"
        r"标准配置|盒内清单|package(?:contents|includes)|whatcomesinthebox)",
        value,
    ))


def _semantic_plan_requests_accessories(phase1_plan: dict[str, Any] | None) -> bool:
    """Honor a sealed accessories field contract in RAG evidence validation."""
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    semantic = plan.get("semantic_preplan") if isinstance(plan.get("semantic_preplan"), dict) else {}
    fields = semantic.get("canonical_fields")
    return isinstance(fields, list) and "accessories" in fields


_UNSUPPORTED_SAFETY_GUARANTEE_RE = re.compile(
    r"(?:放心(?:使用)?|完全没问题|没有问题|绝对安全|安全可靠|无风险|零风险|保证安全|risk[- ]free|completely safe|no problem)",
    re.IGNORECASE,
)


def _bound_unsupported_rag_safety_guarantee(
    answer: str,
    answer_payload: dict[str, Any],
    evidence_content: str,
) -> tuple[str, bool]:
    """Fail closed when a RAG draft turns compatibility into a guarantee.

    This is output safety validation only. It neither detects intent nor
    selects a field, entity, or source; the replacement quotes the already
    selected same-SKU evidence and retains its limited scope.
    """
    if not _UNSUPPORTED_SAFETY_GUARANTEE_RE.search(str(answer or "")):
        return answer, False
    if _UNSUPPORTED_SAFETY_GUARANTEE_RE.search(str(evidence_content or "")):
        return answer, False
    quotes = answer_payload.get("evidence_quotes") if isinstance(answer_payload, dict) else []
    quote = next((str(item).strip() for item in (quotes or []) if str(item).strip() in str(evidence_content or "")), "")
    if not quote:
        return "", True
    return f"当前同 SKU 资料显示：{quote}。这说明该场景的适配信息，不能据此作出安全或无风险保证。", True


async def _try_sealed_same_sku_knowledge_answer(
    db: Session,
    question: str,
    phase1_plan: dict[str, Any] | None,
) -> dict | None:
    """Answer a sealed product-QA turn from directly selected same-SKU RAG evidence."""
    safe_missing = _sealed_semantic_product_qa_entity_guard(db, question, phase1_plan)
    if not isinstance(safe_missing, dict):
        return None
    if str((safe_missing.get("debug") or {}).get("agent_mode") or "") != "sealed_product_qa_safe_missing":
        return None
    sku = str(safe_missing.get("sku") or "").strip().upper()
    if not sku:
        return None
    semantic_value = (phase1_plan or {}).get("semantic_preplan") if isinstance(phase1_plan, dict) else {}
    retrieval_question = str(
        semantic_value.get("supplemental_qa_evidence_query")
        or semantic_value.get("qa_evidence_query")
        or question
    ).strip() or question
    try:
        # Same-SKU sealing has already removed the principal precision risk.
        # Keep a wider retriever pool so natural paraphrases and multilingual
        # file chunks are not discarded before the semantic evidence selector
        # can judge them. The downstream prompt remains bounded to 24 atomic
        # evidence units and still fails closed on low confidence.
        retrieved = await knowledge_service.semantic_retrieve(db, retrieval_question, sku=sku, limit=12)
    except Exception:
        return None
    retrieved = _knowledge_rows_with_approved_qa_provenance(db, retrieved, sku=sku)
    # Semantic vector results are strongest for narrow QA, but may omit the
    # product's Listing/profile on broad product questions.  Add one bounded,
    # customer-facing same-SKU context; the semantic selector below still
    # decides whether it directly answers this particular question.
    try:
        authoritative_context = knowledge_service.same_sku_customer_context(db, sku, limit=1)
    except Exception:
        authoritative_context = []
    seen_contents = {
        str(item.get("content") or "").strip()
        for item in (retrieved or [])
        if isinstance(item, dict) and str(item.get("content") or "").strip()
    }
    retrieved = [
        *(retrieved or []),
        *[
            item
            for item in authoritative_context
            if isinstance(item, dict) and str(item.get("content") or "").strip() not in seen_contents
        ],
    ]
    # Knowledge chunks are ingestion units, not necessarily atomic evidence.
    # A product Listing can contain several unrelated paragraphs (for example
    # material, heat source, care, and package composition).  Presenting the
    # entire Listing as one candidate lets a semantic selector correctly find
    # the chunk but leaves the answer writer free to cite a different sentence
    # from that same chunk. Split only after SKU sealing and customer-safe
    # filtering; DeepSeek still decides relevance.
    candidates: list[dict[str, Any]] = []
    candidate_evidence_items: dict[int, dict[str, Any]] = {}
    seen_candidate_content: set[str] = set()
    for row in retrieved or []:
        if not isinstance(row, dict) or str(row.get("sku") or "").strip().upper() != sku:
            continue
        safe_content = _customer_safe_same_sku_knowledge_content(str(row.get("content") or ""))
        for evidence_unit in _same_sku_knowledge_evidence_units(safe_content):
            normalized_unit = "".join(evidence_unit.casefold().split())
            if not normalized_unit or normalized_unit in seen_candidate_content:
                continue
            seen_candidate_content.add(normalized_unit)
            candidate_index = len(candidates)
            candidates.append({"index": candidate_index, "content": evidence_unit})
            metadata = row.get("metadata") if isinstance(row.get("metadata"), dict) else {}
            section = str(metadata.get("section") or "").strip().casefold()
            source_type = (
                "product_qa"
                if section in {"qa", "product_qa"} or section.startswith("qa:")
                else "knowledge_chunk"
            )
            candidate_evidence_items[candidate_index] = {
                "evidence_id": customer_evidence_bundle.stable_customer_evidence_id(
                    namespace="knowledge",
                    sku=sku,
                    value=evidence_unit,
                ),
                "sku": sku,
                "source_type": source_type,
                "source": source_type,
                "field": "product_qa",
                "value": evidence_unit,
                "visibility": customer_evidence_bundle.CUSTOMER_VISIBLE,
            }
            # Bound the semantic prompt while retaining a diverse set of
            # retriever-ranked, independently auditable facts.
            if len(candidates) >= 24:
                break
        if len(candidates) >= 24:
            break
    if not candidates:
        return None
    result_rows = safe_missing.get("results") if isinstance(safe_missing.get("results"), list) else []
    identity_row = next(
        (
            item
            for item in result_rows
            if isinstance(item, dict) and str(item.get("sku") or "").strip().upper() == sku
        ),
        {},
    )
    product_identity = {
        "sku": sku,
        "canonical_name": str(
            identity_row.get("product_name_cn")
            or identity_row.get("product_name_en")
            or ""
        ).strip(),
        "category": str(identity_row.get("category") or "").strip(),
    }
    explicit_missing_clause_allowed = _semantic_product_qa_allows_explicit_missing_clause(phase1_plan)
    missing_capability_instruction = (
        "The semantic plan identifies multiple independently requested capabilities. Answer only the parts directly supported by the selected evidence. "
        "Do not append a missing-data or cannot-confirm clause here; the compound coverage orchestrator owns unsupported child requests after each child "
        "has been independently retrieved and grounded. Never use a related fact to imply support for a separate unsupported condition. "
        if explicit_missing_clause_allowed
        else "The semantic plan identifies one broad product request, not separate capability checks. Do not add an absence, missing-data, unsupported, "
        "or cannot-confirm sentence, and do not invent a second use case, criterion, or example scenario. "
    )
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        selected: dict[str, Any] = {}
        selected_indexes: Any = None
        # Even a single same-SKU candidate must pass the semantic identity and
        # relevance contract. Same-SKU ingestion mistakes are precisely the
        # case where automatic one-item selection would be unsafe.
        for _selection_attempt in range(2):
            selected = customer_agent_planner_service._extract_json_object(str(
                await customer_llm_service.chat_completion(
                    db,
                    messages=_same_sku_knowledge_evidence_selection_messages(
                        question,
                        sku,
                        candidates,
                        product_identity=product_identity,
                        allow_partial_compound=explicit_missing_clause_allowed,
                    ),
                    temperature=0,
                    max_tokens=min(int(runtime["max_tokens"]), 120),
                    purpose="semantic_product_knowledge_evidence_selection",
                    api_model_override=runtime["model"],
                    response_format=runtime["response_format"],
                    thinking=runtime["thinking"],
                ) or ""
            )) or {}
            selected_indexes = selected.get("indexes")
            if (
                selected.get("identity_consistent") is True
                and str(selected.get("confidence") or "").lower() in {"high", "medium"}
                and isinstance(selected_indexes, list)
                and selected_indexes
                and all(isinstance(index, int) for index in selected_indexes)
            ):
                break
        # Evidence selection is semantic, not an authorization to state a
        # product fact.  A nonempty medium-confidence selection remains
        # cautiously useful for broad product questions because generation is
        # followed by the independent same-SKU grounding gate below.  Low
        # confidence still fails closed before any answer is drafted.
        if (
            selected.get("identity_consistent") is not True
            or str(selected.get("confidence") or "").lower() not in {"high", "medium"}
            or not isinstance(selected_indexes, list)
            or not selected_indexes
            or not all(isinstance(index, int) for index in selected_indexes)
        ):
            return None
        selected_index_set = set(selected_indexes)
        selected_evidence = [item for item in candidates if item["index"] in selected_index_set]
        if len(selected_evidence) != len(selected_index_set):
            return None
        if not await _same_sku_knowledge_identity_is_consistent(
            db,
            question=question,
            product_identity=product_identity,
            selected_evidence=selected_evidence,
        ):
            return None
        if (
            (
                customer_agent_intent_service._looks_like_contents_grounding_question(question)
                or _semantic_plan_requests_accessories(phase1_plan)
            )
            and not any(_is_complete_package_contents_evidence(item["content"]) for item in selected_evidence)
        ):
            return None
        selected_bundle = customer_evidence_bundle.build_customer_evidence_bundle(
            sku=sku,
            product_name=sku,
            evidence_items=[
                candidate_evidence_items[item["index"]]
                for item in selected_evidence
            ],
        )
        if len(selected_bundle.items) != len(selected_evidence):
            return None
        evidence_content = "\n\n".join(item["content"] for item in selected_evidence)
        answer = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "Return only JSON: {answer:string,evidence_quotes:string[]}. Answer in concise, direct, friendly Chinese using only the supplied same-product evidence. "
                        "Start with the customer's actual conclusion, not boilerplate such as '请查看以下说明' or a description of evidence. "
                        "For a safety or operating question, clearly state the permitted action and the prohibited action supported by the evidence so the customer knows what to do next. When the evidence supports a safe sequence, name the customer's immediate condition, the action that must stop first, and the next permitted step instead of collapsing the reply into one short clause. "
                        "The upstream semantic selector has already accepted each supplied evidence item as directly relevant to the customer's complete question. "
                        "For a broad product overview or decision question, synthesize the concrete traits, stated capabilities, applicable scenes, "
                        "limitations, or trade-offs in that selected evidence; do not set answer to NO_EVIDENCE merely because no item repeats the customer's wording. "
                        "For a suitability or decision question, when the evidence supplies relevant decision-support factors but does not explicitly state the "
                        "requested final suitability judgement, present those concrete factors and say the records do not directly establish that final judgement. "
                        "Do not claim suitable or unsuitable merely from adjacent traits. "
                        "A listed target audience never excludes an unlisted audience and never proves that the product was not designed for them. "
                        "For an unlisted audience, state the recorded audience and useful traits, then keep the final suitability judgement explicitly unconfirmed. "
                        "You may make transparent conservative reasoning from an explicit numeric boundary in the evidence when the customer's stated condition "
                        "unambiguously falls outside that boundary. State the recorded boundary and phrase the result as outside the recorded range or not recommended; "
                        "never answer with cannot, impossible, or unsafe unless the evidence explicitly states that stronger conclusion. "
                        "Do not turn boundary reasoning into an unrecorded product failure, safety guarantee, or hidden tolerance. "
                        "Never both resolve a condition from accepted evidence and then say that same condition is unconfirmed or missing. "
                        "Only state that evidence does not directly confirm something when the customer explicitly asks a separate capability or condition. "
                        "For a broad overview or decision request, do not invent a missing use case, accessory, performance criterion, or example scenario. "
                        "Evidence that says suitable, adapted, or compatible with a condition supports only that attributed compatibility statement, never a safety, risk-free, certification, or 'no problem' guarantee without explicit evidence. "
                        "For a question about what a set, package, box, or unboxing includes, state only components explicitly introduced by an includes, contains, or composition statement in the evidence. "
                        "Do not infer included components from a title, dimensions, capacity, material, or a compatibility specification; each evidence quote for such an answer must itself support the stated component. "
                        "For selected evidence written as 'Q: ... A: ...', use the stored A as the direct answer evidence when the stored Q addresses "
                        "the customer's paraphrased question; quote the A text rather than treating the absence of identical wording as missing information. "
                        "For example, a stored same-SKU QA statement that the product has official warranty supports saying that the record states official warranty; "
                        "it does not support inventing a warranty duration, coverage term, or policy detail not present in that A. "
                        + missing_capability_instruction
                        + "Do not add facts, assumptions, or external knowledge. Include one to at most 3 evidence_quotes copied verbatim from the supplied evidence that support the answer; "
                        "each quote must be 60 characters or shorter. "
                        "do not return an answer when no such quote exists. Set answer to NO_EVIDENCE only when the selected evidence contains no customer-facing fact that can answer the question."
                    ),
                },
                {"role": "user", "content": json.dumps({"question": question, "evidence": evidence_content}, ensure_ascii=False)},
            ],
            temperature=0,
            max_tokens=min(int(runtime["max_tokens"]), 360),
            purpose="sealed_same_sku_knowledge_answer",
            api_model_override=runtime["model"],
            response_format=runtime["response_format"],
            thinking=runtime["thinking"],
        )
    except Exception:
        return None
    answer_payload = customer_agent_planner_service._extract_json_object(str(answer or "")) or {}
    answer = str(answer_payload.get("answer") or "").strip()
    if not answer or answer == "NO_EVIDENCE":
        return None
    answer, safety_bounded = _bound_unsupported_rag_safety_guarantee(answer, answer_payload, evidence_content)
    if not answer:
        return None
    quote_contract_failed = not _same_sku_rag_answer_has_selected_quotes(
        answer_payload,
        evidence_content,
    )
    initial_grounded = (
        not quote_contract_failed
        if safety_bounded
        else await _same_sku_rag_answer_is_grounded_after_quote_validation(
            db,
            question,
            answer,
            answer_payload,
            evidence_content,
        )
    )
    # Factual grounding alone is insufficient for a compound customer turn:
    # an answer can faithfully state one evidence fact while silently omitting
    # a second independently asked capability.  Keep semantic ownership of
    # that completeness judgement in a separate bounded LLM audit; the
    # deterministic layer only requires its boolean result before delivery.
    initial_complete = (
        initial_grounded
        if safety_bounded
        else initial_grounded and await _same_sku_rag_answer_covers_question(
            db,
            question,
            answer,
            evidence_content,
        )
    )
    semantic_answer_coverage_complete = bool(initial_complete)
    if not initial_grounded or not initial_complete:
        quote_repair_instruction = (
            "The prior evidence_quotes failed the provenance contract: every replacement quote must be a literal uninterrupted substring "
            "copied from the supplied evidence. Do not paraphrase, abbreviate, or replace part of a source list with ‘etc.’ or ‘and so on’. "
            if quote_contract_failed else ""
        )
        try:
            repaired = await customer_llm_service.chat_completion(
                db,
                messages=[
                    {
                        "role": "system",
                        "content": (
                            "Return only JSON: {answer:string,evidence_quotes:string[]}. Rewrite the answer in concise, direct, friendly Chinese using only the supplied same-product evidence. "
                            "Start with the customer's actual conclusion, not boilerplate such as '请查看以下说明' or a description of evidence. "
                            "For a safety or operating question, clearly state the permitted action and the prohibited action supported by the evidence so the customer knows what to do next. When the evidence supports a safe sequence, name the customer's immediate condition, the action that must stop first, and the next permitted step instead of collapsing the reply into one short clause. "
                            "Correct the previous draft when it says information is missing even though the evidence contains it. "
                            "Complete every independently requested part of the customer question; do not silently omit a part. "
                            "For a suitability or decision question, when the evidence supplies relevant decision-support factors but does not explicitly state the "
                            "requested final suitability judgement, present those concrete factors and say the records do not directly establish that final judgement. "
                            "If the previous draft claimed suitable or unsuitable merely from adjacent traits, replace it with this bounded decision-support answer. "
                            "A listed target audience never excludes an unlisted audience and never proves the product was not designed for them. "
                            "Remove any such exclusion; state the recorded audience and traits, then keep the final suitability judgement explicitly unconfirmed. "
                            "You may make transparent conservative reasoning from an explicit numeric boundary in the evidence when the customer's stated condition "
                            "unambiguously falls outside that boundary. State the recorded boundary and phrase the result as outside the recorded range or not recommended; "
                            "never answer with cannot, impossible, or unsafe unless the evidence explicitly states that stronger conclusion. "
                            "If the prior draft used such a stronger word, replace it with the recorded range plus a bounded not-recommended conclusion. "
                            "Do not turn boundary reasoning into an unrecorded product failure, safety guarantee, or hidden tolerance. "
                            "Never both resolve a condition from accepted evidence and then say that same condition is unconfirmed or missing; remove the contradictory missing clause. "
                            "Only state that evidence does not directly confirm something when the customer explicitly asks a separate capability or condition; "
                            "for a broad overview or decision request, do not invent a missing use case, accessory, performance criterion, or example scenario. "
                            "For a question about what a set, package, box, or unboxing includes, state only components explicitly introduced by an includes, contains, or composition statement in the evidence. "
                            "Do not infer included components from a title, dimensions, capacity, material, or a compatibility specification; each evidence quote for such an answer must itself support the stated component. "
                            "For selected evidence written as 'Q: ... A: ...', use the stored A as direct answer evidence when the stored Q addresses "
                            "the customer's paraphrased question; quote the A text rather than treating non-identical wording as missing information. "
                            "A stored same-SKU QA statement that the product has official warranty supports stating that recorded warranty fact, but never an unrecorded duration or policy term. "
                            "Evidence that says suitable, adapted, or compatible with a condition supports only that attributed compatibility statement, never a safety, risk-free, certification, or 'no problem' guarantee without explicit evidence. "
                            + missing_capability_instruction
                            + quote_repair_instruction
                            + "Do not add facts, make assumptions, or use external knowledge. Include one to at most 3 evidence_quotes copied verbatim from the supplied evidence that support the answer; "
                            "each quote must be 60 characters or shorter. "
                            "do not return an answer when no such quote exists. Set answer to NO_EVIDENCE if the evidence cannot answer."
                        ),
                    },
                    {
                        "role": "user",
                        "content": json.dumps(
                            {"question": question, "evidence": evidence_content, "previous_draft": answer},
                            ensure_ascii=False,
                        ),
                    },
                ],
                temperature=0,
                max_tokens=min(int(runtime["max_tokens"]), 360),
                purpose="sealed_same_sku_knowledge_answer_repair",
                api_model_override=runtime["model"],
                response_format=runtime["response_format"],
                thinking=runtime["thinking"],
            )
        except Exception:
            return None
        repaired_payload = customer_agent_planner_service._extract_json_object(str(repaired or "")) or {}
        answer = str(repaired_payload.get("answer") or "").strip()
        answer, repaired_safety_bounded = _bound_unsupported_rag_safety_guarantee(answer, repaired_payload, evidence_content)
        safety_bounded = safety_bounded or repaired_safety_bounded
        repaired_grounded = bool(
            answer
            and answer != "NO_EVIDENCE"
            and await _same_sku_rag_answer_is_grounded_after_quote_validation(
                db,
                question,
                answer,
                repaired_payload,
                evidence_content,
            )
        )
        repaired_complete = bool(
            repaired_grounded
            and await _same_sku_rag_answer_covers_question(
                db,
                question,
                answer,
                evidence_content,
            )
        )
        semantic_answer_coverage_complete = bool(repaired_complete)
        if not repaired_grounded or not repaired_complete:
            return None
    bounded_marketing_answer = _bound_gifting_qa_answer_to_evidence(question, answer)
    if bounded_marketing_answer != answer:
        safe_missing["answer"] = bounded_marketing_answer
        safe_missing["confidence"] = "medium"
        safe_missing["uncertainty"] = "missing_data"
        safe_missing["evidence"] = []
        safe_missing["sources"] = []
        safe_missing["answer_metadata"] = {
            "contract_field_type": "product_qa",
            "requested_field": "产品问答",
            "evidence_status": "missing",
            "field_evidence_missing": True,
            "evidence_sku": sku,
            "evidence_skus": [],
            "answer_policy": "insufficient_evidence",
        }
        safe_missing_debug = (
            safe_missing.get("debug")
            if isinstance(safe_missing.get("debug"), dict)
            else {}
        )
        safe_missing_debug["agent_mode"] = "sealed_product_qa_safe_missing"
        safe_missing_debug["gifting_evidence_boundary"] = True
        safe_missing["debug"] = safe_missing_debug
        safe_missing["skip_polish"] = True
        return safe_missing
    safe_missing["answer"] = answer
    safe_missing["confidence"] = "high"
    safe_missing["uncertainty"] = "resolved"
    safe_missing["evidence"] = [
        {
            "evidence_id": item.evidence_id,
            "sku": item.sku,
            "field_label": "产品知识库",
            "value": item.value,
            "source_type": item.source_type,
            "source_layer": "RAG",
            "matched_by": "semantic_same_sku_evidence",
        }
        for item in selected_bundle.items
    ]
    safe_missing["sources"] = [{"type": "knowledge_base", "label": "同 SKU 文件知识库", "sku": sku}]
    safe_missing["answer_metadata"] = {
        "contract_field_type": "product_qa",
        "evidence_status": "matched",
        "evidence_sku": sku,
        "evidence_skus": [sku],
        "evidence_bundle_skus": [selected_bundle.sku],
        "evidence_ids": [item.evidence_id for item in selected_bundle.items],
        "evidence_source": "same_sku_knowledge",
        "semantic_answer_coverage_complete": semantic_answer_coverage_complete,
    }
    safe_missing["debug"]["agent_mode"] = "sealed_same_sku_knowledge_rag"
    safe_missing["debug"]["knowledge_evidence_selector"] = {
        "confidence": "high",
        "selection_scope": "same_sku_only",
        "selected_count": len(selected_evidence),
    }
    # This answer has already been written by the customer-facing semantic
    # writer and independently checked against the sealed same-SKU evidence.
    # A second generic polish pass only adds a boilerplate preface and another
    # model call; it cannot improve or expand the permitted evidence.
    safe_missing["skip_polish"] = True
    return safe_missing


def _semantic_product_qa_allows_explicit_missing_clause(phase1_plan: dict[str, Any] | None) -> bool:
    """Use the semantic plan's multi-intent signal before drafting a safe gap.

    This does not infer a product field or capability from wording.  It merely
    keeps a one-intent product overview from being rewritten as if it contained
    a second, unasked customer requirement.
    """
    plan = phase1_plan if isinstance(phase1_plan, dict) else {}
    preplan = plan.get("semantic_preplan") if isinstance(plan.get("semantic_preplan"), dict) else {}
    return bool(
        preplan.get("compound")
        or preplan.get("additional_user_intent")
        or plan.get("compound")
        or plan.get("additional_user_intent")
    )


def _same_sku_rag_answer_has_selected_quotes(payload: dict[str, Any], evidence: str) -> bool:
    """Require model-supplied provenance snippets to come from selected evidence.

    The semantic selector owns relevance. This deterministic gate only checks
    that the answer generator did not silently rely on a different retrieval
    hit or model memory while turning selected same-SKU evidence into a reply.
    """
    return bool(_same_sku_rag_validated_quotes(payload, evidence))


def _same_sku_rag_validated_quotes(payload: dict[str, Any], evidence: str) -> list[str]:
    """Return only exact, nontrivial provenance quotes from selected evidence."""
    quotes = payload.get("evidence_quotes") if isinstance(payload, dict) else None
    if not isinstance(quotes, list) or not quotes:
        return []
    normalized_evidence = "".join(str(evidence or "").casefold().split())
    if not normalized_evidence:
        return []
    cleaned_quotes = [str(quote or "").strip() for quote in quotes]
    normalized_quotes = ["".join(quote.casefold().split()) for quote in cleaned_quotes]
    if not all(len(quote) >= 4 and quote in normalized_evidence for quote in normalized_quotes):
        return []
    return cleaned_quotes


def _bounded_selected_evidence_reference_answer(
    payload: dict[str, Any],
    evidence: str,
) -> str:
    """Render selected literal facts after an overclaiming draft is rejected.

    This fallback never revives the rejected conclusion. It exposes only
    provenance-validated snippets already selected as relevant by DeepSeek,
    then states the remaining judgement boundary explicitly.
    """
    quotes = _same_sku_rag_validated_quotes(payload, evidence)[:3]
    if not quotes:
        return ""
    return (
        f"当前同 SKU 资料中可核验的参考信息是：{'；'.join(quotes)}。"
        "这些资料可以作为判断参考，但未直接确认您询问的最终结论。"
    )


async def _same_sku_rag_answer_is_grounded_after_quote_validation(
    db: Session,
    question: str,
    answer: str,
    payload: dict[str, Any],
    evidence: str,
) -> bool:
    """Cross-check a cited RAG draft and fail closed on unsupported claims.

    Exact quotes first bind the draft to the semantic selector's same-SKU
    evidence. A grounding rejection cannot be retried into an approval: that
    would make a stochastic verifier a fail-open path for unsupported facts.
    """
    quotes = _same_sku_rag_validated_quotes(payload, evidence)
    if not quotes:
        return False
    # Quotes remain a mandatory provenance receipt, but they are intentionally
    # capped at three short spans.  A concise overview can contain more than
    # three directly selected same-SKU facts, so strict entailment must audit
    # the complete sealed selection rather than mistake uncited selected facts
    # for hallucinations.
    # The strict claim-entailment audit is the delivery gate.  A second,
    # broader relevance-style vote made a directly evidenced compound answer
    # fail merely because another separately requested condition was safely
    # marked as absent from the record.  Coverage remains independently
    # checked below; this gate stays fail-closed on every positive claim.
    return await _same_sku_evidence_answer_is_grounded(
        db,
        question,
        answer,
        evidence,
        strict_entailment=True,
    )


def _customer_safe_same_sku_knowledge_content(content: str) -> str:
    """Remove internal-only catalog fields before any RAG model sees a chunk.

    Product knowledge chunks are assembled from mixed database layers.  The
    product identity and customer-facing fields are valid same-SKU evidence,
    while owner, lifecycle planning status, search terms, and quality/remarks
    are not customer facts.  Filter by the chunk's labelled field boundary,
    rather than by SKU, product name, or customer wording.
    """
    internal_labels = {"负责人", "生命周期", "关键词", "品质情况", "备注"}
    kept: list[str] = []
    for line in str(content or "").splitlines():
        stripped = line.strip()
        label, separator, _ = stripped.lstrip("- ").partition(":")
        if separator and any(internal_label in label.strip() for internal_label in internal_labels):
            continue
        kept.append(line)
    return "\n".join(kept).strip()


def _same_sku_knowledge_evidence_units(content: str) -> list[str]:
    """Expose independently auditable paragraphs from a sealed knowledge chunk.

    This is source-agnostic: it does not identify a field, infer a fact, or
    select an answer. It only prevents unrelated facts in one imported document
    from being treated as one evidence claim.
    """
    text = str(content or "").strip()
    if not text:
        return []
    blocks = [block for block in re.split(r"\n\s*\n+", text) if block.strip()]
    if not blocks:
        return [text]
    units: list[str] = []
    for raw_block in blocks:
        block = raw_block.strip()
        if units and raw_block[:1].isspace():
            units[-1] = f"{units[-1]}\n{block}"
            continue
        # A malformed import can hold a whole document in one paragraph.
        # Preserve source text while exposing sentence-level evidence there.
        if len(block) > 900:
            sentences = [
                sentence.strip()
                for sentence in re.split(r"(?<=[。！？!?])\s*", block)
                if sentence.strip()
            ]
            if len(sentences) > 1:
                units.extend(sentences)
                continue
        units.append(block)
    return units


async def _same_sku_evidence_answer_is_grounded(
    db: Session,
    question: str,
    answer: str,
    evidence: str,
    *,
    strict_entailment: bool = False,
) -> bool:
    """Verify a generated RAG answer before it becomes a customer fact."""
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        raw = await customer_llm_service.chat_completion(
            db,
            messages=(
                _same_sku_knowledge_strict_entailment_messages(question, answer, evidence)
                if strict_entailment
                else _same_sku_knowledge_grounding_messages(question, answer, evidence)
            ),
            temperature=0,
            max_tokens=min(int(runtime["max_tokens"]), 80),
            purpose=(
                "sealed_same_sku_knowledge_strict_entailment"
                if strict_entailment
                else "sealed_same_sku_knowledge_grounding"
            ),
            api_model_override=runtime["model"],
            response_format=runtime["response_format"],
            thinking=runtime["thinking"],
        )
    except Exception:
        return False
    verdict = customer_agent_planner_service._extract_json_object(str(raw or "")) or {}
    return verdict.get("grounded") is True


async def _same_sku_rag_answer_covers_question(
    db: Session,
    question: str,
    answer: str,
    evidence: str,
) -> bool:
    """Ask the semantic layer whether a sealed draft covers every customer ask.

    This is deliberately separate from factual grounding.  It cannot select a
    route, field, SKU, or evidence; it only catches a generated reply that
    leaves an independent part of a compound customer question unanswered.
    """
    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        raw = await customer_llm_service.chat_completion(
            db,
            messages=_same_sku_knowledge_coverage_messages(question, answer, evidence),
            temperature=0,
            max_tokens=min(int(runtime["max_tokens"]), 80),
            purpose="sealed_same_sku_knowledge_coverage",
            api_model_override=runtime["model"],
            response_format=runtime["response_format"],
            thinking=runtime["thinking"],
        )
    except Exception:
        return False
    verdict = customer_agent_planner_service._extract_json_object(str(raw or "")) or {}
    return (
        verdict.get("complete") is True
        and verdict.get("internally_consistent", True) is True
    )


def _same_sku_knowledge_grounding_messages(
    question: str,
    answer: str,
    evidence: str,
) -> list[dict[str, str]]:
    """Build the evidence-faithfulness check for a sealed product RAG answer."""
    return [
        {
            "role": "system",
            "content": (
                "Return only JSON: {grounded:boolean}. The upstream semantic evidence selector has already decided that the supplied same-product evidence is relevant to the current request. "
                "Do not override the upstream semantic evidence selection by demanding a separate source that repeats the request's scenario wording. "
                "grounded is true when the answer faithfully states one or more supplied evidence facts without adding a fact, limitation, promise, comparison, or inference not in evidence. "
                "Use the supplied customer question only to identify the conditions that the draft claims to answer; it must not change the route, identity, or evidence selection. "
                "Approve a claimed condition only when the evidence directly supports that condition or conclusion. A related property, specification, range, or general benefit does not by itself support a distinct duration, environment, causal guarantee, compatibility, or safety conclusion. "
                "Evidence that says suitable, adapted, or compatible with a condition supports only that attributed compatibility statement; it does not prove safety, no risk, certification, or that there will be no problem unless those stronger claims are explicitly present. "
                "Absence of evidence never proves a negative claim: a product being manual, unpowered, or described for one capability does not by itself prove "
                "that it cannot perform a different capability. Require an explicit supporting fact before approving any no, cannot, does-not, unsupported, "
                "or other negative product assertion. "
                "For a compound question, it is allowed to keep a directly supported part and state that the supplied evidence does not directly confirm another requested capability, "
                "provided this is not presented as a negative product fact. "
                "Treat product-title, model, version, compatibility, or configuration descriptors embedded in the question as identity context, "
                "not independent unanswered customer conditions, unless the customer separately asks to verify that descriptor. "
                "For a broad preparation, overview, or decision question, directly selected product traits may answer the question without a separate travel, preparation, or scenario guide. "
                "Do not require the answer to exhaust every possible product consideration or every detail absent from the selected evidence; "
                "a concise answer that directly addresses the question using verified evidence is grounded. "
                "Return false when the answer says a recorded fact is missing or adds unsupported content."
            ),
        },
        {
            "role": "user",
            # Relevance remains sealed by semantic selection. The raw question
            # is supplied only so this verifier can distinguish a direct
            # evidence-backed fact from a newly inferred customer condition;
            # it has no authority to choose route, field, identity, or evidence.
            "content": json.dumps({"question": question, "answer": answer, "evidence": evidence}, ensure_ascii=False),
        },
    ]


def _same_sku_knowledge_strict_entailment_messages(
    question: str,
    answer: str,
    evidence: str,
) -> list[dict[str, str]]:
    """Build an adversarial claim-entailment audit for a sealed RAG draft."""
    return [
        {
            "role": "system",
            "content": (
                "Return only JSON: {grounded:boolean}. Audit strict evidence entailment, not route selection. "
                "The product, route, and same-SKU evidence are already sealed and cannot be changed. "
                "Use the customer question only to identify which conditions the draft purports to answer. "
                "Return true only if every factual conclusion, promise, condition, duration, environment, causal implication, compatibility, or safety statement in the draft is directly stated by the supplied evidence. "
                "A faithful concise synthesis of several directly stated same-product traits is allowed, including ordinary same-language paraphrase or grouping; "
                "do not reject it merely because no one source sentence repeats the combined wording. "
                "Customer-facing framing such as what to know before taking a product out, whether it fits a plan, or what to consider is not itself a new product claim; "
                "when the selected evidence directly states traits or applicable scenes, summarizing those facts for that framing is allowed. "
                "For a suitability or decision request, a draft may present directly stated decision-support factors and explicitly say the records do not directly "
                "establish the requested final suitability judgement. That evidence-boundary sentence is allowed; a definitive suitable/unsuitable conclusion still "
                "requires direct evidence. "
                "A related property, specification, numeric range, or general benefit is not enough to prove a distinct condition or conclusion. "
                "A statement about how many physical items are included in a package requires evidence that directly states the included quantity or package composition. "
                "Never infer included quantity from a product title, serving or user count, capacity descriptor, model number, or a statement that the product meets a number of users' needs. "
                "One narrow exception is transparent conservative boundary reasoning: when the evidence explicitly states a numeric operating range and the customer's "
                "named condition unambiguously falls outside it, the draft may state the recorded range and say that condition is outside the recorded range or is not recommended. "
                "When the named condition is a universally defined physical threshold (for example freshly boiling water), normalizing that threshold to the standard measurement needed for that boundary comparison is allowed. "
                "This permits only the arithmetic comparison against the recorded range; it does not add a product fact, hidden tolerance, failure mode, or safety guarantee. "
                "Example that must return grounded=true: evidence records 32°F to 140°F, the customer asks about freshly boiling water, and the draft says the recorded upper limit is 140°F (60°C), freshly boiling water exceeds it, so direct filling is not recommended. "
                "This exception does not permit an unrecorded failure mode, hidden tolerance, safety guarantee, or stronger cannot/unsafe claim. "
                "A supplied same-SKU QA statement that the product has official warranty directly entails the bounded claim that the record states official warranty. "
                "It never entails a warranty duration, coverage term, or any policy detail absent from that statement. "
                "For a semantic compound question, a statement that the supplied evidence does not directly confirm a separately requested condition is allowed "
                "when it is plainly an evidence boundary, not a negative product fact or product conclusion. Such a bounded record-coverage statement "
                "does not need separate product evidence and must not be rejected merely because the record is silent on that separate condition. "
                "Do not rely on common knowledge or plausible inference beyond this narrow threshold normalization. If any other claim needs an inference beyond the evidence, return false."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {"question": question, "answer": answer, "evidence": evidence},
                ensure_ascii=False,
            ),
        },
    ]


def _same_sku_knowledge_coverage_messages(
    question: str,
    answer: str,
    evidence: str,
) -> list[dict[str, str]]:
    """Build a semantic answer-coverage audit without granting new evidence."""
    return [
        {
            "role": "system",
            "content": (
                "Return only JSON: {complete:boolean,internally_consistent:boolean}. You are a coverage auditor and internal-consistency auditor, not a route planner or answer writer. "
                "Decide whether the draft addresses every independently requested product fact in the complete customer question. "
                "A compound question is complete only when every part is either answered from the supplied same-product evidence "
                "or explicitly and safely stated as not directly confirmed by that evidence. "
                "Return false when a draft both resolves a condition and says that same condition is not confirmed or missing; that contradiction must be rewritten. "
                "Return true when the draft both gives a directly evidenced part and names the other separately requested condition as not directly confirmed by the supplied evidence; "
                "that is a complete evidence-boundary answer, not an unsupported negative product claim. "
                "A factually grounded answer that silently omits an independent requested capability is incomplete. "
                "A draft that only acknowledges the request, offers to list information, or gives a generic lead without stating at least one "
                "concrete fact from the supplied evidence is incomplete, even when its evidence_quotes are valid. "
                "A broad overview or decision question (for example asking what to notice, consider, or weigh) is one request for a useful synthesis, "
                "not a list of hidden capabilities. Do not invent an independent requested condition, use case, accessory, or performance criterion "
                "from that broad wording, from a product name, or from an illustrative phrase in the draft. "
                "Only state that evidence does not directly confirm something when the question explicitly asks a separate capability or condition; "
                "do not return false merely because the evidence cannot resolve that named separate condition. "
                "Do not require an answer to introduce facts absent from the evidence, and do not treat evidence absence as a negative product fact."
            ),
        },
        {
            "role": "user",
            "content": json.dumps(
                {"question": question, "answer": answer, "evidence": evidence},
                ensure_ascii=False,
            ),
        },
    ]


def _safe_missing_product_qa_answer(name: str, sku: str, question: str) -> str:
    """Keep a safe missing answer specific enough to be useful to a shopper."""
    # Do not quote proposed values from the question.  A safe-missing answer
    # that repeats an unverified rating, certification, or number can look as
    # though the catalogue supports it and also leaks unsupported values into
    # downstream evidence checks.
    return (
        f"{name}（{sku}）：当前商品资料没有维护你问到的这项信息，因此无法确认具体结论。"
        "如这项信息会影响购买或使用，请以官方规格、检测资料或人工核验结果为准。"
    )


def _sealed_semantic_product_qa_entity_guard(
    db: Session,
    question: str,
    phase1_plan: dict[str, Any] | None,
) -> dict | None:
    """Fail closed before legacy retrieval can answer an unsealed QA turn."""
    if not _semantic_prefers_sealed_product_qa(phase1_plan or {}):
        return None
    preplan = (phase1_plan or {}).get("semantic_preplan") if isinstance(phase1_plan, dict) else {}
    subject = str((preplan or {}).get("subject_text") or "").strip()
    if not subject:
        return None
    versioned_subject = customer_entity_resolution_contract.recover_explicit_versioned_subject(
        question,
        subject,
    )
    if versioned_subject:
        subject = versioned_subject
    products = db.query(Product).order_by(Product.sku.asc()).all()
    resolver_candidates = _products_named_in_question(db, question)
    # The semantic planner decides the QA meaning, while the catalogue owns
    # identity. A unique canonical name explicitly present in this turn is
    # therefore stronger than a semantic subject that either absorbs predicate
    # words or drops a meaning-bearing name prefix. This supplies only the
    # validated display span, never an SKU, field, or answer; ambiguity still
    # fails closed in the entity contract below.
    canonical_subject = customer_entity_resolution_contract.unique_canonical_subject_in_question(
        question,
        products,
    )
    if canonical_subject:
        subject = canonical_subject
    contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        products,
        resolver_candidates=resolver_candidates or None,
        entity_text_override=subject,
        field_type_override="product_qa",
    )
    resolution = customer_entity_resolution_contract.can_resolve_single_product(contract, products)
    field_contract = {
        "field_type": "product_qa",
        "requested_field": "产品问答",
        "requested_fields": ["产品问答"],
        "canonical_fields": ["product_qa"],
        "supported_fields": ["product_qa"],
        "unsupported_fields": [],
        "subject": subject,
        "requested_scope": "subject",
        "source": "validated_semantic_product_qa",
        "confidence": float((preplan or {}).get("confidence") or 0),
        "compound": False,
    }
    if not resolution.allowed:
        action = "ambiguous_clarification" if contract.status == "ambiguous" else "unresolved_clarification"
        result = _build_phase2_entity_state_response(
            db,
            question,
            {
                "action": action,
                "contract": contract,
                "products": products,
                "displayable_candidate_skus": _displayable_phase2_clarification_candidate_skus(contract),
            },
        )
        if isinstance(result, dict):
            debug = result.setdefault("debug", {})
            debug["field_contract"] = field_contract
            debug["agent_mode"] = "sealed_product_qa_entity_clarification"
        return result

    product = next((item for item in products if str(item.sku or "").strip().upper() == resolution.resolved_sku), None)
    if product is None:
        return None
    sku = str(product.sku or "").strip().upper()
    name = str(product.product_name_cn or product.product_name_en or sku).strip()
    answer = _safe_missing_product_qa_answer(name, sku, question)
    return _sealed_product_qa_safe_missing_result(
        product,
        answer,
        field_contract=field_contract,
        debug={
            "entity_resolution_contract": contract.to_dict(),
            "binding_provenance": "resolved_entity_contract",
        },
    )


def _is_exact_product_qa_question_match(qa: ProductQa, question: str) -> bool:
    qa_question = _normalize_qa_question_text(qa.question or "")
    user_question = _normalize_qa_question_text(question or "")
    # Substring containment would let an old QA consume an added condition or
    # a second question. Natural rewrites are handled by semantic selection.
    return bool(qa_question and qa_question == user_question)


def _normalize_qa_question_text(question: str) -> str:
    text = re.sub(r"[（(]\s*[A-Za-z0-9]+(?:[-_][A-Za-z0-9]+)+\s*[）)]", "", str(question or ""))
    text = text.translate(str.maketrans("", "", "「」『』“”"))
    text = customer_agent_service.normalize_search_text(text)
    return re.sub(r"[?？。!！]+$", "", text).rstrip()


def _has_direct_structured_detail_answer(db: Session, product: Product, question: str) -> bool:
    """Keep exact field reads on the deterministic product_detail path."""
    text = str(question or "")
    if not text:
        return False
    field_groups = (
        (("热源", "适用热源", "燃料", "适用燃料"), ("heat_source",)),
        (("材质", "材料", "主体材质"), ("body_material",)),
        (("容量", "规格"), ("capacity",)),
        (("重量", "净重", "毛重"), ("gross_weight_g", "net_weight_g")),
        (("尺寸", "大小"), ("dimensions", "package_size")),
        (("颜色",), ("color",)),
        (("功率",), ("power",)),
        (("表面处理", "表面工艺"), ("surface_finish",)),
        (("适用人群", "适合哪些人群", "适合几人", "适合几个人"), ("target_audience",)),
    )
    matched_fields: tuple[str, ...] = ()
    for terms, fields in field_groups:
        if any(term in text for term in terms):
            matched_fields = fields
            break
    if not matched_fields:
        if "洗碗机" in text:
            detail = product_service.get_product_detail(db, product.sku)
            specs = detail.get("specs") or {}
            return bool(_dishwasher_specific_evidence(specs.get("usage_instruction")))
        return False
    detail = product_service.get_product_detail(db, product.sku)
    specs = detail.get("specs") or {}
    for field in matched_fields:
        if _display_value(specs.get(field)):
            return True
    return False


def _explicit_product_from_question(db: Session, question: str) -> Product | None:
    sku = _resolve_sku(db, question, None)
    if sku:
        product = db.query(Product).filter(Product.sku == sku).first()
        if product:
            return product
    normalized_question = customer_agent_service.normalize_search_text(question)
    if not normalized_question:
        return None
    matches: list[tuple[int, int, Product]] = []
    for product in db.query(Product).all():
        names = [
            customer_agent_service.normalize_search_text(product.product_name_cn or ""),
            customer_agent_service.normalize_search_text(product.product_name_en or ""),
        ]
        for name in names:
            if not name or name not in normalized_question:
                continue
            matches.append((normalized_question.index(name), -len(name), product))
            break
    if not matches:
        return _product_from_exact_qa_question(db, question)
    matches.sort(key=lambda item: (item[0], item[1]))
    return matches[0][2]


def _product_from_exact_qa_question(db: Session, question: str) -> Product | None:
    normalized_question = _normalize_qa_question_text(question)
    if not normalized_question:
        return None
    exact_qa = db.query(ProductQa).filter(
        ProductQa.question == str(question or "").strip(),
        ProductQa.integrity_status == "approved",
    ).first()
    if exact_qa and exact_qa.product_id:
        product = db.query(Product).filter(Product.id == exact_qa.product_id).first()
        if product:
            return product
    qa_matches: list[tuple[int, ProductQa]] = []
    for qa in db.query(ProductQa).filter(
        ProductQa.integrity_status == "approved",
    ).order_by(ProductQa.priority.desc().nullslast(), ProductQa.updated_at.desc()).all():
        qa_question = _normalize_qa_question_text(qa.question or "")
        if qa_question and qa_question in normalized_question:
            qa_matches.append((len(qa_question), qa))
    if not qa_matches:
        return None
    qa_matches.sort(key=lambda item: item[0], reverse=True)
    product_id = qa_matches[0][1].product_id
    if not product_id:
        return None
    return db.query(Product).filter(Product.id == product_id).first()


def _best_product_qa_match(
    db: Session,
    product: Product,
    question: str,
    *,
    requested_field_type: str | None = None,
    semantic_query: str | None = None,
) -> ProductQa | None:
    qas = product_service.customer_visible_product_qas(db, product.id)[:20]
    if not qas:
        return None
    question_terms = _qa_match_terms(question)
    semantic_terms = _qa_semantic_similarity_terms(semantic_query) if semantic_query else set()
    requested_qa_type = requested_field_type or customer_field_contract.classify_product_qa_request_type(question)
    best: tuple[int, ProductQa] | None = None
    for qa in qas:
        exact_question_match = _is_exact_product_qa_question_match(qa, question)
        field_compatible_qa = bool(requested_field_type and customer_field_contract.qa_evidence_matches_field(
            str(qa.question or ""),
            str(getattr(qa, "tags", "") or ""),
            requested_field_type,
        ))
        if requested_field_type and not field_compatible_qa and not exact_question_match:
            continue
        qa_text = f"{qa.question or ''} {qa.answer or ''}"
        qa_terms = _qa_match_terms(qa_text)
        overlap = question_terms & qa_terms
        # The semantic retrieval phrase represents the requested QA intent.
        # Match it only to the stored QA's request-side metadata, never to an
        # answer body.  Generic prose such as “正常使用” or “适合” in an answer
        # must not cause an unrelated same-SKU QA to win retrieval.
        qa_retrieval_text = f"{qa.question or ''} {getattr(qa, 'tags', '') or ''}"
        semantic_overlap = semantic_terms & _qa_semantic_similarity_terms(qa_retrieval_text)
        if semantic_query and not semantic_overlap and not exact_question_match:
            # Once semantic preplanning has sealed product-QA intent, an
            # unaligned candidate is not evidence. Fail closed rather than
            # falling back to legacy QA priority or answer-word matching.
            continue
        evidence_type = customer_field_contract.classify_product_qa_evidence_type(
            str(qa.question or ""),
            str(getattr(qa, "tags", "") or ""),
        )
        if not customer_field_contract.is_qa_evidence_compatible(
            requested_qa_type,
            evidence_type,
            has_semantic_overlap=bool(overlap or semantic_overlap),
        ) and not exact_question_match and not field_compatible_qa:
            continue
        score = len(overlap) + (2 * len(semantic_overlap))
        if exact_question_match:
            score += 8
        if requested_qa_type != "unknown" and requested_qa_type == evidence_type:
            score += 1
        elif field_compatible_qa:
            score += 1
        if score <= 0:
            continue
        if best is None or score > best[0]:
            best = (score, qa)
    return best[1] if best else None


def _qa_match_terms(text: str) -> set[str]:
    value = str(text or "")
    terms = set()
    for term in (
        "燃料", "热源", "气罐", "高山气罐", "卡式气罐", "酒精",
        "核心卖点", "卖点", "棋盘格", "材质", "耐用", "不易打滑", "易清洁",
        "正常使用", "正确保养", "使用多年", "越用越顺手", "用多久",
    ):
        if term in value:
            terms.add(term)
    for token in re.findall(r"[A-Za-z0-9]+", value.lower()):
        if len(token) >= 2:
            terms.add(token)
    return terms


def _qa_semantic_similarity_terms(text: str | None) -> set[str]:
    """Build language-agnostic retrieval tokens for an LLM-provided QA intent.

    This is evidence retrieval, not a route classifier: it never chooses a
    SKU, changes the semantic field, or authorizes an answer.  The caller has
    already required a high-confidence semantic ``product_qa`` decision and a
    sealed same-SKU entity contract.  CJK character n-grams make normal
    paraphrases comparable without maintaining a phrase whitelist.
    """
    normalized = customer_agent_service.normalize_search_text(str(text or "")).lower()
    terms: set[str] = set()
    for span in re.findall(r"[\u4e00-\u9fff]{2,}", normalized):
        for size in (2, 3):
            terms.update(span[index:index + size] for index in range(max(0, len(span) - size + 1)))
    for token in re.findall(r"[a-z0-9]{2,}", normalized):
        terms.add(token)
    return terms


def _get_or_create_conversation(
    db: Session,
    user_id: str,
    question: str,
    sku: str | None,
    conversation_id: str | None,
) -> CustomerServiceConversation:
    user_id = str(user_id)
    if conversation_id:
        conversation = db.query(CustomerServiceConversation).filter(
            CustomerServiceConversation.id == conversation_id,
            CustomerServiceConversation.user_id == user_id,
        ).first()
        if not conversation:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="客服会话不存在")
        return conversation

    # SQLite permits one writer at a time.  A short, bounded retry makes
    # independent customer sessions robust under concurrent first messages
    # without conflating users, swallowing other persistence failures, or
    # changing any product/field answer contract.
    for attempt in range(5):
        conversation = CustomerServiceConversation(
            user_id=user_id,
            title=_make_title(question, sku),
            sku=sku,
        )
        db.add(conversation)
        try:
            db.flush()
            return conversation
        except OperationalError as exc:
            message = str(exc).casefold()
            transient_sqlite_lock = "sqlite" in message and "locked" in message
            if not transient_sqlite_lock or attempt == 4:
                raise
            db.rollback()
            sleep(0.01 * (2 ** attempt))

    raise RuntimeError("unreachable conversation creation retry state")


def _touch_conversation(conversation: CustomerServiceConversation, sku: str | None = None) -> None:
    if sku:
        conversation.sku = sku
    conversation.updated_at = datetime.now(timezone.utc)


def update_active_product_anchor(
    *,
    previous_anchor: str | None,
    current_result_skus: list[str],
    answer_type: str,
    user_question: str,
    replacement_recommendation: bool = False,
) -> tuple[str | None, str]:
    previous = str(previous_anchor or "").strip().upper() or None
    if any(term in str(user_question or "") for term in ("不说这个了", "换个话题", "重新开始", "清空上下文")):
        return None, "explicit_reset"
    confirmed = [str(sku or "").strip().upper() for sku in current_result_skus if str(sku or "").strip()]
    if answer_type == "product_detail" and len(confirmed) == 1:
        return confirmed[0], "confirmed_single_product"
    if answer_type in {"comparison", "recommendation"} and len(confirmed) == 1:
        if answer_type == "recommendation" and replacement_recommendation and previous:
            return previous, "preserved_for_replacement_recommendation"
        return confirmed[0], "single_selected_product"
    if answer_type in {"comparison", "recommendation"} and len(confirmed) >= 2:
        ordinal_target = _ordinal_followup_target_sku_from_ordered(user_question, confirmed)
        if ordinal_target:
            return ordinal_target, "explicit_ordinal_focus"
    if previous:
        return previous, "preserved_without_new_single_product"
    return None, "no_active_single_product"


def _sources_with_result_context(
    agent_result: dict,
    turn_index: int | None = None,
    user_question: str | None = None,
    inherited_recommendation_context: dict | None = None,
    inherited_candidate_context: dict | None = None,
) -> list[dict]:
    sources = list(agent_result.get("sources") or [])
    existing_meta_entry = next(
        (
            item
            for item in sources
            if isinstance(item, dict)
            and item.get("type") == "agent_meta"
        ),
        None,
    )
    entities = _entities_from_agent_result(agent_result)
    if "result_skus" in agent_result:
        result_skus = [
            str(sku or "").strip().upper()
            for sku in (agent_result.get("result_skus") or [])
            if str(sku or "").strip()
        ]
        allowed_skus = set(result_skus)
        entities = [item for item in entities if item.get("sku") in allowed_skus]
    else:
        result_skus = [item["sku"] for item in entities]
    answer_ordered_skus = _order_skus_by_answer(result_skus, str(agent_result.get("answer") or ""))
    if answer_ordered_skus:
        result_skus = answer_ordered_skus
    recommendation_context = None
    candidate_context = None
    existing_candidate_context = next(
        (
            item.get("candidate_context")
            for item in sources
            if isinstance(item, dict)
            and item.get("type") == "agent_meta"
            and isinstance(item.get("candidate_context"), dict)
        ),
        {},
    )
    product_scope = customer_dialogue_state.product_scope_from_text(str(user_question or ""))
    if not product_scope and isinstance(inherited_recommendation_context, dict):
        product_scope = str(inherited_recommendation_context.get("product_scope") or "").strip()
    if not product_scope and isinstance(inherited_candidate_context, dict):
        product_scope = str(inherited_candidate_context.get("product_scope") or "").strip()
    inherited_candidate_skus = [
        str(sku).strip().upper()
        for sku in ((inherited_candidate_context or {}).get("candidate_skus") or [])
        if str(sku or "").strip()
    ]
    inherited_ordered_skus = [
        str(sku).strip().upper()
        for sku in (
            (inherited_candidate_context or {}).get("ordered_result_skus")
            or inherited_candidate_skus
            or []
        )
        if str(sku or "").strip()
    ]
    inherited_original_candidate_skus = [
        str(sku).strip().upper()
        for sku in (
            (existing_candidate_context or {}).get("original_candidate_skus")
            or (existing_candidate_context or {}).get("parent_candidate_skus")
            or (inherited_candidate_context or {}).get("original_candidate_skus")
            or (inherited_candidate_context or {}).get("parent_candidate_skus")
            or inherited_ordered_skus
            or inherited_candidate_skus
            or []
        )
        if str(sku or "").strip()
    ]
    current_ordered_skus = [
        str(sku).strip().upper()
        for sku in result_skus
        if str(sku or "").strip()
    ]
    result_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    current_scope_skus = _normalized_recommendation_scope_skus(
        result_metadata.get("recommendation_scope_skus")
    )
    inherited_scope_skus = (
        _normalized_recommendation_scope_skus(
            (inherited_recommendation_context or {}).get("ordinal_reference_scope_skus")
        )
        or _normalized_recommendation_scope_skus(
            (inherited_candidate_context or {}).get("ordinal_reference_scope_skus")
        )
        or _normalized_recommendation_scope_skus(
            (existing_candidate_context or {}).get("ordinal_reference_scope_skus")
        )
    )
    debug_metadata = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    answer_type = str(agent_result.get("answer_type") or "").strip()
    metadata_source = str(result_metadata.get("source") or "").strip()
    debug_agent_mode = str(debug_metadata.get("agent_mode") or "").strip()
    recommendation_like = bool(
        answer_type == "recommendation"
        or (
            answer_type in {"product_query", "query_products"}
            and (
                str(agent_result.get("intent") or "").strip() in {"recommendation", "recommend_products"}
                or metadata_source in {
                    "product_catalog_structured_recommendation",
                    "validated_semantic_preplan_then_same_sku_verification",
                }
                or debug_agent_mode == "semantic_recommendation_contract"
            )
        )
    )
    ordinal_detail_from_inherited_candidate_domain = bool(
        answer_type == "product_detail"
        and _looks_like_ordinal_product_field_followup(str(user_question or ""))
        and inherited_candidate_skus
        and current_ordered_skus
        and set(current_ordered_skus).issubset(set(inherited_candidate_skus))
    )
    result_debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    verified_candidate_skus = [
        str(sku).strip().upper()
        for sku in (agent_result.get("candidate_skus") or [])
        if str(sku or "").strip()
    ]
    if len(verified_candidate_skus) < 2:
        for key in ("all_verified_candidate_skus", "verified_candidate_skus"):
            verified_candidate_skus = [
                str(sku).strip().upper()
                for sku in result_debug.get(key) or []
                if str(sku or "").strip()
            ]
            if len(verified_candidate_skus) >= 2:
                break
    verified_candidate_skus = list(dict.fromkeys(verified_candidate_skus))[:10]
    final_choice_sku = str(result_metadata.get("final_choice_sku") or "").strip().upper()
    if final_choice_sku not in set(current_ordered_skus):
        final_choice_sku = ""
    replacement_recommendation = (
        str(result_debug.get("agent_mode") or "") == "recommendation_context_alternative_followup"
        or str(result_metadata.get("source") or "") == "recommendation_context_alternative_followup"
    )
    ordinal_reference_skus = (
        inherited_ordered_skus
        or inherited_original_candidate_skus
        or current_ordered_skus
    ) if replacement_recommendation else current_ordered_skus
    active_anchor, anchor_update_reason = update_active_product_anchor(
        previous_anchor=(inherited_recommendation_context or {}).get("active_single_product_anchor"),
        current_result_skus=current_ordered_skus,
        answer_type=str(agent_result.get("answer_type") or ""),
        user_question=str(user_question or ""),
        replacement_recommendation=replacement_recommendation,
    )
    # A comparison/recommendation may return several ordered rows while also
    # recording one evidence-backed decision.  That explicit final choice is
    # the singular discourse target for “the one just selected”; list order
    # must not silently replace it with the first participant.
    if final_choice_sku:
        active_anchor = final_choice_sku
        anchor_update_reason = "explicit_final_choice"
    if replacement_recommendation and agent_result.get("answer_type") in {"recommendation", "comparison"}:
        # The replacement answer is a filtered view of the parent candidate
        # set.  Resolve an explicit ordinal against that parent order before
        # storing the sealed single-product anchor; otherwise the second
        # choice is re-indexed to the first visible replacement candidate.
        parent_ordinal_target = _ordinal_followup_target_sku_from_ordered(
            str(user_question or ""),
            ordinal_reference_skus,
        )
        if parent_ordinal_target:
            active_anchor = parent_ordinal_target
            anchor_update_reason = "explicit_ordinal_focus_parent_context"
    preserve_inherited_candidate_domain = bool(
        (recommendation_like or ordinal_detail_from_inherited_candidate_domain)
        and inherited_candidate_skus
        and result_skus
        and set(result_skus).issubset(set(inherited_candidate_skus))
    )
    if result_skus and (
        agent_result.get("answer_type") in {"query_products", "product_query", "recommendation", "comparison"}
        or ordinal_detail_from_inherited_candidate_domain
    ):
        comparison_like = agent_result.get("answer_type") == "comparison"
        # Participant order remains available for ordinal follow-ups.  When
        # the answer records a final choice, that choice is the singular
        # discourse referent for phrases such as "the one just selected".
        last_referenced_sku = final_choice_sku or (
            current_ordered_skus[0] if current_ordered_skus else None
        )
        if comparison_like:
            ordinal_target = _ordinal_followup_target_sku_from_ordered(str(user_question or ""), current_ordered_skus)
            if ordinal_target:
                last_referenced_sku = ordinal_target
        candidate_domain_skus = (
            inherited_candidate_skus
            if preserve_inherited_candidate_domain
            else verified_candidate_skus
            if recommendation_like and len(verified_candidate_skus) >= 2
            else current_ordered_skus
        )
        candidate_context = {
            "candidate_skus": candidate_domain_skus,
            "ordinal_reference_scope_skus": current_scope_skus or inherited_scope_skus,
            "ordered_result_skus": (
                inherited_ordered_skus
                if preserve_inherited_candidate_domain
                else candidate_domain_skus
            ),
            "recommended_skus": result_skus if recommendation_like else [],
            "final_choice_sku": final_choice_sku or None,
            "pair_skus": current_ordered_skus[:2] if comparison_like and len(current_ordered_skus) >= 2 else [],
            "user_question": str(user_question or "").strip(),
            "product_scope": product_scope,
            "last_referenced_sku": last_referenced_sku,
            "source": (
                "recommendation"
                if recommendation_like or agent_result.get("intent") == "recommend_products"
                else "comparison"
                if agent_result.get("answer_type") == "comparison"
                else "result"
            ),
        }
    elif _is_empty_candidate_subset_result(agent_result, inherited_candidate_skus):
        candidate_context = {
            "candidate_skus": [],
            "ordinal_reference_scope_skus": inherited_scope_skus,
            "ordered_result_skus": [],
            "filtered_skus": [],
            "original_candidate_skus": inherited_original_candidate_skus,
            "parent_candidate_skus": inherited_original_candidate_skus,
            "recommended_skus": [],
            "user_question": str(user_question or "").strip(),
            "product_scope": product_scope,
            "empty_subset": True,
            "applied_filter": (
                (existing_candidate_context or {}).get("applied_filter")
                if isinstance((existing_candidate_context or {}).get("applied_filter"), dict)
                else None
            ),
        }
    elif (
        not result_skus
        and isinstance(existing_candidate_context, dict)
        and existing_candidate_context.get("empty_subset")
        and inherited_original_candidate_skus
    ):
        candidate_context = {
            "candidate_skus": [],
            "ordinal_reference_scope_skus": inherited_scope_skus,
            "ordered_result_skus": [],
            "filtered_skus": [
                str(sku).strip().upper()
                for sku in (existing_candidate_context.get("filtered_skus") or [])
                if str(sku or "").strip()
            ],
            "original_candidate_skus": inherited_original_candidate_skus,
            "parent_candidate_skus": inherited_original_candidate_skus,
            "recommended_skus": [],
            "user_question": str(user_question or "").strip(),
            "product_scope": product_scope,
            "empty_subset": True,
            "applied_filter": (
                (existing_candidate_context or {}).get("applied_filter")
                if isinstance((existing_candidate_context or {}).get("applied_filter"), dict)
                else None
            ),
        }
    answer_metadata = result_metadata
    current_contract = answer_metadata.get("recommendation_contract") or answer_metadata.get("current_recommendation_contract")
    effective_contract = answer_metadata.get("effective_recommendation_contract") or current_contract
    merge_provenance = answer_metadata.get("contract_merge_provenance") or (
        effective_contract.get("field_provenance") if isinstance(effective_contract, dict) else {}
    )
    verification_by_sku = {
        str(item.get("sku") or "").strip().upper(): dict(item)
        for item in (debug_metadata.get("candidate_verifications") or [])
        if isinstance(item, dict) and str(item.get("sku") or "").strip()
    }
    if recommendation_like and result_skus:
        recommendation_context = {
            "recommended_skus": result_skus,
            "ordered_result_skus": current_ordered_skus,
            "candidate_skus": current_ordered_skus,
            "ordinal_reference_scope_skus": current_scope_skus or inherited_scope_skus,
            "user_question": str(user_question or "").strip(),
            "product_scope": product_scope,
            "answer_type": "recommendation",
            "top_recommended_sku": current_ordered_skus[0] if current_ordered_skus else None,
            "last_referenced_sku": current_ordered_skus[0] if current_ordered_skus else None,
            "ordinal_reference_skus": ordinal_reference_skus,
            "active_single_product_anchor": active_anchor,
            "anchor_update_reason": anchor_update_reason,
            "replacement_top_sku": current_ordered_skus[0] if replacement_recommendation and current_ordered_skus else None,
            "recommendation_request_contract": dict(current_contract) if isinstance(current_contract, dict) else {},
            "effective_recommendation_contract": dict(effective_contract) if isinstance(effective_contract, dict) else {},
            "verification_by_sku": verification_by_sku,
            "previous_result_skus": result_skus,
            "excluded_skus": list((effective_contract or {}).get("exclusions") or []) if isinstance(effective_contract, dict) else [],
            "contract_merge_provenance": dict(merge_provenance) if isinstance(merge_provenance, dict) else {},
        }
    elif isinstance(inherited_recommendation_context, dict) and inherited_recommendation_context.get("recommended_skus"):
        recommendation_context = {
            "recommended_skus": [
                str(sku).strip().upper()
                for sku in inherited_recommendation_context.get("recommended_skus") or []
                if str(sku or "").strip()
            ],
            "ordered_result_skus": [
                str(sku).strip().upper()
                for sku in (
                    inherited_recommendation_context.get("ordered_result_skus")
                    or inherited_recommendation_context.get("recommended_skus")
                    or []
                )
                if str(sku or "").strip()
            ],
            "candidate_skus": [
                str(sku).strip().upper()
                for sku in (
                    inherited_recommendation_context.get("candidate_skus")
                    or inherited_recommendation_context.get("ordered_result_skus")
                    or inherited_recommendation_context.get("recommended_skus")
                    or []
                )
                if str(sku or "").strip()
            ],
            "user_question": str(inherited_recommendation_context.get("user_question") or "").strip(),
            "product_scope": str(inherited_recommendation_context.get("product_scope") or "").strip(),
            "answer_type": str(inherited_recommendation_context.get("answer_type") or "recommendation"),
            "top_recommended_sku": str(
                inherited_recommendation_context.get("top_recommended_sku")
                or (
                    (inherited_recommendation_context.get("ordered_result_skus") or inherited_recommendation_context.get("recommended_skus") or [None])[0]
                )
                or ""
            ).strip().upper() or None,
            "last_referenced_sku": str(inherited_recommendation_context.get("last_referenced_sku") or "").strip().upper() or None,
            "ordinal_reference_skus": [
                str(sku).strip().upper()
                for sku in (
                    inherited_recommendation_context.get("ordinal_reference_skus")
                    or inherited_recommendation_context.get("ordered_result_skus")
                    or inherited_recommendation_context.get("recommended_skus")
                    or []
                )
                if str(sku or "").strip()
            ],
            "ordinal_reference_scope_skus": (
                current_scope_skus
                or inherited_scope_skus
            ),
            "active_single_product_anchor": active_anchor,
            "anchor_update_reason": anchor_update_reason,
            "replacement_top_sku": str(inherited_recommendation_context.get("replacement_top_sku") or "").strip().upper() or None,
            "recommendation_request_contract": dict(inherited_recommendation_context.get("recommendation_request_contract") or {}),
            "effective_recommendation_contract": dict(
                answer_metadata.get("effective_recommendation_contract")
                or inherited_recommendation_context.get("effective_recommendation_contract")
                or inherited_recommendation_context.get("recommendation_request_contract")
                or {}
            ),
            "verification_by_sku": dict(inherited_recommendation_context.get("verification_by_sku") or {}),
            "previous_result_skus": list(inherited_recommendation_context.get("previous_result_skus") or inherited_recommendation_context.get("recommended_skus") or []),
            "excluded_skus": list(
                (answer_metadata.get("effective_recommendation_contract") or {}).get("exclusions")
                or inherited_recommendation_context.get("excluded_skus")
                or []
            ),
            "contract_merge_provenance": dict(
                answer_metadata.get("contract_merge_provenance")
                or inherited_recommendation_context.get("contract_merge_provenance")
                or {}
            ),
        }
    elif agent_result.get("answer_type") == "clarification":
        clarification_skus = [
            str(sku or "").strip().upper()
            for sku in (agent_result.get("candidate_skus") or [])
            if str(sku or "").strip()
        ]
        debug_contract = (
            (agent_result.get("debug") or {}).get("entity_resolution_contract")
            if isinstance(agent_result.get("debug"), dict)
            else {}
        ) or {}
        if len(clarification_skus) >= 2 and debug_contract.get("status") == "ambiguous":
            candidate_context = {
                "candidate_skus": clarification_skus,
                "ordered_result_skus": clarification_skus,
                "recommended_skus": [],
                "user_question": str(user_question or "").strip(),
                "product_scope": product_scope,
                "source": "entity_ambiguity",
                "entity_text": str(debug_contract.get("entity_text") or "").strip(),
                "field_type": str(debug_contract.get("field_type") or "").strip(),
            }
        if agent_result.get("answer_type") == "product_detail" and len(result_skus) == 1:
            recommendation_context["last_referenced_sku"] = result_skus[0]
        elif agent_result.get("answer_type") == "comparison" and current_ordered_skus:
            referenced_sku = _ordinal_followup_target_sku_from_ordered(str(user_question or ""), current_ordered_skus)
            if referenced_sku:
                recommendation_context["last_referenced_sku"] = referenced_sku
    if candidate_context and candidate_context.get("empty_subset") and isinstance(existing_candidate_context, dict):
        if not candidate_context.get("candidate_skus"):
            candidate_context["candidate_skus"] = [
                str(sku).strip().upper()
                for sku in existing_candidate_context.get("candidate_skus") or []
                if str(sku or "").strip()
            ]
        if not candidate_context.get("original_candidate_skus"):
            candidate_context["original_candidate_skus"] = [
                str(sku).strip().upper()
                for sku in (
                    existing_candidate_context.get("original_candidate_skus")
                    or existing_candidate_context.get("parent_candidate_skus")
                    or []
                )
                if str(sku or "").strip()
            ]
        if not candidate_context.get("parent_candidate_skus"):
            candidate_context["parent_candidate_skus"] = [
                str(sku).strip().upper()
                for sku in (
                    existing_candidate_context.get("parent_candidate_skus")
                    or existing_candidate_context.get("original_candidate_skus")
                    or candidate_context.get("original_candidate_skus")
                    or []
                )
                if str(sku or "").strip()
            ]
        if not candidate_context.get("applied_filter") and isinstance(existing_candidate_context.get("applied_filter"), dict):
            candidate_context["applied_filter"] = existing_candidate_context.get("applied_filter")
    if candidate_context and isinstance(existing_meta_entry, dict):
        existing_meta_entry["candidate_context"] = dict(candidate_context)
    if recommendation_context and isinstance(existing_meta_entry, dict):
        existing_meta_entry["recommendation_context"] = dict(recommendation_context)
    meta_entry = {
        "type": "agent_meta",
        "label": "客服回复元数据",
        "intent": agent_result.get("intent"),
        "answer_type": agent_result.get("answer_type"),
        "confidence": agent_result.get("confidence"),
        "uncertainty": agent_result.get("uncertainty"),
        "needs_clarification": agent_result.get("needs_clarification", False),
        "anomalies": agent_result.get("anomalies") or [],
        "suggested_followups": agent_result.get("suggested_followups") or agent_result.get("followups") or [],
        "followups": agent_result.get("followups") or agent_result.get("suggested_followups") or [],
        "warnings": agent_result.get("warnings") or [],
        "evidence": agent_result.get("evidence") or [],
        "agent_quality": agent_result.get("agent_quality") or {},
        "debug": agent_result.get("debug") or {},
        "feedback": agent_result.get("feedback") or None,
    }
    pending_clarification_context = _pending_clarification_context_for_result(agent_result, user_question)
    if pending_clarification_context:
        meta_entry["pending_clarification_context"] = pending_clarification_context
    if recommendation_context:
        meta_entry["recommendation_context"] = recommendation_context
    if candidate_context:
        meta_entry["candidate_context"] = candidate_context
    sources.append(meta_entry)
    if agent_result.get("steps"):
        sources.append({"type": "agent_steps", "label": "Agent执行过程", "steps": agent_result.get("steps")})
    if result_skus:
        context_entry = {
            "type": "agent_context",
            "label": "上下文结果",
            "result_skus": result_skus,
            "current_sku": agent_result.get("sku") if agent_result.get("sku") in result_skus else (result_skus[0] if len(result_skus) == 1 else None),
            "entities": entities,
            "count": len(result_skus),
        }
        if turn_index is not None:
            context_entry["turn_index"] = turn_index
        sources.append(context_entry)
    return sources


def _latest_recommendation_context_for_sources(db: Session, conversation_id: str | None) -> dict:
    if not conversation_id:
        return {}
    messages = (
        db.query(CustomerServiceMessage)
        .filter(
            CustomerServiceMessage.conversation_id == conversation_id,
            CustomerServiceMessage.role == "assistant",
        )
        .order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc())
        .limit(5)
        .all()
    )
    anchor_fallback: dict[str, Any] = {}
    context_fallback: dict[str, Any] = {}
    for message in messages:
        sources = _safe_json(message.sources_json, [])
        for source in sources:
            if not isinstance(source, dict) or source.get("type") != "agent_meta":
                continue
            context = source.get("recommendation_context")
            if isinstance(context, dict) and context.get("recommended_skus"):
                # Explanation and ordinal-detail turns may preserve the prior
                # SKU list but intentionally carry no recommendation contract.
                # Keep scanning for the last decision-bearing recommendation
                # so an alternative can inherit its real category/constraints
                # instead of treating the explanation as a new recommendation.
                if not context_fallback:
                    context_fallback = context
                if isinstance(context.get("effective_recommendation_contract"), dict) and context.get("effective_recommendation_contract"):
                    return context
                if isinstance(context.get("recommendation_request_contract"), dict) and context.get("recommendation_request_contract"):
                    return context
    for message in messages:
        sources = _safe_json(message.sources_json, [])
        for source in sources:
            if not isinstance(source, dict) or source.get("type") != "agent_context":
                continue
            current_sku = str(source.get("current_sku") or "").strip().upper()
            result_skus = [
                str(sku or "").strip().upper()
                for sku in (source.get("result_skus") or [])
                if str(sku or "").strip()
            ]
            if current_sku and result_skus == [current_sku] and not anchor_fallback:
                anchor_fallback = {
                    "active_single_product_anchor": current_sku,
                    "anchor_update_reason": "confirmed_single_product_context",
                }
    return context_fallback or anchor_fallback


def _is_empty_candidate_subset_result(agent_result: dict, inherited_candidate_skus: list[str]) -> bool:
    if not inherited_candidate_skus:
        return False
    if agent_result.get("answer_type") != "product_query":
        return False
    if agent_result.get("results"):
        return False
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    parsed = debug.get("parsed_intent") if isinstance(debug.get("parsed_intent"), dict) else {}
    if parsed.get("source_context") == "previous_results":
        return True
    for step in agent_result.get("steps") or []:
        if isinstance(step, dict) and "filter_previous_results" in str(step.get("type") or ""):
            return True
    return False


def _latest_candidate_context_for_sources(db: Session, conversation_id: str | None) -> dict:
    if not conversation_id:
        return {}
    messages = (
        db.query(CustomerServiceMessage)
        .filter(
            CustomerServiceMessage.conversation_id == conversation_id,
            CustomerServiceMessage.role == "assistant",
        )
        .order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc())
        .limit(5)
        .all()
    )
    for message in messages:
        for source in _safe_json(message.sources_json, []):
            if not isinstance(source, dict) or source.get("type") != "agent_meta":
                continue
            context = source.get("candidate_context")
            if isinstance(context, dict) and (context.get("candidate_skus") or context.get("empty_subset")):
                return {
                    "candidate_skus": [
                        str(sku).strip().upper()
                        for sku in context.get("candidate_skus") or []
                        if str(sku or "").strip()
                    ],
                    "ordered_result_skus": [
                        str(sku).strip().upper()
                        for sku in context.get("ordered_result_skus") or context.get("candidate_skus") or []
                        if str(sku or "").strip()
                    ],
                    "recommended_skus": [
                        str(sku).strip().upper()
                        for sku in context.get("recommended_skus") or []
                        if str(sku or "").strip()
                    ],
                    "pair_skus": [
                        str(sku).strip().upper()
                        for sku in (
                            context.get("pair_skus")
                            or context.get("ordered_result_skus")
                            or context.get("candidate_skus")
                            or []
                        )
                        if str(sku or "").strip()
                    ][:2],
                    "original_candidate_skus": [
                        str(sku).strip().upper()
                        for sku in (
                            context.get("original_candidate_skus")
                            or context.get("parent_candidate_skus")
                            or []
                        )
                        if str(sku or "").strip()
                    ],
                    "parent_candidate_skus": [
                        str(sku).strip().upper()
                        for sku in (
                            context.get("parent_candidate_skus")
                            or context.get("original_candidate_skus")
                            or []
                        )
                        if str(sku or "").strip()
                    ],
                    "filtered_skus": [
                        str(sku).strip().upper()
                        for sku in context.get("filtered_skus") or []
                        if str(sku or "").strip()
                    ],
                    "user_question": str(context.get("user_question") or "").strip(),
                    "product_scope": str(context.get("product_scope") or "").strip(),
                    "source": str(context.get("source") or "").strip(),
                    "entity_text": str(context.get("entity_text") or "").strip(),
                    "field_type": str(context.get("field_type") or "").strip(),
                    "empty_subset": bool(context.get("empty_subset")),
                    "applied_filter": context.get("applied_filter") if isinstance(context.get("applied_filter"), dict) else None,
                    "last_referenced_sku": str(context.get("last_referenced_sku") or "").strip().upper() or None,
                    "final_choice_sku": str(context.get("final_choice_sku") or "").strip().upper() or None,
                }
    return {}


def _latest_pending_clarification_context_for_sources(db: Session, conversation_id: str | None) -> dict:
    if not conversation_id:
        return {}
    messages = (
        db.query(CustomerServiceMessage)
        .filter(
            CustomerServiceMessage.conversation_id == conversation_id,
            CustomerServiceMessage.role == "assistant",
        )
        .order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc())
        .limit(5)
        .all()
    )
    for message in messages:
        for source in _safe_json(message.sources_json, []):
            if not isinstance(source, dict) or source.get("type") != "agent_meta":
                continue
            context = source.get("pending_clarification_context")
            if isinstance(context, dict) and context.get("intent") in {"product_detail", "product_context_required"}:
                return dict(context)
    return {}


def _is_clarification_followup_product_only_text(question: str) -> bool:
    text = str(question or "").strip()
    if not text or len(text) > 40:
        return False
    if any(punct in text for punct in "？?！!。.,，；;：:"):
        return False
    if any(term in text for term in ("推荐", "怎么", "为何", "为什么", "可以吗", "能不能", "能否", "是否", "可不可以", "清洗", "保养", "质保", "售后", "购买", "渠道")):
        return False
    return True


def _clarification_followup_target_products(db: Session, question: str) -> list[Product]:
    exact_skus = [
        str(sku or "").strip().upper()
        for sku in customer_agent_service._extract_skus(question)
        if str(sku or "").strip()
    ]
    if exact_skus:
        rows = db.query(Product).filter(Product.sku.in_(exact_skus[:3])).all()
        by_sku = {str(item.sku or "").strip().upper(): item for item in rows}
        matched = [by_sku[sku] for sku in exact_skus if sku in by_sku]
        if matched:
            return matched[:3]
    products = _products_named_in_question(db, question)
    if products:
        return products[:3]
    explicit_rows = _question_entities_for_entity_stack(db, question)
    skus = [
        str(item.get("sku") or "").strip().upper()
        for item in explicit_rows
        if str(item.get("sku") or "").strip()
    ]
    if not skus:
        return []
    rows = db.query(Product).filter(Product.sku.in_(skus[:3])).all()
    by_sku = {str(item.sku or "").strip().upper(): item for item in rows}
    return [by_sku[sku] for sku in skus if sku in by_sku]


def _clarification_scope_matches_product(scope: str, product: Product | None) -> bool:
    normalized_scope = str(scope or "").strip()
    if not normalized_scope or not product:
        return True
    category = str(getattr(product, "category", "") or "")
    name = str(getattr(product, "product_name_cn", "") or getattr(product, "product_name_en", "") or "")
    if "锅" in normalized_scope:
        return "锅" in category or "锅" in name or "炊具" in name
    if "水壶" in normalized_scope or "壶" in normalized_scope:
        return ("壶" in category) or ("壶" in name) or ("杯" in name)
    return True


def _clarification_scope_mismatch_result(context: dict, product: Product) -> dict:
    scope = str(context.get("product_scope") or "上一轮产品范围").strip()
    name = str(product.product_name_cn or product.product_name_en or product.sku or "").strip()
    sku = str(product.sku or "").strip().upper()
    answer = f"你上一轮问的是{scope}，这次提供的是{name}（{sku}）。请确认是要查这个产品，还是继续查{scope}。"
    steps = [{
        "type": "clarify",
        "label": "clarification 类目错配保护",
        "detail": f"{scope} vs {sku}",
        "ok": True,
    }]
    return {
        "answer": answer,
        "intent": "clarify",
        "answer_type": "clarification",
        "confidence": "low",
        "uncertainty": "ambiguous_product_scope",
        "needs_clarification": True,
        "anomalies": [],
        "suggested_followups": [f"请确认是否查询 {sku}。"],
        "followups": [f"请确认是否查询 {sku}。"],
        "evidence": [],
        "sources": [{"type": "agent_clarification", "label": "clarification 类目错配保护"}],
        "actions": [],
        "results": [],
        "steps": steps,
        "warnings": ["clarification_scope_mismatch"],
        "sku": None,
        "result_skus": [],
        "debug": {
            "agent_mode": "clarification_scope_mismatch",
            "intent": "clarify",
            "steps": steps,
            "warnings": ["clarification_scope_mismatch"],
            "anomalies": [],
            "raw_results": [],
            "pending_clarification_context": context,
        },
        "skip_polish": True,
    }


def _is_new_question(question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    if any(term in text for term in ("推荐", "建议", "清洗", "保养", "质保", "售后", "购买", "渠道", "能不能", "是否", "可不可以", "可以吗", "怎么", "为什么", "为何")):
        return False
    if len(text) <= 30 and not any(punct in text for punct in "？?！!。.,，；;：:"):
        return True
    return False


def _compose_clarification_followup_question(context: dict, product: Product) -> str | None:
    requested_field = str(context.get("requested_field") or "").strip()
    capability_question = str(context.get("capability_question") or "").strip()
    name_or_sku = str(product.sku or product.product_name_cn or product.product_name_en or "").strip()
    if not name_or_sku:
        return None
    if requested_field == "price":
        return f"{name_or_sku} 多少钱？"
    if requested_field == "heat_source" and capability_question:
        return f"{name_or_sku} {capability_question}"
    return None


async def _deterministic_clarification_followup_result(
    db: Session,
    *,
    product: Product,
    pending_context: dict,
    synthesized_question: str,
) -> dict | None:
    requested_field = str(pending_context.get("requested_field") or "").strip()
    field_label = ""
    if requested_field == "price":
        # ``price`` is a live-information field.  Keep its canonical label so
        # the Phase-2 formatter can either present same-SKU price-positioning
        # as a clearly non-real-time reference, or use the price-specific safe
        # missing response.  Relabelling it as ``价格定位`` here bypasses both
        # behaviours and degrades to the generic unknown-field formatter.
        field_label = customer_field_contract.product_detail_field_label("price") or "价格"
    elif requested_field == "heat_source":
        field_label = "热源"
    if not field_label:
        return None
    field_contract = customer_field_contract.field_contract_for_type(requested_field)
    resolved_sku = str(product.sku or "").strip().upper()
    if not field_contract or not resolved_sku:
        return None
    # Carry-over supplies an already explicit field from the previous
    # clarification and the current SKU supplies identity.  Re-enter the same
    # Phase-2 formatter used by normal detail questions so final evidence
    # enforcement cannot re-parse the bare SKU as the unrelated ``model``
    # field.
    entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        synthesized_question,
        [product],
        resolver_candidates=[product],
        entity_text_override=resolved_sku,
    )
    resolution = customer_entity_resolution_contract.can_resolve_single_product(
        entity_contract,
        [product],
    )
    if not resolution.allowed or resolution.resolved_sku != resolved_sku:
        return None
    return _build_phase2_entity_state_response(
        db,
        synthesized_question,
        {
            "action": "resolved_detail",
            "contract": entity_contract,
            "products": [product],
            "field": field_contract,
            "requested_field": field_label,
        },
    )


async def _try_pending_clarification_followup(
    db: Session,
    *,
    user_id: str,
    conversation_id: str | None,
    question: str,
) -> dict | None:
    if not conversation_id:
        return None
    if not _is_clarification_followup_product_only_text(question):
        return None
    pending_context = _latest_pending_clarification_context_for_sources(db, conversation_id)
    if not pending_context:
        return None
    if str(pending_context.get("intent") or "") == "product_context_required":
        text = str(question or "").strip()
        implicit_reference = any(term in text for term in (
            "它", "这个", "这款", "该产品", "该商品", "第一个", "第二个", "第三个", "最后一个",
        ))
        if implicit_reference and not _clarification_followup_target_products(db, question):
            return {
                "intent": "clarify",
                "answer_type": "clarification",
                "answer": "上一轮还没有形成可引用的商品列表。请直接补充 SKU 或完整商品名，我再继续查材质、场景或其他信息。",
                "results": [], "result_skus": [], "candidate_skus": [],
                "needs_clarification": True,
                "answer_metadata": {"source": "clarification_context_required"},
                "debug": {"agent_mode": "clarification_context_required"},
                "skip_polish": True,
            }
        return None
    requested_field = str(pending_context.get("requested_field") or "").strip()
    product_scope = str(pending_context.get("product_scope") or "").strip()
    if requested_field not in {"price", "heat_source"}:
        return None
    if _is_recommendation_followup_question(question):
        return None
    if customer_agent_intent_service._looks_like_usage_care_question(question) or _classify_customer_faq_intent(question):
        return None
    products = _clarification_followup_target_products(db, question)
    if len(products) != 1:
        if _is_new_question(question):
            return None
        return None
    product = products[0]
    if not _clarification_scope_matches_product(product_scope, product):
        return _clarification_scope_mismatch_result(pending_context, product)
    synthesized_question = _compose_clarification_followup_question(pending_context, product)
    if not synthesized_question:
        return None
    result = await _deterministic_clarification_followup_result(
        db,
        product=product,
        pending_context=pending_context,
        synthesized_question=synthesized_question,
    )
    if not result:
        return None
    debug = dict(result.get("debug") or {})
    debug["agent_mode"] = "clarification_slot_carryover"
    debug["pending_clarification_context"] = pending_context
    debug["synthesized_question"] = synthesized_question
    result["debug"] = debug
    return result


def _recognized_intent_for_agent_fast_path(db: Session, question: str, conversation_id: str | None) -> str | None:
    intent = customer_agent_intent_service.parse_intent(question, previous_result_skus=[])
    if intent and getattr(intent, "intent", "") == "recommend_products":
        return "recommend_products"
    if _asks_for_alternative_recommendation(question) and (
        _latest_recommendation_context_for_sources(db, conversation_id)
        or _latest_candidate_context_for_sources(db, conversation_id)
    ):
        return "recommend_products"
    return getattr(intent, "intent", None) if intent else None


def _previous_result_skus_for_pre_runtime(
    db: Session,
    *,
    user_id: str,
    conversation_id: str | None,
    question: str,
) -> list[str]:
    candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    if not candidate_context:
        candidate_context = _latest_recommendation_context_for_sources(db, conversation_id)
    if candidate_context and _is_plural_heat_source_followup(question):
        scoped_skus = (
            candidate_context.get("ordered_result_skus")
            or candidate_context.get("candidate_skus")
            or candidate_context.get("recommended_skus")
            or candidate_context.get("original_candidate_skus")
            or candidate_context.get("parent_candidate_skus")
            or []
        )
        scoped_skus = [
            str(sku).strip().upper()
            for sku in scoped_skus
            if str(sku or "").strip()
        ]
        if len(scoped_skus) >= 2:
            return scoped_skus[:2]
    if candidate_context and (
        _is_recommendation_followup_question(question)
        or _is_contextual_power_followup_question(question)
        or customer_dialogue_state.needs_previous_context(question)
    ):
        scoped_skus = (
            candidate_context.get("candidate_skus")
            or candidate_context.get("ordered_result_skus")
            or candidate_context.get("recommended_skus")
            or candidate_context.get("original_candidate_skus")
            or candidate_context.get("parent_candidate_skus")
            or []
        )
        scoped_skus = [
            str(sku).strip().upper()
            for sku in scoped_skus
            if str(sku or "").strip()
        ]
        if scoped_skus:
            return scoped_skus[:10]
    if conversation_id and customer_dialogue_state.needs_previous_context(question):
        ordered_skus = _latest_ordered_result_skus_for_followup(db, conversation_id, user_id)
        if ordered_skus:
            return ordered_skus[:10]
    return _latest_active_product_skus(db, conversation_id, user_id)


def _should_force_runtime_empty_subset_followup(
    question: str,
    *,
    explicit_sku: str | None,
    named_products: list[dict] | None,
    candidate_context: dict[str, Any] | None,
) -> bool:
    if explicit_sku or (named_products or []):
        return False
    if not isinstance(candidate_context, dict) or not candidate_context.get("empty_subset"):
        return False
    preserved_domain = (
        candidate_context.get("original_candidate_skus")
        or candidate_context.get("parent_candidate_skus")
        or []
    )
    if not any(str(item or "").strip() for item in preserved_domain):
        return False
    return bool(customer_agent_runtime_service._is_empty_subset_followup(question))


def _empty_candidate_subset_followup_result(
    question: str,
    candidate_context: dict[str, Any] | None,
) -> dict[str, Any]:
    context = candidate_context if isinstance(candidate_context, dict) else {}
    parent_skus = [
        str(item or "").strip().upper()
        for item in (
            context.get("original_candidate_skus")
            or context.get("parent_candidate_skus")
            or []
        )
        if str(item or "").strip()
    ]
    empty_context = {
        "candidate_skus": [],
        "ordered_result_skus": [],
        "filtered_skus": [],
        "original_candidate_skus": parent_skus,
        "parent_candidate_skus": parent_skus,
        "recommended_skus": [],
        "user_question": question,
        "product_scope": str(context.get("product_scope") or "").strip(),
        "empty_subset": True,
        "applied_filter": context.get("applied_filter") if isinstance(context.get("applied_filter"), dict) else None,
    }
    return {
        "intent": "query_products",
        "answer_type": "product_query",
        "answer": "上一轮在当前候选范围内未筛到符合条件的产品；结果为空，无法继续在这个空结果里比较、判断最轻或推荐替代。你可以放宽上一轮条件，或重新指定产品范围。",
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "sources": [{"type": "agent_meta", "candidate_context": empty_context}],
        "debug": {
            "agent_mode": "candidate_context_followup",
            "intent": {
                "intent": "recommend_products",
                "source_context": "previous_results",
                "target_skus": parent_skus,
            },
            "trace": {
                "stages": [{
                    "stage": "context_read",
                    "extra": {
                        "previous_result_skus_count": len(parent_skus),
                        "candidate_context_present": True,
                        "empty_subset": True,
                    },
                }],
            },
        },
        "skip_polish": True,
    }


def _asks_for_alternative_recommendation(question: str) -> bool:
    text = str(question or "")
    terms = (
        "\u6362\u4e00\u4e2a",
        "\u6362\u4e00\u6b3e",
        "\u6362\u4e2a",
        "\u518d\u63a8\u8350",
        "\u518d\u6765\u4e00\u4e2a",
        "\u53e6\u5916\u63a8\u8350",
        "\u8fd8\u6709\u5176\u4ed6\u63a8\u8350",
        "\u53e6\u4e00\u4e2a",
        "\u6709\u6ca1\u6709\u522b\u7684",
        "\u8fd8\u6709\u522b\u7684",
        "\u8fd8\u6709\u522b\u7684\u5417",
        "\u8fd8\u6709\u522b\u7684\u4e48",
        "\u522b\u7684\u5417",
        "\u522b\u7684\u4e48",
        "\u4e0d\u8981\u521a\u624d",
        "\u522b\u8981\u521a\u624d",
        "\u66ff\u4ee3",
        "\u66f4\u4fbf\u5b9c",
        "\u4fbf\u5b9c\u70b9",
        "\u7a0d\u5fae\u4fbf\u5b9c",
        "\u66f4\u8f7b",
        "\u66f4\u5408\u9002",
        "\u66f4\u9002\u5408",
    )
    return any(term in text for term in terms)


def _explicit_single_recommendation_request(question: str) -> bool:
    """Whether the customer has explicitly requested one recommendation."""
    return bool(re.search(
        r"(?:推荐|选|挑)\s*(?:一(?:个|款|口|套)|1(?:个|款|口|套))",
        str(question or ""),
    ))


def _asks_for_recommendation_explanation(question: str) -> bool:
    text = str(question or "")
    terms = (
        "\u4e3a\u4ec0\u4e48\u63a8\u8350",
        "\u63a8\u8350\u7406\u7531",
        "\u7406\u7531",
        "\u89e3\u91ca",
        "\u4f9d\u636e",
        "\u7b2c\u4e00\u4e2a",
        "\u7b2c\u4e00\u6b3e",
        "\u9996\u4e2a",
        "\u9996\u6b3e",
        "\u524d\u9762\u63a8\u8350\u7684",
        "\u521a\u624d\u63a8\u8350\u7684",
    )
    return any(term in text for term in terms)


def _is_recommendation_followup_question(question: str) -> bool:
    text = str(question or "")
    candidate_scope_terms = (
        "这些里",
        "这些里面",
        "里面哪些",
        "里面哪个",
        "上面这些",
        "其中哪个",
        "哪个更适合",
        "哪些支持",
    )
    budget_refinement = (
        any(term in text for term in ("预算不高", "预算有限", "预算低", "便宜点", "别太贵", "性价比"))
        and any(term in text for term in ("推荐", "来一个", "一款", "一个"))
    )
    return (
        _asks_for_alternative_recommendation(question)
        or _asks_for_recommendation_explanation(question)
        or any(term in text for term in candidate_scope_terms)
        or budget_refinement
    )


def _is_ordinal_compare_followup_question(question: str) -> bool:
    text = str(question or "")
    ordinal_terms = ("第一个", "第二个", "第三个", "第一款", "第二款", "第三款")
    compare_markers = ("比", "更", "哪个")
    compare_fields = ("轻", "重", "重量", "容量", "大", "小", "贵", "便宜", "价格")
    if not any(term in text for term in ordinal_terms):
        return False
    if not any(term in text for term in compare_markers):
        return False
    if not any(term in text for term in compare_fields):
        return False
    explanation_terms = ("推荐理由", "为什么推荐", "理由是什么", "解释一下为什么", "为什么选", "为什么是第一个")
    if any(term in text for term in explanation_terms):
        return False
    single_detail_patterns = (
        "多少钱",
        "价格定位",
        "容量是多少",
        "重量是多少",
        "材质是什么",
    )
    if any(term in text for term in single_detail_patterns):
        return False
    return True


def _is_contextual_candidate_comparison_question(question: str) -> bool:
    text = str(question or "")
    if not any(term in text for term in ("对比", "比较", "区别")):
        return False
    return any(term in text for term in ("这几款", "这些", "上面", "前面", "其中", "容量", "人群", "重量", "材质", "适合"))


def _has_followup_result_context(context: dict[str, Any] | None) -> bool:
    if not isinstance(context, dict):
        return False
    return bool(
        context.get("recommended_skus")
        or context.get("ordered_result_skus")
        or context.get("candidate_skus")
        or context.get("empty_subset")
    )


def _should_bypass_usage_care_and_faq_for_followup(
    db: Session,
    *,
    conversation_id: str | None,
    question: str,
    conversation_history: list[dict] | None = None,
) -> bool:
    if not conversation_id:
        return False
    recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
    candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    if not _is_recommendation_followup_question(question) and not (
        _is_ordinal_compare_followup_question(question)
        and (_has_followup_result_context(recommendation_context) or _has_followup_result_context(candidate_context))
    ):
        return False
    history_has_context = any(
        isinstance(item, dict) and item.get("role") == "assistant"
        for item in (conversation_history or [])
    )
    return bool(
        _has_followup_result_context(recommendation_context)
        or _has_followup_result_context(candidate_context)
        or history_has_context
    )


def _entities_from_agent_result(agent_result: dict, limit: int = 20) -> list[dict]:
    entities: list[dict] = []
    seen: set[str] = set()

    def add_entity(raw: dict, role: str, source: str) -> None:
        sku = str(raw.get("sku") or "").strip().upper()
        if not sku or "," in sku or sku in seen or len(entities) >= limit:
            return
        seen.add(sku)
        entities.append({
            "sku": sku,
            "name": raw.get("product_name_cn") or raw.get("product_name_en") or raw.get("name") or "",
            "turn": None,
            "role": role,
            "source": source,
        })

    primary_sku = str(agent_result.get("sku") or "").strip().upper()
    for item in agent_result.get("results") or []:
        if isinstance(item, dict):
            role = "current" if primary_sku and str(item.get("sku") or "").strip().upper() == primary_sku else "result"
            add_entity(item, role, "results")
    for action in agent_result.get("actions") or []:
        if isinstance(action, dict):
            add_entity(action, "current" if len(entities) == 0 else "result", "actions")
    if primary_sku and primary_sku not in seen:
        add_entity({"sku": primary_sku}, "current", "sku")
    return entities


def _order_skus_by_answer(skus: list[str], answer: str) -> list[str]:
    normalized = [
        str(sku or "").strip().upper()
        for sku in skus
        if str(sku or "").strip()
    ]
    if not normalized:
        return []
    positions: list[tuple[int, int, str]] = []
    for index, sku in enumerate(normalized):
        pos = str(answer or "").upper().find(sku)
        if pos >= 0:
            positions.append((pos, index, sku))
    if not positions:
        return normalized
    seen = {sku for _, _, sku in positions}
    ordered = [sku for _, _, sku in sorted(positions)]
    ordered.extend(sku for sku in normalized if sku not in seen)
    return ordered


def _steps_from_sources(sources_json: str | None) -> list[dict]:
    for source in _safe_json(sources_json, []):
        if isinstance(source, dict) and source.get("type") == "agent_steps":
            return source.get("steps") or []
    return []


def _message_meta(sources_json: str | None) -> dict:
    for source in _safe_json(sources_json, []):
        if isinstance(source, dict) and source.get("type") == "agent_meta":
            return source
    return {}


def _synchronize_context_read_trace(
    agent_result: dict | None,
    *,
    previous_result_skus: list[str] | None,
    recommendation_context: dict | None,
    candidate_context: dict | None,
) -> dict | None:
    if not isinstance(agent_result, dict):
        return agent_result
    debug = dict(agent_result.get("debug") or {})
    trace = dict(debug.get("trace") or {})
    stages = trace.get("stages")
    if not isinstance(stages, list):
        perf_state = customer_perf_service.get_state() or {}
        perf_stages = perf_state.get("stages")
        stages = list(perf_stages) if isinstance(perf_stages, list) else []
        trace["stages"] = stages

    updated = False
    synced_stages: list[dict] = []
    for stage in stages:
        if isinstance(stage, dict) and stage.get("stage") == "context_read" and not updated:
            merged_stage = dict(stage)
            merged_extra = dict(merged_stage.get("extra") or {})
            merged_extra.update(
                {
                    "previous_result_skus_count": len(previous_result_skus or []),
                    "recommendation_context_present": bool(recommendation_context),
                    "candidate_context_present": bool(candidate_context),
                }
            )
            merged_stage["extra"] = merged_extra
            synced_stages.append(merged_stage)
            updated = True
            continue
        synced_stages.append(stage)
    if not updated:
        synced_stages.append(
            {
                "stage": "context_read",
                "elapsed_ms": None,
                "extra": {
                    "previous_result_skus_count": len(previous_result_skus or []),
                    "recommendation_context_present": bool(recommendation_context),
                    "candidate_context_present": bool(candidate_context),
                },
            }
        )
    trace["stages"] = synced_stages
    debug["trace"] = trace
    agent_result["debug"] = debug
    return agent_result


def _normalize_agent_result(agent_result: dict) -> dict:
    result = dict(agent_result)
    results = result.get("results") or []
    warnings = result.get("warnings") or []
    debug = dict(result.get("debug") or {})
    debug_intent = debug.get("intent")
    parsed_intent = debug.get("parsed_intent") if isinstance(debug.get("parsed_intent"), dict) else None
    result.setdefault("answer_type", _answer_type_from_intent(result.get("intent")))
    result.setdefault("uncertainty", _uncertainty_from_answer(result.get("answer") or "", results, warnings, result.get("needs_clarification", False)))
    result.setdefault("evidence", _evidence_from_results(results))
    result.setdefault("followups", result.get("suggested_followups") or [])
    result.setdefault("suggested_followups", result.get("followups") or [])
    result.setdefault("agent_quality", {})
    if not isinstance(debug_intent, dict):
        debug["intent"] = dict(parsed_intent or {"intent": result.get("intent")})
    debug.setdefault("steps", result.get("steps") or [])
    debug.setdefault("warnings", warnings)
    debug.setdefault("anomalies", result.get("anomalies") or [])
    debug.setdefault("raw_results", results)
    result["debug"] = debug
    return result


def _should_retry_with_deterministic_agent(agent_result: dict | None) -> bool:
    if not agent_result:
        return False
    if agent_result.get("results") or _has_usable_agent_answer(agent_result):
        return False
    warnings = set(agent_result.get("warnings") or [])
    if "missing_product_results" in warnings:
        return True
    if agent_result.get("needs_clarification") and not agent_result.get("results"):
        return True
    return str(agent_result.get("confidence") or "").lower() == "low" and not agent_result.get("results")


def _has_usable_agent_answer(agent_result: dict) -> bool:
    answer = str(agent_result.get("answer") or "").strip()
    if not answer:
        return False
    unusable_markers = (
        "没有找到足够匹配",
        "没有找到匹配",
        "未找到匹配",
        "未找到足够",
        "no matching",
        "not enough matching",
    )
    normalized_answer = answer.lower()
    return not any(marker in normalized_answer for marker in unusable_markers)


def _prepare_deterministic_retry_result(retry_result: dict) -> dict:
    result = dict(retry_result)
    if result.get("results"):
        result["skip_polish"] = True
    return result


_FAQ_PURCHASE_TERMS = (
    "哪里买",
    "哪儿买",
    "在哪买",
    "在哪里买",
    "可以买到",
    "怎么买",
    "想买",
    "去哪里",
    "小程序",
    "商城",
    "购买链接",
    "购买渠道",
    "官方渠道",
    "哪个平台",
    "平台可以买",
    "店铺",
    "店铺入口",
    "下单",
    "官网吗",
    "官方店",
    "旗舰店",
    "淘宝",
    "天猫",
    "京东",
    "拼多多",
    "抖音",
    "亚马逊",
    "Amazon",
    "amazon",
    "独立站",
    "线下",
    "速卖通",
    "eBay",
    "ebay",
    "阿里国际站",
    "B2C",
    "b2c",
)
_FAQ_AFTERSALES_TERMS = (
    "售后",
    "退换",
    "退货",
    "换货",
    "保修",
    "质保",
    "人工客服",
    "发票",
    "物流",
    "快递",
    "订单",
    "发错货",
    "少发",
    "补寄",
    "维修",
    "七天无理由",
    "买错",
    "不喜欢",
    "开发票",
    "坏了怎么办",
    "有瑕疵怎么办",
)
_FAQ_AFTERSALES_PROBLEM_TERMS = ("问题", "质量", "坏了", "瑕疵", "破损")
_FAQ_AFTERSALES_HELP_TERMS = ("怎么办", "咋办", "怎么处理", "找谁", "谁处理", "联系谁")
_FAQ_COMPANY_TERMS = (
    "公司在哪里",
    "公司地址",
    "地址",
    "联系方式",
    "电话",
    "营业时间",
    "工作时间",
    "上班时间",
)
_FAQ_GREETING_TERMS = ("你好", "您好", "谢谢", "再见", "拜拜", "hello", "hi")
_USAGE_CARE_TERMS = (
    "怎么使用",
    "如何使用",
    "使用方法",
    "清洗",
    "保养",
    "护理",
    "清洁",
    "怎么洗",
    "怎么清洗",
    "怎么保养",
    "怎么护理",
    "怎么处理",
    "咋办",
    "不好洗",
    "洗碗机",
    "收拾",
    "擦干",
    "烘干",
    "泡水",
    "浸泡",
    "冷水",
    "冲洗",
    "冷水冲",
    "热锅骤冷",
    "骤冷骤热",
    "洗洁精",
    "钢丝球",
    "硬刷",
    "硬物",
    "刮擦",
    "水垢",
    "积碳",
    "异味",
    "第一次使用",
    "首次使用",
    "用完",
    "使用后",
    "收纳前",
    "不好清洗",
    "糊锅",
    "烧糊",
    "糊了",
    "焦",
    "粘锅",
    "不粘",
    "不沾",
    "涂层",
    "点不着",
    "打不着",
    "点着火",
    "点不起来",
    "点火",
    "怎么装",
    "如何装",
    "怎么安装",
    "如何安装",
    "安装方法",
    "安装视频",
    "装不上",
    "连接",
    "注意",
    "注意事项",
    "安全",
    "安全吗",
    "存放",
    "怎么存放",
    "气罐",
    "阀门",
    "酒精",
    "燃料",
    "炉头",
    "炉芯",
    "内胆",
    "没有管",
    "没管",
    "没有电",
    "没电",
    "能用多久",
    "可以用多久",
    "用多久",
    "能烧多久",
    "可以烧多久",
    "使用时长",
    "续航多久",
    "功率越大",
    "火力越大",
    "耗气",
    "消耗气",
    "燃料消耗",
    "节省时间",
    "节约时间",
    "不一定能节省",
    "不一定能节约",
)
_USAGE_CARE_PRODUCT_TERMS = ("锅", "锅具", "套锅", "炒锅", "煎锅", "单锅", "烤盘", "煎盘", "盘", "壶", "杯", "炉", "炉具", "酒精炉", "气炉", "气罐", "燃料", "酒精", "炉头", "炉芯", "内胆", "茶具", "套装")
_PURE_AFTERSALES_FLOW_TERMS = ("退换货", "退货", "换货", "售后电话", "保修多久", "质保多久", "联系客服", "售后联系方式")


def _is_customer_faq_question(question: str) -> bool:
    return _classify_customer_faq_intent(question) is not None


def _classify_customer_faq_intent(question: str) -> str | None:
    text = str(question or "").strip()
    if not text:
        return None
    if _is_product_usage_care_question(text):
        return None
    # A bare “你好，这个怎么使用？” is not a greeting/FAQ request.  There
    # is no product identity to ground an instruction answer, so let the
    # product runtime return a SKU/name clarification instead of the generic
    # greeting card (“我可以帮你查询产品资料…”).
    if _is_unbound_product_usage_question(text):
        return None
    normalized = customer_cache_service.normalize_text(text)
    if any(term in normalized for term in _FAQ_GREETING_TERMS):
        return "greeting"
    if _looks_like_purchase_channel_question(text) and not _looks_like_recommendation_request(text):
        return "purchase_channel"
    if any(term in text for term in _FAQ_AFTERSALES_TERMS):
        return "aftersales"
    if _is_unscoped_aftersales_help_request(text):
        return "aftersales"
    if any(term in text for term in _FAQ_COMPANY_TERMS):
        return "company_info"
    return None


def _is_unbound_product_usage_question(question: str) -> bool:
    """Recognize usage wording that lacks any product identity anchor."""
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("怎么使用", "怎么用", "如何使用", "使用方法")):
        return False
    return not (
        SKU_RE.search(text)
        or any(term in text for term in ("锅", "炉", "壶", "杯", "烤盘", "套装", "气罐", "酒精"))
    )


def _is_unbound_accessory_availability_question(question: str) -> bool:
    """Recognize a short accessory-inclusion/availability ask without a SKU."""
    text = str(question or "").strip()
    if not text or len(text) > 80:
        return False
    # “炉头推荐/哪款炉头” is a product-selection request, not a question
    # about whether an accessory is included or sold separately. Let the
    # semantic recommendation route own that decision instead of asking for
    # an identity slot prematurely.
    if any(term in text for term in ("推荐", "哪款", "哪个", "怎么选", "适合", "帮我选", "帮我挑")):
        return False
    subjects = (
        "气罐", "配件", "附件", "炉头", "点火器", "点火装置", "调节开关", "火力调节", "调节旋钮",
        "转接头", "转换头", "适配头", "管线", "连接管", "连接软管", "气管", "软管",
    )
    markers = (
        "有没有", "有吗", "是否有", "是否配有", "是否包含", "包含吗", "一起", "组合", "选择", "可配",
        "单独买", "单独购买", "单独卖", "单卖", "分开卖", "有卖", "卖吗", "卖么", "另购", "增配",
    )
    # Customers often shorten “点火器/点火装置” to “点火” when they ask
    # whether it can be combined with a regulating switch.  Treat that as
    # an accessory noun only in an accessory/combination context; a bare
    # ignition-how-to question remains on the usage-care route.
    shorthand_ignition_accessory = (
        "点火" in text
        and any(term in text for term in ("配件", "调节开关", "火力调节", "调节旋钮", "一起", "组合", "选择", "增配"))
    )
    if not any(subject in text for subject in subjects) and not shorthand_ignition_accessory:
        return False
    separate_sale_wording = bool(re.search(r"(?:单独|分开|另外|另有).{0,8}(?:卖|买|购)", text))
    return any(marker in text for marker in markers) or separate_sale_wording or bool(
        re.search(r"(?:气罐|配件|附件|炉头|点火器|点火装置|调节开关|火力调节|调节旋钮|转接头|转换头|适配头)\s*有[?？]?$", text)
    )


def _has_resolved_conversation_product_context(
    db: Session,
    *,
    user_id: str,
    conversation_id: str | None,
) -> bool:
    """Whether a follow-up has a previously sealed product/candidate scope.

    A conversation id alone is not product identity: the first turn of a new
    session can already carry an id supplied by a client.  Only reuse context
    when a prior assistant turn actually persisted a single product or a
    candidate/recommendation set.
    """
    if not conversation_id:
        return False
    if _latest_active_product_skus(db, conversation_id, user_id):
        return True
    candidate_context = _latest_candidate_context_for_sources(db, conversation_id)
    if isinstance(candidate_context, dict) and any(
        str(item or "").strip()
        for item in (
            candidate_context.get("candidate_skus")
            or candidate_context.get("ordered_result_skus")
            or candidate_context.get("original_candidate_skus")
            or candidate_context.get("parent_candidate_skus")
            or [],
        )
    ):
        return True
    recommendation_context = _latest_recommendation_context_for_sources(db, conversation_id)
    return isinstance(recommendation_context, dict) and any(
        str(item or "").strip()
        for item in (
            recommendation_context.get("recommended_skus")
            or recommendation_context.get("candidate_skus")
            or [],
        )
    )


def _is_unbound_fuel_compatibility_question(question: str) -> bool:
    """Require a product identity for a bare explicit-fuel capability ask."""
    text = str(question or "").strip()
    explicit_fuels = ("固体酒精", "固态酒精", "酒精块", "固体燃料", "液体酒精", "酒精燃料", "木炭", "炭火", "柴火", "煤炭")
    if not text or not any(term in text for term in explicit_fuels):
        return False
    if not any(term in text for term in ("能用", "可以用", "支持", "适配", "兼容", "能否", "可不可以", "可以吗", "能吗", "适合吗")):
        return False
    if _looks_like_recommendation_request(text):
        return False
    # Generic nouns such as ``酒精炉`` or ``这个炉子`` do not identify a
    # catalogue row.  The caller separately checks resolved page/request
    # anchors and named products before applying this clarification.
    return not (customer_agent_service._extract_skus(text) or SKU_RE.search(text))


def _is_unbound_environmental_fuel_safety_question(question: str) -> bool:
    text = str(question or "").strip()
    return bool(
        any(term in text for term in ("帐篷", "密闭空间", "封闭空间", "一氧化碳"))
        and any(term in text for term in ("酒精炉", "液体酒精", "固体酒精", "燃气炉", "炉具", "明火"))
    )


def _looks_like_unbound_canister_compatibility_question(question: str) -> bool:
    """Recognize a gas-canister connection/adapter ask without an identity.

    A phrase such as “这个用哪个气罐，需要转接头吗” contains a catalogue noun
    and a recommendation verb, but the customer is primarily asking whether the
    interface is compatible.  Keep that compatibility concern ahead of the
    gas-canister catalogue route unless a concrete product anchor is present.
    """
    text = str(question or "").strip()
    if not text or not any(term in text for term in ("气罐", "气瓶", "燃气罐", "通用罐")):
        return False
    compatibility_terms = (
        "能不能接", "能否接", "能接", "接上", "连接", "接口", "转接头", "转换头",
        "适配头", "适配", "兼容", "通用气罐", "需要转接", "配转接",
    )
    if not any(term in text for term in compatibility_terms):
        return False
    # “连接时要注意什么” asks for general operating/safety guidance; it
    # does not claim a specific canister is compatible with a specific stove.
    # Keep the identity gate for concrete yes/no or adapter decisions.
    if any(term in text for term in ("注意什么", "要注意", "注意事项", "怎么连接", "如何连接", "连接时")):
        return False
    return not (customer_agent_service._extract_skus(text) or SKU_RE.search(text))


def _looks_like_home_or_electric_stove_safety_question(question: str) -> bool:
    """Recognize a safety/compatibility scope, not a product browse request."""
    text = str(question or "").strip()
    if not text:
        return False
    environment_terms = ("家里", "家中", "室内", "电磁炉", "电池炉", "电陶炉", "电热炉")
    safety_predicates = (
        "不适合", "不建议", "不能", "不可", "不能放", "不能用", "能不能", "可以吗", "能用吗", "适合吗",
    )
    if not any(term in text for term in environment_terms):
        return False
    if not any(term in text for term in safety_predicates):
        return False
    # “推荐适合家里用的炉具” is a genuine browse request; a negative or
    # compatibility predicate is required before this safety boundary wins.
    return not any(term in text for term in ("推荐", "哪款", "哪个", "怎么选", "帮我选", "帮我挑")) or any(
        term in text for term in ("不适合", "不建议", "不能", "不可", "不能放", "不能用")
    )


def _looks_like_category_availability_question(question: str) -> bool:
    """Recognize a bare category availability ask for card-scope hygiene."""
    text = str(question or "").strip()
    if not text or SKU_RE.search(text):
        return False
    if not any(term in text for term in ("有没有", "有吗", "有无", "有卖", "卖吗", "还有吗", "没有")):
        return False
    if any(term in text for term in ("推荐", "适合", "哪款", "哪个", "怎么选", "怎么用", "里面", "包括", "包含", "套装")):
        return False
    return bool(_specific_category_availability_subject(text)) or any(
        term in text
        for term in ("杯子", "水杯", "水壶", "锅具", "炉具", "炉子", "餐具", "配件", "收纳包", "收纳袋")
    )


def _specific_category_availability_subject(question: str) -> str:
    """Extract a specific product subtype from a short availability ask."""
    text = str(question or "").strip().strip("?？!！。 ")
    if not text or SKU_RE.search(text):
        return ""
    subject = ""
    for pattern in (
        r"^(?:有没有|有无|有卖)\s*(?P<subject>[A-Za-z0-9\u4e00-\u9fff-]{2,20}?)(?:卖|销售)?$",
        r"^(?P<subject>[A-Za-z0-9\u4e00-\u9fff-]{2,20}?)(?:有吗|有卖吗|卖吗|卖么|还有吗)$",
    ):
        match = re.fullmatch(pattern, text)
        if match:
            subject = str(match.group("subject") or "").strip()
            break
    if not subject or subject.startswith(("什么", "其他", "其它", "别的", "适合")):
        return ""
    # Selection qualifiers belong to the recommendation route.  Keeping them
    # in the availability subject would turn “有没有推荐的卡式炉” into a
    # literal product subtype named “推荐的卡式炉” and emit a false no-match.
    if subject.startswith("推荐"):
        return ""
    if any(term in text for term in ("单独", "单卖", "配件卖", "点火器", "调节开关", "管线", "连接管")):
        return ""
    if subject in {
        "杯", "杯子", "水杯", "水具", "水壶", "壶", "锅", "锅具", "炉", "炉具", "炉子",
        "餐具", "配件", "附件", "收纳包", "收纳袋", "商品", "产品",
    }:
        return ""
    return subject if subject.endswith(("杯", "壶", "锅", "炉", "盘", "袋", "包", "配件", "餐具")) else ""


def _looks_like_page_current_item_availability_question(question: str) -> bool:
    """Recognize a current-page component availability ask.

    The page marker supplies identity; the availability/component vocabulary
    supplies intent.  Explicit browse language still belongs to the
    category-recommendation route, so this boundary cannot swallow a genuine
    multi-product request.
    """
    text = str(question or "").strip()
    if not text or "当前商品：" not in text:
        return False
    # Only the customer's predicate before the page marker defines the
    # availability target.  Product nouns inside the page title (for example
    # “水壶” or “锅具”) must not turn a fuel/material question into a
    # component-availability request.
    predicate = text.split("当前商品：", 1)[0].strip()
    if not any(term in predicate for term in ("有没有", "有吗", "有无", "没有")):
        return False
    if not any(term in predicate for term in ("炉头", "炉芯", "配件", "水壶", "锅", "炉具")):
        return False
    return not any(term in predicate for term in ("推荐", "哪些", "哪款", "哪个", "其他", "还有"))


def _is_plural_catalogue_recommendation_request(question: str) -> bool:
    text = str(question or "").strip()
    if not text or any(term in text for term in ("哪个", "哪款", "优先选", "该选", "还是")):
        return False
    return any(term in text for term in ("有哪些", "哪些", "哪一些", "什么产品", "什么锅", "几款", "几种"))


def _looks_like_recommendation_request(text: str) -> bool:
    value = str(text or "").strip()
    if not value:
        return False
    if customer_agent_intent_service._looks_like_fuel_storage_comparison_question(value):
        return False
    if _looks_like_home_or_electric_stove_safety_question(value):
        return False
    if any(term in value for term in ("有哪些", "哪些", "哪一些", "找")) and not any(
        term in value for term in ("推荐", "哪款", "选什么", "帮我选", "帮我挑", "推荐一个", "推荐几个")
    ):
        if any(term in value for term in ("轻便", "轻量", "不要太重", "比较轻", "预算低", "预算不高", "便宜", "新手", "徒步", "卡式炉", "酒精炉", "双人", "两个人", "两人")):
            return False
    if _contains_explicit_negative_product_marker(value):
        if any(term in value for term in ("推荐", "推荐一下", "推荐一个", "推荐几个", "有什么", "有哪些")):
            return True
    explicit_recommendation_terms = ("推荐", "哪款", "选什么", "用什么", "帮我选", "帮我挑", "合适", "适合")
    recommendation_terms = (*explicit_recommendation_terms, "哪个")
    product_terms = ("锅", "套锅", "单锅", "烤盘", "煎盘", "炉", "炉具", "酒精炉", "壶", "水壶", "餐具", "套装", "挡风板", "防风板", "炉芯", "炉心", "内胆", "气罐", "收纳包", "收纳袋", "配件")
    cookware_terms = ("锅", "锅具", "单锅", "套锅")
    scenario_terms = ("野餐", "露营", "徒步", "爬山", "公园", "周末", "两个人", "三个人", "一个人", "轻便", "轻量")
    open_purchase_decision_terms = ("想买", "买个", "买口", "买套", "买一套", "买哪个", "该买", "应该买")
    if (
        not _looks_like_purchase_channel_question(value)
        and any(term in value for term in open_purchase_decision_terms)
        and any(term in value for term in product_terms)
        and (any(term in value for term in scenario_terms) or any(term in value for term in recommendation_terms))
    ):
        return True
    if (
        any(term in value for term in ("只想买", "只买", "只要", "其他不要", "不要其他"))
        and any(term in value for term in product_terms)
        and not _looks_like_purchase_channel_question(value)
    ):
        return True
    if _looks_like_purchase_channel_question(value) and not any(term in value for term in explicit_recommendation_terms):
        return False
    single_person_terms = ("一个人用", "一人用", "单人用", "1人用", "1-2人用", "1－2人用", "适合一个人", "适合一人", "适合单人")
    open_purchase_terms = ("想买", "买个", "买口", "推荐", "适合", "那种", "有没有", "帮我选", "帮我挑")
    if (
        not SKU_RE.search(value)
        and "「" not in value
        and "」" not in value
        and any(term in value for term in cookware_terms)
        and any(term in value for term in single_person_terms)
        and any(term in value for term in open_purchase_terms)
    ):
        return True
    if any(term in value for term in ("轻便", "轻量", "不要太重", "预算低", "预算不高", "预算低一点", "便宜点", "推荐一个", "推荐几个")) and any(
        term in value for term in ("水杯", "杯", "钛杯", "水壶", "锅具", "锅", "炉具", "炉子", "装备")
    ):
        return True
    if any(term in value for term in ("公园", "野餐", "徒步", "露营")) and any(term in value for term in ("带什么", "装备", "轻便")):
        return True
    return any(term in value for term in recommendation_terms) and any(term in value for term in product_terms)


def _looks_like_purchase_channel_question(text: str) -> bool:
    value = str(text or "").strip()
    if not value:
        return False
    # ``怎么买``/``买到`` also occur in ordinary product selection questions
    # (e.g. “杯子怎么买”“燃料酒精随处能买到吗”).  Those must stay on the
    # product/fuel semantic route; otherwise the FAQ fast path exposes the
    # internal listing-channel table instead of answering the customer's need.
    explicit_channel_terms = (
        "购买渠道",
        "销售渠道",
        "购买链接",
        "商品链接",
        "有链接",
        "链接吗",
        "官网",
        "官方店",
        "旗舰店",
        "淘宝",
        "京东",
        "店铺",
        "下单入口",
    )
    if any(term in value for term in explicit_channel_terms):
        return True
    if any(term in value for term in ("哪里买", "去哪买", "去哪儿买", "哪里可以买", "在哪买", "在哪里买")):
        return True
    if any(term in value for term in ("燃料", "酒精", "医用", "气罐", "炉具", "炉子", "水壶", "杯子", "锅具", "锅")):
        return False
    return False


def _is_unscoped_aftersales_help_request(text: str) -> bool:
    if not text or SKU_RE.search(text):
        return False
    if _is_product_usage_care_question(text):
        return False
    has_problem_signal = any(term in text for term in _FAQ_AFTERSALES_PROBLEM_TERMS)
    has_help_signal = any(term in text for term in _FAQ_AFTERSALES_HELP_TERMS)
    return has_problem_signal and has_help_signal


def _is_product_usage_care_question(question: str) -> bool:
    text = str(question or "").strip()
    if not text:
        return False
    if any(term in text for term in _PURE_AFTERSALES_FLOW_TERMS):
        return False
    # A general fuel-definition/availability question is educational usage
    # guidance even when it contains purchase wording such as “哪里能买到”.
    # Keep it out of the catalogue/purchase-channel route so the answer can
    # explain the fuel-vs-medical-alcohol boundary without inventing a SKU.
    if customer_agent_intent_service._looks_like_general_fuel_definition_question(text):
        return True
    # A purchase-channel ask about a consumable (for example, where to buy
    # 95% alcohol) is not a request for stove usage guidance.  Let the FAQ /
    # purchase-channel path answer it without inheriting catalogue results.
    if _looks_like_purchase_channel_question(text) and not _looks_like_recommendation_request(text):
        return False
    if customer_agent_intent_service._looks_like_water_container_capability_question(text):
        return False
    # Compatibility asks need an explicit confirmed/unconfirmed conclusion;
    # do not let the formal heat_source field heuristic downgrade them to a
    # bare product-detail field response.
    if _looks_like_compatibility_usage_question(text):
        return True
    if customer_agent_intent_service._looks_like_power_tradeoff_question(text):
        return True
    if customer_agent_intent_service._looks_like_utensil_material_safety_question(text):
        return True
    if customer_agent_intent_service._looks_like_contents_grounding_question(text):
        # “包含/里面有什么” is a package-composition question, not usage
        # care.  Only keep it here when the same sentence also carries an
        # explicit compatibility predicate (for example, whether a canister
        # inside a set is usable); otherwise page identity must remain on the
        # composition/detail route or be clarified without catalogue fan-out.
        return _looks_like_compatibility_usage_question(text)
    # The intent service evaluates the full cleaning/care utterance.  A local
    # detail-field heuristic may see a noun such as "冷水" but must not erase
    # that already-recognized usage/care meaning when no formal non-usage
    # field contract is present.
    if (
        customer_agent_intent_service._looks_like_usage_care_question(text)
        and not _has_non_usage_care_field_contract(text)
        and not _should_preserve_recommendation_priority(text)
    ):
        return True
    if _looks_like_product_detail_field_question(text):
        return False
    if _should_preserve_recommendation_priority(text):
        return False
    matched_usage_terms = [term for term in _USAGE_CARE_TERMS if term in text]
    if not matched_usage_terms:
        return False
    # Product nouns such as “炉头/气罐/酒精” are not usage intent by
    # themselves.  Without an action or decision predicate, a page question
    # like “是一个炉头？” or “包含酒精吗？” can be mistaken for usage care,
    # bypass the unresolved-page guard, and fan out into unrelated catalogue
    # results.  Keep this language-level boundary broad enough for natural
    # wording without binding it to a SKU or category exception.
    usage_action_markers = (
        "怎么", "如何", "能不能", "能否", "可以", "是否", "支持", "适合", "安全",
        "安装", "连接", "使用", "用", "烧", "燃烧", "放", "装", "加热", "点火",
        "存放", "多久", "漏", "拧紧", "扭紧",
    )
    if not any(marker in text for marker in usage_action_markers):
        return False
    has_product_context = bool(
        customer_agent_service._extract_skus(text)
        or SKU_RE.search(text)
        or any(term in text for term in _USAGE_CARE_PRODUCT_TERMS)
    )
    has_script_context = "客服怎么回复" in text or "怎么回复客户" in text or "客户说" in text
    return has_product_context or has_script_context or len(matched_usage_terms) >= 2


def _looks_like_compatibility_usage_question(question: str) -> bool:
    """Keep compatibility questions on the usage/safety route.

    A formal ``heat_source`` field contract is useful for ordinary product
    detail questions, but phrases such as "can this gas canister connect" ask
    for an explicit compatibility conclusion.  Letting Phase 2 entity detail
    arbitration win here produces a bare field dump and hides the required
    confirmed/unconfirmed answer.  This predicate is intentionally semantic
    (subject + compatibility marker), not SKU-specific.
    """
    value = str(question or "").strip()
    if not value:
        return False
    # Keep one compatibility ontology for the usage-care and page routes.
    # This covers marketplace phrasing such as “普通那种气罐” and
    # “燃气罐可以增配” in addition to the shorter “能不能接”.
    return (
        customer_agent_intent_service._looks_like_canister_compatibility_question(value)
        or customer_agent_intent_service._looks_like_canister_storage_fit_question(value)
        or customer_agent_intent_service._looks_like_fuel_compatibility_usage_question(value)
    )


def _should_preserve_recommendation_priority(text: str) -> bool:
    value = str(text or "").strip()
    if not value or not _looks_like_recommendation_request(value):
        return False
    explicit_sku = bool(SKU_RE.search(value))
    clear_usage_care_terms = (
        "清洗",
        "清洁",
        "保养",
        "护理",
        "怎么洗",
        "怎么清洗",
        "怎么保养",
        "怎么护理",
        "洗碗机",
        "泡水",
        "浸泡",
        "钢丝球",
        "刮擦",
        "使用限制",
        "注意事项",
        "禁忌",
    )
    return not (explicit_sku and any(term in value for term in clear_usage_care_terms))


def _looks_like_product_detail_field_question(text: str) -> bool:
    value = str(text or "")
    if not value:
        return False
    field_contract = customer_field_contract.detect_field_contract(value)
    if field_contract:
        return customer_field_contract.is_supported_detail_field(field_contract.field_type)
    product_terms = ("套锅", "单锅", "酒精炉", "小方锅", "炊墨", "行山", "旋焰", "烽宴", "CW-", "CS-", "TW-")
    field_terms = (
        "有没有不粘涂层", "有涂层吗", "有没有涂层", "是不是304", "是不是不锈钢", "是不是木头",
        "手柄", "把手", "锅体", "锅盖", "盖子", "煎盘", "材质", "尺寸", "容量", "重量", "净重",
        "热源", "燃料", "支持酒精炉", "用酒精炉", "适用人群", "适合哪些人群", "适合几人", "适合几个人",
        "几个人", "几人", "人数", "洗碗机", "说明书", "官方说明书", "保修", "质保", "安装视频", "视频教程",
        "库存", "现货", "销量", "评价", "价格", "认证", "BPA", "使用限制", "冷水", "热水", "补水", "烧水"
    )
    if not any(term in value for term in product_terms) or not any(term in value for term in field_terms):
        return False
    if "洗碗机" in value and any(term in value for term in ("能放", "放进", "可以放", "适合", "能不能放")):
        return True
    usage_action_terms = ("清洗", "清洁", "保养", "护理", "怎么洗", "怎么清洗", "糊锅", "糊了", "烧糊", "刮擦", "钢丝球", "泡水", "浸泡")
    return not any(term in value for term in usage_action_terms)


async def _answer_customer_faq_fast_path(db: Session, question: str, intent: str) -> dict | None:
    cache_key = customer_cache_service.make_key("faq_fast_path", id(db), intent, customer_cache_service.normalize_text(question))
    cached = customer_cache_service.faq_cache.get(cache_key)
    if cached is not None:
        return cached

    if intent == "greeting":
        result = _build_customer_faq_result(
            question=question,
            intent=intent,
            answer="你好，我可以帮你查询产品资料、购买渠道、售后和公司信息。",
            sources=[{"type": "faq", "label": "问候快捷回复"}],
            confidence="high",
        )
        customer_cache_service.faq_cache.set(cache_key, result)
        return result

    structured = _lookup_structured_faq_answer(db, question, intent)
    if structured:
        customer_cache_service.faq_cache.set(cache_key, structured)
        return structured

    knowledge_result = await _lookup_faq_from_knowledge(db, question, intent)
    if knowledge_result:
        customer_cache_service.faq_cache.set(cache_key, knowledge_result)
        return knowledge_result

    result = _build_customer_faq_result(
        question=question,
        intent=intent,
        answer="目前系统里暂未配置具体购买渠道/售后联系方式，建议联系人工客服确认。我可以继续帮你查询产品材质、规格、适用场景等资料。",
        sources=[{"type": "faq", "label": "未配置 FAQ 资料"}],
        confidence="low",
        uncertainty="faq_not_configured",
        warnings=["faq_data_missing"],
    )
    customer_cache_service.faq_cache.set(cache_key, result)
    return result


def _lookup_structured_faq_answer(db: Session, question: str, intent: str) -> dict | None:
    if intent == "purchase_channel":
        channels = product_service.get_listing_channels(db)
        items = []
        for channel in channels[:8]:
            name = getattr(channel, "channel_name", None)
            if not name:
                continue
            items.append(f"{name}{('（' + channel.channel_code + '）') if getattr(channel, 'channel_code', None) else ''}")
        if not items:
            return None
        # Keep the response customer-facing.  Channel codes and the phrase
        # “系统里记录” are implementation metadata; only expose the usable
        # channel names when the customer explicitly asks for channels.
        visible_channels = []
        for item in items[:8]:
            visible_channels.append(str(item).split("（", 1)[0].strip())
        answer = "目前可核对到的购买渠道包括：" + "、".join(visible_channels) + "。"
        return _build_customer_faq_result(
            question=question,
            intent=intent,
            answer=answer,
            sources=[{"type": "structured_faq", "label": "销售渠道", "count": len(items)}],
            confidence="high",
            answer_type="faq",
            agent_mode="purchase_channel_fast_path",
            result_skus=[],
        )
    if intent == "aftersales":
        return _build_customer_faq_result(
            question=question,
            intent=intent,
            answer="目前系统里暂未配置可直接公开的售后电话。建议通过购买渠道的店铺客服或官方客服入口联系人工售后确认，我也可以继续帮你查询产品材质、规格和使用说明。",
            sources=[{"type": "structured_faq", "label": "售后联系方式未配置", "count": 0}],
            confidence="high",
            answer_type="faq",
            agent_mode="customer_faq_fast_path",
            result_skus=[],
        )
    return None


async def _lookup_faq_from_knowledge(db: Session, question: str, intent: str) -> dict | None:
    category_map = {
        "purchase_channel": ("purchase_channel", "faq", "company_info"),
        "aftersales": ("aftersales", "faq"),
        "company_info": ("company_info", "faq"),
    }
    categories = category_map.get(intent)
    if not categories:
        return None
    cache_key = customer_cache_service.make_key("faq_knowledge", id(db), intent, customer_cache_service.normalize_text(question))
    cached = customer_cache_service.faq_cache.get(cache_key)
    if cached is not None:
        return cached
    limit = 3
    try:
        rows = await knowledge_service.semantic_retrieve(db, question, limit=limit)
    except Exception:
        rows = knowledge_service.keyword_retrieve(db, question, limit=limit)
    filtered_rows = []
    for row in rows[:limit]:
        metadata = row.get("metadata") if isinstance(row, dict) else {}
        source_type = str(row.get("source_type") or "").strip().lower() if isinstance(row, dict) else ""
        category = str(metadata.get("category") or metadata.get("type") or source_type).strip().lower()
        content = str(row.get("content") or "").strip() if isinstance(row, dict) else ""
        if category in categories or any(term in content for term in categories):
            filtered_rows.append(row)
    if not filtered_rows:
        return None
    snippets = []
    for row in filtered_rows[:3]:
        content = str(row.get("content") or "").strip()
        if content:
            snippets.append(content[:220])
    if not snippets:
        return None
    if intent == "purchase_channel":
        answer = "根据系统里已有资料，可参考：" + "；".join(snippets)
    elif intent == "aftersales":
        answer = "系统里记录的售后资料如下：" + "；".join(snippets)
    else:
        answer = "系统里记录的公司信息如下：" + "；".join(snippets)
    result = _build_customer_faq_result(
        question=question,
        intent=intent,
        answer=answer[:3000],
        sources=[{"type": "faq_knowledge", "label": intent, "count": len(filtered_rows), "results": filtered_rows[:3]}],
        confidence="medium",
        answer_type="faq",
        agent_mode="customer_faq_fast_path",
        result_skus=[],
    )
    customer_cache_service.faq_cache.set(cache_key, result)
    return result


def _build_customer_faq_result(
    *,
    question: str,
    intent: str,
    answer: str,
    sources: list[dict],
    confidence: str = "high",
    answer_type: str = "faq",
    agent_mode: str = "customer_faq_fast_path",
    uncertainty: str = "resolved",
    warnings: list[str] | None = None,
    result_skus: list[str] | None = None,
) -> dict:
    result_skus = result_skus or []
    warnings = warnings or []
    answer = _shape_faq_answer(answer)
    result = {
        "answer": answer,
        "intent": intent,
        "answer_type": answer_type,
        "confidence": confidence,
        "uncertainty": uncertainty,
        "needs_clarification": False,
        "anomalies": [],
        "suggested_followups": [],
        "followups": [],
        "evidence": [],
        "sources": sources,
        "actions": [],
        "results": [],
        "steps": [{"type": "faq_fast_path", "label": "FAQ 快速路径", "detail": intent, "ok": True}],
        "warnings": warnings,
        "sku": None,
        "result_skus": result_skus,
        "debug": {
            "agent_mode": agent_mode,
            "intent": intent,
            "question": question,
            "steps": [{"type": "faq_fast_path", "label": "FAQ 快速路径", "detail": intent, "ok": True}],
            "warnings": warnings,
            "anomalies": [],
            "raw_results": [],
            "tool_results": [],
        },
        "skip_polish": True,
    }
    quality = customer_agent_quality_service.evaluate_agent_response(
        question,
        answer=result["answer"],
        intent=result["intent"],
        results=result["results"],
        sources=result["sources"],
        actions=result["actions"],
        warnings=result["warnings"],
        needs_clarification=result["needs_clarification"],
        direct_answer=True,
        tool_results=[],
    )
    result["agent_quality"] = quality
    result["debug"]["agent_quality"] = quality
    return result


def _shape_faq_answer(answer: str) -> str:
    value = str(answer or "").strip()
    if not value:
        return ""
    value = re.sub(r"我也可以继续帮你.*$", "", value).strip()
    value = re.sub(r"\s+", " ", value).strip()
    sentences = [part.strip(" 。；;") for part in re.split(r"[。！？!?；;]+", value) if part.strip()]
    kept: list[str] = []
    for sentence in sentences:
        if any(term in sentence for term in ("产品材质", "规格", "适用场景", "推荐")):
            continue
        kept.append(sentence)
        if len(kept) >= 2:
            break
    if not kept and sentences:
        kept = sentences[:2]
    return "。".join(kept) + "。"


def _sku_identity_key(value: str | None) -> str:
    """Normalize a displayed SKU to its stable base identity for alignment."""
    text = str(value or "").strip().upper()
    return re.sub(r"[（(][^）)]*[）)]$", "", text).strip()


def _align_answer_named_result_cards(result: dict) -> dict:
    """Keep verified rows named in the answer represented by visible cards.

    This only restores identities already present in the result packet (or its
    raw result packet).  It never turns a textual SKU into a new product hit.
    """
    if not isinstance(result, dict):
        return result
    # Clarification candidates are resolver/diagnostic options, not selected
    # catalogue results.  Do not rehydrate their raw rows into public cards.
    if (
        str(result.get("answer_type") or "").strip() == "clarification"
        or result.get("needs_clarification") is True
    ):
        return result
    answer_skus = [
        str(item or "").strip().upper()
        for item in customer_agent_service._extract_skus(str(result.get("answer") or ""))
        if str(item or "").strip()
    ]
    rows = [row for row in (result.get("results") or []) if isinstance(row, dict)]
    if rows:
        answer_text = str(result.get("answer") or "")
        name_positions: list[tuple[int, str]] = []
        for row in rows:
            sku = str(row.get("sku") or "").strip().upper()
            names = [
                str(row.get(key) or "").strip()
                for key in ("product_name_cn", "product_name_en")
                if str(row.get(key) or "").strip()
            ]
            positions = [answer_text.find(name) for name in names if answer_text.find(name) >= 0]
            if sku and positions:
                name_positions.append((min(positions), sku))
        if len(name_positions) == len(rows) and len({position for position, _ in name_positions}) == len(rows):
            answer_skus = [sku for _, sku in sorted(name_positions)]
    if not answer_skus:
        return result
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    raw_rows = [row for row in (debug.get("raw_results") or []) if isinstance(row, dict)]
    row_by_identity: dict[str, tuple[str, dict]] = {}
    for row in (*rows, *raw_rows):
        sku = str(row.get("sku") or "").strip().upper()
        identity = _sku_identity_key(sku)
        if not sku or not identity:
            continue
        # A base identity is usable only when it maps to one verified row;
        # otherwise a shortened display token would be ambiguous.
        previous = row_by_identity.get(identity)
        if previous and previous[0] != sku:
            row_by_identity.pop(identity, None)
        elif identity not in row_by_identity:
            row_by_identity[identity] = (sku, row)
    if not row_by_identity:
        return result
    current_skus = [
        str(item or "").strip().upper()
        for item in (result.get("result_skus") or [])
        if str(item or "").strip()
    ]
    current_identity = {_sku_identity_key(sku) for sku in current_skus}
    existing_result_identity = {
        _sku_identity_key(str(row.get("sku") or "").strip().upper())
        for row in rows
        if str(row.get("sku") or "").strip()
    }
    restored_rows: list[dict] = []
    for answer_sku in answer_skus:
        identity = _sku_identity_key(answer_sku)
        matched = row_by_identity.get(identity)
        if matched and identity not in existing_result_identity:
            restored_rows.append(matched[1])
            existing_result_identity.add(identity)
    if restored_rows:
        result["results"] = [*rows, *restored_rows]
        rows = list(result["results"])
    # When every visible result identity is explicitly named, the prose order
    # is the customer-facing order. Keep result_skus and cards in that order so
    # “first/second”, replacement context and API evidence cannot disagree.
    named_current_skus: list[str] = []
    for answer_sku in answer_skus:
        matched = row_by_identity.get(_sku_identity_key(answer_sku))
        if matched and matched[0] in current_skus and matched[0] not in named_current_skus:
            named_current_skus.append(matched[0])
    if current_skus and set(named_current_skus) == set(current_skus):
        row_by_sku = {
            str(row.get("sku") or "").strip().upper(): row
            for row in rows
            if str(row.get("sku") or "").strip()
        }
        result["result_skus"] = named_current_skus
        result["results"] = [row_by_sku[sku] for sku in named_current_skus if sku in row_by_sku]
        result["candidate_skus"] = list(named_current_skus)
        current_skus = list(named_current_skus)
        rows = list(result["results"])
    additions: list[str] = []
    for answer_sku in answer_skus:
        identity = _sku_identity_key(answer_sku)
        matched = row_by_identity.get(identity)
        if not matched or identity in current_identity:
            continue
        additions.append(matched[0])
        current_identity.add(identity)
    if not additions:
        return result
    ordered_skus = [*current_skus, *additions]
    result["result_skus"] = ordered_skus
    existing_result_skus = {
        str(row.get("sku") or "").strip().upper()
        for row in rows
        if str(row.get("sku") or "").strip()
    }
    result["results"] = [*rows]
    for sku in additions:
        if sku in existing_result_skus:
            continue
        matched = row_by_identity.get(_sku_identity_key(sku))
        if matched:
            result["results"].append(matched[1])
            existing_result_skus.add(sku)
    result["candidate_skus"] = [
        str(item or "").strip().upper()
        for item in (result.get("candidate_skus") or [])
        if str(item or "").strip()
    ]
    for sku in additions:
        if sku not in result["candidate_skus"]:
            result["candidate_skus"].append(sku)
    return result


def _align_recommendation_result_cards(result: dict) -> dict:
    """Keep visible recommendation cards aligned with a grounded narrative.

    A validated narrative can mention several ranked candidates while an
    older post-filter leaves only the first row in ``results``.  If the other
    named candidates are present in the database-backed raw result packet and
    in the selected evidence, restore those cards; never expand from names
    that lack a matching verified row.
    """
    if not isinstance(result, dict) or str(result.get("answer_type") or "").strip() != "recommendation":
        return result
    answer = str(result.get("answer") or "")
    if not answer:
        return result
    rows: list[dict] = [row for row in (result.get("results") or []) if isinstance(row, dict)]
    debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
    raw_rows = [row for row in (debug.get("raw_results") or []) if isinstance(row, dict)]
    row_by_sku: dict[str, dict] = {}
    for row in (*rows, *raw_rows):
        sku = str(row.get("sku") or "").strip().upper()
        if sku and sku not in row_by_sku:
            row_by_sku[sku] = row
    if len(row_by_sku) < 2:
        return result
    evidence_skus = {
        str(item.get("sku") or "").strip().upper()
        for item in (result.get("evidence") or [])
        if isinstance(item, dict) and str(item.get("sku") or "").strip()
    }
    if not evidence_skus:
        return result
    answer_upper = answer.upper()
    named_rows = [
        row for sku, row in row_by_sku.items()
        if sku in evidence_skus
        and (
            sku in answer_upper
            or any(
                name and name.upper() in answer_upper
                for name in (
                    str(row.get("product_name_cn") or "").strip(),
                    str(row.get("product_name_en") or "").strip(),
                )
            )
        )
    ]
    if len(named_rows) < 2:
        return result
    ordered_rows: list[dict] = []
    seen: set[str] = set()
    for row in (*rows, *named_rows):
        sku = str(row.get("sku") or "").strip().upper()
        if sku in {str(item.get("sku") or "").strip().upper() for item in named_rows} and sku not in seen:
            ordered_rows.append(row_by_sku[sku])
            seen.add(sku)
    if len(ordered_rows) < 2:
        return result
    result["results"] = ordered_rows
    result["result_skus"] = [str(row.get("sku") or "").strip().upper() for row in ordered_rows]
    result["candidate_skus"] = list(result["result_skus"])
    return result


def _shape_answer_for_output(result: dict) -> dict:
    metadata_evidence = _field_metadata_display_evidence(result)
    metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
    is_sealed_field_detail = bool(
        str(result.get("answer_type") or "").strip() == "product_detail"
        and str(metadata.get("answer_policy") or "").strip() in {"field_only", "field_only_missing"}
    )
    is_safe_missing_field_detail = bool(
        str(result.get("answer_type") or "").strip() == "product_detail"
        and str(metadata.get("contract_field_type") or "").strip()
        and str(metadata.get("evidence_status") or "").strip() in {"missing", "insufficient"}
        and not str(metadata.get("evidence_value") or "").strip()
    )
    is_sealed_same_sku_rag = bool(
        str(result.get("answer_type") or "").strip() == "product_detail"
        and str(metadata.get("contract_field_type") or "").strip() == "product_qa"
        and str(metadata.get("evidence_source") or "").strip() == "same_sku_knowledge"
        and str(metadata.get("evidence_sku") or "").strip()
    )
    is_product_qa_detail = bool(
        str(result.get("answer_type") or "").strip() == "product_detail"
        and (
            str(metadata.get("contract_field_type") or "").strip() == "product_qa"
            or str(metadata.get("evidence_source") or "").strip().startswith("product_qa:")
        )
    )
    # A sealed field answer must show the evidence that supports that exact
    # field, not an arbitrary capacity/material/color summary from the product
    # result row.  Product-row evidence remains appropriate for search,
    # comparison, and recommendation answers where multiple facts are asked.
    derived_result_evidence = [] if (is_sealed_field_detail or is_safe_missing_field_detail or is_product_qa_detail) else _evidence_from_results(result.get("results") or [])
    if is_sealed_field_detail or is_safe_missing_field_detail:
        explicit_evidence = []
    elif is_product_qa_detail:
        evidence_sku = str(metadata.get("evidence_sku") or "").strip().upper()
        if not evidence_sku:
            evidence_sku = next(
                (str(sku).strip().upper() for sku in (result.get("result_skus") or []) if str(sku or "").strip()),
                "",
            )
        explicit_evidence = [
            item for item in (result.get("evidence") or [])
            if isinstance(item, dict)
            and (not evidence_sku or str(item.get("sku") or item.get("product_id") or "").strip().upper() == evidence_sku)
            and _display_source_type(item) in {"product_qa", "knowledge_chunks"}
        ]
    else:
        explicit_evidence = result.get("evidence") or []
    result["evidence"] = _normalize_display_evidence([
        *explicit_evidence,
        *metadata_evidence,
        *derived_result_evidence,
    ])
    answer_type = str(result.get("answer_type") or "").strip()
    if answer_type == "recommendation":
        # Recommendation prose is owned by the evidence-bounded semantic
        # writer. Do not re-rank or synthesize reasons with lexical templates.
        pass
    elif answer_type == "product_detail":
        question = (
            str(((result.get("answer_metadata") or {}).get("question")) or "")
            or str((((result.get("debug") or {}).get("plan") or {}).get("raw_question")) or "")
        )
        result["answer"] = _shape_product_detail_output(
            result.get("answer"),
            result.get("results") or [],
            question=question,
            answer_metadata=(result.get("answer_metadata") or {}),
        )
    elif answer_type == "product_query":
        result["answer"] = _shape_product_query_output(
            result.get("answer"),
            result.get("results") or [],
            result.get("result_skus") or [],
            question=(
                (((result.get("debug") or {}).get("intent") or {}).get("recommendation_query"))
                or (((result.get("debug") or {}).get("intent") or {}).get("semantic_query"))
                or (((result.get("debug") or {}).get("intent") or {}).get("term"))
                or str(((result.get("answer_metadata") or {}).get("question")) or "")
            ),
        )
    elif answer_type == "chat":
        result["answer"] = re.sub(
            r"\*\*(.*?)\*\*",
            r"\1",
            str(result.get("answer") or ""),
        ).strip()
    elif answer_type == "clarification" or str(result.get("intent") or "").strip() == "clarify":
        metadata = result.get("answer_metadata") if isinstance(result.get("answer_metadata"), dict) else {}
        if metadata.get("source") not in {
            "semantic_field_ambiguity_clarification",
            "semantic_pairwise_overview_uncovered_dimension_clarification",
        }:
            result["answer"] = shape_answer_tone(
                str(result.get("answer") or ""),
                intent=result.get("intent"),
                answer_type=answer_type,
            )
    # Some category/query executors return through a lightweight branch that
    # does not pass the central save wrapper.  Reapply the same customer-text
    # hygiene and catalogue-card alignment here so those branches cannot leak
    # provenance wording or broad cards beside a single bundle conclusion.
    raw_question = str(
        (((result.get("debug") or {}).get("plan") or {}).get("original_question"))
        or (((result.get("debug") or {}).get("plan") or {}).get("raw_question"))
        or ((result.get("answer_metadata") or {}).get("question") if isinstance(result.get("answer_metadata"), dict) else "")
        or ""
    ).strip()
    if raw_question:
        result = _clear_unrelated_catalogue_cards(raw_question, result)
    result = _align_answer_named_result_cards(result)
    result["answer"] = _sanitize_final_answer_text(
        str(result.get("answer") or ""),
        _pick_primary_answer_source(result),
    )
    if (
        isinstance(result.get("answer_metadata"), dict)
        and result["answer_metadata"].get("source") == "semantic_field_ambiguity_clarification"
    ):
        result["answer"] = str(result.get("answer") or "").replace("多个信息", "多个字段")
    result = _align_recommendation_result_cards(result)
    return result


def _field_metadata_display_evidence(result: dict[str, Any]) -> list[dict[str, Any]]:
    """Expose the validated field evidence that actually supports a detail answer.

    A product-field response may use a same-SKU QA fallback after a structured
    provider safely rejects a conflicting or absent database value.  The
    selected evidence is already recorded in answer_metadata; omitting it from
    the public evidence list makes a correct answer look unsupported and lets
    unrelated product-row values appear as its source.  This adapter only
    projects metadata after checking that its SKU belongs to the sealed result.
    """
    metadata = result.get("answer_metadata")
    if not isinstance(metadata, dict):
        return []
    evidence_value = str(metadata.get("evidence_value") or "").strip()
    evidence_sku = str(metadata.get("evidence_sku") or "").strip().upper()
    canonical_field = str(metadata.get("evidence_field") or "").strip()
    source = str(metadata.get("evidence_source") or "").strip()
    if not (evidence_value and evidence_sku and canonical_field and source):
        return []

    sealed_skus = {
        str(item).strip().upper()
        for item in (result.get("result_skus") or [])
        if str(item or "").strip()
    }
    for row in result.get("results") or []:
        if isinstance(row, dict) and str(row.get("sku") or "").strip():
            sealed_skus.add(str(row.get("sku") or "").strip().upper())
    if evidence_sku not in sealed_skus:
        return []

    source_lower = source.lower()
    if source_lower.startswith("product_qa:"):
        source_type = "product_qa"
    elif "knowledge" in source_lower:
        source_type = "knowledge_chunks"
    else:
        source_type = "product_db"
    product_name = ""
    for row in result.get("results") or []:
        if not isinstance(row, dict):
            continue
        if str(row.get("sku") or "").strip().upper() == evidence_sku:
            product_name = str(row.get("product_name_cn") or row.get("product_name_en") or "").strip()
            break
    return [{
        "sku": evidence_sku,
        "product_name": product_name,
        "field_label": customer_field_contract.product_detail_field_label(canonical_field) or canonical_field,
        "value": evidence_value,
        "source_type": source_type,
        "source_layer": _layer_for_field_label(
            customer_field_contract.product_detail_field_label(canonical_field) or canonical_field
        ),
        "matched_by": "已验证字段证据",
    }]


def _normalize_display_evidence(evidence: list[dict]) -> list[dict]:
    deduped: list[dict] = []
    seen: set[tuple[str, str, str, str]] = set()
    for item in evidence or []:
        normalized = _normalize_display_evidence_item(item)
        if not normalized:
            continue
        source_type = str(normalized.get("source_type") or "").strip()
        evidence_identity = str(normalized.get("evidence_text") or "").strip()
        field_identity = str(normalized.get("question_type") or normalized.get("field_label") or "").strip()
        # Product QA rows can be emitted once by the QA shortcut and again by
        # the metadata adapter with different display labels.  Their source
        # text, not the incidental label, is the evidence identity.
        if source_type == "product_qa":
            evidence_identity = str(normalized.get("value") or evidence_identity).strip()
            field_identity = ""
        key = (
            str(normalized.get("sku") or "").strip().upper(),
            source_type,
            field_identity,
            evidence_identity,
        )
        if key in seen:
            continue
        seen.add(key)
        deduped.append(normalized)
    return deduped


def _normalize_display_evidence_item(item: dict[str, Any]) -> dict[str, Any] | None:
    if not isinstance(item, dict):
        return None
    source_type = _display_source_type(item)
    sku = str(item.get("sku") or item.get("product_id") or "").strip().upper()
    field_label = str(item.get("field_label") or "").strip()
    question_type = str(item.get("question_type") or "").strip()
    value = str(item.get("value") or "").strip()
    content = str(item.get("content") or item.get("text") or item.get("quote") or "").strip()
    if source_type == "product_qa" and field_label in {"", "product_qa", "产品 QA", "产品问答"}:
        field_label = customer_field_contract.product_detail_field_label("product_qa") or "产品问答"
    evidence_text = ""
    if field_label and value:
        evidence_text = f"{field_label} {value}".strip()
    elif content:
        evidence_text = re.sub(r"\s+", " ", content).strip("：:；;，,。 ")
    elif source_type == "product_db" and sku:
        evidence_text = ""
    elif source_type == "product_db" and not sku and not content and not field_label and not value:
        return None
    if not evidence_text and source_type != "product_db":
        return None
    if evidence_text.lower() in {"资料", "none", "null"}:
        return None
    normalized = dict(item)
    normalized["source_type"] = source_type
    normalized["source_label"] = _display_source_label(source_type)
    normalized["sku"] = sku
    if question_type:
        normalized["question_type"] = question_type
    normalized["evidence_text"] = evidence_text
    return normalized


def _display_source_type(item: dict[str, Any]) -> str:
    raw_type = str(item.get("source_type") or item.get("type") or item.get("source") or "").strip().lower()
    raw_kind = str(item.get("source_kind") or "").strip().lower()
    if raw_type == "product_db" or raw_type == "product" and not raw_kind and str(item.get("field_label") or "").strip():
        return "product_db"
    if raw_type == "product" and raw_kind == "qa":
        return "product_qa"
    if raw_type in {"knowledge_base", "knowledge", "knowledge_chunk"} or raw_kind == "kb":
        return "knowledge_chunks"
    if raw_type in {"safety_guardrail", "guardrail"}:
        return "safety_guardrail"
    if raw_type == "product" and raw_kind == "kb":
        return "knowledge_chunks"
    if raw_type == "product":
        return "product_qa" if str(item.get("content") or "").strip() else "product_db"
    return raw_type or "product_db"


def _display_source_label(source_type: str) -> str:
    return {
        "product_db": "产品基础资料",
        "product_qa": "产品问答",
        "knowledge_chunks": "知识库资料",
        "safety_guardrail": "安全规则",
    }.get(source_type, source_type)


def _shape_recommendation_output(
    answer: str | None,
    results: list[dict],
    evidence: list[dict],
    *,
    require_visible_choice: bool = True,
) -> str:
    """Compatibility shim that deliberately leaves semantic prose unchanged.

    Candidate choice and reasons belong to the evidence-bounded Flash writer.
    A deterministic formatter must not invent a winner, reorder candidates,
    or synthesize suitability from result order.
    """
    del results, evidence, require_visible_choice
    return str(answer or "").strip()


def _shape_product_query_output(
    answer: str | None,
    results: list[dict],
    result_skus: list[str] | None = None,
    question: str = "",
) -> str:
    """Compatibility shim; product-query prose stays owned by its executor.

    Product rows are evidence and UI data, not permission for a formatter to
    promote the first row into a recommendation or invent suitability prose.
    """
    del results, result_skus, question
    return str(answer or "").strip()


def _render_customer_product_qa_answer(answer: str) -> str:
    """Repair generic imported-QA grammar without changing selected evidence."""
    text = str(answer or "").strip()
    if not text:
        return ""
    text = re.sub(
        r"(?P<topic>.{2,24}?)的原因：主要由于(?P<cause>[^；。！？!?]+?)导致的\s*(?P<effect>[^，,；。！？!?]+)[，,]\s*",
        lambda match: (
            f"{match.group('topic')}通常是由于{match.group('cause')}"
            f"导致{match.group('effect')}；"
        ),
        text,
    )
    return text.replace("擦清洗", "擦拭清洁")


def _shape_product_detail_output(
    answer: str | None,
    results: list[dict],
    question: str = "",
    answer_metadata: dict[str, Any] | None = None,
) -> str:
    answer_text = str(answer or "").strip()
    evidence_field = str((answer_metadata or {}).get("evidence_field") or "").strip()
    evidence_source = str((answer_metadata or {}).get("evidence_source") or "").strip()
    if evidence_source == "specs.usage_instruction" and evidence_field in {"cleaning", "care", "usage_instruction"}:
        answer_text = _render_customer_product_qa_answer(answer_text)
    if (
        str((answer_metadata or {}).get("contract_field_type") or "").strip() == "product_qa"
        and str((answer_metadata or {}).get("evidence_status") or "").strip() == "missing"
    ):
        return _normalize_handle_material_phrase(answer_text)
    # A product-QA contract can be fulfilled by either a QA row or a sealed
    # same-SKU knowledge chunk.  Both bodies have already passed evidence
    # grounding, so the generic single-sentence formatter must not discard
    # the verified details merely because the source is knowledge RAG.
    if (
        str((answer_metadata or {}).get("contract_field_type") or "").strip() == "product_qa"
        and str((answer_metadata or {}).get("evidence_status") or "").strip() == "matched"
    ):
        return _normalize_handle_material_phrase(_render_customer_product_qa_answer(answer_text))
    # A sealed same-SKU QA answer is already evidence-complete. Do not reduce
    # it to the first sentence, which would discard verified conditions and
    # reasons such as material or compatibility qualifications.
    if str((answer_metadata or {}).get("evidence_field") or "").strip() == "product_qa":
        return _normalize_handle_material_phrase(_render_customer_product_qa_answer(answer_text))
    if str((answer_metadata or {}).get("answer_policy") or "") in {
        "field_only",
        "compound_field_evidence",
        "sealed_same_sku_multi_field_evidence",
    }:
        return _normalize_handle_material_phrase(answer_text)
    if answer_text and any(
        term in answer_text
        for term in ("适用热源", "不能仅凭现有资料确认", "未显示支持", "支持酒精炉")
    ):
        return _normalize_handle_material_phrase(answer_text)
    if results and isinstance(results[0], dict):
        waterware_capability_answer = _shape_waterware_capability_answer(question, answer_text, results[0])
        if waterware_capability_answer:
            return _normalize_handle_material_phrase(waterware_capability_answer)
    if answer_text and (
        ("\n" in answer_text and "：" in answer_text)
        or any(
            term in answer_text
            for term in (
                "当前资料未直接标明",
                "产品问答显示",
                "知识库资料显示",
                "支持酒精炉",
                "未显示支持酒精炉",
                "不是炉具或炊具",
                "不建议按酒精炉适配产品理解",
                "当前资料未标注适用酒精炉",
                "当前资料未标注",
                "当前资料未维护",
                "无法仅凭现有资料确认",
            )
        )
        or (
            any(term in answer_text for term in ("装冷水", "装热水", "装饮用水", "装水", "开水", "沸水"))
            and any(marker in answer_text for marker in ("可以", "可装", "支持", "不建议", "不能", "避免"))
        )
    ):
        return _normalize_handle_material_phrase(answer_text)
    if results and isinstance(results[0], dict):
        row = results[0]
        sku = str(row.get("sku") or "").strip()
        name = str(row.get("product_name_cn") or row.get("product_name_en") or "").strip()
        field_values = row.get("field_values") or {}
        if isinstance(field_values, dict) and field_values:
            detail_parts = []
            for key, value in field_values.items():
                key_text = str(key)
                value_text = str(value)
                if key_text in {"材质", "主体材质"} and "、" in value_text and any(term in value_text for term in ("木", "白蜡木")):
                    primary_material, handle_material = [part.strip() for part in value_text.split("、", 1)]
                    if primary_material:
                        detail_parts.append(f"主体材质：{primary_material}")
                    if handle_material:
                        detail_parts.append(f"手柄材质：{handle_material}（手柄{handle_material}）")
                    continue
                if key_text == "手柄材质" and value_text and "手柄" not in value_text:
                    detail_parts.append(f"手柄材质：{value_text}（手柄{value_text}）")
                    continue
                detail_parts.append(f"{key}：{value}")
            detail = "；".join(detail_parts)
            prefix = f"{name}（{sku}）" if name else sku
            return _normalize_handle_material_phrase(f"{prefix}：{detail}。")
    text = answer_text
    sentence = re.split(r"[。！？!?]", text)[0].strip()
    return _normalize_handle_material_phrase(sentence + ("。" if sentence and not sentence.endswith("。") else ""))


def _shape_waterware_capability_answer(question: str, answer_text: str, row: dict[str, Any]) -> str | None:
    text = str(question or "").strip()
    if not text or not isinstance(row, dict):
        return None
    if not any(term in text for term in ("冷水", "热水", "补水", "烧水", "直接加热", "装热水", "喝热水")):
        return None

    row_text = " ".join(
        str(row.get(key) or "")
        for key in (
            "sku",
            "product_name_cn",
            "product_name_en",
            "category",
            "sub_category",
            "features",
            "usage_scenarios",
            "positioning",
            "long_description_cn",
            "usage_instruction",
            "heat_source",
        )
    )
    if not (
        _phase1_is_water_kettle_candidate(row)
        or any(term in row_text for term in ("咖啡器具", "手冲", "天鹅壶", "保温杯", "随行杯", "饮水", "补水"))
    ):
        return None

    sku = str(row.get("sku") or "").strip().upper()
    name = str(row.get("product_name_cn") or row.get("product_name_en") or sku).strip()
    prefix = f"{name}（{sku}）"
    category = str(row.get("category") or "").strip()
    combined = f"{row_text} {answer_text}"
    heat_source = str(row.get("heat_source") or "").strip().strip("/").strip()
    has_direct_heat_evidence = bool(heat_source) or any(
        term in combined for term in ("烧水", "煮水", "热饮", "泡茶", "手冲", "咖啡壶", "气炉", "卡式炉", "热源")
    )
    has_hot_container_evidence = any(
        term in combined for term in ("冷热两用", "保温", "装热水", "热水", "热饮", "喝热水", "饮水")
    )
    has_cold_or_hydration_evidence = any(
        term in combined for term in ("冷水", "补水", "随身补水", "饮水", "便携饮水", "冷饮")
    )

    asks_direct_heat = any(term in text for term in ("直接加热", "明火加热", "上火加热"))
    asks_boil_vs_hydration = "烧水还是补水" in text or ("烧水" in text and "补水" in text)
    asks_cold_or_hot = any(term in text for term in ("冷水还是热水", "适合冷水", "适合热水"))
    asks_hot_container = any(term in text for term in ("装热水", "喝热水")) and not asks_direct_heat

    if asks_direct_heat:
        if has_direct_heat_evidence:
            if heat_source:
                return f"{prefix}：当前资料显示它更偏向热饮/烧水类器具，热源信息为“{heat_source}”；如果你问的是直接加热或烧水，可以优先按这个方向理解。"
            return f"{prefix}：当前资料显示它更偏向热饮/烧水类器具；如果你问的是直接加热或烧水，现有资料只能保守按热饮/烧水用途理解。"
        return f"{prefix}：当前资料更偏向装热水/饮水或使用场景描述；是否可以直接加热或烧水，当前资料未标注，不能仅凭现有资料确认。"

    if asks_boil_vs_hydration:
        if has_direct_heat_evidence and not has_cold_or_hydration_evidence:
            return f"{prefix}：当前资料更偏向烧水/热饮使用，不是主打随身补水的杯具；如果你更看重补水便携，当前资料没有把它标成补水杯。"
        if has_cold_or_hydration_evidence and not has_direct_heat_evidence:
            return f"{prefix}：当前资料更偏向补水/饮水容器；是否适合烧水或直接加热，当前资料未标注，不能仅凭现有资料确认。"
        if has_direct_heat_evidence and has_cold_or_hydration_evidence:
            return f"{prefix}：当前资料同时出现了热饮/烧水和饮水描述；如果只问“烧水还是补水”，它更适合按具体热源与携带场景区分，现有资料不建议把两种能力直接等同。"
        return f"{prefix}：关于烧水还是补水，当前资料未给出明确结论；如果你更关心直接加热能力，仍要以热源说明为准。"

    if asks_cold_or_hot:
        if has_cold_or_hydration_evidence and has_hot_container_evidence and not has_direct_heat_evidence:
            return f"{prefix}：当前资料更偏向冷水/热水都可装的饮水容器，也能覆盖日常补水；但是否适合直接烧水，当前资料未标注。"
        if has_direct_heat_evidence and not has_cold_or_hydration_evidence:
            return f"{prefix}：当前资料更偏向热饮/烧水类器具；如果你问冷水还是热水，它会更贴近热水/热饮场景，是否适合冷水随身补水当前资料未明确标注。"
        if has_hot_container_evidence or category in {"水具", "水壶"}:
            return f"{prefix}：当前资料可按装冷水/装热水的饮水容器来理解；如果你进一步关心烧水或直接加热，仍要单独看热源和使用说明。"
        return f"{prefix}：当前资料未明确标注更偏冷水还是热水使用；如果你更关心直接加热能力，也不能仅凭现有资料确认。"

    if asks_hot_container:
        if has_hot_container_evidence or has_direct_heat_evidence or category in {"水具", "水壶", "咖啡器具"}:
            return f"{prefix}：当前资料可按装热水/喝热水场景理解；但“能装热水”不等于“能直接烧水”，这两者需要分开看。"
        return f"{prefix}：当前资料未明确标注是否适合装热水或喝热水，不能仅凭现有资料确认。"

    return None


def _normalize_handle_material_phrase(text: str) -> str:
    value = re.sub(r"\t+", "", str(text or ""))
    if not value:
        return value
    # The label already scopes the value to the handle. Older formatters could
    # append an identical parenthetical (for example
    # ``手柄材质：白蜡木（手柄白蜡木）``); collapse that duplication without
    # changing the database-backed material value.
    return re.sub(r"(手柄材质：\s*([^；。\n（）]+))（手柄\2）", r"\1", value)


def _public_intent_name(intent: str | None, answer_type: str | None = None) -> str | None:
    value = str(intent or "").strip()
    if not value:
        return value or None
    if value == "product_detail" and answer_type == "product_detail":
        return "product_detail"
    if value in {"aftersales", "company_info", "greeting"} and answer_type == "faq":
        return "customer_faq"
    if value == "recommend_products":
        return "recommendation"
    return value


async def _try_named_product_shortcut(db: Session, *, user_id: str, question: str) -> dict | None:
    products = _products_named_in_question(db, question)
    if not products:
        unresolved_clarification = _unresolved_product_like_contents_clarification_result(db, question)
        if unresolved_clarification:
            return unresolved_clarification
    if not products:
        return None
    subject_selection = customer_field_contract.select_entity_subject_for_routing(
        raw_question=question,
        fallback_product_like_subject=_product_like_scope_subject(question),
        fallback_named_subject=customer_agent_intent_service._detail_subject_from_question(question).strip(),
    )
    subject = subject_selection.entity_subject
    named_requested_fields = _explicit_sku_detail_requested_fields(question)
    named_field_type_override = (
        "usage_scene"
        if named_requested_fields == ["适用场景"]
        else None
    )
    all_products = db.query(Product).order_by(Product.sku.asc()).all()
    named_entity_contract = customer_entity_resolution_contract.build_entity_resolution_contract(
        question,
        all_products,
        resolver_candidates=products,
        entity_text_override=subject or None,
        field_type_override=named_field_type_override,
    )
    named_resolution_decision = customer_entity_resolution_contract.can_resolve_single_product(
        named_entity_contract,
        all_products,
    )
    if named_resolution_decision.allowed:
        products = [
            product
            for product in products
            if str(product.sku or "").strip().upper() == named_resolution_decision.resolved_sku
        ]
    named_identity_provenance = None
    if len(products) == 1:
        named_identity_provenance = customer_entity_resolution_contract.identity_provenance_from_entity_contract(
            named_entity_contract,
            bound_sku=str(products[0].sku or "").strip().upper(),
            origin_stage="named_product_resolver",
        )
    if subject and _looks_like_product_like_scope_subject(subject):
        strong_products = [product for product in products if _subject_strongly_matches_product(subject, product)]
        if strong_products:
            products = strong_products
        elif _looks_like_unresolved_product_like_question(question):
            label = _resolved_entity_realtime_commercial_field_guard_label(question) or _detect_unknown_product_fact_label(question)
            if label:
                unresolved = _build_unresolved_product_like_unknown_field_clarification_result(
                    question,
                    label=label,
                    subject_override=subject,
                )
                if unresolved:
                    return unresolved
            return {
                "intent": "clarify",
                "answer_type": "clarification",
                "answer": f"没有找到“{subject}”对应的产品资料，请确认商品名或提供 SKU。",
                "results": [],
                "result_skus": [],
                "candidate_skus": [],
                "needs_clarification": True,
                "answer_metadata": {"source": "entity_scope_product_not_found", "evidence_status": "product_not_found"},
                "debug": {"agent_mode": "entity_scope_product_not_found"},
                "skip_polish": True,
            }
    if _has_non_usage_care_field_contract(question):
        return None
    detail_requested_fields = named_requested_fields
    looks_like_named_product_detail = bool(detail_requested_fields) or _looks_like_product_detail_field_question(question)
    if len(products) > 1 and (
        customer_agent_intent_service._looks_like_contents_grounding_question(question)
        or customer_agent_intent_service._looks_like_usage_care_question(question)
        or looks_like_named_product_detail
    ):
        return _build_phase2_entity_state_response(
            db,
            question,
            {
                "action": "ambiguous_clarification",
                "contract": named_entity_contract,
                "products": db.query(Product).order_by(Product.sku.asc()).all(),
                "displayable_candidate_skus": _displayable_phase2_clarification_candidate_skus(named_entity_contract),
            },
        )
    if customer_agent_intent_service._looks_like_contents_grounding_question(question):
        return await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=products,
        )
    if (
        customer_agent_intent_service._looks_like_usage_care_question(question)
        or looks_like_named_product_detail
    ):
        usage_care_result = await customer_agent_intent_service.answer_product_usage_care_request(
            db,
            question=question,
            named_products=products,
        )
        if usage_care_result:
            return usage_care_result
        if len(products) == 1 and looks_like_named_product_detail:
            requested_fields = detail_requested_fields
            requested_field = ""
            if (
                ("热源" in requested_fields or "适配情况" in requested_fields)
                and (
                    customer_agent_intent_service._looks_like_water_container_capability_question(question)
                    or _is_explicit_sku_heat_source_question(question)
                )
            ):
                requested_field = "heat_source"
            elif requested_fields:
                requested_field = requested_fields[0]
            if requested_field:
                detail_result = _product_field_followup_result(
                    db,
                    str(products[0].sku or "").strip().upper(),
                    requested_field,
                    question,
                    identity_source=(
                        named_identity_provenance.source
                        if named_identity_provenance is not None
                        else "planner_candidate"
                    ),
                )
                if detail_result:
                    debug = detail_result.get("debug") if isinstance(detail_result.get("debug"), dict) else {}
                    debug["agent_mode"] = "named_product_detail_shortcut"
                    debug["plan"] = {
                        "primary_intent": "product_field",
                        "product_ref": str(products[0].sku or "").strip().upper(),
                        "requested_field": requested_field,
                        "raw_question": question,
                    }
                    debug["entity_resolution_contract"] = named_entity_contract.to_dict()
                    if named_identity_provenance is not None:
                        debug["identity_provenance"] = named_identity_provenance.to_dict()
                    detail_result["debug"] = debug
                    return detail_result
        return None
    if _is_variant_compare_question(question) and len(products) >= 2:
        sku_text = " 和 ".join(product.sku for product in products[:3])
        return await customer_agent_intent_service.process_intent_request(
            db,
            user_id=user_id,
            question=f"{sku_text} 有什么区别？客户该选哪个？",
            sku=None,
            previous_result_skus=[],
        )
    if len(products) == 1 and _is_generic_named_product_question(question):
        detail = product_service.get_product_detail(db, products[0].sku)
        result = _named_product_context_result(question, detail)
        if isinstance(result, dict):
            debug = result.get("debug") if isinstance(result.get("debug"), dict) else {}
            debug["entity_resolution_contract"] = named_entity_contract.to_dict()
            if named_identity_provenance is not None:
                debug["identity_provenance"] = named_identity_provenance.to_dict()
            result["debug"] = debug
        return result
    return None


def _unresolved_product_like_contents_clarification_result(db: Session, question: str) -> dict | None:
    if not customer_agent_intent_service._looks_like_contents_grounding_question(question):
        return None
    if customer_agent_intent_service._looks_like_multi_product_relation_question(question):
        return None
    if customer_agent_service._extract_skus(question):
        return None
    if _resolve_sku(db, question, None):
        return None
    if _products_named_in_question(db, question):
        return None
    subject = customer_agent_intent_service._detail_subject_from_question(question)
    if not subject:
        return None
    if not _looks_like_product_like_scope_subject(subject):
        return None
    if _is_generic_contents_subject(subject):
        return None
    answer = (
        f"我没能在当前商品资料里明确找到“{subject}”对应的具体 SKU。"
        "你可以提供 SKU，或确认是哪一款商品，我再帮你查包装清单、配件或开箱内容。"
    )
    return {
        "intent": "clarify",
        "answer_type": "clarification",
        "answer": answer,
        "results": [],
        "result_skus": [],
        "candidate_skus": [],
        "needs_clarification": True,
        "answer_metadata": {
            "source": "unresolved_product_like_contents_clarification",
            "evidence_status": "unresolved_product_like_subject",
        },
        "debug": {"agent_mode": "unresolved_product_like_contents_clarification"},
        "skip_polish": True,
    }


def _is_generic_contents_subject(subject: str) -> bool:
    value = customer_agent_service.normalize_search_text(subject).strip()
    if not value:
        return True
    generic_terms = {
        "户外",
        "露营",
        "野营",
        "配件",
        "水壶",
        "水具",
        "锅具",
        "套锅",
        "单锅",
        "炉具",
        "炉子",
        "茶具",
        "咖啡器具",
        "杯具",
        "杯子",
        "水杯",
        "壶",
        "锅",
        "炉",
        "杯",
        "包",
    }
    if value in generic_terms:
        return True
    stripped = value
    for prefix in ("户外", "露营", "野营", "徒步", "公园", "野餐", "车载", "营地", "新手"):
        if stripped.startswith(prefix):
            stripped = stripped[len(prefix):].strip()
    return stripped in generic_terms


def _is_generic_named_product_question(question: str) -> bool:
    text = str(question or "")
    specific_terms = (
        "材质", "材料", "功率", "认证", "食品级", "水洗", "冷水", "冲洗", "清洗", "热源", "燃料",
        "表面处理", "表面工艺", "爆炒", "大火", "颜色", "容量", "重量", "尺寸", "安全", "不粘", "不沾",
    )
    if any(term in text for term in specific_terms):
        return False
    return any(
        term in text
        for term in (
            "怎么样", "介绍一下", "适合露营吗", "适合户外吗", "适合吗", "好用吗",
            "再看", "改看", "看看", "看一下",
        )
    )


def _drop_covered_canonical_name_candidates(question: str, products: list[Product]) -> list[Product]:
    """Keep distinct named products but discard a name wholly inside a longer one.

    This is identity normalization, not intent routing: when ``A`` is a
    strict canonical-name substring of ``A套装`` in the same text span, the
    latter is the only explicitly named product.  Separate mentions retain
    both products and continue through normal comparison arbitration.
    """
    normalized_question = customer_agent_service.normalize_search_text(question)
    if not normalized_question or len(products) < 2:
        return products
    spans: dict[str, tuple[int, int]] = {}
    for product in products:
        name = customer_agent_service.normalize_search_text(product.product_name_cn or "")
        if name and name in normalized_question:
            start = normalized_question.index(name)
            spans[str(product.sku or "").upper()] = (start, start + len(name))
    kept: list[Product] = []
    for product in products:
        sku = str(product.sku or "").upper()
        span = spans.get(sku)
        if not span:
            kept.append(product)
            continue
        covered = any(
            other_sku != sku
            and other_span[0] <= span[0]
            and other_span[1] >= span[1]
            and (other_span[1] - other_span[0]) > (span[1] - span[0])
            for other_sku, other_span in spans.items()
        )
        if not covered:
            kept.append(product)
    return kept


def _products_named_in_question(db: Session, question: str) -> list[Product]:
    resolved_skus: list[str] = []
    seen_resolved_skus: set[str] = set()
    for raw_sku in customer_agent_service._extract_skus(question):
        resolved_sku = _resolve_sku(db, question, raw_sku)
        normalized_sku = str(resolved_sku or "").strip().upper()
        if normalized_sku and normalized_sku not in seen_resolved_skus:
            resolved_skus.append(normalized_sku)
            seen_resolved_skus.add(normalized_sku)
    if resolved_skus:
        sku_map = {
            str(product.sku or "").strip().upper(): product
            for product in db.query(Product).filter(Product.sku.in_(resolved_skus)).all()
        }
        exact_products = [sku_map[sku] for sku in resolved_skus if sku in sku_map]
        if exact_products:
            if customer_agent_intent_service._looks_like_multi_product_relation_question(question):
                return customer_agent_intent_service._relation_products_from_question(db, question, exact_products)
            return exact_products
    subject = (
        _product_like_scope_subject(question)
        or customer_agent_intent_service._detail_subject_from_question(question)
        or question
    )
    subject = customer_field_contract.strip_leading_entity_reference_modifier(subject) or question
    if customer_agent_intent_service._looks_like_contents_grounding_question(question):
        normalized_subject = customer_agent_service.normalize_search_text(subject)
        if (
            normalized_subject
            and not customer_agent_service._extract_skus(normalized_subject)
            and len(normalized_subject) < 3
        ):
            return []
    products = db.query(Product).all()
    matched_products = customer_agent_service.resolve_named_product_candidates(question, products, subject=subject)
    matched_products = _drop_covered_canonical_name_candidates(question, matched_products)
    if customer_agent_intent_service._looks_like_multi_product_relation_question(question):
        return customer_agent_intent_service._relation_products_from_question(db, question, matched_products)
    return matched_products


def _is_variant_compare_question(question: str) -> bool:
    text = str(question or "")
    return "pro" in text.lower() and any(word in text for word in ("选", "对比", "比较", "区别", "哪个", "怎么回复"))


def _named_product_context_result(
    question: str,
    detail: dict,
    *,
    identity_overview: bool | None = None,
) -> dict:
    specs = detail.get("specs") or {}
    business = detail.get("business") or {}
    name = detail.get("product_name_cn") or detail.get("product_name_en") or detail.get("sku")
    sku = detail.get("sku")
    scenarios = _display_value(business.get("usage_scenarios"))
    features = _display_value(business.get("top_selling_points"))
    capacity = _display_value(specs.get("capacity"))
    material = _display_value(specs.get("body_material"))
    question_text = str(question or "")
    navigation_only = any(term in question_text for term in ("再看", "改看", "看看", "看一下"))
    if identity_overview is None:
        identity_query = question_text
        for reference in (str(sku or ""), str(name or "")):
            if reference:
                identity_query = re.sub(re.escape(reference), "", identity_query, flags=re.IGNORECASE)
        identity_query = identity_query.strip(" 。！？!?，,；;：:")
        identity_overview = identity_query in {"是什么", "是什么产品", "介绍一下"}
    category = detail.get("category") or "未标注类目"
    answer_parts = [
        (
            f"已切换到{name}（{sku}）。你可以继续问它的材质、尺寸、重量、配件或使用方式。"
            if navigation_only
            else (
                f"{name}（{sku}）是一款{category}。"
                if identity_overview
                else f"{name}（{sku}）可以结合这个场景判断。"
            )
        ),
        f"类目：{category}。",
    ]
    if scenarios:
        answer_parts.append(f"适用场景：{scenarios}。")
    if features:
        answer_parts.append(f"主要卖点：{features}。")
    if capacity:
        answer_parts.append(f"容量/规格：{capacity}。")
    if material:
        if "手柄" in question and "、" in material:
            primary_material, handle_material = [part.strip() for part in material.split("、", 1)]
            if primary_material:
                answer_parts.append(f"主体材质：{primary_material}。")
            if handle_material:
                answer_parts.append(f"手柄材质：{handle_material}（手柄{handle_material}）。")
        else:
            answer_parts.append(f"材质：{material}。")
    if "水壶" in question or "餐具" in question or "带" in question:
        answer_parts.append("如果要携带餐具、水壶等物品，建议按实际尺寸和数量确认；小件随身/收纳适合，大件或整套锅具不建议强行装。")
    return {
        "answer": "".join(answer_parts),
        "intent": "product_detail",
        "answer_type": "product_detail",
        "confidence": "high",
        "uncertainty": "confirmed",
        "needs_clarification": False,
        "sources": [{"type": "product", "label": "命名产品资料", "sku": sku}],
        "actions": [],
        "results": [{
            "sku": sku,
            "product_name_cn": detail.get("product_name_cn"),
            "category": detail.get("category"),
            "capacity": capacity,
            "body_material": material,
            "features": features,
            "usage_scenarios": scenarios,
        }],
        "steps": [{"type": "named_product_shortcut", "label": "命名产品优先", "detail": f"命中 {sku}", "ok": True}],
        "warnings": [],
        "evidence": [],
        "debug": {"agent_mode": "named_product_shortcut"},
        "sku": sku,
        "skip_polish": True,
    }


def _display_value(value) -> str:
    if value in (None, ""):
        return ""
    if isinstance(value, list):
        return "，".join(_display_value(item) for item in value if item not in (None, ""))
    if isinstance(value, dict):
        label = _display_value(value.get("label"))
        item_value = _display_value(value.get("value"))
        if label and item_value:
            return item_value if label == item_value else f"{label} {item_value}"
        for key in ("value", "label", "text", "name"):
            if value.get(key) not in (None, ""):
                return _display_value(value.get(key))
        return "，".join(f"{key}: {_display_value(item)}" for key, item in value.items() if item not in (None, ""))
    return str(value).strip()


def _project_evidence_sources(agent_result: dict) -> list[dict]:
    """Expose sealed customer evidence through the standard source contract."""
    sources = [dict(item) for item in (agent_result.get("sources") or []) if isinstance(item, dict)]
    result_skus = {
        str(item.get("sku") or "").strip().upper()
        for item in (agent_result.get("results") or [])
        if isinstance(item, dict) and str(item.get("sku") or "").strip()
    }
    existing = {
        (
            str(item.get("type") or "").strip(),
            str(item.get("sku") or "").strip().upper(),
            str(item.get("field") or "").strip(),
        )
        for item in sources
    }
    for evidence in agent_result.get("evidence") or []:
        if not isinstance(evidence, dict):
            continue
        sku = str(evidence.get("sku") or "").strip().upper()
        if not sku or result_skus and sku not in result_skus:
            continue
        source_type = _display_source_type(evidence)
        field = str(evidence.get("field_label") or evidence.get("field") or "").strip()
        key = (source_type, sku, field)
        if key in existing:
            continue
        sources.append({
            "type": source_type,
            "label": str(evidence.get("source_label") or _display_source_label(source_type)).strip(),
            "sku": sku,
            **({"field": field} if field else {}),
        })
        existing.add(key)
    metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    missing_field = str(metadata.get("contract_field_type") or metadata.get("evidence_field") or "").strip()
    if not sources and len(result_skus) == 1 and metadata.get("field_evidence_missing") is True and missing_field:
        sources.append({
            "type": "product_field_missing",
            "label": "同 SKU 商品资料未标注该字段",
            "sku": next(iter(result_skus)),
            "field": missing_field,
        })
    return sources


def _attach_agent_quality(agent_result: dict, question: str) -> dict:
    result = dict(agent_result)
    if result.get("agent_quality"):
        return result
    result["sources"] = _project_evidence_sources(result)
    quality = customer_agent_quality_service.evaluate_agent_response(
        question,
        answer=result.get("answer") or "",
        intent=result.get("intent") or "",
        results=result.get("results") or [],
        sources=result.get("sources") or [],
        actions=result.get("actions") or [],
        warnings=result.get("warnings") or [],
        needs_clarification=bool(result.get("needs_clarification")),
        direct_answer=not bool(result.get("sources") or result.get("actions") or result.get("results")),
        tool_results=(result.get("debug") or {}).get("tool_results") or [],
    )
    result["agent_quality"] = quality
    debug = dict(result.get("debug") or {})
    debug["agent_quality"] = quality
    result["debug"] = debug
    if quality.get("level") == "low":
        result["confidence"] = "low"
    elif not quality.get("passed") and result.get("confidence") == "high":
        result["confidence"] = "medium"
    if quality.get("risks"):
        result["warnings"] = list(dict.fromkeys([*(result.get("warnings") or []), *quality["risks"]]))
        result["debug"]["warnings"] = result["warnings"]
    return result


def _review_quality_summary(items: list[dict]) -> dict:
    scores = []
    levels: dict[str, int] = {}
    risks: dict[str, int] = {}
    for item in items:
        quality = item.get("agent_quality") or {}
        if quality.get("score") is not None:
            scores.append(float(quality.get("score") or 0))
        level = str(quality.get("level") or "unknown")
        levels[level] = levels.get(level, 0) + 1
        for risk in quality.get("risks") or []:
            key = str(risk).split(":", 1)[0]
            risks[key] = risks.get(key, 0) + 1
    top_risks = sorted(risks.items(), key=lambda item: item[1], reverse=True)[:8]
    return {
        "avg_score": round(sum(scores) / max(len(scores), 1), 3) if scores else None,
        "levels": levels,
        "top_risks": [{"risk": risk, "count": count} for risk, count in top_risks],
    }


async def _polish_customer_answer(db: Session, question: str, agent_result: dict) -> str:
    answer = str(agent_result.get("answer") or "").strip()
    if not answer or agent_result.get("answer_type") == "action_proposal":
        return answer
    answer_metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    if answer_metadata.get("evidence_insufficient") is True or answer_metadata.get("answer_policy") == "insufficient_evidence":
        return answer
    evidence = agent_result.get("evidence") or []
    if not evidence and agent_result.get("uncertainty") == "confirmed":
        return answer
    payload = {
        "question": question,
        "draft_answer": answer,
        "uncertainty": agent_result.get("uncertainty"),
        "evidence": evidence[:8],
        "followups": (agent_result.get("followups") or agent_result.get("suggested_followups") or [])[:3],
    }
    system = (
        "你是亲切、简洁的产品客服话术润色器。只能依据输入的 draft_answer、evidence、uncertainty 和 followups 改写，"
        "不得新增事实、参数、认证、价格、库存或承诺。"
        "如果 uncertainty 不是 confirmed，必须保留“资料未标注/不能确认/需要人工确认”的含义。"
        "如果 draft_answer 已经明确表达“当前知识库没有维护/现有资料不足以确认”，不要改写成产品列表、规格介绍或新的结论。"
        "如果输入内容是在回答“能不能承诺/能不能宣传/有没有禁用话术”这一类问题，且 evidence 里没有专门的负向或限制性资料，不要把正向卖点改写成禁用内容；应继续保留资料不足或未维护的结论。"
        "只输出一句简短、自然的客服承接语，不要改写、总结、复述或截取 draft_answer；系统会在你的承接语后原样附上已验证正文。"
        "承接语不得包含任何产品事实、参数、适用性、结论或承诺，也不要寒暄堆砌。"
        "输出纯中文承接语，不要输出 JSON，不要出现意图、置信度、Agent、调试、异常提示等工程词。"
        "不要逐字分析或引用用户问题中的措辞（如人数、场景词），用户问题只提供上下文。"
    )
    try:
        polished = await customer_llm_service.chat_completion(
            db,
            messages=[
                {"role": "system", "content": system},
                {"role": "user", "content": json.dumps(payload, ensure_ascii=False, default=str)},
            ],
            temperature=0.2,
            max_tokens=800,
            purpose="evidence_bounded_customer_polish",
        )
    except Exception:
        return answer
    friendly_lead = str(polished or "").strip()
    if not friendly_lead:
        return answer
    forbidden = ("意图", "置信度", "Agent", "执行过程", "异常提示")
    if any(item in friendly_lead for item in forbidden):
        return answer
    if not _is_nonduplicative_customer_lead(friendly_lead, answer):
        return answer
    if agent_result.get("uncertainty") not in {"confirmed", "resolved"} and not any(item in friendly_lead for item in ("未标注", "不能确认", "人工确认", "资料不足", "暂不能确认")):
        return answer
    rendered = f"{friendly_lead}\n{answer}"
    if not await _polished_answer_is_evidence_grounded(db, question, answer, rendered, evidence):
        return answer
    return rendered


async def _semantic_render_structured_usage_answer(
    db: Session,
    *,
    question: str,
    agent_result: dict,
    semantic_preplan: dict | None = None,
) -> dict:
    """Turn a broad same-SKU usage field into a relevant customer answer.

    Product usage columns sometimes contain guidance for several components.
    The semantic model may omit unrelated clauses, but both product identity
    and every remaining fact must stay inside the already sealed evidence.
    """
    if not isinstance(agent_result, dict):
        return agent_result
    metadata = (
        agent_result.get("answer_metadata")
        if isinstance(agent_result.get("answer_metadata"), dict)
        else {}
    )
    if not (
        isinstance(semantic_preplan, dict)
        and semantic_preplan.get("called")
        and not str(semantic_preplan.get("fallback_reason") or "").strip()
        and str(metadata.get("contract_field_type") or "").strip() == "usage_instruction"
        and str(metadata.get("evidence_status") or "").strip() == "structured"
        and str(metadata.get("evidence_source") or "").strip() == "specs.usage_instruction"
        and str(metadata.get("evidence_value") or "").strip()
        and len(agent_result.get("result_skus") or []) == 1
    ):
        return agent_result

    rows = [row for row in (agent_result.get("results") or []) if isinstance(row, dict)]
    row = rows[0] if rows else {}
    payload = {
        "question": str(question or "").strip(),
        "product": {
            "sku": str((agent_result.get("result_skus") or [""])[0]),
            "name": str(row.get("product_name_cn") or row.get("product_name_en") or "").strip(),
        },
        "evidence": str(metadata.get("evidence_value") or "").strip(),
    }

    def reject_unusable_usage_evidence() -> dict:
        product_name = str(payload["product"].get("name") or "该商品").strip()
        product_sku = str(payload["product"].get("sku") or "").strip()
        product_label = f"{product_name}（{product_sku}）" if product_sku else product_name
        agent_result["answer"] = (
            f"{product_label}：现有同 SKU 使用说明无法形成与这个问题直接对应、可核验的完整操作步骤，"
            "暂不建议把这段资料当作首次操作指南；请以该型号说明书或人工核对结果为准。"
        )
        metadata["semantic_usage_rendered"] = False
        metadata["semantic_usage_rejected"] = True
        metadata["semantic_usage_renderer_model"] = str(
            customer_agent_planner_service._semantic_preplan_runtime_settings().get("model") or ""
        )
        metadata["evidence_status"] = "insufficient"
        agent_result["answer_metadata"] = metadata
        debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
        debug["semantic_usage_rejected"] = True
        agent_result["debug"] = debug
        agent_result["skip_polish"] = True
        return agent_result

    runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        raw = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "你是产品客服回答编辑器。只根据输入的同一商品使用说明回答客户当前问题。"
                        "提取与当前问题和该商品直接相关的步骤，删除其他器具、其他组件或无关场景的说明。"
                        "只保留写清楚动作和对象、客户能直接照做的步骤。省略笼统、循环或无法执行的句子；"
                        "例如证据只说“根据场景采用相应方法”时，必须说明资料没有给出具体方法，不能复述该句；"
                        "如果删去后步骤不完整，应直接指出现有资料还缺哪些操作细节。"
                        "不得补充常识、推测步骤、参数或安全承诺；证据没有写明的细节要明确说资料未写明。"
                        "只输出 JSON：{\"confirmed_actions\":[\"证据直接支持的具体动作\"],"
                        "\"missing_details\":\"证据缺少的操作细节\"}。"
                        "confirmed_actions 中每项都必须有具体动作和对象，不得放入空泛指令；没有则返回空数组。"
                    ),
                },
                {"role": "user", "content": json.dumps(payload, ensure_ascii=False)},
            ],
            temperature=0,
            max_tokens=320,
            purpose="structured_usage_relevance_render",
            api_model_override=runtime["model"],
            response_format=runtime["response_format"],
            thinking=runtime["thinking"],
        )
        rendered = customer_agent_planner_service._extract_json_object(str(raw or "")) or {}
        confirmed_actions = [
            str(item or "").strip().rstrip("。")
            for item in (rendered.get("confirmed_actions") or [])
            if str(item or "").strip()
        ]
        missing_details = str(rendered.get("missing_details") or "").strip().rstrip("。")
        # The JSON field already means “what the evidence does not state”.
        # Providers sometimes repeat that frame inside the value itself;
        # normalize it before adding the fixed customer-facing lead.
        missing_details = re.sub(
            r"^(?:(?:现有)?资料(?:中)?未(?:明确)?写明|未(?:明确)?写明)\s*[：:]?\s*",
            "",
            missing_details,
        ).strip()
        if not confirmed_actions and not missing_details:
            return reject_unusable_usage_evidence()
        product_name = str(payload["product"].get("name") or "该商品").strip()
        product_sku = str(payload["product"].get("sku") or "").strip()
        product_label = f"{product_name}（{product_sku}）" if product_sku else product_name
        answer_parts = []
        if confirmed_actions:
            answer_parts.append(f"{product_label}根据现有使用说明可确认：{'；'.join(confirmed_actions)}。")
        if missing_details:
            answer_parts.append(f"现有资料未写明{missing_details}。")
        candidate = "".join(answer_parts)
        verification_raw = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "只输出 JSON：{\"grounded\":true或false,\"relevant\":true或false,\"vague\":true或false}。"
                        "grounded 仅在答案每个产品事实和步骤都可由 evidence 直接推出时为 true；"
                        "relevant 仅在答案直接回应 question 且没有混入其他器具、组件或无关说明时为 true。"
                        "如果答案把“按相应方式操作”“遵循对应方法”等没有具体动作和对象的循环表述当作步骤，"
                        "或者只是换一种说法复述这种空泛指令，vague 必须为 true 且 relevant 必须为 false。"
                    ),
                },
                {
                    "role": "user",
                    "content": json.dumps({**payload, "candidate_answer": candidate}, ensure_ascii=False),
                },
            ],
            temperature=0,
            max_tokens=80,
            purpose="structured_usage_relevance_verify",
            api_model_override=runtime["model"],
            response_format=runtime["response_format"],
            thinking=runtime["thinking"],
        )
    except Exception:
        return reject_unusable_usage_evidence()
    verdict = customer_agent_planner_service._extract_json_object(str(verification_raw or "")) or {}
    if (
        verdict.get("grounded") is not True
        or verdict.get("relevant") is not True
        or verdict.get("vague") is not False
    ):
        return reject_unusable_usage_evidence()
    agent_result["answer"] = candidate
    metadata["semantic_usage_rendered"] = True
    metadata["semantic_usage_renderer_model"] = str(runtime.get("model") or "")
    agent_result["answer_metadata"] = metadata
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    debug["semantic_usage_rendered"] = True
    agent_result["debug"] = debug
    agent_result["skip_polish"] = True
    return agent_result


def _is_nonduplicative_customer_lead(lead: str, draft_answer: str) -> bool:
    """Accept only a fact-free lead instead of echoing the verified body.

    This is a presentation guard after semantic routing, entity sealing and
    grounding have completed.  It does not classify customer intent or alter
    facts: if a style model repeats any meaningful body fragment, preserve the
    original verified answer rather than render it twice.
    """
    normalized_lead = re.sub(r"[\W_]+", "", str(lead or "").casefold())
    normalized_body = re.sub(r"[\W_]+", "", str(draft_answer or "").casefold())
    if not normalized_lead:
        return False
    window = 6
    if len(normalized_lead) < window or len(normalized_body) < window:
        return True
    return not any(
        normalized_lead[index:index + window] in normalized_body
        for index in range(len(normalized_lead) - window + 1)
    )


async def _polished_answer_is_evidence_grounded(
    db: Session,
    question: str,
    draft_answer: str,
    polished_answer: str,
    evidence: list[dict],
) -> bool:
    """Let the model reject a polish that exceeds the sealed evidence packet."""
    semantic_runtime = customer_agent_planner_service._semantic_preplan_runtime_settings()
    try:
        raw = await customer_llm_service.chat_completion(
            db,
            messages=[
                {
                    "role": "system",
                    "content": (
                        "Return only JSON: {grounded:boolean}. Return true only when polished_answer preserves the factual scope "
                        "of draft_answer and the supplied evidence. Return false if it adds, removes, changes, summarises, generalises, or strengthens any product fact, "
                        "parameter, promise, suitability conclusion, safety instruction, or uncertainty."
                    ),
                },
                {
                    "role": "user",
                    "content": json.dumps(
                        {
                            "question": question,
                            "draft_answer": draft_answer,
                            "polished_answer": polished_answer,
                            "evidence": evidence[:8],
                        },
                        ensure_ascii=False,
                        default=str,
                    ),
                },
            ],
            temperature=0,
            max_tokens=80,
            purpose="evidence_bounded_customer_polish_grounding",
            api_model_override=semantic_runtime["model"],
            response_format=semantic_runtime["response_format"],
            thinking=semantic_runtime["thinking"],
        )
    except Exception:
        return False
    verdict = customer_agent_planner_service._extract_json_object(str(raw or "")) or {}
    return verdict.get("grounded") is True


def _should_skip_polish_for_agent_result(agent_result: dict) -> bool:
    answer_type = str(agent_result.get("answer_type") or "").strip().lower()
    intent = str(agent_result.get("intent") or "").strip().lower()
    results = agent_result.get("results") or []
    warnings = agent_result.get("warnings") or []
    sources = agent_result.get("sources") or []

    if _is_evidence_bounded_single_product_answer(agent_result):
        return False

    if answer_type in {"faq", "product_usage_care", "recommendation", "comparison", "clarification", "product_detail"}:
        return True
    if intent in {"purchase_channel", "aftersales", "company_info", "greeting", "product_usage_care", "recommend_products", "compare_products", "clarify", "product_detail"}:
        return True
    if answer_type == "product_query" and not results:
        return True
    if answer_type == "product_query" and any(source.get("type") == "product_qa" for source in sources if isinstance(source, dict)):
        return True
    if "missing_product_results" in warnings:
        return True
    return False


def _is_evidence_bounded_single_product_answer(agent_result: dict) -> bool:
    """Allow style-only rendering after identity and evidence are already sealed."""
    if str(agent_result.get("uncertainty") or "").strip() not in {"confirmed", "resolved"}:
        return False
    metadata = agent_result.get("answer_metadata") if isinstance(agent_result.get("answer_metadata"), dict) else {}
    if metadata.get("evidence_status") != "matched" or metadata.get("field_evidence_missing") is True:
        return False
    result_skus = [str(item or "").strip().upper() for item in agent_result.get("result_skus") or [] if str(item or "").strip()]
    if len(result_skus) != 1:
        return False
    debug = agent_result.get("debug") if isinstance(agent_result.get("debug"), dict) else {}
    contract = debug.get("entity_resolution_contract") if isinstance(debug.get("entity_resolution_contract"), dict) else {}
    if contract.get("status") != "resolved" or str(contract.get("resolved_sku") or "").strip().upper() != result_skus[0]:
        return False
    evidence = [item for item in agent_result.get("evidence") or [] if isinstance(item, dict)]
    return bool(evidence) and all(str(item.get("sku") or "").strip().upper() == result_skus[0] for item in evidence)


_FINAL_SOURCE_PRIORITY = {
    "product_usage_care": 1,
    "customer_faq": 2,
    "product_qa": 3,
    "structured_product_detail": 3,
    "knowledge_chunks": 4,
    "query_products": 5,
    "llm_fallback": 6,
}


def _pick_primary_answer_source(agent_result: dict) -> dict:
    intent = str(agent_result.get("intent") or "").strip().lower()
    answer_type = str(agent_result.get("answer_type") or "").strip().lower()
    sources = agent_result.get("sources") or []

    if intent == "product_usage_care" or answer_type == "product_usage_care":
        return {"type": "product_usage_care", "priority": 1}
    if answer_type == "faq" or intent in {"purchase_channel", "aftersales", "company_info", "greeting"}:
        return {"type": "customer_faq", "priority": 2}
    if answer_type in {"product_detail", "comparison"}:
        return {"type": "structured_product_detail", "priority": 3}
    if answer_type == "recommendation" and (agent_result.get("results") or []):
        return {"type": "query_products", "priority": 5}
    if (intent == "query_products" or answer_type == "product_query") and (agent_result.get("results") or []):
        return {"type": "query_products", "priority": 5}
    if answer_type == "knowledge_base_answer":
        return {"type": "knowledge_chunks", "priority": 4}
    if any(isinstance(source, dict) and source.get("type") == "product_qa" for source in sources):
        return {"type": "product_qa", "priority": 3}
    if any(isinstance(source, dict) and source.get("type") in {"knowledge_base", "usage_care_knowledge", "faq_knowledge"} for source in sources):
        return {"type": "knowledge_chunks", "priority": 4}
    if intent == "query_products" or answer_type == "product_query":
        return {"type": "query_products", "priority": 5}
    return {"type": "llm_fallback", "priority": 6}


def _tag_and_order_sources(sources: list[dict], primary: dict) -> list[dict]:
    tagged: list[dict] = []
    for source in sources:
        if not isinstance(source, dict):
            continue
        item = dict(source)
        item["role"] = "primary" if _source_matches_primary(item, primary.get("type")) else "supporting"
        tagged.append(item)
    tagged.sort(key=lambda item: 0 if item.get("role") == "primary" else 1)
    return tagged


def _source_matches_primary(source: dict, primary_type: str | None) -> bool:
    source_type = str(source.get("type") or "").strip().lower()
    if primary_type == "product_usage_care":
        return source_type in {"product_qa", "usage_care_knowledge"}
    if primary_type == "customer_faq":
        return source_type in {"faq", "structured_faq", "faq_knowledge"}
    if primary_type == "product_qa":
        return source_type == "product_qa"
    if primary_type == "structured_product_detail":
        return source_type in {"product", "product_compare", "product_search"}
    if primary_type == "knowledge_chunks":
        return source_type in {"knowledge_base", "usage_care_knowledge", "faq_knowledge"}
    if primary_type == "query_products":
        return source_type == "product_search"
    if primary_type == "llm_fallback":
        return source_type == "agent_meta"
    return False


def _llm_allowed_for_final_answer(agent_result: dict, primary: dict) -> bool:
    # This is a style-only pass after a resolved EntityResolutionContract and
    # same-SKU evidence have been sealed. It does not authorize the model to
    # select a product, field, or fact.
    if _is_evidence_bounded_single_product_answer(agent_result):
        return True
    primary_type = primary.get("type")
    if primary_type in {"product_usage_care", "customer_faq", "product_qa", "structured_product_detail", "knowledge_chunks"}:
        return False
    if primary_type == "query_products" and (agent_result.get("results") or agent_result.get("sources")):
        return False
    return primary_type == "llm_fallback"


def _sanitize_final_answer_text(answer: str, primary: dict) -> str:
    text = str(answer or "").strip()
    if not text:
        return text
    # A clarification generated before the dialogue context is resolved can
    # contain internal orchestration wording.  Keep the customer action, but
    # remove references to turns/results that are not meaningful to a shopper.
    text = re.sub(
        r"你(?:说的|提到的)[“\"]?这些[”\"]?(?:我还没有可引用的上一轮商品结果|目前没有可引用的上一轮结果)[。！!]?\s*",
        "",
        text,
    )
    text = re.sub(
        r"我还没有可引用的上一轮商品结果[。！!]?\s*",
        "",
        text,
    )
    text = re.sub(
        r"你提到的[“\"]?这些[”\"]?目前没有可引用的上一轮结果[。！!]?\s*",
        "",
        text,
    )
    text = re.sub(r"上一轮商品结果|上一轮结果|可引用的上一轮", "此前商品信息", text)
    text = text.replace("上一轮", "前面")
    # Imported catalogue rows can contain search-index and listing-title
    # metadata in an otherwise customer-facing field list. Preserve adjacent
    # product facts, but remove each internal metadata segment in full.
    text = re.sub(
        r"(?:^|[；;\n])\s*(?:关键词库|中文标题|英文标题)\s*[:：][^；;\n]*",
        "",
        text,
        flags=re.I,
    )
    text = re.sub(
        r"(?:^|[；;\n])[^；;\n]*(?:keyword|priority)\s*[:：][^；;\n]*",
        "",
        text,
        flags=re.I,
    )
    # Imported product-detail rows can be accidentally rendered as a single
    # customer answer.  Remove the internal label block (especially owner and
    # lifecycle fields) while preserving a genuine customer-facing conclusion
    # that follows it.
    internal_labels = (
        "SKU", "中文名", "英文名", "品牌", "系列", "类目", "等级", "生命周期", "负责人",
        "规格信息", "业务信息", "label", "value", "unit", "产品资料", "内容信息",
        "中文标题", "英文标题", "中文描述", "英文描述",
    )
    label_count = sum(
        1
        for label in internal_labels
        if re.search(rf"(?:^|[\n；;：:\-])\s*{re.escape(label)}\s*[:：]", text, flags=re.I)
    )
    compact_product_dump = sum(
        1
        for label in ("内容信息", "中文标题", "英文标题", "中文描述", "英文描述")
        if re.search(rf"{re.escape(label)}\s*[:：]", text, flags=re.I)
    ) >= 2
    if compact_product_dump or label_count >= 3 or (
        re.search(r"(?:^|[\n；;])\s*负责人\s*[:：]", text)
        and re.search(r"(?:^|[\n；;])\s*SKU\s*[:：]", text, flags=re.I)
    ):
        lines = [line.strip() for line in text.splitlines() if line.strip()]
        customer_heading = re.compile(
            r"^(?:兼容性结论|处理建议|注意事项|安全提醒|配件结论|存放结论|先说结论|结论)\s*[:：]"
        )
        tail_index = next((index for index, line in enumerate(lines) if customer_heading.search(line)), None)
        if tail_index is not None:
            text = "\n".join(lines[tail_index:])
        else:
            inline_heading = re.search(
                r"(?:兼容性结论|处理建议|注意事项|安全提醒|配件结论|存放结论|先说结论|结论)\s*[:：]",
                text,
            )
            text = text[inline_heading.start():] if inline_heading else "当前资料未标注可直接回答的商品信息，暂时无法确认。"
    # Evidence provenance remains in API metadata.  Field paths are internal
    # implementation details and make a customer-facing answer look like a
    # debug trace when an upstream renderer includes them.
    internal_path = r"(?:content|specs|business|product|answer_metadata|debug)(?:\.[A-Za-z_][A-Za-z0-9_]*)+"
    text = re.sub(
        rf"[（(]\s*来源\s*[:：]\s*{internal_path}\s*[）)]",
        "",
        text,
        flags=re.I,
    )
    text = re.sub(rf"\b{internal_path}\b", "相关商品资料", text, flags=re.I)
    # Product-detail renderers may append provenance markers intended for
    # internal review (for example ``（补充资料）`` and a QA/KB field note).
    # The evidence remains available in structured API metadata; remove the
    # implementation-source wording from the customer-visible answer.
    text = re.sub(r"[（(]\s*补充资料\s*[）)]", "", text)
    text = re.sub(
        r"依据：以上字段已结合当前 SKU 的\s*QA\s*/\s*KB\s*补充资料。?",
        "",
        text,
        flags=re.I,
    )
    # Keep provenance useful without exposing the internal renderer's source
    # label.  Phrases such as “依据产品资料” are not a product fact and read
    # like backend metadata in a customer-facing answer.
    text = re.sub(r"依据\s*(?:来自\s*)?产品资料", "根据已核对信息", text)
    text = re.sub(r"依据\s*(?:是|来自)\s*产品资料", "根据已核对信息", text)
    text = re.sub(r"产品资料\s*，", "已核对信息，", text)
    text = re.sub(r"产品资料\s*明确标注", "已核对信息明确标注", text)
    text = re.sub(r"产品资料\s*中", "已核对信息中", text)
    # Structured catalogue fallbacks used implementation labels that are
    # meaningful to an operator but not to a shopper.  Keep the conclusion
    # and the requested condition while removing the backend vocabulary.
    text = re.sub(r"结构化商品库", "已核对资料", text)
    text = re.sub(
        r"查询类目\s*[:：]\s*[【\[]?(?P<category>[^；;。\n】\]]+)[】\]]?\s*[；;]\s*",
        lambda match: f"类别为“{match.group('category').strip()}”；",
        text,
    )
    text = re.sub(r"同\s*SKU\s*的已验证资料作为筛选条件", "同一款商品资料同时核对这些要求", text)
    # Same-SKU sealing is an internal evidence guarantee, not useful shopping
    # language. Keep the facts while removing duplicated provenance tails and
    # translating any remaining ownership wording into ordinary customer prose.
    text = re.sub(r"；\s*同\s*SKU\s*资料\s*[:：]", "；关键参数：", text, flags=re.I)
    text = re.sub(r"适用热源\s*/\s*同\s*SKU\s*证据\s*为\s*[:：]", "商品资料写明：", text, flags=re.I)
    text = re.sub(r"按已核验的同\s*SKU\s*资料", "根据已核对的商品资料", text, flags=re.I)
    text = re.sub(r"同\s*SKU\s*热源信息", "商品热源资料", text, flags=re.I)
    text = re.sub(r"同\s*SKU\s*资料", "该商品资料", text, flags=re.I)
    text = re.sub(r"同\s*SKU\s*证据", "该商品资料", text, flags=re.I)
    text = re.sub(r"放宽筛选条件", "放宽部分要求", text)
    text = re.sub(r"筛选条件\s*[:：]\s*", "按你的条件：", text)
    # Any residual operator phrasing should still read as ordinary customer
    # language when it comes from a fallback assembled elsewhere.
    text = text.replace("筛选条件", "要求")
    text = re.sub(r"结构化硬筛选条件", "已核对条件", text)
    text = re.sub(r"[“\"]类目[”\"]\s*(?:明确)?(?:标注|标明)为", "类别是", text)
    text = re.sub(r"类目\s*(?:明确)?(?:标注|标明)为", "类别是", text)
    # The runtime and imported QA prompts use “知识库/数据库/字段/系统里”
    # as internal provenance language.  It must never leak into the answer
    # shown to a customer; preserve the underlying uncertainty while using
    # the neutral customer-facing term “当前资料/信息”.
    text = re.sub(r"(?:当前|现有|本次)?知识库", "当前资料", text)
    text = re.sub(r"数据库商品类目字段", "商品目录", text)
    text = re.sub(r"数据库", "商品资料", text)
    text = re.sub(r"系统里记录(?:的)?", "目前可核对到的", text)
    text = re.sub(r"系统里", "当前资料里", text)
    text = re.sub(r"(?:当前|现有|结构化|商品资料|商品库)\s*字段", "信息", text)
    text = re.sub(r"(?:该|这个|此)字段", "该信息", text)
    text = re.sub(r"字段(确认|里|中)", r"信息\1", text)
    # A renderer can also prepend a field's human label (for example
    # “热源字段” or “容量字段”), which is still implementation language even
    # though it does not match the longer provenance patterns above.
    text = re.sub(r"字段", "信息", text)
    # Some imported QA/listing snippets contain a bare ORM field name without
    # its dotted path.  That is still an internal implementation label and
    # must not be shown to customers.
    bare_internal_fields = {
        "heat_source": "适用热源",
        "body_material": "主体材质",
        "surface_finish": "表面处理",
        "gross_weight_g": "重量",
        "size_info": "尺寸信息",
        "usage_scenarios": "适用场景",
        "target_audience": "适用人群",
        "technical_advantages": "技术特点",
        "top_selling_points": "主要卖点",
        "person_in_charge": "负责人资料",
        "subject_subtype": "商品类型",
        "subject_kind": "商品类型",
        "recommendation_contract": "推荐条件",
        "semantic_constraints": "推荐条件",
        "verified_constraints": "已核对条件",
        "evidence_usage": "依据",
    }
    for field_name, label in bare_internal_fields.items():
        text = re.sub(rf"(?<![A-Za-z0-9_]){re.escape(field_name)}(?![A-Za-z0-9_])", label, text, flags=re.I)
    # LLM/tool renderers occasionally stringify a list-valued catalogue field
    # into Python/JSON syntax.  Convert a closed literal list to ordinary
    # customer prose instead of exposing brackets and quoted debug values.
    def _replace_inline_literal_list(match: re.Match) -> str:
        raw = str(match.group(0) or "")[1:-1]
        values = [item.strip().strip("'\"") for item in re.split(r"\s*,\s*", raw) if item.strip()]
        return "、".join(values)

    text = re.sub(
        r"\[(?:\s*(?:\"[^\n\"]*\"|'[^\n']*')(?:\s*,\s*(?:\"[^\n\"]*\"|'[^\n']*'))*)\s*\]",
        _replace_inline_literal_list,
        text,
    )
    # Owner/lifecycle fields are operational metadata, not customer-facing
    # product facts.  Remove an inline sentence even when it was not rendered
    # as a labelled dump block.
    # Owner names are operational metadata.  Replace the whole label/name
    # span with a customer-facing support contact instead of exposing staff
    # identities (for example, “联系负责人 Greta”).
    text = re.sub(
        r"负责人\s*(?:为|是)?\s*[A-Za-z][A-Za-z0-9_\-]*",
        "店铺客服",
        text,
        flags=re.I,
    )
    text = re.sub(r"负责人", "店铺客服", text)
    # Customer output is plain text in the service UI.  Strip Markdown control
    # markers that otherwise render as noisy literal asterisks/backticks when
    # an upstream model returns formatted prose.
    text = text.replace("**", "").replace("```", "").replace("`", "")
    text = re.sub(r"(^|\n)\s*#{1,6}\s*", r"\1", text)
    text = re.sub(r"[ \t]{2,}", " ", text)
    text = re.sub(r"(^|\n)\s*Q:\s*", r"\1", text, flags=re.I)
    text = re.sub(r"(^|\n)\s*A:\s*", r"\1", text, flags=re.I)
    text = re.sub(r"\n{3,}", "\n\n", text).strip()
    return text


def _answer_type_from_intent(intent: str | None) -> str:
    return {
        "query_products": "product_query",
        "product_detail": "product_detail",
        "compare_products": "comparison",
        "recommend_products": "recommendation",
        "propose_delete": "action_proposal",
        "propose_update": "action_proposal",
        "clarify": "clarification",
    }.get(str(intent or ""), "unknown")


def _uncertainty_from_answer(answer: str, results: list, warnings: list, needs_clarification: bool) -> str:
    if needs_clarification:
        return "ambiguous_product"
    if any(item in answer for item in ("没有标注", "不能直接确认", "暂时不能确认", "资料未标注")):
        return "not_recorded"
    if not results and any(item in answer for item in ("没有找到", "暂时无法", "不能可靠")):
        return "insufficient_data"
    if warnings:
        return "insufficient_data"
    return "confirmed"


def _result_evidence_display_value(field_label: str, value: Any) -> str:
    """Apply structured-value validation before result evidence reaches the API."""
    canonical_field = {
        "容量": "capacity",
        "材质": "material",
        "颜色": "color",
        "类目": "category",
        "卖点": "selling_point",
    }.get(str(field_label or "").strip(), str(field_label or "").strip())
    return _phase1_comparison_evidence_value(canonical_field, value)


def _evidence_from_results(results: list) -> list[dict]:
    evidence = []
    for item in results[:8]:
        if not isinstance(item, dict):
            continue
        field_values = item.get("field_values") if isinstance(item.get("field_values"), dict) else {}
        if field_values:
            for label, value in field_values.items():
                if str(label or "").strip() in {"负责人", "品质情况", "备注"}:
                    continue
                display_value = _result_evidence_display_value(str(label), value)
                if not display_value:
                    continue
                evidence.append({
                    "sku": item.get("sku"),
                    "product_name": item.get("product_name_cn") or item.get("product_name_en"),
                    "field_label": label,
                    "value": display_value,
                    "source_layer": _layer_for_field_label(str(label)),
                    "matched_by": item.get("matched_by") or "产品资料",
                })
            continue
        for label, key in (("容量", "capacity"), ("材质", "body_material"), ("颜色", "color"), ("类目", "category"), ("卖点", "features")):
            value = item.get(key)
            display_value = _result_evidence_display_value(label, value)
            if display_value:
                evidence.append({
                    "sku": item.get("sku"),
                    "product_name": item.get("product_name_cn") or item.get("product_name_en"),
                    "field_label": label,
                    "value": display_value,
                    "source_layer": _layer_for_field_label(label),
                    "matched_by": item.get("matched_by") or "产品资料",
                })
    return evidence


def _layer_for_field_label(label: str) -> str:
    if any(item in label for item in ("容量", "重量", "材质", "颜色", "热源", "功率", "表面")):
        return "L2"
    if any(item in label for item in ("卖点", "场景", "定位", "人群", "竞品")):
        return "L3"
    if any(item in label for item in ("标题", "描述", "关键词", "listing", "Listing")):
        return "L4"
    return "L1"



def _build_conversation_history(db: Session, conversation_id: str | None, user_id: str) -> list[dict]:
    """Build conversation context from DB history (last 10 turns)."""
    if not conversation_id:
        return []
    conversation = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.id == conversation_id,
        CustomerServiceConversation.user_id == str(user_id),
    ).first()
    if not conversation:
        return []
    messages = db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation.id
    ).order_by(CustomerServiceMessage.created_at.desc()).limit(20).all()
    history = []
    for msg in reversed(messages):
        role = "assistant" if msg.role == "assistant" else "user"
        history.append({"role": role, "content": msg.content})
    return history


def _build_feedback_lessons(db: Session, user_id: str, limit: int = 8) -> list[dict]:
    messages = (
        db.query(CustomerServiceMessage)
        .join(CustomerServiceConversation, CustomerServiceConversation.id == CustomerServiceMessage.conversation_id)
        .filter(
            CustomerServiceConversation.user_id == str(user_id),
            CustomerServiceMessage.role == "assistant",
        )
        .order_by(CustomerServiceMessage.created_at.desc())
        .limit(50)
        .all()
    )
    lessons = []
    for message in messages:
        meta = _message_meta(message.sources_json)
        feedback = meta.get("feedback") if isinstance(meta, dict) else None
        if not isinstance(feedback, dict) or feedback.get("rating") == "helpful":
            continue
        user_message = (
            db.query(CustomerServiceMessage)
            .filter(
                CustomerServiceMessage.conversation_id == message.conversation_id,
                CustomerServiceMessage.role == "user",
                CustomerServiceMessage.created_at <= message.created_at,
            )
            .order_by(CustomerServiceMessage.created_at.desc())
            .first()
        )
        lessons.append({
            "question": user_message.content if user_message else "",
            "bad_answer": message.content[:500],
            "rating": feedback.get("rating"),
            "reason": feedback.get("reason") or "",
            "comment": feedback.get("comment") or "",
        })
        if len(lessons) >= limit:
            break
    return lessons


def _latest_result_skus(db: Session, conversation_id: str | None, user_id: str) -> list[str]:
    return [item["sku"] for item in _latest_entity_stack(db, conversation_id, user_id)[:10]]


def _latest_persisted_agent_context_sku(db: Session, conversation_id: str | None) -> str:
    if not conversation_id:
        return ""
    message = db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation_id,
        CustomerServiceMessage.role == "assistant",
    ).order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc()).first()
    if message is None:
        return ""
    sources = _safe_json(message.sources_json, [])
    for source in sources:
        if not isinstance(source, dict) or source.get("type") != "agent_context":
            continue
        result_skus = [
            str(value or "").strip().upper()
            for value in (source.get("result_skus") or [])
            if str(value or "").strip()
        ]
        if len(result_skus) != 1:
            continue
        sku = str(source.get("current_sku") or "").strip().upper()
        if sku:
            return sku
        if len(result_skus) == 1:
            return result_skus[0]
    # Older and deterministic result paths persist first-party product
    # evidence but not a dedicated agent_context entry. A single distinct SKU
    # on the immediately preceding assistant turn is still a sealed identity;
    # multiple SKUs remain deliberately ambiguous.
    evidence_skus = {
        str(source.get("sku") or "").strip().upper()
        for source in sources
        if isinstance(source, dict)
        and str(source.get("sku") or "").strip()
        and source.get("type") in {"product_db", "product_field", "knowledge_qa", "knowledge_document"}
    }
    if len(evidence_skus) == 1:
        return next(iter(evidence_skus))
    return ""


def _latest_active_product_skus(db: Session, conversation_id: str | None, user_id: str) -> list[str]:
    if not conversation_id:
        return []
    conversation = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.id == conversation_id,
        CustomerServiceConversation.user_id == str(user_id),
    ).first()
    if not conversation:
        return []
    messages = db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation_id,
        CustomerServiceMessage.role == "assistant",
    ).order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc()).limit(5).all()
    for message in messages:
        for source in _safe_json(message.sources_json, []):
            if not isinstance(source, dict) or source.get("type") != "agent_context":
                continue
            current_sku = str(source.get("current_sku") or "").strip().upper()
            if current_sku:
                return [current_sku]
            entities = [item for item in (source.get("entities") or []) if isinstance(item, dict)]
            current_entities = [
                str(item.get("sku") or "").strip().upper()
                for item in entities
                if str(item.get("role") or "") == "current" and str(item.get("sku") or "").strip()
            ]
            if len(current_entities) == 1:
                return current_entities
            result_skus = [
                str(sku or "").strip().upper()
                for sku in (source.get("result_skus") or [])
                if str(sku or "").strip()
            ]
            if len(result_skus) == 1:
                return result_skus
        sku = str(message.sku or "").strip().upper()
        if sku:
            return [sku]
        primary_sku = _primary_sku_from_message_content(message.content)
        if primary_sku:
            return [primary_sku]
    return []


def _latest_ordered_result_skus_for_followup(db: Session, conversation_id: str | None, user_id: str) -> list[str]:
    if not conversation_id:
        return []
    conversation = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.id == conversation_id,
        CustomerServiceConversation.user_id == str(user_id),
    ).first()
    if not conversation:
        return []
    messages = db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation_id,
        CustomerServiceMessage.role == "assistant",
    ).order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc()).limit(5).all()
    for message in messages:
        sources = _safe_json(message.sources_json, [])
        for source in sources:
            if not isinstance(source, dict):
                continue
            if source.get("type") == "agent_meta":
                for context_key in ("candidate_context", "recommendation_context"):
                    context = source.get(context_key)
                    if not isinstance(context, dict):
                        continue
                    ordered = [
                        str(sku or "").strip().upper()
                        for sku in (
                            context.get("ordered_result_skus")
                            or context.get("recommended_skus")
                            or context.get("candidate_skus")
                            or []
                        )
                        if str(sku or "").strip()
                    ]
                    if ordered:
                        return ordered
            if source.get("type") == "agent_context":
                ordered = [
                    str(sku or "").strip().upper()
                    for sku in (source.get("result_skus") or [])
                    if str(sku or "").strip()
                ]
                if ordered:
                    return ordered
    return []


def _latest_user_question_for_followup(
    db: Session,
    conversation_id: str | None,
    user_id: str,
    current_question: str | None = None,
) -> str:
    if not conversation_id:
        return ""
    conversation = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.id == conversation_id,
        CustomerServiceConversation.user_id == str(user_id),
    ).first()
    if not conversation:
        return ""
    current_text = str(current_question or "").strip()
    messages = (
        db.query(CustomerServiceMessage)
        .filter(
            CustomerServiceMessage.conversation_id == conversation_id,
            CustomerServiceMessage.role == "user",
        )
        .order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc())
        .limit(5)
        .all()
    )
    for message in messages:
        content = str(message.content or "").strip()
        if current_text and content == current_text:
            continue
        if content:
            return content
    return ""


def _latest_entity_stack(db: Session, conversation_id: str | None, user_id: str, limit: int = 30) -> list[dict]:
    if not conversation_id:
        return []
    conversation = db.query(CustomerServiceConversation).filter(
        CustomerServiceConversation.id == conversation_id,
        CustomerServiceConversation.user_id == str(user_id),
    ).first()
    if not conversation:
        return []
    messages = db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation_id,
        CustomerServiceMessage.role == "assistant",
    ).order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc()).limit(5).all()
    records: list[tuple[int, int, int, dict]] = []
    for message_index, message in enumerate(messages):
        message_turn_index = message_index
        preceding_user = (
            db.query(CustomerServiceMessage)
            .filter(
                CustomerServiceMessage.conversation_id == conversation_id,
                CustomerServiceMessage.role == "user",
                CustomerServiceMessage.created_at <= message.created_at,
            )
            .order_by(CustomerServiceMessage.created_at.desc(), CustomerServiceMessage.id.desc())
            .first()
        )
        if preceding_user:
            explicit_rows = _question_entities_for_entity_stack(db, str(preceding_user.content or ""))
            if len(explicit_rows) >= 2:
                for entity_index, raw in enumerate(explicit_rows):
                    sku = str(raw.get("sku") or "").strip().upper()
                    if not sku:
                        continue
                    entity = dict(raw)
                    entity["sku"] = sku
                    entity.setdefault("name", raw.get("product_name_cn") or raw.get("product_name_en") or "")
                    entity["turn"] = message_turn_index
                    entity["role"] = "mentioned"
                    entity["source"] = "user_question"
                    records.append((entity["turn"], message_index, -1000 + entity_index, entity))
        for source in _safe_json(message.sources_json, []):
            if not isinstance(source, dict) or source.get("type") != "agent_context":
                continue
            source_turn_index = source.get("turn_index")
            turn_index = source_turn_index if isinstance(source_turn_index, int) else message_index
            entities = source.get("entities") or []
            if entities:
                for entity_index, raw in enumerate(entities):
                    if not isinstance(raw, dict):
                        continue
                    sku = str(raw.get("sku") or "").strip().upper()
                    if not sku:
                        continue
                    entity = dict(raw)
                    entity["sku"] = sku
                    entity.setdefault("name", "")
                    entity["turn"] = turn_index
                    records.append((turn_index, message_index, entity_index, entity))
                continue
            for entity_index, sku in enumerate(source.get("result_skus") or []):
                sku = str(sku or "").strip().upper()
                if not sku:
                    continue
                records.append((turn_index, message_index, entity_index, {"sku": sku, "name": "", "turn": turn_index, "role": "result", "source": "legacy_context"}))
        if message.sku:
            sku = str(message.sku).strip().upper()
            if sku:
                records.append((message_index, message_index, 0, {"sku": sku, "name": "", "turn": message_index, "role": "current", "source": "message_sku"}))
        primary_sku = _primary_sku_from_message_content(message.content)
        if primary_sku:
            records.append((message_index, message_index, 1, {"sku": primary_sku, "name": "", "turn": message_index, "role": "current", "source": "message_content"}))
    records.sort(key=lambda item: (-item[0], -item[1], item[2]))
    stack: list[dict] = []
    seen: set[str] = set()
    for _, _, _, entity in records:
        sku = str(entity.get("sku") or "").strip().upper()
        if not sku:
            continue
        if sku in seen:
            existing = next(item for item in stack if item.get("sku") == sku)
            existing_turn = existing.get("turn")
            new_turn = entity.get("turn")
            existing_source = str(existing.get("source") or "")
            new_source = str(entity.get("source") or "")
            low_priority_sources = {"message_sku", "message_content", "legacy_context", "user_question"}
            if (
                existing_source in low_priority_sources
                and new_source not in low_priority_sources
            ) or (
                isinstance(existing_turn, int)
                and isinstance(new_turn, int)
                and new_turn < existing_turn
                and new_source not in low_priority_sources
            ):
                existing.update(entity)
            if not existing.get("name") and entity.get("name"):
                existing["name"] = entity["name"]
            continue
        seen.add(sku)
        stack.append(entity)
        if len(stack) >= limit:
            return stack
    return stack


def _conversation_explicit_skus_in_mention_order(
    db: Session,
    conversation_id: str | None,
    user_id: str,
) -> list[str]:
    """Return exact SKU mentions in chronological user-turn order."""
    if not conversation_id:
        return []
    messages = (
        db.query(CustomerServiceMessage)
        .filter(
            CustomerServiceMessage.conversation_id == conversation_id,
            CustomerServiceMessage.role == "user",
        )
        .order_by(CustomerServiceMessage.created_at.asc(), CustomerServiceMessage.id.asc())
        .all()
    )
    ordered: list[str] = []
    seen: set[str] = set()
    for message in messages:
        text = str(message.content or "")
        for raw_sku in customer_agent_service._extract_skus(text):
            resolved_sku = str(_resolve_sku(db, text, raw_sku) or "").strip().upper()
            if resolved_sku and resolved_sku not in seen:
                seen.add(resolved_sku)
                ordered.append(resolved_sku)
    return ordered


def _question_entities_for_entity_stack(db: Session, question: str, limit: int = 20) -> list[dict]:
    text = customer_agent_service.normalize_search_text(question)
    if not text:
        return []
    exact_matches: list[tuple[int, int, str, dict]] = []
    matches: list[tuple[int, int, str, dict]] = []
    seen: set[str] = set()
    for product in db.query(Product).all():
        sku = str(product.sku or "").strip().upper()
        if not sku or sku in seen:
            continue
        sku_text = customer_agent_service.normalize_search_text(sku)
        name_cn = customer_agent_service.normalize_search_text(getattr(product, "product_name_cn", "") or "")
        name_en = customer_agent_service.normalize_search_text(getattr(product, "product_name_en", "") or "")
        exact_pos: int | None = None
        exact_len = 0
        for candidate in (sku_text, name_cn, name_en):
            if not candidate or candidate not in text:
                continue
            pos = text.index(candidate)
            if exact_pos is None or pos < exact_pos or (pos == exact_pos and len(candidate) > exact_len):
                exact_pos = pos
                exact_len = len(candidate)
        if exact_pos is not None:
            seen.add(sku)
            exact_matches.append((
                exact_pos,
                -exact_len,
                sku,
                {
                    "sku": sku,
                    "product_name_cn": getattr(product, "product_name_cn", None),
                    "product_name_en": getattr(product, "product_name_en", None),
                    "category": getattr(product, "category", None),
                },
            ))
            continue
        candidates: list[str] = []
        for raw_candidate in (
            name_cn,
            name_en,
        ):
            normalized = customer_agent_service.normalize_search_text(raw_candidate)
            if not normalized:
                continue
            candidates.append(normalized)
            stripped_people_prefix = re.sub(r"^[0-9一二两三四五六七八九十－\-]+人", "", normalized)
            if stripped_people_prefix and stripped_people_prefix != normalized:
                candidates.append(stripped_people_prefix)
        best_pos: int | None = None
        best_len = 0
        for candidate in candidates:
            if not candidate:
                continue
            if candidate in text:
                pos = text.index(candidate)
                if best_pos is None or pos < best_pos or (pos == best_pos and len(candidate) > best_len):
                    best_pos = pos
                    best_len = len(candidate)
                continue
            if len(candidate) >= 4:
                for prefix_len in range(len(candidate) - 1, 3, -1):
                    prefix = candidate[:prefix_len]
                    if prefix and prefix in text:
                        pos = text.index(prefix)
                        if best_pos is None or pos < best_pos or (pos == best_pos and prefix_len > best_len):
                            best_pos = pos
                            best_len = prefix_len
                        break
        if best_pos is None:
            continue
        seen.add(sku)
        matches.append((
            best_pos,
            -best_len,
            sku,
            {
                "sku": sku,
                "product_name_cn": getattr(product, "product_name_cn", None),
                "product_name_en": getattr(product, "product_name_en", None),
                "category": getattr(product, "category", None),
            },
        ))
    if exact_matches:
        exact_matches.sort(key=lambda item: (item[0], item[1], item[2]))
        if not re.search(r"(依次问|/|／|、|以及|还有|和)", text):
            return [item[3] for item in exact_matches[:limit]]
        exact_skus = {item[2] for item in exact_matches}
        for item in matches:
            if item[2] not in exact_skus:
                exact_matches.append(item)
        exact_matches.sort(key=lambda item: (item[0], item[1], item[2]))
        return [item[3] for item in exact_matches[:limit]]
    matches.sort(key=lambda item: (item[0], item[1], item[2]))
    return [item[3] for item in matches[:limit]]


def _should_use_previous_result_skus(question: str) -> bool:
    text = str(question or "")
    explicit_refs = (
        "这些", "这几个", "这几款", "刚才", "上面", "上一轮", "前面",
        "他", "他的", "它", "它的", "该产品", "它们", "他们", "哪个", "哪款", "哪种", "这款", "那款", "这个", "那个", "其中",
    )
    if any(item in text for item in explicit_refs):
        return True
    if len(text) <= 12 and any(item in text for item in ("容量", "材质", "卖点", "价格", "适合", "好不好")):
        return True
    return False


def _should_keep_preruntime_for_scoped_heat_source_followup(
    question: str,
    *,
    candidate_context: dict[str, Any] | None,
) -> bool:
    text = str(question or "")
    if "酒精炉" not in text and "alcohol stove" not in text.lower():
        return False
    if not any(term in text for term in ("里面", "这些", "这几款", "这些里", "哪些", "有哪", "支持", "能用", "适合")):
        return False
    return bool((candidate_context or {}).get("candidate_skus") or (candidate_context or {}).get("ordered_result_skus"))


def _refine_scoped_followup_previous_result_skus(
    db: Session,
    *,
    question: str,
    candidate_context: dict[str, Any] | None,
    previous_result_skus: list[str] | None,
) -> list[str]:
    normalized = [
        str(sku or "").strip().upper()
        for sku in (previous_result_skus or [])
        if str(sku or "").strip()
    ]
    keep_scoped_domain = _should_keep_preruntime_for_scoped_heat_source_followup(
        question,
        candidate_context=candidate_context,
    )
    if not normalized and keep_scoped_domain:
        # The general pre-runtime context reader intentionally requires a
        # pronoun for ordinary follow-ups.  A plural field filter over the
        # immediately preceding candidate set is itself an explicit scope, so
        # preserve that sealed domain instead of falling back to a global
        # search.  Candidate context is the only source used here.
        normalized = [
            str(sku or "").strip().upper()
            for sku in (
                (candidate_context or {}).get("ordered_result_skus")
                or (candidate_context or {}).get("candidate_skus")
                or []
            )
            if str(sku or "").strip()
        ]
    if not normalized:
        return []
    if not keep_scoped_domain:
        return normalized
    product_scope = str((candidate_context or {}).get("product_scope") or "").strip()
    if not _is_service_pot_cookware_scope(product_scope, str((candidate_context or {}).get("user_question") or "")):
        return normalized
    products = db.query(Product).filter(Product.sku.in_(normalized)).all()
    product_by_sku = {str(product.sku or "").strip().upper(): product for product in products}
    refined = [
        sku for sku in normalized
        if _is_service_pot_cookware_product(product_by_sku.get(sku))
    ]
    return refined or normalized


def _normalize_candidate_context_followup_result(
    agent_result: dict | None,
    db: Session,
    *,
    question: str,
    candidate_context: dict[str, Any] | None,
) -> dict | None:
    if not isinstance(agent_result, dict) or not isinstance(candidate_context, dict):
        return agent_result
    debug = dict(agent_result.get("debug") or {})
    if debug.get("agent_mode") == "candidate_context_followup":
        return agent_result
    intent_debug = debug.get("intent") if isinstance(debug.get("intent"), dict) else {}
    is_closed_domain_filter = str(intent_debug.get("source_context") or "") == "previous_results"
    if not _asks_for_alternative_recommendation(question) and not is_closed_domain_filter:
        return agent_result
    domain_skus = [
        str(sku or "").strip().upper()
        for sku in (
            candidate_context.get("ordered_result_skus")
            or candidate_context.get("candidate_skus")
            or candidate_context.get("parent_candidate_skus")
            or candidate_context.get("original_candidate_skus")
            or []
        )
        if str(sku or "").strip()
    ]
    result_skus = [
        str(item.get("sku") or "").strip().upper()
        for item in (agent_result.get("results") or [])
        if isinstance(item, dict) and str(item.get("sku") or "").strip()
    ]
    if not domain_skus:
        return agent_result
    if not result_skus:
        if str(intent_debug.get("source_context") or "") != "previous_results":
            return agent_result
        # An empty result is still a meaningful closed-world result.  Persist
        # its parent domain so later turns cannot silently reopen a broader
        # catalogue or reuse an older candidate set.
        debug["agent_mode"] = "candidate_context_followup"
        agent_result["debug"] = debug
        sources = list(agent_result.get("sources") or [])
        meta = next((item for item in sources if isinstance(item, dict) and item.get("type") == "agent_meta"), None)
        empty_context = {
            "candidate_skus": [],
            "ordered_result_skus": [],
            "filtered_skus": [],
            "original_candidate_skus": domain_skus,
            "parent_candidate_skus": domain_skus,
            "recommended_skus": [],
            "user_question": question,
            "product_scope": str(candidate_context.get("product_scope") or "").strip(),
            "empty_subset": True,
            "applied_filter": intent_debug.get("filters") if isinstance(intent_debug.get("filters"), dict) else None,
        }
        if isinstance(meta, dict):
            meta["candidate_context"] = empty_context
        else:
            sources.append({"type": "agent_meta", "candidate_context": empty_context})
        agent_result["sources"] = sources
        agent_result["result_skus"] = []
        agent_result["candidate_skus"] = []
        return agent_result
    if not set(result_skus).issubset(set(domain_skus)):
        return agent_result
    product_scope = str(candidate_context.get("product_scope") or "").strip()
    user_question = str(candidate_context.get("user_question") or "").strip()
    refined_domain = _refine_scoped_followup_previous_result_skus(
        db,
        question="里面哪些支持酒精炉",
        candidate_context=candidate_context,
        previous_result_skus=domain_skus,
    ) if _is_service_pot_cookware_scope(product_scope, user_question) else domain_skus
    if refined_domain and not set(result_skus).issubset(set(refined_domain)):
        return agent_result
    debug["agent_mode"] = "candidate_context_followup"
    agent_result["debug"] = debug
    sources = list(agent_result.get("sources") or [])
    meta = next((item for item in sources if isinstance(item, dict) and item.get("type") == "agent_meta"), None)
    scoped_context = {
        "candidate_skus": result_skus,
        "ordered_result_skus": result_skus,
        "filtered_skus": result_skus,
        "original_candidate_skus": [
            str(sku or "").strip().upper()
            for sku in (
                candidate_context.get("original_candidate_skus")
                or candidate_context.get("parent_candidate_skus")
                or domain_skus
            )
            if str(sku or "").strip()
        ],
        "parent_candidate_skus": [
            str(sku or "").strip().upper()
            for sku in (
                candidate_context.get("parent_candidate_skus")
                or candidate_context.get("original_candidate_skus")
                or domain_skus
            )
            if str(sku or "").strip()
        ],
        "recommended_skus": [
            str(sku or "").strip().upper()
            for sku in candidate_context.get("recommended_skus") or []
            if str(sku or "").strip()
        ],
        "user_question": question,
        "product_scope": product_scope,
        "empty_subset": False,
        "applied_filter": candidate_context.get("applied_filter") if isinstance(candidate_context.get("applied_filter"), dict) else None,
    }
    if isinstance(meta, dict):
        meta["candidate_context"] = scoped_context
    else:
        sources.append({"type": "agent_meta", "candidate_context": scoped_context})
    agent_result["sources"] = sources
    agent_result["result_skus"] = result_skus
    agent_result["candidate_skus"] = result_skus
    return agent_result


def _is_service_pot_cookware_scope(product_scope: str, user_question: str) -> bool:
    scope_text = str(product_scope or "")
    question_text = str(user_question or "")
    if any(term in question_text for term in ("水壶", "茶壶", "烧水壶", "壶类")):
        return False
    if any(term in question_text.lower() for term in ("煎盘", "烤盘", "煎烤盘", "griddle", "grill pan", "fry pan", "frying pan")):
        return False
    return any(term in scope_text or term in question_text for term in ("锅具", "锅", "套锅", "炊具", "单锅"))


def _is_service_pot_or_cookware_set_candidate(row: dict[str, Any], product: Product | None = None) -> bool:
    if not isinstance(row, dict):
        return False
    row_text = " ".join(
        str(row.get(key) or "")
        for key in (
            "sku",
            "product_name_cn",
            "product_name_en",
            "name",
            "title",
            "category",
            "sub_category",
            "capacity",
            "features",
            "usage_scenarios",
            "target_audience",
            "positioning",
            "semantic_match",
            "long_description_cn",
        )
    )
    product_text = ""
    if product is not None:
        product_text = " ".join(
            str(value or "")
            for value in (
                product.product_name_cn,
                product.product_name_en,
                product.category,
                product.sub_category,
            )
        )
    text = f"{row_text} {product_text}".lower()
    category = str(row.get("category") or "").strip()
    identity_text = " ".join(
        str(row.get(key) or "")
        for key in ("product_name_cn", "product_name_en", "name", "title")
    ).lower()
    if product is not None:
        identity_text = f"{identity_text} {str(product.product_name_cn or '')} {str(product.product_name_en or '')}".lower()
    if (
        any(term in identity_text for term in ("炉", "stove", "burner"))
        and not any(term in identity_text for term in ("单锅", "套锅", "锅具套装", "炊具套装", "cookware set", "pot set"))
    ):
        return False
    # A cookware set may legitimately list a frying/griddle component beside
    # multiple pots.  Treat that as a cookware set only when the canonical
    # category and structured capacity both establish the multi-pot set; a
    # standalone griddle remains outside the cookware candidate domain.
    multi_pot_set_signature = bool(
        category == "\u9505\u5177"
        and "\u5957\u88c5" in text
        and len(re.findall(r"\d+(?:\.\d+)?\s*(?:l|ml)\s*\u9505", text, flags=re.I)) >= 2
    )
    if _is_service_pan_or_griddle_candidate_text(text) and not multi_pot_set_signature:
        return False
    accessory_terms = (
        "防刮手夹",
        "锅夹",
        "手夹",
        "夹子",
        "收纳包",
        "收纳袋",
        "配件",
        "accessory",
    )
    if any(term in text for term in accessory_terms) and not any(term in text for term in ("套锅", "单锅", "锅具套装", "炊具套装")):
        return False
    if any(term in text for term in ("炉具", "炉子", "酒精炉", "气炉", "燃气炉", "stove", "burner")):
        if not multi_pot_set_signature and not any(
            term in text for term in ("套锅", "单锅", "锅具套装", "炊具套装", "cookware set", "pot set")
        ):
            return False
    strong_cookware_terms = (
        "单锅",
        "套锅",
        "炒锅",
        "汤锅",
        "锅具套装",
        "炊具套装",
        "炊具组合",
        "野餐锅",
        "野营锅",
        "小方锅",
        "cookware set",
        "cook set",
        "pot set",
    )
    water_container_terms = ("水壶", "茶壶", "烧水壶", "kettle", "bottle", "flask", "cup", "杯")
    product_identity_text = " ".join(
        str(value or "")
        for value in (
            row.get("product_name_cn"),
            row.get("product_name_en"),
            row.get("name"),
            row.get("title"),
            product.product_name_cn if product is not None else "",
            product.product_name_en if product is not None else "",
        )
    ).lower()
    non_category_cookware_text = " ".join(
        str(value or "")
        for value in (
            row.get("product_name_cn"),
            row.get("product_name_en"),
            row.get("name"),
            row.get("title"),
            row.get("features"),
            row.get("usage_scenarios"),
            row.get("target_audience"),
            row.get("positioning"),
            row.get("long_description_cn"),
            product.product_name_cn if product is not None else "",
            product.product_name_en if product is not None else "",
            product.sub_category if product is not None else "",
        )
    ).lower()
    if not multi_pot_set_signature and any(term in text for term in ("水壶", "茶壶", "烧水壶", "kettle", "bottle", "flask", "cup", "杯")) and not any(
        term in text for term in strong_cookware_terms
    ):
        return False
    if not multi_pot_set_signature and any(term in product_identity_text for term in water_container_terms) and not any(
        term in non_category_cookware_text for term in strong_cookware_terms
    ):
        return False
    has_positive_cookware_shape = _is_service_pot_or_cookware_set_text(text)
    if not multi_pot_set_signature and any(term in text for term in ("水壶", "茶壶", "烧水壶", "kettle", "bottle", "flask", "cup", "杯")) and not has_positive_cookware_shape:
        return False
    return multi_pot_set_signature or has_positive_cookware_shape


def _is_service_pan_or_griddle_candidate_text(text: str) -> bool:
    value = str(text or "").lower()
    positive_set_terms = (
        "套锅",
        "锅具套装",
        "炊具套装",
        "炊具组合",
        "野餐锅",
        "野营锅",
        "cookware set",
        "cook set",
        "pot set",
    )
    if any(term in value for term in positive_set_terms):
        return False
    excluded_terms = (
        "煎盘",
        "烤盘",
        "煎烤盘",
        "烧烤盘",
        "griddle",
        "grill pan",
        "grill plate",
        "fry pan",
        "frying pan",
        "pan plate",
    )
    return any(term in value for term in excluded_terms)


def _is_service_pot_or_cookware_set_text(text: str) -> bool:
    value = str(text or "").lower()
    allowed_terms = (
        "单锅",
        "套锅",
        "锅具",
        "炊具",
        "炊具套装",
        "锅具套装",
        "野餐锅",
        "野营锅",
        "cookware set",
        "cook set",
        "pot set",
        " pot",
    )
    if any(term in value for term in allowed_terms):
        return True
    return "锅" in value


def _is_service_pot_cookware_product(product: Product | None) -> bool:
    if product is None:
        return False
    name_text = " ".join(
        str(value or "")
        for value in (
            product.product_name_cn,
            product.product_name_en,
            product.category,
            product.sub_category,
        )
    )
    return _is_service_pot_or_cookware_set_candidate({}, product) or _is_service_pot_or_cookware_set_text(name_text)


def _should_use_conversation_history(question: str) -> bool:
    text = str(question or "")
    if _should_use_previous_result_skus(text):
        return True
    followup_starts = ("那", "如果", "那如果", "还有", "另外", "继续", "改成", "换成")
    if text.startswith(followup_starts) and len(text) <= 30:
        return True
    return False


def _should_defer_legacy_rule_result_to_runtime(question: str, agent_result: dict | None) -> bool:
    if not agent_result:
        return False
    # A legacy helper may return a non-empty shell without any public route,
    # entity, result, or action.  It is not a valid customer answer contract
    # and must not preempt the runtime/semantic executor for a stateless turn.
    if agent_result.get("intent") or agent_result.get("answer_type") or agent_result.get("sku"):
        return False
    if agent_result.get("results") or agent_result.get("actions"):
        return False
    return True


def _save_and_return_guidance(
    db: Session,
    user_id: str,
    question: str,
    conversation_id: str | None,
    *,
    unresolved_sku: str | None = None,
) -> dict:
    conversation = _get_or_create_conversation(db, user_id, question, None, conversation_id)
    normalized_unresolved_sku = str(unresolved_sku or "").strip().upper()
    answer = shape_answer_tone(
        (
            f"当前资料未找到 SKU {normalized_unresolved_sku} 对应的商品，暂时无法核对它是否满足你的需求。"
            "请检查 SKU 是否输入正确，或提供商品名称让我继续查找。"
            if normalized_unresolved_sku
            else "我还不能可靠回答这个问题，因为当前没有识别到明确的产品范围。请先输入 SKU，或者先让我查一批产品，再继续追问。"
        ),
        intent="clarify",
        answer_type="clarification",
    )
    agent_quality = customer_agent_quality_service.evaluate_agent_response(
        question,
        answer=answer,
        intent="clarify",
        results=[],
        sources=[{"type": "agent_clarification", "label": "需要核对商品标识" if normalized_unresolved_sku else "需要明确产品范围"}],
        actions=[],
        warnings=[],
        needs_clarification=True,
    )
    meta_sources = [{
        "type": "agent_meta",
        "label": "客服回复元数据",
        "intent": "clarify",
        "answer_type": "clarification",
        "confidence": "low",
        "uncertainty": "ambiguous_product",
        "needs_clarification": True,
        "anomalies": [],
        "suggested_followups": ["你可以直接给我 SKU，或者让我先列出某个类目的产品。"],
        "followups": ["你可以直接给我 SKU，或者让我先列出某个类目的产品。"],
        "warnings": [],
        "evidence": [],
        "agent_quality": agent_quality,
        "debug": {"intent": "clarify", "steps": [], "warnings": [], "anomalies": [], "raw_results": []},
        "feedback": None,
    }]
    db.add(CustomerServiceMessage(conversation_id=conversation.id, role="user", content=question))
    assistant_message = CustomerServiceMessage(
        conversation_id=conversation.id,
        role="assistant",
        content=answer,
        sources_json=json.dumps(meta_sources, ensure_ascii=False, default=str),
    )
    db.add(assistant_message)
    _touch_conversation(conversation)
    conversation_id_value = conversation.id
    message_id_value = assistant_message.id
    db.commit()
    release_session_connection(db)
    return {
        "conversation_id": conversation_id_value,
        "message_id": message_id_value,
        "intent": "clarify",
        "answer_type": "clarification",
        "confidence": "low",
        "uncertainty": "ambiguous_product",
        "needs_clarification": True,
        "anomalies": [],
        "suggested_followups": ["你可以直接给我 SKU，或者让我先列出某个类目的产品。"],
        "followups": ["你可以直接给我 SKU，或者让我先列出某个类目的产品。"],
        "warnings": [],
        "evidence": [],
        "agent_quality": agent_quality,
        "debug": {"intent": "clarify", "steps": [], "warnings": [], "anomalies": [], "raw_results": [], "agent_quality": agent_quality},
        "sku": None,
        "answer": answer,
        "sources": [],
        "actions": [],
        "results": [],
        "steps": [],
    }


def _make_title(question: str, sku: str | None) -> str:
    clean = re.sub(r"\s+", " ", question).strip()
    return clean[:20] or (sku or "客服会话")


def _stringify(value) -> str:
    if isinstance(value, (dict, list)):
        return json.dumps(value, ensure_ascii=False)
    return str(value)


def _safe_json(value: str | None, fallback):
    if not value:
        return fallback
    try:
        return json.loads(value)
    except json.JSONDecodeError:
        return fallback


def _should_use_previous_result_skus(question: str) -> bool:
    from . import customer_dialogue_state

    return customer_dialogue_state.should_use_previous_result_skus(question)


def _should_bypass_preruntime_for_runtime_direct_detail(
    db: Session,
    *,
    question: str,
    entity_stack: list[dict] | None,
    conversation_history: list[dict] | None,
) -> bool:
    text = str(question or "")
    if not _has_runtime_direct_detail_field_reference(text):
        return False
    if customer_agent_runtime_service._ordinal_skus_from_conversation_history(text, conversation_history or []):
        return True
    context_markers = ("刚才", "前面", "之前", "上次", "最开始", "最早", "第一个", "第一款", "最后一个", "最后那个")
    if any(term in text for term in context_markers) and any(
        str(item.get("sku") or "").strip()
        for item in (entity_stack or [])
        if isinstance(item, dict)
    ):
        return True
    if not any(term in text for term in context_markers):
        return False
    explicit_rows = _question_entities_for_entity_stack(db, text)
    explicit_skus = [
        str(item.get("sku") or "").strip().upper()
        for item in explicit_rows
        if isinstance(item, dict) and str(item.get("sku") or "").strip()
    ]
    if not explicit_skus:
        explicit_skus = [
            str(product.sku or "").strip().upper()
            for product in _products_named_in_question(db, text)
            if str(getattr(product, "sku", "") or "").strip()
        ]
    if len(explicit_skus) != 1:
        return False
    stack_skus = {
        str(item.get("sku") or "").strip().upper()
        for item in (entity_stack or [])
        if isinstance(item, dict) and str(item.get("sku") or "").strip()
    }
    return explicit_skus[0] in stack_skus


def _has_runtime_direct_detail_field_reference(question: str) -> bool:
    text = str(question or "")
    field_terms = (
        "材质",
        "材料",
        "表面处理",
        "表面工艺",
        "工艺",
        "功率",
        "热源",
        "燃料",
        "容量",
        "重量",
        "认证",
        "FDA",
        "LFGB",
        "手柄",
        "锅盖",
        "配件",
        "承重",
        "负重",
    )
    return any(term in text for term in field_terms)


def _is_category_reference_detail_question(question: str) -> bool:
    text = str(question or "")
    if not any(term in text for term in ("前面", "刚才", "之前", "上次")):
        return False
    if not any(term in text for term in ("酒精炉", "气炉", "炉", "套锅", "炒锅", "煎锅", "单锅", "锅", "杯套装", "杯", "水壶", "壶", "包")):
        return False
    return bool(customer_agent_intent_service.parse_intent(text, previous_result_skus=[]))


def _assistant_turn_index(db: Session, conversation_id: str) -> int:
    return db.query(CustomerServiceMessage).filter(
        CustomerServiceMessage.conversation_id == conversation_id,
        CustomerServiceMessage.role == "assistant",
    ).count()


def _primary_sku_from_message_content(content: str | None) -> str | None:
    text = str(content or "")
    if not text:
        return None
    if not any(word in text for word in ("首选", "优先推荐", "推荐", "建议")):
        return None
    match = SKU_RE.search(text)
    if not match:
        return None
    return match.group(0).upper()


